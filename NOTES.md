# NOTES

개발할 때 필요한 맥락을 모아둔다. 소스의 주석은 그 파일의 로직을 설명하고,
"무엇을 재봤고 무엇이 안 됐나" 는 여기 적는다.

두 구역이고 규칙이 다르다.

**열린 과제** — 아직 안 한 것 · 아직 모르는 것. 자유롭게 고치고 해결되면 지운다.

**측정 기록** — **덧붙이기만 한다. 지우지 않는다.**
틀린 것이 밝혀져도 지우지 말고 아래에 정정을 덧붙인다. 옛 기록과 정정이 나란히
있어야 "무엇이 언제 바뀌었나" 를 읽을 수 있다. 실제로 2026-08-15 기록이 남아
있었기 때문에 08-19 에 환경이 움직였다는 것을 알아냈다.
날짜 · 모델 · recipe 수 · 반복 횟수를 반드시 적는다. 조건 없는 숫자는 쓸모없다.

재는 방법은 `python tools/check_resolve.py --runs 3` (uvicorn 이 떠 있어야 함).
**10 이 아니라 3 이다** — 갈리는 것은 1회차와 2회차 이후 두 가지뿐이라
3회면 둘을 다 본다 (2026-08-26 「스물아홉째」 60발화 × 2모델 실측,
「서른째」에서 다시 확인). 갈린 회차가 1회차가 아닌 것이 나오면 되돌린다.

---

## 건드리기 전에 읽을 것

소스에는 값만 있고 이유는 여기 있다. 고치기 전에 본다.

| 어디 | 지금 값 | 왜 |
|---|---|---|
| `models.yaml` `default` | `qwen3:32b` | 8b 와 아홉 발화를 각 10회 재서 갈랐다 — 20/90 대 50/90. 갈린 것은 `want` 축 하나다 (아래 2026-08-22 (이어서) 참고). 더 작은 것으로도 못 내린다. qwen2.5:7b · qwen3:4b 는 발화 하나씩을 못 맞춘다 |
| `llm_engine/ollama.py` `options` | `repeat_penalty` 없음 | 올리면 반복은 멎지만 판정까지 깎인다. 반복은 아래 `maxLength` 로 막는다 |
| `models.yaml` `defaults.reason_max_length` | `200` | 없애면 모델이 recipe id 나열을 무한 반복해 202초에 끊긴다. 올려도 답은 그대로이고 시간만 는다 (200 · 400 · 600 -> 30 · 50 · 70초) |
| `workflows/static/prompts/recipe_selection.md` | reason 에 ID 금지 | 이 규칙이 루프와 판정을 함께 잡는다. 빼면 3번 발화가 0/10 |
| `workflows/static/menu/menu.yaml` | recipe 를 끝에 이어 붙임 | 대상별로 묶는 것을 재봤는데 모델에 따라 반대로 작용한다 |
| `workflows/static/prompts/recipe_selection.md` 축 목록 | id + 이름 + 설명 734자 (문맥이 오면 922자) | 채운 프롬프트가 7648자다 (문맥이 오면 7836자). 2026-08-28 「마흔여섯째」에 다시 쟀다 — 화면 시작 노드 둘이 붙어 730 -> 734 · 7133 -> 7648 이 됐고, 는 것의 대부분은 축 목록이 아니라 menu(3321 -> 4833자)다. 문맥을 안 보내면 given 은 셋 그대로다. `num_ctx` 는 32768(토큰) 이라 지금도 여유가 있다 — 옛 근거(8192 · 6758자)는 2026-08-21 측정 기록 쪽에 날짜와 함께 남아 있다. 선택지를 늘릴 때 다시 재는 것은 그대로다 |
| `ontology/registry.py` `MENU_BUDGET` | `6000` | 근거가 낡았다. `num_ctx` 8192 시절 값이고 지금은 32768 이다. 프로덕션에서 읽는 곳이 0 이고 테스트 하나가 보는 회귀 방지선이다 (`registry.py` 주석). 올릴 이유도 없다 — 진짜 제약은 컨텍스트 크기가 아니라 문장 변별력이다. menu 문장의 앞 30자가 23개나 같아서 shortlist 를 만들었다. 자수를 늘리면 오히려 나빠진다. 개편에서 잴 것은 자수가 아니라 "앞토막이 같은 문장 수" 다 |
| `ontology/registry.py` `MAX_STEPS` | `4` | 같은 성격이다. `registry.py` 주석이 스스로 "임시방편이다. 경로 길이가 문제가 아니라 말이 안 되는 조합이 섞이는 것이 문제" 라고 적고 있고, 그 조합 문제는 온톨로지 개편이 푼다. 개편에서 다시 본다 |
| `demo/graph_svg/dot.py` `NODE_ATTRS` fontsize · `layout_store.py` `SPREAD_X` · `NEATO_SPREAD_ATTRS` 의 `sep` | `32` · `5.0` · `+12` | **셋이 한 덩어리다. 하나만 고치면 노드가 겹친다.** 화면 글씨는 `fontsize x 맞춤배율` 이고 맞춤배율은 캔버스가 정한다 — 글씨를 키우면 상자가 커져 붙고, 늘리기를 줄이면 캔버스가 커져 글씨가 작아진다. 지금 값에서 가장 가까운 쌍이 24pt 떨어져 있고 칸의 폭과 높이를 100%·98% 쓴다. **고친 뒤에는 좌표를 다시 만들어야 한다** — `layout.json` 을 지우고 서버를 켜면 다시 계산되고, 그 결과를 `_init/layout.json` 에 복사해야 커밋된다. `tests/demo/graph_svg/test_nodes_do_not_overlap.py` 를 돌린다. 후보 표는 2026-08-29 「쉰셋째」 |
| `demo/graph_svg/layout_store.py` `DRAW_SCALE` | `1.0` | 그릴 때만 곱하는 배율이다. 지금은 1.0 이라 아무 일도 안 한다 — 배치가 처음부터 촘촘하게 놓이기 때문이다. **좌표를 다시 만들 수 없는 급한 자리에서만 임시로 쓴다.** 줄이면 간격만 줄고 노드 크기는 그대로라 원래 촘촘하던 곳이 먼저 붙는다 (0.64 에서 39쌍 · 0.4 에서 129쌍, 「쉰셋째」) |
| `demo/graph_svg/_init/layout.json` | 추적한다 | 사람이 눈으로 골라 확정한 배치다. `layout.json`(작업본)은 `.gitignore` 다. `.gitignore` 패턴에서 앞의 `/demo/graph_svg/` 를 빼면 **_init 사본까지 함께 무시된다.** `reset_to_init()` 이 이 사본으로 좌표를 되돌린다 |
| `ontology/shortlist.py` `candidates()` about | 두 단 (걸린 것 우선, 없으면 범용) | 범용 recipe 를 늘 통과시키면 "국회의원 선거구" 에 웹 검색과 VWorld 경계가 따라오고, 늘 빼면 대상 없는 발화에서 후보가 0개가 된다 |

모델마다 다른 값(`num_ctx` · `timeout` · `reason_max_length`)은 `models.yaml` 에 있다.
목록에 없는 모델은 `defaults` 로 돈다. 어디에 붙는가(`OLLAMA_HOST` · `BACKEND_URL`)는
기계마다 다르므로 환경변수로 두고 커밋하지 않는다 — `.env.example` 을 복사해 쓴다.

---

## 테스트를 어떻게 쓰는가

무엇을 지키는 테스트인지가 폴더로 갈린다.

```
ontology · orchestrator · llm_engine   제품 명세. 함수 이름이 요구사항 한 문장이고
                                       docstring 에 왜 그런지가 있다
demo/graph_svg                         배치 불변식. 눈이 못 보는 것만
```

**`demo/` 는 189개 중 126개를 지우고 63개를 남겼다.** 남긴 기준은 하나다 —
화면을 봐도 확인할 수 없는 것.

```
로직 오염   "말이 안 되는 경로가 등록됐다"   화면을 보면 안다    -> 지웠다
배치 오염   "노드가 3pt 움직였다"            화면을 봐도 모른다  -> 남겼다
```

3pt 는 안 보이고 30pt 면 이미 시연이 깨진 뒤다. 사람의 눈을 대신하는 테스트는
버리고 눈이 못 보는 것만 남긴다. 지운 126개는 회귀를 잡는 게 아니라 변경을
따라다니고 있었다 — 제품 코드는 한 번에 고쳐지는데 테스트만 네 번씩
실패-수정-재실행이 돌았고, 그 네 번이 전부 "요구사항이 바뀌어서" 였다.

남긴 배치 불변식은 몇 주에 걸쳐 실측으로 알아낸 것들이다. 어떤 강조 조합에서도
좌표가 같다 · 노드를 등록해도 기존 노드가 안 움직인다 · `inputscale=72` ·
핀이 있으면 `overlap` 금지 · 회전은 최초 배치에만. 지우면 다시 알아낼 방법이
없으므로 함부로 손대지 않는다.

**개수를 세어 적지 않는다.** `tests/README.md` 가 폴더별 개수와 합계를 적어뒀다가
조용히 틀렸다. 2026-08-20 에 재보니 여덟 군데가 전부 어긋나 있었다 (리뷰 대상
38 -> 41, `demo/` 58 -> 71, 전체 101 -> 112, `pytest` 가 세는 것 117 -> 128).
어긋나도 빨간불이 없어 아무도 모른다. 그래서 그 문서를 지우고 갱신이 필요 없는
것만 여기로 옮겼다. 개수가 궁금하면 `pytest` 가 언제든 센다.

---

## 열린 과제

- **~~보이는 범위가 안 먹는다~~ — 고쳤다 (2026-08-30 「예순째」).** 한 줄이었다.
  `("search_ev_stations", "map_extent")` 의 `input_first` 가 `{"bbox": [네 수]}`
  를 보내는데 `ev.searchStations` 에는 `bbox` 라는 칸이 없어서(평평한 넷만 받고
  `_parse_bbox_input` 을 안 부른다) 버려지고 전국을 찾았다. CCTV 줄과 같은
  평평한 넷으로 바꿨다.

  ```
  ★ 2026-08-30 화면 실측 (고친 뒤)
    "지금 보이는 곳 충전소 찾아줘"
      좁게(약 3.6km × 2.2km)  36건
      넓게(남한 거의 전부)    500건 (전체 89,194건)
      ★ 고치기 전에는 좁게 둬도 500건(전체 92,821건)이었다
  ```

  **함께 배운 것 — 「같은 꼴이니 같이 고장」을 원천으로 확인해야 한다.** 지도
  계열 다섯이 다 고장이라고 적혀 있었지만 나머지 넷(노선 · 행정구역 · VWorld ·
  인구)은 `bbox` 배열 칸을 정식으로 갖는다. 넷을 눌러 보니 원래부터 범위가
  먹고 있었다. 안 고쳤다. 스키마 확인 표와 실측 일곱은 측정 기록 「예순째」에 있다.

  남은 것 : `check_inputs` 의 「없는 칸」이 0 이 됐다. 51행에 빨간불이 없다.

- **끌 수 있는 동적 그래프** (하정목 박사님 피드백 ①, 2026-08-28). 지금은 파이썬이
  DOT 을 만들고 graphviz 가 SVG 를 그린다. 좌표가 고정이라 노드를 끌 수 없다.
  바꾸려면 브라우저에서 JS 라이브러리가 그려야 한다.

  ```
  걸리는 것
    새 의존성 — pip 패키지나 CDN 자바스크립트. 시연장 인터넷이 필요할 수 있다
    고른 경로 강조 규칙이 파이썬과 JS 두 곳으로 갈린다
    「등록해도 기존 노드가 안 움직인다」의 뜻이 흐려진다 (첫 배치만 고정하면 됨)

  같이 볼 것 (2026-08-29 「쉰두째」)
    배치가 H/W 0.85 인데 칸은 0.35 라 폭을 41% 밖에 못 쓴다. 좌표 배율로는
    못 편다 — 배치 모델(model=subset)이 만든 모양이다. 그리는 방식을 바꿀 때
    함께 푼다
  ```

  **2026-08-29 「쉰셋째」에 폭 사용률이 풀렸다.** 「폭 41% 는 배치 모델이 만든
  모양이라 좌표 배율로는 못 편다」는 맞았지만 배치를 다시 만드는 것으로는 풀렸다 —
  `model=subset` 을 버리고, 가로로 5배 늘린 뒤 겹침만 다시 없애니 100% 가 됐고
  화면 글씨가 4.84pt 에서 19.81pt 가 됐다. **확대 없이 노드 이름이 읽힌다.**
  그래서 이 항목의 동기는 「글씨가 안 보인다」가 아니라 「끌 수 있어야 한다」
  하나만 남았다. 급하지 않다.

  시연 뒤에 한다.

- **되묻기 뒤 고르기가 스위치 뒤에 있다** (2026-08-29 「쉰한째」). 기본은 켬이고
  `CLARIFY_CHOICE=0` 으로 끈다. **세션을 쓰는 유일한 자리다** — 메모리 · 5분 ·
  한 번 쓰면 지움 · 저장 안 함. 하정목 박사님 피드백(2026-08-28)으로 끌 수 있게만
  해 뒀다. **메모리 설계를 정하면 이 자리를 그 설계로 옮긴다.**

  ```
  ★ 끄면 정답표의 근접 서른셋 회가 막다른 길이 된다
     기본을 끔으로 바꿀 때는 그것을 감수하는 판단이 필요하다
  ```

- **★ 1순위 · menu 를 통째로 프롬프트에 넣는 방식이 도구 수에 비례해 무너진다**
  (2026-08-29 「마흔여덟째」). 59문장에서 이미 판정이 흔들렸다. 도구는 늘어난다.
  「마흔여덟째」가 넣은 갈래(발화가 화면을 가리키면 화면 recipe 만)는 **월요일
  시연을 위한 임시방편**이고 이 문제를 풀지 않는다 — 낱말 목록에 없는 말투는
  화면 recipe 를 아예 못 본다.

  ```
  찾아볼 길
    단락으로 나눠 읽는다        여섯 단락쯤. LLM 을 여러 번 부른다
    recipe 를 하나씩 읽는다      59회. 다 읽으므로 놓치지 않는다
    아직 못 찾은 다른 길

  걸리는 것
    나눠 읽으면 LLM 호출이 늘어 느리다 (59회면 30초쯤). 시연에 못 쓴다
    단락마다 "여기 없다" 를 정확히 말해야 하는데, 「좁혀놓고 고르기」가
    두 번 다 졌다 (「마흔한째」 좁히기 · 「마흔두째」 문안 여섯)
  ```

  **★ 임베딩 방식은 지금 보지 않는다 (2026-08-29 판단).** 아래에 그 안이 적힌
  옛 항목이 남아 있는데, 지우지 않고 둔다 — 나중에 다시 볼 때 근거가 필요하다.

- **★★ 재는 자를 먼저 의심한다 — 「67/93」이 오늘 다시 안 나온다**
  (2026-08-29 「마흔여덟째」). `101a155`(first-demo-passed)을 그대로 체크아웃해
  오늘 모델로 다시 재니 **53/93 · 빗나감 16** 이었다. 그 커밋의 기록은
  67/93 · 빗나감 6 이다. `a238b48` 쪽은 51 -> 49 로 잘 재현되므로 **재현이 안 되는
  것은 옛 쪽뿐이다.**

  ```
  101a155   67/93 · 빗나감  6 · 기준선 22/27    「마흔여섯째」가 옛 값으로 적은 것
  101a155   53/93 · 빗나감 16 · 기준선 20/27    ★ 2026-08-29 에 같은 커밋을 다시 잰 값
  ```

  **사이에 우분투 재부팅이 있었다. 왜 그런지는 안 팠다** — 모델 파일도 ollama
  설정도 그 작업 범위 밖이었다.

  ```
  ★ 따라오는 규칙
    「67 로 돌아가자」 같은 목표를 쓸 수 없다. 그 숫자가 오늘 없다
    NOTES 의 옛 숫자와 오늘 숫자를 직접 견주지 않는다
    ★ 같은 날 나란히 잰 것끼리만 견준다 — 번갈아 재기는 그대로 유효하다
      오늘 네 묶음 중 두 쌍이 한 글자도 안 달랐다
    판정을 인용할 때 잰 날짜를 함께 적는다
  ```

  오늘 이 기계에서는 두 묶음이 한 글자도 안 다르다(결정론적). 「마흔두째」의
  잡음 폭 ±5 도 오늘 값이 아니다. **옛 기록은 한 줄도 안 지운다** — 그때 그
  조건에서 나온 값이고, 지우면 무엇이 달라졌는지 볼 수가 없다.

- **★★★ 지금 branch 에 판정 회귀가 실려 있다 — 알고 커밋했다** (2026-08-29 「마흔일곱째」).
  문맥 없는 요청이 적중 67/93 -> 51/93 이다. 원인은 menu 가 40 -> 59 문장이 되어
  LLM 이 문맥도 없는데 못 쓸 열아홉 줄을 집는 것이다.
  **되돌아갈 자리 : 태그 `first-demo-passed` (101a155, 적중 67/93).**

  ```
  git reset --hard first-demo-passed
  ```

  **다음 작업(사람이 정함) : 발화가 화면을 가리키면 화면 recipe 열아홉만, 아니면
  기존 마흔만 보여준다.** 어느 쪽이든 menu 가 짧다. 화면과 도구가 같은 규칙을 쓰므로
  menu 가 갈라지지 않는다.

  **했다 (2026-08-29 「마흔여덟째」). 커밋은 안 했다.** 적중 49 -> 51(시연 조건) ·
  54(문맥 없음)이고 빗나감은 6 이다. **「67 근처」에는 못 갔는데, 같은 날 잰
  대조군이 그 「67」을 부정했다** — 위의 「재는 자를 먼저 의심한다」를 본다.
  menu 를 가른 몫은 +5 이고, 남은 차이는 menu 가 아니라 **축 선택지**에서 온다
  (문맥이 오면 `given` 이 셋에서 넷이 된다). 그 자리는 안 건드렸다.

- **★ 화면 문맥을 받고 판정이 16 내렸다 — 아직 안 고쳤다** (2026-08-28 「마흔여섯째」).
  문맥 없이 재는 적중이 67/93 -> 51/93 이다. 「마흔두째」의 잡음 폭 ±5 를 세 배 넘는다.
  **기능은 된다** (화면 실측 넷 중 셋이 뜻대로 돌았다). 내린 것은 **문맥을 안 보내는
  쪽**이고, 원인은 재서 하나로 좁혔다 — **menu 가 40 -> 59 문장이 됐고 LLM 이 문맥도
  없는데 화면 recipe 를 적어도 35/93 회 집는다.** `_without_dropped` 가 뒤에서 빼지만
  LLM 이 고를 자리를 이미 거기 썼다. 축 목록은 4자밖에 안 늘었다(730 -> 734).

  **손댈 자리 후보 셋.** 아직 아무것도 안 재봤다.

  ```
  가) menu 를 요청마다 거른다     문맥이 없으면 그 열아홉 줄을 빼고 프롬프트에 싣는다.
                                  지금은 menu 가 온톨로지 산물이라 요청마다 못 바꾼다 —
                                  load_menu 를 거치는 자리를 하나 만들어야 한다.
                                  ★ 제일 곧다. 「문맥이 없으면 없는 것처럼」과 같은 규칙을
                                  프롬프트까지 밀어 넣는 것뿐이다
  나) MIN_STEPS / 경로 생성을 좁힌다   덤으로 생긴 열일곱을 안 만든다. 대가는 온톨로지가
                                  아는 경로를 사람이 손으로 지우는 것이라 이 저장소의
                                  원칙(「경로 생성은 규칙이 정한다」)과 부딪힌다
  다) 좁히기 길(narrow)을 켠다     1차에 menu 를 안 넣는 길이다. menu 가 커진 것이
                                  원인이라면 이쪽이 안 흔들려야 한다. **재보면 원인
                                  진단이 맞는지도 함께 갈린다** — 재는 값이 두 배다
  ```

  **다는 쟀다** (2026-08-29 「마흔일곱째」). 적중은 51 -> 60(B) · 58(D) 로 돌아왔고,
  그래서 **원인 진단(「menu 가 길어진 것」)은 이 표가 받쳐 준다.** 그러나 빗나감이
  6 -> 20 · 17 로 세 배라 1순위에서 졌고, 미리 정한 대로 아무것도 고르지 않았다.
  기본값은 끔 그대로다. 가 · 나는 아직 안 재봤다.

- **★ 「찍은 지점」 recipe 아홉이 이제 재어진다** (2026-08-30 「쉰여섯째」).
  018 · 019 · 020 · 021 · 022 · 023 · 053 · 054 · 055 다.

  **여태 한 번도 안 재어졌다.** 두 겹으로 막혀 있었다.

  ```
  1  정답표 서른하나에 화면 발화가 하나도 없었다
  2  `check_resolve` 의 기본 문맥이 bbox 라 selectedLocation 이 안 갔다.
     그러면 resolve_service 가 그 아홉을 menu 에서도(_menu_for) 축
     선택지에서도(_choices_for) 빼서 후보에 오를 길이 없다
  ```

  둘 다 풀었다 — 기본 문맥을 `both` 로 바꾸고 정답표에 화면 다섯을 넣었다.
  **그중 셋이 찍은 지점이다** (32 · 34 · 36 → recipe 019 · 018 · 054).

  ```
  ★ 아홉 중 셋이 정답표에 들어왔다. 여섯은 아직 밖이다
    023 · 053        저쪽 데이터가 미적재라 반드시 0건이다 (dataset.available false)
    020 · 021 · 022  눌러 보면 셋넷으로 되묻는다 (3/3 CLARIFY, 2026-08-30).
                     "여기 선거구 알려줘" · "여기 국회의원 누구야" 로 재봤다
    055              인구 추이. 발화를 아직 안 찾아봤다
  ```

  **대가.** 「쉰다섯째」까지의 숫자는 전부 bbox 로 잰 것이라 옛 기록과 맞대려면
  `--context bbox` 를 적어야 한다. 「마흔여덟째」의 「옛 날짜의 숫자와 직접
  견주지 않는다」와 같은 자리라 새로 생긴 제약은 아니다.

  **아직 안 쟀다 — bbox↔both 가 기존 서른하나를 얼마나 흔드는가.** 「마흔여덟째」에
  문맥이 오면 `given` 선택지가 셋에서 넷이 되어 9 · 12 · 16 · 24 번이 흔들린
  기록이 있다. both 면 다섯이 된다. **네 묶음은 「전」과 「후」를 둘 다 both 로
  돌려 그 몫을 뺀 것이라 이 물음에 답하지 않는다.**

- **(옛 항목 · 2026-08-29 에 위로 옮겨 다시 씀) menu 를 통째로 넣는 방식이 도구
  수에 비례해 무너진다.** 59문장에서 이미 판정이 16점 떨어졌다고 적었는데,
  그 「16」의 위쪽 끝(67)이 오늘 다시 안 나온다 — 위 두 항목을 본다.
  **아래 임베딩 안은 지금 보지 않기로 했다(2026-08-29 판단).** 지우지 않고 둔다.

  ```
  해법 : 발화와 경로 설명을 벡터로 바꿔 가까운 여덟 개만 프롬프트에 넣는다
    ★ 줄이는 것을 수학이 한다. LLM 판단이 한 번뿐이다
      (좁히기는 두 번이었고 두 번 다 졌다)
    ★ 「정답이 상위 k 에 드는가」를 LLM 없이 잴 수 있다 — 잡음이 없다
    걸리는 것 : menu 문장이 틀이 같고 낱말 몇 개만 달라 안 갈릴 수 있다
    먼저 할 일 : 30분짜리 측정. 임베딩 모델 하나로 상위 k 적중률을 센다
  ```

- **노드를 더하면 좌표가 필요하다** (2026-08-28 시연). 온톨로지에 노드를 더하면
  `layout.json` 에 좌표가 없어 그래프가 안 그려진다 (`neato: ... has no position`).
  등록 화면을 거치면 붙지만 **파일을 직접 고치면 안 붙는다.**
  **노드를 더하는 작업의 관문에 「좌표가 붙었는가」를 넣는다.**

  **관문을 시험으로 박았다 (2026-08-29 「마흔아홉째」).**
  `tests/demo/graph_svg/test_layout_covers_every_node.py` 가 그리는 노드마다
  좌표가 있는지 센다. 좌표 파일은 gitignore 라 커밋으로는 못 막고, 노드를 더한
  사람의 장비에서 잡는 수밖에 없다. 붙이는 법은
  `demo/graph_svg/layout_store.ensure_positions` 를 한 번 돌리는 것이다
  (기존 노드는 0.0000pt 움직인다 — 실측).

- **채택한 인자 문안(P1)이 깨뜨린 자리 둘 — 판정에 안 걸린다** (2026-08-28 「마흔넷째」·
  「마흔다섯째」). `말한 키워드` 예시를 "청주 선거구 찾아줘" -> "청주" 로 바꾼 뒤에도
  판정 네 칸은 안 나빠졌다(빗나감 7 → 6). **둘 다 정답표 서른하나 밖의 변주라
  판정 표가 못 본다.** 그런데 사람이 실제로 하는 말투다.

  ```
  v11  전기차 충전소 알려줘      인자를 못 뽑아 전국 500건(상한)이 온다
                                ★ 0건보다 나쁘다. 답처럼 보이는데 엉뚱하다
  v12  철도 안전 문서 보여줘     recipe 가 013(문서 검색)에서 003(철도 노선)으로 간다
                                인자도 못 뽑는다. 6건이 55건이 된다
  ```

  **왜 그런지 안 팠다. reason 을 안 읽었다.** 「마흔셋째」가 「대상의 이름만 · 짧게」쪽으로
  민 문안이 「뽑을 것이 없다고 판단하는 쪽으로도 함께 민다」고 추정했지만 확인한 것이 아니다.
  v11 은 「마흔넷째」에서 P1 두 묶음 다 3/3 으로 깨졌다 — 드리프트가 아니다.
  v10(같은 낱말 · 어미만 「데이터 줘」)은 2/3 만 지켰다. **어미가 「알려줘」일 때 더 크게
  깨진다**는 것까지가 잰 사실이다. 위 「어미가 판정을 흔든다」와 같은 자리로 보인다.

  대가를 알고 채택했다 — 정답표 발화 넷(15 · 16 · 17 · 31)이 0건에서 실제 건수가 되는 것과
  맞바꿨다. **되돌리려면 이 둘을 근거로 든다.**

- **「전부 · 전체」 류 발화가 안 된다 — 도구에 `all` 이 있는데 배선이 안 보낸다**
  (2026-08-28 「마흔셋째」에서 눌러 봤다). `/api/tools` 42개 중 넷이 `all` 을 받는다.
  전부 `boolean · default false` 다.

  ```
  election.searchDistricts               전체 254개 선거구 반환 여부
  election.searchAssemblyDistricts       전체 254개 선거구 반환 여부
  election.searchAssemblyPledgeDistricts 최대 1000개까지 반환 여부
  election.searchLocalPledgeSummaries    전체 17개 시도 반환 여부
  ```

  눌러 본 값 (`asap-mcp-core`, 2026-08-28)

  ```
                                   searchDistricts   searchAssemblyDistricts
    all=true                            254건               254건
    query="전체 선거구"                    0건                 0건
    query="전국"                           0건                  1건
    아무것도 안 보냄                       20건                20건   ← 첫 쪽만
  ```

  **배선은 `{"query": @arg}` 한 칸뿐이다** (`step_service.plan` 으로 확인 —
  recipe_007 · 008 · 010 이 다 그렇다). `all` 을 보낼 자리가 배선에 아예 없고
  `demo/` · `orchestrator/` · `workflows/` 어디에도 `"all"` 이 안 나온다.
  그래서 "전체 선거구 보여줘" 류는 **어떤 인자를 뽑아도 254건에 못 닿는다** —
  잘해야 0건이고 인자를 못 뽑으면 첫 쪽 20건이다.

  뿌리는 **「인자 없는 경로를 못 만든다」**이고 무인자 도구 열하나와 같은 자리다.
  길이 셋 보인다. **어느 것도 지금 안 한다.**

  ```
  가) 배선에 자리를 하나 더 둔다     recipe 를 하나 더 파고 input 을 {"all": true} 로 고정
                                     → 인자 추출을 안 건드린다. recipe 수가 는다
  나) 인자를 둘 받는다               @arg 하나를 둘로 늘린다
                                     → 도달권(장소+분) · 경로(출발+도착) · 문서 지정 검색과
                                       같은 자리다. 「열린 과제」의 「인자 둘」과 함께 풀린다
  다) 인자 없는 경로를 허용한다      argument = null 을 오류가 아니라 「전부」로 읽는다
                                     ★ 위험하다. 지금 null 이면 ev.searchStations 가
                                       전국 500건(상한)을 주고 geo.getRailwayLines 는
                                       3683 features(14.7MB)를 준다 (「서른다섯째」물음 1)
  ```

- **발화 6 「국회의원 선거구 찾아줘」는 경로는 맞는데 어떤 인자로도 0건이다.**
  `recipe_007` 을 맞게 고르고(판정 적중) `election.searchDistricts` 를 부르는데,
  그 `query` 가 훑는 것은 선거구 **이름**("충북 청주서원" · "서울 강서갑")이라
  "국회의원" 도 "선거구" 도 이름의 일부가 아니다. 사람이 골랐을 값("선거구")도 0건이고
  확인용 "청주" 만 4건이다 (2026-08-24 · 2026-08-28 실측이 같다).

  「마흔셋째」에서 P1 · P2 · P3 셋 다 이 발화를 못 고쳤다 — P3 는 '국회의원' 까지
  줄였지만 여전히 0건이다. **인자를 어떻게 뽑아도 안 되는 발화라 인자 문안으로는
  못 고친다.**

  **기준선 아홉이라 지금 안 바꾼다** — 서른한 번의 측정 기록이 이 아홉으로 재어져 있고
  「서른두째」이후로 한 글자도 안 바꿔 온 자리다. **나중에 같은 범위를 덮으면서 말이
  되는 발화로 바꿀 것을 검토 대상으로 남긴다.** 후보는 이미 정답표 안에 있다 —
  31번 "청주 국회의원 선거구 검색해줘"(같은 recipe_007, 인자 "청주" 로 4건)가
  같은 경로를 덮으면서 실행까지 간다. 바꾸면 **아홉으로 잰 옛 표와 새 표가 안 이어진다**
  는 것이 대가다.

- **★ 16 번 「교통 공약 있는 선거구」는 인자·배선 문제다 — 도구는 할 수 있다**
  (2026-08-30 「쉰여섯째」). **성격이 바뀐 항목이다.** 옛말 "교통 공약 **많은**
  선거구 검색해줘" 는 「도구가 못 하는 일」이었다 —
  `election.searchAssemblyDistricts` 에 `order` 도 `sort` 도 없어 「많은 순」을
  시킬 수가 없다. 그래서 말투를 "**있는**" 으로 고쳤다 (기대값 `recipe_009` 는
  안 바꿨다).

  **고쳐도 실행은 여전히 0건이다.** 무엇이 막는지는 눌러서 갈랐다.

  ```
  인자                      count   totalMatches      (Gateway 직접 호출, 2026-08-30)
  query="교통 공약"            0            0     ← 지금 나오는 것
  query="교통"                20           45
  pledgeCategory="교통"       20           37
  hasPledges=true             20           77
  ```

  **도구는 그 일을 할 수 있다.** 막는 것이 둘이다.

  ```
  가  LLM 이 "교통 공약" 을 통째로 뽑는다 (3/3, --runs 3 실측).
      「찾을 대상의 이름」이 아니라 「하려는 일의 이름」이다 —
      위 「인자를 발화에서 통째로 옮겨 담는다」와 같은 성격이고,
      그 항목은 「스물다섯째」에 닫혔는데 이 자리가 다시 열었다
  나  배선이 query 한 칸만 보낸다.
      STEP_OF ("search_assembly_districts", "keyword") = {"query": @arg}
      pledgeCategory · hasPledges 를 보낼 자리가 배선에 아예 없다
  ```

  **위 「전부 · 전체」 항목과 같은 뿌리다** — 도구에 칸이 있는데 배선에 자리가
  없다. 거기 적힌 세 길(recipe 를 하나 더 판다 · 인자를 둘 받는다 · 인자 없는
  경로를 허용한다)이 그대로 여기에도 적용된다. **어느 것도 지금 안 한다.**

  **★ 「마흔다섯째」의 정정.** 거기에 "많은" 이 `query="교통"` 으로 20건이었다고
  적혀 있다. 2026-08-30 에 같은 발화를 다시 재니 `"교통 공약"` 이다.
  **옛 줄은 안 지운다** — 그때 그 조건의 값이고, 「재는 자를 먼저 의심한다」가
  말하는 자리다.

- **완전 대체의 격차 — 서버가 꺼진 다섯과 인사 하나가 남았다** (2026-08-29
  「마흔아홉째」에서 마지막으로 고침. 아래는 그 자리까지의 이력이다).
  저쪽 `plugins` 12 + `harnesses` 3 = 15 를 우리 배선과 맞췄다.
  **되는 일곱**: place-search · cctv-around-point · cctv-near-place ·
  rail-section-topdown · railway-route · knowledge-search · unsupported-request
  (우리는 `_no_recipe_answer`). **못 하는 여덟**:

  ```
  isochrone-around-point          r5-server 가 없다 (「서른아홉째」)
  isochrone-from-place            같다
  current-view-isochrone          같다 + 「현재 화면 범위」를 받는 입력이 우리에 없다
                                  (2026-08-28 : 뒤엣것은 풀렸다. r5-server 만 남는다)
  otp-plan-trip                   otp-router 가 없다
  transit-route-between-places    같다
  current-view-cctv               「현재 화면 범위」 입력이 없다
                                  (2026-08-28 : 풀렸다. 아래 「고침」)
  show-facility                   디지털 트윈 시설물 선택. 도구가 아니라 화면 명령이다
  system-chat                     일반 대화. 우리는 recipe 없으면 되묻거나 안내한다
  ```

  (unsupported-request 를 「되는」쪽에 둔 것은 내 셈이다. 그것을 빼면 아홉이다.)

  **고침 (2026-08-27, 대표님 회신 뒤).** r5 · otp 를 쓰는 다섯(도달권 셋 ·
  경로 탐색 둘)은 「우리가 안 한 것」이 아니라 **「서버가 꺼져 있어 지금
  아무도 못 하는 것」**이다. 저쪽 오케스트레이터도 지금 그 다섯이 안 된다.

  지금 남는 격차는 셋이었다

  ```
  current-view-cctv     도구는 살아 있다. 화면 문맥(지금 보는 지도 범위)만 있으면 된다
                        → 「찍은 지점」 노드 + context 받기. 이미 계획에 있다
  show-facility         오송 테스트트랙 시설물 선택
  system-chat / unsupported-request   잡담과 안내
  ```

  **`current-view-cctv` 를 지운다 (2026-08-28 「마흔여섯째」).** 시작 데이터 노드
  둘(`picked_point` 찍은 지점 · `visible_extent` 보이는 범위)을 온톨로지에 더하고
  `ChatRequest.context` 를 해석과 배선에 흘렸다. 저쪽 둘을 우리 recipe 둘이 받는다.

  ```
  current-view-cctv    → recipe_025  보이는 범위 → CCTV 조회
                         road.getCctv 에 $context.view 의 네 칸을 넣는다. 저쪽 plugin.md 와 같은 자리
  cctv-around-point    → recipe_018  찍은 지점 → CCTV 조회
                         $context.selectedLocation + 반경. 저쪽은 기본 1000m 이고 우리는 15000m 다
                         (우리가 이미 재본 값 `RADIUS_METERS` 를 쓴 것이다)
  ```

  **`show-facility` 를 지운다 (2026-08-29 「마흔아홉째」).** 온톨로지에 노드 둘
  (`show_facility` 시설물 표시 · `facility_view` 시설물 화면)을 더하고 배선표에
  **도구가 아닌 줄 하나**를 적었다. `TOOL_OF` 가 「노드 -> 서버 · 도구」에서
  「노드 -> 실행 수단」이 됐고, 실행 수단이 둘이다 — MCP 도구(마흔 줄)와 지도
  명령(한 줄)이다. 온톨로지는 그것을 모른다. 도구 이름을 온톨로지에 안 적기로
  한 규칙이 여기서 값을 했다.

  ```
  show-facility  → recipe_004  말한 장소 → 시설물 표시
                   {op:"digitalTwin.showFacility", args:{facilityName:"…"}} 하나를 낸다
                   MCP 도구를 하나도 안 부르고 vendor 실행기를 아예 안 지난다
                   ★ 대가 : 적중이 63/93 → 51/93 으로 12 내렸다. 빗나감은 9 → 9 다
                     남길지는 사람이 정한다 (「마흔아홉째」의 표)
  ```

  **`system-chat` · `unsupported-request` 는 반쯤 지운다.** NO_MATCH 한 자리가
  둘을 함께 받는다. 첫 줄은 그대로 두고 뒤에 안내 두 줄을 붙였는데 **그 낱말을
  온톨로지가 댄다** — 대상 넷과 시작 데이터 셋이다. 저쪽 unsupported-request 는
  고정 문구 한 줄이라 무엇을 대신 말해야 할지 안 알려주므로 이쪽이 낫다.
  **못 하는 것은 인사에 인사로 답하는 것이다** — 저쪽 system-chat 은 Gemini 가
  `message` 를 써서 답하고, 우리는 발화 해석이 쓴 `reason` 한 줄이 그 자리를
  맡는다("사용자가 '안녕'이라고 인사했을 뿐, 구체적인 요청이 없음"). 가르려면
  LLM 을 한 번 더 불러야 한다.

  **셋이 다 풀렸다. 남은 것은 서버가 꺼져 아무도 못 하는 다섯뿐이다** —
  도달권 셋(r5-server) · 경로 탐색 둘(otp-router)이고 월요일에 켜진다.

  ```
  ★ 다만 show-facility 는 우리가 명령을 내는 데까지다.
    저쪽 화면이 그것을 받아 쓰는지는 확인되지 않았다.
    사람이 저쪽 화면에서 눌러 보니 명령은 응답에 실려 나가는데 화면이 안
    바뀐다 — useChat 의 setIsOsongMonitoringMode · setPendingFacilityName 이
    밖에서 안 넘어오면 조용히 아무 일도 안 한다 (「마흔아홉째」 10절).
    우리가 내는 op 과 args.facilityName 은 저쪽이 기대하는 모양과 같다
  ```

  그리고 **시설물명 alias 를 안 베꼈다.** 저쪽 skill.md 의 「Facility Aliases」
  11줄이 "제3터널" 을 "시험 제3터널" 로 바꾸는데, 그것은 저쪽 설정이고 우리
  온톨로지에 둘 자리가 없다. 발화에서 온 값을 그대로 넘긴다.

  그리고 **도달권 셋 · 경로 탐색 둘은 그대로 서버가 꺼져 있다.** 그중
  `current-view-isochrone` 은 이제 「화면 범위를 받는 입력이 없다」가 아니다 —
  `visible_extent` 가 생겼으므로 r5-server 만 켜지면 된다. 격차의 까닭이 하나 줄었다.

  그리고 **「인자 둘」이 셋에서 걸린다.** `@arg` 는 하나뿐인데
  도달권(장소 + 분) · 경로 탐색(출발 + 도착) · 문서 지정 검색(문서 + 검색어)
  은 발화에서 둘을 뽑아야 한다. 「서른한째」에 설계만 남긴 것이 이제 셋이 됐다.
  **무게는 줄었다** — 도달권(장소+분)과 경로(출발+도착)가 서버와 함께 미뤄지고
  문서 지정 검색만 남는다. 서버가 켜지면 다시 필수가 된다.

- **★ 노드 하나를 더했더니 적중이 열둘 흩어졌다 — 축 문제다** (2026-08-29
  「마흔아홉째」). recipe 59 -> 60, `want` 축 선택지에 `facility_view` 한 줄
  (+86자)이 늘었을 뿐인데 적중이 63/93 -> 51/93 이 됐다.

  ```
  빗나감      9 = 9        안 늘었다
  못 붙음     0 -> 0
  잃은 열둘   전부 SELECT -> CLARIFY 흩어짐이다. 사람이 번호로 고르면 실행된다
              셋   발화 1 "오송역 위치 보여줘" 가 새 recipe_004 로 간다
              아홉 9 · 14 · 15 · 16 · 24 · 30. ★ 원인을 못 집었다
  ```

  **문안으로 고치려다 졌다.** `facility_view` 의 설명을 「시설물·모니터링·점검·
  손상·센서를 짚을 때만 쓴다」로 좁히니 발화 1 은 살아났는데(0/3 -> 3/3)
  무관한 다섯이 새로 깨져 **빗나감이 9 -> 14** 가 됐다. 1순위가 빗나감이라
  되돌렸다.

  ```
  ★ 알아낸 것 : 문안 한 문장을 고치면 정답표 서른한 자리가 통째로 다시 섞이고,
    판정이 결정적(temperature 0 · seed 0)이라 「잡음이었다」로 넘길 수도 없다.
    정답표를 보고 문안을 맞추는 것은 자를 답에 맞추는 것이다
  ★ 축 문제다. 이번 주에 두 번(「서른여섯째」 축 문안 · 「마흔두째」 좁히기
    프롬프트 문안) 막힌 자리와 같다. 문안을 흔드는 것으로는 안 풀린다
  ```

- **★ 축 선택지 한 줄이 발화를 흔든다** (2026-08-30 「쉰일곱째」). 위 항목의
  이어짐이다. **사례가 둘 쌓였다.**

  ```
  「마흔아홉째」 want 에 facility_view 한 줄 (+86자)   적중 63 → 51 · 열둘이 흩어짐
  「쉰일곱째」   given 에 picked_point 한 줄 (+89자)  다섯이 갈림 (합 -1회)
  ```

  **★ menu 가 아니다. 「쉰일곱째」에서 갈랐다** — 기존 서른하나가 보는 menu 는
  bbox·both 에서 3,406자로 한 글자도 안 다르고(화면 낱말이 32~36 에만 걸린다),
  흔든 것은 축 목록 249 → 338자(프롬프트 6,399 → 6,488자 · +1.4%)다.
  **후보가 늘어서도 아니다** — 1번에서 나온 {001,004,044}는 셋 다
  `spoken_place` 출발이라 `_without_dropped` 와 무관하다.

  **두 사례에서 같은 발화가 같은 방식으로 깨진다.** 1번 「오송역 위치 보여줘」가
  둘 다 `want` 를 `point` 에서 `facility_view` 로 바꿔 쓰고 3/3 → 0/3 이 된다.

  **★ 노드나 축 선택지를 더할 때마다 이 값을 치른다. 얼마인지 미리 알 수 없다.**

  **★ 이것은 측정만의 이야기가 아니다.**
  저쪽 화면에서 우클릭을 한 번 하면 `selectedLocation` 이 채워지고, 그 뒤의
  모든 발화가 both 조건으로 간다. 「쉰일곱째」에서 잰 다섯이 그때 흔들린다 —
  1(오송역 위치) · 8(철도 안전 문서) · 20(대전역 연령대별 인구)이 나빠지고
  6 · 15 가 좋아진다.

  **★ 시연에서는 우클릭이 필요한 발화를 맨 뒤에 둔다** (2026-08-30 리허설).
  고치려면 「값이 있는데 이 발화는 안 쓴다」를 가려야 하고, 그것은 축 문제다.

- **대표님께 물어볼 목록**

  ```
  r5-server · otp-router 재가동            월요일 회의 뒤. 도달권 셋 · 경로 탐색 둘이 여기 걸려 있다
  web-search 권한                          "MCP tool 'web-search/web.search' is not applied
                                           for this user." 우리가 못 연다
  ★ digitalTwin.showFacility 를 화면이     명령은 나가는데 화면이 안 바뀐다.
    받아 쓰려면 무엇이 필요한가            useChat 에 setIsOsongMonitoringMode ·
                                           setPendingFacilityName 이 넘어와야 한다
                                           (「마흔아홉째」 10절)
  ★ geo.geocode 가 행정구역 이름도         지금은 POI 검색(type="place" · size=1)이라
    찾게 할 수 있는가                      "대전시" 가 경상북도 청송군으로 간다.
                                           0건이 아니라 엉뚱한 답이 나온다
                                           (「쉰셋째」 2-12)
  ```

- **조회 후보가 정답을 놓친다 — 확장 표에서 13회다.** 축 셋(`given` · `want` ·
  `about`)으로 뽑은 후보에 기대값이 아예 안 들어오는 자리다. 발화
  12(1회) · 17(3) · 28(3) · 29(3) · 30(3) 이고 「서른두째」에서 처음 0 이
  아니게 됐다(아홉으로 재면 여전히 0 이다).

  **「서른셋째」는 어긋났을 때의 처신만 정했다. 원인은 그대로다.** 겹치는 것이
  0개일 때 둘을 합쳐 되묻게 바꿔 확신하고 틀리는 것을 13회에서 4회로 줄였지만,
  **축이 정답을 못 집는 것 자체는 안 고쳤다.** 그래서 28 · 29 · 30 은 적중이
  아니라 근접이고 후보가 셋넷으로 늘었다. 되묻기가 는 것이 그 대가다.

  자리마다 원인이 다르게 보인다 — 28 은 "경부선" 을 「말한 키워드」로 읽어
  철도 경로(`002` · `003` 은 「말한 장소」로 시작한다)가 통째로 빠지고,
  17 은 반대로 "청주" 를 「말한 장소」로 읽어 키워드 길이 빠진다.
  **같은 낱말을 장소로 읽느냐 키워드로 읽느냐가 후보 집합을 통째로 가른다.**

  **12 「청주시 행정경계 보여줘」는 인자 문제가 아니라 recipe/축 문제다** — 갈렸다
  (2026-08-28 「마흔넷째」·「마흔다섯째」). 「서른다섯째」물음 3 이 이 발화를 0건 원인 넷
  중 「대상 종류 낱말을 넣는다」(인자가 "행정경계" 로 나온다)로 적어 두었는데,
  **네 묶음을 번갈아 재니 인자가 넷 다 "청주시" 로 안 흔들렸다.** 흔들린 것은 recipe 다
  (P0a 만 `004` · 나머지 셋은 `019` 단건). **인자 문안(P1)은 여기에 아무 힘도 못 쓴다** —
  판정도 P0 · P1 넷 다 빗나감 3/3 으로 같다(`{019}` 를 확신하고 SELECT 한다).
  기대값은 `004`(키워드 → 행정구역 조회, 4건)이고 `019` 는 단건이다.
  **고칠 자리는 축이거나 menu 문장이다. 인자 쪽을 더 손봐도 안 움직인다.**

  **다음 작업이다.** 고치면 「서른셋째」의 세 안이 거의 같아지므로 그때 다시
  재서 제일 단순한 것으로 되돌릴지 정한다.

  **좁히기 길(「마흔한째」)의 폴백이 이 자리를 얼마나 건졌나.** 28 · 29 · 30 은
  1차 축으로 조회 후보가 0 이라 폴백 가가 3회씩 돌았고 셋 다 끔과 같은 결과가
  나왔다(건졌다기보다 지금 길로 되돌아간 것이다). 12 · 17 은 후보가 3~4개인데
  정답이 없어 폴백이 안 돌고 그대로 빗나감 · 근접이 됐다. 새로 빠진 19 는
  1차가 want 를 item_list 로 써서 011 이 후보 밖이 됐고 2차가 "없다" 고 안 하고
  013 을 골랐다 — **폴백 나는 정답이 후보에 없는 자리에서 한 번도 안 돌았다.**

- **좁히기를 스위치 뒤에 둔 채로 접었다** (2026-08-27 「마흔두째」, 사람이 정했다).
  `resolve_service.resolve` 가 스위치이고 `_resolve_full`(지금 길) 과 `_resolve_narrow`
  (좁히기 길)가 나란히 있다. **기본은 끔이고 그대로 둔다.**

  「마흔한째」에서 켬이 졌고(빗나감 6 → 9), 「마흔두째」에서 2차 · 1차 문안 여섯을
  번갈아 쟀는데 **전부 F(끔, 빗나감 6)보다 나빴다.** 1순위에서 전멸이라 좁히기를
  살릴 근거가 없다.

  **지우지도 않는다.** 기본이 끔이라 지금 동작에 영향이 0 이고, 길과 성적표가 남아
  있으면 나중에 다시 볼 수 있다. 지우려면 기본을 켬으로 바꾸는 것이 먼저인데 그
  조건이 안 섰다.

  다시 열 조건
  ```
  16 이 008 과 009 를 가르게 되면 — 지금은 어떤 문안으로도 안 갈린다.
     문안이 아니라 두 노드의 description 문제로 보인다
  측정 잡음이 줄면 — 지금 폭이 빗나감 4 · 적중 5 라 미세한 차이를 못 읽는다
  1차의 축 정확도가 오르면 — 조회 빠짐이 줄어야 좁히기의 전제가 선다
  ```

  **건진 것 하나.** 1차에 「통계」 단서를 주면 19 가 고쳐진다(want=statistics → 후보
  011 하나). 다만 같은 문안이 4 · 26 · 27 을 깨뜨렸다(「내용」→ item_detail). 단서를
  주는 방향은 값이 있고 어떻게 주느냐가 문제다 — **이것은 좁히기를 접어도 남는
  과제다.** 지금 길의 축 고르기에도 같은 문제가 있다.

  좁히기 길의 폴백이 「조회 후보가 정답을 놓친다」를 얼마나 건졌는지는 「마흔한째」
  기록에 있다 — 28 · 29 · 30 은 폴백 가로 지금 길에 되돌아갔고, 정답이 후보에 없는
  자리에서 폴백 나는 한 번도 안 돌았다.

- **문서를 지정하는 검색을 하려면 인자가 둘 필요하다.** `knowledge.query` 는
  `filter_docs`(문서 이름 배열)를 받는데 배선의 발화 값 자리는 `@arg`
  하나이고, 발화 해석 LLM 도 `argument` 하나만 돌려준다. "시행규칙에서 교육
  내용 찾아줘" 를 문서 지정으로 풀려면 발화 해석이 「문서 이름」과 「검색어」를
  따로 내놓아야 한다 — 응답 스키마 · 프롬프트 · `@arg` 치환까지 건드리는 큰
  변경이다.

  **지금은 급하지 않다** (2026-08-26 「서른한째」 1단계 실측). 의미 검색이라
  검색어에 문서 이름이 섞이면 그 문서 조각이 위로 온다 — "철도안전법 시행규칙
  교육" k=4 에서 시행규칙 4/4건, "철도안전법" k=4 에서 법 4/4건. 문서가
  둘이라 이름이 서로를 포함하는 지금도 갈린다. **문서가 늘어 이름이 안 갈리기
  시작하면 그때 연다.** 그때 `filter_docs` 를 붙일 자리는
  `("search_documents", "keyword")` 배선 한 줄이다.

- **저쪽 화면 따라 보기는 한 사람이 쓰는 것을 전제한다.** 시연용 연동이다
  (2026-08-27 「서른여덟째」). 회차를 서버 프로세스 메모리 한 벌에 쌓고
  `GET /recent` 는 그것을 통째로 준다 — **세션을 안 가른다.** 두 사람이 저쪽
  화면을 동시에 쓰면 회차가 한 줄에 섞이고, Streamlit 은 그중 마지막 하나를
  그리므로 남의 발화를 자기 것처럼 따라 그린다.

  가르려면 회차에 `sessionId` 를 함께 남기고 `/recent` 가 그것으로 걸러야
  하는데, 그러면 **Streamlit 이 어느 세션을 볼지 골라야 한다** — 저쪽 화면이
  세션 id 를 만들고 우리는 그 값을 사람에게 보여줄 자리가 없다. 화면에
  세션 고르기를 두는 일이고 시연 크기를 넘는다.

  **워커를 늘려서도 안 된다.** 회차가 프로세스 메모리라 uvicorn 워커가 둘이면
  회차가 반씩 갈린다. 지금 `--reload` 단일 워커가 그것을 지키고 있다.

  **통합 때 다시 본다.** 실제 서비스가 소켓·MCP 로 바뀌면 이 연동은 통째로
  사라지는 자리다.

- **폐기 예정 도구를 온톨로지가 대등하게 둔다.** `ev.searchChargers` 하나를
  뺐다 (2026-08-26 「스물일곱째」). 저쪽 도구 설명에 "기존 호출 호환용 …
  ev.searchStations 사용 권장" 이라고 적혀 있는데 온톨로지는 그것을
  `ev.searchStations` 와 똑같은 무게로 두고 있었다. 축 셋이 글자까지 같아
  조회로 못 갈리고, "옛 호출 호환용으로 보여줘" 라고 말하는 사람이 없으니
  발화로도 못 갈린다. 그래서 전기차 후보가 늘 두 배였다.

  **다른 도구에는 같은 것이 없다** (2026-08-26 「스물여덟째」). catalog 42개의
  설명을 "호환" · "권장" · "deprecated" · "폐기" · "대신" 으로 훑어 걸리는
  것이 `ev.searchChargers` 하나뿐임을 확인했다. 후보가 두 배인 자리는 그
  하나가 전부였다.

  판별하는 자리는 이미 있다 — `tools/check_wiring.py` 가 아니라
  `ontology/shortlist.py` 쪽이다. 축 셋이 같은 recipe 짝을 세는 검사를
  두면 사람이 catalog 를 훑기 전에 온톨로지 안에서 먼저 걸린다. 그것도 안 했다.

- **LLM 이 고를 범위를 온톨로지가 먼저 좁히는 안 — 만들어서 재봤고 접었다.**
  프롬프트에 menu 를 통째로 넣는 대신 온톨로지가 축 셋으로 먼저 좁히고 LLM 은 그중에서
  고르는 안이다. 노린 것은 프롬프트 길이 문제가 사라지는 것 · 도구를 늘리는 값이
  싸지는 것 · "온톨로지가 후보를 좁힌다" 를 화면에서 숫자로 보이는 것(45 → 2~5) 이었다.

  **만들었고(「마흔한째」) 문안을 여섯 벌 재봤고(「마흔두째」) 접었다.** 위의
  「좁히기를 스위치 뒤에 둔 채로 접었다」 항목에 결론과 다시 열 조건이 있다.
  걱정하던 위험이 그대로 나왔다 — **축이 틀리면 정답이 후보에서 아예 빠지고,
  되물음이 아니라 확신하고 틀리는 것이 된다.** 빗나감 6 → 9 가 그것이다.

  아홉 발화로 재던 시절의 「조회 빠짐 0」(「스물여섯째」 · 「스물일곱째」)은 발화를
  스물둘로 늘리자 무너졌다(「서른두째」 뒤로 9~13회). 그 숫자가 좁히기의 전제였다.

- **프롬프트에 예시를 더하면 「하나로 확신」이 는다** (2026-08-27 「마흔두째」).
  2차 프롬프트에 NO_MATCH 예시를 하나 더한 변형 셋(N1 · N3 · N4)에서 **11(오송역
  행정경계) 과 23(경부선 주변 CCTV) 이 세 번 다 같은 모양으로 깨졌다** — 되묻던
  자리가 하나를 SELECT 로 확신하고 틀리는 자리가 됐다. 「맞는 것이 없으면 없다고
  해라」를 가르치려던 예시가 「하나만 남겨라」로도 읽힌 것으로 보인다.

  **2차 reason 을 안 읽었으므로 추정이다.** 확인하려면 그 세 묶음의 reason 을 발화별로
  읽어야 한다. **인자 문안이나 다른 프롬프트에 예시를 더할 때 같은 일이 날 수 있어
  적어 둔다** — 예시를 더하면 빗나감이 어떻게 되는지를 함께 재라는 뜻이다.

- **측정 잡음의 폭 — 같은 세션 안에서도 빗나감 4 · 적중 5 다** (2026-08-27 「마흔두째」).
  같은 문안(N0)을 열두 묶음 사이에 여섯 번 끼워 쟀는데 **빗나감 9 · 13 · 11 · 11 · 13 · 13,
  적중 61 · 59 · 56 · 59 · 59 · 59** 가 나왔다. temperature 0 · seed 0 · 같은 세션 ·
  겹쳐 돌린 것 없음이다. 제일 크게 움직인 자리는 21(경부선 선형 데이터)로, 1차 want 가
  N0a 뒤로 map_extent → boundary_shape 로 바뀌어 그것만으로 빗나감이 3 늘었다.

  **이 폭보다 작은 차이는 변형의 효과로 읽지 않는다.** 앞선 폭 기록과 이어 읽을 것 —
  「서른일곱째」가 세션 간 폭을 적중 ±3 · 빗나감 ±6 · 발화 7~11개로 쟀고, 「마흔한째」는
  같은 세션 안에서 발화 0~1개였다. **「마흔두째」에서 세션 안 폭이 그보다 훨씬 커졌고
  원인을 못 찾았다.** Ollama 쪽 KV · 프리픽스 캐시가 의심 가는 자리인데 잰 것이 없다.
  잡음이 이만큼이면 문안 미세 조정은 가릴 수 없다.

- **★ 측정 기준선이 날짜를 건너 안 이어진다** (2026-08-29 「마흔여덟째」).
  위의 잡음 폭은 **같은 날 안에서**의 이야기다. 날짜를 건너면 그 폭으로도 안 덮인다.
  `101a155` 을 그대로 체크아웃해 다시 재니 67/93 · 빗나감 6 이 **53/93 · 빗나감 16**
  이었다. 사이에 우분투 재부팅이 있었다. 원인은 안 팠다.

  ```
  ★ 같은 날 나란히 잰 것끼리만 견준다. 번갈아 재기는 그대로 유효하다
    옛 숫자와 오늘 숫자를 직접 견주지 않는다
    판정을 인용할 때 잰 날짜를 함께 적는다
  ```

  **옛 기록은 한 줄도 안 지운다.** 위의 「2026-08-15 측정이 재현되지 않는다」와
  같은 자리로 보인다 — 그때도 저장소 안에서 원인을 못 찾았다.

- **어휘 계층이 없어 이름이 안 걸린다 — 사례를 모은다** (2026-08-29 화면 실측).

  ```
  "대전시 인구 구성" -> 되묻기 -> 「인구 통계 조회」를 고르면
    population.searchStatistics query="대전시"  0건
  ```

  **데이터의 이름이 "대전광역시" 라 "대전시" 로는 안 걸린다.** "청주시" 가 되는
  것은 데이터 이름이 "청주시 흥덕구" 여서지 규칙이 있어서가 아니다.
  **★ "충북 제1선거구" -> DB 는 "충북 청주서원" 과 같은 병이다.**

  좌표를 거치는 길(말한 장소 -> 좌표 -> 행정구역 판별 -> 연령별 인구 구성)을
  고르면 이름이 안 걸려도 된다. **되묻기에서 어느 쪽을 고르느냐로 0건이 갈린다.**

  **★ 그런데 그 우회로도 안 된다 — 0건이 아니라 엉뚱한 답이 온다**
  (2026-08-29 화면 실측, 「쉰셋째」 2-12).

  ```
  "대전시 인구 구성" -> 되묻기 -> 「연령별 인구 구성 조회」를 고르면
    geo.geocode "대전시"  ->  ★ 경상북도 청송군 주왕산면 상의리 202-3
    findBoundaryByPoint   ->  경상북도 3건
    getAgeProfile         ->  청송군 24,020명
  ```

  저쪽 `geo.geocode` 는 VWorld 를 `type="place"` · `size=1` 로 부르고
  `items[0]` 을 쓴다(「스물다섯째」에서 코드로 확인). **장소(POI) 검색이라
  행정구역 이름은 부분 일치로 엉뚱한 곳이 첫 번째로 걸린다.**

  ```
  이름 길   query="대전시"   0건            안 나온다
  좌표 길   geocode "대전시"  청송군 24,020명  ★ 나오는데 다른 도의 군이다
  ```

  **0건보다 나쁘다.** 답처럼 보이는데 틀렸고, 화면만 봐서는 알 수 없다.
  같은 병이 앞서 둘 있었다 — `ev.searchStations "전기차 충전소"` 120건 ·
  일출랜드, `geo.getRailwayLines stationName="경부선"` 전국 500건.
  **셋 다 "0건보다 나쁘다" 는 같은 모양이다.**

  시연에서는 "대전시" 같은 행정구역 이름을 안 쓴다. "대전역" 은 실제 POI 라
  제대로 걸린다(대덕구 164,047명 · 정답표 20번).
  **우리가 못 고친다 — 저쪽 코드다.** 대표님께 물어볼 목록에 올렸다.

- **재는 도구가 조용히 죽는다.** `tools/check_argument.py` 가 `4dd552a`
  (2026-08-24 18:46, `_call_resolve` 의 반환값을 넷에서 다섯으로 늘림) 뒤로
  2026-08-25 까지 `ValueError` 로 실행 불능이었다 — 만들어진 `2b25290`
  (2026-08-24 02:26) 이래 한 번도 안 고쳐졌으므로 그 사이가 통째로 고장
  구간이다. **「열셋째」의 측정은 고장 전이라 유효하다**(그 측정은 `2b25290`
  직후이고 `4dd552a` 앞이다 — 커밋 시각으로 확인했다).
  자주 돌리는 `check_wiring` · `check_resolve` 와 달리 가끔 돌리는 것이라
  몰랐다. 표가 오류로만 차는 것이 아니라 점 대신 `!` 가 찍힐 뿐이라
  화면으로도 안 두드러진다.
  **서버 없이 되는 최소 검사 하나면 막혔다** — 언팩 개수가 맞는지는 서버를
  안 켜고도 확인된다. 다만 `tools/` 는 "테스트를 두지 않는다" 가 규칙이라
  넣을지는 사람이 정한다. **이번에 안 넣었다.**

  **★ 세 번째가 났다 — `check_resolve` (2026-08-31 「예순셋째」). 이제 넣는다.**

  ```
  check_argument   나흘   ValueError   (「스물다섯째」)
  check_inputs     하루   KeyError     (「쉰째」 → 「쉰아홉째」에서 살림)
  check_resolve    ★ 이번  KeyError    (--only 로 화면 발화만 돌릴 때)
  ```

  세 번 다 **판정은 다 해 놓고 표를 찍는 마지막에** 죽었다. 앞의 둘은 배선표가
  바뀌어 도구가 못 따라간 것이고, 이번은 발화가 늘어(화면 다섯) 묶음이 셋이
  됐는데 묶음 이름을 고르는 자리가 두 갈래에 머문 것이다. `_groups()` 는 빈
  묶음을 빼므로 화면만 돌리면 확장 칸이 아예 없는데 거기를 찾아 `KeyError` 가
  났다. 같은 두 갈래가 세 자리에 있었다 (`_print_candidates` 하나만 고쳤으면
  바로 다음 표에서 또 죽었다).

  `_groups` 와 경계를 공유하는 `_group_label()` 하나로 셋을 묶어 고쳤다.
  **적중 표는 옛/새 코드에서 한 글자도 안 다르다** — 같은 자료를 두 사본에
  넣어 확인했다. 달라진 것은 화면 다섯이 확장에 섞여 세어지던 합계 줄 둘이
  제 칸으로 갈린 것뿐이다 (27 = 22 + 5).

  **「넣을지는 사람이 정한다」에 답이 났다 — 넣었다.** 「쉰아홉째」가
  `check_inputs` 에 선례를 만들었고, 그 선례를 따라 `check_resolve._selfcheck()`
  와 `tests/tools/test_check_resolve_selfcheck.py` 를 뒀다. 진짜 `UTTERANCES`
  를 묶음 조합 일곱 가지로 잘라 표 여섯을 다 찍어 본다. 서버를 안 부르고
  0.3초에 끝난다. 옛 두 갈래로 되돌리면 죽는 음성 대조군도 함께 뒀다.

  남은 것 : `check_wiring` 에는 자체 검사가 없다. 아직 죽은 적이 없어 뒀다.

- **~~`/chat` 과 `/chat/stream` 이 갈려도 아무도 안 잡는다~~ — 지키는 시험을
  뒀다 (2026-08-31 「예순셋째」).** 저쪽 화면은 `POST /chat/stream` 을 쓰고
  시연에서 도는 길이 이쪽인데, `check_resolve --execute` 는 `POST /chat` 을
  쓴다. 둘은 같은 흐름(`_chat_events`)을 쓰므로 지금은 같은 답이 나오고
  `main.py` 주석도 「두 경로가 다른 답을 하면 화면과 curl 중 무엇을 믿을지가
  갈린다」라고 적어뒀다. **그런데 경고만 있고 `tests/` 에 `chat/stream` 을
  건드리는 시험이 0건이었다.** 누가 한쪽만 고쳐도 안 잡혔다.

  `tests/demo/api/test_chat_stream_parity.py` 여섯 줄로 메웠다 — answer 와
  commands 가 두 길에서 같은지, `/chat` 이 중간 이벤트를 버리는 의도된 차이,
  SSE 틀(`data: ` · 빈 줄 · `[DONE]`), 한글이 이스케이프 없이 나가는지
  (`ensure_ascii` 제약). `test_recent.py` 의 이벤트 대역 관례를 그대로 쓰고
  `main._chat_events` 한 자리만 바꾼다 — 창구 둘의 진짜 코드는 돌고 LLM ·
  온톨로지 · vendor 는 안 돈다.

  **진짜 서버를 띄워 부르지 않는다.** 시연 중 pytest 가 8000 과 부딪히고,
  LLM 이 회차마다 다른 답을 내면 시험이 답의 내용에 흔들리고, 서버가 안 떠
  있을 때 나는 빨간불은 코드가 깨진 것이 아니기 때문이다. 볼 것은
  「두 길이 같은가」지 「답이 맞는가」가 아니다.

  남은 것 : 두 길이 **정말로** 갈리는 날은 `_chat_events` 를 안 거치는 변경이
  들어올 때다. 그때는 이 시험이 대역을 물리는 자리 자체가 사라지므로 시험을
  다시 봐야 한다. 지금 구조에서는 이것이 맞는 자리다.
- **어미가 판정을 흔든다** (2026-08-25 「스물다섯째」, 변주 4개 × 5회 실측).
  같은 낱말에 어미만 바꿔도 다른 길로 간다. 흔들리는 것은 인자가 아니라
  `given` 축과 recipe 선택이다.

  ```
  전기차 충전소 데이터 검색해줘   SELECT 5/5   given=말한 키워드  120건
  전기차 충전소 데이터 줘         CLARIFY 5/5  given=null · 인자도 null · 후보 {012,013,032,033}
                                               ↑ 013 · 033 은 2026-08-26 에 없어진 번호다. 다시 안 쟀다
  전기차 충전소 알려줘            SELECT 5/5   given=말한 키워드  120건
  철도 안전 문서 찾아줘           SELECT 5/5   recipe_014 (문서 검색)  4건
  철도 안전 문서 보여줘           SELECT 4/5 가 recipe_006 (웹 검색) — 축 넷은 글자까지 같다
  청주시 인구 구성 알려줘         CLARIFY 5/5  {042, 043}
  청주시 인구 알려줘              CLARIFY 4/5  {031, 042, 043} · SELECT 1/5 {031}
  ```

  "데이터 줘" 조합만 `given` 을 통째로 비우고("구체적인 장소, 키워드, 식별자가
  명시되지 않았다"), "보여줘" 는 축을 그대로 둔 채 recipe 를 웹 검색으로
  넘긴다 — 그 web.search 는 USER_CONTEXT(`asap-mcp-core/*`) 권한 밖이라
  실행되면 HTTP 500 이다. 확신하고 실패하는 자리라 근접보다 나쁘다.
  menu 문장 탓인지 프롬프트 탓인지는 안 갈랐다.
- **경부선은 구간 하나로 좁혀진다. 저쪽 도구가 `LIMIT 1` 이라 우리가 못
  고친다** (2026-08-25 「스물다섯째」 실측). `rail.getSectionGeometry` 의
  `sectionName="경부선"` 이 ILIKE 매치 중 id 가 제일 작은 구간 하나(서울 도심,
  bbox [126.9608, 37.5194 ~ 127.0487, 37.5807])만 주고, 그 bbox 로 CCTV 를
  불러 0건이 된다. 인자는 맞다. 역명("오송역" · "부산역")을 넣으면 다른
  구간이 나오고, "오송-청주공항" 같은 하이픈 구간명은 NOT_FOUND 다.
  선택지 셋 — 그대로 둔다(구간명 발화에서는 맞게 동작) · 대표님께
  `LIMIT 1` 완화를 요청 · `geo.getRailwayLines(railwayName="경부선")` 우회
  (224 features, 서울~부산 전체 bbox 가 나온다 — 다만 그 bbox 로 CCTV 를
  부르면 전국 규모가 되므로 배선을 새로 정할 일이다).
- **1단계의 bbox 가 화면에 안 보인다.** `rail.getSectionGeometry` 가
  `칸: sectionId · geometry · bbox` 로 나가는데, 다음 단계 `road.getCctv` 로
  넘어가는 값이 `bbox` 라 그것만은 보여줘야 사람이 1단계와 2단계 줄을
  맞대볼 수 있다 (2026-08-25 「스물다섯째」 — 경부선 0건이 어디서 왔는지
  화면만으로는 못 읽었다). `geometry` 는 좌표 덩어리라 안 보여주는 것이 맞다.
  작은 일이라 다음에 묶는다.
- **`workflow_answer` 의 한 건 요약을 언제 다른 안으로 옮기나.**
  인구 수치를 못 싣던 것은 풀렸다 (2026-08-25 「스물셋째」). 목록형이 아닌
  응답을 요약하는 갈래를 칸 이름 규칙(`name` · `total…` · `male/female…` ·
  `referenceDate`)으로 만들었고 화면에
  `청주시 흥덕구 292,625명 (남 149,484 · 여 143,141) · 2026-06-30 기준` 이 뜬다.
  **남은 것은 그 규칙의 수명이다.** 규칙이라 틀린 칸을 집을 수 있고 실제로
  집었다 — `ev.getDatasetInfo` 의 `totalRegionCount` 17. 단위를 아는 수치만
  쓰도록 막았지만 그 사전(`SUBJECT_UNITS` · `PART_WORDS`)이 늘면 규칙이 아니라
  표가 된다. **예닐곱 줄을 넘거나 한 도구만을 위한 줄이 들어가면** 도구별
  표(가)나 배선표(다)로 옮긴다.
  **연령대별은 아직 안 싣는다** — `ageBands` 23건 · `ages` 111건을 한 줄에
  담는 방법을 안 정했다.
  **㉠ ㉡ ㉢ 셋은 풀렸다** (2026-08-25 「스물넷째」). ㉠ 단계마다 `input` 을
  적는다 (`ev.searchStations  query="전기차 충전소"  120건 · 일출랜드`).
  ㉡ `status` 가 `not_found` · `empty` 면 응답의 `message` 를 그대로 내고
  첫 줄도 `"찾지 못했습니다."` 가 된다. ㉢ 목록의 첫 항목에서 출처와 본문을
  읽는다 (`4건 · 「철도안전법(법률)(제21188호)(20260303).pdf」 · "법제처 …"`).
  **RAG 시연은 화면까지 이어졌다.**
  남은 것은 이 항목 첫머리의 수명 물음과 아래 셋이다 — 여러 항목을 보이는 것 ·
  `geojson.features` 안 · `properties.name`. 셋 다 「스물넷째」의 「안 한 것」에 있다.
- **목록의 첫 항목이 답처럼 읽힌다** (2026-08-25 「스물넷째」).

  ```
  "전기차 충전소 데이터 검색해줘"
    → ev.searchStations  query="전기차 충전소"  120건 · 일출랜드
  ```

  `"일출랜드"` 는 120건 중 첫 번째일 뿐인데 답처럼 읽힌다. 문서 검색
  (`knowledge.query`)에서는 첫 항목이 가장 관련 높은 것이라 자연스럽지만,
  목록 검색에서는 대표성이 없다. **둘을 가르려면 이 파일이 도구 이름을 알아야
  하는데 그것이 이 파일의 계약과 어긋난다** — 그래서 이번에 안 갈랐다.
  가르는 수가 생기면 다시 본다.
- **여럿 중 하나를 화면에서 못 봤다.** `totalMatches` 가 `count` 보다 크면
  답이 `"충북 청주서원 · 전체 8건 중 하나"` 로 나가게 했고 응답 전문으로는
  확인했다 (2026-08-25 「스물셋째」). **화면 발화로는 다섯 번 눌러 그 자리에
  못 갔다** — 되물음이 실행까지 안 가고, 가더라도 인자가 `"충북 인"` 처럼
  발화 토막째 실린다. **번호로 답한 것을 받아 실행하는 경로**가 생기면 그때
  다시 본다.
  **그 경로는 생겼다 (2026-08-26 「서른넷째」). 이 자리는 아직 안 봤다** —
  선거 발화로 되묻기 뒤를 눌러 보지 않았다.
  `getDistrict.name` 이 여덟 중 무엇을 기준으로 하나를 고르는지는 **여전히
  모른다**(「물음 4」). 답 문구는 여덟 중 하나라는 사실까지만 말한다.
- **되묻기 뒤 번호로 답할 수 없다.** CLARIFY 가 후보에 번호를 붙여 내놓는데
  `"2번"` 이라고 답해 실행하는 길이 없다. 발화를 처음부터 다시 말해야 한다
  (`execute_service._clarify_head` 의 제약 절이 "번호로 답한 것을 받아 실행하지
  않는다. 세션 상태와 저쪽 화면 계약이 필요함. 지금은 문구만 냄" 이라고 적어 뒀다).
  **2026-08-25 화면 실측에서 실제로 막혔다** (「스물셋째」) — `"춘천역 인구
  알려줘"` 의 되물음 뒤를 보려고 `"춘천역 연령대별 인구 구성 알려줘"` 를 다시
  말했고, 여럿 중 하나를 화면에서 못 본 원인의 절반도 이것이다.

  **풀렸다 (2026-08-26 「서른넷째」).** 되묻기를 세션마다 하나 기억해 두고
  다음 발화가 고르기면 그것을 실행한다 —
  `demo/api/services/clarify_service.py` 다. 화면에서 `"1번"` 과
  `"3 연령별 인구 구성 조회"` 둘 다 실행까지 갔다. LLM 을 다시 안 부른다.
  `_clarify_head` 의 제약 절도 함께 고쳤다 — 이제 그 줄이 곧 고를 수 있는
  이름이다.

  **남은 것 셋.**
  - **선택지 이름이 새 발화로 처리되어 인자가 오염되던 것은 막혔다.**
    `"1 전기차 충전소 검색"` 이 `ev.searchStations query="전기차 충전소"`
    120건을 내놓던 자리가 이제 고른 것으로 받아진다. **다만 다른 꼴로
    남았다** — 그 되묻기의 `argument` 가 `None` 이라(`POST /resolve` 3/3회)
    고른 뒤에 실행이 아니라 `"어느 장소인지 알 수 없습니다"` 로 멈춘다.
    인자를 못 뽑는 것은 발화 해석 쪽 일이고, 「스물다섯째」가 이 발화를
    `"전기차 충전소"` 로 닫았을 때는 SELECT 였다. CLARIFY 로 바뀐 뒤
    `argument` 와 `given` 이 둘 다 `null` 이다.
  - **번호와 이름이 서로 다른 줄을 가리키면 안 받는다.** 화면 목록의
    3번 줄 이름을 `"2 연령별 인구 구성 조회"` 라고 적으면 새 발화로 간다.
    일부러 그렇게 했다 — 어느 쪽이 뜻인지 알 수 없고, 잘못 실행하는 것보다
    다시 묻는 것이 낫다. 사람이 자주 이렇게 누르면 그때 다시 본다.
  - **인자만 뒤에 이어 붙이는 길은 없다.** 고른 뒤 `"청주시"` 라고 답해
    빈 인자를 채우지 못한다. 되묻기 하나를 기억하는 일과 대화를 이어 가는
    일은 크기가 다르다.
- **문자열로 고르는 것은 임시다.** `clarify_service.pick` 이 발화를 문자열로
  갈라 되묻기 선택지를 집는다 (2026-08-26 「서른넷째」). 그 규칙 대부분은
  **저쪽 화면이 되묻기 선택지를 버튼으로 주면 안 쓰인다** — 버튼을 누르면
  번호가 그대로 넘어오므로 번호만 받으면 된다.

  ```
  번호만 · 번호 + 이름   버튼이 와도 번호 쪽은 남는다
  이름 일치              안 쓰인다. 사람이 이름을 옮겨 적을 일이 없어진다
  차례말("첫 번째")      안 쓰인다. 특히 이것이다
  ```

  **화면은 우리 소관이 아니다.** 저쪽이 언제 버튼으로 바꿀지 모른다. 그때까지
  사람이 글로 답하는 길이 유일하다.

  **그래도 지금 지우지 않는다.** 이미 짜여 있고 시험이 붙어 있어 두는 값이
  0 이다 (`tests/demo/api/test_clarify_choice.py` 36개). 빼면 코드를 고치고
  화면을 다시 재야 한다 — 안 쓰이는 것을 지우는 데 그만큼 쓸 이유가 없다.
  **버튼이 오면 그때 줄인다.** 줄이는 자리는 `pick` 한 함수다.

- **철도 노선 조회가 노선 이름을 못 받았다 — 고쳤다** (2026-08-29 「쉰째」).
  **나) 를 골랐다. 배선이 값을 보고 칸을 고른다** — "…선" 으로 끝나면
  `railwayName`, 아니면 지금 그대로 `stationName` 이다. `"경부선 노선 보여줘"`
  0건 -> 224건 · `"호남선 노선 보여줘"` 0건 -> 79건 · `"오송역 지나는 노선
  알려줘"` 31건 그대로다. **판정은 네 묶음이 바이트까지 같다**(적중 51/93 ·
  빗나감 9) — 배선은 recipe 를 고른 뒤에 읽혀 프롬프트에 안 닿는다.
  **온톨로지도 menu 도 축도 GT 번호도 안 움직였다.**

  가) 를 안 고른 근거 넷과 규칙의 한계 셋은 「쉰째」에 있다. 아래는 그 자리까지의
  이력이다. **2026-08-26 화면 실측** —

  ```
  "경부선 노선 보여줘" → 되묻기 → "1 철도 노선 조회"
    geo.getRailwayLines  stationName="경부선"  0건
  ```

  저쪽 도구는 칸이 둘이다. `railwayName="경부선"` 은 224개 조각이 오고
  (서울 126.87 ~ 부산 129.06) `stationName="경부선"` 은 0건이다.
  **배선이 `stationName` 하나만 보낸다** —
  `step_service.STEP_OF` 의 `("get_railway_lines", "place_name")` 줄이다.

  **배선 주석이 이 자리를 이미 적어 뒀다** — "railwayName 은 안 건드린다.
  stationName 과 같은 「장소 이름」이라 줄이 갈리지 않는다 — 역명으로 볼지
  노선명으로 볼지는 사람이 정할 일이다." **그 일이 왔다.**

  같은 노드 · 같은 「말한 장소」 자리인데 값의 성격이 다르다.

  ```
  "오송역 지나는 노선 알려줘"  stationName 이 맞다. 지금 된다
  "경부선 노선 보여줘"         railwayName 이 맞다. 지금 0건이다
  ```

  길이 셋 보였다. **나) 를 골랐다 (2026-08-29 「쉰째」).**

  ```
  가) 「말한 노선 이름」 타입을 따로 둔다   안 골랐다. 온톨로지가 갈라 주지만
                                            given 축 선택지가 늘고 recipe 번호가
                                            또 밀린다 (「서른째」에서 겪었다).
                                            ★ 이번 주에 축을 건드린 세 번이 다 졌고
                                              (「서른여섯째」·「마흔두째」·「마흔아홉째」),
                                              고치려는 28번이 이미 그 축에서 지고 있다
  나) 값을 보고 고른다                      ★ 골랐다. 배선표에 값 판단이 처음 들어갔다.
                                            아래 새 항목에 걷어낼 조건을 적었다
  다) 둘 다 보낸다                          닫혔다. 저쪽이 두 절을 AND 로 잇는다
  ```

  **다음 「축 한 묶음」 작업에서 재고 정한다.** 그때 `check_resolve` 확장 표의
  28번(`"경부선 노선 보여줘"`)이 함께 움직이므로 같이 재야 한다.

  **미리 쟀다 (2026-08-27 「서른다섯째」).** 칸별 표가 나왔다 —
  `railwayName="경부선"` 224건 · `stationName="경부선"` 0건 · **둘 다 보내면
  AND 로 묶여** 같은 값이면 0건이다 (저쪽 `rail_repository._build_section_filters`
  가 두 절을 AND 로 잇는다. 읽어서 확인). 그래서 **다) 둘 다 보낸다는 닫혔고**
  가) · 나) 는 열려 있다 — 맞는 칸 하나만 보내면 된다. 인자 없이 부르면 전국
  3683 features 가 온다는 것도 함께 적었다. 표 전문은 「서른다섯째」에 있다.

  **계기판이 잡는다 (2026-08-27 「마흔째」).** `tools/check_inputs.py` 의 ★ 표에
  `geo.getRailwayLines × place_name` 이 `railwayName(S)` 로 올라온다 — 우리 칸
  `stationName` 과 낱말 Name 을 공유해서다. 판정은 「맞다」(없는 칸 0 · required
  없음)이므로 **화면 0건은 스키마 위반이 아니라 칸 선택 문제**였다.

  **★ 「이 표에서 `railwayName` 이 사라지는지로 확인한다」는 못 쓴다** (2026-08-29
  「쉰째」). 나) 를 고르면 사라질 수가 없다 — 그 도구는 `STEP_OF` 를 정적으로
  읽으므로 표에 적힌 `stationName` 한 칸만 본다. 칸을 고르는 것이 값이라 표에
  안 실린다. 판정은 고친 뒤에도 「맞다」로 같다.

  **고쳐 적는다 (2026-08-30 「쉰아홉째」).** check_inputs 를 살리면서 이 줄을
  「갈래마다 한 행」으로 세게 했다 — 기본 갈래(stationName) 한 행 · "…선" 갈래
  (railwayName) 한 행이다. 이제 표에 두 칸이 다 실리므로 위 문단의 「사라질
  수가 없다」는 더는 맞지 않다. step_service 의 배선 주석("★ 표에서
  railwayName(S) 은 안 사라진다")이 그래서 낡았는데, 이번 작업은 제품 코드를
  안 건드리는 규칙이라 그대로 두었다 — **다음에 그 파일을 열 때 고친다.**
  죽어 있던 계기판 자체도 「쉰아홉째」가 살렸다 (측정 기록 참고).

- **되묻기 후보 안에 「말이 되지만 답이 나쁜」 길이 섞인다** (2026-08-29 「쉰째」
  7절, 사람이 저쪽 화면에서 눌렀다). 후보가 다 온톨로지가 만든 진짜 경로인데
  **답의 품질이 크게 갈린다.**

  ```
  "호남선 노선 보여줘"
    1  철도 노선 조회                          79건    ★ 맞는 답
    2  장소 좌표 변환 → 철도 노선 조회         14건    노선 이름을 점 하나로 지오코딩해
                                                       (126.7927, 35.1789) 주변만 준다
  "오송역 지나는 노선"
    1  철도 노선 조회                          31건    ★ 맞는 답
    3  철도 구간 형상 조회 → 철도 노선 조회  1156건    전국에 가깝다. 구간 형상의 bbox 가
                                                       넓게 잡히는 것으로 보이나 안 팠다
  ```

  **어떻게 다룰지는 정하지 않는다.** 사실만 적는다. 시연에서는 1번을 고른다.
  경로가 「말이 된다」와 「답이 좋다」가 다른 것이고, 지금 되묻기는 앞엣것만
  본다. 순서를 매기려면 답을 봐야 하는데 그것은 도구를 먼저 부른다는 뜻이다.

- **★ 배선표에 값 판단이 들어왔다 — 언제 걷어내는가** (2026-08-29 「쉰째」).
  `STEP_OF` 는 「무엇을 어느 칸에 넣는가」만 적는 표였고 **값이 무엇인지는 안 봤다.**
  그 경계를 깼다. 철도 노선 조회 한 줄에 `arg_field: ("선", "railwayName")` 을
  적었고 `_by_argument` 가 발화에서 온 값의 어미를 본다.

  **왜 깼나.** 온톨로지의 `place_name`(장소 이름) 하나가 역 이름과 노선 이름을
  함께 담아 `(노드, 받는 타입)` 으로는 줄이 안 갈린다. 가르는 길(가)은 `given` 축
  선택지를 늘리는 것인데, **이번 주에 축을 건드린 세 번이 다 졌고**
  (「서른여섯째」·「마흔두째」·「마흔아홉째」) 고치려는 28번이 이미 그 축에서
  지고 있다(`given` 을 `spoken_keyword` 로 쓴다). 대가를 견주어 사람이 정할 일이
  아니라 표가 정해 준 자리였다.

  ```
  ★ 걷어낼 조건 — 하나라도 서면 이 칸을 지운다
    온톨로지가 「말한 노선 이름」을 갖게 되면   줄이 타입으로 갈리므로 값을 볼 일이 없다
    같은 칸을 적은 줄이 셋을 넘으면            자가 하나씩 늘어나는 것이라 표가 규칙집이 된다.
                                               그때는 자를 한 곳에 모으거나 축으로 옮긴다
    "…선" 자가 헛맞아 되던 발화가 깨지면       1순위(빗나감)와 같은 무게로 되돌린다
  걷어내는 자리
    step_service 의 RAILWAY_LINE_SUFFIX · _by_argument · 배선 한 줄의 arg_field.
    셋뿐이고 tests/demo/api/test_step_argument.py 의 여덟 시험이 그 셋을 지킨다
  ```

  **자를 넓히지 않는다.** "…선" 하나이고 "…역" 으로도 안 좁혔다 — "청주" 가
  `stationName` 으로 9건인데 좁히면 그 자리를 잃는다. `"1호선"` 은 두 칸 다 답이
  있는데(521건 · 345건) 규칙이 `railwayName` 으로 떨어뜨렸고 **재서 고른 것이
  아니다.** 표는 「쉰째」 3절에 있다.

- **배선이 안 쓰는 칸 — 계기판 「마흔째」가 올린 자리들.** 고치는 것은 나중이다.
  목록만 둔다. 판정 넷 중 「없는 칸」 0 · 「안 보낸 required」 0 이라 지금 반드시
  틀린 줄은 없다.

  ```
  election.getLocalPledgeSummary × admin_code   sidoName 안 보냄 (S)
      철도와 같은 꼴. 지금은 items.0.code 가 오니 sidoCode 가 맞다.
      「말한 시도 이름」자리가 생기면 철도처럼 된다. 그때 이 줄을 본다
  adminBoundary.searchBoundaries · election.search* ×3 · population.searchStatistics
  · vworld.getAdministrativeBoundaries · geo.getRailwayLines · ev.searchStations
      keyword 줄은 bbox 를, map_extent 줄은 query 를 안 보낸다 (O 19)
      자리마다 받는 것이 달라 그런 것이다. 틀린 것이 아니라 확인한 자리
  includeBbox ×2 · minOutputKw                  S 의 소음. 손잡이다
  ```

  S 를 「마지막 낱말이 같을 때만」으로 좁힐지는 사람이 정한다.

  **★ 고쳐 적는다 (2026-08-30 「쉰여섯째」). 「지금 반드시 틀린 줄은 없다」가
  더는 맞지 않다.** 위 목록은 「자리마다 받는 것이 다르다」로 넘긴 것인데,
  그중 넷은 **사람이 실제로 하는 말을 막고 있다.** 계기판의 판정 넷으로는
  「안 쓰는 칸(O)」이라 눈에 안 띄던 자리다.

  **도구에 있는 칸을 우리 배선이 안 보낸다 — 사례 넷**

  ```
  all               election 계열 넷. 「전부·전체」 발화가 안 된다
                    all=true 면 254건, 아무것도 안 보내면 첫 쪽 20건 (2026-08-28 실측)
  includeGeometry   adminBoundary.searchBoundaries. 응답에 geometry 가 아예 없다
                    query="청주" 만 2,798자 · includeGeometry=true 면 39,059자
                    (2026-08-30 실측). 그릴 것이 안 오니 지도에 안 그려진다
  pledgeCategory    election.searchAssemblyDistricts
                    ★ 2026-08-30 실측 — query="교통 공약" 0건 · query="교통" 20건 ·
                      pledgeCategory="교통" 20건 · hasPledges=true 20건
  order             population.searchStatistics. 「많은 순」을 못 한다
  ```

  **★ 정답표 16 · 19 가 이 자리다.** 둘 다 「도구가 못 하는 일」이 아니라
  **「인자·배선 문제」**다. 16 은 "교통 공약 있는 선거구", 19 는
  "인구 많은 시군구 순위" 이고 둘 다 실행 칸이 0건이다 (「쉰여섯째」).

  **★ `tools/check_inputs.py` 가 이런 자리를 찾는 계기판인데 `KeyError` 로 죽어
  있다** (「쉰째」의 항목). **살리면 나머지도 한 번에 센다** — 지금 넷은 손으로
  찾은 것이라 더 있는지 모른다. 살릴 때 판정 이름을 다시 볼 것 : 「안 쓰는 칸」이
  「판단할 자리」인 것은 맞지만, 그 안에 **사람 말을 막는 자리**가 섞여 있다.

  **★ 고쳐 적는다 (2026-08-30 「쉰아홉째」). 계기판을 살려 한 번에 셌다.**
  발화(@arg) 줄의 안 쓰는 칸을 전부 올리는 기준 A 를 더했고 위 넷이 전부
  걸린다. 전체 셈은 안 쓰는 칸 306 · 그중 A 94 이고, **없는 칸을 보내는 줄이
  하나 새로 나왔다** — `ev.searchStations` 의 보이는-범위 첫 자리가 스키마에
  없는 `bbox` 를 보낸다. 표 전문과 사람이 볼 자리 목록은 측정 기록
  「쉰아홉째」에 있다. **어느 길로 고칠지(recipe 를 더 판다 · 인자를 둘 받는다 ·
  무인자 허용)는 여전히 안 정했다** — 그 물음만 이 항목에 남는다.

- **`items.1` 로 시군구에 고정된다.** `get_age_profile` ·
  `get_population_trend` 의 배선이 `$prev.items.1.layerId` 와
  `$prev.items.1.code` 를 쓴다 (2026-08-24 「열여덟째」).
  `findBoundaryByPoint` 는 늘 `sido -> sigungu -> emd` 3건을 내놓고 그중
  **둘째를 집는다.** 그래서 발화가 시도를 말해도 시군구 값이 나온다 —
  "충청북도 인구 구성 알려줘" 도 청주시 것을 받는다.
  **가르려면 발화 해석이 층위를 함께 내놓아야 한다.** 지금 축 셋(`given` ·
  `want` · `about`)에는 층위를 적을 자리가 없고, `items` 셋 중 무엇을 집을지는
  배선 표가 값을 고르는 일이 되어 위의 `_filled` 항목과 같은 자리로 간다.
  **사촌이 하나 있다 (2026-08-25 「스물다섯째」). 같은 것으로 묶지 않는다** —
  증상은 같고(물은 것보다 좁은 답) 고치는 자리가 다르다.

  ```
  items.1 문제   배선이 세 층위 중 둘째를 집는다 — 배선표의 선택. 우리 것이다
  이번 것        저쪽 geocode 의 bbox 가 1.1km 상수다 — 우리가 못 고친다
  ```

  이번 것은 `"청주시 인구 알려줘"` → 상당구 193,079명이고, 원인은
  `ASAP-mcp/app/tools/geo.py:92-95` 의 `lon ± 0.005` 상수다.
  자세한 것은 「스물다섯째」에 있다.
- **코드 자릿수로 `level` 을 정하는 안.** 2 자리면 `sido` · 5 자리면 `sigungu` ·
  8 자리면 `emd` 다 (2026-08-24 실측). **동기가 바뀌었다** — 발화로 코드를 대는
  경로가 없어져(「열여덟째」) 발화 자리를 적을 일 자체가 없다. 급하지 않다.
  배선 한 줄이 아니라 `step_service._filled` 에 변환을 넣는 일이라는 것은 그대로다.
- **발화 4 는 지명마다 판정이 갈린다** (2026-08-24 실측, 각 3회).
  같은 문형인데 지명만 바꿔도 갈린다.

  ```
  청주시 인구 구성 알려줘   SELECT  {011}                    3/3
  대전시 인구 구성 알려줘   CLARIFY {034, 045} 1 · SELECT {045} 2
  오송시 인구 구성 알려줘   CLARIFY {034, 045, 046} 1 · SELECT {045} 2
  서울시 인구 구성 알려줘   CLARIFY {034, 045, 046}          3/3
  ```

  **"청주시" 만 `{011}` 로 간다.** 나머지 셋은 `045` 를 맞히거나 후보에 담는다.
  체계적 편향이 아니라 경계선이다 — GT 발화가 하필 그 선 위에 있다.
  위의 「발화 4 가 안 갈린다」 항목과 같은 자리인데, 지명을 바꾸면 지금도
  `045` 가 나온다는 것이 새로 안 것이다. **GT 를 다시 볼 근거가 하나 늘었다.**
  **정정 (2026-08-24 「열여덟째」).** "청주시" 도 이제 정답으로 간다.
  지명마다 갈리는지는 다시 재봐야 안다 — 위 넷은 옛 온톨로지에서 잰 것이고
  번호도 옛 것이다(`045` 는 지금 `042`).
- **노드 등록 후(recipe 8)의 판정을 안 쟀다.** 등록 전 6개에서만 30/30 을
  굳혔다. 등록 장면이 시연의 핵심이라 재야 한다. 8개에서 3번 발화가 흔들린
  전례가 있다 (아래 2026-08-19 참고).
- **2026-08-15 측정이 재현되지 않는 이유를 못 찾았다.** 저장소 안에는 원인이
  없다. 남는 것은 Ollama 런타임(0.32.14)뿐이다.
  **2026-08-24 「열넷째」에서 후보 하나를 지웠다** — 모델을 내렸다 올리는 것은
  답을 안 바꾼다. 한 세션 안 여섯 묶음이 바이트 단위로 같다. 남은 후보는 ollama
  프로세스 재시작 · uvicorn 재시작 · ollama 버전(0.32.14 -> 0.32.15)이다.
  `keep_alive` 2시간은 환경변수가 아니라 `llm_engine/ollama.py:88` 에 박혀 있다.
  **2026-08-27 「서른일곱째」에서 다시 봤다.** 같은 파일을 연달아 재면 바이트까지
  같고(A1=A2 · A3=A4 · B1=B2), 사이에 다른 프롬프트(V2)가 끼면 다음 V0 판이
  발화 11개 갈린다(A1≠A3). 모델을 내렸다 올리면 A1 로 돌아온다(A5=A1, 판정 네
  칸 전부). **「열넷째」의 "내렸다 올리는 것은 답을 안 바꾼다" 는 그대로 맞다** —
  다만 그것은 "처음부터 계산한 상태로 되돌린다" 는 뜻이었다.
  **지워진 후보 — `OLLAMA_NUM_PARALLEL` 배칭.** 값이 1 이다(journal server config).
  **남은 후보가 하나로 모인다** — ollama 재시작 · uvicorn 재시작 · 버전 차이가 아니라
  **그 시점에 llama-server 가 물려주던 공통 접두사 KV 캐시의 이력**이다(자세한
  것은 「서른일곱째」 "무엇이 쌓이는가"). 2026-08-15 표는 그때의 캐시 이력을
  모르니 재현이 안 되는 것이 정상이다. 후보이지 확인은 아니다 — 확인은 ollama
  설정(프롬프트 캐시)을 바꿔야 해서 안 했다. 버전(0.32.14→0.32.15)은 지금 0.32.15 고
  되돌려 재지 않았으니 후보로 남는다.
- 형식 동의어("워드" · "피피티" · "파워포인트")를 프롬프트 지시문에 명시하는
  안. menu 문장(데이터)에 담는 것은 실패했다 (아래 2026-08-15 참고).
- **발화 4 가 안 갈린다. 「열한째」에서 성격이 바뀌었다.** 셋 다 인구이고 셋 다
  `statistics` 를 내놓아 shortlist 가 `034` · `045` · `046` 을 다 올린다.
  앞토막을 57자에서 31자로 줄여도 그대로였다 (2026-08-23 아홉째). 문장 길이가
  아니라 능력이 인접한 것이 원인이다.
  `group_document` 를 없앤 뒤로는(「열한째」) 셋을 다 올리는 CLARIFY 가 아니라
  `{011}` 하나를 확신하고 SELECT 한다 — "청주시" 를 장소가 아니라 키워드로
  읽는다. 점수는 둘 다 0/10 이지만 되물어보던 것이 조용히 틀리게 된 것이라
  화면에서는 나빠졌다. **GT 를 다시 볼 자리일 수 있다** — "인구 구성" 이
  `045`(연령대별 남녀) 하나만 뜻하는지 사람이 정할 일이다.
  **이제 표가 그 차이를 본다** (2026-08-24 「열두째」). 예전 상태는 「근접」이고
  지금은 「빗나감」이다. 점수는 둘 다 0/10 이지만 칸이 다르다.
  **그리고 인자도 함께 틀린다** (「열셋째」). `{011}` 을 골라도 인자가
  `"인구 구성"` 이라 `population.searchStatistics` 가 0건이다 — `"청주시"` 면
  4건이 나온다. **recipe 를 고쳐도 인자를 안 고치면 답은 그대로 0건이다.**
  둘은 따로 고쳐야 하는 두 가지다.
  **정정 (2026-08-24 「열여덟째」). 갈렸다.** `말한 식별자 is-a 행정구역 코드`
  를 떼면서 옛 `019`(말한 식별자 → 연령별 인구)가 사라지자 LLM 이 내는 후보가
  `{011, 019}` 에서 `{011, 042}` 로 바뀌고 검산이 `{042}` 를 남긴다. 세 묶음
  모두 적중 10/10 이다. **인자는 이제 `"청주시"` 로 뽑힌다** — 위의 인자 항목도
  이 발화에 한해서는 풀렸다. GT 를 다시 볼 자리라던 물음은 닫혔다.
  **다시 열렸다 (2026-08-25 「스물한째」). 커밋 안 한 작업본에서다.**
  시작 데이터 노드 셋의 description 을 다시 쓰자 `given` 축이
  `말한 장소` 에서 `말한 키워드` 로 뒤집히고 다시 `{011}` 을 확신한다.
  세 묶음 모두 빗나감 10 이다. 온톨로지의 관계는 그대로인데 **문장만으로
  같은 자리가 돌아왔다.** `git checkout -- ontology workflows` 로 되돌아간다.
  **범인이 갈렸다 (2026-08-25 「스물두째」, 세 묶음).** 시작 데이터 노드 셋
  **셋만** 되돌리자 `given` 이 `말한 장소` 로 돌아오고 빗나감이 사라졌다.
  **적중까지는 안 간다.** LLM 이 내는 후보가 HEAD 의 `{011, 042}` 가 아니라
  `{042, 043}` 이라 둘 다 조회 후보 안에 있고 검산이 못 줄여 CLARIFY 로 남는다
  (근접 10). `given` 축 선택지가 HEAD 와 글자까지 같으므로 **남는 원인은
  menu 의 선거 일곱 줄뿐이다** — 선거와 무관한 발화가 menu 문장에 흔들리는
  네 번째 전례다. 지금 이 발화의 자리는 셋으로 갈린다.
  **옛 문장 = 적중 · 아홉 개 다 고침 = 빗나감 · 선거 여섯만 = 근접.**
  가운데와 오른쪽 중 어느 것이 나은지는 점수가 아니라 「틀린 답보다 정직한
  되물음이 낫다」로 가른다.
- **발화 6 은 menu 문장으로는 안 갈린다** (2026-08-25 「스물한째」 실측).
  `007` 과 `008` 의 문장에 이름 형식("충북 청주서원" 대 "청주시 흥덕구")과
  공약 집계 실물(254곳 중 77곳 · 476건)을 넣어 크게 갈라 놨는데도
  `{007, 008, 009}` 로 되묻는 것이 안 움직였다(근접 9 → 근접 10).
  「열아홉째」가 못 갈랐다고 적어 둔 「menu 의 224자냐 번호표냐」에서
  **문장 쪽 후보가 약해졌다.** 남은 것을 보려면 번호표 쪽을 건드려야 한다.
  다만 `009`(공약 검색)는 「스무째」에서 안 눌러 문장을 못 고쳤다 —
  셋 중 하나가 옛 문장인 채로 잰 것이라 **깨끗한 대조는 아니다.**
  **한 번 더 굳었다 (2026-08-25 「스물두째」, 세 묶음).** 축 설명 셋을
  되돌려 세 번째 상태를 만들었는데도 `{007, 008, 009}` 가 안 움직인다.
  옛 문장에서도 · 아홉을 다 고친 뒤에도 · 선거 여섯만 남긴 지금도 근접이다.
  **이 발화는 세 상태 모두 같다.**
- **인자를 발화에서 통째로 옮겨 담는다. 그래서 recipe 가 맞아도 0건이다.**
  (2026-08-24 「열셋째」 실측) `"청주시 인구 구성 알려줘"` 에서 `"인구 구성"` 을,
  `"전기차 충전소 데이터 검색해줘"` 에서 `"전기차 충전소 데이터"` 를 뽑는다.
  도구가 받는 것은 **찾을 대상의 이름**인데 **하려는 일의 이름**을 넣는다.
  7번은 낱말 하나("데이터")가 붙었을 뿐인데 123건이 0건이 된다.
  **어떻게 고칠지는 안 정했다** — 프롬프트를 손볼지, `given` 축을 보고 뽑는 법을
  가를지, 도구 쪽 검색을 넓힐지가 다 다른 일이다. `tools/check_argument.py` 로
  고치기 전후를 잴 수 있다.
  **정정 (2026-08-25 「스물셋째」). 대표 예시가 풀렸다.**
  `"전기차 충전소 데이터 검색해줘"` 가 화면에서 `ev.searchStations 120건` 이다.
  「열셋째」에 0건이라고 적힌 그 발화다. `check_resolve` 의 축 표를 보면 인자가
  `"전기차 충전소 데이터"` 에서 `"전기차 충전소"` 로 바뀌어 있다 —
  `말한 식별자 is-a 행정구역 코드` 를 뗀 뒤다(「열여덟째」). 두 묶음 모두
  10/10 로 같은 인자가 나온다.
  **남은 것이 무엇인지는 다시 재야 안다.** 발화 아홉의 인자가 다 좋아진 것인지
  이 하나만인지를 안 쟀다. `tools/check_argument.py` 로 잰다.
  **닫는다 (2026-08-25 「스물다섯째」, 아홉 발화 × 5회 실측).**
  아홉 전부에서 LLM 인자 = 사람이 골랐을 값이다 — 1·2·3·5 "오송역" ·
  4 "청주시" · 6 "국회의원 선거구"(0건) · 7 "전기차 충전소"(120건) ·
  8 "철도 안전"(4건) · 9 "충북 제1선거구". 대표 예시(7번)는 5/5 로 굳었다.
  0건은 6번 하나인데 사람 값("선거구")도 0건이라 어휘 불일치지 추출 탓이
  아니다. 발화를 통째로 옮겨 담는 자리는 더 없다. **남은 것은 인자가 아니라
  어미가 흔드는 축·recipe 선택이다** — 아래 「어미가 판정을 흔든다」로 간다.
- **발화 9 는 `given` 축 선택지에 민감하다** (2026-08-25 「스물두째」, 세 묶음).
  `말한 식별자` 의 description 을 "한 건을 집어내는 코드나 정식 이름.
  검색어로는 안 걸린다." 로 바꾸자 `{015, 016, 017}` 로 벌어지고, 되돌리자
  적중 10/10 으로 돌아왔다. **선거 여섯의 새 문장(이름 형식)은 그대로 둔
  채였다** — 「스물한째」가 이름 형식 탓으로 짚은 것은 틀렸다.
  `말한 식별자` 를 손볼 때는 이 발화를 함께 재야 한다.
- **`recipe_015` 는 적중 10/10 인데 화면에서 아무것도 못 한다.**
  (2026-08-24 「열셋째」 실측) `get_election_district` 에 배선이 없어
  `execute_service.run` 이 도구를 하나도 안 부른다. `check_resolve` 의 적중
  표만 보면 안 보이는 자리다. 「막힌 노드 여덟」과는 다른 것이다 — 저건 데이터가
  없는 것이고 이건 `STEP_OF` 에 줄이 없는 것이다. `tools/check_wiring.py` 의 C
  부류다.
  **정정 (2026-08-25 「스물셋째」).** `STEP_OF` 에 `get_election_district` 줄이
  있고 화면 발화 `"선거구명 충북 인 지역구 하나만 집어줘"` 가
  `election.getDistrict` 를 실제로 불렀다. **지금 막는 것은 배선이 아니라
  되물음과 인자다** — 대개 CLARIFY 로 끝나고, 가더라도 인자가 `"충북 인"` 이라
  not_found 다.
- **머리말 겹침은 풀렸다** (2026-08-25 「스물넷째」).
  `"전기차 충전소 데이터 검색해줘"` 가 `"전기차 충전소 전기차 충전소를
  조회했습니다."` 로 나오던 것이 `"전기차 충전소를 조회했습니다."` 가 됐다.
  `step_service.plan` 의 조립 한 줄(`_headline`)에서 고쳤고 `TOOL_OF` 48줄은
  안 건드렸다 — **배선을 손볼 때 묶어 고칠 일이 아니었다.** 겹치는 것은 한
  줄이 아니라 「인자 + 도구 이름」이 만나는 자리였다.
  **남은 것은 문형 가정이다.** `template.format(arg="")` 로 뒷 문장을 얻으므로
  `TOOL_OF` 에 `"{arg} …"` 가 아닌 문형이 생기면 다시 봐야 한다.
- **`STEP_OF` 의 headline 이 완결된 한국어 문장이라 부정형을 못 만든다.**
  `"{arg} 행정구역을 조회했습니다."` 같은 문형이라 0건일 때 `"…조회하지
  못했습니다."` 로 바꾸려면 어미를 문자열로 잘라야 한다. 문형이 하나 늘 때마다
  자르는 규칙이 하나 늘어 안 했다. 지금은 `workflow_answer` 가 빈 결과 · 오류일
  때 headline 을 통째로 버리고 `"찾지 못했습니다." / "조회하지 못했습니다."` 만
  쓴다 — 무엇을 조회하려던 것인지는 단계 줄이 말한다.
  headline 을 명사형(`"{arg} 행정구역"`)으로 바꾸면 성공 · 빈 결과 · 오류 셋을
  한 문장 틀로 합칠 수 있다. `STEP_OF` 48줄을 다시 쓰는 일이라 이번에 안 했다.

### 정답표에 「고쳐야 할 자리」로 박아둔 다섯 (2026-08-26 「서른두째」)

정답표를 아홉에서 스물여덟로 늘리면서 **지금 틀리는 것을 알면서 넣은 발화가
다섯** 있다. 기대값은 사람이 정했다. 지금은 근접이나 빗나감으로 잡히고,
아래를 고치면 적중이 된다. **기대값을 여럿으로 적어 지금 상태를 적중으로
만들지 않았다** — 고쳐야 할 자리라는 것이 표에 남는 것이 목적이다.

`tools/check_resolve.py` 의 표시 칸에서 `위험` · `애매` 로 보인다.

#### 애매 셋 — 사람이 기대값을 정한 자리

| # | 발화 | 기대값 | 왜 그것인가 | 지금 |
|---|---|---|---|---|
| 29 | 청주시 인구 알려줘 | `011` 키워드 → 인구 통계 | 청주시 **전체**를 물었으니 네 구가 다 나와야 한다. 좌표 길(`029`)은 `geo.geocode` 가 늘 1.1km 상자라 한 구만 준다 (「스물다섯째」 「아홉째 한계」) | `{029, 038, 039}` 로 되묻는다. **011 이 후보에 아예 없다** |
| 30 | 오송역 선거구 알려줘 | `022` 장소 → 좌표 → 지점 선거구 판별 | 그 지점이 든 선거구 「이름」을 묻는 것이다. 검색이 아니라 판별이다 | 검색 셋으로 되묻는다 |
| 31 | 청주 국회의원 선거구 검색해줘 | `007` 키워드 → 지역구 검색 | 발화 6 과 같은 자리다. 선거구명을 달라는 뜻이므로 `007` — `008` · `009` 는 공약이고 `010` 은 지방선거다 (「열째」에서 GT 6번을 바로잡을 때의 근거와 같다) | 넷으로 되묻는다 |

29 와 30 은 같은 모양이다 — **「장소를 말했으니 좌표로 간다」가 너무 세다.**
지명이 나오면 `spoken_place` 로 시작하는 사슬이 다 후보로 들어오고, 그중
좌표를 지나는 것이 많아 키워드 길이 밀린다. 29 는 그 때문에 정답이 후보에서
아예 빠졌다.

#### 위험 둘 — 지금 확신하고 틀린다

- **27 「철도안전법 내용 찾아줘」 — 기대값 `013`(키워드 → 문서 검색).**
  지금 웹 검색(`006`)으로 **확신하고 가서**(SELECT) 권한 없음으로 실패한다.
  "문서" 라는 낱말이 있으면(25 · 26) 문서 검색으로 가고 없으면 샌다.

  **웹 검색도 말이 되는 읽기다.** 사람이 "철도안전법 내용" 이라고만 하면
  어디서 찾을지는 안 말한 것이다. **이상적인 답은 되묻기**이고, 그 다음이
  사슬 조건부(문서 검색 → 0건이면 웹)나 UX(출처를 고르는 칸)다.
  **이번 범위가 아니라 안 건드렸다.** 기대값을 `013` 으로 둔 이유는 시연
  요구가 "문서에서 찾아온다" 이고 지식베이스에 그 법이 실제로 들어 있기
  때문이다(철도안전법 47쪽, 「서른한째」).

  고칠 자리는 축이거나 UX 다. 온톨로지 한 줄로 풀리는 것이 아니다.

- **28 「경부선 노선 보여줘」 — 기대값 `003`(장소 → 철도 노선 조회).**
  지금 `{006, 013}`(웹 검색 · 문서 검색)으로 되묻는다. **철도가 후보에 아예
  안 든다.**

  까닭은 축이다. "경부선" 이 「말한 키워드」로 읽히면 `spoken_keyword` 로
  시작하는 사슬만 후보가 되는데, 철도 경로(`002` 장소 → 철도 구간 형상 ·
  `003` 장소 → 철도 노선 조회)는 둘 다 **`spoken_place` 로 시작한다.**
  그래서 철도 전체가 통째로 빠진다.

  27 과 정반대 방향의 같은 문제다 — 29 · 30 은 지명이 좌표를 너무 잘 끌고,
  28 은 노선 이름이 지명으로 안 읽힌다. **`spoken_place` 가 지명 · 역 이름 ·
  구간 이름을 다 삼키는데**(위 「막힌 노드」의 `place_name` 항목과 같은 뿌리)
  정작 "경부선" 은 못 삼켰다. 어느 쪽으로 고쳐도 다른 쪽이 흔들리므로
  **29 · 30 · 28 을 한 번에 봐야 한다.**

### 막힌 노드 — 여덟(2026-08-23)이 다섯(2026-08-26)이 됐다

2026-08-23 에는 recipe 가 될 수 있는 노드 28개 중 여덟이 데이터가 없거나
권한이 없었다. **2026-08-26 「스물여덟째」에서 스물일곱을 전부 다시 눌렀다** —
그 사이 저쪽 PostGIS 에 데이터가 들어와(행정구역 5,335 · 인구 18,623 ·
충전소 92,863 · 충전기 488,162 · 철도 2,243 · 문서) 셋이 풀렸다.

```
                                    2026-08-23      2026-08-26 (지금)
search_admin_boundaries             0건             있다  청주시 4건
find_admin_boundary_by_point        0건             있다  오송역 3건 (43 → 43113 → 43113250)
search_documents                    0건             있다  "철도 안전" 4건 · 철도안전법 본문
search_local_pledge_summaries       0건             0건   getLocalPledgeSummaryDatasetInfo 가
get_local_pledge_summary            0건             0건   available false · featureCount 0 ·
find_local_pledge_summary_by_point  0건             0건   "…DB 데이터가 적재되지 않았습니다"
web_search                          권한            권한  HTTP 500 · "not applied for this user"
web_fetch                           권한(추정)      못 부름 (추정 그대로) — url 근원인
                                                    web.search 가 막혀 여전히 못 눌러봤다
```

남은 다섯 중 지방선거 셋은 한 데이터셋
(`kr-local-election-sido-pledge-summaries-2026`)이고 웹 둘은 한 서버 권한이다 —
**실질적으로 막힌 자리는 둘이다.** 나머지 스물둘은 전부 실제 데이터가 온다.
스물일곱 줄 전체 표와 인자·건수·대표 값은 「스물여덟째」에 있다.

옛 목록의 "막힌 노드를 지나는 recipe 13개(004 006 010 014 019 020 029 030
034 038 041 042 043)" 는 recipe 45개 시절 번호다. 지금은 41개라 번호가
밀렸을 수 있고 이번에 다시 안 셌다 — 남은 다섯이 지나는 recipe 를 세려면
번호부터 다시 매겨야 한다.

**남은 다섯은 전부 저쪽 것이고 우리가 못 채운다.** 채워지면 코드를 한 줄도
안 고치고 그대로 돈다 — 행정구역 · 문서가 이번에 그것을 증명했다. 배선은
2026-08-23 그대로인데 데이터가 들어오자 셋이 저절로 살아났다.

**이 다섯은 개편이 못 고친다.** 개편이 고치는 것은 "경로가 진짜인가" 이고
이것은 "데이터가 있는가 · 권한이 열렸는가" 다. 층이 다르다.

이것들이 0건일 때 그 이유는 답에 실린다 — `workflow_answer` 가 0건이면서
`warning` · `message` 가 온 응답의 그 문장을 건수 뒤에 붙인다.
`knowledge.query` 처럼 응답이 `[]` 하나인 것은 실릴 자리가 없어 "0건" 까지만
나온다 (다만 `knowledge.query` 는 이제 4건이 온다).

#### 아홉째 한계 — `geo.geocode` 가 늘 1.1km 다 (2026-08-25 「스물다섯째」)

0건이 아니라서 위 여덟에 안 들어가는데, 성격은 같다(저쪽 것이고 우리가 못
고친다). `ASAP-mcp/app/tools/geo.py:92-95` 가 bbox 를 `lon · lat ± 0.005`
상수로 만든다 — 무엇을 찾았든 약 1.1km 정사각형이다. VWorld 를
`type="place"` · `size="1"` 로 부르고(`geo.py:30·33`) `items[0]` 만
쓰므로(`geo.py:87`) `"청주시"` 는 행정구역이 아니라 그 안의 주소 한 곳이
걸린다.

**`geocode_place` 를 지나는 recipe 는 전부 1.1km 반경 작업이다.** 역 · 건물
이름에는 맞고 시 · 군 이름에는 안 맞는다. 0건이 아니라 **좁은 답이 조용히
나오는** 것이라 위 여덟보다 알아차리기 어렵다.

**시연 준비.** 「시 · 군 이름 + 좌표 경로」 발화(`"청주시 인구 알려줘"` 꼴)를
피하거나, 나온 답이 왜 하위 행정구역인지 설명할 준비가 필요하다. 역 이름
발화(`"오송역 …"`)는 안전하다.

**대안 하나를 미리 쟀다 (2026-08-27 「서른다섯째」).**
`adminBoundary.findBoundaryByPoint` 의 `items` 에 시도·시군구·읍면동의
**진짜 bbox 가 들어 있다.** 같은 검색을 1.1km · 시군구 · 시도 bbox 로 각각
부르면 — election 26→44→89건 · population 1→10→48건 · ev 3→500(상한)→500 ·
cctv 0→30→852건. **진짜 bbox 는 빠진 것(청주 네 구)을 후보에 들이지만 첫
결과를 물음에 맞추지는 못한다** — election 은 공약 한 건이 한 줄이고 정렬이
관련도가 아니라 첫 페이지 20건이 세 bbox 모두 보은군이다. ev·cctv 는 시도
bbox 에서 상한·이웃 도로 못 쓰게 된다. 게다가 geocode("청주")의 점 자체가
시내가 아니라 상당구 남일면에 떨어진다 — 보은군 첫 결과의 절반은 점 위치다.
표 전문은 「서른다섯째」에 있다.

### 화면 실측으로 셋이 한꺼번에 보였다 (2026-08-23 · 1회)

```
발화    "청주오스코 충전소 알려줘"
결과    SELECT recipe_048
        말한 장소 -> 장소 좌표 변환 -> 지점 행정구역 판별 -> 충전소 상세 조회
답      "충전소 상세 조회 기능이 아직 붙지 않아 실행할 수 없습니다."
        도구를 하나도 안 불렀다 (resolve 한 단계만)
```

**recipe_012 를 못 골랐다.** 그 menu 문장에 이름으로 찾는다는 말이 없다.

> **정정 (2026-08-23 밤, 기준선 측정).** 바로 위 두 문장 중 "죽은 능력" 이라는
> 판단은 틀렸다. 발화 7 "전기차 충전소 데이터 검색해줘" 가 `{012, 013}` 을
> 10/10 으로 후보에 올렸다.
>
> ```
> recipe_012 는 후보에는 오른다. 다만 recipe_013 과 영원히 비겨 SELECT 가 안 된다.
> 사람이 CLARIFY 에서 1번을 골라 실행하는 길이 생기면 그때 인자를 버리는 것이
> 실제로 보이게 된다. "죽은 능력" 이 아니라 "아직 안 드러난 오류" 다.
> "청주오스코 충전소 알려줘" 가 recipe_048 로 간 것과는 별개의 사실이다.
> ```
>
> 따라서 "실제로 사용자에게 보이는 오류는 B 부류다" 도 반만 맞다. A 부류는
> 안 보이는 것이 아니라 CLARIFY 에 가려 아직 안 보인 것이다.

> **해결 (2026-08-26 「스물일곱째」).** `recipe_013` 이 없어졌다.
> `ev.searchChargers` 가 저쪽이 폐기 예정이라 적어 둔 도구라 온톨로지에서
> 노드를 뺐고, 그것을 지나는 recipe 넷(옛 013 · 033 · 040 · 045)이 함께
> 사라졌다. **"영원히 비긴다" 는 상대가 없어졌다** — 발화 7 이 세 묶음
> 30회 전부 `{012}` 로 SELECT 다. 인자를 버리는 오류가 실제로 보이는지는
> 이제 CLARIFY 에 안 가리므로 화면에서 바로 확인할 수 있다. 아직 안 봤다.

**recipe_048 이 뽑혔다.** 「적을 수 없었던 경로」의 가짜 넷 중 하나다.
`find_admin_boundary_by_point` 가 내놓는 행정구역 코드와 `ev.getStation` 이 받는
충전소 번호를 온톨로지가 같은 `식별자` 타입으로 묶어 생긴 경로다.
`given` 이 `spoken_place` 로 갔다 — "청주오스코" 를 장소로 읽었고, `place_name`
이 지명 · 역 이름 · 구간 이름을 다 삼키는 문제와 같은 뿌리다.

**`unwired` 가 막았다.** "오송시" 사례와 정반대다. 그쪽은 전부 성공해서 틀린
답이 나갔고 이쪽은 정직하게 못 한다고 말했다. 반쪽 실행을 막아둔 판단이 값을
했다.

**1회다.** 반복해서 재지 않았으므로 비율로 읽으면 안 된다.

### 「찍은 지점」 시작 데이터 노드가 없다

저쪽 화면은 지도에서 찍은 점을 `context.selectedLocation` 으로 보내고 저쪽
프롬프트는 `$context.selectedLocation.lon` 을 쓴다. 우리는 그 칸을 안 읽는다.
그래서 "이 근처 CCTV 보여줘" 를 못 한다. 시작 데이터 노드를 더하면 `find_cctv`
가 혼자 첫 노드로 불리는 진짜 경우가 생기고, 그때 `$prev` 자리에 `$context` 가
들어가야 한다. 위 간선별 배선과 같은 자리에서 풀린다.

### 경로와 배선이 맞는지 세는 검사가 없다

recipe 를 배선표와 맞대어 A · B 를 세면 13개가 나온다. 스무 줄이면 된다.
개편 때 `tools/` 에 넣는다. 세는 규칙은 이렇다.

```
배선이 다 있는 recipe 만 본다 (unwired 가 빈 것)
첫 실행 노드인데 input 이 $prev 만 쓴다        -> A
앞 실행 노드가 있는데 input 이 @arg 만 쓴다    -> B
```

### 배선이 includeGeometry 를 안 보낸다 — 지도에 그려지는 것이 갈린다

2026-08-30 화면 실측 — 같은 발화의 두 후보가 지도에서 다르게 보인다.

```
"청주시 행정경계 보여줘" → 「1 행정구역 조회」
  adminBoundary.searchBoundaries  4건 · 청주시 상당구
  ★ 지도에 아무것도 안 그려진다

"청주시 행정경계 보여줘" → 「2 VWorld 행정경계 조회」
  vworld.getAdministrativeBoundaries  4건 · 청주시 상당구
  ★ 지도에 경계가 그려진다
```

까닭은 도구 설명에 있다 (저쪽 catalog, 읽기만 했다).

```
adminBoundary.searchBoundaries      기본은 코드 · 이름 · 계층 요약이고
                                    ★ GeoJSON 은 includeGeometry=true 일 때만 온다
vworld.getAdministrativeBoundaries  처음부터 GeoJSON 이다
```

우리 배선이 `includeGeometry` 를 안 보낸다. 「보여줘」 라는 발화에는 둘 다
그려지는 것이 맞아 보이는데 **배선은 발화를 모른다.**

**★ 「전부 · 전체」 류와 같은 자리다** — 도구에 있는 칸을 배선이 안 보낸다.
보내면 응답에 도형 좌표가 통째로 붙어 커진다. 어느 발화에 붙일지를 가르는
방법이 필요하다. **지금 안 한다.**

---

## 측정 기록

### 2026-08-31 (예순셋째) · 계기판이 조용히 죽은 세 번째 자리를 살리고, 두 길이 갈리는 것을 지키는 시험을 뒀다

무인 실행. 조건 — `integration/ASAP-Ontology` · `_init` (recipe 60) · 모델 서버
기본 · 좁히기 끔 · 지도 문맥 both · 8000 · 8501 떠 있는 채로 쟀다.
**서버가 쓰는 파일을 한 줄도 안 고쳤다** — 고친 것은 `tools/check_resolve.py`
(표를 찍는 자리만) 와 `tests/` 뿐이다.

**★ 이 기록은 「예순셋째」다. 예순한·예순두째는 `feature/dopamo` 브랜치에 있다**
(독파모 측정 둘). 그 브랜치를 못 보는 자리에서 작업했으므로 번호를 비워 두었다.
합칠 때 번호가 안 부딪힌다.

---

#### 1부 · `--only` 로 화면 발화만 돌리면 죽던 자리

**어느 칸이 없어서 죽었나 — 축도 후보 칸도 아니었다.**

들어가기 전 짐작은 「화면 발화라 축이나 후보 칸이 빈다」였다. **틀렸다.**
빈 칸은 하나도 없었다. 죽은 것은 **묶음 이름을 고르는 자리**다.

```
.venv/bin/python tools/check_resolve.py --only 32 --runs 1
  → _print_candidates 에서 KeyError: '확장 스물둘'
    판정 표까지는 다 찍히고 그 뒤에서 죽는다
```

`_groups()` 는 발화를 기준선 아홉 · 확장 스물둘 · 화면 다섯으로 가르고
**빈 묶음은 뺀다** (그 자리 docstring 에 적힌 규칙이다 — 소계와 합계가 같은
값으로 두 번 나오면 읽는 사람이 둘을 다른 것으로 본다). 그래서 `--only 32` 면
`totals` 에는 `화면 다섯` 칸 하나만 생긴다.

그런데 표를 채우는 쪽은 번호에서 이름을 **손으로** 골랐다.

```python
totals[BASELINE_LABEL if number <= BASELINE_LAST else EXTENSION_LABEL]
```

**두 갈래뿐이고 화면 갈래가 없다.** 32~36 이 전부 `확장 스물둘` 로 갔고,
화면만 돌리면 그 칸이 아예 안 만들어져 없는 칸을 찾았다.

같은 두 갈래가 **세 자리**에 있었다. `_print_candidates` 하나가 아니다 —
그것만 고쳤으면 바로 다음 표에서 또 죽었다.

```
1440  _print_candidates                    조회 판정 합계
1626  _print_narrow (좁히기 끔 갈래)       시간 합계     ← 기본 경로다
1658  _print_narrow (좁히기 켬 갈래)       좁히기 합계
```

옳게 하고 있던 자리가 이미 둘 있었다 (`_print_table` 1281 · `_print_execution`
1745). **둘 다 `_groups()` 를 그대로 돌 뿐 번호에서 이름을 되짚지 않는다.**
그래서 이 둘은 처음부터 화면 다섯을 제대로 찍고 있었다.

**어떻게 살렸나.** `_groups` 옆에 `_group_label(number)` 하나를 두고 — 같은
경계를 쓰는 세 갈래다 — 손으로 고르던 세 자리를 그것으로 바꿨다. 없는 칸을
건너뛰거나 빈 값으로 찍는 미봉이 아니라, **`_groups` 가 만든 칸과 `_group_label`
이 고른 이름이 서로 어긋날 수 없게** 한 것이다. 발화가 있으면 그 묶음은 비지
않고, 비지 않으면 `_groups` 가 그 이름을 내주므로 KeyError 가 날 수 없다.

**★ 표의 다른 칸은 안 건드렸다.** 아래에 그것을 증명한 것을 적는다.

#### 1부 · 판정이 안 달라졌다는 증명 — 같은 자료를 옛/새 코드에 넣어 견줬다

「표를 찍는 코드만 고쳤으니 판정 숫자가 안 달라져야 한다」를 벽시계나 기억으로
말하지 않으려고, **고치기 전 사본과 고친 사본에 똑같은 가짜 자료를 넣어 표
여섯을 통째로 diff 했다.** 서버를 안 부르므로 LLM 잡음이 안 섞인다.

```
적중 표 (판정 넷)        한 글자도 안 다르다
축 표                    한 글자도 안 다르다
검산 표                  한 글자도 안 다르다
실행 표                  한 글자도 안 다르다
후보 표 · 좁히기 표      합계 줄 둘만 다르다 (아래)
```

```
- 조회 판정 · 확장 스물둘  조회 적중 27회 …  (합 27회)
+ 조회 판정 · 확장 스물둘  조회 적중 22회 …  (합 22회)
+ 조회 판정 · 화면 다섯    조회 적중 5회  …  (합 5회)

- 시간 · 확장 스물둘  /resolve 한 번 평균 1.0초 (합 27회)
+ 시간 · 확장 스물둘  /resolve 한 번 평균 1.0초 (합 22회)
+ 시간 · 화면 다섯    /resolve 한 번 평균 1.0초 (합 5회)
```

**27 = 22 + 5 다.** 판정이 바뀐 것이 아니라, 화면 다섯이 확장에 섞여 세어지던
것이 제 칸으로 갈라져 나온 것이다. 이것은 되돌릴 것이 아니라 이 파일이
`BASELINE_LAST` 옆 주석에 스스로 적어둔 규칙이다 — 「그 묶음의 점수가 두 종류의
발화를 섞은 값이 되면 『화면 발화가 지금 어떤가』를 물을 자가 없어진다」.
**죽지 않던 전체 실행에서도 이 두 줄은 조용히 틀린 값을 찍고 있었다.**

#### 1부 · 실측 — `--only` 다섯과 전체 하나

```
--only 1 · 20 · 32 · 33 · 36 (각 --runs 1)   다섯 다 표가 끝까지 · exit 0
--only 32 --narrow --runs 1                  좁히기 켬 갈래도 끝까지 · exit 0
```

전체 `--runs 3` (발화 36 × 3 = 108회, 한 번만 돌렸다) —

```
                     LLM 단독   적중      근접  빗나감  못 붙음
기준선 아홉          18/27      15/27     9     3       0        56%
확장 스물둘          24/66      39/66     15    11      1        59%
화면 다섯            12/15      15/15     0     0       0        100%
합계                 54/108     69/108    24    14      1        64%

조회 판정 · 화면 다섯   조회 적중 9회 · 조회 근접 6회 · 조회 빠짐 0회
시간 · 화면 다섯        /resolve 한 번 평균 4.1초 (합 15회)
```

**★ 마지막 줄이 이번에 되찾은 것이다.** 「예순한째」는 이 값이 안 찍혀 화면
발화의 시간을 벽시계로 어림해야 했다. 이제 표가 직접 낸다. 기준선 4.3초 ·
확장 4.7초 · 화면 4.1초로, 화면 발화가 다른 둘보다 느리지 않다.

#### 1부 · 자체 검사를 둘지 — **뒀다**

`check_inputs` 에는 「쉰아홉째」가 `_selfcheck()` 를 뒀고 pytest 한 줄이 그것을
부른다. `check_resolve` 에는 그런 것이 **없었다.** 두기로 정했다.

**왜 두는가.**

```
1  ★ 계기판이 조용히 죽은 것이 이번이 세 번째다
     check_argument   나흘   ValueError   (「스물다섯째」)
     check_inputs     하루   KeyError     (「쉰째」 → 「쉰아홉째」에서 살림)
     check_resolve    ★ 이번  KeyError
   세 번 다 판정은 다 해 놓고 **표를 찍는 마지막에** 죽었다. 우연이 아니다
2  이 도구는 어쩌다 한 번, 그것도 서버를 띄워야 돈다. 그런데 발화 목록과
   묶음 경계는 커밋마다 바뀐다. 커밋마다 도는 것은 pytest 다
3  이번 고장의 원인이 바로 「발화가 늘어 묶음이 셋이 됐는데 이름을 고르는
   자리가 둘에 머물렀다」다. 발화는 앞으로도 는다. 같은 고장이 또 난다
4  「테스트를 어떻게 쓰는가」의 기준을 지난다 — **화면을 봐도 확인할 수 없는
   것.** 도구가 죽은 것은 그 도구를 돌리기 전에는 아무도 모른다
```

**옛 항목이 「`tools/` 는 테스트를 두지 않는다가 규칙이라 넣을지는 사람이
정한다. 이번에 안 넣었다」로 남아 있었지만, 「쉰아홉째」가 `check_inputs` 에
이미 선례를 만들었다.** 그 선례를 따랐다.

**무엇을 보는가.** 「쉰아홉째」의 교훈 — 손으로 적은 것만 보는 검사는 정작 그
파일의 KeyError 를 못 잡았다 — 을 그대로 가져왔다. 그래서 이 검사는
**진짜 `UTTERANCES` 를 쓴다.**

```
묶음 조합 일곱 가지(기준선 · 확장 · 화면 · 그 짝 셋 · 셋 다)를 각각
--only 처럼 잘라, 표 여섯을 좁히기 끔 · 켬 두 갈래로 다 찍어 본다
찍은 것은 버린다. 보는 것은 「죽지 않는가」다
번호 → 이름이 _groups 의 경계와 어긋나지 않는지도 함께 본다
★ 서버 · Gateway · 온톨로지를 안 부른다. 0.3초에 끝난다
```

**음성 대조군을 함께 뒀다.** `_group_label` 을 옛 두 갈래로 되돌리면 자체
검사가 **죽어야** 한다. 이것이 없으면 검사가 통과할 때 그것이 「버그를
잡아서」인지 「아무것도 안 봐서」인지 못 가른다. 되돌려 보니 발화 32 에서
`AssertionError: (32, '화면 다섯', '확장 스물둘')` 로 죽는다 — 잡는다.

**이 검사가 안 보는 것.** 판정이 맞는지는 안 본다. 값이 모양만 맞는 가짜라
「죽지 않는가」만 잰다. 판정은 실측이 보는 것이다.

---

#### 2부 · `/chat` 과 `/chat/stream` 이 같은 답을 내는지 지키는 시험

**무엇이 문제였나.** 저쪽 화면은 `POST /chat/stream` 을 쓰고 — 시연에서 도는
길이 이쪽이다 — `check_resolve --execute` 는 `POST /chat` 을 쓴다. 둘은 같은
흐름(`_chat_events`)을 쓰므로 지금은 같은 답이 나오고, `main.py` 주석도
「두 경로가 다른 답을 하면 화면과 curl 중 무엇을 믿을지가 갈린다」라고 적고
있다. **경고만 있고 지키는 장치가 없었다.** `tests/` 에 `chat/stream` 을
건드리는 시험이 **0건**이라 누가 한쪽만 고쳐도 아무도 안 잡았다.

**무엇을 지키는가** — `tests/demo/api/test_chat_stream_parity.py`, 여섯 줄.

```
answer 가 두 길에서 같다
commands 가 두 길에서 같다     ★ 지도 명령이 어긋나면 화면에만 안 그려진다.
                                answer 로는 안 잡히는 자리다
/chat 은 중간 이벤트를 버리고 result 만 준다   ← 두 길의 **의도된** 차이.
                                이것까지 같아지면 SSE 를 쓸 까닭이 없다
SSE 틀 — "data: " 접두어 · 이벤트마다 빈 줄 · 마지막 [DONE]
단계 이벤트가 순서대로 흐르고 result 가 마지막이다
한글이 이스케이프 없이 그대로 나간다   ← main.py 가 적은 ensure_ascii 제약
```

**대역을 어떻게 했나 — 이미 있는 것을 썼다.** `tests/demo/api/test_recent.py`
가 「`/chat` 이 내는 이벤트를 가짜로 흘려 넣는」 관례를 이미 갖고 있었다
(`executed()` 의 step_start · step_end · result 목록). **그 모양을 그대로
가져왔다.** 한글과 좌표를 일부러 담아 `ensure_ascii` 와 `commands` 를 함께
본다.

바꾼 것은 `main._chat_events` **하나뿐**이다. 창구 둘은 그것을 모듈 전역으로
찾으므로 한 자리만 바꾸면 둘이 같은 입력을 받는다. **두 창구에 서로 다른
대역을 물리지 않는 것이 이 시험의 전부다** — 같은 입력에서 같은 답이 나오는지를
보는 것이라 입력이 갈리면 아무것도 못 잰다. 그래서 창구 두 개의 진짜 코드
(`/chat` 의 result 추리기 · `/chat/stream` 의 SSE 직렬화)는 그대로 돌고,
LLM · 온톨로지 · vendor 실행기는 안 돈다.

**★ 진짜 서버를 띄워 부르는 시험은 안 만들었다. 왜인가.**

```
1  시연 중에 pytest 가 돌면 8000 을 쓰는 저쪽 화면과 부딪힌다
2  LLM 이 회차마다 다른 답을 내므로 시험이 답의 내용에 흔들린다.
   여기서 볼 것은 「두 길이 같은가」지 「답이 맞는가」가 아니다
3  서버가 안 떠 있으면 빨간불이 나는데, 그것은 코드가 깨진 것이 아니다.
   커밋마다 도는 시험이 그러면 아무도 안 믿게 된다
```

`TestClient` 는 프로세스 안에서 앱을 부르므로 셋 다 피한다.

**음성 대조군으로 확인했다** (저장소 밖 임시 파일로 돌렸고 남기지 않았다).

```
두 창구가 다른 answer 를 내게 만들었다   → parity 시험이 죽는다   ✔
ensure_ascii=True 로 바꿔 봤다           → 한글 시험이 죽는다     ✔
```

---

#### 관문

```
서버가 쓰는 파일 무손상   git status 에 tools/check_resolve.py · tests/ 둘 ·
                          NOTES.md 말고 없다. demo · vendor · ontology ·
                          workflows · models.yaml 변경 0
--only 가 산다            발화 다섯 + --narrow 까지 여섯 다 끝까지 · exit 0
판정 무손상               적중 표가 옛/새 코드에서 한 글자도 안 다르다 (위 증명)
pytest                    453 passed / 1 failed
                          445 + 새 시험 8 (자체 검사 2 · parity 6). 기존 삭제 0
                          실패 1 은 graphviz 음성 대조군 — 알려진 것이고 안 고쳤다
저쪽 무결                 KRRI_ASAP/ 에 내가 만든 변경 0
서버                      8000 · 8501 띄운 채로 뒀다
```

#### 안 한 것

```
★ 지시가 「열린 과제」의 두 항목(「--only 가 죽는다」·「/chat 과 stream 이
  같은지 지키는 것이 없다」)을 고치라 했는데, **이 브랜치의 NOTES.md 에는 그
  문구의 항목이 둘 다 없었다.** 까닭은 나중에 밝혀졌다 — 그 두 항목은
  예순한·예순두째와 함께 `feature/dopamo` 브랜치에 있고, 여기서는 안 보인다.
  값을 지어내지 않기로 했으므로 없는 항목을 고쳐 적지 않았다. 대신
    - 이미 있던 「재는 도구가 조용히 죽는다」에 세 번째 사례와 결말을 붙였다
    - `/chat`·stream 쪽은 항목이 아예 없어 새로 세우고 곧바로 닫았다
  **합칠 때 저쪽의 두 항목과 여기 둘이 겹친다. 닫는 것은 이쪽이 맞다** —
  고친 코드가 이 브랜치에 있다

check_argument · check_wiring 에는 자체 검사를 안 뒀다. 이번에 죽은 것은
  check_resolve 이고, 셋을 한꺼번에 손대면 범위를 넘는다. check_argument 는
  「스물다섯째」에 이미 살렸다

_print_axes · _print_verdict_changes 는 안 고쳤다 — 묶음을 안 쓰므로 이
  고장에 해당이 없다. 확인만 하고 뒀다

전체 --runs 3 을 한 번만 돌렸다 (지시대로). 잡음 폭은 「마흔두째」가 적어둔
  대로 크므로 이 숫자를 다른 날과 곧바로 견주지 않는다

push 하지 않았다. 커밋만 했다
```

### 2026-08-30 (예순째) · 보이는 범위가 안 먹던 한 줄을 고쳤다 — 다섯이 아니라 하나였다

무인 실행. 조건 — 배선 STEP_OF 36줄 · 스키마 원천 `KRRI_ASAP/ASAP-mcp/main.py`
(읽기만 했다) 와 `tools/probe_out/tools.json` (2026-08-27 수신) · 8000 · 8501
떠 있는 채로 `/chat` 을 눌렀다. 문맥은 저쪽 `ChatRequest` 모양 그대로 보냈다.

**왜 했나.** 「쉰아홉째」가 죽어 있던 `check_inputs` 를 살렸고, 살리자마자
판정 51행 중 **유일한 빨간불**로 이 자리가 잡혔다. 그 뒤 사람이 저쪽 화면에서
눌러 확인했다 — "지금 보이는 곳 충전소 찾아줘" 가 지도를 아무리 좁혀도
500건이었다. 계기판이 죽어 있던 동안 이 고장은 숨어 있었다. 배선이 프롬프트에
안 실려 판정 지표로는 영영 안 보이고, 답 문구도 "조회했습니다" 라 멀쩡해
보였기 때문이다. **정적 계기판이 아니었으면 못 찾았을 자리다.**

#### 스키마 확인 표 — 고치기 전에 원천을 읽었다

`ASAP-mcp/main.py` 의 `inputSchema` 를 도구마다 직접 읽었다. 줄 번호는
`"name"` 이 있는 줄이다.

| 도구 | 배선 줄 | 스키마가 받는 범위 칸 | 우리가 보내던 것 | 판정 |
|---|---|---|---|---|
| `ev.searchStations` (532) | `search_ev_stations × map_extent` 첫 자리 | **`minLon` · `minLat` · `maxLon` · `maxLat` 평평한 넷** (넷 다 optional) | `{"bbox": [네 수]}` | **★ 없는 칸. 고쳤다** |
| `geo.getRailwayLines` (272) | `get_railway_lines × map_extent` | `bbox` 배열 (minItems/maxItems 4) | `{"bbox": [네 수]}` | 맞다. **안 고쳤다** |
| `adminBoundary.searchBoundaries` (307) | `search_admin_boundaries × map_extent` | `bbox` 배열 | `{"bbox": [네 수]}` | 맞다. **안 고쳤다** |
| `vworld.getAdministrativeBoundaries` (436) | `get_vworld_boundaries × map_extent` | `bbox` 배열 | `{"bbox": [네 수]}` | 맞다. **안 고쳤다** |
| `population.searchStatistics` (364) | `search_population_statistics × map_extent` | `bbox` 배열 | `{"bbox": [네 수]}` | 맞다. **안 고쳤다** |
| `road.getCctv` (249) | `find_cctv × map_extent` | 평평한 넷 (넷 다 **required**) | 평평한 넷 | 맞다. **본보기였다** |

**「같은 자리가 다섯」이 아니었다. 한 줄이다.** 사람이 적어 온 지시에는 지도
계열 다섯이 전부 평평한 넷을 받으니 다섯 줄이 같이 고장이라고 되어 있었다.
원천을 읽으니 아니다 — 넷은 `bbox` 배열 칸을 정식으로 갖는다. 지시에 있던
「다섯 중 bbox 배열을 받는 것이 섞여 있으면 그 줄은 안 고친다」 를 따랐고,
「쉰아홉째」가 이미 같은 답을 적어 둔 것과도 맞는다 (「열린 과제」의 길 가)).

선거 계열 셋(`election.searchAssemblyDistricts` 642 ·
`searchAssemblyPledgeDistricts` 702 · `searchLocalPledgeSummaries` 766)도
`bbox` 배열이라 그대로 뒀다.

#### 사람이 잘못 짚었고 스키마가 바로잡았다

사람이 준 지시에는 「같은 자리가 다섯」이라고 적혀 있었다. 원천 스키마를
도구마다 잘라 읽어 보니 하나였다.

```
ev.searchStations                  bbox 칸 없음 · 평평한 넷      ★ 고친 자리
road.getCctv                       평평한 넷 (required)          원래 맞았다
geo.getRailwayLines                bbox 배열을 정식으로 받는다   안 고쳤다
adminBoundary.searchBoundaries     "
vworld.getAdministrativeBoundaries "
population.searchStatistics        "
```

**어쩌다 그렇게 됐나 (사람이 스스로 적어 온 경위다).** grep 으로 확인할 때
도구 경계를 안 잘라 **옆 도구의 `bbox` 를 같이 셌다.** `main.py` 에서
`road.getCctv`(249) 바로 다음이 `geo.getRailwayLines`(272)인 것처럼 도구가
잇달아 있어서, 한 도구의 `inputSchema` 를 벗어난 줄이 같은 덩어리로 보였다.

**★ 지시에 있던 「bbox 배열을 받는 것이 섞여 있으면 그 줄은 안 고친다」가
넷을 지켰다.** 사람이 결론을 잘못 짚고도 확인 절차를 함께 적어 둔 덕에
넷이 안 깨졌다. `check_inputs` 도 「없는 칸 1」로 같은 답을 냈다 —
사람 · 원천 · 계기판 셋이 한 자리에서 만났다.

**★ 눌러 보니 넷은 원래부터 범위가 먹고 있었다.** 좁게↔넓게 건수가 갈린다
(인구 2↔100 · 행정경계 2↔20 · 노선 50↔3683). 「스키마상 통할 것」에서
「눌러 보니 통한다」로 옮겨졌다.

**제주 증거.** 넓게 둔 「전체」가 92,821 이 아니라 **89,194** 다 — 넓은
사각형([[126.00, 34.00], [129.50, 38.20]])도 제주(위도 33.2~33.6)를 안 덮는다.
상한 500 에 걸린 수가 아니라 **범위가 세는 데까지 먹은 수**라서, 500건이라는
같은 숫자가 고치기 전과 뜻이 다르다는 것을 이 한 자리가 갈라 준다.

#### 앞 단계에서 오는 자리 — 왜 첫 자리만 틀렸나

`("search_ev_stations", "map_extent")` 의 `input`(앞 단계에서 오는 쪽)은
`center` + `radiusMeters` + `point_radius_to_bbox` 어댑터다. 어댑터가 평평한
넷으로 바꿔 내보내므로 **그쪽은 처음부터 맞았다.** 「오송역 근처 충전소」가
잘 돌던 것이 그 증거다 (아래 실측에서 다시 확인했다).

**왜 어긋났나 — 배선 주석에 근거가 있다.** `a238b48` 이 `input_first` 열다섯
줄을 한 번에 더하면서 **옆의 `input` 이 쓰는 모양을 그대로 베끼고 `$prev` 만
문맥으로 바꿨다.** 그 규칙이 나머지 열넷에는 맞았다 — 그 도구들의 `input` 이
이미 `{"bbox": BBOX_FROM_PREVIOUS}` 이거나(선거 · 노선 · 행정경계 · 인구)
평평한 넷이었다(`road.getCctv`). **이 줄만 옆의 `input` 이 center + 반경 +
어댑터라 베낄 `bbox` 꼴이 없었고**, 그래서 옆줄 대신 일반 관용구인
`{"bbox": …}` 로 적었다. 바로 위 주석에는
"`ev.searchStations` 는 bbox 를 평평한 네 수로 받는다" 고 **옳게** 적어
놓고도 줄이 그것을 안 따랐다. 주석이 맞고 코드가 틀린 자리였다.

#### 고친 다섯 줄 — 실은 한 줄이다

```diff
   ("search_ev_stations", "map_extent"): {
       "input": {"center": f"{PREVIOUS_STEP}.location", "radiusMeters": RADIUS_METERS},
       "adapter": POINT_RADIUS_TO_BBOX,
-      "input_first": {"bbox": BBOX_FROM_CONTEXT},
+      "input_first": {
+          "minLon": BBOX_FROM_CONTEXT[0],
+          "minLat": BBOX_FROM_CONTEXT[1],
+          "maxLon": BBOX_FROM_CONTEXT[2],
+          "maxLat": BBOX_FROM_CONTEXT[3],
+      },
   },
```

`BBOX_FROM_CONTEXT` 의 순서가 `minLon · minLat · maxLon · maxLat` 인 것을
확인하고 적었다(시험으로도 박았다). CCTV 줄과 같은 꼴이고 새 방식을 안 만들었다.
주석에는 왜 어긋났는지와 **나머지 넷을 왜 안 고쳤는지**를 적었다.

#### ★ 화면 실측 일곱 (문구 그대로)

좁게 = `[[127.28, 36.61], [127.32, 36.63]]` (오송역 둘레 약 3.6km × 2.2km) ·
넓게 = `[[126.00, 34.00], [129.50, 38.20]]` (남한 거의 전부).

```
지금 보이는 곳 충전소 찾아줘   ★ 고친 자리
  좁게  전기차 충전소를 조회했습니다.
        1. ev.searchStations  minLon=127.2800 · minLat=36.6100 ·
           maxLon=127.3200 · maxLat=36.6300  36건 · 청주시 한국전기공사협회중앙회
  넓게  전기차 충전소를 조회했습니다.
        1. ev.searchStations  minLon=126.0000 · minLat=34.0000 ·
           maxLon=129.5000 · maxLat=38.2000  500건 (전체 89,194건) · 영동지사
  ★ 36 ↔ 500. 범위가 먹는다. 고치기 전에는 좁게 둬도 500건(전체 92,821건)이었다
    ★ 넓게 둔 「전체」가 92,821 이 아니라 89,194 인 것 자체가 증거다 —
      넓은 사각형도 제주(위도 33.2~33.6)를 안 덮는다. 범위가 세는 데까지 먹었다

지금 보이는 곳 인구 알려줘
  좁게  인구 통계를 조회했습니다.
        1. population.searchStatistics  2건 · 세종특별자치시 390,923명
           (남 194,731 · 여 196,192) · 2026-06-30 기준
  넓게  인구 통계를 조회했습니다.
        1. population.searchStatistics  100건 (전체 249건) · 화성시 998,746명
           (남 517,392 · 여 481,354) · 2026-06-30 기준
  ★ 2 ↔ 100(전체 249). 안 고쳤는데 원래 먹고 있었다. 스키마 확인 표대로다

지금 보이는 곳 행정경계 보여줘
  좁게  행정구역 경계를 조회했습니다.
        1. adminBoundary.searchBoundaries  2건 · 세종특별자치시
  넓게  행정구역 경계를 조회했습니다.
        1. adminBoundary.searchBoundaries  20건 (전체 249건) · 종로구
  ★ 2 ↔ 20(전체 249). 안 고쳤는데 원래 먹고 있었다

지금 보이는 곳 노선 보여줘
  좁게  철도 노선을 조회했습니다.
        1. geo.getRailwayLines  50건
  넓게  철도 노선을 조회했습니다.
        1. geo.getRailwayLines  3683건
  ★ 50 ↔ 3683. 안 고쳤는데 원래 먹고 있었다

지금 보이는 곳 CCTV 보여줘   ★ 원래 잘 되던 것
  좁게  찾지 못했습니다.
        1. road.getCctv  minLon=127.2800 · minLat=36.6100 ·
           maxLon=127.3200 · maxLat=36.6300  0건
  넓게  CCTV 를 조회했습니다.
        1. road.getCctv  minLon=126.0000 · minLat=34.0000 ·
           maxLon=129.5000 · maxLat=38.2000  4696건
  ★ 0 ↔ 4696. 안 깨졌다. 좁게 0건인 것은 그 사각형에 CCTV 가 없어서다 —
    같은 좌표로 오송역 반경(아래)을 재면 83건이 나오므로 범위 탓이 맞다
```

**앞 단계에서 오는 자리 둘 — 안 깨졌다.**

```
오송역 근처 충전소 자세히 알려줘   ★ 4단 사슬
  오송역 충전소 상세를 조회했습니다.
  1. geo.geocode       오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1
                       (127.3277, 36.6200)
  2. ev.searchStations  minLon=127.1598 · minLat=36.4852 · maxLon=127.4956 ·
                        maxLat=36.7547  500건 (전체 2,173건) · 청주오송역차고지
  3. ev.getStation     statId="PK000313"  8건
  ★ 500건(전체 2,173건). 예전과 같다

오송역 CCTV 보여줘
  오송역 CCTV 를 조회했습니다.
  1. geo.geocode       오송역 → … (127.3277, 36.6200)
  2. road.getCctv      minLon=127.1598 · minLat=36.4852 · maxLon=127.4956 ·
                       maxLat=36.7547  83건
  ★ 83건. 지시에 적혀 온 수는 82 였다. CCTV 배선은 한 글자도 안 건드렸고
    (diff 로 확인) 저쪽 CCTV 목록이 실시간이라 ±1 은 저쪽 데이터다.
    83 은 「마흔여섯째」에 적힌 수와 같다
```

#### 관문

```
★ 화면 실측 일곱      다섯이 범위를 쓰고(좁게 ↔ 넓게 건수가 갈린다),
                      앞 단계에서 오는 둘이 안 깨졌다
check_inputs          없는 칸 1 → 0. 맞다 50 → 51 · 안 보낸 required 0
                      안 쓰는 칸 306 → 302 (★ 122 → 119)
                      ★ 51행 중 빨간불이 하나도 안 남았다
판정 무손상           check_resolve --runs 3
                      적중 69/108 (64%) · 근접 19 · 빗나감 20 · 못 붙음 0
                      LLM 단독 50/108
                      ★ 고치기 전과 **표가 한 칸도 안 달라졌다** (발화별 행까지 같다)
                      검산 표만 「바꿨지만 판정은 같다」가 26 → 28 회 —
                      판정이 같다고 그 표가 스스로 이름 붙인 자리이고
                      발화 1 의 회차 노이즈다. 배선은 프롬프트에 안 실린다
check_wiring          recipe 60 · 배선 59 · STEP_OF 36줄 · A 0 · B 0 · C 1
                      (C 는 web_fetch × web_address. 예전 그대로)
pytest                445 passed / 1 failed
                      실패 1건은 graphviz 음성 대조군
                      (test_dense_graph_would_move_if_overlap_removal_were_used).
                      고치기 전 435/1 에서 **더한 만큼만 늘었다. 기존 삭제 0**
저쪽 무결             KRRI_ASAP/ · vendor/ 변경 0
온톨로지 등 무손상     ontology/*.yaml · workflows/ · demo/graph_svg/ ·
                      demo/ui/ · tools/ 변경 0
서버                  8000 (uvicorn --reload) · 8501 (streamlit) 떠 있다
```

#### 더한 시험 열

`tests/demo/api/test_step_input_schema.py` (여덟, 새 파일) —
**STEP_OF 전 줄이 보내는 칸이 그 도구 스키마에 있는가**를
`tools/probe_out/tools.json` 으로 대조한다. LLM 도 서버도 안 부른다.
어댑터가 걸리는 줄(`point_radius_to_bbox`, 적어 둔 것과 vendor 가 저절로 거는
것 둘 다)은 `check_inputs.sent_fields` 와 같은 규칙으로 뺀다. 나머지 일곱은
ev 첫 자리가 평평한 넷인 것 · ev 앞 단계 자리가 안 달라진 것 ·
**bbox 배열을 받는 넷은 그대로 `bbox` 한 칸인 것**(같이 고치면 깨지는 자리라
확인을 박아 뒀다) · `BBOX_FROM_CONTEXT` 의 순서다.

`tests/demo/api/test_map_context.py` (둘 더함, 기존 아홉 그대로) —
plan 이 실제로 내놓는 값으로 본다. 보이는 범위로 시작하는 충전소가 평평한 넷을
받는 것과, 노선이 그대로 `bbox` 한 칸을 받는 것.

**음성 대조군으로 확인했다.** 고친 줄을 잠깐 되돌려 놓으니
`test_배선이_보내는_칸이_전부_스키마에_있다` 와
`test_ev_충전소는_첫_자리도_평평한_넷으로_보낸다` 둘이 곧바로 깨졌고,
되돌리니 여덟이 다시 통과했다. 시험이 진짜로 이 고장을 잡는다.

#### 안 한 것

- **나머지 넷을 평평한 넷으로 통일하지 않았다** (「열린 과제」의 길 나)).
  스키마상 `bbox` 배열이 정식이고 `_parse_bbox_input` 이 둘 다 받으므로
  고쳐도 달라지는 것이 없다. 고장이 아닌 자리를 만지지 않았다.
- 온톨로지 · menu · 정답표 · 프롬프트 넷 · `demo/api/services/` 의 다른 파일 ·
  `demo/graph_svg/` · `demo/ui/` · `vendor/` · `KRRI_ASAP/` 를 안 건드렸다.
  `KRRI_ASAP/ASAP-mcp/main.py` 는 **읽기만** 했다.
- 커밋하지 않았다. 브랜치는 `integration/ASAP-Ontology` 그대로다.
- `check_resolve` 를 `--execute` 로는 안 돌렸다. 실행 쪽은 `/chat` 을 직접
  일곱 번 눌러 봤고 그것이 더 곧은 증거다.
- 좁게 둔 CCTV 가 0건인 것을 더 파고들지 않았다. 같은 사각형에서 충전소는
  36건이 나오므로 배선 문제가 아니라 그 범위에 CCTV 가 없는 것이다.

### 2026-08-30 (쉰아홉째) · 죽은 check_inputs 를 살리고 배선이 안 보내는 칸을 한 번에 셌다

무인 실행. 조건 — 스키마는 `tools/probe_out/tools.json` (2026-08-27 수신,
도구 42개. Gateway 가 꺼져 있어 다시 못 받았다) · 배선 STEP_OF 36줄 ·
`python tools/check_inputs.py` 서버 없이. LLM 판정과 무관한 정적 셈이라
모델 · 반복 횟수가 없다. **제품 코드 · 배선 · 온톨로지 · 프롬프트 0줄 변경** —
그래서 check_resolve 는 안 돌렸다 (프롬프트에 닿는 것이 없다).

#### 1. 무엇이 왜 죽어 있었나

「쉰째」에 적힌 그대로다 — `rows_of` 가 `TOOL_OF[node]["tool"]` 을 무조건
읽는데, 「마흔아홉째」가 TOOL_OF 를 「실행 수단」으로 넓히면서 tool 없이
command 만 있는 줄(show_facility)이 생겼다. `KeyError: 'tool'` 로 표가 한 줄도
안 나왔다. 재현했다 — 첫 실행에서 같은 자리, 같은 오류.

#### 2. 어떻게 살렸나 — 세는 법 둘을 정했다

```
지도 명령 줄   tool 이 없는 줄은 판정에서 갈라 「지도 명령 줄」로 따로 센다.
               맞댈 inputSchema 가 없다 — 그 args 의 계약은 저쪽 화면(useChat)이다.
               표 끝에 몇 줄인지 찍는다. 지금 1줄 (show_facility × place_name).

값 갈래       「살릴 때 정한다」던 철도 줄(arg_field)은 input_first 전례대로
               **갈래마다 한 행**으로 셌다. 기본 갈래(stationName) 한 행 ·
               "…선" 갈래(railwayName) 한 행. 「쉰째」 9절의 「표에 stationName
               한 칸만 실린다」가 이것으로 풀렸다.
```

#### 3. 자체 검사를 어떻게 고쳤나

_selfcheck 가 이번 고장을 못 잡은 구멍은 「손으로 적은 배선만 보고 진짜 표는
한 줄도 안 읽는다」였다. **「TOOL_OF · STEP_OF 의 모든 줄을 한 번씩 읽어
본다」를 더했다** — 빈 스키마로 `rows_of({})` 를 끝까지 돌리고, 모든 줄이
판정 행 아니면 지도 명령 줄에 정확히 한 번 나오는지 본다. 서버 · tools.json
없이 돈다. **일부러 깨뜨려 확인했다** — 갈라 세는 분기를 지우고 돌리니
_selfcheck 가 표를 찍기 전에 같은 KeyError 로 죽는다. 되돌렸다.

시험 하나를 더했다 — `tests/tools/test_check_inputs_selfcheck.py` 가
`_selfcheck()` 를 부른다. 근거 : 계기판이 죽는 순간은 배선표가 바뀌는 커밋인데
이 도구는 어쩌다 한 번 돌고, 커밋마다 도는 것은 pytest 다. check_argument
나흘 · check_inputs 하루가 그렇게 새어 나갔다. tests/tools/ 전례는
「쉰여섯째」의 test_execution_column 이다. pytest 434 → 435 passed.

#### 4. 표 — 도구 · 배선 줄 · 보내는 칸 · 판정 (2026-08-30)

`python tools/check_inputs.py` 출력 그대로다 (스키마의 칸 절만 뺐다. 도구가
언제든 다시 찍는다).

```
  도구                                   배선 줄 (노드 × 받는 타입)                       보내는 칸                        판정
  adminBoundary.findBoundaryByPoint      find_admin_boundary_by_point × point             lat, lon                         맞다 · 안 쓰는 칸 4
  adminBoundary.findBoundaryByPoint      find_admin_boundary_by_point × point (첫)        lat, lon                         맞다 · 안 쓰는 칸 4
  adminBoundary.searchBoundaries         search_admin_boundaries × keyword                query                            맞다 · 안 쓰는 칸 10
  adminBoundary.searchBoundaries         search_admin_boundaries × map_extent             bbox                             맞다 · 안 쓰는 칸 10
  adminBoundary.searchBoundaries         search_admin_boundaries × map_extent (첫)        bbox                             맞다 · 안 쓰는 칸 10
  election.findAssemblyDistrictByPoint   find_assembly_district_by_point × point          lat, lon                         맞다 · 안 쓰는 칸 6
  election.findAssemblyDistrictByPoint   find_assembly_district_by_point × point (첫)     lat, lon                         맞다 · 안 쓰는 칸 6
  election.findAssemblyPledgeDistrictByPoint  find_assembly_pledge_district_by_point × point       lat, lon                맞다 · 안 쓰는 칸 6
  election.findAssemblyPledgeDistrictByPoint  find_assembly_pledge_district_by_point × point (첫)  lat, lon                맞다 · 안 쓰는 칸 6
  election.findDistrictByPoint           find_election_district_by_point × point          lat, lon                         맞다 · 안 쓰는 칸 1
  election.findDistrictByPoint           find_election_district_by_point × point (첫)     lat, lon                         맞다 · 안 쓰는 칸 1
  election.findLocalPledgeSummaryByPoint find_local_pledge_summary_by_point × point       lat, lon                         맞다 · 안 쓰는 칸 2
  election.findLocalPledgeSummaryByPoint find_local_pledge_summary_by_point × point (첫)  lat, lon                         맞다 · 안 쓰는 칸 2
  election.getAssemblyDistrict           get_assembly_district × district_code            name                             맞다 · 안 쓰는 칸 2
  election.getAssemblyPledgeDistrict     get_assembly_pledge_district × district_code     name                             맞다 · 안 쓰는 칸 3
  election.getDistrict                   get_election_district × district_code            name                             맞다 · 안 쓰는 칸 2
  election.getLocalPledgeSummary         get_local_pledge_summary × admin_code            sidoCode                         맞다 · 안 쓰는 칸 2
  election.searchAssemblyDistricts       search_assembly_districts × keyword              query                            맞다 · 안 쓰는 칸 9
  election.searchAssemblyDistricts       search_assembly_districts × map_extent           bbox                             맞다 · 안 쓰는 칸 9
  election.searchAssemblyDistricts       search_assembly_districts × map_extent (첫)      bbox                             맞다 · 안 쓰는 칸 9
  election.searchAssemblyPledgeDistricts search_assembly_pledge_districts × keyword       query                            맞다 · 안 쓰는 칸 12
  election.searchAssemblyPledgeDistricts search_assembly_pledge_districts × map_extent    bbox                             맞다 · 안 쓰는 칸 12
  election.searchAssemblyPledgeDistricts search_assembly_pledge_districts × map_extent (첫)  bbox                          맞다 · 안 쓰는 칸 12
  election.searchDistricts               search_election_districts × keyword              query                            맞다 · 안 쓰는 칸 5
  election.searchLocalPledgeSummaries    search_local_pledge_summaries × keyword          query                            맞다 · 안 쓰는 칸 9
  election.searchLocalPledgeSummaries    search_local_pledge_summaries × map_extent       bbox                             맞다 · 안 쓰는 칸 9
  election.searchLocalPledgeSummaries    search_local_pledge_summaries × map_extent (첫)  bbox                             맞다 · 안 쓰는 칸 9
  ev.getStation                          get_ev_station × station_id                      statId                           맞다 · 안 쓰는 칸 0
  ev.searchStations                      search_ev_stations × keyword                     query                            맞다 · 안 쓰는 칸 17
  ev.searchStations                      search_ev_stations × map_extent                  maxLat, maxLon, minLat, minLon ·어댑터  맞다 · 안 쓰는 칸 14
  ev.searchStations                      search_ev_stations × map_extent (첫)             bbox                             ★ 없는 칸을 보낸다 — bbox
  geo.geocode                            geocode_place × place_name                       query                            맞다 · 안 쓰는 칸 0
  geo.getRailwayLines                    get_railway_lines × map_extent                   bbox                             맞다 · 안 쓰는 칸 4
  geo.getRailwayLines                    get_railway_lines × map_extent (첫)              bbox                             맞다 · 안 쓰는 칸 4
  geo.getRailwayLines                    get_railway_lines × place_name                   stationName                      맞다 · 안 쓰는 칸 4
  geo.getRailwayLines                    get_railway_lines × place_name (…선)             railwayName                      맞다 · 안 쓰는 칸 4
  knowledge.query                        search_documents × keyword                       k, query                         맞다 · 안 쓰는 칸 1
  population.getAgeProfile               get_age_profile × admin_code                     code, level                      맞다 · 안 쓰는 칸 1
  population.getTrend                    get_population_trend × admin_code                code, level                      맞다 · 안 쓰는 칸 3
  population.searchStatistics            search_population_statistics × keyword           query                            맞다 · 안 쓰는 칸 12
  population.searchStatistics            search_population_statistics × map_extent        bbox                             맞다 · 안 쓰는 칸 12
  population.searchStatistics            search_population_statistics × map_extent (첫)   bbox                             맞다 · 안 쓰는 칸 12
  rail.getSectionGeometry                get_railway_section × place_name                 sectionName                      맞다 · 안 쓰는 칸 0
  road.getCctv                           find_cctv × map_extent                           maxLat, maxLon, minLat, minLon   맞다 · 안 쓰는 칸 0
  road.getCctv                           find_cctv × map_extent (첫)                      maxLat, maxLon, minLat, minLon   맞다 · 안 쓰는 칸 0
  road.getCctv                           find_cctv × point                                maxLat, maxLon, minLat, minLon ·어댑터  맞다 · 안 쓰는 칸 0
  road.getCctv                           find_cctv × point (첫)                           maxLat, maxLon, minLat, minLon ·어댑터  맞다 · 안 쓰는 칸 0
  vworld.getAdministrativeBoundaries     get_vworld_boundaries × keyword                  query                            맞다 · 안 쓰는 칸 8
  vworld.getAdministrativeBoundaries     get_vworld_boundaries × map_extent               bbox                             맞다 · 안 쓰는 칸 8
  vworld.getAdministrativeBoundaries     get_vworld_boundaries × map_extent (첫)          bbox                             맞다 · 안 쓰는 칸 8
  web.search                             web_search × keyword                             query                            맞다 · 안 쓰는 칸 4

  지도 명령 줄 1 (도구를 안 불러 판정에서 뺌)  show_facility × place_name → digitalTwin.showFacility

  합계  판정한 행 51 (input 35 · input_first 15 · 값 갈래 1) · 지도 명령 줄 1 · 도구 26
        줄 판정  맞다 50 · 없는 칸 1 · 안 보낸 required 0 · 스키마 못 받음 0
        칸 합계  없는 칸 1 · 안 보낸 required 0 · 안 쓰는 칸 306 (그중 ★ 122 — R 0 · S 8 · O 36 · A 94)
```

#### 5. 추리는 기준 — R · S · O 에 A 를 더했다

**지난번 셋(R · S · O)은 이미 아는 넷을 하나도 못 잡는다.** 확인했다 —
`all` 도 `includeGeometry` 도 `pledgeCategory` 도 `order` 도 required 가 아니고,
우리가 보내는 `query` · `bbox` 와 낱말을 공유하지 않고, 같은 도구의 다른 줄도
안 보낸다. 그래서 기준을 고쳤다.

```
A  발화(@arg) 줄의 안 쓰는 칸 전부. limit · k · offset 만 뺀다
```

근거 : 넷은 칸의 성질이 제각각이라(boolean 둘 · string 둘 · 기본값 있는 것과
없는 것 · 거르는 칸과 출력 칸) **칸의 성질로는 못 좁힌다.** 공통점은 자리다 —
넷 다 발화가 `query` 한 칸으로 접히는 줄이었다. 발화는 자유 문장이라 그 줄의
다른 모든 칸이 「사람 말이 갈 수 없는 자리」다. $prev · $context 줄은 사람
말이 아니라 앞 단계 · 화면이 채우므로 옛 판정대로 둔다. limit · k · offset 을
뺀 것은 개수 손잡이는 말을 막지 않아서다 — "100개만 보여줘" 류가 나오면
이 제외를 다시 본다. **넷이 다 걸리는지 확인했다** — 아래 6절, 전부 A 다.

#### 6. ★ 사람이 볼 자리

**하나 · 없는 칸을 보내는 줄이 나왔다 — 유일한 빨간 판정.**

```
("search_ev_stations", "map_extent") 의 input_first 가 {"bbox": $context.view…} 를
보내는데 ev.searchStations 스키마에는 bbox 칸이 없다 — minLon · minLat ·
maxLon · maxLat 넷을 따로 받는다 (tools.json 2026-08-27). 배선 주석은
"ev.searchStations 는 bbox 를 평평한 네 수로 받는다" 인데 스키마와 어긋난다.
맞으면 "지금 보이는 곳 충전소"(recipe_032 · 화면 발화)가 범위를 잃고 전국
검색이 된다. ★ Gateway 가 꺼져 있어 눌러서 못 갈랐다 — 주석이 낡았는지
스키마가 낡았는지는 서버를 켜고 한 번 누르면 갈린다. 배선은 안 고쳤다.
```

**★ 갈렸다 (2026-08-30, 같은 날 늦게). 계기판이 맞았고 배선 주석이 낡았다.**
사람이 저쪽 화면에서 "지금 보이는 곳 충전소 찾아줘" 를 눌러 **500건(전체
92,821건)** 을 봤고 지도를 좁혀도 같았다. 원천(`ASAP-mcp/main.py:961`)도
같은 답이다 — 그 핸들러는 `minLon` 넷을 직접 읽고 `_parse_bbox_input` 을
안 부른다. **위 문단의 「배선 주석과 스키마 중 어느 쪽이 낡았나」의 답은
주석이다.** 열린 과제에 항목으로 옮겨 적었다 (배선은 여전히 안 고쳤다).

**둘 · 이미 아는 넷 — 전부 A 로 걸렸다.**

```
all              A  election search 넷 (searchDistricts · searchAssembly* 둘 · searchLocalPledgeSummaries)
includeGeometry  A  adminBoundary.searchBoundaries — 이 도구만 기본 False 라 지도에 안 그려진다.
                    election 계열의 같은 칸은 기본 True 라 문제가 아니다 (스키마 default 실측)
pledgeCategory   A  election.searchAssemblyDistricts (정답표 16번 자리)
order            A  population.searchStatistics (정답표 19번 자리)
```

**셋 · A 94 중 새로 눈에 띄는 것** (도구가 할 수 있는데 말이 못 가는 자리.
발화가 떠오르는 순으로 사람이 볼 것) :

```
hasPledges                election.searchAssemblyDistricts. 16번 실측에서 20건 나온 그 칸
sido                      election search 셋. "충북 선거구 보여줘" 류가 지금 query 로 접힌다
metric · level            population.searchStatistics. 19번 "인구 많은 시군구" 와 한 묶음
party · winner            공약 검색. "국민의힘 공약" · "당선인 공약" 류
theme · leader            지방선거 요약 검색 (단 데이터 미적재라 후순위)
availableOnly ·chargerType ev.searchStations. "사용 가능한 급속 충전소" 류
filter_docs               knowledge.query. 이미 열린 과제 「인자 둘」에 있는 자리
domains · freshness       web.search (권한이 막혀 있어 후순위)
```

나머지 A 는 include* · simplifyM · zoom · pageNo 같은 출력 손잡이가 많다 —
걸렸다고 틀린 것이 아니라는 원칙 그대로다. S 8 중 railwayName · stationName
은 갈래 행이 생기며 서로를 가리키는 것이라 해소로 본다. includeBbox ×2 ·
minOutputKw 는 예전 그대로 소음이다.

#### 7. 안 한 것

```
배선 · 제품 코드 0줄        표를 내고 멈췄다. 넷을 어느 길로 고칠지(recipe 추가 ·
                            인자 둘 · 무인자)는 열린 과제에 그대로 있다
step_service 낡은 주석      "railwayName(S) 은 안 사라진다" — 제품 코드라 못 고쳤다.
                            다음에 그 파일을 열 때 고친다 (열린 과제에 적음)
스키마 재수신               Gateway 꺼짐. tools.json 은 2026-08-27 것이다
ev bbox 실측                서버가 있어야 눌러 갈린다 (위 6절 하나)
판정 이름                   「안 쓰는 칸」 등 부류 이름은 안 바꿨다 — 기준 A 를
                            더한 것으로 「사람 말을 막는 자리」가 표에 오른다
check_resolve               프롬프트에 닿는 변경이 없어 안 돌렸다
```

### 2026-08-30 (쉰여덟째) · 시연 리허설 — 사람이 저쪽 화면에서 일곱을 눌렀다

2026-08-30, 사람이 저쪽 화면에서 대본 순서대로 눌렀다. 일곱이 다 됐다.
지명은 사람이 임의로 바꿔 눌렀다 (오송역 · 의왕역 · 철도기술연구원).

```
1  오송역 위치 보여줘         geo.geocode 오송역 → (127.3277, 36.6200)
2  의왕역 CCTV 보여줘          좌표 → road.getCctv 290건
3  오송역 연령대별 인구 알려줘  좌표 → 구역 판별 → 청주시 흥덕구 292,625명
4  철도기술연구원 근처 충전소 자세히 알려줘
                              좌표 → ev.searchStations 500건(전체 8,616) ·
                              「코레일 인재개발원」 → ev.getStation 4건
                              ★ 4단 사슬. 철도연에서 하는 시연이라 지명도 맞다
5  문서에서 철도안전법 관련 내용 찾아줘
                              knowledge.query 6건 · 1쪽 · 2쪽 · 32쪽
6  지금 보이는 곳 CCTV 보여줘   ★ 두 번 눌렀다
                              지도가 좁을 때(126.94~127.00) 0건
                              넓힌 뒤(126.79~127.06) 19건
                              ★ 확대 정도가 건수를 정한다. 시연에서는 넓게 둔다
7  여기 CCTV 보여줘            ★ 우클릭 뒤. road.getCctv 249건
                              저쪽 화면이 "관심 지점이 설정되었습니다" 를 먼저 띄운다
```

★ 우클릭을 하면 그 뒤 발화가 both 조건이 된다 (「쉰일곱째」).
그래서 「여기 CCTV」를 맨 뒤에 두었다. 앞의 여섯은 우클릭 전에 눌렀다.

---

### 2026-08-30 (쉰일곱째) · 문맥 bbox ↔ both 가 기존 발화를 얼마나 흔드는가

무인 실행. 모델 qwen3:32b(서버 기본) · recipe 60 · `--runs 3` · 좁히기 끔 ·
브랜치 `integration/ASAP-Ontology`. **코드를 한 줄도 안 고쳤다. 기본값도 안
바꿨다(both 그대로). 커밋 안 했다.** 고친 것은 이 파일 하나다.

**★ 결론 한 줄 : 흔든다. 기존 서른하나 중 다섯 발화가 문맥만으로 갈리고,
그 원인은 menu 가 아니라 축 선택지다.**

---

## 1. 왜 쟀나

「쉰여섯째」에서 `check_resolve` 의 기본 문맥을 `bbox` 에서 `both` 로 바꿨다.
bbox 뿐이면 찍은 지점 recipe 아홉이 menu 에도 축 선택지에도 안 실려 아예
후보에 못 오르기 때문이다. **그런데 그 바꿈이 기존 발화를 얼마나 흔드는지는
안 쟀다.** 「쉰여섯째」 56/93 과 「쉰다섯째」 52/93 의 넷이 문맥 때문인지
날짜 드리프트인지 몰랐다. 그것을 가른다.

---

## 2. 네 묶음 (적중 기준. 한 세션에서 연달아 돌렸다)

```
              A bbox    B both    C bbox    D both
기준선 아홉    18/27     19/27     19/27     19/27
확장 스물둘    31/66     37/66     35/66     37/66
화면 다섯       6/15     15/15      6/15     15/15
★ 기존 서른하나 49/93    56/93     54/93     56/93
합계          55/108    71/108    60/108    71/108
한 묶음 소요   8m13s     8m13s     8m11s     8m13s
/resolve 평균  4.6초     4.6초     4.5초     4.6초
```

**화면 다섯은 bbox 에서도 다 재어졌다.** 「못 잴 수 있다」고 적었는데 아니었다 —
33 · 35(보이는 범위)는 3/3 이고, 32 · 34 · 36(찍은 지점)은 오류가 아니라
**세 번 다 빗나감**으로 찍힌다. 각각 {026} · {024} · {031} 을 골랐다.
셋 다 `visible_extent` 에서 출발하는 recipe 다. 정답인 찍은 지점 recipe 가
목록에 없으니 옆칸을 고른 것이고, **구조상 닿을 길이 없다는 것이 숫자로
찍힌 셈이다.** 6/15 는 그 자리의 바닥값이지 측정 실패가 아니다.

---

## 3. ★ 발화별로 달라진 것 — 이번의 답

**both 둘(B · D)은 서른여섯 발화가 한 칸도 안 다르다.** 그래서 both 쪽은 값이
하나로 확정된다. bbox 둘(A · C)은 아홉 발화가 갈렸다. 그 아홉은 문맥의 몫을
못 읽는다.

**bbox 둘이 같고 both 둘도 같아 문맥의 몫이 확정된 발화 — 기존 서른하나 중 다섯**

```
 1 오송역 위치 보여줘         3/3 → 0/3   ★ 나빠짐
 6 국회의원 선거구 찾아줘      0/3 → 3/3   ★ 좋아짐
 8 철도 안전 문서 찾아줘       3/3 → 1/3   ★ 나빠짐
15 청주 선거구 찾아줘         0/3 → 3/3   ★ 좋아짐
20 대전역 연령대별 인구 알려줘  3/3 → 1/3   ★ 나빠짐
                            합 -1회 (좋아짐 +6 · 나빠짐 -7)
```

셋이 나빠지고 둘이 좋아진다. **합은 거의 상쇄돼 -1회지만, 다섯 발화가 실제로
흔들린다.** 「점수가 안 움직였으니 안 흔들렸다」로 읽으면 안 되는 자리다.

무엇이 어떻게 갈리는가 (both 쪽 값. B · D 동일)

```
 1  bbox  given=spoken_place · want=point         → recipe_001 적중
    both  given=spoken_place · want=facility_view → {001,004,044} 근접 2회 · {004} 빗나감 1회
        ★ 축의 want 가 갈렸다. 후보에 찍은 지점 recipe 가 낀 것이 아니다 —
          001 · 004 · 044 는 셋 다 spoken_place 에서 출발한다
 6  bbox  {008,009,010,039,041,043} 근접 CLARIFY  → both 3/3 적중
15  bbox  {008,009} 근접 CLARIFY 3회             → both 3/3 적중
 8  both  {007,014} 근접 CLARIFY 2회
20  both  {045,058,059} 근접 CLARIFY 2회
```

**적중 수는 같은데 칸이 옮겨간 발화 둘** (판정은 안 바뀌지만 성격이 바뀐다)

```
18 오송역 일대 인구 얼마야   근접 3 → 빗나감 3   ({045,059} → {059} 로 좁혀서 틀림)
27 철도안전법 내용 찾아줘    빗나감 3 → 빗나감 2 + 못 붙음 1
        ★ 서른여섯 중 「못 붙음(NO_MATCH)」이 나온 유일한 자리다. both 에서만 난다
```

**화면 다섯 중 셋** (문맥이 있어야 닿는 자리. 예상대로다)

```
32 여기 CCTV 보여줘          0/3 → 3/3
34 여기 행정구역 알려줘       0/3 → 3/3
36 여기 연령대별 인구 알려줘   0/3 → 3/3
```

---

## 4. menu 자수와 축 선택지 수 — ★ 가설이 틀렸다

재기 전 가설은 「문맥이 both 면 찍은 지점 recipe 아홉이 menu 에 더 실려
menu 가 길어지고, 그래서 무관한 발화가 흔들린다」였다. **아니다.**

```
                     menu 자수   given 선택지   given 목록 자수   채운 프롬프트
bbox · 기존 발화        3406자        4개          249자          6399자
both · 기존 발화        3406자        5개          338자          6488자
bbox · 화면 발화         809자        4개          249자          3801자
both · 화면 발화        1535자        5개          338자          4616자
(참고) none · 기존 발화  3406자        3개          150자
```

**기존 서른하나가 보는 menu 는 bbox 와 both 에서 한 글자도 안 다르다 —
3406자로 같다.** `_menu_for` 가 menu 를 가르는 것은 발화가 `SCREEN_WORDS` 에
걸릴 때뿐이고, 걸리는 것은 32~36 다섯뿐이다(실측으로 확인. `test_menu_split.py`
가 지키는 그대로다). 화면 발화에서만 809 → 1535자로 는다.

**그러면 기존 다섯을 흔든 것은 무엇인가. 축 선택지 하나다.**

```
given 목록에 picked_point 한 줄이 붙는다   4개 → 5개 · 249 → 338자 (+89자)
응답 스키마의 given enum 도 함께 는다      _choices_for 가 둘을 같이 다룬다
프롬프트 전체는 6399 → 6488자 (+89자 · 1.4%)
```

**89자 · 선택지 한 개가 다섯 발화를 흔든다.** 후보가 늘어서가 아니다 —
1번에서 갈린 것은 `want` 축이고, 나온 후보 {001,004,044}는 셋 다
`spoken_place` 출발이라 `_without_dropped` 와 무관하다. 선택지 목록이
한 줄 길어진 것만으로 LLM 이 축을 다르게 쓴다.

**이 저장소가 여러 번 겪은 「menu 가 길어지면 흔들린다」와 다른 이야기다.**
menu 는 그대로였다. 흔든 것은 축 목록이다. `MENU_BUDGET` 주석이 이미
「진짜 제약은 컨텍스트 크기가 아니라 문장 변별력」이라고 적고 있는데,
이번 것은 그 말을 축 목록 쪽에서 다시 본 것이다.

---

## 5. 드리프트 폭 — bbox 는 갈리고 both 는 안 갈린다

```
both 둘(B · D)   서른여섯 발화 전부 한 칸도 안 다르다. 묶음 값도 같다
bbox 둘(A · C)   기존 서른하나가 49/93 대 54/93. ★ 5회 차이
                 갈린 발화 아홉 : 9 · 12 · 13 · 14 · 21 · 24 · 25 · 26 · 31
                 그중 25번이 가장 크다 (0/3 대 3/3) · 26번 (3/3 대 1/3) ·
                 31번 (0/3 대 3/3) · 12 · 24 도 2회씩 움직인다
```

**「읽는 법」에 미리 적은 셋 중 둘이 동시에 나왔다.** bbox 둘이 갈리니
묶음 점수의 차(49 → 56 과 54 → 56)는 그대로 못 읽는다. 하지만
**발화별로 보면 읽힌다** — 네 묶음이 다 같은 값을 낸 발화만 골라 세면
문맥의 몫이 다섯이고, 나머지는 드리프트로 갈라 둔다. 그래서 3절의 다섯을
답으로 삼는다.

**「쉰여섯째」의 56/93 대 「쉰다섯째」의 52/93, 그 넷은 결국 뭐였나.**
이번에 both 를 두 번 재서 56/93 이 나왔다. 「쉰여섯째」와 같은 값이다.
bbox 는 49 와 54 로 갈린다. **「쉰다섯째」의 52 는 그 흔들리는 구간 안에
있다.** 넷 중 얼마가 문맥 몫인지는 이 자료로 못 가른다 — 다만 문맥의 순
효과가 기존 서른하나에서 **-1회**(3절)라는 것은 확정됐으므로,
**그 넷의 대부분은 드리프트다.**

---

## 6. ★ 결론 한 줄

**문맥 both 는 기존 발화를 흔든다 — 서른하나 중 다섯이 갈리고(셋 나빠지고
둘 좋아져 합은 -1회), 원인은 menu 길이가 아니라 given 축 선택지에 붙는
`picked_point` 한 줄(+89자)이다. 대신 화면 다섯이 6/15 에서 15/15 가 되고
both 는 재현이 완벽하다(두 번 돌려 한 칸도 안 다름). 기본값 both 를 그대로 둔다.**

---

## 7. 안 한 것

```
아무것도 안 고쳤다        코드 0줄. 기본값 both 그대로. 커밋 안 했다
드리프트 아홉을 못 갈랐다  bbox 둘이 갈린 아홉 발화는 문맥 몫을 못 읽는다.
                          더 재려면 bbox 를 서너 번 더 돌려야 한다. 안 했다
none 을 안 돌렸다         네 묶음은 bbox · both 둘뿐이다. none 의 자수만 쟀다
--execute 를 안 붙였다    경로만 쟀다. 실행 칸은 「쉰여섯째」 것이 그대로다
왜 축이 갈리는지 모른다   picked_point 한 줄이 want 를 흔드는 기전은 안 봤다.
                          프롬프트를 고치는 일이고 이번 범위가 아니다
좁히기(--narrow) 안 켰다  기본 길로만 쟀다
```

---

### 2026-08-30 (쉰여섯째) · 정답표에 실행 칸을 더하고 화면 발화 다섯을 넣는다

무인 실행. 모델 qwen3:32b(서버 기본) · recipe 60 · `--runs 3` · 좁히기 끔 ·
브랜치 `integration/ASAP-Ontology`. **커밋하지 않았다.** 고친 것은
`tools/check_resolve.py` · `tests/` · `NOTES.md` 셋뿐이고 **제품 코드는 한 줄도
안 고쳤다.**

**★ 결론부터 셋.**

```
1  실행 칸이 붙었다. 정답표가 「경로를 맞게 골랐는가」에 더해
   「그 recipe 가 답을 내놓는가」를 잰다. 발화 6 이 적중 3/3 · 실행 ✗ 로 찍힌다
2  16 번은 「도구가 못 하는 일」이 아니라 「인자 문제」였다. 말투를 고쳐도
   실행은 여전히 ✗ 다 — 도구에 pledgeCategory 가 있는데 배선이 query 만 보낸다
3  화면 recipe 열아홉이 정답표에 하나도 없던 것이 다섯 들어왔다.
   그중 셋이 「찍은 지점」이고, 그것을 재려고 이 도구의 기본 문맥을 both 로 바꿨다
```

---

## 왜 했나

정답표가 재던 것은 **「경로를 맞게 골랐는가」 하나**였다. 그래서 이런 자리가
표에서 안 보였다.

```
발화 6  국회의원 선거구 찾아줘   적중 3/3   ← 표만 보면 다 잘 되는 자리다
실제    election.searchDistricts  query="선거구"  0건
        화면에는 "찾지 못했습니다."
```

그리고 **recipe 가 60 인데 정답표가 덮는 것이 스무 개쯤**이었다. 특히
화면 recipe 열아홉(018~032 · 053~056)은 **하나도 없었다.**

---

## 1. 실행 칸을 어떻게 채웠나

### 무엇을 부르나 — 저쪽 화면과 같은 길

`--execute` 를 붙이면 발화마다 `POST /chat` 을 한 번 더 부른다. 저쪽 화면이
부르는 것과 같은 흐름이고(`/chat/stream` 과 `_chat_events` 를 함께 쓴다),
해석부터 도구 호출까지 한 번에 지난다. 그다음 `GET /recent` 로 그 회차를 읽는다.

**회차에 필요한 것이 다 들어 있다** — status · recipe_id ·
candidate_recipe_ids · 단계 줄 · 답 문구다. 응답 본문을 다시 파싱하지 않는다.

```
평소       /resolve 만 부른다      경로만 잰다. 빠르다
--execute  실행까지 부른다          가끔 돌려 실행 칸을 갱신한다
```

**발화마다 한 번만 누른다.** 이 칸은 분포가 아니라 「지금 답이 나오는가」를
적는 자리다. 흔들리는 것은 해석이고 그것은 위의 적중 표가 이미 세 번씩 재고 있다.

### 판정 문구를 여기서 다시 적지 않았다

✓ 인지 ✗ 인지는 **답의 첫 줄**로 가른다. `vendor/asap/workflow_answer` 의
`_verdict` 가 거기에 결과를 적어 두었기 때문이다 — 성공이면 recipe 가 아는
문장, 빈 결과·오류면 우리 문구다.

**그 문구를 상수로 import 한다. 베껴 적지 않았다.**

```
EMPTY_HEADLINE · ERROR_HEADLINE · MISSING_STATUS · NO_PERMISSION_REASON
                                                vendor/asap/workflow_answer
CHOICE_HEAD · NO_ARGUMENT_ANSWER · UNWIRED_ANSWER
                                                demo/api/services/execute_service
```

베껴 적으면 저쪽이 문구를 갱신했을 때 **화면은 "찾지 못했습니다" 인데 표는 ✓**
로 찍힌다. 실제로 「쉰다섯째」에 그 머리말이 한 줄에서 두 줄이 됐다.
`tests/tools/test_execution_column.py` 가 그 어긋남을 잡는다 — 답 문구를
시험이 짓지 않고 `compose_workflow_answer` 에게 만들게 해서 먹인다.

**0건과 not_found 는 단계 줄로 가른다.** 답의 둘째 줄(안내)로 가르려다 틀렸다 —
그 줄은 **글자로 부른 단계에만** 붙어서(`workflow_answer._retry_line` 의
`_called_with_words`) 좌표로 부른 not_found 를 놓친다. 시험이 그것을 잡았다.
지금은 `MISSING_STATUS` 의 문구를 단계 줄에서 찾는다.

### 되묻기 자리를 어떻게 했나 — 이 도구가 사람 대신 고른다

되묻기가 나는 발화는 화면에서 **사람이 번호를 골라야** 실행된다.
`?` 로 두면 29~31 번 같은 「애매」 자리가 영영 안 재어진다.

**기대 recipe 가 후보에 있으면 그 번호를 골라 이어 누르기로 했다.**

```
근거  고르기는 해석을 다시 하지 않는다 (execute_service._run_choice 가
      resolve 도 LLM 도 안 부른다). 그래서 부르는 것이 정확히 기대 recipe 다
      — 「그 recipe 가 답을 내놓는가」라는 이 칸의 물음과 정확히 맞는다
      사람이 화면에서 하는 동작과 같다. 없는 길을 지어내지 않았다
표시  「되묻기→고름」이라고 밝혀 곧장 실행된 자리와 갈라 적는다
```

기대 recipe 가 후보에 아예 없거나 다른 것을 확신하고 골랐으면 `?` 다.
**그 자리에서 실행이 되는지는 이 표가 말할 수 없다.** 무엇이 그 자리를 막았는지는
적중 표의 「빗나감」·「못 붙음」이 이미 말하고 있다.

### 값 셋

```
✓  기대 recipe 가 돌았고 결과가 왔다
✗  기대 recipe 가 돌았는데 답이 안 나왔다.  0건 · not_found · 권한 · 인자 · 배선
?  기대 recipe 를 아예 안 지났다. 해석이 다른 데로 갔다
```

**★ 실행 칸을 관문으로 삼지 않는다.** 저쪽 데이터가 늘면 건수가 바뀌고
Gateway 가 꺼지면 전부 실패한다. 적중 판정은 이 칸을 안 본다 — 네 칸과 그
합계는 `--execute` 를 붙이기 전과 같은 숫자가 나와야 한다.

---

## 2. 16 번 — 말투를 고쳤고, 인자를 눌러 쟀다

```
옛말   교통 공약 많은 선거구 검색해줘     기대 recipe_009
새말   교통 공약 있는 선거구 검색해줘     기대 recipe_009 그대로
```

**왜 고쳤나.** 도구에 정렬 칸이 없어 「많은 순」을 시킬 수가 없다.
`election.searchAssemblyDistricts` 의 inputSchema 를 다시 읽었다
(`tools/probe_out/tools.json`).

```
query · sido · code · hasPledges · pledgeCategory · bbox · limit ·
includeGeometry · all · simplifyM
★ order 도 sort 도 없다. required 는 없다
```

「있는」은 `hasPledges` · `pledgeCategory` 와 결이 맞는다. **기대값은 한 글자도
안 바꿨다.**

### 인자 실측 — 추측으로 안 적었다

`--runs 3` · 문맥 bbox(고치기 전과 같은 조건).

```
발화                          status    후보                        argument
교통 공약 많은 선거구 검색해줘  CLARIFY   {009, 010, 011}  3/3       "교통 공약"
교통 공약 있는 선거구 검색해줘  CLARIFY   {009, 010, 011}  3/3       "교통 공약"
```

**둘이 한 글자도 안 다르다.** 「마흔다섯째」에 "많은" 이 `query="교통"` 으로
20건이었다고 적혀 있는데, **오늘 이 기계에서는 둘 다 `"교통 공약"` 이다.**
옛 줄은 안 지운다 — 그때 그 조건의 값이다.

되묻기가 나므로 기대 recipe_009 를 골라 이어 눌렀다. 둘 다 같았다.

```
election.searchAssemblyDistricts   query="교통 공약"   0건
찾지 못했습니다.
찾아본 곳은 「2024 제22대 국회의원 선거구 전체」입니다. 다른 낱말로 다시 말씀해 주세요.
```

**★ 실행은 여전히 ✗ 다.** 말투를 고친 것이 실행을 살리지 못했다.

### 그래서 무엇이 막고 있나 — 도구를 직접 눌러 갈랐다

`asap-mcp-core/election.searchAssemblyDistricts` 를 Gateway 에 바로 눌렀다
(2026-08-30 실측. `user_context` 는 `execute_service.USER_CONTEXT` 그대로).

```
인자                      count   totalMatches
query="교통 공약"            0            0     ← 지금 나오는 것
query="교통"                20           45
pledgeCategory="교통"       20           37
hasPledges=true             20           77
```

**도구는 그 일을 할 수 있다.** 못 하는 것은 「많은 순」뿐이었고, 「교통 공약이
있는 선거구」는 `pledgeCategory` 나 `hasPledges` 로 된다.

막는 것은 둘이다.

```
가  LLM 이 "교통 공약" 을 통째로 인자로 뽑는다. 「하려는 일의 이름」이지
    「찾을 대상의 이름」이 아니다 — 「열셋째」의 그 자리와 같은 성격이다
나  배선이 query 한 칸만 보낸다.
    STEP_OF ("search_assembly_districts", "keyword") = {"query": @arg}
    pledgeCategory · hasPledges 를 보낼 자리가 배선에 아예 없다
```

**★ 16 번은 「도구가 못 하는 일」에서 「인자·배선 문제」로 바뀌었다.**
「열린 과제」에 옮겼다. **이번에 안 고쳤다** — 배선표는 이번 범위가 아니다.

---

## 3. 화면 발화 다섯 — 고른 법과 근거

### 먼저, 기본 문맥을 both 로 바꿨다

```
바꾼 것  tools/check_resolve.py 의 --context 기본값.  bbox → both
```

**근거.** bbox 뿐이면 **찍은 지점 recipe 아홉을 아예 못 잰다.**
`resolve_service` 가 그 아홉을 두 곳에서 뺀다.

```
_menu_for     문맥에 값이 온 노드의 recipe 만 menu 에 싣는다.
              우클릭 전이면 보이는 범위 열 벌뿐이다
_choices_for  given 축 선택지에서도 picked_point 를 뺀다
```

후보에 오를 길이 없으므로 기대값을 적어도 처음부터 못 닿는 줄이 된다.
**저쪽 화면에서 우클릭한 뒤와 같은 조건이 both 다.**

**대가를 적어 둔다.** 「쉰다섯째」까지의 숫자는 전부 bbox 로 잰 것이라
**옛 기록과 맞대려면 `--context bbox` 를 적어야 한다.** 도구 docstring 과
`CONTEXT` 옆 주석에 그렇게 적었다. 「마흔여덟째」가 이미 「옛 날짜의 숫자와
직접 견주지 않는다」를 규칙으로 세워 두었으므로 새로 생긴 제약은 아니다.

### 고르는 법 — 16 번에서 저지른 잘못을 되풀이하지 않았다

16 번은 **경로만 보고 만든 발화**였다. 그래서 세 관문을 세우고 그것을 다 지난
것만 넣었다.

```
1  화면 낱말이 걸리는가       SCREEN_WORDS 에 걸려야 화면 recipe 가 menu 에 실린다
2  도구가 그 일을 할 수 있는가  inputSchema 를 읽어 배선과 맞댄다
3  실제로 눌러 데이터가 오는가  /resolve 3회 + /chat 1회
```

**2 번은 `tools/check_inputs.py` 가 할 일인데 그 도구가 죽어 있다**
(`KeyError: 'tool'`. 「쉰째」의 「열린 과제」에 이미 있는 항목이고 오늘도 같은
자리에서 같은 오류다). **이번 범위가 아니라 안 고쳤고**, 대신
`tools/probe_out/tools.json` 을 직접 읽어 맞댔다.

```
recipe  도구                                   required        배선이 보내는 것
019     road.getCctv                    minLon·minLat·maxLon·maxLat   찍은 지점 + 반경 15km
026     road.getCctv                    같음                          문맥 bbox 넷 그대로
018     adminBoundary.findBoundaryByPoint  lon·lat                    찍은 지점 lon·lat
031     population.searchStatistics     (없음)                        문맥 bbox
054     adminBoundary.findBoundaryByPoint  lon·lat                    찍은 지점
        population.getAgeProfile        level·code                    앞 단계 items.1
```

### 재보고 고른 다섯

`--context both` · `--runs 3` · 2026-08-30 실측.

```
 #  발화                       기대     resolve      실행         쪽
32  여기 CCTV 보여줘           019   3/3 SELECT   ✓ 82건        찍은 지점
33  지금 보이는 곳 CCTV 보여줘  026   3/3 SELECT   ✓ 82건        보이는 범위
34  여기 행정구역 알려줘        018   3/3 SELECT   ✓ 3건 · 충청북도  찍은 지점
35  현재 화면 인구 알려줘       031   3/3 SELECT   ✓ 9건         보이는 범위
36  여기 연령대별 인구 알려줘    054   3/3 SELECT   ✓ 청주시 흥덕구 292,625명  찍은 지점
```

**32 · 33 은 「서른한째」·「마흔여덟째」의 화면 실측에서 이미 확인된 말투다.**
나머지 셋은 이번에 찾아 확인했다.

```
찍은 지점 셋 · 보이는 범위 둘   두 쪽을 다 넣으라는 요구를 채우면서 찍은 지점을
                                하나 더 뒀다. 그 아홉이 한 번도 안 재어진 자리다
도구 넷                         road.getCctv · findBoundaryByPoint ·
                                searchStatistics · getAgeProfile
사슬 2단 넷 · 3단 하나          36 번이 유일한 3단이다. 24 번이 4단을 지키듯
                                이 줄이 찍은 지점에서 출발하는 3단을 지킨다
```

### 안 고른 것과 그 까닭

```
023 · 030 · 053   저쪽 데이터가 미적재라 반드시 0건이다
                  (dataset.available false. STEP_OF 주석의 실측)
020 · 021         "여기 선거구 알려줘" · "여기 국회의원 누구야" 를 눌러 봤다.
                  셋넷으로 되묻는다 (3/3 CLARIFY). 되묻는 자리는 29~31 이
                  이미 셋을 맡고 있어 더 넣을 값이 없었다
032               "이 근처 충전소 찾아줘" 는 3/3 SELECT · 실행 ✓ 로 좋았는데
                  충전소는 7 · 24 번이 이미 덮는다. 도구를 갈라 쓰려고 뺐다
```

---

## 4. `--execute` 결과 — 실행 칸 표를 통째로 싣는다

`--runs 1 --execute` · 문맥 both · 2026-08-30 · 발화마다 한 번.

```
  ── 실행 칸 (잰 날 2026-08-30 · 발화마다 한 번) ──
  #  발화                                      정답    실행  왜
  1  오송역 위치 보여줘                        {001}   ?     SELECT {004} 로 갔다
  2  오송역 좌표 알려줘                        {001}   ✓     geo.geocode 오송역 → 충청북도 청주시 흥덕구 오송읍 …
  3  오송역 CCTV 보여줘                        {036}   ✓     road.getCctv minLon=127.1598 · minLat=36.4852 · max…
  4  청주시 인구 구성 알려줘                   {058}   ✓     population.getAgeProfile level="sigungu" · code="43…
  5  오송역 근처 충전소 찾아줘                 {046}   ✓     ev.searchStations minLon=127.1598 · minLat=36.4852 …
  6  국회의원 선거구 찾아줘                    {008}   ✗     0건 · election.searchDistricts query="선거구" 0건  …
  7  전기차 충전소 데이터 검색해줘             {013}   ✓     ev.searchStations query="전기차 충전소" 120건 · 일…
  8  철도 안전 문서 찾아줘                     {014}   ✓     knowledge.query query="철도 안전" 6건 「철도안전법(…
  9  충북 제1선거구 알려줘                     {015}   ✗     not_found · election.getDistrict name="충북 제1선거…
                                               ✓ 6 · ✗ 2 · ? 1   ← 기준선 아홉

  10 오송역이 어느 동인지 알려줘               {034}   ✓     adminBoundary.findBoundaryByPoint lon=127.3277 · la…
  11 오송역 행정경계 보여줘                    {033}   ✓     adminBoundary.searchBoundaries 1건 · 청주시 흥덕구
  12 청주시 행정경계 보여줘                    {005}   ✓     adminBoundary.searchBoundaries 4건 · 청주시 상당구
  13 오송역 국회의원 누구야                    {040}   ✓     election.findAssemblyDistrictByPoint lon=127.3277 ·…
  14 오송역 국회의원 공약 보여줘               {041}   ✓     election.searchAssemblyPledgeDistricts 9건 · 세종특…
  15 청주 선거구 찾아줘                        {008}   ✓     election.searchDistricts 4건 · 충북 청주서원
  16 교통 공약 있는 선거구 검색해줘            {009}   ✗     0건 · election.searchAssemblyDistricts query="교통 …
  17 청주 국회의원 공약 검색해줘               {010}   ✓     election.searchAssemblyPledgeDistricts query="청주"…
  18 오송역 일대 인구 얼마야                   {045}   ?     SELECT {059} 로 갔다
  19 인구 많은 시군구 순위 보여줘              {012}   ✗     0건 · population.searchStatistics query="인구 많은 …
  20 대전역 연령대별 인구 알려줘               {058}   ✓     population.getAgeProfile level="sigungu" · code="30…
  21 경부선 선형 데이터 줘                     {002}   ✓     rail.getSectionGeometry sectionName="경부선" 칸: se…
  22 오송역 지나는 노선 알려줘                 {003}   ✓     geo.getRailwayLines stationName="오송역" 31건  (되…
  23 경부선 주변 CCTV 보여줘                   {049}   ✗     0건 · road.getCctv minLon=126.9608 · minLat=37.5194…
  24 오송역 근처 충전소 자세히 알려줘          {060}   ✓     ev.getStation statId="PK000313" 8건
  25 문서에서 철도안전법 관련 내용 찾아줘      {014}   ?     SELECT {007} 로 갔다
  26 문서에서 철도 안전 교육 내용 찾아줘       {014}   ✓     knowledge.query query="철도 안전 교육" 6건 「철도안…
  27 철도안전법 내용 찾아줘                    {014}   ?     SELECT {007} 로 갔다
  28 경부선 노선 보여줘                        {003}   ✓     geo.getRailwayLines railwayName="경부선" 224건  (되…
  29 청주시 인구 알려줘                        {012}   ✓     population.searchStatistics 4건 · 청주시 흥덕구 292…
  30 오송역 선거구 알려줘                      {038}   ✓     election.findDistrictByPoint lon=127.3277 · lat=36.…
  31 청주 국회의원 선거구 검색해줘             {008}   ✓     election.searchDistricts 4건 · 충북 청주서원
                                               ✓ 16 · ✗ 3 · ? 3   ← 확장 스물둘

  32 여기 CCTV 보여줘                          {019}   ✓     road.getCctv minLon=127.1598 · minLat=36.4853 · max…
  33 지금 보이는 곳 CCTV 보여줘                {026}   ✓     road.getCctv minLon=127.1598 · minLat=36.4853 · max…
  34 여기 행정구역 알려줘                      {018}   ✓     adminBoundary.findBoundaryByPoint lon=127.3277 · la…
  35 현재 화면 인구 알려줘                     {031}   ✓     population.searchStatistics 9건 · 세종특별자치시 39…
  36 여기 연령대별 인구 알려줘                 {054}   ✓     population.getAgeProfile level="sigungu" · code="43…
                                               ✓ 5 · ✗ 0 · ? 0   ← 화면 다섯

  ✓ 답이 나온다 · ✗ 기대 recipe 가 돌았는데 안 나온다 · ? 기대 recipe 를 안 지났다
  ★ 실행 칸은 관문이 아니다. 저쪽 데이터가 늘면 바뀌고 Gateway 가 꺼지면 전부 실패한다
```

**합계 : ✓ 27 · ✗ 5 · ? 4** (36 발화).

**「왜」 칸은 폭 52 에서 잘린다.** 갈래(0건 · not_found · 권한 · 인자 · 배선)와
도구 이름과 첫 인자가 그 안에 들어오게 자리를 잡았다. 잘린 뒤를 보려면 그
발화만 `--only` 로 다시 누른다.

### ✗ 다섯 — 무엇이 막고 있나

```
 6  0건       election.searchDistricts  query="선거구"
            ★ 「열린 과제」에 이미 있는 자리다. 그 query 가 훑는 것은 선거구
              이름이라 "선거구" 도 "국회의원" 도 이름의 일부가 아니다
 9  not_found election.getDistrict  name="충북 제1선거구"
            데이터에 그 이름이 없다. "충북 청주서원" 은 있다 —
              workflow_answer 의 RETRY_OF_STATUS 주석에 실측이 있다
16  0건       election.searchAssemblyDistricts  query="교통 공약"
            ★ 인자·배선 문제. 위 2 절과 「열린 과제」를 본다
19  0건       population.searchStatistics  query="인구 많은 …"
            ★ 16 과 같은 병이다. 「하려는 일의 이름」을 통째로 인자에 담았다.
              ★ 이 도구에는 order 칸이 있다 (실측). 「많은 순」을 시킬 수 있는데
                배선이 query 한 칸만 보낸다 — 16 과 같은 뿌리다
23  0건       road.getCctv  minLon=126.9608 · minLat=37.5194 …
            경부선 구간 형상의 bbox 가 그대로 갔다. 앞 단계는 성공했다
```

**다섯 중 셋(6 · 16 · 19)이 「인자를 통째로 옮겨 담는다」와 「도구에 칸이 있는데
배선에 자리가 없다」의 교집합이다.** 「스물다섯째」에 닫힌 그 항목이 다시 열렸다.

### ? 넷 — 기대 recipe 를 안 지났다

```
 1  SELECT {004} 로 갔다    적중 표에서 2/3 인 자리다. 한 번 눌러 그 하나를 뽑았다
18  SELECT {059} 로 갔다
25  SELECT {007} 로 갔다    웹 검색이다. 적중 표에서 빗나감 3/3 인 자리
27  SELECT {007} 로 갔다    ★ 알고 넣은 「위험」 자리다. 예상대로 웹 검색으로 샜다
```

**★ 한 번 누른 값이라 해석이 흔들리는 자리는 그날의 뽑기에 걸린다.**
1 번이 그 예다 — 적중 표에서 2/3 인데 실행 때 나머지 1/3 을 뽑았다.
**그래서 이 칸을 관문으로 삼지 않는다.** 무엇이 그 자리를 막았는지는
적중 표의 근접·빗나감이 세 번씩 재어 말하고 있다.

### 되묻기를 지나 고른 자리 둘

22 번과 28 번이 「되묻기→고름」으로 찍혔다. 되묻기가 나서 이 도구가 기대
recipe 의 번호를 골라 이어 눌렀고, 둘 다 실행까지 갔다.

```
22  geo.getRailwayLines  stationName="오송역"   31건
28  geo.getRailwayLines  railwayName="경부선"  224건
```

**28 번은 「위험」으로 넣은 자리다.** 「쉰째」가 노선명 갈래를 고친 뒤라
고르기만 하면 224건이 나온다 — **되묻기 자리를 `?` 로 뒀으면 못 봤을 사실이다.**

### 화면 다섯 — ✓ 5 · ✗ 0 · ? 0

```
32  road.getCctv                       82건
33  road.getCctv                       82건
34  adminBoundary.findBoundaryByPoint   3건 · 충청북도
35  population.searchStatistics         9건 · 세종특별자치시
36  population.getAgeProfile            청주시 흥덕구 292,625명
```

**★ 「찍은 지점」 recipe 가 실행까지 가는 것을 처음 표로 남긴다** (32 · 34 · 36).

---

## 5. 네 묶음 — 기존 서른하나가 안 바뀌었는가

`--runs 3` · `--context both` · 같은 세션에서 연달아 · 다른 것과 안 겹쳐 돌렸다.
**「전」과 「후」를 둘 다 both 로 돌렸다** — 문맥을 바꾼 몫이 섞이면 「발화를
더한 것이 기존을 흔들었는가」를 못 읽는다.

```
묶음1  전 (고치기 전 파일)   02:55~03:02
묶음2  후 (고친 파일)        03:02~03:10
묶음3  전                    03:10~03:17
묶음4  후                    03:17~03:25
```

### 묶음별 소계

```
묶음   기준선 아홉  확장 스물둘  화면 다섯   기존 서른하나   합계
 1 전   17/27        39/66        —           56/93          56/93   60%
 2 후   17/27        39/66        15/15       56/93          71/108  66%
 3 전   17/27        39/66        —           56/93          56/93   60%
 4 후   17/27        39/66        15/15       56/93          71/108  66%
```

**★ 발화별 판정이 네 묶음에서 한 글자도 안 다르다.** 1~31 번 서른한 발화의
(적중/근접/빗나감/못 붙음) 네 칸이 네 번 다 같은 값이다. 오늘 이 기계는
「마흔여덟째」가 적은 대로 결정론적이다.

```
1~31 중 네 묶음이 안 같은 발화 : 0개
전끼리(묶음1 = 묶음3)          : 같다
후끼리(묶음2 = 묶음4)          : 같다
전1 vs 후2 다른 번호           : 없다
근접 21 · 빗나감 16 · 못 붙음 0   네 묶음 모두
```

**1순위 물음의 답 : 안 바뀌었다.** 정답표에 발화를 더하는 것은 프롬프트를
안 건드리므로 기존 판정이 그대로여야 하고, 실제로 그랬다.

**★ 합계를 「전」과 「후」로 직접 견주지 않는다.** 60% → 66% 는 좋아진 것이
아니라 **분모가 93 에서 108 로 바뀐 것**이다. 새로 넣은 다섯이 15/15 라
평균을 끌어올렸을 뿐이고, 기존 서른하나는 56/93 그대로다.

**16 번은 말투를 고쳤는데 판정이 안 움직였다.** 묶음1·3(옛말 "많은")과
묶음2·4(새말 "있는")가 둘 다 `0/3 · 근접 3 · CLARIFY {009, 010, 011}` 이다.
**고친 값은 「스키마와 결이 맞는가」이지 판정이 아니다.**

### 2순위 — 화면 다섯이 새 기준선이다

```
 #  발화                       기대    적중   LLM 단독   조회 판정   status
32  여기 CCTV 보여줘           019    3/3    0/3        조회 적중   SELECT
33  지금 보이는 곳 CCTV 보여줘  026    3/3    3/3        조회 근접   SELECT
34  여기 행정구역 알려줘        018    3/3    3/3        조회 적중   SELECT
35  현재 화면 인구 알려줘       031    3/3    3/3        조회 적중   SELECT
36  여기 연령대별 인구 알려줘    054    3/3    3/3        조회 근접   SELECT
                              15/15   12/15   근접 0 · 빗나감 0 · 못 붙음 0
```

**★ 32 번에서 검산이 값을 했다.** LLM 은 `{019, 026}` 을 썼다 — 찍은 지점 CCTV 와
보이는 범위 CCTV 를 못 갈랐다. 축 조회가 `{019}` 로 좁혔고 3/3 다
(검산 표에 「검산이 살렸다」 3회). **「LLM 단독 ≪ 최종」의 실례**이고,
화면 다섯의 LLM 단독 12/15 와 최종 15/15 의 차이가 이 한 발화다.

**이것이 새 기준선이다.** 15/15 는 오늘 값이고, 화면 발화가 나빠지면 여기서
내려간다.

---

## 6. 무엇을 고쳤나 — 파일 셋

```
tools/check_resolve.py            --execute · 화면 다섯 · 16번 말투 · 기본 문맥 both ·
                                  묶음 셋으로 가름
tests/tools/test_execution_column.py   새 파일. 실행 칸 판정 아홉
tests/demo/api/test_menu_split.py      기존 둘을 사실에 맞춰 고치고 하나를 더함
NOTES.md                          이 글
```

**제품 코드 0줄.** 온톨로지 · menu · 배선표 · 프롬프트 · `demo/` · `vendor/` ·
`KRRI_ASAP/` 를 하나도 안 건드렸다.

### 시험을 어떻게 손댔나 — 지운 것 0, 이름 고친 것 1

```
더함  tests/tools/test_execution_column.py            9개
더함  test_정답표의_화면_다섯은_다_화면_낱말에_걸린다   1개
고침  test_check_resolve_의_세_가지_문맥               기본이 bbox → both
★이름 test_정답표_서른한_발화에_화면_낱말이_하나도_안_걸린다
      → test_말로_하는_서른하나에_화면_낱말이_하나도_안_걸린다
```

**이름을 고친 까닭.** 정답표가 서른여섯이 됐고 그중 32~36 은 **화면 낱말이
걸리라고 넣은 발화**다. 옛 이름 그대로 두면 시험 이름이 거짓이 된다.
**보는 것은 한 글자도 안 줄였다** — 1~31 번에 헛걸림이 없는지를 그대로 보고,
32~36 은 새로 더한 시험이 반대쪽(다 걸리는가)을 본다.

**지운 시험은 없다.** 「기존 삭제 0」에서 벗어난 것은 이 이름 하나뿐이고,
되돌리려면 그 한 줄만 되돌리면 된다.

---

## 7. 안 한 것

```
16 번을 실제로 되살리지 않았다     배선에 pledgeCategory 자리를 만드는 일이고
                                   그것은 제품 코드다. 이번 범위가 아니다
check_inputs 를 안 고쳤다          죽어 있는 것을 오늘 다시 확인했다.
                                   「쉰째」의 열린 과제 항목 그대로 둔다
화면 recipe 열네 벌은 여전히 정답표 밖   열아홉 중 다섯만 넣었다.
                                   나머지는 데이터 미적재이거나 되묻는 자리다
실행 칸을 여러 번 안 눌렀다        발화마다 한 번이다. 분포를 재는 칸이 아니다
문맥 bbox 로 다시 안 쟀다          네 묶음을 both 로만 돌렸다. bbox↔both 가
                                   기존 서른하나를 얼마나 흔드는지는 이번에 안 쟀다
                                   ★ 「전」과 「후」를 같은 문맥으로 돌린 것이
                                     이번에 답해야 할 물음이었다
커밋 안 했다                       사람이 화면을 보고 정한다
```

**★ 위 마지막 줄은 그 시점의 사실이다.** 사람이 보고 확인한 뒤 커밋했다 —
아래 절을 본다. push 는 안 했다.

---

## 8. 사람이 확인한 것과 남은 물음

사람이 두 판단을 확인했다 (2026-08-30).

```
시험 이름 변경   정답표가 서른여섯이 되고 32~36 은 화면 낱말이 걸리라고
                 넣은 발화이므로 옛 이름이 사실과 어긋난다. 고치는 것이 맞다
기본 문맥 both   bbox 뿐이면 찍은 지점 recipe 아홉을 아예 못 잰다
```

**★ 남은 물음 : bbox ↔ both 가 기존 서른하나를 얼마나 흔드는지 안 쟀다.**
이번 판정이 56/93 인데 「쉰다섯째」 관문은 52/93 이었다. **그 넷이 문맥
때문인지 날짜 드리프트인지 모른다.** 네 묶음을 둘 다 both 로 돌려 전·후
비교에서는 그 몫이 상쇄됐으므로 이번 판단에는 지장이 없다. 15분이면 잰다.

**★ 답 (2026-08-30 「쉰일곱째」에서 쟀다. 위 물음은 지우지 않는다).**
`bbox → both → bbox → both` 를 `--runs 3` 으로 한 세션에서 연달아 돌렸다.

```
              A bbox   B both   C bbox   D both
기존 서른하나  49/93    56/93    54/93    56/93
화면 다섯       6/15    15/15     6/15    15/15
```

```
흔드는가        ★ 흔든다. 기존 서른하나 중 다섯이 문맥만으로 갈린다
                  1 · 8 · 20 이 나빠지고 (3/3→0/3 · 3/3→1/3 · 3/3→1/3)
                  6 · 15 가 좋아진다 (0/3→3/3 둘)  합은 -1회
                  18 · 27 은 적중은 그대로인데 칸이 옮겨간다
무엇 때문인가    ★ menu 가 아니다. 기존 서른하나가 보는 menu 는 bbox 와
                  both 에서 3406자로 똑같다 (화면 낱말이 32~36 에만 걸림).
                  갈리는 것은 given 축 선택지다 — picked_point 한 줄이
                  붙어 4개 249자 → 5개 338자, 프롬프트 6399 → 6488자
그 넷은 뭐였나    both 는 두 번 다 56/93 이라 「쉰여섯째」와 같다. bbox 는
                  49 와 54 로 갈리고 「쉰다섯째」의 52 가 그 사이에 든다.
                  문맥의 순 효과가 -1회이므로 ★ 그 넷의 대부분은 드리프트다
드리프트 폭      both 둘은 서른여섯 발화가 한 칸도 안 다르다. bbox 둘은
                  아홉 발화가 갈린다 (9·12·13·14·21·24·25·26·31)
```

**기본값은 both 그대로 둔다.** 화면 다섯이 6/15 에서 15/15 가 되고, 기존
서른하나에서 잃는 것이 순 1회이며, both 쪽이 재현이 완벽하기 때문이다.
자세한 것은 「쉰일곱째」에 있다.

---

### 2026-08-30 (쉰다섯째) · 0건일 때 무엇으로 찾았고 어떻게 말하면 되는지 보인다

무인 실행. 모델 qwen3:32b(서버 기본) · recipe 60 · 정답표 서른하나 · `--runs 3` ·
문맥 bbox · 좁히기 끔. 브랜치 `integration/ASAP-Ontology`.
**사람이 저쪽 화면에서 눌러 보고 커밋했다** (아래 「사람이 저쪽 화면에서
확인했다」). push 는 안 했다.

**★ 결론부터.** 0건 · not_found 머리말이 한 줄에서 **두 줄**이 됐다. 늘어난 줄은
**어디를 뒤졌는가**(응답이 들고 온 데이터 이름)와 **어떻게 말하면 되는가**(0건과
not_found 를 갈라 다르게) 둘이다. **고친 파일은 `vendor/asap/workflow_answer.py`
하나다.** 0건이 아닌 답은 화면 실측 · 옛코드 대조 둘 다에서 한 바이트도 안 달라졌다.

---

## 왜 했나

화면이 이렇게만 말했다 (2026-08-30 실측).

```
찾지 못했습니다.

1. election.searchDistricts  query="국회의원 선거구"  0건
```

**왜 못 찾았는지도, 어떻게 말하면 되는지도 없다.** 시연에서 예상 못 한 발화가
들어와 0건이 나면 사용자가 거기서 멈춘다. 걸리는 자리는 셋을 실측으로 잡았다.

```
발화 6    국회의원 선거구 찾아줘   election.searchDistricts   0건
발화 9    충북 제1선거구 알려줘    election.getDistrict       status not_found
행정경계 보여줘                    adminBoundary.searchBoundaries  0건
```

**발화 12(「청주시 행정경계 보여줘」)는 오늘 0건이 아니었다.** 인자 추출이
`query="행정경계"` 가 아니라 `query="청주시"` 를 뽑아 4건이 나온다. 대신 같은
도구로 0건이 나는 자리를 실측으로 찾아 「행정경계 보여줘」로 갈음했다.
**발화 12 가 고쳐졌다는 뜻이 아니다** — 그 자는 흔들린다(같은 발화가 4건과
2단 사슬 1건 사이를 오갔다).

---

## 문구를 어떻게 정했고 무엇을 근거로 낱말을 댔나

### 가) 「무엇으로 찾았는지」는 머리말이 되풀이하지 않기로 했다

단계 줄이 이미 `query="국회의원 선거구"` 를 적는다 (`_input_text`, 「스물넷째」).
**같은 값을 두 줄에 적으면 늘어난 줄이 새로 알려주는 것이 없다.** 그래서 머리말은
단계 줄이 못 말하는 둘만 맡는다 — **어디를 뒤졌는가**와 **어떻게 말하면 되는가**.

### 나) 낱말의 출처 — ★ 온톨로지가 아니라 응답이다

「마흔아홉째」의 방식은 **이름을 코드에 안 두고 데이터에서 뽑아 넘기는 것**이었다
(`no_match_answer(reason, topics, starts)` · `_offer_names()` 가 `group_ids()` ·
`start_ids()` 로 낱말을 댔다). 그 방식은 지켰는데 **출처가 온톨로지가 아니다.**
까닭 둘 다 이 파일 밖의 사정이다.

```
부르는 자리가 다르다   NO_MATCH 는 execute_service 가 workflow_answer 를 직접
                       부른다 — 그래서 온톨로지에서 뽑아 인자로 넘길 수 있었다.
                       0건은 vendor 가 실패로 안 보므로(errors 가 빈다)
                       execute_service._answer 가 answer_draft 를 그대로 쓰고,
                       그 문구는 vendor 안에서
                       generic_mcp_executor._compose_workflow_answer 가
                       `compose_workflow_answer(intent, trace)` 로 만든다.
                       **그 파일은 불가침 다섯이라 인자를 못 늘린다**
의존이 거꾸로 선다     vendor 가 ontology 를 import 하면 방향이 뒤집힌다.
                       probe_tools.find_warning 을 안 가져다 쓴 것과 같은
                       까닭이다 (NOTICE_KEYS 위 주석)
```

**그래서 응답이 스스로 들고 온 이름을 쓴다.** 지어내지 않고 고르기만 한다.
`tools/probe_out` 240건을 세어 근거를 잡았다 (2026-08-30).

```
최상위 dataset 칸이 있는 응답      101건
그중 dataset.name 이 있는 것        85건
  "2024 제22대 국회의원 선거구" · "2024 제22대 국회의원 선거구 전체" ·
  "2024 제22대 국회의원 선거구 공약포함" · "2026 지방선거 시도별 교통 공약 요약" ·
  "한국환경공단 전기자동차 충전소" · "시군구"
name 이 없는 것                     16건  adminBoundary 둘(findBoundaryByPoint ·
                                          searchBoundaries)
```

**`dataset.source` 는 안 쓴다.** 그 칸이 들고 오는 것은 데이터의 출처지 사람이
읽을 이름이 아니다 — `"2026 지방선거 공약 GIS 프로젝트 행정구역 shapefile"` ·
`"election.shp"` · `"https://github.com/OhmyNews/2024_22_elec_map"`. **그래서
adminBoundary 의 0건에는 「찾아본 곳은」 줄이 통째로 안 붙는다.** 없는 것을
억지로 채우지 않는다.

**건수도 안 싣는다.** `dataset` 안에 `featureCount` · `districtCount` ·
`stationCount` 가 있는데, 단위를 모르는 수치를 이름 옆에 놓으면 딴 뜻으로 읽힌다.
이 파일이 `_measure_text` 에서 이미 겪었다 (`totalRegionCount 17` 이 충전소 수로
읽혔다, 「스물셋째」).

**★ "예: 청주" 같은 값은 하나도 안 박았다.** 무엇이 데이터에 있는지 이 파일은
모른다. 「데이터에 있는 이름을 그대로 말씀해 주세요」라고만 한다.

---

## 0건과 not_found 를 어떻게 갈랐나

**둘의 뜻이 다르고 사람이 할 일도 다르다.** 그래서 문구를 갈랐다.

```
0건        검색어로 훑었는데 아무 이름과도 안 겹쳤다
           → 낱말을 바꾸면 나올 수 있다        "다른 낱말로 다시 말씀해 주세요."
not_found  이름을 지정해 집어 오는 호출인데 그 이름이 데이터에 없다
           → 낱말을 바꾸는 것이 아니라 그 데이터에 있는 이름을 그대로 대야 한다
                                              "데이터에 있는 이름을 그대로 말씀해 주세요."
```

실측 근거 : `election.getDistrict` 는 `name="충북 청주서원"` 은 주고
`name="충북 제1선거구"` 는 not_found 다. **낱말을 바꾸라고 하면 틀린 안내가 된다.**

### 안내를 아예 안 붙이는 자리 둘

```
status "empty"       적재된 것이 없다는 뜻이다 (ev.getDatasetInfo ·
                     population.getDatasetInfo). 사람이 다시 말해서 될 일이
                     아니다. **될 리 없는 일을 시키지 않는다**
글자 인자가 없는 호출  좌표 · 번호로만 부른 단계다 (findBoundaryByPoint 의
                     lon · lat, road.getCctv 의 bbox 넷). 그 단계에 사람이
                     고쳐 말할 낱말이 없다. 「찾아본 곳은」 줄만 남는다
```

**가르는 것은 결과와 인자의 모양뿐이다.** 도구 이름을 아는 규칙을 하나도 안 박았다
— 이 파일의 계약이 그것이다 (파일 머리말).

---

## 무엇을 만들었나

```
vendor/asap/workflow_answer.py
  상수  DATASET_KEY · DATASET_NAME_KEY · DATASET_NAME_LIMIT(48) ·
        WHERE_LINE · RETRY_EMPTY · RETRY_OF_STATUS · GUIDE_JOIN
  함수  _empty_headline · _dataset_name · _retry_line · _called_with_words  넷
  고친 곳  compose_workflow_answer 의 EMPTY 갈래 한 줄뿐
tests/vendor/test_workflow_answer.py
  새 시험 일곱. **기존 삭제 0** (46 → 53)
```

---

## 화면 실측 아홉 (문구 그대로)

`POST /chat` · 문맥 bbox · 되묻기가 나오면 겨냥한 후보를 번호로 골랐다.
**같은 대본을 옛 코드로 한 번, 새 코드로 한 번 돌려 맞댔다.**

### 달라진 셋

```
########## 국회의원 선거구 찾아줘
찾지 못했습니다.
찾아본 곳은 「2024 제22대 국회의원 선거구」입니다. 다른 낱말로 다시 말씀해 주세요.

1. election.searchDistricts  query="선거구"  0건

########## 충북 제1선거구 알려줘 -> 1
고르신 것 — 1 국회의원 지역구 상세

찾지 못했습니다.
찾아본 곳은 「2024 제22대 국회의원 선거구」입니다. 데이터에 있는 이름을 그대로 말씀해 주세요.

1. election.getDistrict  name="충북 제1선거구"  조건에 맞는 선거구를 찾지 못했습니다.

########## 행정경계 보여줘
찾지 못했습니다.
다른 낱말로 다시 말씀해 주세요.

1. adminBoundary.searchBoundaries  query="행정경계"  0건
```

셋째가 **`dataset.name` 이 없는 자리**다. 「찾아본 곳은」 줄이 빠지고 안내만 남는다.

### ★ 안 달라진 여섯 — 옛 코드 · 새 코드가 한 바이트도 같다

```
########## 청주시 행정경계 보여줘
청주시 행정구역 경계를 조회했습니다.

1. adminBoundary.searchBoundaries  4건 · 청주시 상당구

########## 오송역 근처 충전소 자세히 알려줘 -> 2
고르신 것 — 2 장소 좌표 변환 -> 전기차 충전소 검색 -> 충전소 상세 조회

오송역 충전소 상세를 조회했습니다.

1. geo.geocode       오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1 (127.3277, 36.6200)
2. ev.searchStations  minLon=127.1598 · minLat=36.4852 · maxLon=127.4956 · maxLat=36.7547  500건 (전체 2,173건) · 청주오송역차고지
3. ev.getStation     statId="PK000313"  8건

########## 문서에서 철도안전법 관련 내용 찾아줘
조회하지 못했습니다.

1. web.search        query="철도안전법"  실패 · 이 도구를 쓸 권한이 없습니다

########## 오송역 CCTV 보여줘
오송역 CCTV 를 조회했습니다.

1. geo.geocode       오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1 (127.3277, 36.6200)
2. road.getCctv      minLon=127.1598 · minLat=36.4852 · maxLon=127.4956 · maxLat=36.7547  83건

########## 오송역 인구 구성 알려줘
오송역 연령대별 인구 구성을 조회했습니다.

1. geo.geocode       오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1 (127.3277, 36.6200)
2. adminBoundary.findBoundaryByPoint  lon=127.3277 · lat=36.6200  3건 · 충청북도
3. population.getAgeProfile  level="sigungu" · code="43113"  청주시 흥덕구 292,625명 (남 149,484 · 여 143,141) · 2026-06-30 기준

########## 지금 보이는 곳 CCTV 보여줘
조회하지 못했습니다.

1. geo.geocode       실패 · 장소 '지금 보이는 곳'을(를) 찾을 수 없습니다.
2. road.getCctv      radiusMeters=15000  실패 · 필수 입력값이 비어 있습니다: minLon, minLat, maxLon, maxLat
```

**「문서에서 철도안전법 관련 내용 찾아줘」가 RAG 조각 셋이 아니라 `web.search`
권한 없음으로 갔다.** 옛 코드에서도 똑같이 그랬다 — **이번 변경과 무관한
축 · 어휘 문제다.** 아래 「안 한 것」.

---

## 무엇으로 확인했나

### ★ 옛 코드 대조 — probe_out 240건을 통째로 다시 답으로 만들었다

화면 실측 아홉은 LLM 이 끼어 있어 회차마다 흔들린다. **흔들리지 않는 자로 한 번 더
쟀다.** `tools/probe_out` 의 응답 전문을 전부 읽어 인자 세 벌
(`{"query":"청주"}` · `{"lon":…, "lat":…}` · `{}`)로 trace 를 만들고, `git show
HEAD:` 로 꺼낸 **옛 workflow_answer 와 새 workflow_answer 가 같은 답을 내는지**
714쌍을 맞댔다.

```
같음        609
달라짐      105
★ EMPTY 판정이 아닌데 달라진 것    0건
```

**성공 · 오류 문구는 하나도 안 움직였다.**

### 판정 무손상

`tools/check_resolve.py --runs 3` (2026-08-30 · 모델 서버 기본 · recipe 60 ·
정답표 서른하나 · 문맥 bbox · 좁히기 끔).

```
합계            52/93 적중 · 근접 35 · 빗나감 6 · 못 붙음 0   56%   (LLM 단독 54%)
기준선 아홉     18/27 적중 · 근접  6 · 빗나감 3 · 못 붙음 0   67%   (LLM 단독 67%)
확장 열아홉     34/66 적중 · 근접 29 · 빗나감 3 · 못 붙음 0   52%   (LLM 단독 48%)
/resolve 평균   4.6초 (기준선 아홉 4.4 · 확장 열아홉 4.7)
```

**★ 「쉰넷째」(2026-08-30) 와 네 칸이 한 칸도 안 다르다.** 적중 52 · 근접 35 ·
빗나감 6 · 못 붙음 0 이 그대로다. 소수 칸(기준선 아홉의 LLM 단독 67%, /resolve
평균 4.4초)까지 같은 자리에 있다.

**답 문구는 프롬프트에 안 실린다.** 이 작업은 `menu.yaml` · 프롬프트 넷 ·
온톨로지 · 배선표를 한 글자도 안 건드렸으므로 판정이 움직이면 그것이 범위를
넘었다는 뜻이다.

### pytest

```
424 passed / 1 failed  (전 417 passed / 1 failed 에서 새 시험 일곱만큼 늘었다)
tests/vendor/test_workflow_answer.py  46 → 53.  기존 삭제 0
```

남은 실패 하나는 graphviz 판 차이로 음성 대조군이 안 걸리는 것이다
(`test_dense_graph_would_move_if_overlap_removal_were_used`, 「쉰셋째」 2-6).
이번 변경과 무관하다.

---

## 사람이 저쪽 화면에서 확인했다

2026-08-30, 사람이 저쪽 화면에서 직접 눌렀다.

**0건 · not_found 문구 — 뜻대로 갈렸다.**

```
"국회의원 선거구 찾아줘" → 「1 국회의원 지역구 검색」
  찾지 못했습니다. 찾아본 곳은 「2024 제22대 국회의원 선거구」입니다.
  다른 낱말로 다시 말씀해 주세요.
    election.searchDistricts  query="선거구"  0건

"충북 제1선거구 알려줘" → 「1 국회의원 지역구 상세」
  찾지 못했습니다. 찾아본 곳은 「2024 제22대 국회의원 선거구」입니다.
  데이터에 있는 이름을 그대로 말씀해 주세요.
    election.getDistrict  name="충북 제1선거구"  조건에 맞는 선거구를 찾지 못했습니다.
```

**시연 대본 여섯 — 안 깨졌다.**

```
오송역 좌표 · CCTV 82건 · 연령대별 인구 292,625명 ·
4단 사슬(상세 8건) · 문서에서 철도안전법 조각 셋 ·
지금 보이는 곳 CCTV 6건
```

**★ 「문서에서 철도안전법 관련 내용 찾아줘」가 저쪽 화면에서는 knowledge.query 로
제대로 간다.** 측정에서 web.search 로 샜던 것은 문맥을 안 실었을 때다.

---

## 안 한 것

```
★ 0건이 아니라 엉뚱한 답이 나오는 자리는 못 고친다
    "전기차 충전소 알려줘"  → 120건 (첫 결과 "일출랜드")
    "대전시 인구 구성"      → 청송군 24,020명 (저쪽 지오코더가 POI 검색이다)
  **답 문구 층의 일이 아니다. 축 · 어휘 문제라 뒤로 미룬다.**
  화면은 0건도 오류도 아닌 「성공」으로 찍히므로 이번에 넓힌 머리말이
  아예 안 걸린다. 그것을 잡으려면 결과가 발화와 맞는지를 봐야 하는데
  이 파일은 발화를 모른다

"문서에서 …" 가 web.search 로 가는 것도 안 고쳤다
  같은 까닭이다. 옛 코드에서도 똑같이 갔다 (위 실측)

dataset.source 를 안 썼다
  adminBoundary 둘은 「찾아본 곳은」 줄이 빠진 채로 둔다.
  출처 문자열은 사람이 읽을 이름이 아니다

dataset 안의 건수를 안 실었다
  단위를 모르는 수치다 (_measure_text 의 totalRegionCount 17 교훈)

EMPTY_HEADLINE 을 안 바꿨다
  "찾지 못했습니다." 첫 줄을 그대로 뒀다. 판정이 그 줄이고, 그 줄에 매인
  기존 시험 넷이 손 안 대고 통과해야 이번 변경이 넓히기만 한 것이 된다

STEP_OF headline 을 명사형으로 안 바꿨다
  EMPTY_HEADLINE 위 주석이 적어 둔 48줄짜리 일이다. 이번에도 범위 밖이다

온톨로지에서 낱말을 못 뽑았다
  위 「나)」. 부르는 자리가 vendor 안이라 인자를 못 늘리고, 인자를 늘리려면
  generic_mcp_executor(불가침) 나 execute_service(불가침) 를 고쳐야 한다.
  **그 둘 중 하나가 열리면 온톨로지 낱말로 바꾸는 것이 낫다** —
  「그 데이터에 있는 이름」보다 「제가 다루는 것은 …입니다」가 더 멀리 안내한다
```

---

### 2026-08-30 (쉰넷째) · 고른 경로 위로 흐르는 표시를 얹는다

무인 실행. 모델 qwen3:32b(서버 기본) · recipe 60 · `--runs 3` · 문맥 bbox ·
좁히기 끔. **커밋 안 함.** 브랜치 `integration/ASAP-Ontology`.

**★ 결론부터.** 굵은 teal 실선은 그대로 두고 **그 위에 같은 길을 따라가는 점선
사본 하나를 얹어 그것만 움직인다.** 원본 `<path>` 를 한 글자도 안 고치므로
캡처(정지 그림)는 지금과 **PNG 한 바이트까지 같다**(실측). 새 의존성 0 ·
CDN 0 · 좌표 변경 0 · 사람이 정한 값 변경 0.

---

## 왜 했나

하단에서 고른 실행 경로가 굵은 teal 선(penwidth 8)과 화살촉(arrowsize 2.5)으로
보인다. **정지 그림이라 어느 쪽으로 흐르는지 한눈에 안 들어온다.**

「쉰셋째」에서 사람이 순번(xlabel)을 지웠다 — 노드가 커지면서 순번이 노드에
가렸다. **그래서 방향 단서가 화살촉뿐이 됐다.** 시연의 핵심 장면인 4단 사슬
(「오송역 근처 충전소 자세히 알려줘」)에서 「좌표 → 검색 → 상세」 순서가 보여야
한다. 그 자리를 메운다.

---

## 두 길 중 무엇을 골랐나

프롬프트가 준 두 길은 이랬다.

```
가) SVG 가 만들어진 뒤 브라우저에서 얹는다 (JS 가 사본을 만든다)
나) DOT 에 표시를 붙여 CSS 로 고른다
```

**둘을 갈라 고르는 것이 아니었다.** 재보니 이렇다.

```
나) 만으로는 안 된다   CSS 는 요소를 복제할 수 없다. CSS 로 할 수 있는 것은
                      원본 <path> 에 stroke-dasharray 를 거는 것뿐인데,
                      그러면 **원본 선 자체가 점선이 된다.**
                      관문("원본 선이 그대로 남는다")을 바로 어긴다
가) 만으로는 약하다    사본은 만들 수 있는데 무엇을 복제할지 고르는 손잡이가
                      색이나 굵기밖에 없다. 색은 상수라 바뀔 수 있고,
                      굵기(8)로 고르면 등록 경로(PATH_NEW)와 물러난 경로
                      (PATH_NEW_DIM)까지 함께 걸린다 — 셋이 같은 굵기다
```

**그래서 가)로 얹고, 고르는 손잡이만 나)에서 가져왔다.** `build_dot` 이 해석
경로에만 `class="flow"` 를 붙이고(`dot.FLOW_CLASS`) JS 는 그것만 복제한다.

`class` 는 레이아웃 속성이 아니다. Graphviz 2.43.0 이 `<g class="edge flow">`
로 그대로 내보내고 좌표 · 색 · 굵기는 하나도 안 바뀐다(실측).

---

## 값과 근거

**전부 `demo/ui/components/flow.py` 맨 위 한 곳에 있다. 시연장에서 여기만
고치면 된다.**

단위는 SVG 사용자 단위(= DOT 포인트)이지 화면 픽셀이 아니다. 하단 그래프는
viewBox **2383 x 811** 을 **523px** 높이 패널에 `xMidYMid meet` 으로 넣는다
(패널 높이는 `styles.panel_heights` 실측값). 그래서 배율은 아무리 커도
523/811 = **0.645** 이고 폭에 걸리면 그보다 작다. 아래는 0.45~0.65 범위에서
고른 값이다.

(**정정** — 아래 표는 기계가 처음 고른 값이다. 그날 사람이 화면을 보고
`FLOW_DASH` · `FLOW_GAP` · `FLOW_OPACITY` 셋을 바꿨다. 지금 값과 왜 그렇게
됐는지는 아래 「사람이 화면을 보고 정한 값」 절에 있다. 첫 값을 지우지 않고
남긴다 — 무엇에서 무엇으로 갔는지가 다음 사람에게 값이다.)

| 값 | 크기 | 화면(배율 0.45~0.65) | 왜 |
|---|---|---|---|
| `FLOW_DASH` | 32 | 14~21px | 이보다 짧으면 점처럼 보이고 방향이 안 읽힌다 |
| `FLOW_GAP` | 56 | 25~36px | 칸이 길어야 아래 teal 실선이 계속 보인다. 주인공은 여전히 원본 선이다 |
| `FLOW_PERIOD_SECONDS` | 1.1 | 초속 36~52px | 한 칸(88단위 = 40~57px)을 1.1초에 지난다. 걷는 속도로 읽힌다. 0.5초대는 깜빡임처럼 보여 시선을 뺏는다 |
| `FLOW_WIDTH` | 4 | — | `PATH_PENWIDTH`(8)의 절반. 양옆에 teal 이 2단위씩 남아 원본 선이 테두리처럼 계속 보인다 |
| `FLOW_SHEEN` · `FLOW_OPACITY` | `#FFFFFF` · 0.5 | — | **팔레트에 색을 새로 안 더한다.** 흰빛을 반쯤 섞어 teal 자신을 밝힌다 |

색 대비(배경 `#0E1117` 기준, 계산):

```
합성색 #8ADCD2   대비 11.92   teal(7.59)의 1.57배
```

노드 테두리(8.76)보다 밝다. **위계가 뒤집힌 것은 아니다** — 면이 아니라 4단위
줄이고, 주기의 32/88 = 36%만 그 자리에 있고, 고른 경로 위에만 있다. teal 위에서
확실히 보이는 것은 무엇이든 teal(7.59)보다 밝을 수밖에 없다.

---

## 사람이 화면을 보고 정한 값

2026-08-30, 사람이 브라우저에서 보며 정했다. **여기가 지금 값이다.**

```
FLOW_DASH     32 -> 50
FLOW_GAP      56 -> 50    ★ 대시와 빈틈이 같아 주기의 절반씩이다
FLOW_OPACITY  0.5 -> 1.0  ★ 합성이 아니라 순백이 됐다
FLOW_PERIOD_SECONDS 1.1 · FLOW_WIDTH 4   그대로
```

화면 환산이 이만큼 달라진다(배율 0.45~0.65).

| 값 | 크기 | 화면 |
|---|---|---|
| `FLOW_DASH` | 50 | 22~32px |
| `FLOW_GAP` | 50 | 22~32px |
| 한 칸(주기) | 100 | 45~65px · 1.1초에 지나므로 초속 41~59px |

색 대비(배경 `#0E1117` 기준, 계산):

```
순백  #FFFFFF   배경 18.90   teal 위 2.49
```

teal 계통을 밝히는 안(`#99F6E4` · teal-200)도 검토했다 — 팔레트와 같은 계통이라
"같은 색이 빛난다" 로 읽히고 배경 대비 14.99 · **teal 위 1.97** 이다.
**사람이 순백(teal 위 2.49)을 골랐다.** 팔레트의 다른 색은 뜻이 있어 못 쓴다 —
금색은 「대상」, 분홍은 「새로 생긴 것」, 보라는 「관련」이다. 흰색만 색상(hue)이
없어 새 뜻을 안 만들고, teal 선 위에서 "다른 색이 지나간다" 가 아니라 "그 선이
밝아진다" 로 읽힌다.

노드 테두리(8.76)보다 밝은 것은 그대로다. **위계가 뒤집힌 것은 아니다** — 면이
아니라 4단위 줄이고, **주기의 절반**(50/100)만 그 자리에 있고, 고른 경로 위에만
있다. 앞 절의 36% 는 대시 32 · 빈틈 56 일 때 값이고 지금은 50%다.

**주석도 이 값으로 맞췄다**(`flow.py`). 값이 바뀌었는데 주석이 옛 값을
설명하고 있으면 다음에 고치는 사람이 없는 근거를 믿는다.

---

## 지킨 것

```
원본 선이 그대로 남는다   사본을 붙일 뿐 원본 <path> 에 손대지 않는다.
                        JS 가 안 도는 곳(캡처 · 보도자료 이미지)에서는
                        사본이 아예 안 생긴다
움직임을 끌 수 있다      prefers-reduced-motion 이면 아무것도 안 얹는다.
                        화면이 지금과 한 픽셀도 다르지 않다
줌 · 팬과 함께 돈다      사본이 SVG 안에 있고 zoom.py 는 <svg> 에 CSS
                        transform 을 건다. 확대하면 대시도 함께 커진다
상단 · 하단이 한 벌      zoom.py 와 같은 규칙. 상단은 draw_solid=False 라
                        걸리는 엣지가 0 이고 아무 일도 안 일어난다
SVG 를 갈아끼워도 다시   하단은 노드를 누를 때마다 innerHTML 을 통째로 바꾼다.
                        zoom.py 와 같은 자리에서 MutationObserver 로 다시 얹는다
예외를 안 올린다         matchMedia · style 삽입 · 복제 · 관찰자 넷 다 try/catch.
                        시연 중에 그래프가 죽는 것이 가장 나쁘다
```

---

## 무엇으로 확인했나

**브라우저를 못 연다**(chromium · playwright · selenium 없음, 새 의존성 금지).
둘로 나눠 봤다.

```
정지로   같은 DOT 을 class 있는 것 · 없는 것 두 벌로 neato -Tpng 했다.
         610,613 바이트로 **한 바이트도 안 다르다.**
         tests/demo/graph_svg/test_path_flow.py 가 이것을 지킨다
문서로   focus_html · graph_fill_html 이 낸 iframe 문서에 @keyframes
         recipeflow · stroke-dashoffset · 값 셋 · 선택자가 들어 있는지 봤다.
         streamlit 런타임이 필요 없는 순수 함수라 AppTest 도 안 썼다
완성 SVG POST /render 응답에서 **표시가 붙은 엣지 집합 == 굵은 teal 실선 엣지
         집합** 인지 봤다. 단위 테스트만 두면 조립부가 인자를 안 넘겨도
         통과한다(group_attrs · review_edges 에서 두 번 당했다)
```

### 판정 무손상

`tools/check_resolve.py --runs 3` (2026-08-30 · 모델 서버 기본 · recipe 60 ·
정답표 서른하나 · 문맥 bbox · 좁히기 끔). **기준선(2026-08-29)과 한 칸도 안 다르다.**

```
합계            52/93 적중 · 근접 35 · 빗나감 6 · 못 붙음 0   56%   (LLM 단독 54%)
기준선 아홉     18/27 적중 · 근접  6 · 빗나감 3 · 못 붙음 0   67%
확장 열아홉     34/66 적중 · 근접 29 · 빗나감 3 · 못 붙음 0   52%
/resolve 평균   4.6초 (기준선 아홉 4.3 · 확장 열아홉 4.7)
```

그릴 것만 고쳤으니 판정이 안 움직이는 것이 맞다 — **움직였다면 그것이 더
이상한 일이다.** 그래도 잰다. 이 화면은 판정 결과를 그리는 자리라 한 번도
안 재고 넘어가면 무엇이 흔들렸는지 나중에 못 가른다.

---

### ★ 사람이 눈으로 확인할 것

**하단 그래프에서 굵은 teal 경로 위로 밝은 대시가 「좌표 → 검색 → 상세」 방향으로
흐르는지, 그리고 그 속도가 거슬리지 않는지.** (거슬리면 `flow.py` 의
`FLOW_PERIOD_SECONDS` 를 올린다. 대시가 잘 안 보이면 `FLOW_DASH` 를 올린다.)

---

## 안 한 것

```
등록 장면(주황)은 안 흐른다   해석 경로와 굵기 · 화살표가 같아 함께 걸리기
                            쉬운데, 여러 갈래가 동시에 흐르면 방향이 오히려
                            안 읽힌다. teal 하나만 흐른다
순번(xlabel)을 안 되살렸다     「쉰셋째」에서 사람이 지운 것이다. 노드에 가리는
                            문제가 그대로 남아 있다
원본 선의 굵기 · 색 · 화살촉을 안 건드렸다
                            PATH_PENWIDTH 8 · PATH_ARROWSIZE 2.5 그대로
flow.py 가 demo/graph_svg 를 import 하지 않는다
                            계층을 지킨다. 대신 class 이름이 두 곳에 있고
                            tests/demo/ui/test_path_flow_document.py 가 두
                            값이 같은지 붙잡는다
```

**「파이썬이 변형을 만들고 JS 는 고르기만 한다」와의 관계.** JS 가 요소를
하나 더 만들므로 글자 그대로는 어긋난다. 그런데 **그리는 규칙(색 · 굵기 ·
순번)은 여전히 build_dot 한 곳에만 있다** — 사본은 파이썬이 이미 그린 `d` 를
그대로 복제할 뿐이고 좌표를 새로 계산하지 않는다. 무엇을 복제할지도 파이썬이
`class` 로 정한다. 원칙이 막으려던 것(두 곳이 조용히 어긋나는 것)은 안 생긴다.

---

### 2026-08-29 (쉰셋째) · 좌표에 _init 사본을 두고, 배치를 다시 만든다

무인 실행. 모델 qwen3:32b(서버 기본) · recipe 60 (안 늘었다) · 정답표 서른하나 ·
`--runs 3` · 문맥 bbox · 좁히기 끔. **1부는 커밋했고 2부는 안 했다.**

**★ 결론부터.** 화면 글씨가 4.84pt 에서 **19.81pt** 로, 칸 폭 사용이 35% 에서
**100%** 로 올랐다. (**정정** — 그 뒤 사람이 노드 글씨를 32 에서 24 로 되돌려
지금은 **15.11pt** 다. 2-10 · 2-11 참고. 폭 100% 와 「확대 없이 읽힌다」는
그대로다.) **확대하지 않아도 노드 이름이 읽힌다.** 사람이 고른 크기
(글씨 32 · 노드 1.8x0.9in · 순번 53)를 한 글자도 안 줄이고 겹침 0 을 지켰다.
좌표 배율(DRAW_SCALE)은 0.64 에서 1.0 으로 되돌렸다 — 배치가 처음부터
촘촘하게 놓이므로 줄일 것이 없다.

---

## 1부 · 좌표에 _init 사본

`layout.json` 이 `.gitignore` 였고 근거는 "좌표는 캐시일 뿐이다" 였다.
**두 가지로 그 전제가 깨졌다.**

```
가) 사람이 고른 배치가 파일에만 남는다
    이번에 배치 후보를 여덟 벌 만들어 사람이 표와 그림을 보고 하나를 골랐다.
    어느 후보를 골랐는지는 코드 어디에도 없고 좌표 파일에만 있다
나) reset_to_init() 이 좌표를 안 되돌리고 있었다
    온톨로지 · recipe · menu 는 되돌리는데 좌표는 그대로 남았다.
    시연에서 노드를 등록하고 초기화를 눌러도 그 노드의 좌표가 남는다 —
    ★ 시연을 두 번 하면 두 번째는 첫 배치가 아니다
```

온톨로지 · recipe · menu 와 같은 꼴로 만들었다.

```
demo/graph_svg/_init/layout.json    git 이 추적한다. 사람이 고른 배치
demo/graph_svg/layout.json          작업본. .gitignore 그대로
```

`.gitignore` 의 `layout.json` 을 `/demo/graph_svg/layout.json` 으로 바꿨다.
**앞의 경로가 없으면 `_init` 사본까지 함께 무시된다** — 패턴 하나로 둘 다
사라질 뻔한 자리다.

`registry.reset_to_init()` 은 좌표를 **함수 안에서** import 한다. 좌표는
그리기(`demo/`)의 자산이고 핵심이 시연 계층을 모듈 수준에서 의존하면
`demo/` 를 지울 때 import 가 깨진다. 시험으로 박았다.

**범위 밖으로 한 곳 나갔다 — `tests/conftest.py`.** `reset_to_init()` 이
좌표를 되돌리게 되자 `isolated_workspace` 가 좌표를 격리하지 않아
**pytest 가 시연용 작업본을 `_init` 으로 덮어썼다.** 작업본에 표시를 넣고
`pytest tests/ontology/test_registry.py` 를 돌려 표시가 사라지는 것을 확인했고,
격리한 뒤 다시 돌려 남는 것을 확인했다. 이 fixture 가 원래 막으려던 사고와
같은 것이다.

---

## 2부 · 배치를 다시 만든다

### 2-1. 왜 다시 만들었나

사람이 크기를 글씨 32 · 노드 1.8x0.9in 로 올렸는데 옛 배치로는 겹쳤다.

```
옛 배치(subset)에 사람이 원한 크기를 씌우면      겹치는 쌍   최소간격
  DRAW_SCALE 1.0                                    0      9.0pt
  DRAW_SCALE 0.64                                  39    -23.0pt
  DRAW_SCALE 0.4                                  129    -67.3pt
      지방선거 교통 공약 상세 x 국회의원 전체 선거구 데이터 정보  가로 -207pt
      국회의원 선거구 공약 상세 x 지점 국회의원 선거구 공약 판별  가로 -193pt
      국회의원 전체 선거구 상세 x 지점 국회의원 지역구 판별       가로 -188pt
★ 저장소의 겹침 시험 계산으로 다시 재도 같다 (여유 8pt 기준 151쌍)
```

**좌표를 균등하게 줄이는 것으로는 못 푼다.** 간격만 줄고 노드 크기는 그대로라
원래 촘촘하던 곳이 먼저 붙는다. 「쉰두째」가 "폭 41% 는 배치 모델이 만든
모양이라 좌표 배율로는 못 편다" 고 적은 그 자리다.

### 2-2. ★ model=subset 의 근거가 사라졌다

**옛 배치가 큰 원인이 `model=subset` 이었다.** 그 값을 고를 때의 근거는
"교차 12 -> 3, 최소 간격 8.1 -> 50.3pt" 였는데, 그것은 노드가 다섯인 시험
그래프에서 잰 값이다. **노드 48개인 지금 다시 재니 근거가 남아 있지 않다.**

```
                     캔버스        교차
model=subset      5357x5348       101
기본값(shortpath) 2054x1726       101      ← 교차는 같고 캔버스만 2.6배 작다
```

기본값으로 되돌렸다. 그것만으로 화면 글씨가 3.13 -> 9.70pt 가 됐다.
`NEATO_ATTRS` · `NEATO_FRESH_ATTRS` 둘 다 바꿨다 — 최초와 증분이 같은 모델을
써야 새 노드가 같은 규칙으로 놓인다.

### 2-3. 늘리고 겹침만 다시 없앤다

힘 기반 배치는 등방이라 늘 동그랗게 나온다(H/W 0.84). 화면 칸은 가로로 길다
(1500x523px = 0.35). **가로로 늘린 뒤 겹침만 다시 없애면 칸 모양에 맞는다.**

```
1  neato 기본모델 + overlap=voronoi 로 최초 배치
2  x 좌표만 SPREAD_X(5.0) 배
3  그 자리를 시작 위치로 넘겨(pin=False) neato 를 다시 —
   overlap=prism · maxiter=0 · sep="+12"
   ★ maxiter=0 이 배치 반복을 건너뛰므로 늘려둔 모양이 그대로 남고,
     prism 은 겹친 노드만 국소적으로 밀어낸다
```

`build_dot` 에 `pin` 키워드를 더했다(키워드 전용 · 기본값 True · 기본 출력
불변). 못박으면 겹침 제거가 할 일이 없다.

**★ 배치가 그리는 것과 같은 조건을 봐야 했다.** 처음 붙였을 때 프로덕션이
19.81pt 가 아니라 15.21pt 로 나왔는데, `ensure_positions` 가 노드 이름을
두 줄로 접지 않고 넘기고 있었다. 배치가 계산한 상자와 화면에 뜨는 상자가
달라 겹침 제거가 헛돈 것이다. 셋을 다 넘기게 했다 — **두 줄 접기 · 노드 크기 ·
점선 라벨 끄기.** 그래서 `wrap_label`/`wrap_node_labels` 를 `build.py` 에서
`dot.py` 로 옮겼다(`build.py` 가 다시 내보내므로 부르던 곳은 그대로다).
`layout_store` 가 `build` 를 import 하면 순환이 된다.

### 2-4. 후보 표

```
후보                              겹침  최소간격     캔버스     H/W  폭사용 높이사용  화면글씨  화면노드  교차
가) 옛 배치 subset · 그릴 때 0.64배   2   -16.3  3499x3454  0.987   35%  100%   4.84pt  26x12  101
나) 옛 배치 subset · 배율 1.0        0    51.8  5357x5348  0.998   35%  100%   3.13pt   17x8  101
다) 기본모델 · 안 늘림                0     8.1  2054x1726  0.840   41%  100%   9.70pt  53x24  101
라) 기본모델 x3.5 prism sep+8        0    16.0  2199x928   0.422   83%  100%  18.03pt  98x45   98
마) 기본모델 x5.0 prism sep+8        0    15.2  2276x790   0.347  100%  100%  21.09pt 115x53  113
바) 기본모델 x5.0 prism sep+10       0    17.8  2345x803   0.342  100%   98%  20.47pt 111x51  120
사) 기본모델 x5.0 prism sep+12       0    24.0  2423x829   0.342  100%   98%  19.81pt 108x50  116  ← 골랐다
아) 기본모델 x6.0 prism sep+12       0    21.3  2486x886   0.356   98%  100%  18.89pt 103x47  120
겹침은 여유 8pt 기준. 화면 글씨 = 32 x 맞춤배율. 하단 칸 1500x523px
```

**SVG · PNG** (저장소에 안 남긴다. `/tmp/p2/`) —

```
A_now_subset_0.64  B_major_voronoi  C_x3.5_sep8  D_x5.0_sep8  E_x5.0_sep12  (.png/.svg)
FINAL_bottom.png · FINAL_top.png · FINAL_resolve.png   고른 배치. 화면 픽셀 크기 그대로
```

### 2-5. 고른 값과 근거

```
바탕      neato 기본모델 + overlap=voronoi   subset 의 근거가 사라졌다 (2-2)
늘리기    x 좌표만 5.0배                     4.0 은 79%, 5.0 에서 폭·높이를 둘 다
                                            100% 쓴다. 6.0 은 오히려 준다
겹침 제거  overlap=prism · maxiter=0          늘려둔 모양을 지키면서 겹친 쌍만 민다
여유      sep="+12"                          +8 이 21.09pt 로 가장 크지만 가장
                                            가까운 쌍이 15.2pt 다. +12 는 24.0pt —
                                            글씨 6% 를 내주고 여유를 1.6배 얻는다.
                                            ★ 19.81pt 는 이미 확대 없이 읽히므로
                                              더 키우는 것보다 이름이 길어져도
                                              안 붙는 쪽이 값어치가 크다
DRAW_SCALE 0.64 -> 1.0                      배치가 촘촘하니 줄일 것이 없다
```

**교차는 101 -> 116 으로 늘었다.** 15개 늘어난 값을 크기 4배와 바꿨다.
그림으로 보면 늘어난 교차는 「선거」 별 안쪽에 몰려 있고, 그 별은 원래도
읽히는 자리가 아니었다.

### 2-6. ★ 겹침 제거를 썼다 — 음성 대조군의 뜻

`test_dense_graph_would_move_if_overlap_removal_were_used` 는 "핀이 있는데
overlap 제거를 쓰면 노드가 움직인다" 를 남겨둔 음성 대조군이다.

**뜻은 안 바뀌었다.** 겹침 제거를 쓰는 자리가 하나에서 둘로 늘었지만
(최초 voronoi · 늘리기 prism) **둘 다 좌표를 못박지 않는 자리다.**
못박는 자리는 증분 배치 하나뿐이고 거기에는 여전히 `overlap` 이 없다.
지켜야 할 것은 "겹침 제거를 쓰지 마라" 가 아니라 "핀과 함께 쓰지 마라" 다.

시험을 둘 더했다.

```
test_overlap_removal_is_not_used_when_pinning   NEATO_SPREAD_ATTRS 도 함께 봄
test_spreading_never_pins                       늘리기가 내는 DOT 에 느낌표가
                                                없어야 함
```

`test_dense_graph_would_move_if_overlap_removal_were_used` 는 이 기계의
graphviz 2.43 에서 원래부터 실패한다(drift 0.0). 이번 변경과 무관하고
고치지 않았다.

### 2-7. ★ 「등록해도 안 움직인다」의 기준선이 새로 잡혔다

배치를 통째로 다시 만들었으므로 **좌표가 전부 바뀌었다.** 이 성질이 지키는
것은 "어떤 특정 좌표" 가 아니라 "등록 전후가 같다" 이므로 기준선만 옮겨졌다.
새 배치에서 다시 쟀다.

```
등록 뒤 기존 노드 최대 어긋남   0.004pt   (시험 기준 0.51pt)
새 노드 자리                   (1161.4, 40.0)
등록 뒤 겹침                    0쌍 · 최소간격 22.6pt
```

**증분 모델을 기본값으로 바꾼 것이 여기서도 낫다** — `model=subset` 으로
증분을 돌리면 어긋남이 0.08pt 로 기본값(0.004pt)의 20배였다. 둘 다 시험은
통과하지만 같은 모델을 쓰는 편이 실제로 덜 흔들린다.

### 2-8. 눈으로 본 것

```
하단 (FINAL_bottom.png · 1500x523px)
  ★ 확대하지 않고 노드 이름이 전부 읽힌다. 「국회의원 전체 선거구 검색」
    「지점 행정구역 판별」 「연령별 인구 구성 조회」 가 그대로 읽혔다
  덩어리가 넷으로 보인다 — 왼쪽 아래 「전기차 충전」, 가운데 왼쪽 「선거」,
    아래 가운데 「교통」, 오른쪽 「인구」. 금색 타원 넷이 그 중심이다
  ★ 「선거」 별 하나에 몰려 보이던 것이 나아졌다. 예전에는 화면 왼쪽 절반이
    비고 오른쪽에 전부 몰려 있었는데, 지금은 넷이 가로로 늘어서 있다
  빈 곳은 맨 위 한 줄이다 — 아무 관계도 없는 노드 넷(3D 모델 목록 조회 ·
    문서 목록 조회 · 표고 데이터 정보 · VWorld 데이터 정보)이 거기 뜬다.
    ★ 폭·높이를 이미 100% 쓰고 있으므로 손해는 아니다
  선이 노드를 통과해 비치지 않는다(채우기 + stack_nodes_on_top)
하단 · 「오송역 좌표 보여줘」 (FINAL_resolve.png)
  SELECT · recipe_001. 「말한 장소」에서 「장소 좌표 변환」으로 굵은 teal 선이
  가고 순번 「1」이 선 옆에 크게 뜬다. 두 노드 테두리도 teal 이다.
  ★ 확대 없이 어느 길이 켜졌는지 읽힌다
상단 (FINAL_top.png · 1630x538px)
  점선만의 관계 지도. 금색 타원 넷이 각자 자기 무리를 거느린 모양이 한눈에
  보인다. 실선이 없어 하단보다 한산하고 「선거」의 부챗살이 특히 뚜렷하다
```

### 2-8-1. 관문 열셋

```
겹침 0             상단 · 하단 둘 다 0쌍 (여유 8pt). 가장 가까운 쌍 24.0pt
등록 불변          기존 노드 최대 어긋남 0.004pt (시험 기준 0.51pt)
크기 유지          글씨 32 · 노드 1.8x0.9in · 순번 53. 한 글자도 안 줄였다
★ 폭 사용률       35% -> 100% (안 늘린 기본모델만으로도 41%)
★ 화면 글씨       4.84pt -> 19.81pt. 「쉰두째」의 4.20pt 를 4.7배 넘는다
_init 과 작업본     같은 파일이다 (diff 0)
판정 무손상        52/93 · 근접 35 · 빗나감 6 · 못 붙음 0   기준값과 같다
/graph · /render   둘 다 200 · SVG · viewBox 2422.60x829.00
새 의존성 없음      requirements.txt 변화 0
저쪽 무결          KRRI_ASAP/ · vendor/ 변경 0
온톨로지 등 무손상  ontology/ · workflows/ · tools/ · demo/api/ 변경 0
pytest             397 passed / 1 failed (graphviz 음성 대조군)
                   1부 394 에서 셋 늘었다. 기존 삭제 0
결정적             layout.json 을 지우고 두 프로세스에서 다시 만들어 같은 파일
```

**★ 판정은 두 판을 돌렸다.** 첫 판이 51/93 이었고 갈린 것은 1번 발화
「오송역 위치 보여줘」의 세 회 중 한 회뿐이다(`{004}` 로 빗나감). 두 번째 판이
52/93 로 기준값과 같다. **「쉰두째」에서도 같은 발화가 같은 방식으로 흔들렸다**
— 그때는 51 · 52 · 52 였고 이번은 51 · 52 다. 코드 탓이 아니다.
`/resolve` 는 그리기 쪽으로 `dot.COLORS` 하나만 닿고 색은 이번에도 안 고쳤다.

**★ 정정 (2026-08-30). 위 표는 사람이 값을 손보기 전의 것이다.** 그 뒤
2-10 의 변경이 들어오면서 **pytest 가 397/1 에서 395/3 이 됐다.**
늘어난 실패 둘은 둘 다 시험 쪽이고 화면은 멀쩡하다(`/render` 200 ·
굵기 8 · 노드 테두리 6).

```
test_order_labels_survive_no_layout   순번(xlabel)을 지웠으니 ">1<" 이 없다.
  (test_layout_invariants.py:313)     시험이 「순번이 보여야 한다」를 요구하는데
                                      사람이 일부러 없앤 것이다
test_order_uses_xlabel_not_label      PATH_PENWIDTH 가 3 -> 8 이라 "penwidth=3"
  (test_layout_is_fixed.py:153)       인 줄을 못 찾는다. 상수를 안 보고 숫자를
                                      박아둔 자리다. 순번과는 상관이 없다 —
                                      제 검사에 닿기도 전에 걸린다
```

**둘 다 이번에 안 고쳤다** — 「코드를 한 줄도 고치지 않는다」는 규칙으로 돈
작업이고, 첫째는 요구사항이 바뀐 것이라 시험을 지울지 남길지가 사람의 판단이다.

### 2-10. 사람이 화면을 보고 정한 값들

2026-08-29 저녁, 사람이 브라우저에서 보며 손으로 정했다. **아래는 파일에서
읽은 값이다** — 2-1 ~ 2-8 의 측정은 그 전 상태(글씨 32)에서 잰 것이라
숫자가 다른 자리가 있다. 2-11 에 지금 값으로 다시 잰 표를 뒀다.

```
PATH_PENWIDTH              3 -> 8      고른 경로가 굵어야 한눈에 보인다
PATH_ARROWSIZE           1.6 -> 2.5    선이 굵어진 만큼 화살촉도 키웠다
HIGHLIGHT_NODE_PENWIDTH   새로 둠 = 6
  ★ 코드 한복판(dot.py 389줄)에 박혀 있던 숫자 2 를 상수로 뺐다
ORDER_FONTSIZE            40 -> 53
순번(xlabel)을 지웠다      dot.py 428줄을 주석으로 막았다
  ★ 노드가 4배 커지면서 순번이 노드에 가렸다
  ★ 그래서 방향 단서가 화살촉뿐이 됐다 — 다음 작업이 그것을 다룬다

노드 크기는 되돌렸다        글씨 32 -> 24 · 노드 1.8x0.9 -> 1.3x0.6in
  ★ 배치는 32 로 잡은 것을 그대로 쓴다. 상자가 작아진 쪽이므로 겹치지 않는다 —
    가장 가까운 쌍이 24.0pt 에서 40.1pt 로 오히려 벌어졌다(실측)

확대 폭도 다시 잡았다 (zoom.py)
  MIN_SCALE   0.5 -> 0.9
  MAX_SCALE  12.0 -> 2.5
  ZOOM_STEP   1.3 -> 1.03
  ★ 「쉰두째」가 올려둔 값을 사람이 내렸다. 배치를 다시 만들어 확대 없이
    읽히게 됐으므로 확대는 미세 조정용이 됐다는 뜻이다
```

점선(`DOTTED_PENWIDTH` 1.6)과 고른 경로(8)의 위계는 그대로다 —
배경 실선 1 < 점선 1.6 < 고른 경로 8.

**★ 낡은 주석이 셋 남았다. 이번에 안 고쳤다** (코드를 안 건드리는 작업이었다).

```
zoom.py 17~18줄    "한 칸이 30% 라 바닥에서 끝까지 아홉 번" — 지금은 3% 다
dot.py 155~159줄   ORDER_FONTSIZE 를 "순번 글씨 크기" 라고 적고 있는데
                   순번을 안 그린다. 상수도 쓰이지 않는 자리에 남아 있다
                   (주석 처리된 줄 다음의 fontsize=53 만 붙는다)
dot.py 306·315줄   build_dot docstring 이 순번 규칙을 설명한다
```

### 2-11. 지금 값으로 다시 잰 것

2-4 의 표는 글씨 32 에서 잰 것이다. 사람이 24 로 되돌린 뒤 같은 배치를 다시 쟀다.

```
              겹침  최소간격     캔버스     H/W  폭사용 높이사용  화면글씨  화면노드  교차
글씨 32(잰 때)   0    24.0   2423x829  0.342  100%   98%  19.81pt 108x50  116
글씨 24(지금)    0    40.1   2383x811  0.340  100%   98%  15.11pt  85x39  116
옛 배치(비교용)  2   -16.3  3499x3454  0.987   35%  100%   4.84pt  26x12  101
```

**결론은 안 바뀐다.** 폭 사용 35% -> 100%, 화면 글씨 4.84 -> **15.11pt** 로
3.1배다. **확대하지 않아도 노드 이름이 읽힌다**는 것도 그대로다
(`/tmp/p2/NOW_bottom.png` · `NOW_top.png` 로 확인했다).

### 2-12. 지오코딩이 엉뚱한 곳을 준다 — 0건보다 나쁘다

2026-08-29 화면 실측이다.

```
"대전시 인구 구성 알려줘" -> 되묻기 -> 「1 연령별 인구 구성 조회」
  1. geo.geocode  대전시 -> ★ 경상북도 청송군 주왕산면 상의리 202-3
                          (129.1458, 36.3956)
  2. adminBoundary.findBoundaryByPoint -> 3건 · 경상북도
  3. population.getAgeProfile  level="sigungu" · code="47750"
                               청송군 24,020명
```

저쪽 `geo.geocode` 는 VWorld 를 `type="place"` · `size=1` 로 부르고 `items[0]`
을 쓴다 (「스물다섯째」에서 코드로 확인). 장소(POI) 검색이라 "대전시" 라는
이름의 POI 가 없으면 부분 일치로 엉뚱한 곳이 첫 번째로 걸린다.

```
★ "대전광역시 인구" 가 0건인 것보다 나쁘다. 답이 나오는데 다른 도의 군이다
★ 시연에서 "대전시" 같은 행정구역 이름을 쓰지 않는다
  "대전역" 은 실제 POI 라 제대로 걸린다 (대덕구 164,047명 · 정답표 20번)
우리가 못 고친다 — 저쪽 코드다
```

**어휘 계층 항목과 짝이다.** 거기는 이름이 안 걸려 0건이 되는 자리이고,
여기는 **좌표를 거치는 길로 우회해도 엉뚱한 좌표가 나오는 자리**다.
「되묻기에서 어느 쪽을 고르느냐로 0건이 갈린다」의 반대편이 이것이다 —
어느 쪽을 골라도 "대전시" 는 안 된다.

### 2-13. 순번을 지우면서 시험 둘이 함께 사라졌다

사람이 순번(xlabel)을 안 그리기로 했다 — 노드가 커지면서 순번이 노드에 가렸다.
**그 요구사항을 지키던 시험 둘을 함께 지웠다.**

```
test_order_labels_survive_no_layout   「순번이 보여야 한다」
  (test_layout_invariants.py)
test_order_uses_xlabel_not_label      「label 말고 xlabel 을 쓴다」
  (test_layout_is_fixed.py)
  ★ 이 시험이 지키던 뜻은 「강조가 노드를 밀지 않는다」였다.
    엣지 라벨이 아예 없어져 저절로 참이 된다.
    다시 라벨을 붙이는 날이 오면 그 성질을 다시 재야 한다
    (label 을 쓰면 노드가 밀린다 — 실측으로 남아 있다).
```

**★ 시험을 남기고 통과만 시키지 않았다.** 무엇을 지키는지 모르는 시험이 된다.
`test_order_uses_xlabel_not_label` 은 사실 순번 때문이 아니라 `PATH_PENWIDTH`
가 3 -> 8 이 되면서 먼저 걸렸다(`"penwidth=3"` 인 줄을 못 찾는다). 상수를 안
보고 숫자를 박아둔 자리였는데, **고쳐서 살릴 수도 있었지만 지웠다** — 살려봐야
지킬 성질이 남아 있지 않다.

**실측은 코드에 남겼다.** `build_dot` 의 「제약」이 순번 규칙을 말하던 자리를
「엣지에 라벨을 붙이지 않는다」로 바꾸고, `label` 이 노드를 민다는 실측은
「이력」 절로 옮겼다 — 다시 붙이는 사람이 그 함정을 다시 밟지 않게.

**dot.py 에서 함께 지운 것.**

```
ORDER_FONTSIZE            쓰는 데가 하나도 없었다 (주석 처리된 줄 다음의
                          fontsize=53 만 붙고 있었다)
orders 계산 (7줄)          「경로가 정확히 하나면 번호를 매긴다」. 유일한
                          소비자가 위의 xlabel 이었다
엣지에 순번을 붙이던 6줄     주석 처리된 줄과 그 아래 fontsize 까지
docstring 넷              highlight · highlight_paths · dim_edges · 제약
```

**남겨둔 자국이 있다. 이번 범위 밖이라 안 건드렸다.**

```
demo/graph_svg/build.py 10 · 193 · 231줄      "순번" 을 말하는 주석 셋
demo/graph_svg/focus.py 23줄                  "순번을 붙여야 함"
demo/ui/components/zoom.py 6줄                 "색·굵기·순번을 정하는 규칙"
demo/ui/components/focus_panel.py 7 · 111줄    같은 문장 둘
tests/ontology/test_graph.py 239줄             "순번 라벨(1, 2, 3)" — 거기서
                                               지키는 것은 순서이지 순번이 아니다
demo/ui/components/zoom.py 17~18줄             확대 한 칸을 "30%" 라고 적었는데
                                               사람이 1.03(3%)으로 내렸다
```

**★ 대가 : 4단 사슬에서 실행 순서를 보이는 단서가 화살촉뿐이 됐다.**
다음 작업이 그것을 다룬다 — 고른 경로 위로 흐르는 표시.

### 2-9. 안 한 것

```
휠을 손으로 굴려 보기      이 장비에 브라우저가 없다(chromium · playwright ·
                          selenium 전부). 새 의존성 금지라 설치하지 않았다.
                          「쉰두째」와 같은 방법으로 화면 픽셀 크기 PNG 를 떠서
                          봤다
맨 위 빈 줄 채우기         관계가 없는 노드 넷이다. pack · defaultdist 를 걸어
                          봤지만 결과가 한 픽셀도 안 바뀌었다. 폭·높이를 이미
                          다 쓰고 있어 급하지 않다
가로세로를 다른 비율로 누르기  「쉰두째」에서 이미 졌다. 이번 방식은 누르는
                          것이 아니라 늘린 뒤 겹침을 다시 없애는 것이라 다르다
demo/ui/ 의 여백 · 높이     한 줄도 안 고쳤다
```

### 2026-08-29 (쉰두째) · 그래프를 키우고 확대 폭을 넓힌다 — 화면만 고친다

무인 실행. 모델 qwen3:32b(서버 기본) · recipe 60 (안 늘었다) · 정답표 서른하나 ·
`--runs 3` · 문맥 bbox · 좁히기 끔. **커밋 안 했다.**

**★ 결론부터.** 화면 글씨가 1.81pt 에서 4.20pt 로, 화면에 뜨는 노드가 11x5px 에서
24x11px 로 커졌다. **둘 다 2.3배다.** 겹침은 0 그대로이고 좌표 파일은 한 글자도
안 고쳤다 — 그릴 때만 0.64 를 곱한다. 확대는 5배에서 12배로 늘리고 한 칸을
1.12 에서 1.3 으로 키웠다. 판정은 안 건드렸다.

#### 1. 왜 했나

**2026-08-28 시연 뒤 하정목 박사님 피드백 셋 중 둘.**

```
② 확대 단계가 너무 적다. 더 크게 확대되면 좋겠다
③ 노드와 글씨가 잘 안 보인다. 노드끼리 거리가 너무 멀다
① 끌 수 있는 동적 그래프 — 이번에 안 했다. 아래 「열린 과제」에 새 항목
```

**③ 은 정확한 지적이었고 원인은 fontsize 가 아니었다.** 화면 글씨는
`fontsize x 맞춤배율` 이고 맞춤배율은 캔버스 크기가 정한다. `dot.py` 주석에
"16 이 화면에서 10.1pt" 라고 적힌 것은 온톨로지가 작아 캔버스가 세로 830pt 이던
때의 값이다. 지금은 노드가 48개이고 캔버스가 5375x4616pt 다.

```
하단 그래프 칸 1500x523px (2560x1440 화면 · viewport 1282 · bottom_left_ratio 0.60)
상단 그래프 칸 1630x538px
맞춤배율 = min(1500/5375, 523/4616) = 0.113   ← 높이에 걸린다
화면 글씨 16 x 0.113 = 1.81pt = 2.4px         ← 점으로 보인다는 말이 맞았다
가장 가까운 두 노드 사이 78.1pt              ← "너무 멀다" 도 맞았다
```

#### 2. 무엇을 어떻게 쟀나

노드 상자는 `neato -n -Tdot` 이 내주는 pos · width · height 로 잡았다. SVG 로
안 잰 이유는 `style="rounded,filled"` 라 노드가 `<path>` 로 나와 사각형을
되찾으려면 경로 문자열을 파싱해야 하기 때문이다. 같은 DOT · 같은 엔진이고
출력 형식만 다르다.

**겹침 0 만으로는 안 골랐다. 여유 10pt 를 함께 요구했다** — 0 으로 두면
이름 한 글자가 길어진 다음 등록에서 바로 붙는다.

#### 2-1. 옛 주석에서 옮겨 온 실측 (지우지 않으려고 여기 남긴다)

`dot.py` 의 fontsize 주석을 줄이면서 걷어낸 숫자다. **온톨로지가 작던 때의
값이고 지금 기준값이 아니다.**

```
화면 글씨          11 -> 7.3pt · 16 -> 10.1pt   (캔버스가 세로 830pt 이던 때)
종횡비             0.72 -> 0.75                 글씨 11 -> 16 로 올렸을 때
칸 폭 사용         55% -> 53%                   같은 조건. 2%p 손해라 16 을 골랐다
두 줄 접기를 풀면   노드 150~245pt · 종횡비 0.78  그 폭이 회전 후 높이가 된다
겹침               fontsize 18 · 한 줄까지 0쌍   그때는 제약이 겹침이 아니라 종횡비였다
```

#### 3. 후보 표

**가) 글씨마다, 겹침 0 + 여유 10pt 를 지키는 가장 작은 좌표 배율**

```
글씨   배율   겹침  최소간격   전체 크기    H/W    화면글씨   화면노드
 16   0.51    0     12.3   2807x2380   0.848    3.52pt   22x10px
 20   0.57    0     11.0   3130x2662   0.851    3.93pt   23x11px
 22   0.60    0     10.1   3293x2803   0.851    4.11pt   24x11px
 24   0.64    0     10.7   3508x2989   0.852    4.20pt   24x11px   ← 골랐다
 28   0.73    0     10.6   3994x3410   0.854    4.29pt   24x11px
 32   0.80    0     10.3   4374x3737   0.854    4.48pt   24x11px
```

**나) 고른 글씨(24)에서 배율만 바꾼 것 — 어디서 붙는지**

```
글씨   배율   겹침  최소간격   전체 크기    화면글씨
 24   1.00    0     58.8   5413x4632    2.71pt
 24   0.80    0     32.1   4355x3719    3.37pt
 24   0.70    0     18.7   3826x3263    3.85pt
 24   0.64    0     10.7   3508x2989    4.20pt   ← 골랐다
 24   0.60    0      4.8   3297x2807    4.47pt   ← 겹치진 않으나 여유가 없다
 24   0.55    2     -6.6   3032x2579    4.87pt   ← 붙는다
 24   0.50    2    -18.0   2768x2351    5.34pt
 24   0.40    4    -40.9   2239x1895    6.63pt
```

**다) 최소 크기 · 고치기 전과의 대조**

```
                       겹침  최소간격   전체 크기    화면글씨  화면노드   노드폭
고치기 전 16/1.00        0     78.1   5375x4616   1.81pt   11x5px   54~148pt
최소크기만 더함 16/1.00   0     78.1   5406x4616   1.81pt   11x5px   94~148pt
고른 값 24/0.64          0     10.7   3508x2989   4.20pt   24x11px  94~205pt
```

**SVG 후보** (저장소에 안 남긴다. `/tmp/graph_probe/`) —

```
now_16_1.00.svg    고치기 전
cand_16_0.51.svg   cand_20_0.57.svg   cand_24_0.64.svg   cand_32_0.80.svg
final_bottom.png · final_top.png · resolve.png   화면 픽셀 크기 그대로 뽑은 것
final_bottom_zoom.png                            2.6배로 확대한 것
```

#### 4. 고른 값과 근거

```
글씨      16 -> 24        표 가) 에서 계단이 꺾이는 자리다.
                          16->20 이 +12%, 20->24 가 +7%, 24->28 은 +2% 다.
                          24 가 마지막으로 의미 있는 계단이다
좌표 배율  1.0 -> 0.64     글씨 24 에서 겹침 0 을 지키는 하한이 0.62,
                          여유 10pt 까지 요구하면 0.64 다
노드 최소  없음 -> 1.3x0.6in  짧은 이름의 노드가 54pt 까지 줄어 긴 이름(205pt)과
                          네 배가 났다. 94pt 로 바닥을 깔면 두 배 남짓이 된다.
                          **공짜였다** — 하한 배율이 0.64 로 그대로였고
                          캔버스는 3493 -> 3508pt, 0.4% 만 늘었다
순번 글씨  26 -> 40        노드 글씨보다 확실히 커야 실행 순서가 라벨 위로 뜬다.
                          옛 비(26/16 = 1.63)를 지킨 값이다.
                          xlabel 이라 캔버스가 안 커진다 — 26 · 40 · 48 이
                          전부 3508x2989 였다(실측)
```

**★ 좌표 파일을 안 고치고 그릴 때만 곱한다** (`layout_store.DRAW_SCALE` ·
`for_drawing`). 증분 배치는 기존 노드를 `pos="x,y!"` 로 박고 neato 를 돌리는데,
그 핀은 저장된 좌표계의 값이어야 한다. 파일을 줄여 쓰면 다음 등록의 핀도 줄어든
좌표가 되고 스프링 길이(`len` 4.0 / 0.7)는 안 줄어 있으므로 새 노드가 옛 노드와
다른 규칙으로 놓인다. 「등록해도 기존 노드가 안 움직인다」가 지키던 것이 흐려진다.
그릴 때만 곱하면 저장된 좌표가 배치의 단일 출처로 남고, 상수 하나를 1.0 으로
되돌리면 화면이 전과 똑같아진다.

**등록 장면을 흉내내 확인했다.** 실제 온톨로지에 노드를 하나 얹어 증분 배치를
돌리니 기존 노드가 **0.0pt** 움직였고, 새 노드는 (2104.7, 2210.7) 에 놓여
0.64 배 뒤에도 겹치지 않았다. 시험으로 박았다.

#### 5. 안 골랐고 왜 안 골랐나

**가) 좌표를 가로세로 다른 비율로 누르기.** 배치가 H/W 0.85 인데 칸은 0.35 라
폭을 41% 밖에 못 쓴다. 세로만 눌러 칸 모양에 맞추면 폭 사용이 89% 로 오른다.
**그런데 화면 글씨는 안 커졌다** — sx 1.0 · sy 0.45 에서 3.97pt 로, 균등하게
줄인 16/0.51 의 3.52pt 보다는 낫지만 24/0.64 의 4.20pt 보다 못하다. 32칸을
훑어 가장 좋은 것이 sx 0.9~1.2 · sy 0.6 의 4.47pt 였고, 균등 0.64 대비 +6% 를
얻자고 그림을 찌그러뜨릴 이유가 없다. **폭 41% 는 그대로 남는 숙제다** —
배치 모델이 만든 모양이라 좌표 배율로는 못 편다. 「열린 과제」의 동적 그래프에서
같이 본다.

**나) 글씨를 더 키우기(28 · 32).** 화면 노드가 24x11px 에서 안 커진다.
글씨만 +2%, +7% 늘고 노드 폭이 237 · 270pt 로 벌어져 짧은 이름과의 차이만 는다.

**다) 칸 높이 늘리기.** 상단과 하단이 화면 높이를 반씩 나눠 갖는다. 한쪽을
늘리면 다른 쪽이 준다. `demo/ui/` 는 한 줄도 안 고쳤다.

#### 6. 확대 상수 — 전후

```
              전      후     왜
MIN_SCALE    0.5    0.5     그대로. 1배에서 두 칸이면 바닥이다
MAX_SCALE    5.0   12.0     12배면 노드 하나가 290px 로 화면을 채운다.
                            더 가면 이웃이 안 보여 지도가 아니게 된다
ZOOM_STEP   1.12    1.3     한 칸 +30%
```

```
굴리는 횟수                        전      후
1배에서 끝까지                    14.2    9.5
글씨가 읽히기 시작하는 2.6배까지    8.4    3.6
바닥(0.5)에서 끝까지              20.4   12.1
```

**★ 상단과 하단이 한 스크립트를 쓴다.** `zoom_script()` 가 키만 바꿔 두 벌을
만들고, 두 문자열이 키를 빼면 완전히 같음을 확인했다(실측). 한쪽만 고쳐질 수 없다.

#### 7. 화면에서 본 것

**★ 브라우저를 못 열었다.** 이 장비에 브라우저가 없다(chromium · playwright ·
selenium · rsvg 전부 없음). **새 의존성을 안 더한다는 규칙이 있어 설치하지 않았다.**
대신 화면에 뜨는 것과 같은 것을 두 갈래로 확인했다.

```
가) 문서를 그대로 만들어 봤다
    streamlit.testing.v1.AppTest 로 demo/ui/main.py 를 돌렸다 — 예외 0,
    st.components.v1.html 두 번(상단 · 하단)
    그 둘에 들어가는 문서를 뽑아 보니 iframe 높이 상단 538 · 하단 523,
    하단 그래프 칸이 flex 60%, 두 문서 모두 MAX = 12.0 · STEP = 1.3 · <svg> 있음
나) 그 SVG 를 화면 픽셀 크기 그대로 PNG 로 떠서 봤다
    graphviz 에 -Tpng -Gdpi=72*맞춤배율 을 줬다. 새 도구를 안 쓴다
```

**본 것 (글로)**

```
고치기 전 (now_16_1.00)
  노드가 11x5px 라 상자가 아니라 점으로 보인다. 글자는 얼룩이다.
  선이 점들 사이를 길게 가로지르고 화면 왼쪽 절반이 거의 비어 있다
고친 뒤 (하단 · 1500x523)
  노드가 모서리 둥근 상자로 보인다. 글자는 여전히 못 읽지만 "두 줄이 들어
  있다" 가 보인다. 대상 노드의 금색 타원이 기능 노드와 확실히 갈린다.
  붙어 보이는 쌍은 없다
고친 뒤 (상단 · 1630x538)
  점선만 그린 관계 지도. 「선거」와 「교통」과 「인구」를 중심으로 한 별
  모양 셋이 갈려 보인다. 실선이 없어 하단보다 훨씬 한산하다
2.6배로 확대 (final_bottom_zoom)
  글자가 읽힌다 — 「국회의원 지역구 검색」 「전기차 충전소 검색」 「지점
  행정구역 판별」 이 그대로 읽혔다. 겹친 쌍 없음.
  ★ 이 배율이 예전에는 여덟 번, 지금은 네 번이면 닿는다
발화 「오송역 좌표 보여줘」 (resolve.png)
  SELECT · recipe_001. 굵은 teal 선 하나가 「장소 좌표 변환」에서 「말한
  장소」로 이어지고 순번 「1」이 선 위에 뜬다. 확대 없이도 강조가 읽힌다.
  응답 SVG 안에 #14b8a6 여섯 · font-size 24 와 40 두 종류
```

**★ 확대를 굴려 보지는 못했다.** 스크립트가 위아래 같은 것임은 문자열로
확인했지만, 휠이 실제로 어떻게 느껴지는지(한 칸 30%가 너무 큰가)는 사람이
봐야 한다. **1.3 이 거칠면 zoom.py 의 ZOOM_STEP 한 줄만 고치면 된다.**

#### 8. 관문 여덟

```
판정 무손상        52/93 · 근접 35 · 빗나감 6 · 못 붙음 0   기준값과 같다
좌표 불변          test_adding_a_node_does_not_move_the_existing_ones 통과.
                   layout.json 은 안 고쳤다 (git status 에 안 뜬다)
겹침 0             새 시험 넷 통과
/graph · /render   둘 다 200 · SVG · width/height 없이 viewBox 만
새 의존성 없음      requirements.txt 변화 0
저쪽 무결          KRRI_ASAP/ · vendor/ 변경 0
온톨로지 등 무손상  ontology/ · workflows/ · tools/ 변경 0 (git status)
pytest             384 passed / 1 failed (graphviz 음성 대조군). 전 380/1 에서
                   새 시험 넷이 늘었다. 기존 삭제 0
```

**★ 첫 판이 51/93 로 나와 세 판을 돌렸다.** 관문이 "다르면 멈추고 보고한다" 라
멈추기 전에 재는 자를 먼저 의심했다.

```
1판  변경 있음   51/93 · 근접 35 · 빗나감 7 · 못 붙음 0
2판  변경 없음   52/93 · 근접 35 · 빗나감 6 · 못 붙음 0   ← git stash 로 되돌려 잼
3판  변경 있음   52/93 · 근접 35 · 빗나감 6 · 못 붙음 0
```

갈린 것은 1번 발화 「오송역 위치 보여줘」의 세 회 중 한 회뿐이다(3/3 -> 2/3,
`{004}` 로 빗나감). **변경이 만든 것이 아니다** — 같은 코드로 51 과 52 가 다
나왔다. 「★★ 재는 자를 먼저 의심한다」가 말하던 그 흔들림이다.

**구조로도 확인했다.** `resolve_service` 에서 import 를 34개 모듈까지 따라가면
그리기 쪽으로 닿는 것은 `demo.graph_svg.dot.COLORS` 하나뿐이고
(`ontology_service` 가 색만 물어본다), **이번에 색은 한 글자도 안 고쳤다.**
크기 · 좌표 · 확대는 `/resolve` 가 보지 않는다.

#### 9. 안 한 것

```
끌 수 있는 동적 그래프    피드백 ①. 그리는 방식을 바꾸는 일이다.
                         「열린 과제」에 새 항목으로 세웠다
폭 41% 를 채우는 것       배치 모델이 만든 모양이라 좌표 배율로는 못 편다.
                         찌그러뜨려 본 결과는 위 5-가) 에 남겼다
demo/ui/ 의 여백 · 높이   한 줄도 안 고쳤다. 상하가 높이를 반씩 나눠 갖는
                         구조라 한쪽을 늘리면 다른 쪽이 준다
확대를 손으로 굴려 보기    브라우저가 없다. 위 7 참조
```

### 2026-08-29 (쉰한째) · 되묻기 뒤 고르기를 스위치 뒤에 둔다 — 기본은 켬

무인 실행. 모델 qwen3:32b(서버 기본) · recipe 60 (안 늘었다) · 정답표 서른하나 ·
`--runs 3` · 문맥 bbox · 좁히기 끔. **커밋 안 했다.**

**★ 결론부터.** 환경변수 `CLARIFY_CHOICE` 를 두고 `0` 일 때만 끈다. **없으면 켬**
이라 지금 화면이 한 글자도 안 달라진다. 기능은 지우지 않았다 — 끌 수 있게만 했다.
고친 것은 `clarify_service` 에 읽는 함수 하나와 `execute_service` 의 부르는 자리
둘뿐이고, **resolve 경로는 바이트까지 그대로다.**

#### 1. 왜 했나

「서른넷째」가 되묻기 뒤에 번호로 고르는 길을 만들면서 세션마다 후보를 메모리에
잠깐 기억하게 했다(5분 · 한 번 쓰면 지움 · 저장 안 함).

**2026-08-28 하정목 박사님 피드백** — 메모리를 어떻게 쓸지 논의하기 전에 세션을
쓰는 것을 넣지 말자. 관리가 어려워진다.

**사람의 판단 (2026-08-29).** 지우지 않는다. 끌 수 있게 한다. **기본은 켬** —
정답표 서른한 발화 중 서른셋 회가 되묻기라, 끄면 그것들이 막다른 길이 된다.
박사님께는 "환경변수 하나로 끕니다. 메모리 설계가 정해지면 그 설계로 옮깁니다"
라고 설명한다.

#### 2. 무엇을 했나

좁히기(`RESOLVE_NARROW`)와 같은 결로 뒀다. 읽는 함수를 서비스에 두고 부르는
자리가 그것을 본다. **다른 것은 기본값의 방향뿐이다.**

```
좁히기    RESOLVE_NARROW=1 일 때만 켬 (기본 끔)   아직 화면에 안 나온 새 길
되묻기    CLARIFY_CHOICE=0 일 때만 끔 (기본 켬)   이미 화면에서 도는 길
```

```
demo/api/services/clarify_service.py   CHOICE_ENV · enabled()      +27줄
demo/api/services/execute_service.py   chat · _remember_clarify    +25 −8
tests/demo/api/test_clarify_choice.py  스위치 시험 셋              +23줄
tests/demo/api/test_clarify_flow.py    스위치 시험 여섯            +75 −4
```

**끄면 기억도 안 한다.** 고르기만 막고 후보를 메모리에 남기면 끈 것이 아니다 —
끄는 까닭이 세션을 안 쓰는 것이므로 `_remember_clarify` 도 스위치를 본다.
시험 `test_끄면_기억해_두지도_않는다` 가 그것을 박는다.

**`/chat` 에 인자를 안 더했다.** `/chat` 은 저쪽 화면의 계약이고, 환경변수만으로
켬·끔을 둘 다 화면에서 봤다. 인자를 더할 까닭이 안 생겼다.

#### 3. 화면 실측 — 켬 둘 · 끔 둘

`uvicorn` 둘을 띄우고 `POST /chat` 을 그대로 불렀다. 켬은 8000(환경변수 없음),
끔은 8010(`CLARIFY_CHOICE=0`). 문맥은 화면과 같은 고정 bbox 다.

**★ 프롬프트가 적은 「청주시 인구 알려줘」는 오늘 되묻기가 안 난다.** 오늘 자로
그 발화는 SELECT 3/3 이라 후보 목록이 화면에 안 뜬다(적중 표 29번). 그래서
되묻기가 확실히 나는 「오송역 근처 충전소 자세히 알려줘」(문맥 없이 CLARIFY 3/3)로
쟀다. **값을 지어내지 않았다.**

```
켬 (8000 · 환경변수 없음)
  "오송역 근처 충전소 자세히 알려줘"
      어느 것을 보시겠습니까?
        1  전기차 충전소 검색 -> 충전소 상세 조회
        2  장소 좌표 변환 -> 전기차 충전소 검색 -> 충전소 상세 조회
  "1번"
      고르신 것 — 1 전기차 충전소 검색 -> 충전소 상세 조회

      오송역 충전소 상세를 조회했습니다.
      1. ev.searchStations  12건 · 청주오송역차고지
      2. ev.getStation     statId="PK000313"  8건
                                              ★ 실행됐다

끔 (8010 · CLARIFY_CHOICE=0)
  "오송역 근처 충전소 자세히 알려줘"
      어느 것을 보시겠습니까?
        1  전기차 충전소 검색 -> 충전소 상세 조회
        2  장소 좌표 변환 -> 전기차 충전소 검색 -> 충전소 상세 조회
                                              ★ 켬과 문구가 같다
  "1번"
      지금 할 수 있는 일 중에 맞는 것이 없습니다.

      사용자 요청이 없습니다.

      제가 다루는 것은 교통 · 선거 · 인구 · 전기차 충전입니다.
      말한 장소 · 말한 키워드 · 말한 식별자 가운데 하나를 함께 말씀해 주세요.
                                              ★ 새 발화로 갔다
```

**되묻기 문구는 켬·끔이 같다.** 후보 두 줄도 그 아래 reason 도 바이트까지 같았다.
끄는 것은 고르기뿐이고 화면에 보이는 되묻기는 안 건드린다.

#### 4. 화면 실측 — 시연 대본 여섯 (켠 상태)

한 세션에서 순서대로 쳤다. 문맥은 고정 bbox.

```
1  오송역 좌표 보여줘              geo.geocode                          (127.3277, 36.6200)
2  오송역 CCTV 보여줘              geo.geocode -> road.getCctv          83건
3  오송역 연령대별 인구 알려줘     geocode -> findBoundaryByPoint -> getAgeProfile
                                                                       청주시 흥덕구 292,625명
4  오송역 근처 충전소 자세히       geocode -> searchStations -> getStation
                                   ★ 문맥을 실으면 SELECT 라 되묻기가 안 났다.
                                     문맥 없이 부르면 같은 발화가 CLARIFY 다 (3절)
5  문서에서 철도안전법 관련 내용   knowledge.query  6건 · 철도안전법(제21188호) 1·2·32쪽
6  지금 보이는 곳 CCTV 보여줘      road.getCctv  83건 (문맥 bbox 로 바로 감)
```

**여섯 다 안 깨졌다.** 5번은 문맥을 안 실었을 때 `web.search` 권한 없음으로 빠지는데
(「스물셋째」에 적힌 그 자리), 화면과 같은 문맥을 실으면 `knowledge.query` 로 붙는다.
**측정 harness 가 문맥을 빼먹으면 5·6번이 실패로 보인다** — 다음에 잴 사람은
`{"view": {"bbox": ...}}` 를 반드시 싣는다.

#### 5. ★ 판정 — 안 움직였다. 다만 기준선 숫자가 「쉰째」와 다르다

```
             적중     근접   빗나감   못 붙음   기준선 아홉   확장 열아홉
  내 트리   52/93      35       6        0       18/27         34/66
  내 트리   52/93      35       6        0       18/27         34/66    (다시 잰 것)
  ★ HEAD    52/93      35       6        0       18/27         34/66    (git stash 로 뺀 뒤)
  「쉰째」  51/93      33       9        0       15/27         36/66    2026-08-29 앞 작업
```

**내 변경이 판정을 한 글자도 안 움직였다.** 위 세 줄은 적중 표가 바이트까지 같다
(`diff` 로 확인). 셋째 줄은 `git stash` 로 내 diff 를 통째로 뺀 순수 HEAD 다 —
**같은 표가 나온다.** `resolve` 경로(`resolve_service` · `orchestrator/` ·
`ontology/` · `workflows/` · `llm_engine/` · `models.yaml`)는 `git diff` 가 0줄이라
움직일 수가 없는 변경이다.

**★ 그런데 오늘 자 기준선이 「쉰째」가 적은 51/33/9 가 아니라 52/35/6 이다.**
「쉰째」는 HEAD(`22f9b78`)로 커밋돼 있고 그 사이에 저장소가 바뀐 것이 없다.
**코드가 아니라 재는 자가 움직였다** — 「마흔여덟째」의 「67/93 이 오늘 다시 안
나온다」와 같은 자리다. 그 항목이 정한 규칙을 그대로 지켰다: **옛 숫자를 목표로
쓰지 않고, 같은 날 같은 장비에서 전·후를 나란히 잰다.** 그 전·후가 위 표다.

```
따라오는 규칙 (「마흔여덟째」에 이미 있는 것을 다시 확인했다)
  프롬프트에 적힌 기준선 숫자를 관문으로 쓰면 코드와 무관하게 걸린다
  ★ 관문은 「전·후가 같은가」로 재야 한다. 「옛 숫자와 같은가」가 아니다
```

#### 6. 그 밖의 관문

```
check_wiring   recipe 60 · 배선 있는 것 59 · STEP_OF 36줄 · A 0 · B 0 · C 1
               C 는 web_fetch × 웹 주소. 「쉰째」와 같다
pytest         380 passed / 1 failed (graphviz 음성 대조군)
               371 -> 380. 더한 시험 9개(스위치 시험). 기존 삭제 0
좁히기 무손상   narrow_default() False · 프롬프트 넷 체크섬 그대로
               recipe_axes.md aba2919f5650c9518c61f2965557e0df
               recipe_pick.md fe7bc3837cbfffb7689485d7c790e533
               recipe_selection.md d913822628dc7b9f0d02d9d654704cd4
               node_registration.md 8e0f81e7ce2e001c1f4ae117f3d0b8c6
저쪽 무결       KRRI_ASAP/ 추적 파일 변경 0 · vendor/ 변경 0
```

#### 7. 안 한 것

```
/chat 에 인자를 안 더했다        환경변수만으로 켬·끔을 둘 다 화면에서 봤다
메모리 설계를 안 정했다          이번 범위가 아니다. 「열린 과제」에 항목으로 뒀다
기본값을 끔으로 안 바꿨다        사람이 켬으로 정했다. 끄면 정답표 근접 서른셋 회가
                                 막다른 길이 된다
「청주시 인구 알려줘」로 안 쟀다  오늘 그 발화가 SELECT 라 되묻기가 안 난다.
                                 지어내지 않고 되묻기가 나는 발화로 바꿔 쟀다
「쉰째」의 51/33/9 을 안 좇았다   같은 커밋에서 오늘 52/35/6 이 나온다. 재는 자가
                                 움직인 것이라 코드로 좇을 자리가 아니다
```

### 2026-08-29 (쉰째) · 철도 노선 조회가 값을 보고 칸을 고른다 — ★ 판정이 네 묶음 다 바이트까지 같다

무인 실행. 모델 qwen3:32b(서버 기본) · recipe 60 (안 늘었다) · 정답표 서른하나 ·
`--runs 3` · 문맥 bbox · 좁히기 끔. **커밋 안 했다.**

**★ 결론부터.** `"경부선 노선 보여줘"` 가 0건에서 224건이 됐고 `"호남선 노선
보여줘"` 도 0건에서 79건이 됐다. `"오송역 지나는 노선 알려줘"` 는 31건 그대로다.
**판정은 한 글자도 안 움직였다** — 네 묶음이 시간 자릿수 빼고 바이트까지 같다.
움직일 수가 없는 변경이다(아래 4절). **온톨로지도 menu 도 축도 안 건드렸다.**

#### 1. 무엇이 문제였나

`geo.getRailwayLines` 는 칸이 둘인데 배선은 `stationName` 하나만 보냈다.

```
"오송역 지나는 노선 알려줘"   stationName="오송역"    31건    맞다
"경부선 노선 보여줘"          stationName="경부선"     0건    ★ 노선명은 이 칸이 아니다
```

같은 노드 · 같은 「말한 장소」 자리인데 값의 성격이 다르다. 온톨로지의
`place_name`(장소 이름) 하나가 역 이름도 노선 이름도 담으므로 `(노드, 받는 타입)`
으로는 줄이 안 갈린다. 배선 주석이 이 자리를 미리 적어 뒀다 — "역명으로 볼지
노선명으로 볼지는 사람이 정할 일이다". 그 일이 왔다.

#### 2. 두 길을 따졌다 — 나) 를 골랐다

```
가) 「말한 노선 이름」 타입을 새로 만든다      안 골랐다
나) 배선이 값을 보고 칸을 고른다              ★ 골랐다
    (다) 둘 다 보낸다는 「서른다섯째」에서 이미 닫혔다. 저쪽이 AND 로 잇는다)
```

**가) 를 안 고른 이유 넷.**

```
1  ★ 이번 주에 축을 건드린 것이 세 번이고 세 번 다 졌다
     「서른여섯째」 축 문안 여섯 · 「마흔두째」 좁히기 문안 여섯 · 「마흔아홉째」
     want 한 줄(+86자)에 적중 63 -> 51. **「마흔아홉째」가 그 자리를 「축 문제다.
     문안을 흔드는 것으로는 안 풀린다」로 닫아 뒀다**
2  ★ 고치려는 발화가 이미 축에서 지고 있다
     28번은 `given` 을 `spoken_keyword` 로 쓴다(네 묶음 다 3/3). 「말한 장소」로도
     못 읽는 값에 「말한 노선 이름」이라는 셋째 읽기를 더하는 것이라,
     ★ 가) 는 고치려는 그 축을 더 어렵게 만든다. 「열린 과제」의
     「같은 낱말을 장소로 읽느냐 키워드로 읽느냐가 후보 집합을 통째로 가른다」와 같은 자리다
3  가) 는 28번이 실행되는 것을 보장하지도 못한다
     LLM 이 새 given 을 골라야 비로소 새 recipe 로 간다. 나) 는 어느 recipe 로 가든
     그 노드에 닿기만 하면 고쳐진다 — **지금 28번이 닿는 방식이 되묻기 뒤 사람의 선택이라
     그 차이가 그대로 값이다**
4  대가가 크다. recipe 가 늘어 번호가 밀리고 정답표 서른한 줄을 다시 옮겨야 한다
     (「서른째」·「마흔아홉째」에서 두 번 겪었다)
```

**나) 의 대가를 알고 골랐다.** 배선표에 값 판단이 처음 들어왔다. 「열린 과제」에
새 항목으로 적고 걷어낼 조건을 함께 적었다.

#### 3. 규칙과 그 한계

```
"…선" 으로 끝나면  railwayName
아니면             stationName   ← 지금 그대로다
```

배선 줄에 `arg_field: (어미, 칸 이름)` 한 칸을 더했고 `_by_argument` 가 그것을 읽어
**@arg 가 든 칸의 이름만** 바꾼다. 값은 안 고친다. 이 칸을 적은 줄은
서른여섯 중 하나뿐이다.

**눌러서 얻은 표** (`asap-mcp-core` · 2026-08-29 · features 수)

```
                        stationName   railwayName
  경부선                       0건         224건    ★ 고치려던 자리
  호남선                       0건          79건    ★ 안 재본 값이었다
  중앙선                       -           239건
  오송역                      31건           0건    ★ 자가 헛맞으면 잃는 것이 이만큼이다
  서울역                      79건           -
  청주                         9건           0건    ★ 어느 쪽 어미도 아니다
  1호선                      345건         521건    ★ 두 칸 다 답이 있다
```

**한계 셋. 넓히지 않는다.**

```
1  "1호선" 은 두 칸 다 답이 있는데 이 자를 지나면 railwayName(521건)으로 간다.
   지하철 노선명이라 노선 쪽이 맞다고 보지만 ★ 재서 고른 것이 아니라
   규칙이 그렇게 떨어진 것이다. stationName 이 345건인 이유는 안 팠다
2  "…선" 도 "…역" 도 아닌 값은 stationName 에 그대로 둔다. "청주" 가 그 자리이고
   railwayName 으로 보내면 9건이 0건이 된다. ★ 그래서 자를 "…역" 으로 좁히지 않았다
3  이 자는 철도 노선 조회 한 줄에만 걸린다. 철도 구간 형상 조회(21번)는
   sectionName 한 칸뿐이라 가를 것이 없다
```

#### 4. menu · 축 자수 전후 — 넷 다 0 이다

```
                   전        후
  recipe          60개      60개
  menu          3406자    3406자   (127줄)
  given 선택지   3 · 4     3 · 4    (문맥 없음 · bbox. both 면 5)
  want 선택지      11        11
  about 선택지      4         4
```

**recipe 대응표도 GT 옮긴 표도 없다.** 번호가 하나도 안 밀렸다 —
`tools/check_resolve.py` 를 안 건드렸다. 가) 를 골랐다면 이 네 칸이 다 움직였다.

#### 5. 네 묶음 — 전 → 후 → 전 → 후

같은 세션에서 연달아. `--runs 3` · 문맥 bbox · 좁히기 끔 · 겹쳐 돌린 것 없음.

```
  묶음        적중     근접   빗나감   못 붙음   기준선 아홉   확장 열아홉
  A 전       51/93      33       9        0       15/27         36/66
  B 후       51/93      33       9        0       15/27         36/66
  C 전       51/93      33       9        0       15/27         36/66
  D 후       51/93      33       9        0       15/27         36/66
```

**네 파일이 시간 자릿수 하나 빼고 바이트까지 같다** (`diff` 로 확인). 표 넉 장
전부 — 적중 표 · 축 표 · 후보 표 · 검산 표가 다 같다. `/resolve` 평균 4.6초도 같다.

**★ 이것이 예상한 결과다.** 배선(`STEP_OF`)은 recipe 를 고른 **뒤에** 읽힌다.
발화 해석 프롬프트에 실리는 것은 menu 와 축 선택지이고 둘 다 온톨로지 산물이다.
**이번 변경은 프롬프트에 한 글자도 안 닿는다.** 그래서 네 묶음이 같은 것이
잡음이 없다는 뜻이 아니라 **「갈아끼운 것이 판정에 안 닿는다」는 뜻이다** —
「마흔아홉째」가 적은 것과 같은 읽기다.

**읽는 법 넷에 비춘 결과**

```
1순위  빗나감 9 = 9        안 늘었다
2순위  적중  51 = 51        안 떨어졌다
3순위  28번이 실행되는가    ★ 됐다. 0건 -> 224건 (아래 6절)
       판정은 네 묶음 다 근접(CLARIFY {003, 007, 014, 037}) 3/3 으로 같다.
       ★ 이번 프롬프트가 「판정은 원래 적중이었다」고 적었는데 오늘 자로는
         적중이 아니라 근접이다. 되묻기 뒤 사람이 「1」을 고르면 실행된다 —
         화면 실측이 그 경로다. 바뀐 것은 그 실행뿐이라는 점은 같다
4순위  22번이 안 깨졌는가   ★ 안 깨졌다. 31건 그대로. 판정도 근접 3/3 으로 같다
```

#### 6. 화면 실측 넷 — 문구 그대로

`POST /chat` · sessionId 마다 새 세션 · 문맥 bbox(오송역 반경 15km) · 되묻기가
나오면 `"1"` 을 이어 보냈다. 화면이 지나는 경로와 같다.

```
[28] "경부선 노선 보여줘"        되묻기 4개 -> 「1 철도 노선 조회」
  전   찾지 못했습니다.
       1. geo.getRailwayLines  stationName="경부선"  0건
  후   경부선 철도 노선을 조회했습니다.
       1. geo.getRailwayLines  railwayName="경부선"  224건
       ★ commands 가 map.draw 하나(features 0) -> map.draw + view.camera.flyTo (features 224)

[22] "오송역 지나는 노선 알려줘"  되묻기 3개 -> 「1 철도 노선 조회」
  전   오송역 철도 노선을 조회했습니다.
       1. geo.getRailwayLines  stationName="오송역"  31건
  후   오송역 철도 노선을 조회했습니다.
       1. geo.getRailwayLines  stationName="오송역"  31건    ★ 글자까지 같다

[21] "경부선 선형 데이터 줘"      되묻기 없음
  전   경부선 철도 구간 형상을 조회했습니다.
       1. rail.getSectionGeometry  sectionName="경부선"  칸: sectionId · geometry · bbox
  후   같다. 글자까지 같다    ★ 다른 도구다. 안 바뀌어야 하는 자리다

[--] "호남선 노선 보여줘"        되묻기 2개 -> 「1 철도 노선 조회」. ★ 안 재본 값이었다
  전   찾지 못했습니다.
       1. geo.getRailwayLines  stationName="호남선"  0건
  후   호남선 철도 노선을 조회했습니다.
       1. geo.getRailwayLines  railwayName="호남선"  79건
```

**되묻기 문구와 후보는 전후가 같다** — 28번은 넷, 22번은 셋, 호남선은 둘로
전후 글자까지 같다. 배선이 판정에 안 닿는다는 4절의 말이 화면에서도 같다.

#### 7. 되묻기에서 다른 것을 고르면 무엇이 오는가 — 사람이 눌러 봤다

2026-08-29, 사람이 저쪽 화면에서 되묻기의 다른 후보를 눌러 봤다.
**이번 변경과 무관한 자리이고 새로 알게 된 사실이라 적는다.**

```
"호남선 노선 보여줘" → 2 장소 좌표 변환 → 철도 노선 조회
  geo.geocode  호남선 → (126.7927, 35.1789)
  geo.getRailwayLines  14건
  ★ 노선 이름을 지오코딩해 점 하나로 만들고 그 주변만 준다.
    1번(railwayName, 79건)이 맞는 답이다.

"오송역 지나는 노선" → 3 철도 구간 형상 조회 → 철도 노선 조회
  rail.getSectionGeometry  sectionName="오송역"  칸: sectionId · geometry · bbox
  geo.getRailwayLines  1156건
  ★ 1,156건이면 전국에 가깝다. 구간 형상의 bbox 가 넓게 잡히는 것으로 보이나
    안 팠다.
```

**★ 되묻기 후보 안에 「말이 되지만 답이 나쁜」 길이 섞여 있다.**
시연에서는 1번을 고른다. 「열린 과제」에 항목으로 남겼다.

#### 8. 관문

```
저쪽 무결          KRRI_ASAP/ · vendor/ 변경 0 (KRRI_ASAP 의 미추적 둘은 8/22 · 8/24 것이다)
두 ontology.yaml   cmp 동일. ★ 건드리지 않았다
그래프             /graph 200. 노드를 안 더했으므로 좌표 볼 것이 없다
check_wiring       recipe 60 · 배선 있는 것 59 · STEP_OF 36줄 · A 0 · B 0 · C 1 (web_fetch)
check_inputs       ★ 죽는다. 이번 변경 전에도 똑같이 죽는다 — 9절
정답표 무손상       발화 · 기대 사슬 · 번호 전부 무변경. check_resolve.py 를 안 건드렸다
좁히기 무손상       프롬프트 넷 무변경 · narrow_default() False
pytest             363 passed / 1 failed -> 371 passed / 1 failed
                   ★ 실패 1건은 graphviz 음성 대조군으로 전후 같다. 새 시험 여덟 · 삭제 0
화면 실측 넷        6절. 되묻기의 다른 후보를 누른 것은 7절
```

#### 9. ★ check_inputs 가 죽어 있다 — 우리가 깬 것이 아니다

```
File "tools/check_inputs.py", line 272, in rows_of
    tool = TOOL_OF[node]["tool"]
KeyError: 'tool'
```

**「마흔아홉째」가 `TOOL_OF` 에 도구가 아닌 줄(`show_facility`, `command` 만 있는
줄)을 더하면서 죽었다.** 이번 변경 전 코드로도 같은 자리에서 같은 오류가 난다
(재현했다). **이번 범위가 `tools/check_resolve.py` 의 GT 번호 이동뿐이라 안 고쳤다.**

그 판정만 밖에서 다시 돌렸다 — 그 줄 하나만 빼고 `check_inputs` 의
`load_schemas` · `sent_fields` · `judge` 를 그대로 불렀다. **철도 세 줄 다
판정은 「맞다」로 전후 같다.**

```
  geo.getRailwayLines × place_name (input)   보내는 칸 ['stationName']  맞다
      ★ bbox(O) · railwayName(S) · includeStations · simplifyM
  geo.getRailwayLines × map_extent           보내는 칸 ['bbox']         맞다
```

**★ `railwayName(S)` 은 안 사라진다.** 「열린 과제」가 "가) · 나) 를 정한 뒤 이
표에서 `railwayName` 이 사라지는지로 확인한다" 고 적어 뒀는데, **나) 를 고르면
사라질 수가 없다** — 그 도구는 `STEP_OF` 를 정적으로 읽으므로 표에 적힌
`stationName` 한 칸만 본다. 칸을 고르는 것이 값이라 표에 안 실리는 것이고,
**그것이 나) 를 고른 대가다.** 가) 를 골랐다면 타입이 갈려 표에서도 갈렸다.
그래서 이 자리는 **「사라지는지」가 아니라 「죽은 것을 살릴 때 이 줄을 어떻게
셀지」의 문제로 넘긴다** — 「열린 과제」에 적었다.

같은 표에서 `ev.searchStations × map_extent` 가 「없는 칸을 보낸다(bbox)」로
올라온다. **이번 변경과 무관하고 전에도 같다.** 안 팠다.

#### 10. 안 한 것

```
온톨로지                 한 글자도 안 건드렸다. 두 파일 cmp 동일
정답표 · GT 번호          안 건드렸다. recipe 가 안 늘어 밀린 것이 없다
tools/check_inputs.py     죽어 있는데 안 고쳤다 (9절). 범위 밖이다
"1호선" 을 재서 고르기     두 칸 다 답이 있는데 어느 쪽이 맞는지 안 정했다.
                          규칙이 railwayName 으로 떨어뜨렸고 그대로 뒀다
"…역" 으로 자 좁히기       안 했다. "청주" 가 9건에서 0건이 되므로 오히려 나쁘다
철도 구간 형상의 LIMIT 1  「열린 과제」의 저쪽 문제다. 이번에 안 봤다
좁히기 · 프롬프트 · menu   안 건드렸다. 기본값 끔 그대로
```

### 2026-08-29 (마흔아홉째) · 도구를 안 부르는 길 셋 — 하나는 노드로, 둘은 NO_MATCH 로. ★ 적중이 12 내렸다

무인 실행. 모델 qwen3:32b(서버 기본) · recipe 59 -> 60 · 정답표 서른하나 ·
`--runs 3` · 문맥 bbox · 좁히기 끔. **커밋 안 했다.**

**★ 결론부터.** 저쪽 셋을 다 대신한다 — 화면 실측 여섯이 다 된다. 그런데
**적중이 63 -> 51 로 12 내렸다.** 빗나감은 9 -> 9 로 안 늘었으므로 되돌리는
규칙(1순위)에는 안 걸린다. **2순위는 깨졌다. 노드를 남길지는 사람이 정한다.**

**★ 이 자는 결정적이다.** `temperature 0 · seed 0` 이라 같은 파일로 두 번 재면
같은 값이 나온다. 아래 네 묶음에서 1블록과 3블록은 시간 자릿수 하나 빼고 같은
파일이고, 2와 4는 바이트까지 같다. **번갈아 재는 것이 이 자에서는 잡음을 재는
것이 아니라 「갈아끼운 것이 실제로 반영됐는가」를 확인하는 것이다.**

#### 1. 저쪽 셋을 읽은 결과 — 셋 다 `## Run` 이 없다

`KRRI_ASAP/ASAP-orchestrator/plugins/` 열둘을 절 이름으로 맞대 봤다. **아홉에는
`## Run` 이 있고 셋에만 없다.** 그 셋이 이번 셋이다. 「도구를 안 부른다」가
저쪽 파일에 그렇게 적혀 있다.

```
show-facility        Run 없음. `## Map` 의 `show_facility | $facilityName` 한 줄이 전부
                     -> market_plugin_engine._show_facility 가
                        {op:"digitalTwin.showFacility", args:{facilityName}} 하나를 만든다
                     Answer  "**{facilityName}** 시설물 정보를 표시합니다."
                     Inputs  facilityName (필수). skill.md 의 「Facility Aliases」 11줄이
                             낱말을 시설물명으로 바꾼다 ("제3터널" -> "시험 제3터널")
                     ★ 저쪽 화면은 이 명령을 Cesium 으로 안 보낸다 —
                       useChat 이 op 으로 가려내 오송DT 모니터링 모드로 전환하고
                       setPendingFacilityName 을 부른다
system-chat          Run 없음. Inputs 가 message 하나(LLM 이 쓴 한국어)이고 Answer 는 {message}
                     Trigger `Expose To Prompt: yes`
unsupported-request  Run 없음. Inputs 도 없다. Answer 는 고정 한 줄
                     "죄송합니다. 현재 지원하지 않는 요청입니다."
                     Trigger `Expose To Prompt: no` — **고를 수 있는 기능이 아니라 폴백이다**
                     fallback.py 의 `_SKIP_ACTIONS = {"chat", "unsupported"}` 가 같은 말을 한다
```

#### 2. 세 갈래 중 무엇을 왜 골랐나 — 다) 셋을 갈랐다

```
가) 셋 다 온톨로지 노드로     안 골랐다
    unsupported-request 는 저쪽에서도 `Expose To Prompt: no` 다. 그것을 노드로
    두면 「못 한다」가 menu 에 선택지로 들어가 진짜 recipe 와 겨룬다.
    system-chat 은 받는 것도 내놓는 것도 온톨로지에 없다 — 양끝에 가짜 타입을
    만들어야 한다. 그리고 셋 다 menu 를 늘린다

나) resolve 앞뒤에서 낱말로 걸러낸다    안 골랐다
    menu 가 안 느는 것이 값이지만 「온톨로지가 순서를 낸다」 밖의 길이 하나
    생긴다. `SCREEN_WORDS` 로 이미 하나 냈고 그것을 임시방편이라 적어 뒀다
    (「마흔여덟째」). 둘째를 같은 이유로 더 내지 않는다

다) 셋을 갈라 다르게 한다    ★ 골랐다
    show-facility        노드로. 받는 것(장소 이름)과 내놓는 것(시설물 화면)이
                         있는 한 단짜리 기능이라 다른 실행 노드와 모양이 같다
    system-chat          NO_MATCH 를 다듬어서
    unsupported-request  NO_MATCH 를 다듬어서. 우리 NO_MATCH 가 이미 같은 자리다
```

#### 3. 「모든 경로가 도구로 끝난다」 전제를 어떻게 다뤘나 — 온톨로지의 전제가 아니었다

파일을 하나씩 짚어 보니 **그 전제는 온톨로지에 없었다.**

```
ontology.yaml                노드에 name 과 description 뿐이다. 실행 노드인지는
                             hasOutput 이 있는지로 갈린다. 도구라는 말이 없다
step_service.TOOL_OF         ★ 여기 있다. 「노드 -> server_id · tool · headline」
step_service.plan            ★ 여기 있다. steps 배열만 만든다
execute_service.run          ★ 여기 있다. steps 를 vendor 실행기에 넘긴다
```

**그래서 온톨로지를 한 글자도 안 굽혔다.** 넓힌 것은 배선표다.

```
TOOL_OF   노드 -> 서버 · 도구 · 답 첫 줄        (전)
          노드 -> 실행 수단 · 답 첫 줄          (후)

  server_id · tool   Gateway 의 MCP 도구를 부른다. 마흔 줄이 이것이다
  command            지도 명령 하나를 낸다. 부를 도구가 없다. 한 줄이다
```

**도구 이름을 온톨로지에 안 적기로 한 규칙이 여기서 값을 했다.** 노드가 도구가
아닌 것에 묶여도 온톨로지는 안 달라진다. 온톨로지에 적힌 것은 「장소 이름을
받아 시설물 화면을 내놓는다」 뿐이다.

`plan()` 이 `command` 칸이 있는지로 갈라 `steps` 대신 `commands` 를 만들고,
`run()` 이 `steps` 가 비면 vendor 를 아예 안 지난다(빈 steps 는 vendor 가
실패로 본다). 도구 단계와 섞이면 도구 응답에서 나온 명령 뒤에 붙인다 —
그런 노드는 아직 없다.

#### 4. 무엇을 만들었나

```
ontology.yaml   노드 둘 · 관계 셋
                facility_view  시설물 화면   (타입. 받는 노드가 없어 뒤에 아무도 못 선다)
                show_facility  시설물 표시   (실행. hasInput place_name · hasOutput facility_view
                                              · about group_transport)
                ★ show_facility 를 nodes 맨 뒤에 뒀다. 경로 생성이 nodes 차례를
                  따르므로 새 recipe 가 「말한 장소」 2단 사슬의 맨 끝에 들어간다
step_service    TOOL_OF 한 줄 (command: digitalTwin.showFacility)
                STEP_OF 한 줄 ((show_facility, place_name) -> {facilityName: @arg})
                plan() 이 commands · command_nodes 를 함께 냄
execute_service ★ 도구가 하나도 없으면 vendor 를 안 지나고 명령만 냄
                NO_MATCH 답을 workflow_answer 에 넘김. 안내 낱말은 온톨로지가 댐
workflow_answer command_answer() · no_match_answer() 둘. **도구 이름을 여전히 모른다**
layout.json     show_facility 좌표 하나. 기존 47개는 0.0000pt 움직였다
tests           새 시험 열 (test_command_step.py 여섯 · test_layout_covers_every_node.py 둘
                · test_clarify_answer.py 둘). 기존 삭제 0
```

**저쪽 alias 표를 안 베꼈다.** skill.md 의 「Facility Aliases」 11줄이 "제3터널"
을 "시험 제3터널" 로 바꾸는데, 그것은 저쪽 설정이고 우리 온톨로지에 그것을 둘
자리가 없다. 발화에서 온 값을 그대로 `facilityName` 에 넣는다. 아래 「안 한 것」.

#### 5. menu 자수 전후 · recipe 대응표 · GT 옮긴 표

```
menu.yaml 전체        4833 -> 4917자  (+84)
프롬프트에 실리는 것   3322 -> 3406자  (+84)   발화 recipe 40 -> 41
want 축 선택지         395 -> 481자    (+86)   facility_view 한 줄
recipe                59 -> 60
```

recipe 대응표는 새것 하나와 +1 밀림뿐이다.

```
  recipe_001 ~ 003   그대로            말한 장소 → 좌표 / 철도 구간 형상 / 철도 노선
  recipe_004         ★ 새것            말한 장소 → 시설물 표시
  recipe_005 ~ 060   옛 004 ~ 059      사슬 그대로, 번호만 +1
```

GT 는 **기대값의 사슬을 한 글자도 안 바꿨다.** 004 이상이던 스물여섯 줄만 +1 로
옮기고 옛 번호를 옆 주석에 남겼다. 안 움직인 다섯은 1 · 2 · 21 · 22 · 28 번이다.

```
 #  발화                          옛 → 새       #  발화                          옛 → 새
 3  오송역 CCTV 보여줘            035 → 036     18  오송역 일대 인구 얼마야      044 → 045
 4  청주시 인구 구성 알려줘       057 → 058     19  인구 많은 시군구 순위        011 → 012
 5  오송역 근처 충전소 찾아줘     045 → 046     20  대전역 연령대별 인구         057 → 058
 6  국회의원 선거구 찾아줘        007 → 008     23  경부선 주변 CCTV            048 → 049
 7  전기차 충전소 데이터 검색     012 → 013     24  오송역 근처 충전소 자세히    059 → 060
 8  철도 안전 문서 찾아줘         013 → 014     25  문서에서 철도안전법          013 → 014
 9  충북 제1선거구 알려줘         014 → 015     26  문서에서 철도 안전 교육      013 → 014
10  오송역이 어느 동인지          033 → 034     27  철도안전법 내용 찾아줘       013 → 014
11  오송역 행정경계 보여줘        032 → 033     29  청주시 인구 알려줘           011 → 012
12  청주시 행정경계 보여줘        004 → 005     30  오송역 선거구 알려줘         037 → 038
13  오송역 국회의원 누구야        039 → 040     31  청주 국회의원 선거구 검색    007 → 008
14  오송역 국회의원 공약 보여줘   040 → 041
15  청주 선거구 찾아줘            007 → 008     테스트 안에서도 같은 규칙으로 옮겼다
16  교통 공약 많은 선거구         008 → 009     test_shortlist.py 045 → 046 · 007~010 → 008~011
17  청주 국회의원 공약 검색       009 → 010                       006 → 007
```

#### 6. 네 묶음 표 — 그리고 문안을 고쳐 재본 셋

```
                LLM 단독   적중    근접  빗나감  못 붙음    기준선 아홉(27)      확장(66)
  1 전           51/93    63/93    21     9      0     15/27 · 3 · 3 · 0   36/66 · 18 · 6 · 0
  2 후           43/93    51/93    33     9      0     15/27 · 6 · 6 · 0   28/66 · 27 · 3 · 0
  3 전           51/93    63/93    21     9      0     (1 과 같은 파일)
  4 후           43/93    51/93    33     9      0     (2 와 바이트까지 같음)
  차             -8      ★ -12   +12     0      0
```

**★ 적중 -12 의 자리.** 셋은 원인을 집었고 아홉은 흩어짐이다.

```
 1 오송역 위치 보여줘        3/3 → 0/3  ★ 빗나감 {004}     want 를 point 에서 facility_view 로 바꿔 씀
 9 충북 제1선거구 알려줘     3/3 → 0/3  근접 {015,016,017}
14 오송역 국회의원 공약      3/3 → 0/3  근접 {039, 041}
15 청주 선거구 찾아줘        3/3 → 0/3  근접 {008, 009}
16 교통 공약 많은 선거구     3/3 → 0/3  근접 {009,010,011}
24 오송역 근처 충전소 자세히 2/3 → 0/3  근접 {013,046,052,060}
30 오송역 선거구 알려줘      3/3 → 0/3  근접 {038, 040}
                            ------------------------------------
11 오송역 행정경계          1/3 → 3/3   13 오송역 국회의원 누구야  0/3 → 3/3
31 청주 국회의원 선거구      0/3 → 3/3   22 오송역 지나는 노선      빗나감 3 → 근접 3
```

**빗나감은 수도 같고(9) 자리만 갈렸다.** 전 : 4번(3) · 22번(3) · 27번(3).
후 : **1번(3)** · 4번(3) · 27번(3). 22번이 나았고 1번이 새로 깨졌다.

**★ 1번은 기준선 아홉의 첫 줄이고 시연 발화다.** "오송역 위치 보여줘" 가
`want` 를 `facility_view` 로 쓰고 새 recipe_004 로 **확신하고** 갔다. 원인은
내가 쓴 한 문장이다 — `facility_view` 의 설명이 "**화면에 표시하는 것이다**"
였다. 「보여준다」는 거의 모든 발화에 걸리는 말이라 축을 가르는 데 못 쓴다.
**저쪽도 같은 자리를 낱말로 막아 두었다** — show-facility 의 `Do Not Use When`
이 "오송역 보여줘 · 오송역 이동 · 의왕역 가줘" 이고, skill.md 는 "시설물/모니터링
의도가 명시된 경우에만" 이라고 적었다.

**그래서 문안 둘을 고쳐 다시 쟀다. 그리고 되돌렸다.**

```
  고친 문안
    facility_view  시설물 화면 -> 선택된 시설물
                   "…발화가 시설물이나 모니터링, 점검, 손상, 센서를 짚을 때만 쓴다."
    show_facility  "…시설물 한 곳을 …골라 점검·손상·센서 상태를 띄운다."

                LLM 단독   적중    근접  빗나감  못 붙음
  5 후2          43/93    49/93    30   ★ 14     0
  6 전           51/93    63/93    21      9     0     (1 · 3 과 같은 값)
  7 후2          43/93    49/93    30   ★ 14     0     (5 와 같은 값)
```

**1번은 살아났다(3/3 적중).** 그런데 시설물과 아무 상관 없는 다섯 자리가 새로
빗나갔다 — 8번 철도 안전 문서 {007 웹 검색} 3회 · 13번 오송역 국회의원 누구야
2회 · 22번 2회 · 25번 2회. **빗나감 9 -> 14.**

**되돌린 뒤 한 묶음 더 재서 같은 파일인지 확인했다** — 8블록이 2블록과
바이트까지 같은 표를 냈다(51/93 · 빗나감 9). 되돌린 것이 문안뿐이고 주석은
프롬프트에 안 실린다는 것이 이 한 줄로 확인된다.

```
★ 1순위가 「빗나감이 「전」보다 크지 않은가」라 되돌렸다.
  문안 한 문장을 고치면 정답표 서른한 자리가 통째로 다시 섞인다.
  판정이 결정적이라 「잡음이었다」로 넘길 수도 없다.
  ★ 문안을 또 흔들기 전에 이 표를 읽는다. 정답표를 보고 문안을 맞추는 것은
    자를 답에 맞추는 것이다.
```

#### 7. 읽는 법 넷에 비춘 결과

```
1순위  빗나감이 「전」보다 크지 않은가        9 = 9      ★ 통과
2순위  적중이 폭 밖으로 떨어지지 않았는가    63 -> 51   ★ 깨졌다 (「마흔두째」 잡음 폭 ±5 의 두 배 넘음)
3순위  화면 실측 여섯이 다 되는가            여섯 다 됨  통과 (지금 되던 셋도 안 깨졌다)
4순위  menu 가 몇 자 늘었는가                +84자      통과
```

**못 붙음은 0 -> 0 이고 잃은 열둘은 전부 SELECT -> CLARIFY 흩어짐이다.** 사람이
번호로 고르면 실행된다(「서른넷째」의 되묻기 뒤 고르기). 확신하고 틀리는 자리가
안 는 것과 이것이 이번 값의 전부다.

#### 8. 화면 실측 여섯 — 문구 그대로

```
1  오송 테스트트랙 시설물 보여줘
   "오송 테스트트랙 시설물을 화면에 띄웠습니다."
   commands [{"op":"digitalTwin.showFacility","args":{"facilityName":"오송 테스트트랙"}}]
   ★ MCP 도구 0회. vendor 실행기를 아예 안 지난다

2  안녕
   "지금 할 수 있는 일 중에 맞는 것이 없습니다.
    사용자가 '안녕'이라고 인사했을 뿐, 구체적인 요청이 없음.
    제가 다루는 것은 교통 · 선거 · 인구 · 전기차 충전입니다.
    말한 장소 · 말한 키워드 · 말한 식별자 가운데 하나를 함께 말씀해 주세요."

3  오늘 날씨 알려줘
   "지금 할 수 있는 일 중에 맞는 것이 없습니다.
    요청은 '날씨'에 관한 정보를 받는 것으로 끝난다. 그러나 Menu에 날씨 관련 Recipe는 존재하지 않는다.
    제가 다루는 것은 교통 · 선거 · 인구 · 전기차 충전입니다.
    말한 장소 · 말한 키워드 · 말한 식별자 가운데 하나를 함께 말씀해 주세요."

4  오송역 근처 충전소 자세히 알려줘        ★ 4단 사슬 그대로
   "오송역 충전소 상세를 조회했습니다.
    1. geo.geocode       오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1 (127.3277, 36.6200)
    2. ev.searchStations minLon=127.1598 … 500건 (전체 2,173건) · 청주오송역차고지
    3. ev.getStation     statId="PK000313"  8건"

5  문서에서 철도안전법 관련 내용 찾아줘    ★ RAG 그대로
   "철도안전법 문서를 조회했습니다.
    1. knowledge.query   6건
       「철도안전법(법률)(제21188호)(20260303).pdf」 1쪽 · 2쪽 · 32쪽 조각 셋"

6  지금 보이는 곳 CCTV 보여줘             ★ 화면 발화 그대로
   "CCTV 를 조회했습니다.
    1. road.getCctv      minLon=127.1274 · minLat=36.5199 · maxLon=127.4674 · maxLat=36.7199  48건"
```

**둘과 셋의 가운뎃줄은 LLM 이 쓴 `reason` 이다.** 발화마다 다른 그 한 줄이
system-chat 의 `message` 자리를 맡는다. 첫 줄과 안내 두 줄은 고정이다.
**안내의 낱말은 온톨로지가 댄다** — 대상 넷은 `graph.group_ids()`, 시작 데이터
셋은 `graph.start_ids()` 에서 화면에서 오는 둘을 뺀 것이다. 노드를 등록하면
안내도 함께 는다. 저쪽 unsupported-request 는 고정 문구 한 줄이라 무엇을 대신
말해야 할지 안 알려준다.

#### 9. 안 한 것

```
시설물명 alias        저쪽 skill.md 의 「Facility Aliases」 11줄을 안 베꼈다.
                      "제3터널" 을 우리는 "제3터널" 로 넘기고 저쪽은 "시험 제3터널"
                      로 넘긴다. 저쪽 화면이 그 이름으로 자산을 못 찾으면 모니터링
                      모드로 전환만 되고 시설물이 안 잡힌다.
                      ★ 아직 화면에서 확인 못 했다 — 우리는 명령이 나가는 것까지 봤다
system-chat 의 인사    "안녕" 에 인사로 답하지 않는다. 저쪽은 Gemini 가 message 를
                      써서 답한다. 우리는 NO_MATCH 한 자리로 잡담과 안내를 함께
                      받는다. 가르려면 LLM 을 한 번 더 불러야 하고
                      (ㄱ) "안녕" 에 5초가 붙고 (ㄴ) 프롬프트 파일을 더해야 하는데
                      이번 범위가 workflows/static/prompts 를 못 건드린다
                      (ㄷ) 온톨로지 밖의 판단이 하나 는다. 셋 다 값이 안 맞았다
적중 -12 의 아홉      흩어짐의 원인을 못 집었다. 프롬프트 한 바이트가 바뀌면
                      결정적으로 다시 섞인다는 것만 두 번 확인했다
                      (문안 둘 · 「마흔여섯째」의 menu 1511자)
저쪽 화면 실측         우리 8000 만 눌렀다. 저쪽 gateway 를 지나 오송DT 화면이
                      실제로 전환되는 것은 못 봤다
```

#### 10. 저쪽 화면이 명령을 받고도 안 움직인다 — 우리 몫은 끝났다

**사람이 저쪽 화면에서 눌러 봤다** (2026-08-29). "오송 테스트트랙 시설물
보여줘" 를 넣고 브라우저 콘솔을 확인한 것이다.

```
API Response Data: {answer: '오송 테스트트랙 시설물을 화면에 띄웠습니다.',
                    commands: Array(1)}
```

**명령이 응답에 실려 나간다. 그런데 화면은 아무것도 안 바뀐다.**

저쪽 코드를 읽어 자리를 짚었다(읽기만 했다).
`ASAP-web/packages/chat/src/hooks/useChat.ts` 가 op 이
`digitalTwin.showFacility` 인 명령을 골라 오송DT 모니터링 모드로 전환하는데,
그 앞에 조건이 있다.

```
if (facilityName && setIsOsongMonitoringMode && setPendingFacilityName)
```

**뒤의 둘은 `useChat` 에 밖에서 넘겨주는 것이다.** 지금 화면에서 그 둘이 안
넘어오면 조용히 아무 일도 안 한다. 우리가 내는 op 과 `args.facilityName` 은
저쪽이 기대하는 모양과 같다 — 같은 파일의
`isDigitalTwinFacilityCommand` · `getFacilityName` 이 그 둘만 본다.

```
★ 우리 몫은 끝났다. 화면이 받아 쓰는지는 저쪽 소관이다.
  「대표님께 물어볼 목록」에 넣었다.
```

#### 11. 시연 첫 줄을 바꾼다

발화 1 "오송역 위치 보여줘" 가 `want` 를 `facility_view` 로 쓰고 새
recipe_004(시설물 표시)로 **확신해서 간다**(3/3). **시연 대본의 첫 줄이다.**

**사람이 저쪽 화면에서 대안을 눌러 확인했다** (2026-08-29).

```
"오송역 좌표 보여줘"
  geo.geocode  오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1
               (127.3277, 36.6200)
```

```
★ 시연 대본의 첫 줄을 "오송역 좌표 보여줘" 로 바꾼다.
  발화 1 자체는 정답표에서 안 바꾼다 — 고쳐야 할 자리가 표에 남아야 한다.
  (「서른두째」가 정답표에 「고쳐야 할 자리」를 박아둔 것과 같은 뜻이다)
```

### 2026-08-29 (마흔여덟째) · 발화가 화면을 가리키면 화면 recipe 만 넣는다 — ★ 대조군이 「67」을 부정했다

무인 실행. 모델 qwen3:32b(서버 기본) · recipe 59 · 정답표 서른하나 · `--runs 3` ·
좁히기 끔. **커밋 안 했다.**

**★ 결론부터.** 갈래는 뜻대로 돈다(화면 발화 넷 12/12 · 화면 실측 다섯 통과).
적중은 49 -> 51(시연 조건) · 54(문맥 없음)로 올랐고 「67 근처」에는 못 갔다.
**그런데 대조군을 재보니 「67」이 오늘 이 기계에서 다시 안 나온다** — 그 숫자를
낳은 커밋(`101a155`)을 그대로 체크아웃해 재니 **53/93 · 빗나감 16** 이었다.
아래 4를 본다. **판정의 기준선 자체가 흔들린 것이라 「16 내렸다」는 이야기를
이 표로는 더 못 받친다.**

```
고친 파일   workflows/static/menu/load.py           id 를 주면 그 recipe 만 남긴다
            demo/api/services/resolve_service.py    SCREEN_WORDS · _points_at_screen · _menu_for
            demo/api/main.py                        ★ 범위 밖. /resolve 가 context 를 받는 자리가 없었다
            demo/ui/config.py · api_client.py · main.py · components/input_section.py
                                                    고정 문맥을 만들어 보내고 화면에 밝힌다
            tools/check_resolve.py                  --context none|bbox|both (기본 bbox)
            tests/demo/api/test_menu_split.py       새 파일 열여덟
            tests/demo/api/test_map_context.py      낡은 주석 한 문단
            NOTES.md
안 건드림   ontology.yaml 둘 · menu.yaml · recipes/ · _init/ · step_service.py(배선표) ·
            프롬프트 셋(recipe_selection · recipe_axes · recipe_pick) · 정답표 UTTERANCES ·
            vendor/ · KRRI_ASAP/(추적 파일 변경 0) · 좁히기 기본값 끔
```

#### 1. 왜 이 방향인가 — 사람이 정했다 (2026-08-29)

```
menu 를 화면·도구마다 다르게 하지 않는다
  Streamlit 도 저쪽과 같은 발화에 같은 경로를 골라야 한다
  우리 자도 시연과 같은 조건에서 재야 한다
대신 발화가 무엇을 가리키느냐로 가른다. 어느 쪽이든 menu 가 짧다
★ 월요일 시연을 위한 임시방편이다
```

**`resolve_service` 에 적혀 있던 원칙을 깬 것이다** — "menu 는 온톨로지가 만드는
것이고 요청마다 다를 수 없음". 그 자리 주석에 깬 사실과 까닭을 적었다.
**`menu.yaml` 파일은 안 고쳤다.** 읽은 문자열에서 블록을 뺄 뿐이다.

#### 2. 낱말 목록과 근거

```
여기 · 이 위치 · 선택한 위치 · 지금 보이는 · 현재 화면 · 이 근처 · 이 지역   사람이 정한 일곱
관심 지점                                                                     ★ 하나 더했다
```

**「관심 지점」을 더한 근거.** 저쪽 `parse_intent.py` 가 문맥이 있을 때 프롬프트에
덧붙이는 한 줄이 "here, this place, selected location, **관심 지점**, 선택한 위치,
이 위치" 다. 그 한국어 셋 중 둘(선택한 위치 · 이 위치)이 이미 사람 목록에 있었고
남은 하나를 맞춰 넣었다. **저쪽 파일은 읽기만 했다.**

낱말은 부분 문자열로 찾는다. **정답표 서른한 발화에 하나도 안 걸린다** (테스트로
지킨다). "오송역 근처" 는 "이 근처" 가 아니고 "오송역 위치" 도 "이 위치" 가 아니다.

**값이 없으면 안 쓴다.** 낱말이 걸려도 문맥에 값이 없으면 기존 마흔으로 간다.
값이 있는지는 이미 있는 `context_starts` 가 센다 — 새 기준을 안 만들었다.
**우클릭 전(저쪽 평상시)이면 「보이는 범위」 열 벌만 보여준다.** 「찍은 지점」
아홉 벌은 그때 골라도 좌표가 없어 못 쓰기 때문이다.

#### 3. menu 자수 — 갈린 뒤

```
전체 (recipe 59)                        4833자   180줄
기존 마흔        발화가 화면을 안 가리킴  3322자   123줄
화면 열아홉      우클릭 뒤(문맥 둘 다)    1535자    61줄
  보이는 범위 열  우클릭 전(저쪽 평상시)   809자    34줄
  찍은 지점 아홉                          750자    31줄
```

**「기존 마흔」은 화면 노드가 붙기 전 `menu.yaml` 과 본문이 한 글자도 다르지 않다.**
`a238b48~1` 의 파일을 꺼내 recipe 번호만 지우고 맞대 보니 동일하고 자수도 3322 로
같았다. (「마흔여섯째」가 3321 로 적은 것은 한 글자 어긋난 것이다.)

#### 4. ★★★ 네 묶음 + 대조군 — 「67」이 오늘 다시 안 나온다

`--runs 3` · 같은 세션에서 연달아 · 다른 것과 안 겹쳤다.

```
                                        LLM 단독  적중   근접  빗나감  못붙음   기준선 아홉
  전  a238b48 · 문맥 없음   묶음1        41/93   49/93   40     4      0      18/27
  후  이번 변경 · 문맥 bbox 묶음1        42/93   51/93   36     6      0      18/27
  전  a238b48 · 문맥 없음   묶음2        41/93   49/93   40     4      0      18/27
  후  이번 변경 · 문맥 bbox 묶음2        42/93   51/93   36     6      0      18/27
```

**★ 두 묶음이 한 글자도 다르지 않았다.** 발화별 표까지 같다 — 오늘 이 모델은
결정론적으로 답한다. **「마흔두째」가 정한 잡음 폭 ±5 가 오늘은 0 이다.**
그래서 아래 차이는 전부 잡음이 아니다.

그리고 **묶음 둘을 더 쟀다.** 하나는 진단이고 하나는 대조군이다.

```
                                        LLM 단독  적중   근접  빗나감  못붙음   기준선 아홉
  진단  이번 변경 · 문맥 없음            48/93   54/93   34     5      0      16/27
  대조  101a155(first-demo-passed)      48/93 ★53/93   22    16      2      20/27
        recipe 40 · 문맥 없음
```

**진단**은 「menu 를 가른 것」만의 값이다(문맥이 없으니 축 선택지도 후보도 예전
그대로다). 49 -> 54. **menu 를 가른 몫은 +5 다.**

**대조**는 `101a155` 을 그대로 체크아웃해 오늘 모델로 다시 잰 것이다. 커밋도
reset 도 안 했고 재고 나서 원래 자리로 돌아왔다. **67/93 이 나와야 하는 자리에서
53/93 이 나왔다. 빗나감은 6 이 아니라 16 이었다.**

```
              적중    빗나감   기준선 아홉    잰 날
  101a155     67/93     6       22/27      「마흔여섯째」가 옛 값으로 적은 것
  101a155     53/93    16       20/27      ★ 오늘 같은 커밋을 다시 잰 값
```

**무엇을 뜻하는가.** `a238b48` 쪽은 오늘도 잘 재현된다(51 -> 49, 차 2).
**재현이 안 되는 것은 옛 쪽뿐이다.** 그래서 「문맥을 받고 16 내렸다」는 이야기의
두 끝 중 위쪽이 오늘 이 기계에 없다. 왜 그런지는 안 팠다 — 모델 파일도 ollama
설정도 이번 범위 밖이라 건드리지 않았다. **다음에 「67」을 근거로 쓸 때는 그
숫자가 오늘 다시 나오는지부터 재야 한다.**

#### ★ 측정 기준선이 날짜를 건너 안 이어진다

`101a155`(first-demo-passed)을 오늘 그대로 체크아웃해 다시 쟀다.

```
기록 (2026-08-28)   67/93 · 빗나감  6
오늘 (2026-08-29)   53/93 · 빗나감 16
```

**같은 커밋인데 다르다.** `a238b48` 쪽은 51 -> 49 로 잘 재현되므로 **옛 자리만
못 잰다.** 사이에 우분투 재부팅이 있었다. 왜인지는 안 팠다 — 모델·ollama 설정은
이 작업의 범위 밖이다.

```
★ 따라오는 규칙
  「67 로 돌아가자」 같은 목표를 쓸 수 없다. 그 숫자가 오늘 없다
  NOTES 의 옛 숫자와 오늘 숫자를 직접 견주지 않는다
  ★ 같은 날 나란히 잰 것끼리만 견준다 — 번갈아 재기는 그대로 유효하다
    오늘 네 묶음 중 두 쌍이 한 글자도 안 달랐다
```

**앞으로 판정을 인용할 때 잰 날짜를 함께 적는다.**

#### 5. 발화별로 어디서 갈렸나

「67 근처로 안 돌아오면 발화별로 적는다」는 약속에 따라 싣는다. 값은 적중 회수(3회 중).

```
  #  발화                          대조  전  후  진단   후-전
  3  오송역 CCTV 보여줘              3   0   3    3    +3 ★ 시연 자리가 되살아났다
  9  충북 제1선거구 알려줘            3   3   0    3    -3 ★ 문맥이 오면 깨진다
 12  청주시 행정경계 보여줘           0   3   0    0    -3 ★
 14  오송역 국회의원 공약 보여줘       2   0   3    0    +3 ★
 15  청주 선거구 찾아줘              0   1   3    0    +2 ★
 16  교통 공약 많은 선거구 검색해줘     3   3   0    3    -3 ★ 문맥이 오면 깨진다
 21  경부선 선형 데이터 줘            3   0   3    3    +3 ★
 23  경부선 주변 CCTV 보여줘          3   0   3    3    +3 ★
 24  오송역 근처 충전소 자세히         3   3   1    3    -2 ★ 문맥이 오면 깨진다
 11  오송역 행정경계 보여줘           3   3   2    3    -1
     (나머지 스물하나는 후-전 0)
     합계                          53  49  51   54
```

**읽을 것 하나.** 9 · 12 · 16 · 24 는 화면 낱말이 하나도 없는 발화다. menu 는
「기존 마흔」으로 예전과 같은데도 깨졌다. **그 자리들에서 달라진 것은 menu 가
아니라 축 선택지다** — 문맥이 오면 `_choices_for` 가 「보이는 범위」를 안 지우므로
`given` 선택지가 셋에서 넷이 된다. 진단 묶음(문맥 없음)에서 9 · 16 · 24 가 다시
3/3 인 것이 그것을 받쳐 준다. **menu 를 가르는 것만으로는 문맥이 있는 조건을
못 지킨다.** 이번에 손대지 않았다 — 축 선택지는 「값이 있으면 남긴다」가 맞는
규칙이고, 그것을 바꾸면 화면 발화가 죽는다.

**시간.** 전 3.4초 · 후 4.4초 (/resolve 한 번 평균). **프롬프트가 짧아졌는데
느려졌다.** 까닭은 안 팠다.

#### 6. 화면을 가리키는 발화 넷 — 정답표 밖

`--runs 3` · 넷 다 세 번 다 같은 답이었다.

```
발화                        문맥 both        문맥 bbox(저쪽 평상시)
지금 보이는 곳 CCTV 보여줘   SELECT 025      SELECT 025      보이는 범위 -> CCTV
여기 CCTV 보여줘            SELECT 018      SELECT 025      ★ 찍은 지점이 없으면 025 로 내려앉는다
이 근처 충전소 찾아줘        SELECT 031      SELECT 031      보이는 범위 -> 충전소
현재 화면 인구 알려줘        SELECT 030      SELECT 030      보이는 범위 -> 인구
```

**넷 다 화면 recipe 로 갔다. 12/12 · 되묻기 0.** 「여기」가 우클릭 전에 025 로
가는 것은 설계대로다 — 찍은 지점이 없으므로 그 아홉 벌을 아예 안 보여준다.

#### 7. 화면 실측 다섯 — 답 문구 그대로

문맥은 「마흔여섯째」와 같은 것을 썼다 — camera (127.2974, 36.6199, 12000) ·
view.bbox [[127.20, 36.55], [127.40, 36.70]] · selectedLocation (127.2974, 36.6199).

```
가  「오송역 CCTV 보여줘」   Streamlit (고정 문맥 · POST /resolve)
    SELECT  recipe_035  ['recipe_035']
    ★ 곧장 실행된다. 「마흔여섯째」의 「라」에서 되묻기로 내려앉았던 자리다

나  「오송역 CCTV 보여줘」   저쪽 (POST /chat · 문맥 있음)
    오송역 CCTV 를 조회했습니다.
    1. geo.geocode       오송역 -> 충청북도 청주시 흥덕구 오송읍 봉산리 369-1 (127.3277, 36.6200)
    2. road.getCctv      minLon=127.1598 · minLat=36.4852 · maxLon=127.4956 · maxLat=36.7547  83건
    commands 3개
    ★ 가와 같은 경로다(말한 장소 -> 좌표 -> CCTV). 화면과 저쪽이 안 갈렸다

다  「지금 보이는 곳 CCTV 보여줘」  저쪽 (우클릭 전)
    CCTV 를 조회했습니다.
    1. road.getCctv      minLon=127.2000 · minLat=36.5500 · maxLon=127.4000 · maxLat=36.7000  12건
    commands 2개
    ★ 문맥의 bbox 가 그대로 도구에 갔다. recipe_025

라  「여기 CCTV 보여줘」    저쪽 (우클릭 뒤)
    CCTV 를 조회했습니다.
    1. road.getCctv      minLon=127.1295 · minLat=36.4852 · maxLon=127.4653 · maxLat=36.7546  85건
    commands 2개
    ★ 찍은 지점 + 반경 15km. recipe_018

마  「오송역 근처 충전소 자세히 알려줘」  저쪽 (4단 사슬)
    오송역 충전소 상세를 조회했습니다.
    1. geo.geocode       오송역 -> ... (127.3277, 36.6200)
    2. ev.searchStations minLon=127.1598 ... 500건 (전체 2,173건) · 청주오송역차고지
    3. ev.getStation     statId="PK000313"  8건
    commands 6개
    ★ 4단 사슬이 그대로 돈다
```

**건수가 「마흔여섯째」와 다르다** (9 -> 12 · 73 -> 85 · 72 -> 83). 같은 bbox 인데
다르므로 CCTV 데이터가 그동안 늘어난 것이다. 경로는 셋 다 같다.

**그래프.** `/graph` 200 · `/render` 가 SVG 41915자를 낸다. 그리는 노드 47개가
`layout.json` 에 좌표를 다 갖고 있어 `neato` 가 거부하지 않는다. 세 사슬
(말한 장소 · 보이는 범위 · 찍은 지점 -> CCTV)의 칩이 다 나온다.

#### 8. Streamlit 고정값과 근거

이 화면에는 지도가 없다. 그런데도 문맥을 보내는 것은 **저쪽과 같은 발화에 같은
경로를 골라야 하기 때문**이다.

```
view.bbox         [[127.1598, 36.4853], [127.4956, 36.7547]]   고정
selectedLocation  null                                          늘 null. 찍을 지도가 없다
```

**모양은 저쪽 평상시(우클릭 전)와 같다.** 값은 오송역(127.3277, 36.6200)에서
반경 15km 다 — `step_service.RADIUS_METERS` 로 만든 상자이고, `road.getCctv` 가
실제로 그 넷을 받은 적이 있다(`tests/vendor/test_workflow_answer.py` 의
minLon 127.15983…). 지어낸 값이 아니다. 시연이 오송·청주에서 돌기 때문에 그
일대를 보고 있는 셈으로 둔 것이다.

**화면에도 적었다** — 입력칸 아래 한 줄. "지도 문맥 고정 · 오송역 반경 15km
(이 화면에는 지도가 없다)". 화면에 「여기」가 통하는데 지도가 없으면 시연장에서
오해를 부른다.

`tools/check_resolve.py` 는 같은 값을 `demo/ui/config.py` 에서 가져다 쓴다.
베껴 적으면 둘이 조용히 어긋나고 「시연과 같은 조건」이라는 말이 거짓이 된다.

#### 9. 읽는 법 넷에 비춘 결과 — 미리 정해 둔 것

```
1순위  빗나감이 6 보다 크지 않은가        ★ 통과   6 이다 (전 4 · 대조 16)
2순위  적중이 67 에 얼마나 가까운가        ☓ 못 감  51 (진단 54). 다만 대조군이 오늘 53 이다
3순위  기준선 아홉이 22/27 로 돌아오는가   ☓ 못 감  18/27 (전도 18 · 대조 20 · 진단 16)
4순위  화면 발화 넷이 화면 recipe 로 가는가 ★ 통과   12/12
```

**2 · 3 순위는 「67 · 22」라는 옛 값을 자로 쓴다.** 그 자가 오늘 다시 안 나온다는
것이 4 의 대조군이다. **그래서 「진단이 반만 맞았다」로 적는 것도 정확하지 않다** —
반이 맞았는지 재는 자가 흔들린다. 오늘 잰 값끼리만 견주면 이렇다.

```
대조 101a155  53 · 빗나감 16
전   a238b48  49 · 빗나감  4
후   이번      51 · 빗나감  6      대조보다 적중 -2 · 빗나감 -10
진단 이번      54 · 빗나감  5      대조보다 적중 +1 · 빗나감 -11
```

#### 10. 관문 표

```
빗나감             ★ 통과   6. 「6 보다 크지 않다」를 지켰다
menu 자수          ★ 통과   위 3
온톨로지 무손상     ★ 통과   ontology.yaml 둘 · menu.yaml · step_service.py md5 그대로 · recipes diff 0
정답표 무손상       ★ 통과   UTTERANCES 줄 변경 0. --context 옵션만 더했다
좁히기 무손상       ★ 통과   프롬프트 둘 md5 그대로 · narrow_default() False
저쪽 무결          ★ 통과   KRRI_ASAP 추적 파일 변경 0 (미추적 둘은 이번 작업 전부터 있던 것)
그래프             ★ 통과   /graph 200 · /render SVG · 좌표 없는 노드 0
pytest             ★ 통과   353 passed / 1 failed. 실패는 graphviz 음성 대조군 하나 그대로
                            새 파일 열여덟을 더해 335 -> 353. 기존 삭제 0
화면 실측 다섯      ★ 통과   위 7
적중 67 근처        ☓ 못 감  위 9
```

#### 11. ★ 임시방편이다

**낱말 목록에 없는 말투는 화면 recipe 를 아예 못 본다.**

```
"내가 보고 있는 데 CCTV"     목록에 없다
장소 없는 "CCTV 보여줘"      가리키는 낱말이 없다
```

**후보에서 빼는 것이 아니라 아예 안 보여주는 것이라 LLM 이 고칠 길이 없다.**
`_without_dropped` 는 LLM 이 고른 뒤에 거르므로 LLM 이 다시 생각할 여지가 있지만,
menu 에서 뺀 줄은 프롬프트에 없으므로 그런 여지가 없다.

#### 12. 안 한 것

```
축 선택지를 안 갈랐다        문맥이 오면 given 이 넷이 된다. 위 5 의 9 · 16 · 24 가
                             그것 때문에 깨지는데, 「값이 있으면 남긴다」가 맞는 규칙이라
                             바꾸면 화면 발화가 죽는다. 다음 사람이 잴 자리다
「67」이 왜 안 나오는지 안 팠다  모델 파일 · ollama 설정이 이번 범위 밖이다
좁히기를 안 켰다             기본값 끔 그대로. 이번에 안 쟀다
menu.yaml 을 안 고쳤다       읽은 문자열에서 뺄 뿐이다
프롬프트 셋을 안 고쳤다       md5 그대로
커밋 안 했다
```

### 2026-08-29 (마흔일곱째) · 좁히기로 회귀가 풀리는지 쟀다 — 적중은 돌아왔고 빗나감에 졌다

**이 기록은 한 번 사라졌다 되적은 것이다.** 아래 숫자는 사람이 준 것이고
이 기록이 다시 잰 것이 아니다. 재는 일은 끝나 있었는데 그 사이 기록이
없어졌고(아래 「마흔여섯째」의 「시연 때문에 잃었다 되살렸다」), 사람이 갖고 있던 값으로 되적었다.

좁히기로 이번 회귀가 풀리는지 번갈아 쟀다 (문맥 없이, `--runs 3`, 네 묶음).

```
        적중    근접  빗나감
끔      51      34     6
켬 B    60      13    20
켬 D    58      18    17
```

**적중은 돌아왔다(+7~9). 그래서 「menu 가 길어진 것이 회귀의 원인」이라는 진단을
이 표가 받쳐 준다.** 그러나 빗나감이 세 배라 1순위에서 졌고, 미리 정한 대로
아무것도 고르지 않았다. **기본값은 끔 그대로다.**

끔에서 3/3 적중이던 발화 넷이 켬에서 빗나감이 됐다 — **8 · 11 · 12 · 16.**
23(경부선 주변 CCTV)은 반대로 빗나감 3 -> 적중 3 이 됐다.

```
★ 켬의 1차는 결정론이다 — B 와 D 의 축 표 · 좁힌 후보 수 · 폴백 · 조회 판정이
  전부 같았다. 흔들린 것은 2차의 고르기뿐이다.
```

### 2026-08-28 (마흔여섯째) · 저쪽 화면의 지도 문맥을 받았다 — 되지만 ★ 판정이 관문을 깼다

무인 실행. 모델 qwen3:32b(서버 기본) · recipe 40 -> 59 · 정답표 서른하나 · `--runs 3` ·
좁히기 끔 · **판정만** 쟀다. **커밋 안 했다.**

**★ 결론부터. 기능은 되고 판정은 깨졌다.** 화면 실측 넷은 전부 뜻대로 돌았는데,
문맥 없이 재는 판정이 적중 67/93 -> 51/93 으로 **16 내렸다.** 「마흔두째」가 정한
잡음 폭 ±5 를 세 배 넘는다. **지시대로 멈추고 되돌리지 않았다.** 표는 아래 그대로다.

```
고친 파일   ontology/ontology.yaml · ontology/_init/ontology.yaml   노드 둘 · is-a 둘
            workflows/static/_init/ · workflows/static/             rebuild_init --write · reset_to_init
            demo/api/services/step_service.py                       문맥 참조 상수 · input_first 열다섯 줄 · context_starts
            demo/api/services/resolve_service.py                    문맥이 없으면 축 선택지와 후보에서 뺌
            demo/api/services/execute_service.py                    문맥을 해석에 넘김 · 인자 없이도 부름
            tools/check_resolve.py                                  GT 번호 이동만. 기대값은 한 글자도 안 바꿈
            tests/demo/api/test_map_context.py                      새 파일 아홉
            tests/ 넷                                               대역 시그니처와 옮긴 번호
            NOTES.md
안 건드림   KRRI_ASAP/ (추적 파일 변경 0) · vendor/ · 좁히기 프롬프트 둘 · recipe_selection.md ·
            node_registration.md · shortlist.py · graph.py · store.py · registry.py ·
            check_wiring.py · demo/ui/ · demo/graph_svg/ · 기본값 끔(narrow_default() False)
```

#### 1. 저쪽이 실제로 보내는 것 — 읽고 확인했다

```
KRRI_ASAP/ASAP-web/packages/shared/src/types/index.ts  ChatRequest.context
  camera            { lon, lat, height }                     number 셋
  view.bbox         [[number,number],[number,number]]        필수
  selectedLocation  { lon, lat, label?, source? } | null      선택. 우클릭 전에는 null

KRRI_ASAP/ASAP-web/packages/chat/src/hooks/useChat.ts
  우클릭한 적이 있으면 label "관심 지점" · source "map-right-click" 을 얹어 보낸다
  컨트롤러가 없으면 DEFAULT_MAP_CONTEXT — bbox 가 [[-180,-90],[180,90]] 이다
```

**bbox 의 모양이 우리 `$prev` 와 같다.** cesium 의 `CameraManager.getMapContext` 도
maplibre 의 `MapLibre2DMap.getMapContext` 도 `[[west, south], [east, north]]` 로 만들고,
그것은 우리 `BBOX_FROM_PREVIOUS` 가 기대는 `bbox[0][0] … bbox[1][1]` 과 같은 자리다.
**맞출 것이 없었다.** vendor 의 `_resolve_reference` 가 `$context.view.minLon` 을
`bbox[0][0]` 으로 푸는 것을 눌러서 확인했다.

**우리 `ChatRequest` 는 이미 `context` 를 받고 있었다** (`schemas/requests.py`.
"text 와 sessionId 말고 둘은 여전히 안 읽는다" 로 적혀 있던 그 자리다).
`/chat` -> `execute_service.chat` -> `run` -> `state["context"]` 까지도 이미 흐르고
있었다. **없던 것은 「해석이 그것을 안다」 한 자리뿐이었다.**

저쪽 plugin 둘은 읽기만 했다.

```
current-view-cctv     $context.view.bbox.0.0 … .1.1 -> road.getCctv 네 칸.  Expose To Prompt yes
cctv-around-point     center + radiusMeters(기본 1000) -> point_radius_to_bbox -> road.getCctv
                      Expose To Prompt **no** — 저쪽에서도 발화로 직접 부르는 것이 아니라 하네스다
```

**「그것을 쓸까」를 저쪽은 프롬프트에서 가른다.** `app/agents/nodes/parse_intent.py` 가
`selectedLocation` 이 있을 때만 "If the user says here, this place, selected location,
관심 지점, 선택한 위치, or 이 위치, use the selected map interest point." 한 줄을 덧붙인다.

#### 2. 새 노드를 몇 개로 하고 왜 — 둘이다

```
picked_point    찍은 지점     is-a 지점 좌표    context.selectedLocation
visible_extent  보이는 범위   is-a 지도 범위    context.view.bbox
```

**네 안을 다 재고 골랐다** (파일은 안 쓰고 경로 생성만 돌렸다).

```
안                     recipe   menu      새 경로
  지금                    40    3321자
  A 찍은 지점만           49    4047자     9
  B 보이는 범위만         50    4106자    10
  C 둘 다  ← 고른 것      59    4832자    19
  D 하나로 묶음           58    4771자    18
```

**D 를 안 고른 것은 자수 하나 때문이 아니다.** 묶으면 (화면 문맥 -> CCTV 조회)가 한
줄이 되는데, CCTV 조회는 받는 것이 둘(지점 좌표 · 지도 범위)이라 그 한 줄이 어느 칸을
채워야 하는지 말할 수가 없다. `ontology.yaml` 의 「받는 것이 둘이다」 주석이 이미 같은
말을 하고 있다 — **두 줄이 갈리려면 건네는 타입도 둘로 적혀 있어야 한다.** 갈라야 할
것을 붙이면서 recipe 는 하나밖에 안 준다.

**이름을 "말한 ~" 으로 안 지었다.** 앞의 셋은 사람이 입으로 말한 값이고 이 둘은
사람이 아무 값도 말하지 않았는데 화면이 함께 보내온 값이다. "말한 지점" 이라고
적으면 사람이 좌표를 불러 준 것처럼 읽힌다.

#### 3. 축 선택지 문장과 그 근거

```
말한 장소     사용자가 발화에서 지목한 장소.                    (그대로)
말한 키워드   사용자가 발화에서 찾아 달라고 한 말.              (그대로)
말한 식별자   사용자가 발화에서 집어 말한 코드나 이름.          (그대로)
찍은 지점     사용자가 화면에서 찍어 둔 한 지점. 발화가 장소 이름 대신 여기나
              이 위치, 선택한 위치라고 가리킬 때 쓴다.
보이는 범위   사용자 화면에 지금 보이는 좌표 범위. 발화가 장소 이름 대신 지금
              보이는 곳이나 현재 화면, 이 근처라고 가리킬 때 쓴다.
```

**앞의 셋과 달리 문장이 두 마디다** — 무엇이 왔는가, 그리고 언제 쓰는가.
앞의 셋은 한 마디로 끝난다. 발화에서 왔다는 말 자체가 "발화에 그것이 있을 때 고른다"
를 겸하기 때문이다. 화면에서 온 값은 **늘 와 있으므로** 그 겸함이 안 된다 —
"화면에서 온 지점" 이라고만 적으면 "오송역 CCTV" 에서도 고를 수 있게 읽힌다.

**왜 이 문장이 판단이 서는 자리인가.** 축 선택지 문장은 `shortlist._describe` 가
노드의 `name` 과 `description` 으로 만들어 프롬프트에 그대로 싣는다. 그리고 이번
범위에서 프롬프트 파일 셋은 못 건드린다. **같은 말을 적을 데가 여기뿐이었다.**
저쪽이 `parse_intent` 프롬프트에 적은 것과 자리만 다르고 하는 일은 같다.

#### 4. 문맥을 쓸지 말지 판단하는 자리 — 둘로 갈랐다

```
값이 있는가    resolve_service._choices_for · _without_dropped     기계가 센다
그것을 쓸까    온톨로지 노드 description (위 3)                     LLM 이 발화를 보고 고른다
```

**한 자리에 몰지 않았다.** 값이 없는데 LLM 이 고르는 일은 애초에 없어야 하고(관문),
값이 있을 때 쓸지 말지는 발화를 읽어야 아는 일이다(판단). 둘을 LLM 한 곳에 맡기면
Streamlit 에서 "지금 보이는 곳 CCTV" 를 눌렀을 때 null 좌표로 도구를 부른다.

```
_dropped_starts(context)   문맥이 못 채우는 시작 데이터 노드
_choices_for(context)      given 목록과 described.given 에서 함께 뺌.
                           둘 중 하나만 빼면 프롬프트에는 보이는데 스키마가 막는 선택지가 생긴다
_without_dropped(...)      조회 후보에서도 빼고, **LLM 이 고른 것에서도 뺀다** —
                           menu 에는 그 문장이 남아 있어서 LLM 이 고를 수 있다
```

**실측으로 관문을 확인했다.**

```
문맥                 given 축 선택지                                          막히는 recipe
  없음 (Streamlit)   spoken_place · spoken_keyword · spoken_identifier         19개
  우클릭 안 함       위 셋 + visible_extent                                    4개 (017~022 중 찍은 지점 것)
  온전               위 셋 + picked_point + visible_extent                     0개
```

#### 5. recipe 대응표 — 번호가 밀렸다

```
001~016  그대로            (2단 · 말한 장소 셋 · 말한 키워드 열 · 말한 식별자 셋)
017~031  새것 열다섯       찍은 지점 여섯 · 보이는 범위 아홉
032~051  옛 017~036 이 +15
052~055  새것 넷           찍은 지점 -> 행정구역 판별 -> 셋 · 보이는 범위 -> 충전소 -> 상세
056~059  옛 037~040 이 +19
```

새로 생긴 열아홉 (rebuild_init 대응표 그대로)

```
017 찍은 지점 → 지점 행정구역 판별              025 보이는 범위 → CCTV 조회            ★ current-view-cctv
018 찍은 지점 → CCTV 조회        ★ cctv-around-point
                                                026 보이는 범위 → 철도 노선 조회
019 찍은 지점 → 지점 국회의원 지역구 판별        027 보이는 범위 → 국회의원 전체 선거구 검색
020 찍은 지점 → 지점 국회의원 전체 선거구 판별   028 보이는 범위 → 국회의원 선거구 공약 검색
021 찍은 지점 → 지점 국회의원 선거구 공약 판별   029 보이는 범위 → 지방선거 교통 공약 검색
022 찍은 지점 → 지점 지방선거 교통 공약 판별     030 보이는 범위 → 인구 통계 조회
023 보이는 범위 → 행정구역 조회                  031 보이는 범위 → 전기차 충전소 검색
024 보이는 범위 → VWorld 행정경계 조회
052 찍은 지점 → 지점 행정구역 판별 → 지방선거 교통 공약 상세
053 찍은 지점 → 지점 행정구역 판별 → 연령별 인구 구성 조회
054 찍은 지점 → 지점 행정구역 판별 → 인구 변화 추이 조회
055 보이는 범위 → 전기차 충전소 검색 → 충전소 상세 조회
```

**목표는 CCTV 둘이었는데 열아홉이 생겼다.** 경로 생성은 타입으로 도는 것이라 골라서
만들 수가 없다. 열일곱은 덤이고, 그중 배선을 눌러 본 것은 하나도 없다.

#### 6. GT 옮긴 표 — 기대값은 한 글자도 안 바꿨다

```
 #  발화                          옛 번호      새 번호     사슬
 3  오송역 CCTV 보여줘            recipe_020 → recipe_035  말한 장소 → 좌표 → CCTV
 4  청주시 인구 구성 알려줘       recipe_038 → recipe_057  말한 장소 → 좌표 → 행정구역 판별 → 연령별 인구
 5  오송역 근처 충전소 찾아줘     recipe_030 → recipe_045  말한 장소 → 좌표 → 충전소 검색
10  오송역이 어느 동인지 알려줘   recipe_018 → recipe_033  말한 장소 → 좌표 → 지점 행정구역 판별
11  오송역 행정경계 보여줘        recipe_017 → recipe_032  말한 장소 → 좌표 → 행정구역 조회
13  오송역 국회의원 누구야        recipe_024 → recipe_039  말한 장소 → 좌표 → 지점 전체 선거구 판별
14  오송역 국회의원 공약 보여줘   recipe_025 → recipe_040  말한 장소 → 좌표 → 선거구 공약 검색
18  오송역 일대 인구 얼마야       recipe_029 → recipe_044  말한 장소 → 좌표 → 인구 통계
20  대전역 연령대별 인구 알려줘   recipe_038 → recipe_057  (4번과 같은 사슬)
23  경부선 주변 CCTV 보여줘       recipe_033 → recipe_048  말한 장소 → 철도 구간 형상 → CCTV
24  오송역 근처 충전소 자세히     recipe_040 → recipe_059  말한 장소 → 좌표 → 충전소 검색 → 상세
30  오송역 선거구 알려줘          recipe_022 → recipe_037  말한 장소 → 좌표 → 지점 선거구 판별
```

옛 번호는 전부 옆 주석에 남겼다. 테스트 안에서도 같은 규칙으로 옮겼다 —
`test_narrow.py` 030 → 045 (41곳) · 029 → 044 (1곳), `test_shortlist.py` 030 → 045.

#### 7. ★ 판정 표 — 관문을 깼다

문맥 없이(`POST /resolve` 는 `context` 를 안 넘긴다) 잰 값이다.

```
              LLM 단독   적중    근접  빗나감  못 붙음     기준선 아홉(27회)      확장(66회)
  옛 (101a155)  53/93   67/93    20     6      0      22/27 ·  5 · 0 · 0   45/66 · 15 · 6 · 0
  이번          48/93   51/93    34     6      2      18/27 ·  9 · 0 · 0   33/66 · 25 · 6 · 2
  차            -5      ★ -16   +14     0     +2         -4  ·  +4        ★ -12  · +10 · 0 · +2
```

**「마흔두째」의 잡음 폭은 적중 ±5 다. -16 은 그 세 배다. 잡음이 아니다.**

```
빗나감 자리   옛  12 청주시 행정경계(3) · 27 철도안전법 내용(3)                        = 6
              이번 18 오송역 일대 인구(1) · 23 경부선 주변 CCTV(3) ·
                   25 문서에서 철도안전법(1) · 27 철도안전법 내용(1)                   = 6
              ★ 수는 같은데 자리가 통째로 갈렸다. 12 는 3/3 적중으로 나았고
                23 이 새로 생겼다 — 23 은 CCTV 발화다
못 붙음       0 → 2 (27번이 NO_MATCH 2회)
```

발화별 표는 그대로 싣는다.

```
  #  발화                          적중  근접 빗나감 못붙음   틀렸을 때 나온 것
  1  오송역 위치 보여줘             3/3   0    0    0
  2  오송역 좌표 알려줘             3/3   0    0    0
  3  오송역 CCTV 보여줘             0/3   3    0    0     {035, 049}                      CLARIFY 3회
  4  청주시 인구 구성 알려줘        0/3   3    0    0     {011, 057}                      CLARIFY 3회
  5  오송역 근처 충전소 찾아줘      3/3   0    0    0
  6  국회의원 선거구 찾아줘         0/3   3    0    0     {007,008,009,014,015,016,038,039,040,041} CLARIFY 3회
  7  전기차 충전소 데이터 검색해줘  3/3   0    0    0
  8  철도 안전 문서 찾아줘          3/3   0    0    0
  9  충북 제1선거구 알려줘          3/3   0    0    0
                                  18/27  9    0    0    67%  ← 기준선 아홉
 10  오송역이 어느 동인지 알려줘    3/3   0    0    0
 11  오송역 행정경계 보여줘         3/3   0    0    0
 12  청주시 행정경계 보여줘         3/3   0    0    0     ★ 옛 빗나감 3 이 사라졌다
 13  오송역 국회의원 누구야         0/3   3    0    0     {037, 039, 056}                 CLARIFY 3회
 14  오송역 국회의원 공약 보여줘    0/3   3    0    0     {037,038,039,040,041,042,056}   CLARIFY 2회
                                                        {037,038,039,040,041,042}       CLARIFY 1회
 15  청주 선거구 찾아줘             3/3   0    0    0
 16  교통 공약 많은 선거구 검색해줘 3/3   0    0    0
 17  청주 국회의원 공약 검색해줘    0/3   3    0    0     {007, 008, 009}                 CLARIFY 3회
 18  오송역 일대 인구 얼마야        0/3   2    1    0     {011,044,057,058} 근접 CLARIFY 2회 / {058} ★빗나감 SELECT 1회
 19  인구 많은 시군구 순위 보여줘   3/3   0    0    0
 20  대전역 연령대별 인구 알려줘    3/3   0    0    0
 21  경부선 선형 데이터 줘          0/3   3    0    0     {002, 004, 005}                 CLARIFY 3회
 22  오송역 지나는 노선 알려줘      0/3   3    0    0     {003, 036, 049}                 CLARIFY 3회
 23  경부선 주변 CCTV 보여줘        0/3   0    3    0     {006, 013}          ★빗나감     CLARIFY 3회
 24  오송역 근처 충전소 자세히      3/3   0    0    0
 25  문서에서 철도안전법 관련 내용  2/3   0    1    0     {006}               ★빗나감     SELECT  1회
 26  문서에서 철도 안전 교육 내용   3/3   0    0    0
 27  철도안전법 내용 찾아줘         0/3   0    1    2     {} 못붙음 NO_MATCH 2회 / {006} ★빗나감 SELECT 1회
 28  경부선 노선 보여줘             1/3   2    0    0     {003, 006, 013}                 CLARIFY 2회
 29  청주시 인구 알려줘             3/3   0    0    0
 30  오송역 선거구 알려줘           0/3   3    0    0     {037, 038, 040, 042}            CLARIFY 3회
 31  청주 국회의원 선거구 검색해줘  0/3   3    0    0     {007, 008}                      CLARIFY 3회
                                  33/66 25    6    2    50%  ← 확장 열아홉
                                  51/93 34    6    2    55%  ← 합계
```

#### 8. 왜 내렸나 — 재서 하나를 집었다

**LLM 이 문맥도 없는데 화면 recipe 를 계속 집는다.** menu 는 온톨로지가 만드는 것이라
요청마다 다를 수 없다. 그래서 문맥을 안 보낸 요청에서도 LLM 은 "찍은 지점으로 …" ·
"보이는 범위로 …" 열아홉 문장을 그대로 본다. `_without_dropped` 가 그것을 뒤에서
빼주지만, **LLM 이 고를 자리를 이미 그쪽에 썼다.**

검산 표에서 셀 수 있는 것만 세어도 이렇다 (그 표는 검산이 값을 바꾼 회차만 싣는다).

```
  3번 {018,025} 3회   4번 {053} 3회      6번 {019,020,021} 3회   10번 {017} 3회
 12번 {023}     3회  13번 {019} 3회     14번 {019,020}     3회   16번 {027,028,029} 3회
 22번 {026}     3회  23번 {025} 3회     28번 {026}         2회   31번 {027,028}     3회
                                                        ★ 적어도 35회 / 93
```

10번과 12번은 그래도 살았다 — 검산이 화면 것을 빼고 나니 하나만 남아 SELECT 다
(검산 표의 "검산이 살렸다" 자리). **나머지는 후보가 흩어져 CLARIFY 로 떨어졌다.**

**둘째 원인은 menu 자체가 커진 것이다.** 3321 -> 4833자, 문장 40 -> 59개다.
`shortlist.py` 가 애초에 만들어진 까닭이 "menu 문장의 앞 30자가 23개나 같아 LLM 이
48문장 사이에서 하나를 못 고른다" 였는데, 앞토막이 같은 문장이 더 늘었다 —
"찍은 지점으로 그 지점의 …" 여섯과 "보이는 범위로 …" 아홉이 통째로 그 꼴이다.

**축 목록이 커진 탓은 작아 보인다.** 문맥이 없으면 given 은 셋 그대로다.

```
                        옛      이번      문맥이 올 때
  축 목록(셋 합)        730자   734자     922자
  menu.yaml            3321자  4833자    같음 (요청마다 다를 수 없다)
  채운 프롬프트         7133자  7648자    7836자
```

**축 목록은 4자 늘었고 menu 는 1512자 늘었다.** 문맥이 없는 요청에서 커진 것은
menu 하나다. 그래서 "축 선택지가 늘어 흔들렸다" 가 아니라 **"menu 가 열아홉 줄
늘어 흔들렸다"** 로 읽는다. (「건드리기 전에 읽을 것」 표의 그 줄도 이 값으로 고쳤다.)

**이 자리에서 안 고쳤다.** 지시가 「폭을 넘으면 멈추고 보고한다. 되돌리지 말고 표를
그대로 싣는다」였다. 아래 「열린 과제」에 손댈 자리 후보만 적는다.

#### 9. 화면 실측 넷 — 답 문구 그대로

`POST /chat` 에 저쪽 화면이 보내는 모양 그대로 실었다. 화면 문맥은
오송역 언저리를 비추는 것으로 만들었다 — camera (127.2974, 36.6199, 12000) ·
view.bbox [[127.20, 36.55], [127.40, 36.70]] · selectedLocation (127.2974, 36.6199).

```
가  「지금 보이는 곳 CCTV 보여줘」   문맥 있음
    CCTV 를 조회했습니다.

    1. road.getCctv      minLon=127.2000 · minLat=36.5500 · maxLon=127.4000 · maxLat=36.7000  9건
    commands 2개 : map.draw · view.camera.flyTo
    ★ 문맥의 bbox 가 그대로 도구에 갔다. recipe_025

나  「여기 CCTV 보여줘」             문맥 있음
    CCTV 를 조회했습니다.

    1. road.getCctv      minLon=127.1295 · minLat=36.4852 · maxLon=127.4653 · maxLat=36.7546  73건
    commands 2개 : map.draw · view.camera.flyTo
    ★ 찍은 지점 + 반경 15km 를 vendor 의 point_radius_to_bbox 가 사각형으로 폈다. recipe_018

다  「오송역 CCTV 보여줘」           문맥 있음
    오송역 CCTV 를 조회했습니다.

    1. geo.geocode       오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1 (127.3277, 36.6200)
    2. road.getCctv      minLon=127.1598 · minLat=36.4852 · maxLon=127.4956 · maxLat=36.7547  72건
    commands 3개 : map.draw · view.camera.flyTo
    ★ 문맥이 와 있어도 발화에 장소가 있으면 예전 지오코딩 길로 간다. 노드 설명이 한 일이다

라  「오송역 CCTV 보여줘」           문맥 없음 (Streamlit 자리)
    어느 것을 보시겠습니까?
      1  CCTV 조회
      2  철도 노선 조회

    사용자의 요청은 '오송역 CCTV 보여줘'로, 지점에 해당하는 CCTV 목록을 조회하는 작업을
    끝으로 한다. 'CCTV 보여줘'는 지도 범위 안의 CCTV 목록을 조회하는 작업을 의미한다.
    commands 0개
    ★ 예전에는 곧장 실행하던 자리다. 되묻기로 내려앉았다 — 위 판정 표 3번과 같은 자리다
```

**가 · 나 · 다는 뜻대로 됐다. 라가 이번 회귀를 눈으로 보여 준다.**
셋째 물음("문맥이 있어도 발화에 장소가 있으면 지오코딩")은 다에서 확인됐다.

#### 10. 관문 표

```
저쪽 무결          ★ 통과   KRRI_ASAP 추적 파일 변경 0 (git status 는 이번 작업 전부터 있던
                            미추적 둘 — ASAP-mcp/data.bak.20260824 · docker-compose.override.yml)
두 ontology.yaml   ★ 통과   cmp 로 바이트 동일
rebuild_init       ★ 통과   새 recipe 열아홉. 목록은 위 5. 두 번째 돌려도 (새것) 0
check_wiring       ★ 통과   recipe 59개 · 배선이 다 있는 것 58개 · STEP_OF 35줄 · A 0 · B 0 · C 1
                            C 는 web_fetch × 웹 주소 하나. 새 노드는 C 로 안 잡힌다
                            (데이터 노드라 hasInput 이 없다)
pytest             ★ 통과   335 passed / 1 failed. 실패는 graphviz 음성 대조군 하나로 그대로다
                            (stash 로 되돌려 재봤다 — 변경 전에도 같은 하나가 실패한다)
                            새 파일 아홉을 더해 326 -> 335. 기존 삭제 0
좁히기             ★ 통과   기본값 끔 (narrow_default() False) · 프롬프트 둘 md5 그대로
화면 실측 넷       ★ 통과   위 9
문맥 없이 지금과 같다  ★★★ 깨졌다   적중 67 -> 51. 폭 ±5 를 세 배 넘는다
```

#### 안 한 것

- **되돌리지 않았다.** 지시가 그것이다. 판정을 되찾는 손질도 안 했다.
- **정답표에 화면 발화를 안 더했다.** 「마지막에 정한다」였는데, 판정이 이미 깨진
  자리에 자를 늘리면 옛 표와 새 표가 더 안 이어진다. 화면 실측 넷으로만 남긴다.
- **열일곱 개의 덤 recipe 를 눌러 보지 않았다.** 배선(`input_first`)은 열다섯 줄 다
  적었지만 실제로 부른 것은 CCTV 둘뿐이다. 나머지는 `$prev` 로 이미 쓰던 것과 같은
  모양을 문맥으로 바꾼 것이라 지어낸 값은 없지만, **된다고 말할 근거도 없다.**
- **`demo/api/main.py` 의 낡은 한 줄을 못 고쳤다.** `/chat` docstring 의
  "context 는 읽지 않고 vendor 참조 범위($context.…)로 넘기기만 함" 이 이제 거짓이다
  (해석도 읽는다). 이번 범위 밖 파일이라 손대지 않았다.
- **`demo/ui/components/sample_picker.py` 의 SAMPLES 도 안 고쳤다.** 거기 적힌
  recipe_002 는 이번 변경 전부터 이미 어긋나 있었다(철도 구간 형상 조회다).
  범위 밖이고, 이번에 새로 어긋난 것이 아니다.
- **좁히기 길(`--narrow`)로 안 쟀다.** 지금 길만 쟀다.
- **모델을 바꿔 안 쟀다.**

#### 시연 때문에 잃었다 되살렸다 (뒤에 적는다)

2026-08-28 저녁, 갑작스러운 시연 때문에 이 변경을 잠시 되돌렸다.
`git stash` 는 **추적 중인 파일만** 숨기므로 새 recipe 열아홉(두 벌)과
`tests/demo/api/test_map_context.py` 는 남았고, 온톨로지에 없는 노드를 recipe 가
가리켜 그래프가 안 그려졌다 (`neato: picked_point has no position`).
사람이 새 파일을 옆으로 옮기고 `layout.json` 을 지워 시연을 마쳤다.
그 뒤 stash 를 drop 해 변경이 사라졌고, 사람이 따로 갖고 있던 사본으로 되살렸다.

**되살릴 때 stash 커밋(3727640, 64 files)을 안 썼다.** 「마흔여섯째」 보고가
63 files 였는데 그것은 64 files 라, 중단된 다음 작업이 섞여 있을 수 있기
때문이다. 그 커밋은 `git fsck` 로 아직 찾을 수 있다.

```
★ 배운 것 : 온톨로지에 노드를 더하면 layout.json 에 좌표가 없어 그래프가
  안 그려진다. 등록 화면을 거치면 붙지만 파일을 직접 고치면 안 붙는다.
  노드를 더하는 작업의 관문에 「좌표가 붙었는가」를 넣는다.
★ 배운 것 : 새 파일이 많은 변경은 git stash 로 통째로 못 숨긴다.
```

### 2026-08-28 (마흔다섯째) · 인자 예시 한 줄을 P1 로 바꿔 채택했다 — 사람이 정했다

무인 실행. 모델 qwen3:32b(서버 기본) · recipe 40 · 정답표 서른하나 · `--runs 3` · 같은 세션에서
연달아 · **판정만** 쟀다. **커밋했다. 문안은 P1 로 두었다** — 되돌리지 않았다.
**채택은 사람이 정했다.** 이번 작업 지시가 「인자 예시 한 줄을 P1 로 바꾸고 판정을 확인한 뒤
커밋한다」였다. 이 기록이 고른 것이 아니라 사람이 고른 것을 확인하고 적은 것이다.

```
고친 파일   workflows/static/prompts/recipe_selection.md  — 말한 키워드 한 줄. 남긴다
            NOTES.md
안 건드림   온톨로지 · menu · 배선표 · 정답표(UTTERANCES · HUMAN_ARGUMENT) · resolve_service ·
            좁히기 프롬프트 둘(recipe_axes.md · recipe_pick.md) · 기본값 끔 · vendor/ · KRRI_ASAP/
            잰 뒤 md5 로 대조했다 — recipe_axes · recipe_pick · check_resolve · check_argument ·
            sweep_utterances 다섯이 재기 전과 같다
끊김 대비   스크립트에 trap ... EXIT INT TERM 을 걸어 끊겨도 P0 로 돌아가게 했다
            (「마흔셋째」·「마흔넷째」 전례). 다 돌고 나서 사람 판단으로 P1 을 다시 얹었다
```

#### 왜 바꿨나 — 근거는 「마흔넷째」다

```
고친 것 넷   15 · 16 · 17 · 31 이 0건에서 실제 건수로. 전부 정답표 발화다
안정성       P1 두 묶음이 인자 16/16 일치. P0 는 9/16
헛것 하나    「마흔셋째」의 29 손상은 재현되지 않았다 — recipe 드리프트였다
진짜 손상 둘  v11(120건 → 전국 500건) · v12(문서 6건 → 철도 노선 55건)
             ★ 둘 다 정답표 밖 변주다. 아래 「열린 과제」에 새 항목으로 옮겼다
```

「마흔넷째」는 인자 표만 쟀고 판정을 안 쟀다. **판정이 대가를 치르지 않았는지가 이번 물음이다.**

#### 바꾼 줄 — 전후

`[축 고르는 법]` 의 「말한 키워드」 한 줄만이다. 다른 줄은 한 글자도 안 건드렸다.
예시를 늘리지 않았다(「마흔두째」에서 예시를 더했더니 「하나로 확신」이 는 전례가 있다).

```
전   말한 키워드 무엇을 찾는지. "국회의원 선거구 찾아줘" -> "국회의원 선거구"
후   말한 키워드 무엇을 찾는지. "청주 선거구 찾아줘" -> "청주"
```

**「마흔셋째」·「마흔넷째」의 P1 과 글자까지 같다.** 파일 자수가 P0 2138 · P1 2130 으로
그 두 기록에 적힌 값과 같다 — 같은 문안이라는 증거다.

#### 잰 것 — 번갈아 네 묶음

```
P0a → P1a → P0b → P1b     네 묶음 · 31발화 × 3회(93회) · 14:28 ~ 14:48 (묶음당 5분)
서버는 프롬프트를 요청마다 파일에서 읽으므로 재시작 없이 갈아 끼웠다
```

#### 판정 네 묶음 표

```
          LLM 단독   적중    근접  빗나감  못 붙음      기준선 아홉(27회)        확장(66회)
  P0a    54/93    59/93    27     7      0      22/27 ·  5 · 0 · 0    37/66 · 22 · 7 · 0
  P1a    53/93    67/93    20     6      0      22/27 ·  5 · 0 · 0    45/66 · 15 · 6 · 0
  P0b    54/93    59/93    27     7      0      22/27 ·  5 · 0 · 0    37/66 · 22 · 7 · 0
  P1b    53/93    67/93    20     6      0      22/27 ·  5 · 0 · 0    45/66 · 15 · 6 · 0
```

**★ P0a 와 P0b 가 표 전체에서 한 글자도 안 다르다. P1a 와 P1b 도 그렇다.**
발화별 표 · 축 표 · 후보 표 · 검산 표까지 통째로 같다(시간 칸만 다르다). 즉
**이 세션의 판정 드리프트 폭은 0 이다.** 인자 표에서 P0 둘이 7/16 로 갈렸던
「마흔넷째」와 같은 날 같은 장비인데 판정은 안 흔들렸다.

#### 미리 정한 「읽는 법」에 대보면

```
빗나감   P0 7 · 7   P1 6 · 6      ★ P1 이 크지 않다. 오히려 1 적다 → 되돌릴 이유가 없다
적중     P0 59 · 59  P1 67 · 67   +8. 「마흔두째」의 폭 ±5 밖이지만 ★ 근거로 삼지 않는다
                                  (미리 정한 대로 적어만 둔다)
드리프트 P0a = P0b (차이 0)        이 세션의 폭이다. 위 두 줄을 이 폭 위에서 읽는다
못 붙음  넷 다 0                   「마흔셋째」의 P3 가 냈던 못 붙음 2 는 여기 없다
```

**폭이 0 이라 +8 과 −1 이 잡음일 수 없다.** 다만 폭 0 은 묶음 둘씩으로 잰 값이고,
같은 날 인자 표에서는 폭이 컸다 — **「판정은 안 흔들리고 인자는 흔들린다」로 읽을 자리가
생겼지만 표 하나로 결론 낼 것은 아니다.**

#### 어느 발화가 움직였나 (P0 → P1, 적중 회수 / 3)

```
오른 것 여덟 (+14)
   5 오송역 근처 충전소 찾아줘      2 → 3     7 전기차 충전소 데이터 검색해줘  1 → 3
  11 오송역 행정경계 보여줘         0 → 3    14 오송역 국회의원 공약 보여줘    2 → 3
  17 청주 국회의원 공약 검색해줘    2 → 3  ★ 유일하게 빗나감이 사라진 자리 (1 → 0)
  22 오송역 지나는 노선 알려줘      0 → 2    29 청주시 인구 알려줘             1 → 3
  31 청주 국회의원 선거구 검색해줘  1 → 3

내린 것 넷 (−6)
   4 청주시 인구 구성 알려줘        3 → 1    (근접 2 · {011, 038} 로 되묻는다)
   6 국회의원 선거구 찾아줘         1 → 0    (근접 3 · 어차피 어떤 인자로도 0건인 자리다)
  13 오송역 국회의원 누구야         1 → 0    (근접 3)
  18 오송역 일대 인구 얼마야        3 → 1    (근접 2)

★ 내린 넷은 전부 적중 → 근접이다. 빗나감으로 간 것이 하나도 없다.
  「틀린 답보다 정직한 되물음이 낫다」는 이 저장소의 원칙에 비추면 대가가 가볍다
```

빗나감 자리
```
P0   12 청주시 행정경계(3) · 17 청주 국회의원 공약(1) · 27 철도안전법 내용(3)   = 7
P1   12 청주시 행정경계(3) ·                            27 철도안전법 내용(3)   = 6
★ 새로 생긴 빗나감이 없다. 12 와 27 은 P1 이 못 고치는 자리다 (아래 「열린 과제」)
```

#### 「마흔셋째」의 판정 표와 같은 값이 나왔다

「마흔셋째」의 판정 P0 는 `54/93 · 59/93 · 27 · 7 · 0`, P1 은 `53/93 · 67/93 · 20 · 6 · 0`
이었다. **이번 네 묶음이 그 두 줄과 칸마다 같다.** 다른 세션 · 다른 날의 값이 아니라
같은 날이지만 묶음이 넷 더 늘어난 것이라 **「판정은 재현된다」쪽 증거가 하나 늘었다.**
인자 표가 같은 날 갈렸던 것과 나란히 둔다.

#### 관문

```
문안            말한 키워드 한 줄만. 다른 줄 변화 0 (`git diff` 1 insertion · 1 deletion)
정답표          check_resolve.py · check_argument.py · sweep_utterances.py 체크섬 변화 없음
좁히기          recipe_axes.md · recipe_pick.md 체크섬 변화 없음 · 기본값 끔 그대로
빗나감          P1 둘이 P0 둘보다 크지 않다 (6 · 6 대 7 · 7)
pytest          326 passed / 1 failed — graphviz 음성 대조군 하나뿐이다
check_wiring    recipe 40 · 완비 39 · STEP_OF 35줄 · A 0 · B 0 · C 1 (web_fetch×웹 주소)
```

#### 안 한 것

- **인자 표를 안 쟀다.** 이 기록의 수는 전부 판정이다. 인자 근거는 「마흔넷째」것을 쓴다.
- **reason 을 안 읽었다.** v11 이 왜 인자를 안 뽑는지 · 4 · 6 · 13 · 18 이 왜 근접으로
  내렸는지 여전히 모른다.
- v11 · v12 를 안 팠다. 「열린 과제」에 항목만 세웠다.
- 묶음을 셋씩으로 안 늘렸다. 계획한 넷만 돌렸다.
- **push 안 했다.**
- 12 「청주시 행정경계 보여줘」와 27 「철도안전법 내용 찾아줘」는 P1 뒤에도 빗나감 3 이다.
  둘 다 인자가 아니라 축 · UX 자리라 이번 문안으로는 안 고쳐진다.

### 2026-08-28 (마흔넷째) · P1 이 고친 것과 깨뜨린 것을 번갈아 재서 갈랐다 — 고르지 않았다

무인 실행. 모델 qwen3:32b(서버 기본) · `--runs 3` · 같은 세션에서 연달아 · **인자 표만** 쟀다.
**커밋 안 했다. 문안은 되돌렸다** — 끝난 뒤 `recipe_selection.md` 체크섬이 재기 전과 같고
`git diff` 에 그 파일이 없다.

```
고친 파일   workflows/static/prompts/recipe_selection.md  — 재는 동안만. 되돌렸다
안 건드림   온톨로지 · menu · 배선표 · 정답표(UTTERANCES · HUMAN_ARGUMENT) · resolve_service ·
            좁히기 프롬프트 둘(recipe_axes.md · recipe_pick.md) · 기본값 끔 · vendor/ · KRRI_ASAP/
            서버는 프롬프트를 요청마다 파일에서 읽으므로 재시작 없이 갈아 끼웠다
끊김 대비   스크립트에 trap ... EXIT INT TERM 을 걸어 끊겨도 P0 로 돌아가게 했다 (「마흔셋째」 전례)
```

#### 왜 다시 쟀나

「마흔셋째」에서 P1(인자 예시 한 줄)이 발화 16 을 3/3 고쳤다. 그런데 같은 회차에서 P0 를 네 번
쟀더니 **인자가 갈린 발화가 9/35** 였다. 드리프트다. 그래서 **P1 이 깨뜨렸다고 적힌 자리
(29 · v11)가 진짜인지, 고쳤다고 적힌 자리(16)가 대조군 탓은 아닌지 알 수 없었다.**
번갈아 재면 갈린다 — 「열아홉째」·「서른셋째」·「마흔한째」에서 세 번 쓴 방법이다.

#### 잰 것

```
P0a → P1a → P0b → P1b     네 묶음 · 16발화 × 3회 · 13:50 ~ 14:00 (묶음당 2.5분)
```

**발화를 35 에서 16 으로 줄였다.** 이번 물음에 필요한 것만 남겼다 —
표적 넷(16 · 29 · v11 · 12)과 「마흔셋째」에서 P0 넷이 갈렸던 아홉(7 · 12 · 15 · 17 · 26 · 27 ·
31 · v10 · v12). `--only` 는 번호로 거는데 확장 표 10~13 과 변주 10~13 의 번호가 겹쳐서
확장 10 · 11 · 13 이 덤으로 딸려 왔고, 그대로 뒀다. **판정은 안 쟀다** — 「마흔셋째」에서 판정
P0 를 한 묶음밖에 못 재 폭을 모르고, 이번 물음에 필요 없다.

**P1 은 「마흔셋째」의 것을 그대로 썼다.** 새로 쓰지 않았다. 파일 자수가 P0 2138 · P1 2130 으로
「마흔셋째」에 적힌 값과 같다 — 같은 문안이라는 증거다.

**P0 (지금 문안, `[축 고르는 법]` 20줄)**
```
    말한 키워드 무엇을 찾는지. "국회의원 선거구 찾아줘" -> "국회의원 선거구"
```
**P1** — 그 한 줄만 바꾼다. 나머지 줄은 P0 그대로다.
```
    말한 키워드 무엇을 찾는지. "청주 선거구 찾아줘" -> "청주"
```

#### 네 묶음 표 — 인자 낱말 · (recipe 건수)

`×n` 은 세 번 중 몇 번인지. 인자 `-` 는 인자를 못 뽑은 것(`argument = null`)이다.
**「읽는 법」은 인자 낱말로만 가른다** — recipe · status 가 갈려도 인자가 같으면 A 다.

```
읽는 법  #    발화                      P0a                 P1a         P0b                 P1b
A   7   전기차 충전소 데이터 검색해줘  -(500)              -(500)      -(500)              -(500)
A  10   오송역이 어느 동인지          오송역(1단건)        오송역(1단건) 오송역(1단건)        오송역(1단건)
A  11   오송역 행정경계               오송역(1단건)        오송역(1단건) 오송역(1단건)        오송역(1단건)
A  12   청주시 행정경계               청주시(004 4)       청주시(019 1) 청주시(019 1)       청주시(019 1)
A  13   오송역 국회의원 누구야        오송역(024 1단건)    오송역(022 1) 오송역(022 1)×2·(024)×1 오송역(022 1)
B  15   청주 선거구 찾아줘            청주 선거구(0)×2·청주(4)×1  청주(4)     청주(4)             청주(4)
B  16   교통 공약 많은 선거구         교통 공약 많은 선거구(0)     교통(20)    교통 공약(0)         교통(20)
B  17   청주 국회의원 공약            청주 국회의원 공약(0)×2·국회의원 공약(0)×1  청주(16)  청주 국회의원 공약(0)×2·청주(025 1)×1  청주(16)
B  26   문서에서 철도 안전 교육 내용  철도 안전 교육 내용(6)      철도 안전 교육(6) 철도 안전 교육(6)  철도 안전 교육(6)
B  27   철도안전법 내용               -(006 오류)          -(부를 것 없음)×2·(035 오류)×1  철도안전법(006 오류)  -(부를 것 없음)×2·(035 오류)×1
A  29   청주시 인구 알려줘            청주시(011 4)       청주(011 4)  청주시(011 4)       청주(011 4)
B  31   청주 국회의원 선거구          국회의원 선거구(0)   청주(4)      청주(4)             청주(4)
A v10   전기차 충전소 데이터 줘       전기차 충전소(120)   전기차 충전소(120)×2·-(500)×1  전기차 충전소(120)  전기차 충전소(120)×2·-(500)×1
A v11   전기차 충전소 알려줘          전기차 충전소(120)   -(500)      전기차 충전소(120)   -(500)
B v12   철도 안전 문서 보여줘         철도 안전 문서(6)×2·-(오류)×1  -(003 55)  철도 안전 문서(6)   -(003 55)
A v13   청주시 인구 알려줘            청주시(011 4)       청주(011 4)  청주시(011 4)       청주(011 4)
```

**「읽는 법」 넷의 집계**
```
A  P0 둘이 같고 P1 둘이 같다 — 차이는 P1 의 효과다        9/16
B  P0 둘이 갈린다 — 그 발화는 못 읽는다                   7/16  (15 · 16 · 17 · 26 · 27 · 31 · v12)
C  P0 둘은 같은데 P1 둘이 갈린다                          0/16
D(계획에 없던 칸) 넷 다 갈림                              0/16
```

**★ C 가 0 이다. P1 두 묶음은 16발화에서 인자가 한 글자도 안 달랐다(16/16 일치).**
P0 는 9/16 만 일치했다. **이 표에서는 P1 이 P0 보다 흔들림이 적다.** 다만 묶음이 둘씩뿐이라
「P1 이 안정시킨다」고 말할 근거는 아니고, 적어 두는 사실이다.

#### 이번 세션의 드리프트 폭

```
P0a 와 P0b 에서 인자 낱말이 갈린 발화   7/16   (44%)
「마흔셋째」의 P0 넷 사이                9/35   (26%, 네 묶음 중 어느 둘이라도 갈리면 셈)
```

분모가 다르니 두 수를 곧바로 견주지 않는다. 다만 **이번에 고른 16발화는 「마흔셋째」에서
갈렸던 자리를 일부러 모아 놓은 것**이라 폭이 커 보이는 것이 당연하다.
**드리프트가 사라지지 않았다는 것만 이 표로 말할 수 있다.**

갈린 일곱 중 **여섯(15 · 17 · 26 · 27 · 31 · v12)은 「마흔셋째」에서도 갈렸던 아홉 안에 있다.**
`16` 만 새로 갈렸다 — 「마흔셋째」에서 P0 넷이 모두 같았던 자리다.

#### ★ 16 · 29 · v11 의 결론

```
16  교통 공약 많은 선거구 검색해줘        [B] P0 둘이 갈렸다 — 「읽는 법」대로면 못 읽는다
    P0a '교통 공약 많은 선거구' 0건 ×3 / P0b '교통 공약' 0건 ×3
    P1a '교통' 20건 ×3          / P1b '교통' 20건 ×3
```
**★ 「마흔셋째」가 「유일하게 안 흔들린 표적」이라고 적은 자리가 이번에는 흔들렸다.**
그래서 미리 정한 「읽는 법」대로 **B 로 적는다.** 다만 갈린 방식이 이렇다 —
**P0 둘은 낱말이 다르지만 건수가 둘 다 0건이고 사람 값('교통')과 둘 다 다르다.**
P1 둘은 낱말도 건수도 사람 값과 같다. **건수로만 보면 P0 0건 ×6 · P1 20건 ×6 으로 갈린다.**
「인자 낱말」이라는 미리 정한 잣대로는 B 이고, 「사람 값과 같아졌는가」로는 P1 이 6/6 이다.
**두 읽기를 다 적는다. 어느 쪽으로 고를지는 정하지 않는다.**

```
29  청주시 인구 알려줘                    [A] 드리프트 없다. 차이는 P1 의 효과다
    P0a · P0b  '청주시' recipe_011 4건 ×3
    P1a · P1b  '청주'   recipe_011 4건 ×3
```
**★ P1 이 인자를 바꾸는 것은 확실하다(둘 다 3/3). 그런데 건수는 4건 그대로다.**
「마흔셋째」는 29 를 「P1 이 깨뜨린 자리 (4건 → 1(단건), recipe 011 → 039)」로 적었다.
**이번에 recipe 는 네 묶음 모두 011 이고 건수는 넷 다 4건이다. 깨짐이 재현되지 않았다.**
v13(같은 발화의 어미 변주)도 넷 다 011 · 4건으로 같다.
읽으면 — **「마흔셋째」의 29 손상은 인자가 아니라 recipe 가 039 로 튄 것이었고, 그 튐이
이번에는 안 일어났다.** recipe 가 왜 튀었는지는 안 쟀다.

```
v11 전기차 충전소 알려줘                  [A] 드리프트 없다. 차이는 P1 의 효과다
    P0a · P0b  '전기차 충전소'  120건 ×3
    P1a · P1b  인자 못 뽑음(-)  전국 500건(상한) ×3
```
**★ P1 둘 다에서 3/3 으로 깨졌다. 드리프트가 아니다. 「마흔셋째」의 기록이 이번에 확인됐다.**
0건보다 나쁘다 — 답처럼 보이는데 전국 상한이다. `v10`(같은 낱말 · 어미만 다름)은 P1 에서
2/3 만 '전기차 충전소' 를 지켰고 1/3 이 같은 모양으로 깨졌다. **어미가 「알려줘」일 때 더 크게
깨진다.** reason 은 이번에도 안 읽었으므로 왜인지는 모른다.

#### 표적 12 는 어떻게 됐나

```
12  청주시 행정경계 보여줘                [A] 인자는 네 묶음 모두 '청주시' 다. P1 의 효과가 없다
    P0a  recipe_004  4건        P1a · P0b · P1b  recipe_019  1(단건)
```
「마흔셋째」에서 드리프트에 삼켜졌던 자리다. **이번에는 인자가 안 흔들렸다** — 넷 다 '청주시'.
흔들린 것은 recipe 다(P0a 만 004). **이 발화의 문제는 인자가 아니라 recipe/축이라는 것이
이번 표로 갈렸다.** P1 은 여기에 아무 힘도 못 쓴다.

#### 덤으로 나온 것 — P1 이 고친 다른 자리 둘

**둘 다 B 라 「읽는 법」대로면 못 읽는다.** 적어만 둔다.
```
17  청주 국회의원 공약 검색해줘   P0 0건(둘 다 구절째) → P1 '청주' 16건 6/6
31  청주 국회의원 선거구 검색해줘 P0a '국회의원 선거구' 0건 · P0b '청주' 4건 → P1 '청주' 4건 6/6
```
17 은 P0 둘이 낱말은 갈렸어도 **둘 다 0건**이고 P1 둘이 **둘 다 16건**이라 16 과 같은 모양이다.
31 은 P0b 가 이미 고쳐져 있어(드리프트) 못 읽는다.

#### 「마흔셋째」에 붙이는 정정 — 그 기록은 한 줄도 안 지웠다

```
① 「16 이 유일하게 안 흔들린 표적」  → 이번 세션에서는 P0 둘이 갈렸다(0건인 것은 같다)
② 「P1 이 29 · v13 을 깨뜨렸다(4건 → 1건)」 → 이번에는 재현되지 않았다.
   네 묶음 모두 recipe_011 · 4건이다. 그 손상은 recipe 드리프트였을 가능성이 크다
③ 「P1 이 v11 을 깨뜨렸다(120건 → 500건)」 → 이번에 확인됐다. P1 둘 다 3/3
```

#### 안 한 것

- **아무것도 안 골랐다.** "P1 을 채택하자" 를 안 쓴다. 문안은 P0 로 되돌렸다.
- **판정을 안 쟀다.** 이 기록의 수는 전부 인자 표다.
- 발화를 35 에서 16 으로 줄였다. 뺀 19 개가 P1 에서 어떻게 됐는지는 이번에 안 쟀다.
- reason 을 안 읽었다. v11 이 왜 인자를 안 뽑게 되는지는 여전히 추정도 안 했다.
- 드리프트 원인을 또 안 찾았다. 「마흔셋째」에서 남긴 숙제 그대로다.
- 27 「철도안전법 내용 찾아줘」의 `web.search` 오류는 이번에도 안 팠다. 네 묶음 모두 오류다.
- P0 · P1 을 세 묶음씩으로 늘리지 않았다. 계획한 네 묶음만 돌렸다.

### 2026-08-28 (마흔셋째) · 인자를 구절째 뽑는 것을 프롬프트 한 줄로 고쳐 본다 — 고르지 않았다

무인 실행. 모델 qwen3:32b(서버 기본) · recipe 40 · 정답표 서른하나 · `--runs 3` · 같은 세션에서
연달아. **커밋 안 했다. 문안은 다 되돌렸다 — 끝난 뒤 `git status --short` 와 `git diff` 가 비어 있다.**
이 기록이 고른 문안은 없다. 표와 사실만 적는다.

```
고친 파일   workflows/static/prompts/recipe_selection.md  — 재는 동안만. 되돌렸다
안 건드림   온톨로지 · menu · 배선표 · 정답표(UTTERANCES) · resolve_service · execute_service ·
            좁히기 프롬프트 둘(recipe_axes.md · recipe_pick.md) · 기본값 끔 · vendor/ · KRRI_ASAP/
            서버는 프롬프트를 요청마다 파일에서 읽으므로 재시작 없이 갈아 끼웠다
```

**★ 한 번 끊겼다 다시 돌렸다.** 첫 시도가 P0a → P1 → P0b 까지 간 뒤 우분투 리부트로 끊겼고,
그때 `recipe_selection.md` 에 P2 가 남아 있던 것은 사람이 되돌렸다. **이 기록의 표는 리부트
뒤에 처음부터 다시 돌린 것이다.** 끊긴 묶음 셋은 `probe_out/arg.*-2026-08-28.pre-reboot.json`
에 남겨 뒀고 표에는 안 넣었다. 다시 돌릴 때 스크립트에 `trap ... EXIT INT TERM` 을 넣어
끊겨도 문안이 P0 로 돌아가게 했다 — 변형이 파일에 남는 자리를 막은 것이다.

#### 왜 쟀나

「서른다섯째」물음 3 에서 0건의 원인이 넷으로 갈렸고 그중 **「인자를 구절째 넣는다」**가
발화 12 · 15 · 16 이었다. recipe 는 맞게 고르는데 도구가 0건을 준다. 짚이는 곳이
`recipe_selection.md` 18~26줄에 있었다 — **장소 예시는 꼬리말을 떼라고 가르치는데
("근처" 를 뗀다) 키워드 예시는 구절을 통째로 넣으라고 가르친다.** 결이 반대다.
그리고 그 예시로 쓰인 발화 자체가 어떤 낱말을 뽑아도 안 되는 것이다 —
`searchDistricts` 의 `query` 는 선거구명("충북 청주서원")을 훑으므로 "국회의원" 도
"선거구" 도 안 걸린다. **짚이는 것이지 확인이 아니라 재서 갈랐다.**

#### 잰 순서와 변형

```
인자 표   P0a → P1 → P0b → P2 → P0c → P3 → P0d      7묶음 · 35발화 × 3회 (105회씩)
판정      P0 → P1 → P2 → P3                          4묶음 · 31발화 × 3회 (93회씩)
          ★ 판정 넷은 인자 표 일곱이 다 끝난 뒤에 돌렸다. 판정의 P0 는 한 묶음뿐이라
            이 세션의 판정 잡음 폭은 못 쟀다 — 「마흔두째」의 폭(빗나감 4 · 적중 5)을 빌려 쓴다

P0   지금 문안 그대로. 변형 사이마다 끼워 드리프트를 본다
P1   키워드 예시 한 줄만 동작하는 발화로 바꿈. 규칙은 그대로
P2   키워드 규칙만 다시 씀. 예시는 그대로
P3   P1 + P2
```

#### 변형마다 바꾼 곳과 전문

셋 다 `[축 고르는 법]` 의 **`말한 키워드` 한 줄만** 건드렸다. 나머지 줄은 P0 그대로다.
예시를 **늘리지 않았다** — 「마흔두째」에서 2차에 예시를 더했더니 「하나로 확신」이 늘어
발화 11 · 23 이 세 번 다 깨진 전례가 있어서다.

**P0 (지금 문안)**
```
    말한 키워드 무엇을 찾는지. "국회의원 선거구 찾아줘" -> "국회의원 선거구"
```
**P1** — 예시만 동작하는 발화로. `query="청주"` 로 4건이 나오는 것은 눌러서 확인한 값이다.
```
    말한 키워드 무엇을 찾는지. "청주 선거구 찾아줘" -> "청주"
```
**P2** — 규칙만. 예시는 P0 그대로 둔다.
```
    말한 키워드 찾을 대상의 이름만 쓴다. 하려는 일의 이름은 넣지 않는다.
                "국회의원 선거구 찾아줘" -> "국회의원 선거구"
```
**P3** — P1 + P2.
```
    말한 키워드 찾을 대상의 이름만 쓴다. 하려는 일의 이름은 넣지 않는다.
                "청주 선거구 찾아줘" -> "청주"
```

**도구 독립성** — 셋 다 **O**. 규칙 문장에 도구 이름도 도메인 이름도 안 들어갔다.
예시는 P0 때부터 특정 발화였고(오송역 · 충북 제1선거구) 변형이 그 성격을 안 바꿨다.
다만 **P2 는 규칙과 예시가 서로 어긋난 문안이다** — "하려는 일의 이름은 넣지 않는다" 라고
해 놓고 예시가 "국회의원 선거구 찾아줘" -> "국회의원 선거구" 다. 규칙만의 힘을 가르려고
일부러 그렇게 둔 것이고, 그 어긋남이 아래 4번 손상의 자리로 의심된다.

프롬프트 자수 (파일 그대로, 치환 전)
```
recipe_selection.md   P0 2138 · P1 2130 · P2 2179 · P3 2171
```

#### ★ P0 들이 같았는가 — 아니다. 드리프트가 인자 표에도 있다

**P0 넷이 인자 낱말에서 갈린 발화가 9/35 다.** P0c 와 P0d 는 서로 완전히 같았다(0개 차이).

```
        P0a   P0b   P0c   P0d      (인자 낱말이 다른 발화 수 · 35발화 기준)
  P0a     0     8     7     7
  P0b     8     0     5     5
  P0c     7     5     0     0
  P0d     7     5     0     0
```

갈린 아홉은 7 · 12 · 15 · 17 · 26 · 27 · 31 · v10 · v12 다. **P0a 가 유독 떨어져 있고
뒤로 갈수록 붙는다** — 「마흔두째」에서 N0a 만 앞선 측정과 같고 N0b 부터 다른 모양으로
굳었던 것과 같은 결이다. 원인은 이번에도 안 찾았다.

**이 드리프트가 이번 작업의 표적 둘을 삼켰다.**

```
15  청주 선거구 찾아줘     P0a  '청주 선거구' 0건  ← 「서른다섯째」와 같다
                          P0b · P0c · P0d  '청주' 4건   ★ 변형 없이 저절로 고쳐져 있다
12  청주시 행정경계 보여줘  P0a  '행정경계' 0건 ×2 (recipe_004)
                          P0b · P0c · P0d  '청주시' 1(단건) ×3 (recipe_019 — 기대값 004 가 아니다)
16  교통 공약 많은 선거구   P0 넷이 모두 '교통 공약 많은 선거구' 0건   ← 유일하게 안 흔들린 표적
```

**그래서 12 와 15 에서는 변형의 효과를 읽을 수 없다.** 대조군이 저 혼자 움직인 자리에서
변형이 좋아졌다고 말하면 거짓이 된다. **16 하나만 깨끗한 신호다.**

#### ★ 12 · 15 · 16 이 어떻게 움직였나

```
12 청주시 행정경계 보여줘        (사람추측 '청주시' → recipe_004 면 4건 · recipe_017 이면 1건)
   P0a  '행정경계'      004 0건 ×2 | '청주시' 017 1건 ×1
   P1   '청주시'        019 1(단건) ×3
   P0b  '청주시'        019 1(단건) ×3
   P2   '청주시'        019 1(단건) ×3
   P0c  '청주시'        019 1(단건) ×3
   P3   '청주'          004 4건 ×3      ★ 004 로 가서 4건. 판정도 이 묶음만 적중 3/3
   P0d  '청주시'        019 1(단건) ×3

15 청주 선거구 찾아줘            (사람추측 '청주' = 4건)
   P0a  '청주 선거구'   007 0건 ×3
   P1   '청주'          007 4건 ×3
   P0b  '청주'          007 4건 ×3      ← 변형이 없는데도 이미 고쳐져 있다
   P2   '청주'          007 4건 ×3
   P0c  '청주'          007 4건 ×3
   P3   '청주'          007 4건 ×3
   P0d  '청주'          007 4건 ×3

16 교통 공약 많은 선거구 검색해줘  (사람추측 '교통' = 20건)
   P0a  '교통 공약 많은 선거구' 008 0건 ×3
   P1   '교통'                 008 20건 ×3     ★ 3/3 사람 값과 같아졌다
   P0b  '교통 공약 많은 선거구' 008 0건 ×3
   P2   '교통 공약'            008 0건 ×3      구절이 짧아졌지만 여전히 0건
   P0c  '교통 공약 많은 선거구' 008 0건 ×3
   P3   '교통' ×2 | '교통 공약' ×1              20건 ×2 · 0건 ×1
   P0d  '교통 공약 많은 선거구' 008 0건 ×3
```

**16 의 recipe 는 네 묶음 모두 008 로 맞다.** 판정도 넷 다 적중 3/3 이다. 이 발화에서
0건은 오로지 인자 탓이고, **P1 이 그것을 고친다.** P2 는 구절을 "교통 공약" 까지만
줄이고 멈춘다 — 규칙만으로는 마지막 한 낱말을 못 떼어 낸다.

#### 변형이 새로 깨뜨린 자리 — P0 넷이 모두 같았던 26발화에서만 읽는다

P0 넷이 갈린 9발화를 빼고 **안정된 26발화**에서만 변형과 P0 를 견줬다. 그 밖의 차이는
드리프트와 안 갈려서 안 읽는다.

```
P1 — 안정 자리에서 달라진 것 5
  16  '교통 공약 많은 선거구' → '교통'          0건 → 20건   ★ 고쳐진 자리
  19  '인구 많은 시군구 순위' → '인구 많은 시군구'  0건 → 0건    짧아졌으나 얻은 것 없음
  29  '청주시' → '청주'                        4건 → 1(단건)  ★ 잃은 자리 (recipe 도 011 → 039)
 v13  '청주시' → '청주'                        4건 → 1(단건)  ★ 같은 자리
 v11  '전기차 충전소' → 없음 ×2                120건 → 500건(전국 상한) ★ 잃은 자리

P2 — 안정 자리에서 달라진 것 4
   4  '청주시' → '인구 구성'                    1건 → 0건    ★★ 고치려던 실패 모양이 새 발화로 옮겨왔다
   6  '국회의원 선거구' → 없음 ×1               0건 → 0건    (6 은 어떤 인자로도 0건인 자리다)
  16  '교통 공약 많은 선거구' → '교통 공약'       0건 → 0건    안 고쳐짐
 v11  '전기차 충전소' → 없음 ×2                120건 → 500건 ★ 잃은 자리

P3 — 안정 자리에서 달라진 것 4
   6  '국회의원 선거구' → '국회의원' ×2 · 없음 ×1  0건 → 0건
  16  '교통 공약 많은 선거구' → '교통' ×2 · '교통 공약' ×1   0건 → 20건 ×2   ★ 2/3 고쳐짐
  19  '인구 많은 시군구 순위' → '인구 많은 시군구'  0건 → 0건
 v11  '전기차 충전소' → 없음 ×3                120건 → 500건 ★ 셋 중 제일 크게 잃은 자리
```

**★ 셋이 공통으로 깨뜨린 자리는 v11 「전기차 충전소 알려줘」 하나다.** 인자를 아예 못 뽑고
(`argument = null`) 그대로 `ev.searchStations` 를 부르면 0건이 아니라 **전국 500건(상한)**
이 온다. P0 넷은 모두 '전기차 충전소' 120건이었다. 「대상의 이름만 · 짧게」쪽으로 민 문안이
**뽑을 것이 없다고 판단하는 쪽으로도 함께 민다**는 것으로 읽히지만, reason 을 안 읽었으므로
이것은 추정이다.

**★ P2 의 4번이 제일 눈에 띈다.** "청주시 인구 구성 알려줘" 가 '청주시' 에서 **'인구 구성'**
으로 갔다. 규칙은 "하려는 일의 이름은 넣지 않는다" 인데 예시는 구절째 넣는 것을 그대로
보여 주고 있었다. **고치려던 실패 모양이 다른 발화에서 새로 생긴 것이다.**

#### 축 셋이 바뀌었는가

인자를 건드린 문안이라 축이 흔들리는지도 봤다. **안정 자리에서 변형이 축을 바꾼 것은
12 · 15 · 16 중 12 하나뿐이다** — P3 에서 `given` 이 `spoken_place` 에서 `spoken_keyword`
로 돌아갔고(P0b · P0c · P0d 는 `spoken_place`), 그래서 recipe 가 019 가 아니라 004 가 됐다.
**P3 의 12번 개선은 인자가 아니라 축이 바뀐 결과다.** 나머지 축 변화는 P0 끼리도 갈리는
자리라 드리프트와 안 갈린다.

#### 판정 네 칸 (변형마다 한 묶음 · 93회)

```
        LLM 단독   적중    근접  빗나감  못 붙음      기준선 아홉(27회)        확장(66회)
  P0    52/93    59/93    27     7      0      17/27 · 10 · 0 · 0    42/66 · 17 · 7 · 0
  P1    53/93    67/93    20     6      0      22/27 ·  5 · 0 · 0    45/66 · 15 · 6 · 0
  P2    58/93    57/93    25    11      0      22/27 ·  2 · 3 · 0    35/66 · 23 · 8 · 0
  P3    55/93    62/93    23     6      2      21/27 ·  3 · 3 · 0    41/66 · 20 · 3 · 2
```

**「마흔두째」의 잡음 폭(빗나감 4 · 적중 5)에 비추면** — P1 의 적중 +8 만 폭 밖이다.
P2 의 빗나감 +4 는 폭의 경계라 폭 밖이라고 말하지 않는다. P3 의 **못 붙음 2 는 새로 생긴
것이다**(27번 「철도안전법 내용 찾아줘」). 못 붙음은 「마흔두째」에서도 모든 묶음이 0 이었다.
**이 세션의 판정 P0 는 한 묶음뿐이라 판정의 드리프트를 직접 못 쟀다.** 인자 표의 P0 넷이
9/35 로 갈린 것을 보면 판정의 실제 폭도 빌려 쓴 폭보다 클 수 있다.

판정에서 P0 보다 빗나감이 는 자리
```
P2   4 청주시 인구 구성   근접 3 → 빗나감 3      29 청주시 인구  적중 3 → 근접 1 · 빗나감 2
     11 · 14 · 18 은 적중 3 → 근접 3 (빗나감은 안 늘었다)
P3   4 청주시 인구 구성   근접 3 → 빗나감 3      17 청주 국회의원 공약  적중 3 → 적중 2 · 빗나감 1
     30 오송역 선거구     근접 2 · 빗나감 1 → 근접 1 · 빗나감 2
     27 철도안전법 내용   빗나감 3 → 근접 1 · 못 붙음 2
P1   빗나감이 는 자리 없음. 18 오송역 일대 인구가 적중 3 → 적중 1 · 근접 2
     7 · 9 · 31 은 근접 3 → 적중 3 으로 올라갔다
```

#### 리부트를 건너 같은 문안이 같은 인자를 냈다 — 덤으로 얻은 사실

끊긴 첫 시도의 P0a 와 다시 돌린 P0a 는 **35발화의 인자가 한 글자도 안 다르다.** 축은 한
칸만 움직였다(1번 `about` 이 `-` → `group_transport`). 리부트가 사이에 끼어 있고 세션도
다른데 그렇다. **「마흔두째」가 「다른 세션에서 다시 재지 않았다」고 남긴 자리에 값이 하나
생긴 셈이다** — 다만 **묶음 하나끼리의 비교**이고, 같은 세션 안에서 P0 넷이 9/35 로 갈린
것과 나란히 놓으면 **「세션이 갈라서 흔들린다」기보다 「한 세션 안에서 앞뒤 자리 때문에
흔들린다」** 쪽으로 읽힌다. 표 하나로 결론 낼 것은 아니다.

#### 고르는 기준 넷에 비춘 정리 — 고르지 않는다

```
1순위  12 · 15 · 16 의 인자가 사람 값과 같아졌는가
       16  P1 이 3/3 (0건 → 20건) · P3 가 2/3 · P2 는 못 고침(구절만 짧아지고 0건)
       15  ★ 못 읽는다. P0b 부터 변형 없이 이미 '청주' 4건이다 (드리프트)
       12  ★ 못 읽는다. P0 넷이 갈린 자리다. P3 만 004 · '청주' 4건인데
           그것은 인자가 아니라 축(given)이 바뀐 결과다
2순위  다른 발화의 인자가 안 깨졌는가   ★ 셋 다 깨진 자리가 있다
       P1  v11(120건 → 전국 500건) · 29 · v13(4건 → 1건)
       P2  4 (★ 고치려던 실패 모양이 새로 생김) · v11
       P3  v11 (3/3 로 제일 크게) · 6
3순위  판정이 잡음 폭 밖으로 나빠지지 않았는가
       P1  안 나빠졌다. 적중 +8 로 폭 밖으로 좋아졌다
       P2  빗나감 +4 (폭의 경계) · 적중 −2 (폭 안)
       P3  빗나감 −1 · 적중 +3 (둘 다 폭 안) · 못 붙음 2 는 새로 생긴 것
4순위  도구 독립성                     셋 다 O
```

#### 안 한 것

- **아무것도 안 골랐다.** 문안은 P0 로 되돌렸다.
- reason 을 안 읽었다. v11 이 왜 인자를 안 뽑게 됐는지는 추정이다.
- 드리프트 원인을 안 찾았다. P0a 만 떨어져 있고 P0c · P0d 가 붙는 이유를 안 쟀다.
- **판정의 P0 를 한 묶음만 돌렸다.** 판정 잡음 폭은 「마흔두째」것을 빌려 썼다.
- 변형을 더 안 만들었다. 계획한 넷만 돌렸다.
- 발화 6 · 19 · 28 은 이번 문안으로 안 고쳐지는 자리다 — 6 · 19 는 발화에 통하는 낱말이
  없고(「서른다섯째」), 28 은 배선 칸 탓이다(`stationName` vs `railwayName`).

### 2026-08-27 (마흔두째) · 좁히기 길의 프롬프트 문안 여럿을 번갈아 쟀다 — 고르지 않았다

무인 실행. 모델 qwen3:32b(서버 기본) · recipe 40 · 정답표 서른하나 · `--runs 3` · 같은 세션에서
연달아. **커밋 안 했다. 기본값은 끔 그대로다. 문안은 다 되돌렸다 — 끝난 뒤 `git status --short`
와 `git diff --stat` 이 비어 있다.** 이 기록이 고른 문안은 없다. 표와 사실만 적는다.

```
고친 파일   workflows/static/prompts/recipe_pick.md  (2차)  — 재는 동안만. 되돌렸다
            workflows/static/prompts/recipe_axes.md  (1차)  — 재는 동안만. 되돌렸다
안 건드림   recipe_selection.md · resolve_service · 온톨로지 · menu · 배선표 · 정답표(UTTERANCES) ·
            vendor/ · KRRI_ASAP/ · 기본값. 서버는 프롬프트를 요청마다 파일에서 읽으므로 재시작 없이 갈아 끼웠다
```

#### 왜 쟀나

「마흔한째」에서 좁히기 길이 졌다 (빗나감 6 → 9). 나빠진 자리(16 · 19 · 21)가 전부 1차 · 2차
프롬프트 안에 있고, 그 두 파일은 지금 길(끔)이 안 쓰므로 고쳐도 끔의 판정이 안 흔들린다.
2차 문안은 40문장 시절 것을 거의 그대로 옮긴 것이라 짚이는 데가 둘이었다 — NO_MATCH 예시가
"날씨 알려줘"(아예 무관) 하나뿐인 것, "똑같은 Recipe 를 모두 넣는다" 가 후보 몇 개짜리에서
어떻게 읽히는지 안 따져 본 것. 1차는 menu 가 없어져 want 를 정할 단서가 준 것(19).
**짚이는 것이지 확인이 아니라 재서 갈랐다.**

#### 잰 순서와 변형

```
F → N0a → N1 → N0b → N2 → N0c → N3 → N0d → A1 → N0e → N4 → N0f      12묶음 · 93회씩

F    끔 (지금 길). 비교 기준
N0   지금 문안 그대로 켬. 변형 사이마다 끼워 드리프트를 본다
N1   2차 — 「없다」를 미묘한 경우로 가르치는 예시 하나 더함 (기존 "날씨" 예시는 그대로)
N2   2차 — 고르는 규칙을 좁힌 상황에 맞게 다시 씀 (후보가 축으로 걸러진 것임을 알려 줌)
N3   2차 — N1 + N2
A1   1차 — want 를 정할 단서 더함
N4   N1 의 2차 + A1 에서 item_detail 줄 하나를 뺀 1차   (고른 까닭은 아래)
```

**★ 잰 도중 실수 하나.** A1 이 끝난 뒤 N4 를 띄우면서 N0e 가 아직 도는 것을 놓쳐 33초 동안
두 측정이 겹쳤고 그 사이 파일이 N4 로 바뀌었다. 겹친 둘(N0e · N4 첫 시도)은 바로 죽여
버리고 파일을 N0 로 되돌린 뒤 N0e → N4 → N0f 를 처음부터 순차로 다시 돌렸다. 표의 N0e ·
N4 · N0f 는 다시 돌린 것이다. 버린 출력은 표에 안 넣었다.

#### 변형마다 바꾼 곳과 전문

**N1 (recipe_pick.md)** — [예시] 끝, "날씨 알려줘" 예시 뒤에 아래를 더했다. 나머지는 N0 그대로.
```
후보에 아래 세 Recipe 가 있다고 하자.
  C : 말한 장소로 좌표를 찾고 근처 충전소 목록을 조회한다.
  D : 말한 장소로 좌표를 찾고 근처 충전소 한 곳의 상세를 조회한다.
  E : 말한 장소로 좌표를 찾고 충전소를 운영기관 이름으로 검색한다.

발화: "오송역 근처 주차장 찾아줘"
→ 요청은 "주차장 찾기" 에서 끝난다.
→ C · D · E 는 셋 다 "장소 → 좌표" 로 시작해 요청과 비슷해 보인다.
  그러나 셋이 돌려주는 것은 전부 충전소다. 주차장을 돌려주는 것이 없다.
→ 비슷한 것 가운데 제일 가까운 것을 대신 고르지 않는다. status = "NO_MATCH".
```
(예시 소재를 선거 · 인구가 아니라 충전소로 잡은 것은 16 · 19 의 후보와 겹쳐 그쪽만 끌지
않게 하려던 것이다.)

**N2 (recipe_pick.md)** — [규칙] 의 아래 세 줄을
```
- 각 Recipe 의 문장을 사용자 요청과 비교한다.
- 하는 일이 사용자 요청과 똑같은 Recipe 를 모두 candidate_recipe_ids 에 넣는다.
  요청보다 더 많이 하거나 덜 하는 Recipe 는 넣지 않는다.
```
이것으로 바꿨다. 나머지는 N0 그대로.
```
- 후보는 온톨로지가 축 셋(무엇에서 시작하는가 · 무엇을 돌려받는가 · 무엇에
  관한 것인가)으로 이미 거른 것이다. 그래서 후보끼리는 시작과 주제가 비슷하다.
  후보가 서로 갈리는 자리는 「무엇을 돌려주는가」와 「어디까지 하는가」다.
- 후보마다 그 둘을 사용자 요청과 맞춰 본다. 요청이 돌려받으려는 것을 그대로
  돌려주고 요청이 끝나는 자리에서 끝나는 Recipe 가 맞는 것이다.
- 맞는 것이 하나면 그것 하나만 candidate_recipe_ids 에 넣는다. 둘 이상이
  요청과 똑같이 맞으면 모두 넣는다. 주제만 같고 돌려주는 것이 다른 Recipe 는
  넣지 않는다. 요청보다 더 많이 하거나 덜 하는 Recipe 도 넣지 않는다.
```

**N3 (recipe_pick.md)** — N2 의 규칙 + N1 의 예시. 두 변경을 그대로 합친 것이라 전문은 둘을 겹쳐 읽으면 된다.

**A1 (recipe_axes.md)** — [축 고르는 법] 의 `"국회의원 선거구 찾아줘" 에는 … -> want = null` 줄 바로 뒤에
아래를 더했다. 나머지는 N0 그대로. 축 선택지 설명(온톨로지)은 안 건드렸다.
```
- want 는 사용자가 마지막에 손에 쥐려는 것의 꼴이다. 찾는 대상의 이름이
  아니라 그것을 어떤 꼴로 받으려 하는가를 본다 — 여러 항목의 목록인가,
  집계한 수치인가, 한 항목의 자세한 내용인가, 좌표인가, 경계 도형인가.
    "얼마야" "몇 명" "순위" "비율" "변화" 처럼 수치나 집계를 묻는다 -> statistics
    "자세히" "상세" 처럼 한 건의 내용을 묻는다 -> item_detail
    대상만 말하고 어떤 꼴로 받을지 말하지 않았다 -> null
```

**N4** — 2차는 N1 그대로. 1차는 A1 에서 `"자세히" "상세" 처럼 한 건의 내용을 묻는다 -> item_detail`
한 줄만 뺐다. **어떻게 골랐나.** A1 에서 19 를 고친 것은 statistics 단서였고(1차가 want=statistics 로
써서 후보가 011 하나가 됐다), A1 에서 새로 깨진 4 · 26 · 27 은 셋 다 1차가 "내용" 을 item_detail 로
쓴 자리였다. 2차 쪽은 N1 이 16 을 빗나감에서 근접으로 옮기고 손상이 셋 중 제일 작았다(N2 · N3 는
빗나감이 더 늘었다). 그래서 「N1 + item_detail 줄 뺀 A1」이다. 이것도 고른 것이 아니라 재 본 조합이다.

프롬프트 자수 (파일 그대로, 치환 전)
```
recipe_pick.md   N0 1304 · N1 1641 · N2 1544 · N3 1881 · N4 1641
recipe_axes.md   N0  882 · A1 1157 · N4 1146
```

#### 변형 × 묶음 표

적중 · 근접 · 빗나감 · 못 붙음(전부 0) · 조회 빠짐을 기준선 아홉(27회)과 확장 스물둘(66회)로 갈랐다.
2차 안 부름 · 폴백 가 · 폴백 나는 93회 합. s 는 /resolve 평균, 걸린 시간은 93회 한 묶음.
```
          적중  근접 빗나감  못붙 |    아홉  근  빗  빠 |   스물둘  근  빗  빠 | 2차안부름 폴백가 폴백나  s   걸린시간
F      62/93  25   6   0 | 19/27  8  0  0 | 43/66 17  6  9 |      -      -      -     -   5.1분   16:적중3  19:적중3  21:적중3
N0a    61/93  23   9   0 | 21/27  6  0  0 | 40/66 17  9 12 |     17     10      3   3.6   5.6분   16:적중0·빗나감3 {010}SELECT  19:적중1·빗나감2 {013}SELECT  21:적중3
N1     62/93  19  12   0 | 21/27  6  0  0 | 41/66 13 12 12 |     17     10      3   3.8   5.9분   16:적중0·근접3 {008, 009}CLARIFY  19:적중1·빗나감2 {006, 013}CLARIFY  21:적중3
N0b    59/93  21  13   0 | 21/27  4  2  0 | 38/66 17 11 14 |     17     10      1   3.5   5.4분   16:적중0·빗나감3 {010}SELECT  19:적중2·빗나감1 {013}SELECT  21:적중0·빗나감3 {004, 005}CLARIFY
N2     63/93  15  15   0 | 21/27  3  3  0 | 42/66 12 12 12 |     17     10      3   3.7   5.8분   16:적중0·빗나감3 {010}SELECT  19:적중1·빗나감2 {013}SELECT  21:적중3
N0c    56/93  26  11   0 | 18/27  9  0  0 | 38/66 17 11 14 |     17     10      1   3.5   5.5분   16:적중0·빗나감3 {010}SELECT  19:적중2·빗나감1 {013}SELECT  21:적중0·빗나감3 {004, 005}CLARIFY
N3     64/93  12  17   0 | 21/27  3  3  0 | 43/66  9 14 10 |     17     10      5   4.0   6.1분   16:적중0·빗나감3 {009}SELECT  19:적중3  21:적중3
N0d    59/93  23  11   0 | 21/27  6  0  0 | 38/66 17 11 14 |     17     10      1   3.6   5.5분   16:적중0·빗나감3 {010}SELECT  19:적중2·빗나감1 {013}SELECT  21:적중0·빗나감3 {004, 005}CLARIFY
A1     53/93  18  22   0 | 20/27  4  3  3 | 33/66 14 19 19 |     27     11      0   3.3   5.1분   16:적중0·빗나감3 {010}SELECT  19:적중3  21:적중0·빗나감3 {004, 005}CLARIFY
N0e    59/93  21  13   0 | 21/27  4  2  0 | 38/66 17 11 14 |     17     10      1   3.6   5.6분   16:적중0·빗나감3 {010}SELECT  19:적중2·빗나감1 {013}SELECT  21:적중0·빗나감3 {004, 005}CLARIFY
N4     58/93  13  22   0 | 21/27  3  3  3 | 37/66 10 19 15 |     28      3      3   3.5   5.5분   16:적중0·근접3 {008, 009}CLARIFY  19:적중3  21:적중3
N0f    59/93  21  13   0 | 21/27  4  2  0 | 38/66 17 11 14 |     17     10      1   3.6   5.5분   16:적중0·빗나감3 {010}SELECT  19:적중2·빗나감1 {013}SELECT  21:적중0·빗나감3 {004, 005}CLARIFY
```
(F 의 16 · 19 · 21 은 셋 다 적중 3. F 는 「마흔한째」의 끔과 글자까지 같다 — 62/93 · 빗나감 6 · 조회 빠짐 9.
지금 길 무손상 관문 통과.)

#### ★ N0 들이 같았는가 — 아니다. 드리프트가 있다

```
N0a  61/93  빗나감 9    ← 「마흔한째」의 켬과 같다
N0b  59/93  빗나감 13
N0c  56/93  빗나감 11
N0d  59/93  빗나감 11
N0e  59/93  빗나감 13
N0f  59/93  빗나감 13
```
**같은 문안 · 같은 세션인데 빗나감이 9 ~ 13 으로 움직였다.** 「마흔한째」에서 켬 둘이 발화 하나만
갈렸던 것과 다르다. 움직인 자리는 셋이다.
- **21 경부선 선형 데이터 줘** — N0a 는 1차가 want=map_extent 로 써서 후보 {002, ?} → 2차 "없다" → 폴백 나
  → 지금 길이 002 를 골라 적중 3. **N0b 부터는 1차가 want=boundary_shape 로 쓰고** 후보가 {004, 005} 가
  되어 2차가 둘 다 넣어 CLARIFY → 빗나감 3. 그 뒤 N0 다섯이 전부 이 모양이다. 이것 하나로 빗나감 +3.
  폴백 나가 3 → 1 로 준 것도 이것 때문이다(없다고 말할 자리가 없어졌다).
- **3 오송역 CCTV 보여줘** — 근접 3 ↔ 빗나감 2({033} SELECT) 를 오갔다 (N0b · N0e · N0f).
- **6 국회의원 선거구 찾아줘** — N0c 에서만 근접 3 ({007, 008, 009}). 나머지는 적중.
- 19 는 빗나감 2 ↔ 1 을 오갔다.

Ollama 를 부르는 것은 우리 서버 하나였다 (`ss -tnp` 로 봤다). 겹쳐 돌린 것도 없다(위의 실수 33초는
버린 묶음이다). temperature 0 · seed 0 인데도 같은 세션 안에서 이만큼 움직인다 — 원인은 안 찾았다.

**잡음 폭.** 빗나감 기준 N0 최소 9 · 최대 13 → 폭 4. 적중 56 ~ 61 → 폭 5. **이 폭보다 작은 효과는 읽지
않는다.** 변형은 앞뒤 N0 와 견준다.

#### 발화 16 · 19 · 21 이 어떻게 움직였나 (이 셋을 고치려고 한 것이다)

```
       16 교통 공약 많은 선거구 (정답 008)     19 인구 많은 시군구 순위 (정답 011)     21 경부선 선형 데이터 (정답 002)
F      적중 3                                  적중 3                                  적중 3
N0a    빗나감 3  SELECT {010}                  빗나감 2  SELECT {013} (want=item_list)  적중 3   (폴백 나 3회 — 없다 → 지금 길)
N1     근접 3    CLARIFY {008, 009}            빗나감 2  (1회는 CLARIFY {006, 013})       적중 3   (폴백 나 3회)
N0b    빗나감 3  {010}                          빗나감 1                                 빗나감 3  CLARIFY {004, 005} (want=boundary_shape)
N2     빗나감 3  {010}                          빗나감 2                                 적중 3   (폴백 나 3회)
N0c    빗나감 3  {010}                          빗나감 1                                 빗나감 3  {004, 005}
N3     빗나감 3  SELECT {009}                   ★ 적중 3  (2회는 2차 "없다" → 폴백 나 → 011)  적중 3   (폴백 나 3회)
N0d    빗나감 3  {010}                          빗나감 1                                 빗나감 3  {004, 005}
A1     빗나감 3  {010}                          ★ 적중 3  (want=statistics · 후보 011 하나)    빗나감 3  {004, 005}
N0e    빗나감 3  {010}                          빗나감 1                                 빗나감 3  {004, 005}
N4     근접 3    CLARIFY {008, 009}            ★ 적중 3  (후보 011 하나 · 2차 없음)          적중 3   (want=map_extent · 폴백 나 3회)
N0f    빗나감 3  {010}                          빗나감 1                                 빗나감 3  {004, 005}
```
- **16** — 2차 문안으로 움직인다. N1 · N4(예시 더함)에서 010 을 버리고 {008, 009} 되묻기가 됐다.
  적중은 못 됐다 — 008 과 009 를 안 가른다. N2 · N3(규칙 다시 씀)는 그대로 확신했다 (N3 는 009 로).
- **19** — 1차 문안으로 고쳐진다 (A1 · N4: want=statistics → 후보 011 하나). N3 에서는 다른 길로
  적중이 됐다 — 정답이 후보 밖인데 2차가 "없다" 고 해서 폴백 나가 지금 길로 보냈다. 이것이 노린
  「미묘한 없다」인데 N3 에서만 나왔고 N1 에서는 안 나왔다.
- **21** — 변형보다 드리프트가 크다. 1차 want 가 map_extent 냐 boundary_shape 냐로 갈리고
  2차 문안은 관여가 없다. N4 에서 적중인 것은 want 가 map_extent 로 돌아온 것이지 문안 효과로 못 읽는다.

#### 변형이 새로 깨뜨린 자리 (앞뒤 N0 에서 안 틀리던 것)

```
N1   11 오송역 행정경계     근접 → 빗나감 3 SELECT {019}     23 경부선 주변 CCTV  적중 → 빗나감 3 SELECT {034}
     12 청주시 행정경계     CLARIFY 빗나감 → SELECT {017} 빗나감 (모양만 바뀜)
N2   3 오송역 CCTV          근접 → 빗나감 3 SELECT {021}     23  적중 → 빗나감 3 {034}
N3   3 → 빗나감 3 {033} · 11 → 빗나감 3 {019} · 13 오송역 국회의원 → 빗나감 2 {022} · 22 → 빗나감 2 {021, 034}
A1   4 청주시 인구 구성 → 빗나감 2 {035} · 26 · 27 문서 찾기 → 빗나감 3 {035} · 29 청주시 인구 → 빗나감 3 {029}
     (4 · 26 · 27 은 1차가 "내용" 을 want=item_detail 로 썼다. 29 는 given=spoken_place 가 됐다)
     조회 빠짐 9 → 22. 후보 하나라 2차 안 부름 17 → 27 — 축이 더 좁게 나오고 그만큼 더 자주 빠졌다
N4   8 철도 안전 문서 → 빗나감 3 {006} · 11 · 23 · 27 → 빗나감 3 · 12 · 14 → 빗나감 2 · 29 → 빗나감 3 CLARIFY {029, 038, 039}
     폴백 가 10 → 3 (29 가 폴백 대신 후보 셋을 받았다) · 2차 안 부름 28
```
**2차 예시를 더하면(N1 · N3 · N4) 후보 여럿에서 하나로 확신하는 자리가 늘었다** — 11 · 23 이 세 번 다
같은 모양으로 깨졌다. "없다" 를 가르치려던 예시가 「하나만 남겨라」로도 읽힌 것으로 보이지만,
reason 을 따로 안 읽었으므로 이것은 추정이다.

#### 고르는 기준 넷에 비춘 정리 — 고르지 않는다

```
1순위  빗나감이 F(6)보다 크지 않은가      ★ 없다. 전부 F 위다. N1 12 · N2 15 · N3 17 · A1 22 · N4 22 (N0 는 9~13)
                                          잡음 폭(4)을 넘어 나빠진 것: N2 · N3 · A1 · N4. N1 은 폭 안(앞뒤 N0 9 · 13 사이)
2순위  16 · 19 · 21 이 고쳐졌는가         셋 다 적중은 N4 뿐인데(16 은 근접) 빗나감이 22 다.
                                          19 만은 A1 · N3 · N4 에서 고쳐졌다. 16 은 어느 변형도 적중을 못 만들었다
3순위  적중이 F(62)보다 많은가             N2 63 · N3 64. 둘 다 잡음 폭(5) 안이고 빗나감이 그만큼 늘어 얻은 것이다
4순위  폴백이 줄었는가                     N4 만 폴백 가 10 → 3. 대신 그 자리(29)가 빗나감이 됐다
```

#### 안 한 것

- 2차 reason 을 안 읽었다. 11 · 23 이 왜 하나로 좁혀 확신했는지는 추정이다.
- 드리프트 원인을 안 찾았다 (21 의 want 가 N0a 뒤로 바뀐 것). Ollama 쪽 KV · 프리픽스 캐시가
  의심 가는 자리인데 잰 것이 없다.
- 변형을 안 줄였다. 계획한 12묶음을 다 돌렸다 (겹친 둘은 버리고 다시).
- 다른 세션에서 다시 재지 않았다. 세션 안 폭이 이미 4 라 세션 간 폭은 더 클 것이다.

#### 좁히기를 스위치 뒤에 둔 채로 접는다 — 사람이 정했다

**이 절의 판단은 사람이 한 것이다.** 위의 표와 사실은 무인 실행이 냈고, 아래 결론은
그것을 보고 사람이 정했다.

```
문안 여섯이 전부 F(끔, 빗나감 6)보다 나빴다. 1순위에서 전멸이다.
좁히기를 살릴 근거가 없다.

지우지도 않는다. 기본이 끔이라 지금 동작에 영향이 0 이고,
길과 성적표가 남아 있으면 나중에 다시 볼 수 있다.

다시 열 조건
  16 이 008 과 009 를 가르게 되면 — 지금은 어떤 문안으로도 안 갈린다.
     문안이 아니라 두 노드의 description 문제로 보인다
  측정 잡음이 줄면 — 지금 폭이 빗나감 4 · 적중 5 라 미세한 차이를 못 읽는다
  1차의 축 정확도가 오르면 — 조회 빠짐이 줄어야 좁히기의 전제가 선다
```

**19 번에서 건진 것.** 1차에 「통계」 단서를 주면 고쳐진다 — A1 · N4 에서 want 가
item_list 대신 statistics 로 나와 후보가 011 하나가 됐고 적중 3 이 됐다. 다만 A1 은
그 대가로 4 · 26 · 27 을 깨뜨렸다(조회 빠짐 9 → 22). 「내용」을 item_detail 로 쓰게 만든
줄이 그 자리다. **단서를 주는 방향 자체는 값이 있고, 어떻게 주느냐가 문제다.** 이 값은
좁히기를 접어도 남는다 — 1차 프롬프트가 아니라 지금 길의 축 고르기에도 같은 문제가 있다.

### 2026-08-27 (마흔한째) · LLM 이 고를 범위를 온톨로지가 먼저 줄인다 — 두 길을 함께 두고 번갈아 쟀다

무인 실행. 모델 qwen3:32b(서버 기본) · recipe 40 · 정답표 스물여덟(31번까지) · `--runs 3`.
**커밋 안 했다. 기본값은 끔(지금 길)이다. 켜는 것이 낫다고 쓰지 않는다 — 표와 사실만
적고 사람이 정한다.**

```
demo/api/services/resolve_service.py      resolve → 스위치. 지금 길은 _resolve_full 로 이름만
                                          옮기고 안을 안 건드렸다. 좁히기 길 _resolve_narrow 새로
orchestrator/schemas/response_schema.py   axis_selection_schema(1차) · recipe_pick_schema(2차) 더함.
                                          recipe_selection_schema 는 그대로
workflows/static/prompts/recipe_axes.md   새 파일. 1차 — 지금 프롬프트에서 menu 를 뺀 것
workflows/static/prompts/recipe_pick.md   새 파일. 2차 — 좁힌 후보 중 고르기
tools/check_resolve.py                    --narrow 스위치 · 좁히기 표 · 시간 칸. 정답표 · 판정 규칙 0 변경
tests/demo/api/test_narrow.py             새 파일 25개
tests/orchestrator/test_narrow_schema.py  새 파일 4개
demo/api/main.py                          ★ 범위 밖 파일. /resolve 에 narrow query 한 칸 (아래)
```

**온톨로지 · menu · recipe · 배선표 · execute_service · vendor/ · KRRI_ASAP/ 변경 0.**
`git diff --stat` 로 확인했다.

**범위를 하나 넘었다.** 「/resolve 에 켤지 끌지를 받는다」는 `demo/api/main.py` 의
endpoint 를 고쳐야 되는 일인데 그 파일은 고칠 범위 목록에 없었다. query 인자 하나
(`narrow: bool | None = None`)와 그것을 넘기는 한 줄만 더했다. 안 주면 None 이고
None 이면 서비스가 환경변수를 보고, 그것도 없으면 끔이다.

#### 왜 했나

지금은 LLM 에게 menu 40문장을 통째로 주고 고르게 한다. 프롬프트가 조금만 바뀌어도
무관한 발화가 흔들리는 것이 일곱 번 관측됐고(「열한째」·「열여덟째」·「스물한째」·
「스물일곱째」·「서른째」·…) 도구가 늘면 그대로 무너진다. 순서를 바꾼다 —
LLM 이 축 셋만 쓰고, 온톨로지가 2~5개로 좁히고, LLM 이 그중에서 고른다.
전제(축 조합당 후보 평균 2.3 · 최대 7)는 「서른다섯째」가 쟀고, 조회 빠짐 13/66 이
있어 빠져나갈 길이 필요했다. **지금 길을 지우지 않고 두 길을 함께 뒀다.**

#### 만든 것 — 두 번 부르는 길

```
1차  recipe_axes.md     발화만 보고 축 셋(given · want · about)과 argument 를 쓴다. menu 없음
     ↓ shortlist.candidates(축 셋)  온톨로지가 좁힌다. 축이 셋 다 null 이면 조회 안 함 (지금 길과 같은 이유)
     후보 0개  → 폴백 가  지금 길(_resolve_full) 을 그대로 부른다. LLM 2회
     후보 1개  → 2차를 안 부른다. 그것으로 SELECT. LLM 1회
     후보 2개+ → 2차
2차  recipe_pick.md     후보의 menu 문장만 보여주고 고르게 한다. recipe_id 와 candidate_recipe_ids 가
                        후보의 닫힌 목록(enum)이라 밖의 id 를 문법으로 못 쓴다
     NO_MATCH("여기 없다") 거나 남는 것이 없으면 → 폴백 나  지금 길을 부른다. LLM 3회
     하나면 SELECT · 여럿이면 CLARIFY (차례는 조회 후보 = 파일 이름 순)
```

**검산(_verdict)을 안 거친다.** 대조할 두 목록이 없다 — 후보가 곧 조회 결과다.

응답 key 는 지금 길과 같고 켰을 때만 `narrow` 한 칸이 더 실린다.
```
narrow.enabled · shortlist_count(좁힌 후보 수) · second_called(2차를 불렀나) ·
fallback(None · "empty"=가 · "none"=나) · first_seconds · second_seconds · fallback_seconds ·
first(1차가 쓴 축 셋과 인자 — 폴백이 덮어도 남긴다)
```
`shortlist_recipe_ids` 는 1차 축으로 뽑은 후보, `llm_recipe_id` · `llm_candidate_recipe_ids` 는
2차의 날것(안 불렀으면 None · []). 폴백이 돌면 나머지 key 는 지금 길의 결과 그대로다 —
축 표에 찍히는 축도 그쪽 것이다. 1차 것은 `narrow.first` 에 있다.

**스위치.** `/resolve?narrow=true` · `check_resolve.py --narrow` · 환경변수 `RESOLVE_NARROW=1`
(서버 기본을 바꾼다. `/chat` 은 저쪽 화면의 계약이라 인자를 못 더하므로 화면에서 켠
상태를 보려면 이것으로 띄운다). **셋 다 없으면 끔.**

#### 1차 프롬프트에서 바꾼 곳 전부 (recipe_selection.md → recipe_axes.md)

새로 쓰지 않았다. 지금 것에서 뺀 것과 고친 것이 이것이 전부다.

```
뺐다   머리 두 줄 "당신은 Recipe 선택기다 / 아래 Menu 에 적힌 Recipe 만 사용해서…"
       [Menu] {menu} 절 통째로
       [Menu 읽는 법] 절 통째로
       [축 고르는 법] 마지막 줄 "그다음 candidate_recipe_ids 와 status 를 아래 [규칙] 대로 쓴다"
       [규칙] 의 recipe 관련 줄 전부 (Recipe ID 형식 · function 비교 · 후보 넣는 규칙 셋 ·
              status 정하는 규칙 · "reason 에 Recipe ID 를 적지 않는다")
       [예시] 절 통째로 (menu 의 A · B 를 전제한 예시라 뜻이 없다)
고쳤다 머리 → "당신은 발화 해석기다 / 사용자 요청을 읽고 그것이 무엇에서 시작해 무엇을
              돌려받으려 하는지, 무엇에 관한 것인지를 쓴다."
       "Menu 를 보기 전에 발화만 보고 쓴다" → "발화만 보고 쓴다" (Menu 가 없다)
       reason 규칙 "요청이 어디서 끝나는지와 무엇을 비교했는지만" →
                   "요청이 어디서 끝나는지와 무엇을 돌려받으려 하는지만" (비교할 menu 가 없다)
그대로 [축 고르는 법] 나머지 전부 · 축 선택지 셋 · argument 규칙 · [사용자 요청]
```
스키마는 지금 것의 앞 다섯 칸(reason · given · want · about · argument)과 같다
(테스트로 박았다).

#### 2차 프롬프트 전문 (recipe_pick.md)

```
당신은 Recipe 선택기다.
아래 후보에 적힌 Recipe 만 사용해서 사용자 요청을 해석한다.
후보는 온톨로지가 발화의 축으로 미리 좁힌 것이라 몇 개뿐이다.

[후보]
{candidates}

[후보 읽는 법]
- 한 줄이 Recipe 하나다. 앞이 Recipe ID 이고 뒤가 그 Recipe 가 하는 일이다.

[규칙]
- Recipe ID 는 recipe_002, recipe_010 처럼 후보의 ID 를 그대로 쓴다.
- 각 Recipe 의 문장을 사용자 요청과 비교한다.
- 하는 일이 사용자 요청과 똑같은 Recipe 를 모두 candidate_recipe_ids 에 넣는다.
  요청보다 더 많이 하거나 덜 하는 Recipe 는 넣지 않는다.
- 요청은 사용자가 말한 마지막 작업에서 끝난다.
  그 뒤를 더 진행하는 Recipe 는 넣지 않는다.
- 사용자가 어떤 작업을 말했지만 그 작업의 방식만 말하지 않았다면
  네가 대신 정하지 않고, 방식만 서로 다른 Recipe 를 모두 넣는다.
  사용자가 아예 말하지 않은 작업에는 이 규칙을 적용하지 않는다.
- 후보 1개면 status = "SELECT", recipe_id = 그 후보.
  후보 2개 이상이면 status = "CLARIFY", recipe_id = null.
- 후보 가운데 사용자 요청에 맞는 것이 하나도 없으면
  status = "NO_MATCH", recipe_id = null, candidate_recipe_ids = [].
  후보가 맞지 않는데 억지로 고르지 않는다. 없다고 하는 것이 맞다.
- reason 을 먼저 쓴다. 요청이 어디서 끝나는지와 무엇을 비교했는지만
  한두 문장으로 쓴다.
- reason 에 Recipe ID 를 적지 않는다. 고른 Recipe 는 candidate_recipe_ids
  에만 담는다. ID 를 나열하지 않는다.

[예시]

후보에 아래 두 Recipe 가 있다고 하자.
  A : 데이터를 불러와 분석한다.
  B : 데이터를 불러와 분석하고 결과를 문서로 만든다.

발화: "데이터 분석해줘"
→ 요청은 "분석" 에서 끝난다.
→ A 는 하는 일이 같다. B 는 문서 생성을 더 한다.
→ 후보는 A 뿐이다. B 는 넣지 않는다.

발화: "데이터 분석해서 문서로 만들어줘"
→ 요청은 "문서 생성" 에서 끝난다.
→ B 는 하는 일이 같다. A 는 문서 생성을 덜 한다.
→ 후보는 B 뿐이다. A 는 넣지 않는다.

발화: "날씨 알려줘"
→ A 도 B 도 날씨와 무관하다.
→ 맞는 것이 없다. status = "NO_MATCH".

[사용자 요청]
{utterance}
```
`{candidates}` 에는 `- recipe_030: 말한 장소로 좌표를 찾고 충전소를 …` 꼴로 좁힌 후보만
한 줄씩 들어간다 (menu.yaml 의 function). 규칙 부분은 지금 프롬프트의 [규칙] 을
거의 그대로 옮기고 "후보 0개면 NO_MATCH" 를 "맞는 것이 없으면 NO_MATCH · 억지로
고르지 않는다" 로 바꿨다. 예시에 NO_MATCH 하나를 더했다.

#### 폴백 둘의 정의

```
가 (empty)  1차 축 셋으로 shortlist.candidates 가 빈 목록을 냈다. 축이 셋 다 null 인 것도 여기
            → _resolve_full (지금 길) 을 그대로 부른다. 2차는 안 부른다. LLM 합 2회
나 (none)   2차가 status=NO_MATCH 를 썼거나, 쓴 id 가 전부 후보 밖이라 남는 것이 없다
            → _resolve_full 을 부른다. LLM 합 3회
```

#### 네 묶음 표 — 끔 → 켬 → 끔 → 켬, 같은 세션에서 연달아, 겹쳐 돌린 것 없음

```
                    적중    근접  빗나감  못 붙음   기준선 아홉        조회 빠짐   /resolve 평균
1  끔               62/93   25    6       0         19/27 · 근접 8      9          3.3s
2  켬               61/93   23    9       0         21/27 · 근접 6     12          3.6s
3  끔               62/93   25    6       0         19/27 · 근접 8      9          3.3s
4  켬               61/93   23    9       0         21/27 · 근접 6     12          3.6s
```

**잡음 폭.** 끔 둘은 표가 글자까지 같다. 켬 둘은 발화 11 하나만 갈렸다 (되묻기 후보
2개 ↔ 3개, 둘 다 근접). temperature 0 · seed 0 이라 같은 세션 안에서는 이렇게 좁다 —
「서른일곱째」의 폭(발화 7~11개)은 세션이 다를 때의 것이다.

**효과 (끔 평균 vs 켬 평균).** 적중 62 → 61 · 근접 25 → 23 · **빗나감 6 → 9** · 못 붙음 0 → 0.
폭(발화 0~1개)보다 크다. 가를 수 있다.

발화별로 무엇이 움직였나 (끔 → 켬, 3회 중)
```
좋아진 것   4  청주시 인구 구성       근접 3 → 적중 3    켬은 want 를 null 로 두고 3개로 좁혀 2차가 038 을 골랐다
            5  오송역 근처 충전소     적중 1 → 적중 3    후보 하나(030)라 2차 없이 SELECT
            13 오송역 국회의원 누구야 빗나감 1 → 적중 3
            15 청주 선거구            근접 3 → 적중 3
            31 청주 국회의원 선거구   근접 3 → 적중 3
            27 철도안전법 내용(위험)  빗나감 3 → 적중 2 · 빗나감 1 ({035})
나빠진 것   3  오송역 CCTV            적중 3 → 근접 3    {020, 033}. 후보 5개에서 2차가 둘을 남겼다
            7  전기차 충전소 데이터   근접 3(2개) → 근접 3(4개)
            ★ 12 청주시 행정경계      빗나감 2 → 빗나감 3   {017, 019}. 켬 · 끔 다 given=spoken_place. 이미 있던 자리
            ★ 16 교통 공약 많은 선거구 적중 3 → 빗나감 3   SELECT {010}. 축은 끔과 같다(keyword · item_list ·
                                                          election). 후보 4개(007~010)에서 2차가 010 을 확신했다
            17 청주 국회의원 공약     적중 3 → 근접 3    {008, 009}
            ★ 19 인구 많은 시군구 순위 적중 3 → 빗나감 2   SELECT {013}. 1차가 want=item_list · argument="시군구" 로
                                                          썼다 (끔은 statistics · 문장 전체). 후보 2개에 011 이 없었다
            22 오송역 지나는 노선     적중 3 → 근접 3    {003, 021, 034}. 후보 5개
그대로      나머지 17개
```

**★ 1순위 — 빗나감이 늘었다. 6 → 9.** 새로 생긴 것은 16(3회)과 19(2회)이고 둘 다
SELECT 로 확신했다. 16 은 축이 끔과 똑같은데 2차가 넷 중에서 010(지방선거)을 골랐다 —
좁힌 뒤 고르는 자리가 틀렸다. 19 는 1차가 want 를 statistics 가 아니라 item_list 로
써서 정답 011 이 후보에서 빠졌다 — 축이 틀렸고 안전장치가 없어 그대로 확신했다.
프롬프트가 짧아지면서 "무엇을 돌려받는가" 를 menu 문장 없이 정해야 하는 것이
19 에 그대로 드러났다. 27(위험)은 반대로 빗나감이 3 에서 1 로 줄었다.

#### 새 칸들의 뜻 · 2차를 안 부른 횟수 · 폴백 횟수

```
좁힌 후보 수   온톨로지가 1차 축으로 좁힌 후보 수. "5×3" 은 후보 5개가 3회
2차            2차를 부른 횟수 / 폴백 가 · 나  각각 지금 길로 간 횟수
1차 s · 2차 s · 폴백 s   서버가 잰 LLM 시간 평균 / 합계 s  도구가 잰 /resolve 한 번의 시간 평균
LLM 단독 · 검산 표   켰을 때 「해당 없음」. 칸은 남긴다 — 마흔 번의 기록과 이어 읽기 위해
```

```
켬 93회 중   후보 하나라 2차를 안 부름  17회  (1 · 2 · 5 · 10 · 24 가 3회씩, 19 · 27 이 1회씩)
             2차를 부름                 66회
             폴백 가 (조회 후보 0)      10회  (28 · 29 · 30 이 3회씩, 14 가 1회)
             폴백 나 (2차 "여기 없다")    3회  (21 경부선 선형 데이터 — 3회 다. 후보 {002, ?} 둘을 다 아니라고
                                              했고 지금 길이 002 를 골라 적중이 됐다)
             ★ LLM 을 3회 부른 자리       3회 / 93
```
폴백 가 열 번 중 아홉이 「조회 빠짐」이던 28 · 29 · 30 이다 — **축으로 못 거른 자리를
폴백이 그대로 지금 길로 돌려보냈고, 셋 다 끔과 같은 결과(28 · 30 근접 · 29 적중)가
나왔다.** 켬의 조회 빠짐 12 는 12(3) · 19(2) · 1(1) · 28(3) · 30(3) 이다. 28 · 30 은 폴백이 돌아
축 표와 조회 판정이 지금 길 것으로 찍힌 자리라 끔과 같은 빠짐이고, 12 · 19 가
좁히기 길에서 정답이 후보 밖이었던 자리다 (16 은 정답 008 이 후보 안에 있었는데
2차가 010 을 골랐다 — 빠짐이 아니라 고르기가 틀린 것).
LLM 이 "여기 없다" 를 말한 것은 3/66 이고, 그 셋은 정답이 후보 안에 있었는데 없다고
한 것이다(21). 16 · 19 처럼 정답이 없을 때는 없다고 안 하고 골랐다. **"없다" 를 말해야
할 자리에서는 안 말하고, 안 말해야 할 자리에서 말했다.** 3회 · 3회라 더 재봐야 한다.

#### 느려진 정도

```
끔   /resolve 평균 3.3s (기준선 3.1 · 확장 3.4)
켬   /resolve 평균 3.6s (기준선 3.1 · 확장 3.8)   1차 1.5s · 2차 2.2s · 폴백 3.2s
     후보 하나(2차 없음)   1.4~2.0s   ← 끔보다 빠르다
     2차까지              3.0~4.6s
     폴백 가              4.6~5.7s
     폴백 나 (21)         6.4s       ← 제일 느리다. 3회 부른다
```
평균으로는 +0.3s 다. 시연에서 체감되는 것은 폴백이 도는 발화(4.6~6.4s)이고, 하필
28 · 29 · 30 이 그 자리다.

#### 화면 실측 넷 (켠 상태 — `RESOLVE_NARROW=1` 로 uvicorn 을 다시 띄우고 `/chat/stream`)

```
오송역 근처 충전소 자세히 알려줘   ★ 4단 사슬 그대로 돈다. 후보 하나(040)라 2차 없음
  오송역 충전소 상세를 조회했습니다.
  1. geo.geocode       오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1 (127.3277, 36.6200)
  2. ev.searchStations  minLon=… 500건 (전체 2,173건) · 청주오송역차고지
  3. ev.getStation     statId="PK000313"  8건                      commands 6

문서에서 철도안전법 관련 내용 찾아줘   ★ RAG 그대로. 후보 2개에서 2차가 013 골랐다
  철도안전법 문서를 조회했습니다.
  1. knowledge.query   6건
     「철도안전법(법률)(제21188호)(20260303).pdf」 1쪽 · "법제처 1 국가법령정보센터 …"
     「…」 2쪽 · "… 제3조(다른 법률과의 관계) …"  / 「…」 32쪽 · "④ 제1항부터 …"

청주시 인구 알려줘   ★ 되묻기가 안 나왔다. 조회 후보 0 → 폴백 가 → 지금 길이 011 SELECT
  청주시 인구 통계를 조회했습니다.
  1. population.searchStatistics  4건 · 청주시 흥덕구 292,625명 (남 149,484 · 여 143,141) · 2026-06-30 기준
  (끔에서도 이번 세션은 29번이 적중 3/3 이라 같은 답이다. 「서른넷째」 때의 되묻기 모양은 이 발화로는 못 봤다)

오송역 CCTV 보여줘   ★ 켜면 되묻는다 (끔은 SELECT 020). 후보 5개에서 2차가 둘을 남겼다
  어느 것을 보시겠습니까?
    1  장소 좌표 변환 -> CCTV 조회
    2  철도 구간 형상 조회 -> CCTV 조회
  요청은 'CCTV 보여줘'에서 끝난다. CCTV 조회와 관련된 recipe는 recipe_020과 recipe_033이다. …
  → 같은 세션에 "1"
  고르신 것 — 1 장소 좌표 변환 -> CCTV 조회
  조회하지 못했습니다.
  1. geo.geocode       오송역 → … (127.3277, 36.6200)
  2. road.getCctv      minLon=… 실패 · 도구 호출에 실패했습니다
```
**되묻기 고르기(「서른넷째」)는 좁히기 뒤에도 돈다** — 후보 목록을 `candidate_recipe_ids`
와 `paths` 로 만드는 것은 그대로고 좁히기 길이 그 두 key 를 같은 뜻으로 채운다.
`road.getCctv` 실패는 끔으로 되돌린 뒤에도 똑같이 났다. 도구 쪽이지 이번 변경이
아니다. 화면 실측 뒤 서버는 env 없이 다시 띄웠다 (기본 끔).

#### ★ 읽는 법 넷 중 어디인가

```
1순위  빗나감이 늘지 않았는가    ★ 늘었다. 6 → 9 (16 · 19 가 새로 확신하고 틀림). 잡음 폭 밖
2순위  적중이 늘었는가           안 늘었다. 62 → 61. 기준선 아홉은 19 → 21, 확장은 43 → 40
3순위  폴백이 얼마나 도는가      13/93 (가 10 · 나 3). 세 번 부른 자리 3/93
4순위  얼마나 느려졌는가         평균 +0.3s. 폴백 자리 4.6~6.4s
```
**1순위에서 걸린다.** 이 자로는 좁히기 길이 지금 길보다 낫다고 못 읽는다.
기준선 아홉만 보면 좋아지고(19 → 21, 빗나감 0 그대로) 확장에서 나빠진다.

#### 안 한 것

- 기본값을 안 바꿨다. 옛 길을 안 지웠다.
- 2차 프롬프트를 한 판만 썼다. "여기 없다" 를 더 잘 말하게 하는 문안은 안 재봤다.
- 1차 프롬프트에서 want 를 menu 없이 정하는 어려움(19)에 손대지 않았다. 「서른여섯째」의
  축 문안 V1~V6 은 지금 길에서 잰 것이라 1차에 옮겨 재보지 않았다.
- 폴백 가에서 1차 축을 버리고 지금 길을 통째로 다시 부른다. 1차 축을 살려 쓰는 안은 안 만들었다.
- 관문의 「끔 = 적중 62/93 · 근접 24 · 빗나감 4 · 못 붙음 3」과 이번 끔(62 · 25 · 6 · 0)이 다르다.
  **stash 로 고치기 전 코드를 같은 세션에서 돌려 맞댔다 — 표가 글자까지 같다**
  (62 · 25 · 6 · 0 · 조회 빠짐 9). 스위치가 지금 길을 건드린 것이 아니라 관문 숫자를 적은 날과
  이 세션 사이의 드리프트다(「서른일곱째」가 잰 폭 안). 그래서 멈추지 않고 진행했다.
- 화면 실측 넷 중 「청주시 인구 알려줘」의 되묻기 모양은 못 봤다. 이 세션에서는 끔도 켬도 되묻지 않는다.

#### ★ 다음에 고칠 자리는 새 파일 둘 안에 있다

나빠진 자리 넷이 전부 이번에 만든 프롬프트 둘 안에 있다.

```
16   좁혀놓고도 2차가 넷 중 엉뚱한 것을 확신했다       recipe_pick.md
21   정답이 후보에 있는데 "없다" 고 했다               recipe_pick.md
16 · 19  정답이 후보에 없는데 "없다" 를 안 했다        recipe_pick.md
19   1차가 want 를 잘못 써서 정답이 후보에서 빠졌다     recipe_axes.md
```

★ 이 두 파일은 지금 길(`_resolve_full`)이 안 쓴다. 고쳐도 끔의 판정이 안 흔들린다.
프롬프트를 건드리면 무관한 발화가 흔들린 전례가 일곱 번인데
(「열한째」·「열여덟째」·「스물한째」·「스물일곱째」·「서른째」 …)
여기서는 그 문제가 안 생긴다. **이 저장소에서 처음 있는 조건이다.**

지금 2차 문안은 40문장 시절 프롬프트를 거의 그대로 옮긴 것이다.
NO_MATCH 예시가 "날씨 알려줘" 하나뿐이라 아예 무관한 경우만 가르친다.
정작 필요한 것은 "후보 넷이 다 비슷한데 정답은 그중에 없다" 는 미묘한 경우다.
**안 가르쳤다.** 그것이 폴백 나가 반대로 도는 것과 관련 있는지는 안 쟀다.

### 2026-08-27 (마흔째) · 배선이 도구의 어느 칸을 쓰는지 스키마와 맞대는 계기판 — 재기만 했다

무인 실행. **제품 코드 0줄 변경.** 만든 것은 `tools/check_inputs.py` 하나이고 고친
파일은 이 파일뿐이다. 브랜치 `integration/ASAP-Ontology`(b1e4d8e). 배선 · 온톨로지 ·
`demo/` · `vendor/` · `KRRI_ASAP/` 은 안 건드렸다. 스키마 전문은
`tools/probe_out/tools.json`(gitignore) — Gateway `/api/tools` 에서 3.0초에 42개를
받았다(2026-08-22 의 `/tmp/tools.json` 과 도구 수가 같다).

**왜 만들었나.** `check_wiring` 은 배선 줄이 **있는가**만 센다. 그 줄이 **맞는 칸을
쓰는가**는 아무도 안 봐서 철도가 조용히 틀렸다 —

```
geo.getRailwayLines 는 stationName 과 railwayName 을 갖는다
우리 배선은 stationName 하나만 보낸다
  → "경부선 노선 보여줘" 가 0건 (2026-08-26 화면 실측)
  → check_wiring 은 통과였다. 줄은 있으니까
```

도구가 늘면 이런 자리가 비례해서 는다. 사람이 화면에서 0건을 보고 알아채는
방식으로는 못 버틴다.

#### 세는 법 넷 — 배선 줄마다 · 칸마다

```
없는 칸을 보낸다        우리가 보내는 칸이 inputSchema.properties 에 없다   ★ 반드시 0건이나 오류
꼭 필요한데 안 보낸다   required 인데 우리 배선에 없다                       ★ 반드시 오류
있는데 안 쓰는 칸       properties 에 있는데 우리가 안 보낸다                판단할 자리. 고르지 않는다
맞다                    나머지
```

줄의 판정은 그 줄의 칸 판정을 모은 것이다. 첫 둘이 하나도 없으면 「맞다」이고
셋째는 개수만 옆에 적는다. **`input_first` 도 갈래마다 한 행으로 센다** — 지금
그것을 적은 줄이 0 이라 행이 안 늘었다(35 = 35 + 0).

**어댑터 뒤의 칸으로 맞댔다.** vendor 의 `point_radius_to_bbox` 가
center/location · radiusMeters 를 지우고 bbox 넷을 만든다
(`generic_mcp_executor._point_radius_to_bbox_input`). 줄에 adapter 가 적혀 있거나
(ev.searchStations × map_extent), 안 적혀 있어도 required 에 bbox 넷이 다 있고
중심·반경이 있으면 저절로 걸린다(road.getCctv × point,
`_should_auto_apply_point_radius_to_bbox`). 이것을 안 따라 하면 그 두 줄이
「없는 칸을 보낸다」로 잘못 찍힌다 — 첫 실행에서 그렇게 나와서 넣었다.

#### 추리는 기준 셋 — 왜 이 셋인가

셋째 부류가 201개다. 대부분 limit · includeGeometry · simplifyM 같은 손잡이라
그냥 늘어놓으면 못 읽는다. 아래 셋 중 하나에 걸린 칸만 ★ 로 올린다.

```
R  required 다                  둘째 부류와 같다. 반드시 오류
S  우리가 보내는 칸과 닮았다    camelCase 로 쪼갠 낱말을 하나라도 공유한다.
                                stationName 대 railwayName 이 Name 을 공유한다 — 철도가 이것에 걸린다
O  같은 도구의 다른 줄이 쓴다   한 줄은 보내는데 다른 줄은 안 보내는 칸
```

세 기준 모두 **스키마와 배선만으로** 판정된다 — 서버 · LLM · 값 판단 · description
읽기가 없다. "이 칸이 검색어 같다" 는 짐작이라 안 넣었다. **걸렸다고 틀린 것은
아니다.** 사람이 볼 자리를 좁힌 것뿐이고 고르지 않았다.

**S 의 한계를 미리 적는다.** 철도는 두 칸이 둘 다 `…Name` 이라 걸린 것이다. 우리
칸이 `query` 이고 저쪽 맞는 칸이 `railwayName` 이었다면 S 로는 못 잡는다. 그런
자리는 O 나 사람 눈이 잡아야 한다.

#### 표 (35줄 · 도구 26)

```
도구                                      배선 줄 (노드 × 받는 타입)                                보내는 칸                                   판정
  adminBoundary.findBoundaryByPoint         find_admin_boundary_by_point × point                      lat, lon                                    맞다  · 안 쓰는 칸 4
  adminBoundary.searchBoundaries            search_admin_boundaries × keyword                         query                                       맞다  · 안 쓰는 칸 10
  adminBoundary.searchBoundaries            search_admin_boundaries × map_extent                      bbox                                        맞다  · 안 쓰는 칸 10
  election.findAssemblyDistrictByPoint      find_assembly_district_by_point × point                   lat, lon                                    맞다  · 안 쓰는 칸 6
  election.findAssemblyPledgeDistrictByPointfind_assembly_pledge_district_by_point × point            lat, lon                                    맞다  · 안 쓰는 칸 6
  election.findDistrictByPoint              find_election_district_by_point × point                   lat, lon                                    맞다  · 안 쓰는 칸 1
  election.findLocalPledgeSummaryByPoint    find_local_pledge_summary_by_point × point                lat, lon                                    맞다  · 안 쓰는 칸 2
  election.getAssemblyDistrict              get_assembly_district × district_code                     name                                        맞다  · 안 쓰는 칸 2
  election.getAssemblyPledgeDistrict        get_assembly_pledge_district × district_code              name                                        맞다  · 안 쓰는 칸 3
  election.getDistrict                      get_election_district × district_code                     name                                        맞다  · 안 쓰는 칸 2
  election.getLocalPledgeSummary            get_local_pledge_summary × admin_code                     sidoCode                                    맞다  · 안 쓰는 칸 2
  election.searchAssemblyDistricts          search_assembly_districts × keyword                       query                                       맞다  · 안 쓰는 칸 9
  election.searchAssemblyDistricts          search_assembly_districts × map_extent                    bbox                                        맞다  · 안 쓰는 칸 9
  election.searchAssemblyPledgeDistricts    search_assembly_pledge_districts × keyword                query                                       맞다  · 안 쓰는 칸 12
  election.searchAssemblyPledgeDistricts    search_assembly_pledge_districts × map_extent             bbox                                        맞다  · 안 쓰는 칸 12
  election.searchDistricts                  search_election_districts × keyword                       query                                       맞다  · 안 쓰는 칸 5
  election.searchLocalPledgeSummaries       search_local_pledge_summaries × keyword                   query                                       맞다  · 안 쓰는 칸 9
  election.searchLocalPledgeSummaries       search_local_pledge_summaries × map_extent                bbox                                        맞다  · 안 쓰는 칸 9
  ev.getStation                             get_ev_station × station_id                               statId                                      맞다  · 안 쓰는 칸 0
  ev.searchStations                         search_ev_stations × keyword                              query                                       맞다  · 안 쓰는 칸 17
  ev.searchStations                         search_ev_stations × map_extent                           maxLat, maxLon, minLat, minLon ·어댑터      맞다  · 안 쓰는 칸 14
  geo.geocode                               geocode_place × place_name                                query                                       맞다  · 안 쓰는 칸 0
  geo.getRailwayLines                       get_railway_lines × map_extent                            bbox                                        맞다  · 안 쓰는 칸 4
  geo.getRailwayLines                       get_railway_lines × place_name                            stationName                                 맞다  · 안 쓰는 칸 4
  knowledge.query                           search_documents × keyword                                k, query                                    맞다  · 안 쓰는 칸 1
  population.getAgeProfile                  get_age_profile × admin_code                              code, level                                 맞다  · 안 쓰는 칸 1
  population.getTrend                       get_population_trend × admin_code                         code, level                                 맞다  · 안 쓰는 칸 3
  population.searchStatistics               search_population_statistics × keyword                    query                                       맞다  · 안 쓰는 칸 12
  population.searchStatistics               search_population_statistics × map_extent                 bbox                                        맞다  · 안 쓰는 칸 12
  rail.getSectionGeometry                   get_railway_section × place_name                          sectionName                                 맞다  · 안 쓰는 칸 0
  road.getCctv                              find_cctv × map_extent                                    maxLat, maxLon, minLat, minLon              맞다  · 안 쓰는 칸 0
  road.getCctv                              find_cctv × point                                         maxLat, maxLon, minLat, minLon ·어댑터      맞다  · 안 쓰는 칸 0
  vworld.getAdministrativeBoundaries        get_vworld_boundaries × keyword                           query                                       맞다  · 안 쓰는 칸 8
  vworld.getAdministrativeBoundaries        get_vworld_boundaries × map_extent                        bbox                                        맞다  · 안 쓰는 칸 8
  web.search                                web_search × keyword                                      query                                       맞다  · 안 쓰는 칸 4
```

#### ★ 눈여겨볼 자리

```
R required · S 우리 칸과 낱말을 공유 · O 같은 도구의 다른 줄이 쓴다

  도구                                      배선 줄                                                   우리 칸                       안 쓰는 칸 (왜)
  adminBoundary.searchBoundaries            search_admin_boundaries × keyword                         query                         bbox(O)
  adminBoundary.searchBoundaries            search_admin_boundaries × map_extent                      bbox                          includeBbox(S) · query(O)
  election.getLocalPledgeSummary            get_local_pledge_summary × admin_code                     sidoCode                      sidoName(S)
  election.searchAssemblyDistricts          search_assembly_districts × keyword                       query                         bbox(O)
  election.searchAssemblyDistricts          search_assembly_districts × map_extent                    bbox                          query(O)
  election.searchAssemblyPledgeDistricts    search_assembly_pledge_districts × keyword                query                         bbox(O)
  election.searchAssemblyPledgeDistricts    search_assembly_pledge_districts × map_extent             bbox                          query(O)
  election.searchLocalPledgeSummaries       search_local_pledge_summaries × keyword                   query                         bbox(O)
  election.searchLocalPledgeSummaries       search_local_pledge_summaries × map_extent                bbox                          query(O)
  ev.searchStations                         search_ev_stations × keyword                              query                         maxLat(O) · maxLon(O) · minLat(O) · minLon(O)
  ev.searchStations                         search_ev_stations × map_extent                           maxLat, maxLon, minLat, minLonminOutputKw(S) · query(O)
  geo.getRailwayLines                       get_railway_lines × map_extent                            bbox                          stationName(O)
  geo.getRailwayLines                       get_railway_lines × place_name                            stationName                   bbox(O) · railwayName(S)
  population.searchStatistics               search_population_statistics × keyword                    query                         bbox(O)
  population.searchStatistics               search_population_statistics × map_extent                 bbox                          includeBbox(S) · query(O)
  vworld.getAdministrativeBoundaries        get_vworld_boundaries × keyword                           query                         bbox(O)
  vworld.getAdministrativeBoundaries        get_vworld_boundaries × map_extent                        bbox                          query(O)

  걸린 칸 24 · R 0 · S 5 · O 19
  S 만 따로 : adminBoundary.searchBoundaries.includeBbox, election.getLocalPledgeSummary.sidoName, ev.searchStations.minOutputKw, geo.getRailwayLines.railwayName, population.searchStatistics.includeBbox
```

**읽기.** O 19 는 전부 `query` ↔ `bbox` 짝(그리고 ev 의 bbox 넷)이다 — 같은
노드가 「말한 키워드」자리와 「지도 범위」자리 둘에 오므로 자리마다 다른 칸을
보내는 것이 맞다. **철도 같은 것은 S 다섯 중에서 갈린다.**

```
geo.getRailwayLines.railwayName          ★ 철도. 이미 아는 그 자리 (「열린 과제」)
election.getLocalPledgeSummary.sidoName  ★ 철도와 같은 꼴 — 코드 칸 하나만 보내고 이름 칸은 안 보낸다.
                                         지금은 앞 단계 items.0.code 에서 오므로 sidoCode 가 맞다.
                                         「말한 시도 이름」자리가 생기면 여기가 철도처럼 된다
adminBoundary.searchBoundaries.includeBbox   낱말 bbox 공유. 손잡이(응답에 bbox 를 실을지)다. 소음
population.searchStatistics.includeBbox      같다. 소음
ev.searchStations.minOutputKw                낱말 min 공유. 출력 하한 필터다. 소음
```

**S 다섯 중 셋이 소음**이다. 낱말 공유는 `include…` · `min…` 접두사에 쉽게 걸린다.
좁힐 거면 「마지막 낱말이 같을 때만」(Name · Code) 이 한 방법인데, 이번은 넓게
두고 표를 냈다 — 좁히는 것은 사람이 정한다.

#### 합계

```
배선 줄 35 (input 35 · input_first 0) · 도구 26
  줄 판정   맞다 35 · 없는 칸 0 · 안 보낸 required 0 · 스키마 못 받음 0
  칸 합계   없는 칸 0 · 안 보낸 required 0 · 안 쓰는 칸 201 (그중 ★ 24)
```

**스키마를 못 받은 도구 0.** 배선이 가리키는 26개가 전부 `/api/tools` 42개 안에
있다. r5-server · otp-router 는 애초에 배선이 없어 이 표에 안 온다
(「서른아홉째」). `web.fetch` 는 배선이 비어 있어(`STEP_OF` 아래 주석) 표에 없다 —
그것은 `check_wiring` 의 C 가 센다.

#### 조용히 죽지 않게 — 테스트를 안 두고 자체 검사를 뒀다

`check_argument` 가 나흘간 ValueError 로 죽어 있던 전례가 있다(「열린 과제」).
`tools/` 는 "테스트를 두지 않는다" 가 규칙이라 `tests/` 에 안 넣었다. 대신
`_selfcheck()` 가 손으로 적은 스키마 하나와 배선 한 줄로 **네 부류 · 세 기준 ·
어댑터 셋(명시 · 자동 · 안 걸림)** 이 각각 나오는지 본 뒤에야 표를 찍는다.
틀리면 첫 줄에서 죽고 표가 안 나온다 — `!` 가 조용히 찍히는 꼴이 안 된다.
pytest 개수는 그대로다(297 passed / 1 failed, graphviz 음성 대조군).

#### 안 한 것

- **찾은 것을 안 고쳤다.** 철도 · sidoName 둘 다 목록만 「열린 과제」에 남겼다.
- S 를 안 좁혔다(위).
- description 을 안 읽었다. "검색어" 라고 적힌 칸을 찾는 것은 짐작이다.
- 값의 타입(string 대 array)을 안 맞댔다. 칸 이름만 봤다. `bbox` 를 배열로
  보내는 것이 맞는지는 이 표가 모른다 — 실측(「셋째」 이후)이 그것을 본다.
- `TOOL_OF` 에는 있고 `STEP_OF` 에 줄이 없는 노드는 안 셌다. `check_wiring` 의 C 다.

### 2026-08-27 (서른아홉째) · 도달권(접근성) 도구가 우리 창구로 불리는지 눌러본다 — 재기만 했다

무인 실행. **코드 0줄 변경.** 고친 것은 이 파일뿐이고 브랜치는
`dopamo-with-ontology`(0f9f1bf 에서 땄다). 응답 전문은 `tools/probe_out/`
(`r5-server.compute_isochrone.의왕역-*-2026-08-27.json` 일곱 벌 + 의왕역
geocode). `tools/probe_out/` 은 `.gitignore:58` 이라 `git status` 에 안 뜬다.

**왜 눌렀나.** 원장님 지시로 보도자료를 준비한다. 「독파모 + 우리 오케스트레이터」가
접근성 질의에 답하는 화면이 필요하고 **8/31(월) 미팅 전에 좌/우 비교 이미지**를
만들어야 한다. 그런데 접근성 도구가 우리 온톨로지에 없다 — MCP 서버 넷 중
우리는 `asap-mcp-core`(40) · `web-search`(2) 만 넣었다. 이번엔 `r5-server`
하나만 봤다. 붙일 수 있는지 · 어떻게 부르는지만이고, 붙이는 것은 다음 작업이다.

#### 물음 넷

```
물음                          답                                        근거
★1 우리 창구로 불리는가        ★ 못 불렀다 — 서버에 닿지 않는다              7번 전부 HTTP 500 · 3.0초
                              Gateway 에 r5-server 는 등록돼 있다            ASAP-Gateway/data/servers.json
                                (http://175.196.203.153:8002/mcp)              user-mcp-selections.json 에도 있다
                              Gateway 가 그 주소를 못 연다                   docker logs gateway:
                                                                             "[Registry] Failed to refresh tools from
                                                                              r5-server: connect EHOSTUNREACH"
                              그래서 /api/tools 42개에 r5 도구가 없고         curl localhost:3000/api/tools
                              execute 는 도구를 못 찾는다                    "Tool 'compute_isochrone' not found or
                                                                              no adapter available."
                              권한 판정까지 못 갔다                          Executor.ts:22-27 — 도구 못 찾으면 권한
                                                                             검사 전에 던진다
   2 꼭 넣어야 하는 칸          ★ 못 쟀다 — 서버가 안 열려 인자를 빼도 같은     일곱 벌 응답이 바이트까지 같다
                              오류다. inputSchema 도 못 받았다(/api/tools 에
                              없다 · KRRI_ASAP/tools.json 에도 없다)
   3 응답 모양                  저쪽 테스트 픽스처로만 안다(실측 아님)          ASAP-orchestrator/tests/
                              우리 답 문구는 칸 이름만 나열한다 —             test_orchestration_isochrone_verification.py
                              건수 · 수치 없음 · 좌표는 안 샌다               vendor summarize() 를 픽스처로 돌렸다(실측)
   4 지도 판정기가 알아보는가    ★ 알아본다. map.draw r5-isochrone-polygons     vendor/asap/mcp_result_inspector.py:213
                              까지 나온다                                    build_commands_from_artifacts 를 픽스처로
                                                                             돌렸다(실측)
```

#### 물음 1 — 왜 못 부르나 (자세히)

```
이 장비에서   ping 175.196.203.153            9ms 응답. 호스트는 살아 있다
              curl :8002/mcp (tools/list)     exit 7 (연결 안 됨) · 3초
              curl :8001/ (otp-router)        HTTP 000. 같다
gateway 에서  docker logs                     r5-server · otp-router 둘 다 EHOSTUNREACH 반복
```

호스트는 응답하는데 8001 · 8002 포트가 닫혀 있다. 원격 r5 · otp 프로세스가
내려가 있거나 방화벽이 이 장비(192.168.68.231)를 막는 것이다. **어느 쪽인지는
이 장비에서 못 가른다.** `web.search` 때의 "not applied for this user" 와는
다른 층이다 — 그건 권한이었고 이번엔 도구가 Gateway 목록에 아예 없다.

**덧붙임 (같은 날) — 답이 왔다.** `r5-server` 와 `otp-router` 는 철도연 자산이다.
원래 철도연 맥스튜디오에서 돌던 것을 다시 필요하다고 하여 지금 중지해 둔
상태다 (조종식 대표님 회신, 2026-08-27). **방화벽이나 권한 문제가 아니다.**
다음 주 월요일 회의 뒤에 다시 켠다. 위의 「원격 프로세스가 내려가 있거나
방화벽이거나」 중 앞쪽이었고, 이 장비에서 할 일은 없다.

**권한은 코드로만 안다.** `Executor.ts` 가 `user_context.selected_mcp_tool_refs`
를 보고 `<server>/*` 또는 `<server>/<tool>` 이 있어야 통과시킨다. 우리
`USER_CONTEXT` 는 `asap-mcp-core/*` 하나라(`execute_service.py:38`) 서버가
열려도 **그대로는 "not applied" 로 막힌다.** 이건 우리 쪽 상수에 `r5-server/*`
를 더하는 일이지 대표님께 요청할 일이 아니다 — Gateway 는 우리가 보낸 refs 를
그대로 믿는다(`user-mcp-selections.json` 은 저쪽 화면 사용자용이다).
**단, 실측은 아니다.** 서버가 열린 뒤 `r5-server/*` 를 넣고 다시 눌러야 안다.

#### 물음 2 — 못 쟀다. 저쪽이 어떻게 채우는지만 적는다

```
칸                 저쪽 plugin.md                       우리 배선에 있는가
origin_lon/lat     $center.0 · $center.1 (geocode)      있다 — $prev.lon · $prev.lat 모양 (POINT_FROM_PREVIOUS)
max_minutes        $minutes (LLM 이 cutoffs 최대값)      없다 — 「인자 둘」(장소+분). @arg 는 하나뿐이다
cutoffs_minutes    $cutoffs_minutes (LLM)                없다
mode               $mode. LLM 프롬프트 기본 WALK          없다
departure_date     $runtime.date_kst  "%Y-%m-%d"          없다 — market_plugin_engine.py:389 가 now 로 만든다
departure_time     $runtime.time_kst  "%H:%M"             없다 — 같은 곳 :390
include_points     true (상수)                            상수면 배선표에 적으면 된다
```

필수가 무엇인지 · 기본값이 있는지는 **서버가 열려야 안다.** 일곱 벌의 눌러본
조합(기본 3칸 · +mode · +date/time · +cutoffs+include_points · max 15/60)은
`tools/probe_out/` 에 요청 본문째 남겨 뒀으니 서버가 열리면 같은 스크립트로
다시 누르면 된다(`/tmp/...scratchpad/probe_r5.py` 는 세션 것이라 사라진다.
본문은 json 의 `request` 칸에 있다).

의왕역 좌표(실측 · `geo.geocode` "의왕역"): **(126.9482, 37.3201)** ·
경기도 의왕시 삼동 473.

#### 물음 3 — 응답 모양 (저쪽 픽스처 기준)

```
{ status: "success", max_minutes, cutoffs_minutes: [...], mode,
  feature_collections: { polygons: FeatureCollection(properties.cutoff_min),
                         lines?, points? } }
판정기가 더 읽는 칸: origin · scenario_id · reachable_cell_count · elapsed_ms
```

우리 `summarize()` 에 픽스처를 넣으면
`'칸: status · max_minutes · cutoffs_minutes · mode · origin · feature_collections'`
가 나온다. **좌표는 안 샌다**(dict 안은 안 읽는다). **건수 칸도 사람이 볼
수치도 없다** — 면적 · 도달 인구는 응답에 없고(픽스처 기준) `reachable_cell_count`
가 유일한 수치인데 셀 수라 그대로 보이기엔 뜻이 없다. 답 문구는 mode ·
cutoffs 로 "도보 15·30분 도달 영역" 한 줄을 만드는 규칙이 새로 필요하다.

#### 물음 4 — 지도 (실측 · 픽스처)

`_extract_isochrone` 이 도구 이름을 안 보고 `feature_collections.{polygons|lines|points}`
가 GeoJSON 이면 `isochrone` artifact 로 만든다. 렌더러는
`map.clear ×4 → map.draw r5-isochrone-polygons · r5-isochrone-origin → view.camera.flyTo`
를 낸다. **붙이는 순간 지도에 그려진다.** 화면 작업은 늘지 않는다.

#### ★ 붙이는 데 필요한 일 (판단 없이 목록만)

```
서버    r5-server(175.196.203.153:8002) 가 열려야 한다. 누가 여는지 대표님께 물어야 한다
배선    step_service 에 셋째 서버 상수 (R5_SERVER_ID = "r5-server")
        execute_service.USER_CONTEXT.selected_mcp_tool_refs 에 "r5-server/*"
        (tools/probe_tools.py 는 tools.json 의 serverId 를 쓰므로 r5 도구가 목록에 실리면 따라온다)
노드    도구 노드 compute_isochrone (server r5-server) · 데이터 노드 「도달권」(isochrone)
관계    geocode_place → compute_isochrone (장소 → 좌표, $prev.lon/lat)
        찍은 지점 → compute_isochrone 은 「찍은 지점」 노드가 없어 못 잇는다(열린 과제 기존 항목)
인자    max_minutes · cutoffs_minutes · mode 를 발화에서 뽑는 자리 — 「인자 둘」 문제.
        departure_date · departure_time 은 배선이 now(KST) 를 만들어 넣어야 한다.
        include_points=true 는 상수
답 문구 workflow_answer 에 isochrone 규칙(mode · cutoffs 로 한 줄). 지금은 칸 이름만 나간다
화면    없음 — 판정기 · 렌더러가 이미 안다
축      recipe_selection 축 목록에 도달권 recipe 한 줄 (프롬프트 길이 다시 잰다)
정답표  도달권 발화 추가
```

#### 안 한 것

- otp-router(`otp_plan_trip`)는 안 봤다. 같은 호스트 8001 이 같은 이유로 닫혀 있는 것만 봤다.
- 서버가 안 열려 물음 2 는 통째로 못 쟀다. 응답 모양(3)은 저쪽 픽스처지 실측이 아니다.
- 권한(`r5-server/*`)은 코드 읽기로만 안다. 실측 없음.
- 저쪽 `isochrone_geometry.validate_and_repair` 가 진짜 R5 응답에서 무엇을 하는지는 안 봤다.

---

### 2026-08-27 (서른여덟째) · 저쪽 화면에서 넣은 발화를 Streamlit 이 따라 그린다

모델 qwen3:32b(서버 기본) · recipe 40 · 무인 실행(10:05~10:45). 고친 것은
`demo/api` · `demo/ui` · `tests` · 이 파일뿐이다. **`KRRI_ASAP/` 는 추적 파일
0줄 변경**(`git status --short` 에 남은 둘은 이번 것이 아니다 —
`ASAP-mcp/data.bak.20260824/` 와 CLAUDE.md 에 적힌 `docker-compose.override.yml`).
온톨로지 · menu · 배선표 · 정답표 · `vendor/` · `resolve_service` ·
`execute_service` 는 손대지 않았다.

**왜 만들었나.** 시연에서 두 화면이 나란히 선다.

```
저쪽 화면    지도 · 채팅 · 실행 결과       쓰는 사람이 보는 것
Streamlit    온톨로지 그래프 · 고른 경로    그 뒤에서 무슨 일이 일어났나
```

지금까지 둘은 같은 서버를 보면서도 서로를 몰랐다. 저쪽은 `POST /chat/stream`
으로 넣고 Streamlit 은 `POST /resolve` 로 그렸다. **저쪽이 넣은 발화의 결과를
우리 서버가 이미 알고 있으므로** 그것을 기억해 두고 Streamlit 이 물어가면
된다. 저쪽 코드는 한 줄도 안 바뀐다.

**방향은 하나다.** 저쪽에서 넣고 Streamlit 이 따라 그린다. 반대 방향은 저쪽
화면을 고쳐야 하므로 안 했다.

#### 환경 (재기 전에 적었다)

```
uvicorn(8000)     .venv python -m uvicorn demo.api.main:app --reload (pid 1226460)
streamlit(8501)   .venv streamlit run demo/ui/main.py (pid 2008614)
ollama            0.32.15 (pid 72585) · qwen3:32b · GPU0
KRRI_ASAP         postgis · keycloak · gateway · mcp · web-search-mcp · web 을 올렸다
                  orchestrator 컨테이너는 안 올린다 — 8000 자리가 우리 것이다
                  (compose 가 gateway 의 depends_on 으로 그것을 함께 올리려다
                   "bind host port 0.0.0.0:8000: address already in use" 로 멈춘다.
                   `docker compose up -d --no-deps gateway web` 으로 우회했다)
저쪽 화면의 길    POST http://localhost/api/orchestrator/chat/stream
                  → nginx(web:80) → gateway:3000 → host.docker.internal:8000 (우리)
```

**★ 브라우저를 못 눌렀다. 무인 실행이고 이 장비에 브라우저 자동화가 없다.**
그래서 두 화면을 이렇게 대신 몰았다. **아래 문구는 전부 실측이지 옮겨 적은
것이 아니다.**

```
저쪽 화면    저쪽 웹 UI 가 쓰는 그 HTTP 길을 그대로 두드렸다.
             `ASAP-web/packages/chat/src/api.ts` 의 sendChatMessageStream 이
             보내는 본문(text · sessionId · context · target_documents)과 같고,
             nginx 와 gateway 를 통과하는 것도 같다. 다른 것은 DOM 뿐이다
Streamlit    streamlit 내장 `streamlit.testing.v1.AppTest` 로 `demo/ui/main.py`
             를 실제로 돌렸다. 세션 상태 · 위젯 · 그려진 요소를 그대로 읽는다.
             새 의존성이 아니다. 다른 것은 픽셀 뿐이다
```

**사람이 브라우저에서 눌러 본 것은 아직 없다.** 리허설 때 확인할 자리다.

---

#### 1. 기록하는 것과 안 하는 것

`demo/api/services/recent_service.py` 하나다. 메모리에만 둔다 — 파일도 DB 도
안 만들었다. 서버를 다시 띄우면 사라지고, 그때는 발화를 다시 넣으면 된다.

```
남기는 것   언제 · 발화 · status(SELECT · CLARIFY · NO_MATCH · CHOICE)
            축 셋(given · want · about)과 인자
            고른 recipe id 와 후보들
            머리말(head)과 단계 덩이(steps) — 답에서 잘라낸 것이다
            답 문구(answer) 전체
안 남기는 것 commands ★ 이 저장소의 계약이다. geojson · 좌표 배열이 거기 있다
            context · target_documents · reason 밖의 LLM 날것
개수        스무 회차(MAX_TURNS). 넘으면 오래된 것부터 버린다
번호        늘기만 하는 정수. 버려진 번호를 다시 안 쓴다
```

**단계 줄을 다시 만들지 않는다.** `vendor/asap/workflow_answer` 가 이미 만들어
저쪽 화면으로 나간 문자열을 그대로 자른다. 요약하는 코드가 둘이 되는 순간
한쪽이 geojson 을 흘리고, `_preview` 를 지운 것이 그 이유였다(「스물셋째」).

**한 단계가 여러 줄일 수 있다.** `knowledge.query` 는 건수 줄 아래에 문서
조각을 들여 쓴 줄로 늘어놓는다(「서른한째」). 처음에 줄 단위로 셌다가 5번
발화에서 머리말이 조각을 삼켰다. 지금은 `^\d+\. ` 로 시작하는 줄에서 덩이를
열고 그 뒤 줄을 거기 붙인다. 되묻기 답의 후보 줄(`  1  인구 통계 조회`)은
앞에 공백이 있고 마침표가 없어 이 꼴에 안 걸린다.

**훔쳐보는 자리가 둘이다.** `demo/api/main.py` 가 한 번 씌우는 껍데기이고
**값을 하나도 안 바꾼다.**

```
resolve_service.resolve   축 셋 · 인자 · 고른 recipe · 후보들
execute_service.run       실제로 부른 recipe 와 인자
```

두 파일을 못 건드리기로 했는데 그 안의 값이 이벤트로는 안 나오기 때문이다.
`/chat/stream` 이 내는 것은 `step_end` 의 `"SELECT recipe_001"` 한 줄뿐이고
거기에 축도 후보도 없다. **되묻기 뒤에 번호로 고른 회차는 해석을 아예 안
거치므로** `run` 자리에서만 recipe 가 보인다(status `CHOICE` 가 그것이다).
회차를 가르는 것은 `ContextVar` 다 — Streamlit 이 부르는 `POST /resolve` 는
회차 밖이라 아무것도 안 남긴다.

**기록이 터져도 답은 나간다.** 남기는 자리도 훔쳐보는 자리도 전부 감싸서
삼킨다. 이벤트는 이미 다 나간 뒤에 도는 일이다.

#### 2. 읽어가는 창구

```
GET /recent?since=<번호>   그 번호보다 큰 회차들과 지금 번호
GET /recent                마지막 다섯 회차(TAIL)
```

읽기 전용이다. 번호가 그대로면 `turns` 가 빈 목록이고, 화면은 그것으로
"새 것이 없다" 를 안다.

#### 3. 갱신 주기 3초 — 왜 그 값인가

`demo/ui/config.py` 의 `FOLLOW_INTERVAL_SECONDS = 3` 한 곳이다.

```
저쪽 회차 하나가 2.8~5.8초다(아래 실측). 그보다 촘촘히 물어도 새 것이 없다 —
  늦게 따라오는 값은 주기가 아니라 저쪽의 응답 시간이다
번호가 그대로면 화면을 다시 안 그리므로 헛되이 도는 값은 요청 하나뿐이다.
  3초면 분당 20회이고 같은 기계의 FastAPI 라 부담이 없다
2초로 줄여도 사람 눈에 달라지는 것이 없고 요청만 1.5배가 된다
5초는 저쪽에서 답이 나온 뒤 화면이 멈춰 있는 구간이 길어 시연에서 걸린다
```

---

#### 화면 실측 다섯 — 시연 대본 그대로 (10:27:20~10:28:07)

저쪽에서 다섯을 5초 간격으로 넣는 동안 Streamlit 을 3초마다 돌렸다.
**왼쪽이 저쪽 답 문구, 오른쪽이 Streamlit 이 그린 것이다.**

```
1  "오송역 위치 보여줘"                     5.0초 · SELECT recipe_001
   저쪽      오송역 좌표를 조회했습니다.
             1. geo.geocode       오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1 (127.3277, 36.6200)
   Streamlit 같은 두 줄 · caption "10:27:27  SELECT"
             강조 경로  말한 장소 -> 장소 좌표 변환

2  "오송역 CCTV 보여줘"                     4.0초 · SELECT recipe_020
   저쪽      오송역 CCTV 를 조회했습니다.
             1. geo.geocode       오송역 → … (127.3277, 36.6200)
             2. road.getCctv      minLon=127.1598 · minLat=36.4852 · maxLon=127.4956 · maxLat=36.7547  83건
   Streamlit 같은 세 줄 · 강조 경로  말한 장소 -> 장소 좌표 변환 -> CCTV 조회
             ★ 83건. 대본이 적어 둔 그 숫자다

3  "오송역 인구 구성 알려줘"                 3.4초 · SELECT recipe_038
   저쪽      오송역 연령대별 인구 구성을 조회했습니다.
             1. geo.geocode       오송역 → … (127.3277, 36.6200)
             2. adminBoundary.findBoundaryByPoint  lon=127.3277 · lat=36.6200  3건 · 충청북도
             3. population.getAgeProfile  level="sigungu" · code="43113"  청주시 흥덕구 292,625명 (남 149,484 · 여 143,141) · 2026-06-30 기준
   Streamlit 같은 네 줄 · 강조 경로
             말한 장소 -> 장소 좌표 변환 -> 지점 행정구역 판별 -> 연령별 인구 구성 조회
             ★ 대본은 "되묻기 → 골라서 3단계" 인데 이번 세 판에서 모두 곧장
               SELECT 로 3단계까지 갔다. 되묻기가 안 났다. 되묻기 길은 아래에서
               따로 쟀다

4  "오송역 근처 충전소 자세히 알려줘"          3.1초 · SELECT recipe_040
   저쪽      오송역 충전소 상세를 조회했습니다.
             1. geo.geocode       오송역 → … (127.3277, 36.6200)
             2. ev.searchStations  minLon=127.1598 · … · maxLat=36.7547  500건 (전체 2,173건) · 청주오송역차고지
             3. ev.getStation     statId="PK000313"  8건
   Streamlit 같은 네 줄 · 강조 경로
             말한 장소 -> 장소 좌표 변환 -> 전기차 충전소 검색 -> 충전소 상세 조회
             ★ 대본의 4단계. 하이라이트가 네 칸이다

5  "문서에서 철도안전법 관련 내용 찾아줘"       3.6초 · SELECT recipe_013
   저쪽      철도안전법 문서를 조회했습니다.
             1. knowledge.query   6건
                「철도안전법(법률)(제21188호)(20260303).pdf」 1쪽 · "법제처 1 국가법령정보센터 철도안전법 …"
                「…」 2쪽 · "법제처 2 국가법령정보센터 철도안전법 제3조(다른 법률과의 관계) …"
                「…」 32쪽 · "④ 제1항부터 제3항까지에서 규정한 사항 외에 철도안전 자율보고에 …"
   Streamlit 같은 네 줄. 조각 셋이 그대로 따라 나온다
             강조 경로  말한 키워드 -> 문서 검색
```

**다섯 다 따라 나왔다.** 답 문구와 단계 줄이 저쪽과 글자까지 같다 — 다시
만들지 않고 그대로 담기 때문이다.

#### 되묻기와 고르기 (10:28:22~10:28:38)

대본 3번이 되묻기를 안 냈으므로 「서른넷째」가 쓰던 발화로 따로 쟀다.

```
"청주시 인구 알려줘"   2.8초 · CLARIFY · 후보 넷
   저쪽      여러 가지로 해석됩니다. 어느 것을 보시겠습니까?
               1  인구 통계 조회
               2  장소 좌표 변환 -> 인구 통계 조회
               3  연령별 인구 구성 조회
               4  인구 변화 추이 조회
   Streamlit 같은 목록 · caption "10:28:30  CLARIFY"
             ★ 그래프에 후보 넷이 한꺼번에 강조된다
               말한 키워드 -> 인구 통계 조회
               말한 장소 -> 장소 좌표 변환 -> 인구 통계 조회
               말한 장소 -> 장소 좌표 변환 -> 지점 행정구역 판별 -> 연령별 인구 구성 조회
               말한 장소 -> 장소 좌표 변환 -> 지점 행정구역 판별 -> 인구 변화 추이 조회

"2번"                 0.4초 · CHOICE · recipe_029
   저쪽      고르신 것 — 2 장소 좌표 변환 -> 인구 통계 조회
             청주시 인구 통계를 조회했습니다.
             1. geo.geocode       청주시 → 충청북도 청주시 상당구 남일면 단재로 480 (127.5059, 36.5883)
             2. population.searchStatistics  1건 · 청주시 상당구 193,079명 (남 95,877 · 여 97,202) · 2026-06-30 기준
   Streamlit 같은 네 줄 · caption "10:28:38  CHOICE"
             강조 경로  말한 장소 -> 장소 좌표 변환 -> 인구 통계 조회
             ★ 넷이던 강조가 고른 하나로 좁혀진다
```

**되묻기도 고른 뒤도 따라 나온다.** 고르기 회차는 해석을 안 거치므로 축 셋이
비어 있고 recipe 는 `run` 자리에서 온다.

#### 따라오는 데 걸린 시간

저쪽 답이 도착한 때부터 3초 주기 조회가 그것을 본 때까지다.

```
판1 (10:23~10:24 · 다섯)   3.00 · 3.00 · 3.00 · 2.99 · 3.00 초
판2 (10:27~10:28 · 다섯)   0.07 · 3.00 · 3.00 · 2.99 · 3.00 초
되묻기 · 고르기            0.22 · 2.79 초
```

**0~3초이고 상한이 주기 하나다.** 3.00 이 자주 나오는 것은 우연이 아니다 —
vendor 실행기가 도구를 부르며 `await` 하는 동안 이벤트 루프가 풀려 대기 중인
조회가 **회차 도중에** 응답을 받고, 그러면 다음 조회까지 꼬박 한 주기를
기다린다. 발화 간격을 5초로 두면 위상이 고정돼 3.00 이 반복된다.

**★ 조회가 회차 뒤에 줄을 선다.** `resolve_service.resolve` 는 동기 호출이라
LLM 을 부르는 동안 uvicorn 의 이벤트 루프를 막는다. 그 사이에 들어온
`GET /recent` 는 루프가 풀릴 때까지 기다린다(측정 중 실제로 최대 5.2초까지
밀렸다 — 발화를 1초 간격으로 몰아쳤을 때다). 이 작업이 만든 것이 아니라
서버가 원래 그렇다. **워커를 늘려 피할 수 없다** — 회차 기억이 프로세스
메모리라 워커가 둘이면 반씩 갈린다.

#### 깜빡임 정도 — 사람이 판단할 재료

```
주기마다 도는 것    부분 갱신 조각 하나. GET /recent 한 번과 caption 한 줄뿐이다
                    상단 그래프 iframe · 하단 그래프 iframe 을 안 건드린다
                    번호가 그대로면 그리는 것이 caption 한 줄이고 값도 같다
새 회차가 올 때만   전체 rerun 한 번. 그때 iframe 둘이 다시 붙는다
                    줌 배율은 sessionStorage 라 남는다(zoom.py)
전체 rerun 한 번    0.27 ~ 3.29초 (실측 일곱 장면)
                    대부분이 POST /render 다. 같은 후보를 다시 그리면 0.27초
```

**그래프는 좌표가 고정이라 다시 그려도 모양이 안 바뀐다.** 실측했다.

```
장면 일곱(recipe_001 · 020 · 038 · 040 · 013 · 029 · 되묻기 넷)
노드 45개 · 움직인 노드 0개
path 의 첫 좌표와 <text> 의 x · y 를 plain 렌더와 견줘 전부 같다
```

`layout.json` 과 `neato -n` 이 지키는 그것이다. 움직였다면 배치를 건드린 것이다.

**★ 브라우저로는 안 봤다.** 깜빡임과 스크롤 · 줌 유지는 사람이 브라우저에서
봐야 안다. 이 판정은 무인 실행에서 못 했다. 위의 숫자(주기 3초 · 새 회차마다
전체 rerun 0.27~3.29초 · 노드 이동 0)는 그 판단의 재료일 뿐이다.

#### 저쪽 화면 쪽이 느려지지 않았는가

**이벤트를 하나도 안 바꿨다.** `watched` 는 받은 것을 그대로 다시 내는
껍데기이고, 저쪽이 읽는 `step_start` · `step_end` · `result` · `[DONE]` 의
수와 차례와 내용이 같다.

기록에 드는 값을 쟀다(회차 2000건 × 2판, 단계 셋 · commands 500칸).

```
감싸지 않음   3.0ms   회차 하나 1.5마이크로초
감쌈(기록)   15.5ms   회차 하나 7.7마이크로초
차이                  회차 하나 6.2마이크로초
```

`/chat` 한 건이 2.8~5.8초이므로 **50만분의 1이다.** 실측 열두 회차에서
`/chat` 응답 시간이 늘어난 자리는 없다(0.2~5.8초, 앞선 측정 기록과 같은 범위).

#### raw JSON 이 안 샜다

저쪽 답의 `commands` 에는 geojson 이 통째로 들어 있다(실측).

```
{"op": "map.draw", "args": {"layerId": "mcp-place", "features": [{"type": "Feature",
 "geometry": {"type": "Point", "coordinates": [127.3276666158151, 36.61995281117007]}, …
{"op": "view.camera.flyTo", "args": {"bbox": [[127.3197, 36.612], [127.3357, 36.628]], …
```

회차 다섯을 담은 `/recent` 응답 전문(4091바이트)에서 아래 낱말이 하나도 안
나온다.

```
geojson · coordinates · geometry · FeatureCollection · features · cctvUrl ·
rtsp:// · "op" · flyTo · map.draw
```

**답 문구에 나오는 `(127.3277, 36.6200)` 은 다르다.** `workflow_answer` 가
소수 넷째 자리로 잘라 적는 지점 하나이고 저쪽 화면에도 이미 그대로 나가는
문자열이다. 좌표 배열이 아니다.

시험 둘이 이것을 막는다 — `tests/demo/api/test_recent.py` 가 geojson 을 실은
`commands` 로 회차를 만들어 `/recent` 응답 전문을 뒤지고,
`tests/demo/ui/test_follow_panel.py` 가 화면에 그릴 문자열을 같은 낱말로
뒤진다.

#### 따라 보기를 끄면

```
체크박스가 꺼지면 조각을 아예 안 만든다. 주기 요청이 안 나간다
패널도 안 그린다. 발화 넣기 · 샘플 고르기 · 노드 등록 흐름에 손댄 것이 없다
마지막으로 그린 장면(강조)은 남는다 — Run 으로 만든 장면이 남는 것과 같다
```

**조각을 화면 맨 끝에서 부른다.** 앞쪽에서 부르면 조각이 거는 전체 rerun 에
Run 클릭이 삼켜져 발화가 안 들어간다. `demo/ui/main.py` 는 왼쪽 칸에 빈
컨테이너(`follow_slot`)만 잡아 두고 맨 끝에서 채운다.

#### 관문 여덟

```
저쪽 무결          KRRI_ASAP 추적 파일 0줄. git diff --stat 빈 출력
판정 무손상        check_resolve --runs 3 (10:29:36~10:41)
                   적중 62/93 · 근접 24 · 빗나감 4 · 못 붙음 3 (67%)
                   LLM 단독 61/93 (66%) · 조회 빠짐 13 · 못 붙음 3
                   ★ 「서른넷째」 기준선과 여섯 칸이 전부 같다
                   ★ 단독으로 돌렸다. 화면 실측을 끝내고 다른 Ollama 요청이
                     없는 것을 확인한 뒤에 걸었다
check_wiring       recipe 40 · 완비 39 · STEP_OF 35줄 · A 0 · B 0 · C 1
                   C 는 web_fetch × 웹 주소. 기준선 그대로다
pytest             297 passed / 1 failed  (기준선 266/1 + 새 시험 31)
                   실패 1건은 graphviz 음성 대조군. 기존 삭제 0
raw JSON 안 샘     /recent 응답 전문과 화면 문자열에 위 열 낱말이 0회. 시험 둘이 막는다
따라 보기를 끄면    조각을 안 만든다. 주기 요청 0. 기존 흐름 무손상
새 의존성 없음      requirements.txt 새 줄 0. import 는 re · time · collections ·
                   contextvars 넷 다 표준 라이브러리다. 부분 갱신은 streamlit
                   1.62 내장 st.fragment 이고 화면 실측도 내장 AppTest 로 했다
노드 안 움직임      장면 일곱 · 노드 45 · 움직인 노드 0
```

#### 안 한 것

- **반대 방향을 안 만들었다.** Streamlit 에서 넣은 발화가 저쪽 화면에 뜨게
  하려면 저쪽이 우리에게 물어보거나 우리가 저쪽에 밀어 넣어야 하고, 둘 다
  저쪽 코드를 고치는 일이다.
- **저장을 안 한다.** 메모리뿐이다. 서버를 다시 띄우면 회차가 사라지고 번호가
  0 으로 되감긴다. 화면은 번호가 되감긴 것을 알아채고 본 번호를 지운다.
- **여러 사람이 동시에 쓰는 것을 안 본다.** 아래 「열린 과제」에 남겼다.
- **브라우저에서 사람이 눌러 보지 않았다.** 위 「환경」에 적은 대로 저쪽은
  HTTP 길로, 이쪽은 AppTest 로 몰았다. 픽셀과 DOM 은 리허설에서 본다.
- **깜빡임을 눈으로 안 봤다.** 위의 「다시 그리는 것이 무엇이고 몇 초 걸리나」
  가 사람이 판단할 재료다. 거슬리는 정도인지는 사람이 화면을 봐야 안다.
- **`/chat` 응답 시간을 A/B 로 안 쟀다.** 기록을 뺀 서버를 따로 띄워 견주지
  않았고, 대신 기록에 드는 값을 직접 쟀다(6.2마이크로초).
- **조회가 회차 뒤에 줄 서는 것을 안 고쳤다.** 고치려면 `resolve_service` 를
  async 로 바꿔야 하고 그것이 이번에 안 건드리기로 한 파일이다.
- **`check_argument.py` · `check_wiring.py` 의 표를 안 늘렸다.** 따라 그리기는
  발화 해석 밖의 일이라 잴 칸이 없다.

---

### 2026-08-27 (서른일곱째) · 같은 파일로 재도 값이 갈리는 원인을 좁힌다 — 재기만 했다

모델 qwen3:32b(서버 기본 · temperature 0 · seed 0) · recipe 40 · 무인 실행 ·
발화 31 × 3회 × 7판(09:15~09:50). **제품 코드 변경 0줄. 커밋 없음.**
`ontology/ontology.yaml` 만 V0(원문)·V2(어젯밤 문안 B)로 갈아 끼웠고 매 판
끝에 되돌렸다 — 끝난 뒤 `git status --short` 빈 출력, 두 파일 md5 가 시작 전과
같다(`deb62412…` · `5a70b813…`). 정답표·판정 규칙·recipe 40·menu 3322 무손상
(매 판 첫 줄 `_init (recipe 40)` 확인). `rebuild_init` · `reset_to_init` 안 불렀다.
ollama 설정은 읽기만 했다 — 실험 셋의 모델 내리기(`keep_alive: 0`) 한 번이
유일한 쓰기다. 표 전문은 `tools/probe_out/check_resolve.잡음-2026-08-27/`
(A1~A5 · B1 · B2 · progress.log).

**왜 쟀나.** 「서른여섯째」에서 같은 파일(V0)로 세 번 잰 것이 두 값으로 갈렸다.
연달아 잰 것끼리는 같고 사이에 다른 프롬프트가 끼면 갈린다는 규칙성이 보였다.
그것이 진짜인지, 모델을 내리면 돌아오는지를 좁힌다. 고치지 않는다.

#### 환경 (재기 전에 적었다)

```
ollama            0.32.15 (pid 72585 · 2026-08-22 00:51 부터 떠 있음 · systemd · user ollama)
uvicorn(8000)     .venv python -m uvicorn demo.api.main:app --reload (pid 1226460)
모델              qwen3:32b · 28 GB · 100% GPU(GPU0) · CONTEXT 32768 · UNTIL 2h
                  ★ 09:13:51 에 이 실험이 아닌 외부 /resolve 호출 한 번으로 적재돼 있었다
                    (journal: cached n_tokens 0→1024→2048→3072, 처음부터 계산)
환경변수          /proc/<pid>/environ 은 권한으로 못 읽었다. 대신 ollama 가 시작할 때
                  journal 에 찍은 "server config" 를 읽었다 (2026-08-22 00:51:21):
                  OLLAMA_NUM_PARALLEL=1  ← 1 이다. 배칭 후보는 지워진다
                  OLLAMA_MAX_LOADED_MODELS=0  OLLAMA_KEEP_ALIVE=5m (앱이 요청마다 2h 로 덮음)
                  OLLAMA_FLASH_ATTENTION=false  OLLAMA_KV_CACHE_TYPE=(빈값)  OLLAMA_NUM_THREAD=(없음)
                  OLLAMA_CONTEXT_LENGTH=0  OLLAMA_SCHED_SPREAD=false  OLLAMA_DEBUG=INFO
llama-server      -c 32768 -np 1 -b 1024 -ub 1024 --flash-attn auto --split-mode none
                  --main-gpu 0 --context-shift --keep 4
                  "prompt cache is enabled, size limit: 8192 MiB" · "context checkpoints enabled"
GPU               RTX PRO 6000 Blackwell ×4 (97887 MiB 각) · driver 595.71.05 · 실험 전 전부 유휴
sampler(journal)  temp = 0.000 · top_k 20 · top_p 0.95 · seed 는 로그에 안 찍힘
```

**「열넷째」 후보 셋 중 ollama 버전은 지금 0.32.15 다.** 0.32.14 로 되돌려 재지는
않았다.

#### 실험 하나 · 끼어드는 것이 원인인가 — ★ 맞다

`--runs 3` · 이 순서로 연달아. 같은 표 = 적중 표 네 칸(적중·근접·빗나감·못
붙음)이 31 발화 전부 같음. (어젯밤은 발화 수를 9 로 적었는데 다시 세니 V0/V0'
는 **11개**(13 · 14 도 다름)다. 아래 발화 수는 전부 네 칸 기준으로 다시 셌다.)

```
      파일  적중    근접  빗  못   조회빠짐  기준선       첫 요청이 물려받은 KV 접두사(journal cached n_tokens)
A1    V0    62/93   24    4   3    13       21/27       3458  (09:13 외부 호출이 처음부터 계산한 것)
A2    V0    62/93   24    4   3    13       21/27       3458  = A1  바이트까지 같다
B1    V2    64/93   16   13   0    15       22/27       2485
A3    V0    61/93   31    1   0    10       20/27       2485  ★ A1 과 발화 11개 다르다
B2    V2    64/93   16   13   0    15       22/27       2485  = B1  같다 (사이에 A3 이 끼었는데도)
A4    V0    61/93   31    1   0    10       20/27       2485  = A3  같다
```

미리 정한 읽는 법 — **「A1=A2 이고 A3 이 다르다 → 끼어드는 프롬프트가
원인이다」 쪽이 나왔다.** A1≠A2 (그냥 흔들림) 는 아니었다.

A1 vs A3 에서 갈린 발화 (11) — 6 · 7 · 9 · 11 · 13 · 14 · 17 · 22 · 27 · 29 · 31.
그중 발화 27 은 못 붙음 3 → 근접 3, 17 은 빗나감 3 → 근접 2 · 적중 1,
9 · 31 은 적중 3 → 근접 3, 7 은 근접 3 → 적중 3. **판정이 셋 중 어느 칸으로든
넘어간다** — 좋아지는 쪽으로도 나빠지는 쪽으로도.

**어젯밤 표와 잇대면** — A1 · A2 는 어젯밤 첫 V0 와 바이트까지 같고, B1 · B2 는
어젯밤 첫 V2 와 바이트까지 같다. A3 · A4 는 V0' 와도 다르다(9개). 즉 **같은
파일(V0)이 지금까지 세 가지 표**로 나왔다 — {V0, A1, A2, A5} · {V0', V0''} ·
{A3, A4}. V2 는 두 가지 — {V2, B1, B2} · {V2''}.

#### 실험 둘 · 안 했다

실험 하나가 「A1=A2=A3=A4」 가 아니어서 조건에 안 맞는다. 다만 A1=A2 · A3=A4 ·
B1=B2 가 **「호출 횟수만으로는 안 갈린다」** 를 이미 보인다 — 같은 파일을 93
호출씩 이어 재도 표가 그대로였다.

#### 실험 셋 · 모델을 내렸다 올리면 되돌아오는가 — ★ A1 로 돌아온다

A4 뒤 `POST /api/generate {"model":"qwen3:32b","keep_alive":0}` (응답
`done_reason: unload`) → 30초 뒤 `ollama ps` 빈 출력 확인 → A5 V0.

```
A5    V0    62/93   24    4   3    13       21/27       0 → 1024 → 2048 → 3072 (재적재 · 처음부터 계산)
```

**적중 표 네 칸이 A1 과 31 발화 전부 같다.** 다른 것은 「LLM 단독」 칸의 발화
1 · 2 뿐이다(1: 3/3→1/3 · 2: 0/3→2/3 — 적재 직후 첫 요청들이고 최종 판정은
같다). 미리 정한 읽는 법 — **「A1 로 돌아온다 → 상태가 ollama 안에 쌓이는
것이다. 내리면 지워진다」** 쪽이 나왔다.

「열넷째」의 "모델을 내렸다 올리는 것은 답을 안 바꾼다" 는 그때 끼어든
프롬프트가 없었으니(처음부터 계산한 상태 = 지금의 A1 계열) 모순이 아니다.
**내렸다 올리면 언제나 같은 표(A1 계열)로 온다** 는 쪽이 오히려 그 관찰과 맞는다.

#### 무엇이 쌓이는가 — 후보 하나. 확인은 안 했다

journal(`--log-verbosity 4`)에 요청마다 `slot get_availabl: selected slot by LCP
similarity` · `cached n_tokens = N, memory_seq_rm [N, end)` 가 찍힌다. 요청은
앞 요청과 공통인 접두사 N 토큰의 KV 를 그대로 물려받고 나머지만 새로 계산한다.
프롬프트 ~3480 토큰 중 발화만 다른 요청끼리는 3458~3478 을 물려받고, V0↔V2 는
축 설명이 갈리는 자리 2485 까지, V0↔V6 은 2271 까지 물려받는다.

그래서 **공통 접두사의 KV 값이 "언제 · 몇 토큰짜리 배치로" 계산됐느냐**가 판
전체에 상속된다. 처음부터 계산하면 1024 단위 배치(A1 · A5 · 어젯밤 V0). V2 뒤에
V0 를 재면 2485 부터 995 토큰을 한 배치로 다시 계산한 값이 이후 93 요청 전부의
접두사가 된다(A3). 어젯밤 V0' 는 V6 뒤라 2271 부터였다. temperature 0 이라
logits 의 부동소수 차이가 그대로 argmax 를 바꾸고, 그 접두사가 판 내내 같으니
판 안에서는 재현되고 판 사이에서는 갈린다 — 관찰과 전부 맞는다.
**다만 맞지 않는 관찰도 하나 있다** — B2(A3 뒤 · 2485)와 어젯밤 V2''(V0'' 뒤 ·
2485)는 물려받은 토큰 수가 같은데 표가 다르다(7개). 물려받은 구간의 값이
어떤 이력으로 계산됐느냐까지 들어가면 설명은 되지만 재보지는 않았다.
**후보로 적는다. 확인하려면 llama-server 의 프롬프트 캐시를 끄고(`--cache-ram 0`
에 해당) 재야 하는데 그것은 ollama 설정 변경이라 이번 규칙 밖이다.**

이 후보가 맞다면 「열넷째」의 남은 후보(ollama 재시작 · uvicorn 재시작 · 버전)는
전부 **"그때 접두사 캐시가 어떤 이력이었나"** 하나로 모인다 — 2026-08-15 표는
그 시점의 캐시 이력을 모르므로 재현할 수 없는 것이 정상이다.

#### ★ 잡음의 폭 — 같은 파일 재측정끼리

```
V0   A1 vs A3         발화 11개   적중 62→61   근접 24→31   빗나감 4→1    조회 빠짐 13→10   기준선 21→20
     V0 vs V0'(어젯밤) 발화 11개   적중 62→59   근접 24→27   빗나감 4→7    조회 빠짐 13→12   기준선 21→17
     A3 vs V0'        발화  9개   적중 61→59                빗나감 1→7    조회 빠짐 10→12
V2   V2 vs V2''(어젯밤) 발화 7개   적중 64→59   근접 16→18   빗나감 13→16  조회 빠짐 15→18   기준선 22→20
```

**폭 — 적중 ±3 · 빗나감 ±6 · 조회 빠짐 ±3 · 기준선 ±4 · 발화 7~11개.**
「서른여섯째」의 문안 순위(조회 빠짐 6 · 9 · 11 · 12 · 13 · 15 · 21)는 이웃끼리
이 폭 안이다.

#### 실험 넷 · 번갈아 재면 갈리는가 — 실험 하나의 A1 · B1 · A3 · B2 로

```
                V0 (A1 · A3)      V2 (B1 · B2)      차이(V2−V0)     같은 파일 잡음 폭
적중            62 · 61 = 61.5    64 · 64 = 64.0     +2.5           ±3 (V2 는 B1=B2 라 어젯밤 V2/V2'' 5 를 씀)
빗나감           4 ·  1 =  2.5    13 · 13 = 13.0    +10.5           ±6   ★ 폭을 넘는다
조회 빠짐       13 · 10 = 11.5    15 · 15 = 15.0     +3.5           ±3   경계
기준선 적중     21 · 20 = 20.5    22 · 22 = 22.0     +1.5           ±4
```

**읽을 수 있는 것은 빗나감 하나다** — V2(문안 B)는 V0 보다 빗나감을 열 번쯤
늘린다. 적중 · 조회 빠짐 · 기준선은 폭 안이라 이 방법(3회 × 2판)으로는 못 가른다.
단, 번갈아 재기의 전제("두 판이 독립된 표본")가 여기서는 안 선다 — B1=B2 처럼
같은 접두사 상태면 두 판이 표본 하나다.

#### 어젯밤 표를 다시 읽을 수 있는가

- **읽을 수 있는 것** — "방향이 일관된 자리" 여덟(4 · 7 · 28 · 30 · 17 · 12 ·
  27 · 6) 중 V0 계열 세 표(A1 · A3 · V0') 모두에서 안 움직인 것은 **4 · 28 둘뿐**
  이다. 나머지는 같은 파일 재측정에서 움직였다 — 7 (A3 에서 근접 3→적중 3) ·
  6 (A3 · V0' 에서 3/3→2/3) · 12 (V0' 에서 빗나감 1→3) · 17 (A1 빗3 · A3 근2 ·
  V0' 적3 — 세 표가 다 다르다) · 27 (못붙음 3 · 근접 3 · 빗나감 3 — 세 표가 다
  다르다) · 30 (V0' 에서 빗나감 1). ★ **어젯밤 "문안의 효과" 로 읽은 7 · 17 ·
  12 · 27 · 6 은 문안 없이도 그만큼 움직이는 자리다. 4 · 28 · 30(V4~V6 의
  want 변화는 3회 전부라 폭 밖) 만 남는다.**
- **빗나감의 큰 차이** — V1 16 · V2 13 · V2'' 16 vs V0 4 · V0' 7 · A3 1.
  폭 ±6 을 넘으니 "축 설명을 고친 V1 · V2 는 빗나감을 늘린다" 는 읽힌다.
- **읽을 수 없는 것** — 조회 빠짐 순위(V6 6 < V5 9 < V4 11 < V3 12 < V0 13 <
  V2 15)와 적중 순위. 이웃 간 차이가 전부 폭(±3) 안이다. V1~V6 은 각각 다른
  변형 뒤에 한 번씩 잰 것이라 **각 판이 어떤 접두사 상태였는지 모른다.**
  V5=V5'' 도 "V5 가 안정하다" 가 아니라 "둘 다 V2 계열 뒤(2271)에 쟀다" 는 뜻이다.
- **V0 · V2 첫 판은** 오늘 A1 · B1 과 바이트까지 같으니 "처음부터 계산한 상태"
  의 표로 봐도 된다.

#### 측정 규칙 후보 — 사람이 정한다. 확정하지 않았다

```
후보 1  변형마다 모델을 내렸다 올린다 (keep_alive 0 → 30초 → 재기)
        근거    A5 가 A1 과 판정 네 칸 전부 같다. 처음부터 계산한 상태가 재현된다
        비용    판마다 적재 ~10초 + 대기 30초. 첫 발화 1~2 의 LLM 단독 칸이 흔들린다
                (판정은 안 바뀜). 판 순서와 무관해진다
        한계    "처음부터 계산한 상태" 하나로 고정하는 것이지 잡음을 없애는 것이
                아니다 — 다른 상태(A3 · V0')가 더 맞는 표일 수도 있다
후보 2  같은 접두사 상태 안에서만 견준다 — V0 → V2 → V0 → V2 처럼 번갈아 재고
        같은 자리끼리만 비교
        근거    A3=A4 · B1=B2. 한 번 상태가 정해지면 이어 재는 동안 바뀌지 않는다
        한계    B1=B2 처럼 두 판이 같은 표본이 돼 잡음 폭을 못 잰다. 그리고 첫 판
                (A1)과 둘째 판(A3)이 다른 상태라 "번갈아" 자체가 상태를 바꾼다
후보 3  회차가 아니라 상태를 늘린다 — 변형마다 후보 1 로 한 판 + 다른 변형
        뒤에 한 판, 둘의 합집합을 표본으로 쓴다
        근거    잡음의 원천이 "회차" 가 아니라 "접두사 상태" 라 --runs 를 늘려도
                (A1=A2 처럼) 같은 값만 더 나온다. 상태를 바꿔야 표본이 는다
        한계    판 수가 두 배. 어느 상태가 "진짜" 인지 정의가 없다
```

**`--runs` 를 늘리는 것은 후보가 아니다** — 같은 상태에서는 93 호출을 더 해도
바이트까지 같다(A1=A2 · A3=A4 · B1=B2). 회차는 발화 단위 흔들림을 못 잡는다.

#### 안 한 것

- 실험 둘(조건 불충족). 실험 셋 뒤 V2 를 다시 재서 "내렸다 올리면 V2 도 B1 로
  오는가" 는 안 쟀다 — 시간상 V0 만 했다.
- 후보 메커니즘(접두사 KV 캐시 상속)의 확인 — `--cache-ram 0` · `OLLAMA_KV_CACHE_TYPE`
  등 ollama 설정을 바꿔야 해서 안 했다. B2 ≠ V2'' 가 후보에 안 맞는 것도 못 좇았다.
- ollama 0.32.14 로 되돌려 재기. `/proc/<pid>/environ` 직접 읽기(권한).
- `--runs` 를 늘린 재측정. 발화 수 늘리기. 커밋. 측정 규칙 확정.

### 2026-08-27 (서른여섯째) · 축 문안 여러 개를 밤새 재서 표만 만든다 — 고르지 않았다

모델 qwen3:32b(서버 기본 · temperature 0 · seed 0) · recipe 40 · 무인 실행 ·
발화 31 × 3회 × 변형 11판(01:03~01:58). **제품 코드 변경 0줄.** 두 파일
(`ontology/ontology.yaml` · `workflows/static/prompts/recipe_selection.md`)을
갈아 끼워 재고 **전부 되돌렸다** — 끝난 뒤 `git status --short` 빈 출력,
두 파일 md5 가 시작 전과 같다. 정답표(`check_resolve.UTTERANCES`)와 판정
규칙 · recipe 40개 · menu 무손상(변형마다 recipe 수 40 · menu 자수 3322 확인).
표 전문과 변형 파일 원본은 `tools/probe_out/check_resolve.축문안-2026-08-27/`.
**커밋 안 했다. 고르지도 않았다** — 표와 사실만 적는다.

**왜 쟀나.** 확장 표 조회 빠짐 13/66 의 뿌리가 축 셋이다. 「스물한째」는 문안
하나만 써보고 판단해 21점을 잃고 되돌렸다. 이번에는 「서른셋째」 방식대로 여러
문안을 같은 자로 재서 표를 만든다. 「스물한째」의 교훈대로 문안은 「그 값이
어떻게 쓰이는가」가 아니라 「발화에서 무엇을 말했는가」로 썼다.

#### ★ 제일 큰 사실 — V0 와 V0' 가 다르다. 같은 파일인데 발화 9개가 움직였다

대조군을 세 번 쟀다 — V0(01:03 처음) · V0'(01:37) · V0''(01:44 추가).

```
V0' 와 V0''   표 넉 장이 바이트까지 같다
V0  와 V0'    발화 9개가 다르다 (6 · 9 · 11 · 12 · 17 · 27 · 29 · 30 · 31)
              합계 적중 62/93 → 59/93 · 빗나감 4 → 7 · 조회 빠짐 13 → 12
              조회 판정이 갈린 발화 다섯 — 9(무→근) · 12(근2빠1→빠3) ·
              17(빠→근) · 27(무→빠) · 29(빠→적)
```

temperature 0 · seed 0(`llm_engine/ollama.py`)인데도 이렇다. 재현성 확인으로
V2 와 V5 도 한 번씩 더 쟀다(무엇을 왜 더 쟀는지 — 대조군이 흔들려서다).
**V5 재측정은 완전 동일, V2 재측정은 발화 7개가 달랐다**(8 · 15 · 17 · 22 ·
23 · 29 · 31. 빗나감 13→16, 기준선 22/27→20/27 — 기준선 발화 8 에 빗나감
2회가 새로 생겼다). 왜 어떤 판은 바이트까지 같고 어떤 판은 통째로 넘어가는지
원인(ollama 배칭·서버 상태)은 안 좇았다.

**그래서 이 표는 이렇게 읽어야 한다 — 같은 파일로 재도 발화 7~9개가 움직이는
밤이었다. 그 폭 안의 변형 간 차이는 변형의 효과로 읽지 말 것.** 축 표에서
방향이 일관되게 확인되는 자리(아래 「발화별」)만 문안의 효과다.

#### 문안 전문

V1 · 문안 A — 지금 첫 문장을 그대로 두고 뜻 한 문장을 덧붙였다.

```
말한 장소     사용자가 발화에서 지목한 장소. 지도에 한 지점으로 찍힌다.
말한 키워드   사용자가 발화에서 찾아 달라고 한 말. 여러 건이 걸릴 수 있다.
말한 식별자   사용자가 발화에서 집어 말한 코드나 정식 이름. 꼭 한 건을 집는다.
```

V2 · 문안 B — 첫 문장부터 다시 썼다. 셋이 서로 갈리게.

```
말한 장소     발화에서 지목한 곳의 이름. 지도에 한 지점으로 찍히는 장소다.
말한 키워드   발화에서 찾아 달라고 한 말. 같은 말이 든 여러 건이 걸릴 수 있다.
말한 식별자   발화에서 한 건을 집어 말한 정식 이름이나 코드.
```

V3 · 인자 예시 — 「구절을 통째로」에서 「찾을 대상을 가리키는 낱말만」으로.
말한 키워드 줄 하나를 이렇게 바꿨다 (장소·식별자 줄은 그대로).

```
전  말한 키워드 무엇을 찾는지. "국회의원 선거구 찾아줘" -> "국회의원 선거구"
후  말한 키워드 찾을 대상을 가리키는 낱말만. 대상의 종류를 나타내는 말은
                넣지 않는다. "청주 선거구 찾아줘" -> "청주"
                "교통 공약 많은 선거구 검색해줘" -> "교통"
```

V4 · want 설명 — `item_list` · `item_detail` 둘.

```
목록        전  조건에 맞는 항목들.
            후  조건에 맞는 여러 항목. 몇 개가 나올지 발화가 정하지 않는다.
항목 상세   전  한 항목의 자세한 내용.
            후  발화에서 집어 말한 한 항목의 자세한 내용.
```

V5 = V2 + V3 (V1·V2 중 V2 를 고른 근거 — 조회 빠짐 15<21 · 빗나감 13<16).
V6 = V5 + V4.

#### 도구 독립성 점검 — 전부 O

- 특정 도구·도메인·데이터셋 이름이 문안에 없다. 축 설명에 든 명사는
  발화·지도·이름·코드뿐이다. V3 의 예시 두 개는 실측 발화에서 왔지만
  가르치는 규칙("종류 말은 뗀다")은 도메인 무관하다 — 기존 예시도 같은
  방식이었다.
- 축 셋의 뜻이 도구 목록을 몰라도 성립한다 — "지도에 한 지점" · "여러 건" ·
  "한 건" 은 도구가 늘어도 그대로다.

#### 변형 × 묶음 표

기준선 아홉 27회 · 확장 스물둘 66회. 재측정은 ″ 로 적는다.

```
        ★조회빠짐      기준선 아홉            확장 스물둘            합계          LLM   검산       축자수/
        기준/확장/합   적중   근접 빗 못      적중   근접 빗 못      적중    빗    단독  살림/덮음  채운프롬프트
V0      0 / 13 / 13    21/27  6    0  0       41/66  18   4  3       62/93   4     66%   10/9       734/6134
V1      3 / 18 / 21    22/27  3    2  0       38/66  14  14  0       60/93  16     59%   11/6       780/6180
V2      3 / 12 / 15    22/27  2    3  0       42/66  14  10  0       64/93  13     58%   15/5       774/6174
V2″     3 / 15 / 18    20/27  2    5  0       39/66  16  11  0       59/93  16     55%   13/5       774/6174
V3      3 /  9 / 12    21/27  3    3  0       41/66  19   6  0       62/93   9     62%    7/3       734/6218
V4      3 /  8 / 11    18/27  6    3  0       42/66  18   6  0       60/93   9     59%   11/6       769/6169
V5      3 /  6 /  9    19/27  5    3  0       40/66  23   3  0       59/93   6     57%   12/6       774/6258
V5″     V5 와 표 넉 장이 바이트까지 같다
V6      3 /  3 /  6    15/27  9    3  0       38/66  24   4  0       53/93   7     44%   15/3       809/6293
V0'     0 / 12 / 12    17/27 10    0  0       42/66  17   7  0       59/93   7     56%   12/5       734/6134
V0''    V0' 와 표 넉 장이 바이트까지 같다
```

조회 빠짐의 발화 구성 —

```
V0   17(3) 28(3) 29(3) 30(3) 12(1)          V4   4(3) 12(3) 17(2) 28(3)
V1   4(3) 12(3) 17(3) 28(3) 29(3) 30(3) 31(3)   V5   4(3) 28(3) 29(3)
V2   4(3) 12(3) 17(3) 28(3) 30(3)           V6   4(3) 28(3)
V3   4(3) 12(3) 28(3) 30(3)                 V0'  12(3) 27(3) 28(3) 30(3)
```

#### 발화별로 움직인 것 — 방향이 일관된 자리와 소음 자리를 가른다

**축 표에서 방향이 일관된 자리** (여러 변형·재측정에서 같은 쪽으로 감).

```
4  청주시 인구 구성 (기준선)   ★ 문안을 어느 쪽이든 고치면 given 이 place→keyword 로
                              뒤집혀 조회 빠짐 3 이 된다. V1~V6 전부다. 인자 예시만
                              고친 V3 에서도 뒤집혔다. 「스물한째」와 같은 자리 재현.
                              V0 · V0'(원문)에서만 place 로 남는다
7  전기차 충전소 데이터 (기준선)  축 설명을 고친 V1 · V2 · V4 에서 given null 이
                              keyword 로 채워져 근접 3 → 적중 3/3
28 경부선 노선                모든 변형에서 조회 빠짐 3 그대로 — 문안으로 안 움직였다
30 오송역 선거구              want 를 고친 V4 · V5 · V6 에서 3회 전부 want 가
                              item_list→item_detail 로 가 빠짐 3 → 근접 3.
                              최종 적중까지는 못 갔다 (V4 만 2/3)
17 청주 국회의원 공약         예시를 고친 V3 · V5 · V6 에서 keyword·"청주" 가 돼
                              빠짐이 풀린다 (V3 · V5 적중 3/3). 다만 원문 V0' 도
                              keyword 로 갔다 — 문안 없이도 넘어가는 자리다
12 청주시 행정경계            V5 · V6(문안 B+예시)에서만 keyword·"청주" 적중 3/3.
                              축만 고치면(V2) place 로, 예시만 고치면(V3) place ·
                              "청주" 로 가 빠짐이다 — 둘이 같이 있어야 풀렸다
27 철도안전법                 문안을 고친 변형들에서 축 null(V0 · NO_MATCH 3)이
                              keyword·item_list 로 채워져 조회 근접이 되나,
                              최종은 빗나감 3 이다 (V0' 은 web_address · 빠짐)
6  국회의원 선거구 (기준선)   축을 고친 변형들에서 적중 3/3 → 0~1/3 (근접 CLARIFY).
                              V0' 도 2/3 라 일부는 소음이다
```

**소음으로 갈린 자리** — 대조군·재측정끼리도 갈렸다. 변형 탓으로 읽지 말 것.

```
29 청주시 인구      keyword 로 간 회차는 적중, place 로 간 회차는 빠짐.
                    V0 빠→V0' 적 · V2 적→V2″ 빠 · V5 빠 — 변형과 무관하게 들락거린다
9 · 11 · 13 · 14 · 15 · 22 · 23 · 31   변형뿐 아니라 같은 파일 재측정(V0/V0' · V2/V2″)
                    에서도 움직였다
8  철도 안전 문서 (기준선)   V2″ 에서만 빗나감 2회 — 재측정에서 생겨 문안 탓인지 모른다
```

#### 고르는 기준 순서로 늘어놓기만 한다 (아침에 사람이 본다)

```
0순위 도구 독립성   전부 O
1순위 조회 빠짐     V6 6 < V5 9 < V4 11 < V3 12 = V0' 12 < V0 13 < V2 15 < V1 21
                    단, 문안을 고친 모든 변형이 기준선 발화 4 에 빠짐 3 을 새로 만든다
2순위 빗나감        V0 4 < V5 6 < V0' 7 = V6 7 < V3 9 = V4 9 < V2 13 < V1 16
3순위 적중          V2 64 > V0 62 = V3 62 > V1 60 = V4 60 > V5 59 = V0' 59 > V6 53
4순위 기준선 아홉   V1 · V2 22/27 > V0 21 = V3 21 > V5 19 > V4 18 > V0' 17 > V6 15
                    빗나감 0 은 V0 · V0' 뿐이다 (문안 변형은 전부 발화 4 에서 빗나간다)
```

읽히는 맞바꿈 — 조회 빠짐을 줄이는 쪽(V5 · V6)은 근접(되묻기)이 늘고 적중이
준다. V6 은 빠짐이 6(그중 3 은 배선 문제인 28, 3 은 새로 만든 4)까지 주는
대신 적중 53/93 · LLM 단독 44% 로 제일 낮다.

#### 안 한 것

- 회차가 3 이라 한 발화 표본이 셋뿐이다. 소음 바닥(같은 파일에서 발화 7~9개)이
  변형 효과와 같은 자릿수인데 회차를 늘려 다시 재지는 않았다.
- V1 + 예시 조합은 안 쟀다 — V5 의 밑은 기준(빠짐·빗나감)대로 V2 를 썼다.
- 인자 예시의 다른 문안(예시 수를 늘리지 않는 한 줄짜리)은 안 만들었다.
- V0 첫 판만 왜 다른지(서버·ollama 상태) 원인은 안 좇았다.
- `rebuild_init` · `reset_to_init` 안 불렀다. 서버 재시작 없음. 커밋 없음.

### 2026-08-27 (서른다섯째) · 축 작업에 필요한 사실 셋을 미리 잰다 — 재기만 했다

모델 qwen3:32b(서버 기본) · recipe 40 · 무인 실행.
**제품 코드 변경 0줄.** 고친 파일은 `tools/check_argument.py`(재는 도구) 하나다.
응답 전문은 전부 `tools/probe_out/*-2026-08-27.json` 에 있다. **커밋 안 했다.**

**왜 미리 쟀나.** 다음 작업 「축 한 묶음」이 프롬프트를 건드리는 큰 작업이라,
재는 것과 고치는 것을 섞으면 무엇이 무엇을 고쳤는지 못 가린다. 그래서 필요한
사실 셋만 먼저 모았다. **결론은 안 낸다. 어느 길로 갈지는 사람이 정한다.**

#### 물음 1 · 철도 노선 조회는 어느 칸으로 받아야 하는가

`geo.getRailwayLines` 를 Gateway 로 칸별로 눌렀다 (각 1회).

```
stationName="오송역"                     31건 (노선 15 · 역 16)   ← 대조군. 지금 되는 것
railwayName="오송역"                      0건
stationName="경부선"                      0건                     ← 화면 실측(2026-08-26) 재현
railwayName="경부선"                    224건 (노선 130 · 역 94)
둘 다 "경부선" · "경부선"                 0건                     ← ★ AND 로 묶인다
둘 다 stationName="오송역" · railwayName="경부선"
                                          6건 (노선 2 · 역 4)     ← 교집합이다
아무것도 안 보냄                       3683건 (노선 2243 · 역 1440) ← 전국 전부
stationName=""(빈 문자열)              3683건                     ← 안 보낸 것과 같다
```

**어느 칸이 걸렸는지의 근거는 저쪽 코드다** (읽기만 했다).
`ASAP-mcp/app/services/rail_repository.py` `_build_section_filters` —
`railwayName` 은 `r_name_1·2·3 ILIKE %…%`, `stationName` 은
`f_name·t_name·f_name_base·t_name_base ILIKE %…%`, **둘 다 보내면 두 절이
AND** 다. 이기는 칸이 없다 — 둘 다 만족해야 나온다. 절이 하나도 없으면
WHERE 없이 전부다. 빈 문자열은 falsy 라 절이 안 만들어진다.

**지금 응답은 PostGIS 경로다.** 둘 다 보냈을 때 교집합(6건)으로 준 것이
증거다 — 파일 폴백(`app/tools/geo.py:152-160`)은 railway/station 매치가
**OR** 라, DB 가 죽어 폴백으로 떨어지면 「둘 다 보낸다」의 결과가 달라진다.

적어 뒀던 세 길은 이 표에서 이렇게 된다. **고르지 않았다.**

```
가) 타입을 가른다      열려 있다   맞는 칸 하나만 보내면 된다 — railwayName="경부선" 224건
나) 값을 보고 고른다   열려 있다   같은 근거. 값이 노선명인지 역명인지 아는 것이 전제다
다) 둘 다 보낸다       ★ 닫혔다   AND 라 값이 한쪽 칸에만 맞으면 0건이다 ("경부선"·"경부선" → 0)
```

덧붙일 사실 하나 — **인자 없이 부르면 전국 3683 features(14.7MB)가 통째로
온다.** 인자를 못 뽑은 채 이 도구를 부르는 길은 어느 안에서든 막아야 할 자리다.

#### 물음 2 · 진짜 행정구역 경계를 넘기면 나아지는가

`geo.geocode("청주")` → 점 `[127.5115, 36.5825]` · 주소 "충청북도 청주시
상당구 남일면 효덕길 14-5" · bbox 1.1km. **"청주" 가 시내가 아니라 상당구
남쪽 끝(남일면)의 주소 한 곳에 떨어진다** — 보은군 첫 결과의 절반은 이것이다.

`adminBoundary.findBoundaryByPoint(그 점)` → items 3건에 **진짜 bbox 가
들어 있다** (다시 확인).

```
sido     충청북도       43        [127.2757, 36.0124, 128.6521, 37.2583]
sigungu  청주시 상당구  43111     [127.4453, 36.4083, 127.7698, 36.7300]
emd      남일면         43111340  [127.4670, 36.5445, 127.5706, 36.6049]
```

같은 검색을 세 bbox 로 각각 불렀다. 참고로 지금 배선의 넷째 줄도 쟀다 —
ev·cctv 의 「말한 장소」 경로는 1.1km 가 아니라 **반경 15km 어댑터**를 쓴다
(`step_service.RADIUS_METERS`). 1.1km 를 그대로 받는 배선은
election·population 의 `map_extent` 줄이다.

```
                                       1.1km    sigungu(상당구)  sido(충북)   (참고) 15km 어댑터
election.searchAssemblyPledgeDistricts   26        44              89           안 잼
population.searchStatistics               1        10              48           안 잼
ev.searchStations                         3       500(상한)       500(상한)    500(상한)
road.getCctv                              0        30             852            79
```

**결과가 물음에 맞아지는가** — 건수만으로는 안 보여서 limit=200 으로 분포를
셌다 (`…limit200-2026-08-27.json`).

```
election  1.1km    26건 = 보은군_옥천군_영동군_괴산군 23 + 청주시 상당구 3
                   첫 페이지 20건이 전부 보은군이다 — "보은군이 먼저" 재현
          sigungu  44건 = 보은군 23 · 증평진천음성 8 · 청원 6 · 상당 3 · 서원 3 · 흥덕 1
                   ★ 청주 네 구가 후보에 들어온다. 그런데 첫 페이지 20건은 여전히 전부 보은군이다
          sido     89건 = 충주 19 · 제천단양 15 · 세종 11 … 첫 페이지 첫머리가 충주시다
```

항목이 공약 한 건당 한 줄이라(보은군 선거구의 공약이 23건) 건수가 많은
선거구가 페이지를 덮고, 정렬이 관련도가 아니다. **bbox 를 넓히는 것은 빠진
것을 후보에 들이는 일이지, 첫 결과를 물음에 맞추는 일이 아니었다.**

```
population  1.1km    1건 (상당구뿐)
            sigungu  10건 — 청주 네 구가 다 들어온다. 다만 상당구 bbox 가 대전 동구와
                     겹쳐 이웃도 함께 온다 (bbox 교차 판정)
            sido     48건 — 첫머리가 대전 서구 · 구미시 · 천안시다. 충북 bbox 가
                     이웃 도를 스치고, 정렬이 인구순이라 밖의 큰 도시가 위로 온다
```

**너무 넓어져서 못 쓰게 되는 자리** — ev 는 sigungu 부터 이미 500 상한에
걸리고 sido 는 첫머리가 문경시(경북)다. cctv 는 sido 852건. 충전소·CCTV 는
좁은 상자(1.1km~15km)가 맞는 자리라는 쪽으로 읽히는 표다 — 그것도 사실로만
적는다.

#### 물음 3 · 인자를 구절째 넣는 자리가 몇 개인가

`tools/check_argument.py` 를 확장 표로 돌렸다 — 35발화(기준 아홉 + 확장
스물둘 + 변주 넷) × 3회. 재는 도구만 고쳤다 — 확장 표(10~31)의 사람추측 값을
**발화 문장 key** 로 더했다 (check_resolve 확장으로 변주 10~13 과 번호가
겹쳐서다). **1~9번과 기존 `HUMAN_ARGUMENT` 는 한 글자도 안 바꿨다.**
19번("인구 많은 시군구 순위")은 사람도 뽑을 낱말이 없어 사람추측을 일부러
안 정했다. 표 전문은 `probe_out/check_argument.dump-2026-08-27.json`.

**35발화 중 LLM 값과 사람이 골랐을 값이 갈린 것이 9** 이고, 성격이 셋으로
갈린다.

```
구절째 넣는다 — 5 (그중 0건이 4)
   6  "국회의원 선거구"        0건   사람 값 "선거구" 도 0건 · 확인용 "청주" 4건 (어휘 불일치 겹침)
  15  "청주 선거구"            0건   사람 값 "청주" 면 4건   ★ 인자 탓
  16  "교통 공약 많은 선거구"  0건   사람 값 "교통" 이면 20건 ★ 인자 탓
  19  "인구 많은 시군구 순위"  0건   비교 기준 없음 (발화에 넣을 낱말 자체가 없다)
  26  "철도 안전 교육 내용"    6건   "내용" 이 붙었지만 의미 검색이라 안 다쳤다

대상 종류 낱말을 넣는다 — 1
  12  "행정경계"               0건   사람 값 "청주시" 면 4건  ★ 인자 탓 (3회 중 2회.
                                     나머지 1회는 017 로 가서 "청주시" 를 제대로 뽑았다)

인자를 아예 못 뽑는다(argument null) — 3
   7  "전기차 충전소 데이터 검색해줘"  CLARIFY 3/3 · given null · 인자 null
  v10 "전기차 충전소 데이터 줘"        CLARIFY 2/3 (1회는 SELECT "전기차 충전소" 120건)
  v12 "철도 안전 문서 보여줘"          CLARIFY 3/3 · web.search 라 실행하면 권한 오류
```

인자가 같은데도 0건인 자리는 따로다 — **28 "경부선 노선 보여줘" 는 LLM 도
사람도 "경부선" 인데 0건이다.** 인자 탓이 아니라 배선이 `stationName` 한 칸인
탓이고, 그 칸의 표가 물음 1 이다 (`railwayName="경부선"` 이면 224건).

그 밖에 표가 새로 말한 것.

```
27 "철도안전법 내용 찾아줘"   NO_MATCH 3/3 — recipe 가 아예 안 나온다. 「서른두째」에
                              SELECT {006}(웹 검색) 확신으로 적혀 있던 자리가 상태가 바뀌었다
 7 · v10 인자 null 인 채 ev.searchStations 를 부르면 query=null 로 전국 클러스터
                              500건(상한)이 온다 — 0건이 아니라 전부가 온다
 8 knowledge.query "철도 안전" 이 4건에서 6건이 됐다 (문서가 늘었다).
                              확인용 빈 질의("")는 이제 [] 가 아니라 오류다
```

#### 각 물음에서 무엇이 열리고 닫혔나

```
물음 1   가)·나) 열림 · 다) 둘 다 보내기 ★ 닫힘 (AND)
물음 2   진짜 bbox 는 「후보에 들이기」를 연다. 「첫 결과 맞추기」는 못 연다
         (정렬·페이지가 남는다) · ev·cctv 에 sido bbox ★ 닫힘 (상한·이웃 도)
물음 3   0건의 원인이 넷으로 갈렸다 — 구절째(15·16) · 대상 종류 낱말(12) ·
         인자 null(7·v10·v12) · 인자 정상이나 배선 칸(28). 한 가지 고침으로
         다 풀리는 모양이 아니라는 것까지가 이번에 잰 사실이다
```

#### 답이 안 나온 것 · 안 한 것

- election 검색의 **정렬 기준**은 안 읽었다 — 첫 페이지가 왜 보은군인지는
  건수 분포(23건이 최다)로 추정될 뿐, 저쪽 SQL 의 ORDER BY 는 안 봤다.
- 물음 2 에서 15km 어댑터 bbox 로 election·population 은 안 눌렀다 —
  지금 배선이 그 둘에 15km 를 안 쓰므로 표를 좁혔다.
- 물음 3 은 첫 실행 단계만 잰다 — 사슬 끝의 답이 아니다 (check_argument 의
  원래 제약 그대로).
- 27(NO_MATCH)이 언제부터인지 안 좇았다 — 「서른셋째」「서른넷째」 변경 뒤라는
  범위까지만 안다.
- 아무것도 안 고쳤다. 온톨로지 · menu · 배선표 · 정답표 · demo · vendor 무손상.

### 2026-08-26 (서른넷째) · 되묻기 뒤에 번호로 고를 수 있게 한다 — LLM 을 다시 안 부른다

모델 qwen3:32b(서버 기본) · recipe 40.
**커밋 안 했다.** 고친 파일은 여섯이다.

```
demo/api/services/clarify_service.py    새 파일. 되묻기 하나를 세션마다 두고 가른다
demo/api/services/execute_service.py    chat · _run_choice · _remember_clarify
demo/api/main.py                        sessionId 를 chat 에 넘기는 한 줄
demo/api/schemas/requests.py            docstring ("네 필드 모두 안 읽는다" 가 거짓이 됐다)
tests/demo/api/test_clarify_choice.py   새 파일 36개
tests/demo/api/test_clarify_flow.py     새 파일 12개
```

**온톨로지 · menu · 배선표 · 정답표 · `vendor/` · `resolve_service` 를 안
건드렸다.** 프롬프트에 들어가는 것이 하나도 안 움직였다.

#### 왜 했나 — 되묻기가 늘었는데 그 자리에서 사람이 할 수 있는 것이 없었다

「서른셋째」가 겹치는 것 0개일 때 둘을 합쳐 되묻게 바꾸면서 **근접(되묻기)이
15 에서 24 로 늘었다.** 시연에서 되묻기가 나올 자리를 일부러 늘려 놓고,
정작 그 자리에서 번호를 눌러 고를 길이 없었다.

2026-08-26 고치기 전 화면 실측 셋.

```
"전기차 충전소 데이터 검색해줘"
  → 어느 것을 보시겠습니까? 1 전기차 충전소 검색 2 장소 좌표 변환 → 전기차 충전소 검색

"1번"                   → "지금 할 수 있는 일 중에 맞는 것이 없습니다. 사용자 요청이 없습니다"
"1 전기차 충전소 검색"   → 120건이 나온다   ★ 된 것처럼 보인다
```

**둘째가 더 나빴다.** 고른 것이 아니라 그 문구가 **새 발화로 처리된** 것이다.
같은 방식으로 인구를 고르면 선택지 이름이 그대로 검색어가 됐다 —
`population.searchStatistics query="인구 통계"` 로 0건.

#### 가르는 규칙 — 네 꼴만 받는다

`demo/api/services/clarify_service.pick` 이다. **LLM 을 안 부른다.**
부르면 느려지고, 무엇보다 같은 문구에 다른 답이 나온다 — 고르기는 사람이 방금
화면에서 읽은 것을 되뇌는 일이라 흔들릴 자리가 아니다.

```
번호만        "1"  "2번"  "1번요"  "3번이요"  "4."
번호 + 이름   "1 인구 통계 조회"        ← 화면 줄 그대로다
이름만        "연령별 인구 구성 조회"    ← 후보 하나에만 걸릴 때만
차례말        "첫 번째"  "두번째"        ← 전체가 이것일 때만
```

넷 다 **전체 일치**다. 담고 있는 것으로는 안 본다. 그 위에 조건 넷을 더 건다.

```
직전에 되묻기가 없으면   어떤 문구도 고르기가 아니다
번호가 후보 수 밖이면    고르기가 아니다 ("9번" · "0")
번호 뒤 이름이 안 맞으면 고르기가 아니다 (번호만 맞는 것으로는 안 됨)
이름이 여럿에 걸리면     고르기가 아니다 (무엇을 고른 것인지 모름)
```

#### 왜 이렇게 좁게 잡았는가

**멀쩡한 새 발화를 삼키는 것이 못 고르는 것보다 나쁘다.** 못 고르면 사람이
다시 말하면 되지만, 삼키면 묻지도 않은 도구가 돌고 그 답이 맞는 것처럼 나간다.
지금 화면에서 일어나는 일(선택지 이름이 검색어가 되는 것)이 바로 그것이다.

좁게 잡는 값을 실제로 치르는 자리가 둘이다.

```
"1호선 지하철역 알려줘"      숫자로 시작하지만 뒤가 후보 이름이 아니다 → 새 발화
"인구 통계 조회 방법 알려줘"  이름을 담고 있지만 통째로 같지 않다      → 새 발화
```

**둘 다 화면에서 눌러 확인했다** (아래 실측 열넷째 · 열다섯째 줄).
되묻기 바로 뒤에 넣었는데도 새 발화로 갔다.

차례말("첫 번째")을 넣을지 재봤다. 화면에 번호가 보이므로 대개는 번호로
답하지만 사람이 자연히 쓰는 말이고, **전체 일치만 받으므로** "첫 번째 역이
어디야" · "두번째 CCTV 보여줘" 는 안 걸린다. 위험이 없어 넣었다 —
시험 `test_차례말이_발화_앞에_있으면_새_발화다` 가 그 자리다.

#### 오래된 것을 버리는 기준

```
TTL_SECONDS  = 300 (5분)   넘긴 되묻기는 없는 것으로 본다
MAX_SESSIONS = 64          넘치면 가장 오래 안 쓴 세션부터 버린다
한 번 쓰면 지운다           고른 뒤에도, 고르기가 아니라고 판정한 뒤에도 안 남는다
```

**5분인 근거.** 사람이 후보 넷을 읽고 번호를 누르는 데 걸리는 시간은 길어야
한두 분이다. 짧게 잡으면 시연 중에 설명하다가 시간이 지나 못 고르게 되고,
길게 잡으면 화면을 띄워 두고 자리를 비웠다 돌아와 던진 새 발화가 옛 후보
목록에 걸린다. 그 사이다. **재서 고른 값이 아니다** — 화면에서 되묻기 뒤가
느려지는 자리를 아직 못 봤다.

**64인 근거.** 세션 id 는 저쪽 화면이 만들고 우리는 그 세션이 언제 끝나는지
모른다. 놔두면 무한히 는다. 시연은 한두 사람이 쓰므로 64 면 넉넉하고, 넘쳐도
버려지는 것은 남의 되묻기 하나다.

**메모리에만 둔다.** 파일도 DB 도 안 만들었다. 서버를 다시 띄우면 사라지는데
그때 사람은 발화를 다시 말하면 된다 — 되묻기 하나를 잃는 값이 저장소를 하나
더 두는 값보다 싸다.

#### 화면 문구 — 아홉 (`POST /chat`, 각 1회, sessionId 하나)

```
1  "청주시 인구 알려줘"
   여러 가지로 해석됩니다. 어느 것을 보시겠습니까?
     1  인구 통계 조회
     2  장소 좌표 변환 -> 인구 통계 조회
     3  연령별 인구 구성 조회
     4  인구 변화 추이 조회

   사용자의 요청은 '청주시 인구 알려줘'로, 인구 정보를 제공하는 것에 끝난다. 인구
   관련 Recipe 중, 지역별 인구 수와 순위를 이름이나 코드로 집계하는 recipe_011이
   요청과 일치한다.

2  "1번"                                                        ★ 이번 작업의 핵심
   고르신 것 — 1 인구 통계 조회

   청주시 인구 통계를 조회했습니다.

   1. population.searchStatistics  4건 · 청주시 흥덕구 292,625명
      (남 149,484 · 여 143,141) · 2026-06-30 기준
   commands 1개

3  "청주시 인구 알려줘"
   1번과 글자까지 같다 (후보 넷 · 같은 순서 · 같은 reason)

4  "2 연령별 인구 구성 조회"                                     ★ 고르기가 아니다
   어느 것을 보시겠습니까?
     1  연령별 인구 구성 조회
     2  인구 통계 조회

   요청은 '연령별 인구 구성 조회'에서 끝난다. 이는 행정구역 코드와 기준월을
   기반으로 연령대별 인구 구성을 보는 작업을 의미한다.

5  "전기차 충전소 데이터 검색해줘"
   어느 것을 보시겠습니까?
     1  전기차 충전소 검색
     2  장소 좌표 변환 -> 전기차 충전소 검색

   사용자의 요청은 '전기차 충전소 데이터 검색'에서 끝난다. 이 요청은 특정 장소나
   키워드 없이 충전소 데이터를 검색하는 일반적인 요청이다.

6  "오송역 CCTV 보여줘"                                          ★ 새 발화로 갔다
   오송역 CCTV 를 조회했습니다.

   1. geo.geocode       오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1
                        (127.3277, 36.6200)
   2. road.getCctv      minLon=127.1598 · minLat=36.4852 · maxLon=127.4956 ·
                        maxLat=36.7547  83건
   commands 3개

7  "1번"                                                        ★ 고를 것이 없다
   지금 할 수 있는 일 중에 맞는 것이 없습니다.

   사용자 요청이 없습니다.

8  "오송역 근처 충전소 자세히 알려줘"                            ★ 되묻기 없이 돈다
   오송역 충전소 상세를 조회했습니다.

   1. geo.geocode       오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1
                        (127.3277, 36.6200)
   2. ev.searchStations  minLon=127.1598 · minLat=36.4852 · maxLon=127.4956 ·
                        maxLat=36.7547  500건 (전체 2,173건) · 청주오송역차고지
   3. ev.getStation     statId="PK000313"  8건
   commands 6개

9  "9번"
   찾지 못했습니다.

   1. election.searchDistricts  query="국회의원 선거구"  0건
   commands 1개
```

**2번이 됐다.** 「서른셋째」 뒤로 화면에서 처음으로 되묻기가 실행까지 갔다.

**4번은 고르기가 아니었다. 규칙이 그렇게 판정하는 것이 맞다.** 화면 목록에서
`연령별 인구 구성 조회` 는 **3번 줄**이고 2번 줄은 `장소 좌표 변환 -> 인구
통계 조회` 다. 번호와 이름이 서로 다른 줄을 가리키므로 어느 쪽이 뜻인지 알 수
없다 — 「애매하면 고르기가 아닌 쪽」 그대로다. 위 아홉을 적어 준 사람이 3번
줄을 2번으로 본 것이지 규칙이 좁아서 놓친 것이 아니다.

**8번 뒤에 되묻기가 없어 9번이 빈 자리에서 눌렸다.** 8번이 곧장 실행돼
기억해 둔 것이 없었다. 둘 다 다시 눌렀다.

```
4' "청주시 인구 알려줘" → "3 연령별 인구 구성 조회"             ★ 번호 + 이름 꼴
   고르신 것 — 3 연령별 인구 구성 조회

   청주시 연령대별 인구 구성을 조회했습니다.

   1. geo.geocode       청주시 → 충청북도 청주시 상당구 남일면 단재로 480
                        (127.5059, 36.5883)
   2. adminBoundary.findBoundaryByPoint  lon=127.5059 · lat=36.5883  3건 · 충청북도
   3. population.getAgeProfile  level="sigungu" · code="43111"  청주시 상당구
                        193,079명 (남 95,877 · 여 97,202) · 2026-06-30 기준
   commands 3개

9' "전기차 충전소 데이터 검색해줘" → "9번"                       ★ 되묻기 뒤에 눌렀다
   (되묻기는 5번과 글자까지 같다. 후보 둘)
   찾지 못했습니다.

   1. election.searchDistricts  query="국회의원 선거구"  0건
   commands 1개
```

**후보가 둘인데 "9번" 이 왔다. 고르기가 아니라고 보고 새 발화로 넘겼다.**
그 뒤는 이 저장소가 늘 하던 것이다 — `"9번"` 을 발화로 해석해
`election.searchDistricts` 를 불렀다. 고르기 판정은 맞고, 그 뒤 답이 이상한
것은 **이번 작업 밖의 자리**다.

문제 제기에 적힌 ★ 자리와 좁게 잡은 것을 확인하는 자리 셋을 더 눌렀다.

```
10 (되묻기 5 뒤) "1 전기차 충전소 검색"          ★ 120건이 안 나온다
   고르신 것 — 1 전기차 충전소 검색

   어느 장소인지 알 수 없습니다. '오송역' 처럼 장소를 함께 말씀해 주세요.
   commands 0개

11 (되묻기 1 뒤) "1호선 지하철역 알려줘"          ★ 새 발화로 갔다
   여러 가지로 해석됩니다. 어느 것을 보시겠습니까?
     1  철도 노선 조회
     2  장소 좌표 변환 -> 철도 노선 조회
     3  웹 검색
     4  문서 검색

12 (되묻기 1 뒤) "인구 통계 조회 방법 알려줘"     ★ 새 발화로 갔다
   여러 가지로 해석됩니다. 어느 것을 보시겠습니까?
     1  인구 통계 조회
     2  장소 좌표 변환 -> 인구 통계 조회
     3  연령별 인구 구성 조회
     4  인구 변화 추이 조회

   사용자의 요청은 '인구 통계 조회 방법'에 대한 정보를 제공해달라는 것으로,
   구체적인 장소, 키워드, 식별자, 또는 작업 대상이 명시되어 있지 않다. 따라서
   어떤 Recipe와도 정확히 일치하지 않는다.
```

**10번이 이번 작업이 막으려던 자리다.** 고치기 전에는 같은 문구가 120건을
내놓아 된 것처럼 보였다. 이제는 **고른 것으로 제대로 받고**, 그다음 인자가
없어 안내로 멈춘다. 화면에 나가는 건수가 줄었지만 **그것이 맞는 답이다** —
120건은 선택지 이름이 검색어가 되어 나온 것이었다.

**왜 인자가 없나.** `"전기차 충전소 데이터 검색해줘"` 의 `argument` 가 3/3
회차에서 `None` 이다(`POST /resolve` 로 확인). `given` 도 `null` 이다.
「스물다섯째」에 `"전기차 충전소"(120건)` 로 적힌 그 발화인데, 그때는 이 발화가
SELECT 였고 지금은 「서른셋째」 뒤로 CLARIFY 다. **인자를 못 뽑는 것은 이번
변경이 만든 것이 아니다** — 아래 「열린 과제」에 남겼다.

11 · 12 는 좁게 잡은 것이 실제로 듣는지 보는 자리다. 되묻기 바로 뒤에 넣었는데도
둘 다 새 발화로 갔다.

#### 사람이 저쪽 화면에서 직접 눌렀다 (2026-08-26)

위 아홉은 `POST /chat` 을 curl 로 두드린 것이다. **아래는 사람이 저쪽 화면에서
직접 누른 것이고 문구는 실측이다.**

```
"청주시 인구 알려줘" → "2번"
  고르신 것 — 2 장소 좌표 변환 -> 인구 통계 조회
  1. geo.geocode  청주시 → 청주시 상당구 남일면 단재로 480 (127.5059, 36.5883)
  2. population.searchStatistics  1건 · 청주시 상당구 193,079명

"청주시 인구 알려줘" → "1번"
  고르신 것 — 1 인구 통계 조회
  1. population.searchStatistics  4건 · 청주시 흥덕구 292,625명

"오송역 선거구 알려줘" → "1번"
  고르신 것 — 1 지점 국회의원 지역구 판별
  2. election.findDistrictByPoint  lon=127.3277 · lat=36.6200  충북 청주흥덕 · 1건

"경부선 노선 보여줘" → "2번"
  고르신 것 — 2 웹 검색
  조회하지 못했습니다.
  1. web.search  query="경부선"  실패 · 이 도구를 쓸 권한이 없습니다

경부선 되묻기에서 "2번" 을 쓴 직후 다시 "1번"   → 고를 것이 없다
청주시 인구 되묻기 뒤 5분 기다렸다가 "1번"       → 고를 것이 없다
```

**★ 같은 발화에서 두 길의 차이가 한 화면에 나란히 보인다.** 청주시 인구를
1번과 2번으로 각각 골라 보면 키워드 길은 네 구가 다 오고(4건) 좌표 길은
상당구 하나만 온다(1건). `geo.geocode` 상자가 1.1km 라 그 안에 한 구밖에 안
들어간다 — 「스물다섯째」의 아홉째 한계가 화면에서 눈으로 보이는 자리다.
**확장 표 29번의 기대값을 `011` 로 정한 것이 옳았다는 실물 근거다.**

**★ 「근접」이 화면에서 처음으로 정답에 닿았다.** 표에서 근접은 "사람이 고르면
닿는다" 는 뜻인데 **지금까지 화면에서는 못 닿았다** — 고를 길이 없어서 그
말이 표 안의 가정이었다. 오송역 선거구가 첫 사례다.

```
「서른셋째」  검산이 덮던 022 를 후보에 되살렸다   → 빗나감이 근접이 됐다
「서른넷째」  그 후보를 번호로 고를 수 있게 했다   → 근접이 화면에서 닿았다
```

`022` 는 확장 표 30번의 기대값이고 「서른셋째」 전에는 `{023, 025, 027}` 로
덮이던 답이다. **두 작업이 합쳐져야 닿는다** — 한쪽만으로는 후보에 없거나
후보에 있어도 못 고른다.

**★ 권한 없는 도구를 골라도 이유가 보인다.** 경부선 되묻기에서 웹 검색을
고르면 `"이 도구를 쓸 권한이 없습니다"` 가 그대로 나온다. 고른 것이 실행까지
가고 거기서 막히는 것이 보이므로, 되묻기가 실행 전에 끊기던 때와 다르다.

**★ 지우는 규칙 둘이 화면에서 작동했다.** 한 번 쓰면 지우는 것과 5분을 넘기면
안 쓰는 것을 사람이 직접 눌러 확인했다. 위 「오래된 것을 버리는 기준」의
`TTL_SECONDS = 300` 과 `take` 가 꺼내면서 지우는 것이 그 둘이다.

#### 관문 여섯

```
pytest                   266 passed / 1 failed   (기준선 218/1 + 새 시험 48)
                         실패 1건은 graphviz 음성 대조군. 기존 삭제 0
check_wiring             recipe 40 · 완비 39 · STEP_OF 35줄 · A 0 · B 0 · C 1
                         C 는 web_fetch × 웹 주소. 기준선 그대로다
check_resolve --runs 3   적중 62/93 · 근접 24 · 빗나감 4 · 못 붙음 3 (67%)
                         LLM 단독 61/93 (66%) · 조회 빠짐 13 · 못 붙음 3
                         ★ 「서른셋째」 기준선과 표 전체가 바이트까지 같다
화면 실측                아홉 + 덧붙인 다섯(curl) + 사람이 저쪽 화면에서 누른
                         여섯. 셋 다 위에 문구 그대로 옮겼다
새 파일 · 의존성          requirements.txt 에 새 줄 0. import 는 re · time ·
                         collections 셋 다 표준 라이브러리다
vendor 무결              git diff 에 vendor/ 없음. demo/api 셋과 tests 둘뿐이다
```

**★ check_resolve 를 두 번 돌렸다. 첫 회는 믿을 것이 못 됐다.**
처음에 잰 값이 `적중 59/93 · 근접 25 · 빗나감 6` 이라 기준선(62 · 24 · 4)에서
셋이 움직였다. **화면 실측을 같은 GPU 에서 동시에 돌리고 있었다.**

```
첫 회 (화면 실측과 동시)  적중 59/93 · 빗나감 6 · LLM 단독 58/93 · 조회 빠짐 15
stash 로 되돌린 기준선    적중 62/93 · 빗나감 4 · LLM 단독 61/93 · 조회 빠짐 13
되살려 단독으로 다시      적중 62/93 · 빗나감 4 · LLM 단독 61/93 · 조회 빠짐 13
                          ★ 기준선과 표 전체가 바이트까지 같다
```

**변경 자체를 `git stash -u` 로 통째로 뺐다 켜서 셋을 나란히 쟀다.**
갈린 발화는 11 · 12 둘뿐이고, **`LLM 단독` 칸이 함께 움직였다** (43/66 → 46/66).
그 칸은 검산 앞에서 재는 것이라 `_verdict` 아래의 어떤 코드도 못 건드린다 —
움직인 것은 모델이지 이 변경이 아니다.

**다음에 잴 때 지킬 것 : `check_resolve` 를 다른 Ollama 요청과 겹쳐 돌리지
않는다.** 겹치면 표가 세 칸씩 흔들려 「관문이 움직였다」로 잘못 읽힌다.

#### 안 한 것

- **저쪽 화면(KRRI_ASAP)의 계약을 안 건드렸다.** `sessionId` 는 `ChatRequest`
  가 이미 받던 필드다. 새 필드도 새 엔드포인트도 안 만들었다.
- **`/chat/stream` 에 새 이벤트 종류를 안 만들었다.** 고른 것도 `step_start` ·
  `step_end` 한 쌍으로 나간다(`node="choice"`). 저쪽 화면이 모르는 type 이
  오면 어떻게 되는지 안 봤기 때문이다.
- **되묻기를 두 번 이상 이어 붙이지 않는다.** 고른 뒤 그 실행이 또 되물을
  일은 없다(recipe 가 정해졌다). 세션마다 남는 것은 늘 직전 하나다.
- **인자를 고를 때 다시 안 뽑는다.** 되묻기 때 뽑아 둔 것을 그대로 쓴다.
  `"1번"` 에서 인자를 뽑을 수는 없고, 원래 발화를 다시 LLM 에 넣으면 이번에
  안 부르기로 한 것을 부르게 된다.
- **인자가 없는 되묻기를 못 살렸다.** 10번이 그 자리다. 고른 뒤에 `"청주시"`
  처럼 인자만 받아 이어 붙이는 길은 안 만들었다 — 되묻기 하나를 기억하는
  일과 대화를 이어 가는 일은 크기가 다르다.
- **TTL 과 세션 상한을 재서 고르지 않았다.** 5분 · 64 는 시연 크기에서 고른
  값이다. **5분 경계는 사람이 눌러 확인했지만**(위) 그 값이 알맞은지는 안 쟀다 —
  사람이 되묻기 앞에서 얼마나 머무는지를 세어 본 적이 없다. 세션 상한 64 는
  경계를 아예 못 눌러 봤다. 시연에 세션이 하나뿐이라 넘칠 일이 없다.
- **Streamlit 화면(`demo/ui/`)을 안 건드렸다.** 그쪽은 `/chat` 을 아예 안
  부르고 `/resolve` 만 부른다(`demo/ui/api_client.py` 에 `chat` 이 없다).
  되묻기 뒤 고르기는 `POST /chat` · `POST /chat/stream` 계약에만 있다.
- **`check_argument.py` 와 `check_wiring.py` 의 표를 안 늘렸다.** 고르기는
  발화 해석 밖의 일이라 잴 칸이 없다.

---

### 2026-08-26 (서른셋째) · 검산이 맞는 답을 덮던 규칙을 고친다 — 안 셋을 재고 골랐다

모델 qwen3:32b(서버 기본) · recipe 40 · 31발화 × 3회 × 네 묶음.
**커밋 안 했다.** 고친 파일은 `demo/api/services/resolve_service.py`
(`_verdict` 한 함수) · `tests/demo/api/test_verdict.py`(새 파일) · 이 파일뿐이다.
**온톨로지 · menu · 배선표 · 정답표 · `vendor/` 를 안 건드렸다.**

#### 왜 고쳤나 — 규칙 둘이 같은 상황을 다르게 처신하고 있었다

`_verdict` 가 LLM 이 고른 것과 축 조회 후보를 대조해 최종을 정한다.
그 다섯 규칙 중 셋째와 넷째가 서로 앞뒤가 안 맞았다.

```
겹치는 것 0개   뽑힌 후보를 쓴다   "LLM 이 헛짚은 것으로 봄"
뽑힌 후보 0개   LLM 것을 쓴다      "축이 틀린 것이므로 조회 결과를 믿지 않음"
```

**둘 다 「축과 LLM 이 어긋났다」는 같은 상황인데 한쪽은 조회를 믿고 한쪽은
LLM 을 믿는다.** 「서른두째」 실측에서 그 셋째 규칙이 맞는 답을 아홉 번 덮었다.

```
28  경부선 노선 보여줘      LLM 단독 {003} ← 기대값과 같다   최종 {006, 013}       빗나감
29  청주시 인구 알려줘      LLM 단독 {011} ← 기대값과 같다   최종 {029, 038, 039}  빗나감
30  오송역 선거구 알려줘    LLM 단독 {022} ← 기대값과 같다   최종 {023, 025, 027}  빗나감
                            세 발화 × 3회 = 아홉 번
```

그리고 검산의 값이 두 묶음에서 정반대였다.

```
기준선 아홉   LLM 단독 56% → 최종 78%   검산이 이득
확장 열아홉   LLM 단독 70% → 최종 62%   ★ 검산이 순손해
```

**아홉으로만 보면 검산이 늘 이득으로 보였다.** 면적을 넓히니 뒤집혔다.

#### 안 셋 — 미리 고르지 않고 셋을 다 쟀다

`final = overlap or looked_up` 한 줄을 바꾸는 일이다.

```
가) 지금 그대로 (대조군)   겹치는 것 0개 → 조회 후보를 쓴다
나) LLM 을 믿는다          겹치는 것 0개 → LLM 이 쓴 것을 쓴다
                           근거 : 「뽑힌 후보 0개」 규칙과 같은 처신이 된다
                           위험 : LLM 이 정말 헛짚었을 때 걸러낼 것이 없다
다) 둘을 합쳐 되묻는다     겹치는 것 0개 → LLM 것 + 조회 후보를 합쳐 CLARIFY
                           근거 : 어느 쪽이 맞는지 모르니 사람에게 묻는다
                           위험 : 후보가 늘어 되묻기가 는다. 적중은 안 오른다
```

**재는 법.** `_verdict` 가 `/tmp/verdict_plan` 파일을 call time 에 읽게 하는
임시 코드를 넣고 안을 갈아 끼웠다. **uvicorn 을 다시 안 띄웠으므로 네 묶음이
전부 한 세션이다** — 「열넷째」가 세션을 건너면 값이 안 맞는다고 적어 뒀다.
잰 뒤 임시 코드는 지웠고 확정 코드로 네 번째 묶음을 다시 돌려 안 다) 와
바이트까지 같은 것을 확인했다.

#### 안 × 묶음 표

**축 표(given · want · about · argument)가 세 안에서 바이트까지 같다.**
프롬프트에 들어가는 것을 하나도 안 건드렸으므로 그래야 한다. 조회 판정
(조회 적중 18 · 근접 53 · ★ 빠짐 13 · 없음 9)도 셋이 같다 — 검산을 지나기
전 값이라 안에 안 흔들린다.

| 안 | 묶음 | LLM 단독 | 적중 | 근접 | 빗나감 | 못 붙음 |
|---|---|---|---|---|---|---|
| 가 | 기준선 아홉 | 15/27 (56%) | 21/27 (78%) | 6 | **0** | 0 |
| 가 | 확장 열아홉 | 46/66 (70%) | 41/66 (62%) | 9 | **13** | 3 |
| 가 | 합계 | 61/93 (66%) | 62/93 (67%) | 15 | **13** | 3 |
| 나 | 기준선 아홉 | 15/27 (56%) | 21/27 (78%) | 6 | **0** | 0 |
| 나 | 확장 열아홉 | 46/66 (70%) | 50/66 (76%) | 7 | **6** | 3 |
| 나 | 합계 | 61/93 (66%) | 71/93 (76%) | 13 | **6** | 3 |
| 다 | 기준선 아홉 | 15/27 (56%) | 21/27 (78%) | 6 | **0** | 0 |
| 다 | 확장 열아홉 | 46/66 (70%) | 41/66 (62%) | 18 | **4** | 3 |
| 다 | 합계 | 61/93 (66%) | 62/93 (67%) | 24 | **4** | 3 |

**기준선 아홉이 세 안에서 한 글자도 안 움직였다.** 「틀렸을 때 나온 것」 칸까지
같다 — 4번 `{038, 039}` CLARIFY 3회, 7번 `{012, 030}` CLARIFY 3회.
아홉에는 겹치는 것이 0개인 자리가 아예 없어서 이 규칙이 안 걸린다.
**서른두 번의 기록과 그대로 이어 읽힌다.**

검산이 한 일은 이렇다.

| 안 | 검산이 살렸다 | ★ 맞는 답을 덮었다 | 바꿨지만 판정은 같다 |
|---|---|---|---|
| 가 | 10회 | 9회 | 12회 |
| 나 | 10회 | **0회** | 10회 |
| 다 | 10회 | 9회 | 12회 |

**다) 의 「덮었다 9회」는 가) 의 9회와 뜻이 다르다.** 도구는 「LLM 단독이
적중인데 최종이 적중이 아니다」를 덮은 것으로 세는데, 가) 에서는 그 아홉이
**빗나감**이었고 다) 에서는 **근접**이다. 정답이 후보에 남아 있으므로 사람이
고르면 닿는다. **이 칸은 그 차이를 못 본다** — 그래서 1순위가 빗나감이다.

#### 고른 안 — 다) 둘을 합쳐 되묻는다

기준은 숫자를 보기 전에 적어 둔 것이고 순서대로 봤다.

```
1순위  빗나감이 적은 것          다) 4  <  나) 6  <  가) 13     → 다)
2순위  「맞는 답을 덮었다」가 적은 것   나) 0  <  가) 9 = 다) 9     → 나)
3순위  적중이 많은 것            나) 71 >  가) 62 = 다) 62      → 나)
4순위  기준선 아홉이 안 나빠지는 것  셋 다 21/27 (78%) 로 같음      → 무승부
```

**1순위가 다) 를 가리키고 2 · 3순위가 나) 를 가리킨다. 1순위를 따랐다.**
미리 적어 둔 규칙이 「1순위와 3순위가 어긋나면 1순위를 따른다」이고,
그 근거가 「빗나감은 근접보다 나쁘다」는 이 저장소의 원칙이다.

숫자로 보면 나) 를 고르고 싶어진다 — 적중이 아홉 높다(62/93 → 71/93).
**그런데 그 아홉을 어디서 벌었는지가 문제다.** 28 · 29 · 30 에서 아홉을 벌고
14번에서 근접 둘을 빗나감 둘로 잃었다. 발화 14 는 LLM 이 `{026}` 을 SELECT
하는 자리인데 나) 는 그것을 걸러낼 것이 없어 **확신하고 틀린다.**
다) 는 같은 자리에서 `{023, 025, 026, 027}` 로 되물어 정답 `025` 가 남는다.

**대가는 되묻기가 는 것이다.** 근접이 15 에서 24 로 늘었다(합계). 후보 수도
는다 — 28번 2→3, 29번 3→4, 30번 3→4, 14번 3→4. 적중은 62/93 그대로다.
**적중을 못 올리고 되물음만 늘린 안**이라는 것을 숨기지 않는다.

#### 언제 뒤집을지

```
축이 고쳐지면          조회 빠짐 13회가 0 에 가까워지면 겹치는 것이 0개인 자리
                       자체가 줄어든다. 그때 셋의 차이가 거의 사라지므로
                       다시 재서 제일 단순한 것으로 되돌린다
되묻기가 시연을 막으면  후보 넷짜리 CLARIFY 가 화면에서 못 쓸 만하면 나) 로
                       옮긴다. 그때 잃는 것은 14번 같은 자리의 근접이다
14번이 고쳐지면        LLM 이 `{026}` 을 안 고르게 되면 나) 의 빗나감이 4 가 되어
                       1순위가 무승부가 된다. 그러면 적중이 아홉 높은 나) 가 낫다
```

#### 발화별로 달라진 것 — 넷뿐이다

**31발화 중 14 · 28 · 29 · 30 만 안에 따라 달라졌다.** 나머지 스물일곱은
세 안에서 글자까지 같다. 겹치는 것이 0개인 자리에서만 규칙이 걸리기 때문이다.

| # | 발화 | 기대값 | 가) | 나) | 다) |
|---|---|---|---|---|---|
| 14 | 오송역 국회의원 공약 보여줘 | `025` | 적중 1 · 근접 2 `{023,025,027}` | 적중 1 · **빗나감 2** `{026}` SELECT | 적중 1 · 근접 2 `{023,025,026,027}` |
| 28 | 경부선 노선 보여줘 | `003` | **빗나감 3** `{006,013}` | **적중 3/3** `{003}` | 근접 3 `{003,006,013}` |
| 29 | 청주시 인구 알려줘 | `011` | **빗나감 3** `{029,038,039}` | **적중 3/3** `{011}` | 근접 3 `{011,029,038,039}` |
| 30 | 오송역 선거구 알려줘 | `022` | **빗나감 3** `{023,025,027}` | **적중 3/3** `{022}` | 근접 3 `{022,023,025,027}` |

28 · 29 · 30 은 **위험 · 애매 표시가 붙은 자리**다. 가) 에서 확신하고 틀리던
것이 다) 에서 정직한 되물음이 됐다. 「고쳐야 할 자리」로 박아둔 기대값은
그대로 두었고 적중이 되지도 않았다 — 축이 안 고쳐졌으므로 그래야 한다.

**14번이 이번 측정의 값을 다 했다.** 이 한 자리가 없었으면 나) 의 빗나감이
다) 와 같은 4 가 되어 적중이 아홉 높은 나) 를 골랐을 것이다. 아홉으로만
쟀으면 이 자리를 못 봤다 — 14번은 확장에만 있다.

#### 안 건드린 것은 안 움직였다

```
축 · 인자     세 안에서 축 표가 바이트까지 같다
조회 판정     조회 적중 18 · 근접 53 · ★ 빠짐 13 · 없음 9 — 셋이 같다
LLM 단독      61/93 (66%) — 셋이 같다. 검산 앞이라 안에 안 흔들린다
기준선 아홉   21/27 (78%) · 근접 6 · 빗나감 0 · 못 붙음 0 — 셋이 같다
못 붙음       3회(27번 NO_MATCH) — 셋이 같다
```

#### 안 한 것

- **축을 안 고쳤다.** 조회 후보가 정답을 놓치는 자리가 확장 표에서 13회다.
  그것이 진짜 원인이지만 이번은 **어긋났을 때 어떻게 처신할지**만 정했다.
  「열린 과제」에 새 항목으로 적었다.
- **정답표를 안 늘리고 안 고쳤다.** 빗나감이 줄었지만 기대값을 하나도
  안 움직였다. 28 · 29 · 30 은 여전히 적중이 아니다.
- **판정 규칙을 안 건드렸다.** 적중 · 근접 · 빗나감 · 못 붙음 을 세는
  `_grade` 와 `_grade_lookup` 이 그대로다. `tools/check_resolve.py` 는
  한 줄도 안 고쳤다.
- **되묻기 뒤 고르기(화면 쪽)를 안 했다.** 다) 는 후보 넷짜리 CLARIFY 를
  만든다. 화면에서 그것이 쓸 만한지는 안 봤다.
- **모델을 하나만 봤다.** 서버 기본(qwen3:32b)뿐이다.
- **화면(`POST /chat`)으로는 안 눌렀다.** `/resolve` 만 봤다.

---

### 2026-08-26 (서른두째) · 정답표를 아홉에서 스물여덟로 늘린다 — 자를 넓힌다 · 재기만 했다

모델 qwen3:32b(서버 기본) · recipe 40 · 31발화 × 3회 × 세 묶음.
고친 파일은 `tools/check_resolve.py`(발화 목록과 표) 와 이 파일뿐이다.
**제품 코드는 한 줄도 안 고쳤다** — 온톨로지 · menu · 배선표 · `demo/` ·
`vendor/` 를 안 건드렸다.

#### ★ 이 표의 점수는 아홉의 점수와 비교하지 않는다

**발화가 다르므로 다른 자다.** 아래 「확장 열아홉」의 62% 를 「기준선 아홉」의
78% 나 「스물일곱째」의 100% 옆에 놓고 「떨어졌다」고 읽으면 안 된다.
확장 열아홉은 **이번이 첫 측정이고 이번 값이 그 자의 기준선**이다.

같은 이유로 도구도 두 백분율을 따로 찍는다. 합계 한 줄을 내지만 아홉의 값이
그 위에 따로 보인다.

#### 왜 늘렸나 — 커버리지가 첫째 이유가 아니다

첫째는 **한 발화의 무게**다.

```
아홉      발화 하나가 흔들리면 점수가 11점 움직인다
          「서른째」에서 발화 둘이 되묻기가 되자 100% 가 78% 가 됐다
          변경이 좋은지 나쁜지를 이 자에 물으면 답이 요동친다
스물여덟  한 발화가 3.6% 다
```

둘째가 커버리지다. 아홉이 덮던 것은 recipe 40개 중 8개였고 나머지 32개는 한
번도 재어진 적이 없었다. 「스물아홉째」 쓸기에서 확신하고 틀리는 자리가 둘
나온 것이 안 재본 경로에 무엇이 있는지 모른다는 증거였다.

#### 기존 아홉은 한 글자도 안 바꿨다

서른한 번의 측정 기록이 그 아홉으로 재어져 있다. 10번부터 이어 붙였다.

**표시 칸을 튜플이 아니라 따로 둔 `MARKS` 표로 만든 것도 그래서다.** 튜플에
칸을 하나 더하면 1~9 번 줄도 함께 고쳐야 하는데, 그러면 `git diff` 만 보고
「아홉이 안 움직였다」를 확인할 수가 없다. 지금은 그 아홉 줄의 diff 가 0 이다.

#### 표시 넷의 뜻

적중 표에 한 칸으로 찍는다. **판정에는 아무 영향이 없다** — 세는 법은 표시가
없을 때와 글자까지 같다. 낮은 점수가 무엇 때문인지 표에서 바로 읽으라고 둔
칸이다.

```
시연  쓸기에서 두 모델 모두 하나로 갔고 데이터도 나왔다. 시연에서 쓸 발화다
      ★ 여기서 되묻기나 빗나감이 나오면 그것이 발견이다
위험  지금 확신하고 틀리는 것을 알고 넣었다. 빗나감이 나오는 것이 정상이다
애매  사람이 기대값을 정한 자리. 지금은 되묻는다. 나중에 고치면 적중이 된다
-     기준선 아홉. 표시 없이 재던 것이라 그대로 둔다
```

#### 발화 열아홉과 기대값과 고른 근거

값의 출처는 **2026-08-26 쓸기(「스물아홉째」, 60발화 × 2모델 × 10회)** 다.
쓸기는 정답을 안 보고 「무엇이 나오는가」만 적은 것이라, 여기 기대값은 그것을
보고 사람이 정한 것이다. **쓸기의 번호는 옛 번호다** — 「서른째」가 옛 017 을
빼면서 018 이후가 하나씩 당겨졌다. 아래는 당긴 뒤의 번호다.

**열아홉의 기대 사슬은 넣기 전에 하나씩 확인했다** — `ontology.graph.
recipe_nodes` 로 40개를 전부 찍어 맞대 봤고 열아홉이 전부 일치했다.

| # | 발화 | 표시 | 기대값 | 사슬 |
|---|---|---|---|---|
| 10 | 오송역이 어느 동인지 알려줘 | 시연 | `018` | 장소 → 좌표 → 지점 행정구역 판별 |
| 11 | 오송역 행정경계 보여줘 | 시연 | `017` | 장소 → 좌표 → 행정구역 조회 |
| 12 | 청주시 행정경계 보여줘 | 시연 | `004` | 키워드 → 행정구역 조회 |
| 13 | 오송역 국회의원 누구야 | 시연 | `024` | 장소 → 좌표 → 지점 전체 선거구 판별 |
| 14 | 오송역 국회의원 공약 보여줘 | 시연 | `025` | 장소 → 좌표 → 선거구 공약 검색 |
| 15 | 청주 선거구 찾아줘 | 시연 | `007` | 키워드 → 지역구 검색 |
| 16 | 교통 공약 많은 선거구 검색해줘 | 시연 | `008` | 키워드 → 전체 선거구 검색 |
| 17 | 청주 국회의원 공약 검색해줘 | 시연 | `009` | 키워드 → 선거구 공약 검색 |
| 18 | 오송역 일대 인구 얼마야 | 시연 | `029` | 장소 → 좌표 → 인구 통계 |
| 19 | 인구 많은 시군구 순위 보여줘 | 시연 | `011` | 키워드 → 인구 통계 |
| 20 | 대전역 연령대별 인구 알려줘 | 시연 | `038` | 장소 → 좌표 → 행정구역 판별 → 연령별 인구 |
| 21 | 경부선 선형 데이터 줘 | 시연 | `002` | 장소 → 철도 구간 형상 |
| 22 | 오송역 지나는 노선 알려줘 | 시연 | `003` | 장소 → 철도 노선 조회 |
| 23 | 경부선 주변 CCTV 보여줘 | 시연 | `033` | 장소 → 철도 구간 형상 → CCTV |
| 24 | 오송역 근처 충전소 자세히 알려줘 | 시연 | `040` | 장소 → 좌표 → 충전소 검색 → 상세 **4단** |
| 25 | 문서에서 철도안전법 관련 내용 찾아줘 | 시연 | `013` | 키워드 → 문서 검색 |
| 26 | 문서에서 철도 안전 교육 내용 찾아줘 | 시연 | `013` | 키워드 → 문서 검색 |
| 27 | 철도안전법 내용 찾아줘 | 위험 | `013` | 키워드 → 문서 검색 |
| 28 | 경부선 노선 보여줘 | 위험 | `003` | 장소 → 철도 노선 조회 |
| 29 | 청주시 인구 알려줘 | 애매 | `011` | 키워드 → 인구 통계 |
| 30 | 오송역 선거구 알려줘 | 애매 | `022` | 장소 → 좌표 → 지점 선거구 판별 |
| 31 | 청주 국회의원 선거구 검색해줘 | 애매 | `007` | 키워드 → 지역구 검색 |

**20번만 지명이 다르다** (오송역 아닌 대전역). 지명 하나에만 몰리는 것을
피하려고 넣었고 **쓸기에 없던 안 재본 값**이었다. 24번은 사슬이 넷인 유일한
발화다 — 4단이 실제로 서는지를 그 한 줄이 지킨다.

위험 둘과 애매 셋을 왜 그 기대값으로 뒀는지는 「열린 과제」의
「정답표에 「고쳐야 할 자리」로 박아둔 다섯」에 적었다.

#### 세 묶음 — 바이트까지 같다

**31발화 × 3회를 세 번 돌렸는데 네 장의 표가 전부 바이트까지 같다.**
적중 표뿐 아니라 축 표 · 후보 표 · 검산 표까지 `diff` 가 0 이다. 한 묶음이
약 4.8분이다 (20:39:29 → 20:53:54 에 셋).

「스물아홉째」가 세운 「갈리는 것은 1회차와 그 뒤, 두 가지뿐」이 다시 맞았다.
한 묶음 **안에서** 갈린 발화가 셋 있는데(11 · 12 · 14, 2회 대 1회) 그 갈림이
세 묶음에서 똑같이 재현됐다. **3회로 줄인 판단이 또 값을 했다.**

```
#  발화                            표시  LLM 단독  적중   근접  빗나감  못 붙음   틀렸을 때 나온 것
1  오송역 위치 보여줘              -     3/3       3/3    0     0       0
2  오송역 좌표 알려줘              -     0/3       3/3    0     0       0
3  오송역 CCTV 보여줘              -     3/3       3/3    0     0       0
4  청주시 인구 구성 알려줘         -     0/3       0/3    3     0       0        {038, 039}       근접   CLARIFY  3회
5  오송역 근처 충전소 찾아줘       -     0/3       3/3    0     0       0
6  국회의원 선거구 찾아줘          -     3/3       3/3    0     0       0
7  전기차 충전소 데이터 검색해줘   -     0/3       0/3    3     0       0        {012, 030}       근접   CLARIFY  3회
8  철도 안전 문서 찾아줘           -     3/3       3/3    0     0       0
9  충북 제1선거구 알려줘           -     3/3       3/3    0     0       0
                                        ──────────────────────────────────
                                        15/27     21/27  6     0       0        78%  ← 기준선 아홉
                                        56%       ← LLM 단독

10 오송역이 어느 동인지 알려줘     시연  3/3       3/3    0     0       0
11 오송역 행정경계 보여줘          시연  2/3       2/3    1     0       0        {017, 019}       근접   CLARIFY  1회
12 청주시 행정경계 보여줘          시연  2/3       2/3    0     1       0        {017, 019}       빗나감 CLARIFY  1회
13 오송역 국회의원 누구야          시연  3/3       3/3    0     0       0
14 오송역 국회의원 공약 보여줘     시연  0/3       1/3    2     0       0        {023, 025, 027}  근접   CLARIFY  2회
15 청주 선거구 찾아줘              시연  0/3       0/3    3     0       0        {007, 008}       근접   CLARIFY  3회
16 교통 공약 많은 선거구 검색해줘  시연  0/3       3/3    0     0       0
17 청주 국회의원 공약 검색해줘     시연  0/3       0/3    0     3       0        {025}            빗나감 SELECT   3회
18 오송역 일대 인구 얼마야         시연  3/3       3/3    0     0       0
19 인구 많은 시군구 순위 보여줘    시연  3/3       3/3    0     0       0
20 대전역 연령대별 인구 알려줘     시연  3/3       3/3    0     0       0
21 경부선 선형 데이터 줘           시연  3/3       3/3    0     0       0
22 오송역 지나는 노선 알려줘       시연  0/3       0/3    3     0       0        {003, 021}       근접   CLARIFY  3회
23 경부선 주변 CCTV 보여줘         시연  3/3       3/3    0     0       0
24 오송역 근처 충전소 자세히 …     시연  3/3       3/3    0     0       0
25 문서에서 철도안전법 관련 …      시연  3/3       3/3    0     0       0
26 문서에서 철도 안전 교육 …       시연  3/3       3/3    0     0       0
27 철도안전법 내용 찾아줘          위험  0/3       0/3    0     0       3        {}               못붙음 NO_MATCH 3회
28 경부선 노선 보여줘              위험  3/3       0/3    0     3       0        {006, 013}       빗나감 CLARIFY  3회
29 청주시 인구 알려줘              애매  3/3       0/3    0     3       0        {029, 038, 039}  빗나감 CLARIFY  3회
30 오송역 선거구 알려줘            애매  3/3       0/3    0     3       0        {023, 025, 027}  빗나감 CLARIFY  3회
31 청주 국회의원 선거구 검색해줘   애매  3/3       3/3    0     0       0
                                        ──────────────────────────────────
                                        46/66     41/66  9     13      3        62%  ← 확장 열아홉
                                        70%       ← LLM 단독 · LLM 이 아무것도 안 쓴 것 3회

                                        ──────────────────────────────────
                                        61/93     62/93  15    13      3        67%  ← 합계
                                        66%       ← LLM 단독 · LLM 이 아무것도 안 쓴 것 3회
```

#### ★ 기존 아홉은 안 움직였다

```
서른째 (3회 × 3)   적중 21/27 (78%) · 근접 6 (발화 4 · 7) · 빗나감 0 · 못 붙음 0 · LLM 단독 15/27 (56%)
이번   (3회 × 3)   적중 21/27 (78%) · 근접 6 (발화 4 · 7) · 빗나감 0 · 못 붙음 0 · LLM 단독 15/27 (56%)
```

**아홉 줄이 「틀렸을 때 나온 것」 칸까지 글자 하나 안 다르다.** 4번은 `{038,
039}` CLARIFY 3회, 7번은 `{012, 030}` CLARIFY 3회로 「서른째」 그대로다.
발화를 스물둘 더 얹어도 아홉의 값이 안 흔들린다는 것을 이번에 처음 봤다 —
같은 서버 · 같은 menu 를 쓰므로 당연할 것 같지만 재본 적이 없었다.

#### ★★ 예상과 달랐던 것

**하나. 시연 표시 열넷 중 여섯이 쓸기와 달라졌다.** 쓸기에서 두 모델 모두
SELECT 10/10 으로 하나에 갔던 것들이다. **표시가 「시연」인데 빗나갔으면
그것이 발견이다** 라고 도구가 찍는 자리가 실제로 나왔다.

| # | 발화 | 쓸기 (「스물아홉째」) | 이번 |
|---|---|---|---|
| 11 | 오송역 행정경계 보여줘 | SELECT `{017}` 10/10 | 적중 2/3 · 근접 1 (`{017, 019}`) |
| 12 | 청주시 행정경계 보여줘 | SELECT `{004}` 10/10 | 적중 2/3 · **빗나감 1** (`{017, 019}`) |
| 14 | 오송역 국회의원 공약 보여줘 | SELECT `{025}` 10/10 | 적중 1/3 · 근접 2 (`{023, 025, 027}`) |
| 15 | 청주 선거구 찾아줘 | SELECT `{007}` 10/10 | 근접 3/3 (`{007, 008}`) |
| 17 | 청주 국회의원 공약 검색해줘 | SELECT `{009}` 10/10 | ★ **빗나감 3/3** (SELECT `{025}`) |
| 22 | 오송역 지나는 노선 알려줘 | SELECT `{003}` 10/10 | 근접 3/3 (`{003, 021}`) |

**쓸기와 지금 사이에 온톨로지가 한 번 바뀌었다** — 「서른째」가 관계 한 줄을
빼면서 recipe 가 41 에서 40 이 됐고 menu 도 68자 줄었다. 「서른째」는 그때
이미 무관한 발화 둘(4 · 7)이 프롬프트 길이에 흔들린 것을 적어 뒀다.
**그러니 설명 후보는 있지만 이번에도 안 갈랐다.** 소거법으로 남은 것이지
실측이 아니다.

**둘. 17 번이 제일 나쁘다 — 확신하고 틀린다.** 표시가 「시연」인데 빗나감
3/3 이다. 축을 보면 까닭이 보인다.

```
17번 축   쓸기   spoken_keyword · item_list · group_election · "국회의원 공약"
          이번   spoken_place   · item_list · group_election · "청주"
```

**"청주" 를 장소로 읽고 좌표 길로 갔다.** `025`(장소 → 좌표 → 선거구 공약
검색)를 SELECT 한다. 기대값 `009`(키워드 → 선거구 공약 검색)는 조회 후보에
아예 안 들어온다(★ 조회 빠짐 3회).

**셋. 28 · 29 · 30 은 LLM 이 맞혔는데 검산이 덮었다.** 이번 측정에서 제일 큰
것이다. 검산 표가 그대로 보여준다.

```
28  경부선 노선 보여줘            LLM 단독 {003}  →  최종 {006, 013}   ★ 맞는 답을 덮었다  3회
29  청주시 인구 알려줘            LLM 단독 {011}  →  최종 {029, 038, 039}  ★ 맞는 답을 덮었다  3회
30  오송역 선거구 알려줘          LLM 단독 {022}  →  최종 {023, 025, 027}  ★ 맞는 답을 덮었다  3회
```

셋 다 LLM 단독은 **사람이 정한 기대값과 정확히 같은 하나**를 골랐다. 그것을
검산이 갈아치웠다. 까닭은 축 조회 후보에 정답이 없어서다(★ 조회 빠짐).
**책임 소재가 LLM 이 아니라 축 쪽이라는 것이 이번에 갈렸다** — 「열린 과제」에
적어 둔 진단을 실측이 좁혀 준 것이다. 도구도 `resolve_service._verdict 를
다시 본다` 를 찍는다. **이번 범위가 아니라 안 고쳤다.**

**넷. 「조회 빠짐」이 처음 0 이 아니다.**

```
조회 판정 · 기준선 아홉  조회 적중  9회 · 조회 근접 15회 · ★ 조회 빠짐  0회 · 조회 없음 3회  (합 27회)
조회 판정 · 확장 열아홉  조회 적중  9회 · 조회 근접 38회 · ★ 조회 빠짐 13회 · 조회 없음 6회  (합 66회)
조회 판정 · 합계         조회 적중 18회 · 조회 근접 53회 · ★ 조회 빠짐 13회 · 조회 없음 9회  (합 93회)
```

「스물여섯째」부터 「서른째」까지 **줄곧 0 이던 칸**이다. 아홉으로 재면 여전히
0 이고, 열아홉을 얹자 13회가 나왔다. 발화는 12(1회) · 17(3) · 28(3) · 29(3) ·
30(3) 다. **「축으로 먼저 좁혀도 정답이 후보에 남는다」는 지금까지의 결론이
아홉이 좁아서 나온 것이었다.** 이 한 줄이 정답표를 늘린 값을 다 했다.

**다섯. 검산의 값이 두 묶음에서 정반대다.**

```
기준선 아홉   LLM 단독 56%  →  최종 78%    검산이 이득이다
확장 열아홉   LLM 단독 70%  →  최종 62%    ★ 검산이 순손해다
```

살린 10회 중 6회가 기준선(2 · 5번)이고, 덮은 9회는 **전부 확장**(28 · 29 ·
30)이다. **아홉으로만 보면 검산이 늘 이득으로 보였다.**

**여섯. 27번(위험)이 예상과 다르게 틀렸다.** 쓸기에서는 웹 검색(`006`)으로
확신하고 갔는데(SELECT 10/10) 이번에는 `given` 이 비고 `want` 가
`dataset_info` 로 나와 **맞는 recipe 가 없어 NO_MATCH** 다. 여전히 실패지만
**확신하고 틀리는 것에서 정직하게 못 한다고 말하는 쪽으로 옮겨 갔다** —
「틀린 답보다 정직한 되물음이 낫다」 기준으로는 나아진 것이다. 기대값은
안 고쳤다.

**일곱. 좋은 쪽으로 어긋난 것도 하나 있다.** 31번(애매)은 쓸기에서 넷으로
되묻던 것인데(`{007, 008, 009, 010}` CLARIFY 10/10) 이번에는 **SELECT `{007}`
3/3 적중**이다. 사람이 정한 기대값이 이미 맞는 자리가 됐다. 애매 셋 중
하나는 이미 고쳐져 있었던 셈이다.

**여덟. 20번(대전역)은 됐다.** 안 재본 값이었고 **적중 3/3** 이다.
지명을 오송역에서 대전역으로 바꿔도 4단 앞의 세 단이 그대로 선다.
24번(4단, `040`)도 3/3 이다. **사슬 넷짜리 두 자리가 다 섰다.**
25 · 26(문서 둘)도 3/3 이다.

#### 안 한 것

- **기대값을 하나도 안 고쳤다.** 빗나감 13회가 나왔지만 숫자에 맞춰 기대값을
  움직이지 않았다. 애매 셋의 기대값을 여럿으로 적어 지금 상태를 적중으로
  만들지도 않았다.
- **제품 코드를 한 줄도 안 고쳤다.** 온톨로지 · menu · 배선표 · `demo/` ·
  `vendor/` 를 안 건드렸다. `resolve_service._verdict` 도 안 봤다.
- **시연 여섯(11 · 12 · 14 · 15 · 17 · 22)이 왜 쓸기와 달라졌는지 안 갈랐다.**
  「서른째」의 관계 한 줄 빼기와 menu 68자 줄기가 설명 후보이지만 소거법으로
  남은 것이지 실측이 아니다. menu 를 인위로 늘려 되돌아가는지 재면 갈린다.
- **17번의 `given` 이 왜 `spoken_place` 로 갔는지 안 갈랐다.** "청주" 를 장소로
  읽은 것은 보이지만 쓸기 때는 `spoken_keyword` 였다.
- **화면(`POST /chat`)으로는 안 눌렀다.** `/resolve` 만 봤다.
- **모델을 하나만 봤다.** 쓸기는 두 모델이었는데 이번은 서버 기본(qwen3:32b)
  뿐이다. 달라진 여섯이 모델 탓인지 안 봤다.
- 확장 열아홉에는 관문을 안 걸었다. 처음 재는 값이라 기준이 없다.
  **이번 측정이 그 기준선이다.**

---

### 2026-08-26 (서른한째) · 문서 검색이 조각 두셋을 문서 이름 · 쪽과 함께 보여준다

모델 qwen3:32b(서버 기본) · recipe 40 · 지식베이스 문서 2
(철도안전법 47쪽 · 철도안전법 시행규칙 52쪽, `knowledge.listDocs` 실측).
**커밋 안 했다.** 고친 파일은 `demo/api/services/step_service.py`(배선 한 줄) ·
`vendor/asap/workflow_answer.py`(우리 파일) · 그 둘의 테스트 · 이 파일이다.

#### 1단계 — 문서 이름이 검색어로 걸리는가 (filter_docs 없이 되는가)

`knowledge.query` 를 Gateway 로 직접 눌렀다(`execute_service.USER_CONTEXT`
그대로). 응답 전문은 `tools/probe_out/knowledge.query.2026-08-26.*.json`.
칸은 문서별 조각 수이고 [ ] 안이 metadata.page(0부터 셈, 아래 「쪽수」 참조).

| 검색어 | k | 법(47쪽) | 시행규칙(52쪽) |
|---|---|---|---|
| 철도안전법 시행규칙 교육 | 4 | 0 | **4** [50·18·0·40] |
| 철도안전법 시행규칙 교육 | 10 | 1 [11] | **9** |
| 시행규칙 교육 | 4 | 0 | **4** [8·18·12·50] |
| 시행규칙 교육 | 10 | 0 | **10** |
| 철도안전법 교육 | 4 | **3** [11·0·32] | 1 [16] |
| 철도안전법 교육 | 10 | **8** | 2 [16·50] |
| 교육 | 4 | 1 [11] | 3 [7·17·28] |
| 교육 | 10 | 3 | 7 |
| 철도 안전 (기존 발화 인자) | 4 | **3** [0·1·33] | 1 [0] |
| 철도 안전 | 6 | **5** | 1 [0] |
| 철도 안전 | 10 | **9** | 1 [0] |
| 철도 안전 교육 | 4 | **3** [11·32·33] | 1 [16] |
| 철도 안전 교육 | 6 | **4** [11·32·33·0] | **2** [16·47] |
| 철도 안전 교육 | 10 | 5 | 5 |
| 철도안전법 | 4 | **4** [0·1·31·33] | 0 |
| 철도안전법 | 6 | **6** | 0 |
| 철도안전법 | 10 | **9** | 1 [0] |

**문서 이름이 섞이면 그 문서 조각이 위로 온다.** "철도안전법 시행규칙 교육"
k=4 는 시행규칙 4/4, "철도안전법" k=4 는 법 4/4 다. **filter_docs 없이 된다** —
2·3단계로 갔고, 인자 둘 문제는 「열린 과제」에 설계만 남겼다(아래).

#### 2단계 — k=6 으로 정했다

`("search_documents", "keyword")` 배선에 `"k": 6` 한 칸을 더했다.

- k=4 는 "철도 안전 교육" 에서 시행규칙 조각이 1건이라, 화면이 두셋을
  골라도 두 문서가 섞여 보이기 어렵다.
- k=6 이 두 문서가 다 보이는(법 4 · 시행규칙 2) **가장 작은 측정값**이다.
- k=10 은 5·5 로 더 고르지만 화면은 조각 두셋만 실으므로 응답 무게
  (조각당 content 962~995자)만 는다.
- **뒤집는 때** : 문서가 늘어 보여줄 문서가 조각 두셋으로 안 덮이면 올린다.
  값의 근거 주석은 배선 줄 위에 있다.

#### 3단계 — 규칙 한 줄과 왜

**「목록의 항목이 글(TEXT_KEYS)이면 앞 두셋(SHOWN_RECORDS 3)을 건수 아래
줄마다 늘어놓는다. 글이 아닌 목록은 예전대로 첫 항목 하나다.」**
(`workflow_answer._excerpt_lines`)

- 도구 이름이 아니라 결과 모양으로 가른다 — 이 파일은 도구 이름을 모른다는
  계약 그대로다.
- 글이 아닌 목록(CCTV · 행정구역 이름 목록)은 안 걸린다. 첫 항목의
  TEXT_KEYS 유무로 가르므로 이름만 있는 항목이 세 줄로 늘지 않는다
  (테스트 `test_글이_아닌_목록은_예전대로_첫_항목만이다`).
- 조각 줄은 `_record_line` 이 만들므로 정해 둔 칸만 읽고 길이를 자른다 —
  raw JSON 이 새는 자리가 안 늘었다.

**쪽수** : metadata.page 는 0부터 센다. 실측 — page 0 조각의 본문 머리가
"1", page 11 조각이 "12", page 50 조각이 "51". 그래서 1을 더해 사람이 세는
쪽수로 낸다(`_page_text`). page 는 정수(2026-08-26)로도 "0" 같은
문자열(2026-08-25)로도 온 실물이 있어 둘 다 받고, 못 읽으면 조용히 넘어간다.

**덤으로 잡은 것 — 낱자(NFD) 한글** : 실물 source 는 한글이 낱자로 풀려
와서 눈에 41자인 "철도안전법 시행규칙(…).pdf" 가 len 67 이다. SOURCE_LIMIT
48 에서 "「철도안전법 시행규칙(국토교통부령)(제015…」" 로 어중간하게 잘렸다
(첫 화면 실측). `_clip` 이 NFC 로 붙인 뒤 재도록 고쳤다.

#### 화면 문구 전후 (`POST /chat`, 각 1회)

전(前)의 문서 셋은 옛 코드(git HEAD)에 k=4 실물 응답을 넣어 재현한 것이다 —
이 발화 셋을 옛 코드 화면으로 누른 기록이 「스물다섯째」의 "철도 안전 문서
찾아줘" 하나뿐이라서다(그 기록과 재현이 일치한다).

```
"문서에서 철도안전법 관련 내용 찾아줘"
  전  1. knowledge.query   4건 · 「철도안전법(법률)(제21188호)(20260303).pdf」 · "법제처 1 국가법령정보센터 …"
  후  1. knowledge.query   6건
         「철도안전법(법률)(제21188호)(20260303).pdf」 1쪽 · "법제처 1 국가법령정보센터 철도안전법 철도안전법 [시행 2026. 3. 3.] …"
         「철도안전법(법률)(제21188호)(20260303).pdf」 2쪽 · "법제처 2 국가법령정보센터 철도안전법 제3조(다른 법률과의 관계) …"
         「철도안전법(법률)(제21188호)(20260303).pdf」 32쪽 · "④ 제1항부터 제3항까지에서 규정한 사항 외에 철도안전 자율보고에 …"
      (인자 "철도안전법" 이라 법 조각만 온다 — 1단계 표 그대로)

"철도 안전 문서 찾아줘" (기존 발화)
  전  1. knowledge.query   query="철도 안전"  4건 · 「철도안전법(법률)(제21188호)(20260303).pdf」 · "법제처 1 …"
  후  1. knowledge.query   query="철도 안전"  6건
         「철도안전법(법률)(제21188호)(20260303).pdf」 1쪽 · "법제처 1 국가법령정보센터 철도안전법 철도안전법 [시행 2026. 3. 3.] …"
         「철도안전법(법률)(제21188호)(20260303).pdf」 2쪽 · "법제처 2 국가법령정보센터 철도안전법 제3조(다른 법률과의 관계) …"
         「철도안전법 시행규칙(국토교통부령)(제01571호)(20260324).pdf」 1쪽 · "법제처 1 국가법령정보센터 철도안전법 시행규칙 …"

"문서에서 철도 안전 교육 내용 찾아줘"
  전  1. knowledge.query   query="철도 안전 교육"  4건 · 「철도안전법(법률)(제21188호)(20260303).pdf」 · "법제처 12 …"
  후  1. knowledge.query   query="철도 안전 교육"  6건
         「철도안전법(법률)(제21188호)(20260303).pdf」 12쪽 · "법제처 12 국가법령정보센터 철도안전법 제24조(철도종사자에 대한 안전 및 직무교육) …"
         「철도안전법 시행규칙(국토교통부령)(제01571호)(20260324).pdf」 17쪽 · "효기간 만료일”이라 한다) 전 12개월 이내에 실시한다. …"
         「철도안전법(법률)(제21188호)(20260303).pdf」 33쪽 · "법제처 33 국가법령정보센터 철도안전법 2. 철도안전전문기술자 …"
      (★ 두 문서가 섞여 나온다)

"오송역 근처 충전소 찾아줘" — 전과 같은 꼴
  1. geo.geocode       오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1 (127.3277, 36.6200)
  2. ev.searchStations  minLon=127.1598 · minLat=36.4852 · maxLon=127.4956 · maxLat=36.7547  500건 (전체 2,173건) · 청주오송역차고지

"오송역 CCTV 보여줘" — 전과 같은 꼴 (83건 그대로)
  1. geo.geocode       오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1 (127.3277, 36.6200)
  2. road.getCctv      minLon=127.1598 · minLat=36.4852 · maxLon=127.4956 · maxLat=36.7547  83건

"오송역 인구 구성 알려줘" — 되묻지 않고 SELECT 로 갔고 수치 한 줄 그대로
  1. geo.geocode       오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1 (127.3277, 36.6200)
  2. adminBoundary.findBoundaryByPoint  lon=127.3277 · lat=36.6200  3건 · 충청북도
  3. population.getAgeProfile  level="sigungu" · code="43113"  청주시 흥덕구 292,625명 (남 149,484 · 여 143,141) · 2026-06-30 기준
```

셋째 발화의 인구 이름 목록("3건 · 충청북도")이 안 늘어난 것이 3단계 규칙의
「글이 아닌 목록」 확인이기도 하다. 여섯 answer 전부에 geojson · coordinates ·
`[[` · http 가 없는 것을 grep 으로 확인했다.

k=6 인자가 화면에 안 찍히는 것은 `_input_text` 의 「결과 문구에 이미 있는
값은 안 적는다」에 "6" 이 "6건" 의 앞토막으로 걸려서다. 고치지 않았다 —
k 는 사람이 물은 값이 아니라 배선 상수라 안 보여도 잃는 것이 없다.

#### 관문

```
pytest                 210 passed / 1 failed — 기존 202 + 새 8, 기존 삭제 0.
                       실패 1은 graphviz 음성 대조군(기존과 같음)
check_wiring           recipe 40 · 완비 39 · STEP_OF 35줄 · A 0 · B 0 · C 1 (그대로)
check_resolve --runs 3 적중 21/27 · 근접 6 (발화 4 · 7) · 빗나감 0 · 못 붙음 0 ·
                       LLM 단독 15/27 — 「서른째」 기준선 그대로
raw JSON 안 샘         위 여섯 발화 grep 0건
목록 안 망가짐         충전소 500건 · CCTV 83건 · 인구 한 줄 그대로 (위 문구)
vendor 무결            git diff 의 vendor/asap/ 는 workflow_answer.py 하나
step_service 최소 변경 배선 한 줄("k": 6)과 그 줄 주석뿐
```

#### 안 한 것

- **filter_docs** — 1단계 실측으로 지금은 필요 없다. 문서 이름이 검색어에
  섞이면 그 문서가 위로 온다.
- **인자 둘(문서 이름 + 검색어)** — 발화 해석이 argument 하나만 돌려준다.
  「열린 과제」에 설계를 남겼다.
- **조각을 모아 LLM 이 요약하기** — 범위 밖(사람이 정했다).

### 2026-08-26 (서른째) · 사람이 41개를 훑어 고른 둘 중 하나만 뺐다 · 판정이 90/90 에서 21/27 로 떨어졌다

모델 qwen3:32b(서버 기본) · 온톨로지 노드 58 그대로 · 관계 108→107 ·
recipe 41→40 · 발화 9개 × **3회** × 세 묶음 = 81회.
**커밋 안 했다.** 고친 파일은 `ontology/ontology.yaml` ·
`ontology/_init/ontology.yaml` · `demo/api/services/step_service.py` ·
`tools/check_resolve.py` · `tests/ontology/test_shortlist.py` ·
`workflows/static` 의 recipe·menu(도구가 다시 만든 것) · 이 파일이다.

#### 왜 뺐나 — recipe 는 조합으로 자동 생성된다

`tools/rebuild_init.py` 는 온톨로지가 만들 수 있는 경로를 전부 만들고
`crosses_groups` 와 `MIN_STEPS` 로만 거른다. 그러니 **아무도 하지 않을 발화에
대응하는 경로**가 섞여 들어온다. 그것이 후보를 늘려 되묻기를 만든다.
「스물일곱째」에서 `ev.searchChargers` 를 뺀 뒤 전기차 발화가 전부 하나로
간 것이 그 증거다.

#### 사람이 41개를 훑은 기준

2026-08-26 에 사람이 41개 전부를 읽고 두 갈래로 물었다.

```
1  그 경로를 부르는 발화를 사람이 실제로 하는가
2  앞 단계 결과를 뒤 단계가 실제로 쓰는가 (배선을 본다)
```

둘 중 하나라도 「아니다」인 것이 둘이었다.

```
017  말한 식별자 → 충전소 상세 조회
     "PL033780 충전소 상태 알려줘" — 사람이 충전소 번호를 말하지 않는다. 1 에 걸린다

035  말한 장소 → 철도 구간 형상 조회 → 철도 노선 조회
     2 에 걸린다고 보았다
```

**뺀 것은 017 하나다.** 035 는 안 뺐다. 아래 「철도 사슬은 못 뺐다」 를 본다.

#### 무엇을 뺐나 — 노드가 아니라 관계 한 줄이다

```
- { from: spoken_identifier, to: station_id, predicate: is-a }
```

**`충전소 상세 조회` 노드는 남는다.** 검색 뒤에 붙는 경로 둘(새 036 · 040)이
그것을 쓴다. 뺀 것은 「말한 식별자가 충전소 번호이기도 하다」는 선언뿐이다.
`말한 식별자` 는 이제 선거구 코드 하나만 가리킨다 — 2026-08-24 에 행정구역
코드를 뗀 것과 같은 까닭이고(사람은 코드를 말하지 않는다) 같은 자리다.

```
노드    58 → 58   (안 뺐다)
관계   108 → 107
```

두 `ontology.yaml` 은 `cmp` 로 바이트 동일하다. 머리말에서 함께 고친 것 —
「받는 것」표의 `충전소 번호` 줄("ev.searchStations 도 내놓는다" → "만
내놓는다")과 「시작 데이터 노드 셋」의 `말한 식별자` 항목이다.

#### 죽은 배선도 함께 지웠다

`step_service.py` 의 `("get_ev_station", "station_id")` 에서 `input_first`
한 줄. 발화 값을 바로 `statId` 로 넣던 줄이고, 그 자리로 오는 경로가
사라졌으므로 영영 안 걸린다.

**`input_of` 는 안 지웠다.** `input_first` 를 적은 줄이 하나도 없어져 기계가
비었지만, `tools/check_wiring.py` 가 그 함수를 import 해 부르고 있어
함께 고쳐야 한다. `check_wiring.py` 는 이번 범위 밖이라 두고 함수 docstring과
`STEP_OF` 머리말에 "지금 이것을 적은 줄은 하나도 없다" 를 적었다.
**다음에 뺄 때는 두 파일을 함께 본다.**

#### 넷은 검토했고 두기로 했다 — 좌표를 만들어놓고 안 쓰는 줄 알았다

옛 022 · 024 · 026 · 028 (새 021 · 023 · 025 · 027) 넷이다.

```
말한 장소 → 장소 좌표 변환 → 철도 노선 조회
말한 장소 → 장소 좌표 변환 → 국회의원 전체 선거구 검색
말한 장소 → 장소 좌표 변환 → 국회의원 선거구 공약 검색
말한 장소 → 장소 좌표 변환 → 지방선거 교통 공약 검색
```

넷 다 끝 노드가 「장소 이름」이나 「키워드」도 받으므로, 앞의 좌표 변환이
헛돈다고 의심할 만하다. **배선을 보니 아니다.** 앞 노드가 `지도 범위` 를
건네므로 `wiring_at` 이 `map_extent` 줄을 고르고, 그 줄이 앞 단계 bbox 를
그대로 넘긴다.

```
("get_railway_lines",              "map_extent"): {"input": {"bbox": BBOX_FROM_PREVIOUS}}
("search_assembly_districts",      "map_extent"): {"input": {"bbox": BBOX_FROM_PREVIOUS}}
("search_assembly_pledge_districts","map_extent"): {"input": {"bbox": BBOX_FROM_PREVIOUS}}
("search_local_pledge_summaries",  "map_extent"): {"input": {"bbox": BBOX_FROM_PREVIOUS}}
```

즉 **그 지역으로 좁혀 검색한다.** 전국 검색이 아니다.
`말한 장소 → 그 도구` 2단 경로(새 003 · 007~010)와 뜻이 다르다 — 그쪽은
발화 값을 `query` 나 `stationName` 으로 넣는다.

**다음 사람이 같은 의심을 또 하지 않게 여기 적는다.** 넷을 빼려면 배선이
아니라 「같은 도구를 두 자리에 두는 것이 맞는가」를 물어야 하고, 그것은
이 넷만의 문제가 아니다.

#### ★ 철도 사슬은 못 뺐다 — 전제부터 거짓이었다

사람이 적은 근거는 이것이었다.

```
get_railway_lines 가 「장소 이름」 자리에서 발화 값(@arg)을 쓰므로
앞의 구간 형상이 헛돈다. 앞 단계 결과를 안 쓰는 유일한 경로다
```

**틀렸다.** `step_service.plan("recipe_035", "오송역")` 을 그대로 찍어 보면
2단계가 앞 단계 bbox 를 쓴다.

```
s1  rail.getSectionGeometry  {"sectionName": "오송역"}
s2  geo.getRailwayLines      {"bbox": ["$s1.minLon", "$s1.minLat", "$s1.maxLon", "$s1.maxLat"]}
```

`get_railway_section` 은 `map_extent` 만 내놓으므로 `wiring_at` 이 「장소
이름」줄에 닿을 수가 없다. 위의 넷과 똑같은 자리다.
**41개를 다 찍어 보니 앞 단계 결과를 안 쓰는 다단 경로는 하나도 없다.**
(옛 036 = 새 035 「말한 키워드 → 웹 검색 → 웹 문서 가져오기」만 step 이
하나로 줄지만 그것은 `web_fetch` 배선이 없어서다 — `check_wiring` 의 C 1개.)

**뺄 방법도 없다.** edge 를 한 줄씩 빼 보며 사라지는 사슬을 셌다
(파일은 안 건드리고 `store.edges` 를 갈아끼운 일회용 스크립트).
철도 사슬을 없애는 줄은 넷뿐이고 **최소 도미노가 2개**다.

| 뺀 줄 | 함께 사라지는 사슬 |
|---|---|
| `spoken_place is-a place_name` | 25개 |
| `get_railway_section hasInput place_name` | 5개 |
| `get_railway_section hasOutput map_extent` | 4개 |
| `get_railway_lines hasInput map_extent` | **2개** — 철도 사슬 + 옛 022 |

가장 작은 것조차 옛 022(말한 장소 → 좌표 변환 → 철도 노선 조회)를 함께
죽인다. **그 022 는 바로 위에서 「두기로 했다」고 정한 넷 중 하나다.**

`crosses_groups` 도 `MIN_STEPS` 도 이 사슬을 다른 것과 못 가른다 — 셋 다
`group_transport` 이고 3단이다. 손으로 고른 제외 목록을 `rebuild_init.py` 에
두는 길은 있으나, 그러면 **등록으로 생기는 recipe 와 `_init` 의 recipe 가
같은 규칙에서 안 나오게 된다**(`registry.new_recipes_for` 는 그 목록을
모른다). 프롬프트의 「억지로 규칙을 고치지 않는다」에 걸린다.

**그래서 안 뺐다.** 뺄 이유(전제)가 거짓이고, 뺄 방법도 없다.

#### recipe 대응표

`python tools/rebuild_init.py --write` 뒤 `reset_to_init()`.
**사라진 것은 하나뿐이고 예상과 같다.**

```
사라진 recipe   017 (말한 식별자 → 충전소 상세 조회)
합계            41개 → 40개
menu.yaml       3,390자 → 3,322자
```

| 새 번호 | 옛 번호 | 경로 |
|---|---|---|
| 001~016 | 그대로 | 번호가 안 밀린 구간 |
| — | 017 | ★ 사라짐. 말한 식별자 → 충전소 상세 조회 |
| 017 | 018 | 말한 장소 → 좌표 변환 → 행정구역 조회 |
| 018 | 019 | 말한 장소 → 좌표 변환 → 지점 행정구역 판별 |
| 019 | 020 | 말한 장소 → 좌표 변환 → VWorld 행정경계 조회 |
| 020 | 021 | 말한 장소 → 좌표 변환 → CCTV 조회 |
| 021 | 022 | 말한 장소 → 좌표 변환 → 철도 노선 조회 |
| 022 | 023 | 말한 장소 → 좌표 변환 → 지점 국회의원 지역구 판별 |
| 023 | 024 | 말한 장소 → 좌표 변환 → 국회의원 전체 선거구 검색 |
| 024 | 025 | 말한 장소 → 좌표 변환 → 지점 국회의원 전체 선거구 판별 |
| 025 | 026 | 말한 장소 → 좌표 변환 → 국회의원 선거구 공약 검색 |
| 026 | 027 | 말한 장소 → 좌표 변환 → 지점 국회의원 선거구 공약 판별 |
| 027 | 028 | 말한 장소 → 좌표 변환 → 지방선거 교통 공약 검색 |
| 028 | 029 | 말한 장소 → 좌표 변환 → 지점 지방선거 교통 공약 판별 |
| 029 | 030 | 말한 장소 → 좌표 변환 → 인구 통계 조회 |
| 030 | 031 | 말한 장소 → 좌표 변환 → 전기차 충전소 검색 |
| 031 | 032 | 말한 장소 → 철도 구간 형상 조회 → 행정구역 조회 |
| 032 | 033 | 말한 장소 → 철도 구간 형상 조회 → VWorld 행정경계 조회 |
| 033 | 034 | 말한 장소 → 철도 구간 형상 조회 → CCTV 조회 |
| 034 | 035 | 말한 장소 → 철도 구간 형상 조회 → 철도 노선 조회 |
| 035 | 036 | 말한 키워드 → 웹 검색 → 웹 문서 가져오기 |
| 036 | 037 | 말한 키워드 → 전기차 충전소 검색 → 충전소 상세 조회 |
| 037 | 038 | 말한 장소 → 좌표 변환 → 지점 행정구역 판별 → 지방선거 교통 공약 상세 |
| 038 | 039 | 말한 장소 → 좌표 변환 → 지점 행정구역 판별 → 연령별 인구 구성 조회 |
| 039 | 040 | 말한 장소 → 좌표 변환 → 지점 행정구역 판별 → 인구 변화 추이 조회 |
| 040 | 041 | 말한 장소 → 좌표 변환 → 전기차 충전소 검색 → 충전소 상세 조회 |

옛 017 이 사라져 018 이후가 하나씩 당겨졌다.

#### GT 를 옮겼다 — 기대값은 한 글자도 안 바꿨다

**사슬이 같은 recipe 의 새 번호로만 옮겼다.** 아홉 발화의 기대 사슬은 하나도
안 사라졌다.

| # | 발화 | 옛 | 새 | 사슬 |
|---|---|---|---|---|
| 1 | 오송역 위치 보여줘 | 001 | 001 | 말한 장소 → 좌표 변환 |
| 2 | 오송역 좌표 알려줘 | 001 | 001 | 말한 장소 → 좌표 변환 |
| 3 | 오송역 CCTV 보여줘 | 021 | **020** | 말한 장소 → 좌표 변환 → CCTV 조회 |
| 4 | 청주시 인구 구성 알려줘 | 039 | **038** | 말한 장소 → 좌표 변환 → 지점 행정구역 판별 → 연령별 인구 구성 조회 |
| 5 | 오송역 근처 충전소 찾아줘 | 031 | **030** | 말한 장소 → 좌표 변환 → 전기차 충전소 검색 |
| 6 | 국회의원 선거구 찾아줘 | 007 | 007 | 말한 키워드 → 국회의원 지역구 검색 |
| 7 | 전기차 충전소 데이터 검색해줘 | 012 | 012 | 말한 키워드 → 전기차 충전소 검색 |
| 8 | 철도 안전 문서 찾아줘 | 013 | 013 | 말한 키워드 → 문서 검색 |
| 9 | 충북 제1선거구 알려줘 | 014 | 014 | 말한 식별자 → 국회의원 지역구 상세 |

옛 번호는 소스에 옆 주석으로 남겼다. 머리말의 낡은 숫자도 고쳤다
("recipe 가 41개" → 40개, "menu 45문장" → 40문장).
`tools/check_argument.py` 는 안 건드렸다 — GT 번호를 안 갖는다.
그 파일의 머리말에 있는 `recipe_014` · `recipe_011` 은 2026-08-24 실측의
기록이라 그때 번호가 맞다.

#### `check_wiring` 전후

```
전  recipe 41개 · 배선이 다 있는 것 40개 · STEP_OF 35줄 · A 0개 · B 0개 · C 1개
후  recipe 40개 · 배선이 다 있는 것 39개 · STEP_OF 35줄 · A 0개 · B 0개 · C 1개
```

C 는 그대로 `web_fetch × 웹 주소` 하나다. `STEP_OF` 줄 수는 안 줄었다 —
지운 것이 줄이 아니라 줄 안의 `input_first` 키 하나여서다.

`pytest` 202 passed / 1 failed (전과 같은 203개). 실패 하나는 graphviz 버전
차이(음성 대조군)다. `tests/ontology/test_shortlist.py` 의
`test_three_axes_together_narrow_to_what_the_utterance_asked` 를 고쳤다 —
기대값 `[031]` 이 `[030]` 이 됐다. **지키려던 뜻은 그대로다**(축 셋을 다
주면 하나로 좁혀진다). docstring 의 "41개" 도 40개로 고쳤다.

#### ★ 측정 회차를 10 에서 3 으로 줄였다

「스물아홉째」가 **60발화 × 2모델 × 10회** 로 확인한 것이다 —
**갈린 발화의 갈린 회차가 예외 없이 1회차이고 2~10회차는 서로 한 글자도
안 다르다.** 1차에서 갈린 10개, 2차에서 갈린 5개가 전부 그랬고, 셋을 따로
다시 눌러 1회차 값까지 재현됐다.

그러니 「10회 돌렸다」는 독립 표본 10개가 아니라 **1회차 하나와 2~10회차
하나, 두 가지**다. 3회면 그 둘을 다 본다. 한 묶음이 32분에서 10분 아래로
줄어 세 묶음을 같은 시간 안에 돌릴 수 있다.

**이번 세 묶음이 그것을 다시 보였다.** 적중 표는 세 묶음이 글자까지 같고,
갈린 것은 「LLM 단독」칸의 1회차뿐이다.

```
묶음1   1번 LLM 단독 1/3 · 2번 2/3      1번 축이 group_transport 2회 · - 1회
묶음2   1번 LLM 단독 3/3 · 2번 0/3      축은 아홉 발화 모두 3/3 으로 하나
묶음3   묶음2 와 바이트까지 같다
```

합계는 세 묶음 다 `LLM 단독 15/27`, `적중 21/27` 이다.

**되돌릴 조건.** 갈린 회차가 1회차가 아닌 것이 한 번이라도 나오면 3회로는
못 본다. 그때 10회로 되돌리고 여기에 적는다.

#### ★★ 세 묶음 — 판정이 떨어졌다. 되돌리지 않고 그대로 싣는다

적중 표는 세 묶음이 같다. 한 표로 싣는다.

```
#  발화                        LLM 단독   적중    근접  빗나감  못 붙음   틀렸을 때 나온 것
1  오송역 위치 보여줘          3/3        3/3     0     0       0
2  오송역 좌표 알려줘          0/3        3/3     0     0       0
3  오송역 CCTV 보여줘          3/3        3/3     0     0       0
4  청주시 인구 구성 알려줘     0/3        0/3     3     0       0        {038, 039} CLARIFY 3회
5  오송역 근처 충전소 찾아줘   0/3        3/3     0     0       0
6  국회의원 선거구 찾아줘      3/3        3/3     0     0       0
7  전기차 충전소 데이터 검색   0/3        0/3     3     0       0        {012, 030} CLARIFY 3회
8  철도 안전 문서 찾아줘       3/3        3/3     0     0       0
9  충북 제1선거구 알려줘       3/3        3/3     0     0       0
                              ─────────────────────────────────
                              15/27      21/27   6     0       0        78%
```

(위는 묶음2·3 의 표다. 묶음1 은 「LLM 단독」칸만 1번 1/3 · 2번 2/3 으로
다르고 나머지 네 칸은 같다.)

```
기준선 (「스물일곱째」, 10회 × 3)   적중 90/90 (100%) · 근접  0 · 빗나감 0 · 못 붙음 0 · LLM 단독 69/90 (77%)
이번   (3회 × 3)                    적중 21/27 ( 78%) · 근접  6 · 빗나감 0 · 못 붙음 0 · LLM 단독 15/27 (56%)
```

**빗나감은 0 을 지켰다.** 떨어진 둘은 전부 근접(CLARIFY)이라 사람이 고르면
닿는다. 그래도 떨어진 것은 떨어진 것이다.

| # | 발화 | 스물일곱째 | 이번 | 무엇이 움직였나 |
|---|---|---|---|---|
| 4 | 청주시 인구 구성 알려줘 | **적중 10/10 · SELECT** | **근접 3/3 · CLARIFY {038,039}** | 조회 후보 3 그대로. LLM 이 둘로 남겼다 |
| 7 | 전기차 충전소 데이터 검색해줘 | **적중 10/10 · SELECT** | **근접 3/3 · CLARIFY {012,030}** | ★ `given` 이 `spoken_keyword` 에서 **null** 로 · 인자도 null · 조회 후보 1 → 2 |
| 1·2·3·5·6·8·9 | | 적중 10/10 | 적중 3/3 그대로 | |

**이번에 뺀 것과 4 · 7 은 상관이 없다.** 4 는 인구이고, 7 의 기대값 012
(말한 키워드 → 전기차 충전소 검색)는 번호도 사슬도 안 움직였다. 사라진 옛
017 은 두 발화 어느 후보에도 없었다.

**남는 설명은 또 프롬프트 길이다.** menu 가 3,390자에서 3,322자로 줄었다.
「스물일곱째」가 이렇게 적어 뒀다 — *"문장 하나만 더 길어져도 4 와 6 은 다시
CLARIFY 로 돌아갈 수 있는 자리다"*. **68자 짧아지자 4 가 실제로 돌아갔다.**
「열한째」·「열여덟째」·「스물한째」·「스물여섯째」·「스물일곱째」에 이어
**여섯 번째로 프롬프트 길이가 무관한 발화를 흔들었다.** 이번에는 나쁜 쪽이다.

**7 은 결이 다르다.** 판정이 아니라 **축이 움직였다** — `given` 이 null 이 되고
인자도 안 뽑혔다. 「건드리기 전에 읽을 것」의 *"데이터 줘 조합만 given 을
통째로 비운다"* 와 같은 모양인데, 이 발화는 "…데이터 검색해줘" 라 그 조합이
아니다. **왜 이번에 비었는지 안 갈랐다.**

```
7번 축     스물일곱째  spoken_keyword · item_list · group_ev · "전기차 충전소"
           이번        -              · item_list · group_ev · -
7번 후보   스물일곱째  조회 후보 1 (012)
           이번        조회 후보 2 (012 · 030)   given 이 비어 시작점으로 못 좁힌다
```

`given` 이 비면 shortlist 가 시작점으로 못 좁혀 후보가 는다. **축이 판정을
끄는 자리를 처음 본 것은 아니지만**(「스물여섯째」) 그때는 축이 안정적이었다.

#### 조회 판정

```
스물일곱째  조회 적중 40/90 · 조회 근접 50 · ★ 조회 빠짐 0 · 조회 없음 0
이번        조회 적중  9/27 · 조회 근접 15 · ★ 조회 빠짐 0 · 조회 없음 3
```

**「조회 빠짐」은 여전히 0 이다.** 축으로 먼저 좁혀도 정답이 후보에 남는다.
「조회 없음」3회는 9번(충북 제1선거구)이다 — `want` 가 `dataset_info` 로
나와 맞는 recipe 가 없다. 「스물일곱째」에는 조회 후보 3 이었으니 이 자리도
움직였다. 판정은 3/3 적중 그대로다(LLM 이 맞게 골랐고 검산이 안 덮었다).

#### 안 한 것

- **커밋 안 했다.** 커밋 메시지 초안만 냈다.
- **떨어진 판정을 되돌리지 않았다.** GT 도 안 고쳤다. 프롬프트의 관문이
  "떨어지면 표 그대로 싣고 멈춰 보고한다" 다.
- **철도 사슬을 안 뺐다.** 위 「철도 사슬은 못 뺐다」.
- **4 · 7 이 왜 떨어졌는지 확인 안 했다.** 프롬프트 길이는 소거법으로 남은
  설명이지 실측이 아니다. menu 를 인위로 68자 늘려 되돌아가는지 재보면
  갈리지만 안 했다. 7 의 `given` 이 왜 비었는지도 안 갈랐다.
- **화면(`POST /chat`)으로는 안 눌렀다.** `/resolve` 만 봤다.
- `vendor/` · `demo/graph_svg/layout.json` 은 안 건드렸다.
  `layout.json` 에는 노드가 그대로 다 있다 — 이번에 뺀 것이 노드가 아니라
  관계 한 줄이라 애초에 움직일 것이 없다.
- `demo/ui/components/sample_picker.py` 의 `SAMPLES` 는 안 건드렸다.
  거기 적힌 `recipe_002`(CCTV)는 이번 변경 전에 이미 낡은 번호다.
  범위 밖이라 두고 여기에 적는다.

---

### 2026-08-26 (스물아홉째) · 발화 후보 예순을 두 모델로 쓸어 「무엇이 나오는가」만 적는다 · 재기만 했다

**정답을 안 정했다.** 적중·근접 같은 판정을 매기지 않았고 `check_resolve.UTTERANCES`
(정답표)를 읽지도 않았다. 무엇이 맞는지는 이 표를 보고 사람이 정한다.
아래는 전부 「나온 것」이다.

**왜 쓸었나.** 지금 재는 발화는 아홉 개뿐인데 recipe 는 41개다. 내일 발화를
고르려면 후보가 어디로 흘러가는지를 먼저 봐야 한다. 특히 **한 자리에 여러
recipe 가 몰려 안 갈리는 곳**(경계 도형 셋 · 선거 지점 셋 · 인구 셋)이 발화
말투를 바꾸면 갈리는지, 아니면 무슨 말을 해도 되묻는지가 이 표의 요점이다.
「죽음」인 자리(지방선거 공약 셋 · 웹 둘, 「스물여덟째」)는 겨냥하지 않았다 —
다만 발화가 그리로 흘러가면 그대로 적었다.

**어떻게 쟀나.** 새 파일 `tools/sweep_utterances.py` 하나만 만들었다. 발화
예순 개가 그 안에 있다. `check_resolve._call_resolve` 를 그대로 불러 쓴다 —
화면이 지나는 것과 같은 경로여야 두 도구의 표를 나란히 놓을 수 있다.
제품 코드는 한 줄도 안 고쳤다. 산출물은 `tools/sweep_out/` 에 있다
(`.gitignore` 에 한 줄 더했다. 측정 산출물이라 저장소에 안 들어간다).

```
차수  모델                발화 × 회차   걸린 시간   오류
1차   qwen3:32b (서버 기본)  60 × 10      32.3분     0
2차   qwen3:8b              60 × 10       9.4분     0
3차   —                     안 함        —          —
```

**3차를 안 했다.** `ollama list` 에 `qwen3:32b` 와 `qwen3:8b` 둘뿐이다.
`qwen3:14b` 도 더 큰 모델도 이 장비에 안 받아져 있다. 모델을 새로 내려받지
않는 것이 이 작업의 규칙이라 1·2차만 하고 여기 적는다.

```
tools/sweep_out/sweep-server-default-2026-08-26.txt   .json    1차
tools/sweep_out/sweep-qwen3-8b-2026-08-26.txt         .json    2차
tools/sweep_out/sweep-server-default-firstrun-check.txt .json  아래 「첫 회차」 확인용
```

#### ★ 먼저 볼 것 — 10회 반복이 실은 두 가지밖에 안 본다

**갈린 발화의 갈린 회차가 예외 없이 1회차다.** 1차에서 갈린 발화 10개,
2차에서 갈린 발화 5개, 합쳐 15개가 전부 그렇다. 2~10회차는 서로 한 글자도
안 다르다.

발화 셋(31 · 35 · 44)을 따로 다시 눌러 확인했다. 1회차가 또 갈렸고, **1회차에
나온 값이 본 쓸기의 1회차 값과 같았다.**

```
발화                  1회차                      2~10회차
오송역 인구 알려줘      SELECT  {030}              CLARIFY {030, 039, 040}   ×9
청주시 인구 순위 알려줘  CLARIFY {030, 039, 040}    SELECT  {030}             ×9
경부선 구간 형상 보여줘  SELECT  {002}              CLARIFY {004, 005}        ×9
```

**뜻하는 것.** 「10회 돌렸다」가 「독립된 표본 10개」가 아니다. 실제로는
1회차 하나와 2~10회차 하나, **두 가지**를 본 것이다. 흔들림의 폭을 이 표로
재려 하면 안 된다.

**원인은 안 봤다.** 캐시인지 warm-up 인지 다른 것인지 파고들지 않았다 —
이 작업은 관측이고 제품 코드를 안 건드리는 것이 규칙이라 여기까지만 적는다.
**다음에 이 저장소에서 무엇이든 여러 번 눌러 재는 사람은 이것을 먼저 확인할 것.**

#### 예순 줄 표 — 가장 많이 나온 것

「자리」는 무엇을 겨냥했는지지 정답이 아니다.
가 경계 도형 · 나 선거(지점) · 다 선거(이름 검색) · 라 인구 · 마 철도 ·
바 이미 되는 것의 말투 변주.

```
자리 번호  발화                                  qwen3:32b (1차)                         qwen3:8b (2차)
---------------------------------------------------------------------------------------------------------------------------------------
가   1     오송역 행정구역 알려줘                SELECT {019} 10/10                      SELECT {019} 10/10
가   2     오송역이 어느 동인지 알려줘           SELECT {019} 10/10                      SELECT {019} 10/10
가   3     오송역이 어느 행정동에 속하는지 알려줘SELECT {019} 10/10                      SELECT {019} 10/10
가   4     오송역 행정경계 보여줘                SELECT {018} 10/10                      SELECT {019} 10/10
가   5     오송역 경계 도형 내려받아줘           CLARIFY {018, 019, 020} 9/10            SELECT {020} 10/10
가   6     오송역 행정구역 경계 지도에 그려줘    SELECT {018} 10/10                      SELECT {020} 10/10
가   7     청주시 행정경계 보여줘                SELECT {004} 10/10                      SELECT {004} 10/10
가   8     청주시 경계 도형 줘                   CLARIFY {018, 019, 020} 10/10           SELECT {004} 10/10
가   9     경부선이 지나는 행정구역 알려줘       SELECT {032} 10/10                      SELECT {032} 10/10
가   10    경부선 통과 행정구역 목록 뽑아줘      CLARIFY {006, 013} 10/10                SELECT {022} 10/10

나   11    오송역 국회의원 선거구 알려줘         CLARIFY {024, 026, 028} 10/10           SELECT {019} 9/10
나   12    오송역이 어느 선거구인지 알려줘       CLARIFY {024, 026, 028} 10/10           SELECT {019} 9/10
나   13    오송역은 무슨 선거구에 들어가?        CLARIFY {024, 026, 028} 9/10            SELECT {019} 10/10
나   14    오송역 국회의원 누구야                SELECT {025} 10/10                      CLARIFY {023, 025, 027, 038} 10/10
나   15    오송역 지역구 국회의원 알려줘         SELECT {025} 10/10                      CLARIFY {023, 025, 027, 038} 9/10
나   16    오송역 선거구 당선인 알려줘           SELECT {025} 10/10                      CLARIFY {023, 025} 9/10
나   17    오송역 선거구 공약 알려줘             CLARIFY {024, 026, 028} 10/10           CLARIFY {025, 027} 9/10
나   18    오송역 국회의원 공약 보여줘           SELECT {026} 10/10                      CLARIFY {025, 027} 9/10
나   19    오송역 국회의원 교통 공약 알려줘      CLARIFY {026, 028} 10/10                CLARIFY {023, 025, 027, 029, 038} 10/10
나   20    오송역 선거구 철도 공약 알려줘        CLARIFY {024, 026, 028} 10/10           CLARIFY {023, 025, 027, 029, 038} 10/10

다   21    청주 국회의원 선거구 검색해줘         CLARIFY {007, 008, 009, 010} 10/10      CLARIFY {024, 026} 10/10
다   22    청주 선거구 찾아줘                    SELECT {007} 10/10                      SELECT {036} 10/10
다   23    청주 선거구 당선인 검색해줘           CLARIFY {007, 009} 9/10                 SELECT {036} 10/10
다   24    청주 지역 당선인 알려줘               CLARIFY {007, 008, 009} 9/10            CLARIFY {025, 038} 10/10
다   25    청주 국회의원 공약 검색해줘           SELECT {009} 10/10                      SELECT {009} 10/10
다   26    청주 선거구 공약 검색해줘             CLARIFY {008, 009} 10/10                SELECT {026} 9/10
다   27    철도 공약 있는 선거구 찾아줘          SELECT {009} 10/10                      CLARIFY {008, 009} 9/10
다   28    교통 공약 많은 선거구 검색해줘        SELECT {008} 10/10                      SELECT {008} 10/10
다   29    더불어민주당 당선인 선거구 검색해줘   SELECT {009} 10/10                      CLARIFY {007, 008, 009} 10/10
다   30    충북 선거구 목록 보여줘               SELECT {007} 9/10                       CLARIFY {007, 008} 10/10

라   31    오송역 인구 알려줘                    CLARIFY {030, 039, 040} 9/10            CLARIFY {039, 040} 9/10
라   32    오송역 일대 인구 얼마야               SELECT {030} 10/10                      SELECT {030} 10/10
라   33    청주시 인구 알려줘                    CLARIFY {030, 039, 040} 10/10           CLARIFY {030, 039, 040} 10/10
라   34    청주시 인구 몇 명이야                 CLARIFY {030, 039, 040} 10/10           CLARIFY {030, 039, 040} 10/10
라   35    청주시 인구 순위 알려줘               SELECT {030} 9/10                       CLARIFY {030, 039, 040} 10/10
라   36    인구 많은 시군구 순위 보여줘          SELECT {011} 10/10                      SELECT {011} 10/10
라   37    오송역 연령대별 인구 알려줘           SELECT {039} 10/10                      SELECT {039} 10/10
라   38    오송역 주변 연령별 인구 구성 보여줘   SELECT {039} 10/10                      SELECT {039} 10/10
라   39    오송역 인구 변화 알려줘               SELECT {040} 10/10                      SELECT {040} 10/10
라   40    오송역 인구 추이 보여줘               SELECT {040} 10/10                      SELECT {040} 10/10
라   41    청주시 인구 통계 검색해줘             CLARIFY {030, 039, 040} 9/10            CLARIFY {030, 039, 040} 10/10

마   42    경부선 노선 보여줘                    CLARIFY {006, 013} 10/10                CLARIFY {006, 013} 10/10
마   43    경부선 노선도 알려줘                  SELECT {003} 9/10                       SELECT {036} 10/10
마   44    경부선 구간 형상 보여줘               CLARIFY {004, 005} 9/10                 CLARIFY {004, 005} 10/10
마   45    경부선 선형 데이터 줘                 SELECT {002} 10/10                      CLARIFY {004, 005} 9/10
마   46    오송역 지나는 노선 알려줘             SELECT {003} 10/10                      SELECT {022} 9/10
마   47    오송역 정차 노선 뭐 있어              CLARIFY {003, 022, 035} 9/10            CLARIFY {003, 022, 035} 10/10
마   48    경부선 주변 CCTV 보여줘               SELECT {034} 10/10                      CLARIFY {003, 021, 022, 034, 035} 10/10
마   49    경부선 구간 CCTV 알려줘               SELECT {034} 10/10                      CLARIFY {003, 021, 022, 034, 035} 9/10

바   50    철도 안전 문서 찾아줘                 SELECT {013} 10/10                      SELECT {006} 10/10
바   51    철도 안전 문서 줘                     CLARIFY {003, 021, 022, 034, 035} 10/10 SELECT {006} 10/10
바   52    철도안전법 내용 찾아줘                SELECT {006} 10/10                      SELECT {006} 10/10
바   53    철도 안전 관련 자료 검색해줘          SELECT {006} 9/10                       SELECT {006} 10/10
바   54    전기차 충전소 검색해줘                CLARIFY {012, 031} 9/10                 SELECT {012} 10/10
바   55    전기차 충전소 데이터 줘               SELECT {012} 10/10                      CLARIFY {012, 031, 037} 10/10
바   56    충전소 어디 있어                      SELECT {012} 9/10                       SELECT {031} 10/10
바   57    오송역 CCTV 보여줘                    SELECT {021} 10/10                      CLARIFY {021, 034} 10/10
바   58    오송역 주변 CCTV 알려줘               SELECT {021} 10/10                      CLARIFY {021, 034} 10/10
바   59    오송역 근처 충전소 자세히 알려줘      SELECT {041} 10/10                      SELECT {041} 9/10
바   60    오송역 근처 충전소 찾아줘             SELECT {031} 10/10                      SELECT {031} 10/10
```

#### 하나로 가는 발화와 되묻는 발화

```
                         qwen3:32b   qwen3:8b
SELECT 10/10 인 발화        35개        34개
CLARIFY 가 우세한 발화       22개        26개
status 가 10회 한결같은 발화  51/60      57/60
SELECT·CLARIFY 아닌 회차     0/600      0/600
```

**두 차수 다 SELECT 10/10 이고 같은 recipe 로 간 발화 (19개).** 말투를 바꿔도
모델을 바꿔도 안 움직인 자리다.

```
  1  오송역 행정구역 알려줘            {019}      32  오송역 일대 인구 얼마야       {030}
  2  오송역이 어느 동인지 알려줘        {019}      36  인구 많은 시군구 순위 보여줘  {011}
  3  오송역이 어느 행정동에 속하는지…    {019}      37  오송역 연령대별 인구 알려줘   {039}
  7  청주시 행정경계 보여줘            {004}      38  오송역 주변 연령별 인구 구성… {039}
  9  경부선이 지나는 행정구역 알려줘     {032}      39  오송역 인구 변화 알려줘      {040}
 25  청주 국회의원 공약 검색해줘        {009}      40  오송역 인구 추이 보여줘      {040}
 28  교통 공약 많은 선거구 검색해줘      {008}      52  철도안전법 내용 찾아줘       {006}
 59  오송역 근처 충전소 자세히 알려줘    {041}      60  오송역 근처 충전소 찾아줘     {031}
```

(더해서 4 · 6 · 22 · 43 · 46 · 50 · 56 은 두 차수 다 SELECT 10/10 이지만
**모델마다 다른 recipe 로 갔다.** 아래 모델 비교에 있다.)

**두 차수 다 CLARIFY 가 우세한 발화 (16개).** 되묻는 개수와 무엇무엇인지.

```
발화                        qwen3:32b                              qwen3:8b
17 오송역 선거구 공약 알려줘     3개 {024, 026, 028}                    2개 {025, 027}
19 오송역 국회의원 교통 공약     2개 {026, 028}                         5개 {023, 025, 027, 029, 038}
20 오송역 선거구 철도 공약      3개 {024, 026, 028}                    5개 {023, 025, 027, 029, 038}
21 청주 국회의원 선거구 검색     4개 {007, 008, 009, 010}               2개 {024, 026}
24 청주 지역 당선인 알려줘      3개 {007, 008, 009}                    2개 {025, 038}
31 오송역 인구 알려줘          3개 {030, 039, 040}                    2개 {039, 040}
33 청주시 인구 알려줘          3개 {030, 039, 040}                    3개 {030, 039, 040}
34 청주시 인구 몇 명이야        3개 {030, 039, 040}                    3개 {030, 039, 040}
41 청주시 인구 통계 검색해줘     3개 {030, 039, 040}                    3개 {030, 039, 040}
42 경부선 노선 보여줘          2개 {006, 013}                         2개 {006, 013}
44 경부선 구간 형상 보여줘      2개 {004, 005}                         2개 {004, 005}
47 오송역 정차 노선 뭐 있어     3개 {003, 022, 035}                    3개 {003, 022, 035}
```

**자리별로 나온 것.**

```
가 경계 도형   32b 는 018 · 019 · 020 · 004 로 흩어지고 5 · 8 에서 셋을 다 되묻는다.
              「어느 동인지」(1·2·3)는 셋 다 019 로 10/10. 「행정경계 보여줘」(4)는
              018, 「경계 도형 내려받아줘」(5)는 되묻기. 8b 는 되묻는 자리가 없고
              4 를 019, 6 을 020, 8 을 004 로 보냈다
나 선거·지점   32b 는 11 · 12 · 13(선거구가 어디냐)을 전부 {024, 026, 028} 3개로
              되묻고, 14 · 15 · 16(누구냐 · 당선인)은 025 로 10/10 이다.
              8b 는 정반대로 11 · 12 · 13 을 019(경계 도형 자리)로 보내고
              14 · 15 · 16 을 되묻는다
다 선거·검색   32b 는 22(선거구 찾아줘) 007, 25 009, 27 009, 28 008, 29 009,
              30 007 로 하나씩 가고 21 에서만 넷을 다 되묻는다.
              8b 는 22 · 23 을 036 으로, 21 을 {024, 026} 으로 보낸다
라 인구        「인구 알려줘」류(31 · 33 · 34 · 41)는 두 차수 모두 {030, 039, 040}
              언저리로 되묻는다. 반면 「연령대별」(37 · 38) · 「변화 · 추이」(39 · 40)
              · 「순위」(36)는 두 차수 다 하나로 간다. **말에 무엇을 물었는지가
              들어가면 갈리고, 그냥 「인구」면 안 갈린다**
마 철도        42(노선 보여줘)는 두 차수 다 {006, 013} 으로 되묻는다. 43(노선도)은
              32b 003 · 8b 036. 44(구간 형상)는 두 차수 다 {004, 005}.
              48 · 49(경부선 CCTV)는 32b 가 034 로 10/10, 8b 는 다섯을 되묻는다
바 변주        50 · 51 · 52 · 53(철도 안전 문서)이 32b 에서 013 · 되묻기 · 006 ·
              006 넷으로 흩어진다. 「찾아줘」와 「줘」와 「내용 찾아줘」가 서로 다르다.
              8b 는 넷 다 006 으로 10/10. 54 · 55 · 56(충전소)도 32b 에서
              되묻기 · 012 · 012 로 갈리고 8b 는 012 · 되묻기 · 031 로 갈린다.
              57 · 58(오송역 CCTV)은 32b 021 10/10, 8b 는 {021, 034} 되묻기
```

#### 회차마다 갈리는 발화 — 전부 1회차가 갈린 것이다

```
1차 qwen3:32b (10개)
   5  오송역 경계 도형 내려받아줘   CLARIFY {018, 019, 020} ×9 / SELECT {020} ×1
  13  오송역은 무슨 선거구에 들어가?  CLARIFY {024, 026, 028} ×9 / SELECT {019} ×1
  30  충북 선거구 목록 보여줘      SELECT {007} ×9 / CLARIFY {007, 008} ×1
  31  오송역 인구 알려줘         CLARIFY {030, 039, 040} ×9 / SELECT {030} ×1
  35  청주시 인구 순위 알려줘      SELECT {030} ×9 / CLARIFY {030, 039, 040} ×1
  41  청주시 인구 통계 검색해줘     CLARIFY {030, 039, 040} ×9 / SELECT {011} ×1
  43  경부선 노선도 알려줘        SELECT {003} ×9 / CLARIFY {002, 003, 022, 035} ×1
  44  경부선 구간 형상 보여줘      CLARIFY {004, 005} ×9 / SELECT {002} ×1
  47  오송역 정차 노선 뭐 있어     CLARIFY {003, 022, 035} ×9 / CLARIFY {022, 035} ×1
  54  전기차 충전소 검색해줘       CLARIFY {012, 031} ×9 / SELECT {012} ×1

2차 qwen3:8b (5개)
  15  오송역 지역구 국회의원 알려줘   CLARIFY {023, 025, 027, 038} ×9 / CLARIFY {023, 025, 027} ×1
  18  오송역 국회의원 공약 보여줘    CLARIFY {025, 027} ×9 / SELECT {025} ×1
  27  철도 공약 있는 선거구 찾아줘   CLARIFY {008, 009} ×9 / CLARIFY {007, 008, 009} ×1
  31  오송역 인구 알려줘         CLARIFY {039, 040} ×9 / SELECT {039} ×1
  49  경부선 구간 CCTV 알려줘     CLARIFY {003, 021, 022, 034, 035} ×9 / SELECT {034} ×1
```

**status 와 최종 후보는 같은데 LLM 후보(검산 전)만 갈린 발화도 있다.**
1차 4개(23 · 24 · 53 · 56), 2차 8개(11 · 12 · 16 · 17 · 26 · 45 · 46 · 59).
이것도 전부 1회차다. 표 파일의 「판정은 같은데 축·인자만 갈리는 발화」에 있다.

#### 모델을 바꿨을 때 달라진 것

**가장 많이 나온 모양이 두 차수에서 같은 발화가 60개 중 23개, 다른 발화가 37개다.**

같은 SELECT 인데 간 recipe 가 다른 자리 (7개). 되묻지 않고 서로 다른 곳으로 간다.

```
발화                    qwen3:32b   qwen3:8b
 4 오송역 행정경계 보여줘      {018}      {019}
 6 오송역 행정구역 경계 지도에   {018}      {020}
22 청주 선거구 찾아줘         {007}      {036}
43 경부선 노선도 알려줘        {003}      {036}
46 오송역 지나는 노선 알려줘    {003}      {022}
50 철도 안전 문서 찾아줘       {013}      {006}
56 충전소 어디 있어           {012}      {031}
```

32b 가 되묻고 8b 가 하나로 간 자리 (10개) — 5 · 8 · 10 · 11 · 12 · 13 · 23 ·
26 · 51 · 54.
8b 가 되묻고 32b 가 하나로 간 자리 (14개) — 14 · 15 · 16 · 18 · 27 · 29 · 30 ·
35 · 45 · 48 · 49 · 55 · 57 · 58.
21(청주 국회의원 선거구 검색)은 두 차수 다 되묻되 개수가 4개에서 2개로 줄었다.

**되묻는 총수는 8b 가 더 많은데(26 vs 22) 되묻는 자리가 서로 다르다.**
「작은 모델이 더 많이 되묻는다」로 뭉뚱그려지지 않는다 — 자리가 옮겨간다.
1차와 2차의 조회 후보(shortlist)는 같은 발화에서 같다. **달라진 것은 LLM 쪽이다**
(조회는 온톨로지만 보므로 모델과 무관하다. 표 파일의 「조회 후보」 칸에서 확인된다).

#### 안 한 것

```
정답 판정          안 매겼다. 적중·근접·빗나감을 세지 않았다
정답표             `check_resolve.UTTERANCES` 를 읽지도 고치지도 않았다
제품 코드          한 줄도 안 고쳤다. 온톨로지 · menu · 배선표 · demo/ · vendor/ 그대로
커밋               안 했다
3차 (더 큰 모델)    이 장비에 없다. 새로 안 내려받았다
1회차가 왜 다른가    원인을 안 팠다. 사실만 위에 적었다
```


### 2026-08-26 (스물여덟째) · 실행 노드 스물일곱을 전부 눌러 「지금 무엇이 답을 내놓는가」를 다시 만든다 · 재기만 했다

**왜 다시 쟀나.** 「막힌 노드 여덟」은 2026-08-23 목록이라 데이터가 들어오기
전이다. 그 뒤 저쪽 PostGIS 에 데이터가 들어왔고 recipe 도 45개에서 41개가 됐다.
낡은 목록으로는 다음 작업(발화 확장 표)에서 새 발화가 0건일 때
「인자가 틀렸나 · 데이터가 없나 · 권한이 없나」를 못 가른다. 그래서 recipe 에
드는 실행 노드 스물일곱(`TOOL_OF` 26 + 배선 미기록 `web_fetch`)을 Gateway 에
직접 다 눌렀다. `user_context` 는 `execute_service.USER_CONTEXT` 그대로,
`includeGeometry` 는 전부 false. 응답 전문은 `tools/probe_out/*-2026-08-26.json`
(누른 인자 목록은 `probe-index-2026-08-26.json`). 코드는 한 줄도 안 고쳤다.

#### 스물일곱 줄 표

좌표 자리는 전부 오송역 geocode 결과(127.3277, 36.6200)다. 판정은
HTTP 상태가 아니라 본문으로 했다 — 200 + `{"error"}` 는 실패,
200 + `[]` · `count 0` · `status not_found` 는 0건.

```
노드                                    도구                                        누른 인자                            건수  판정      대표 값 · 근거
geocode_place                           geo.geocode                                 query="오송역"                       1     있다      충청북도 청주시 흥덕구 오송읍 봉산리 369-1 · [127.3277, 36.6200]
get_railway_section                     rail.getSectionGeometry                     sectionName="오송역"                 1     있다      sectionId e55869e8… · bbox [[126.8686, 36.6196], [127.3281, 37.5546]]
get_railway_lines                       geo.getRailwayLines                         stationName="오송역"                 31    있다      서울역(고속)→오송역(고속) 구간. includeStations=false 면 15
find_cctv                               road.getCctv                                오송역 ±15km bbox                    83    있다      [경부선] 천안호두휴게소
find_admin_boundary_by_point            adminBoundary.findBoundaryByPoint           오송역 좌표                          3     있다      충청북도(43) → 청주시 흥덕구(43113) → 오송읍(43113250)
search_admin_boundaries                 adminBoundary.searchBoundaries              query="청주시"                       4     있다      청주시 상당구(43111)
get_vworld_boundaries                   vworld.getAdministrativeBoundaries          query="청주시"                       4     있다      청주시 상당구(lt_c_adsigg_info.66)
search_population_statistics            population.searchStatistics                 query="청주시"                       4     있다      청주시 흥덕구 · 2026-06-30 기준
get_age_profile                         population.getAgeProfile                    level=sigungu · code=43113           1     있다      청주시 흥덕구 292,625명 · 연령대 23칸
get_population_trend                    population.getTrend                         level=sigungu · code=43113           1     있다      2026-06-30 292,625명. point 가 한 달치 하나뿐 — 추이 그래프는 아직 안 나온다
search_ev_stations                      ev.searchStations                           query="전기차 충전소"                120   있다      일출랜드(MT064039)
get_ev_station                          ev.getStation                               statId="PL033780"                    1     있다      포빌(플러그링크) · 충전기 1기
search_election_districts               election.searchDistricts                    query="청주"                         4     있다      충북 청주서원(2431401)
get_election_district                   election.getDistrict                        name="충북 청주서원"                 1     있다      충북 청주서원(2431401)
find_election_district_by_point         election.findDistrictByPoint                오송역 좌표                          1     있다      충북 청주흥덕(2430202)
search_assembly_districts               election.searchAssemblyDistricts            query="청주"                         5     있다      청주시 상당구(4311101)
get_assembly_district                   election.getAssemblyDistrict                name="청주시 흥덕구"                 1     있다      청주시 흥덕구(4311301) · pledgeCount 1
find_assembly_district_by_point         election.findAssemblyDistrictByPoint        오송역 좌표                          1     있다      청주시 흥덕구(4311301)
search_assembly_pledge_districts        election.searchAssemblyPledgeDistricts      query="청주"                         16    있다      보은군_옥천군_영동군_괴산군(4372001) 외
get_assembly_pledge_district            election.getAssemblyPledgeDistrict          name="청주시 흥덕구"                 1     있다      청주시 흥덕구(4311301)
find_assembly_pledge_district_by_point  election.findAssemblyPledgeDistrictByPoint  오송역 좌표                          1     있다      청주시 흥덕구(4311301)
search_local_pledge_summaries           election.searchLocalPledgeSummaries         query="청주" · 2차 sido="충청북도"   0·0   0건       getLocalPledgeSummaryDatasetInfo 가 available false · featureCount 0
get_local_pledge_summary                election.getLocalPledgeSummary              sidoCode="43" · 2차 sidoName="충청북도"  0·0  0건    status not_found · 같은 데이터셋
find_local_pledge_summary_by_point      election.findLocalPledgeSummaryByPoint      오송역 · 2차 서울시청 좌표           0·0   0건       status not_found · 같은 데이터셋
search_documents                        knowledge.query                             query="철도 안전"                    4     있다      「철도안전법 [시행 2026. 3. 3.] 법률 제21188호」 본문까지 온다
web_search                              web.search                                  query="오송역"                       -     권한 없음  HTTP 500 · "MCP tool 'web-search/web.search' is not applied for this user."
web_fetch                               web.fetch                                   (못 만듦)                            -     못 부름    url 은 web.search 결과에서만 얻는데 그것이 권한에 막혀 응답 모양을 모른다
```

```
판정별 합계   있다 22 · 0건 3 · 권한 없음 1 · 못 부름 1
```

0건 셋은 인자를 바꿔 두 번씩 눌렀고 두 번째도 0건이다. 데이터 쪽이 근거다 —
같은 데이터셋의 `getLocalPledgeSummaryDatasetInfo` 가
`available: false · featureCount: 0 · "2026 지방선거 시도별 공약 요약 DB
데이터가 적재되지 않았습니다."` 를 그대로 말한다.

#### 「막힌 노드 여덟」(2026-08-23)에서 바뀐 줄

```
search_admin_boundaries        0건 → 있다 4건     shapefile 이 들어왔다
find_admin_boundary_by_point   0건 → 있다 3건     같은 데이터. ★ 이것이 막던 recipe 넷이 풀린다
search_documents               0건 → 있다 4건     지식베이스에 문서가 들어왔다
지방선거 셋                    0건 그대로          위 근거
web_search                     권한 그대로         이번에 다시 눌러 같은 문구로 확인
web_fetch                      권한(추정) 그대로   여전히 못 눌러봤다. 추정이 확인으로 안 바뀌었다
```

#### 폐기 예정으로 보이는 도구가 또 있는가

catalog 42개의 설명을 "호환" · "권장" · "deprecated" · "폐기" · "대신" 으로
훑었다. 걸리는 것은 `ev.searchChargers` 하나뿐이다("기존 호출 호환용 …
ev.searchStations 사용 권장"). **같은 자리는 더 없다.** 「스물일곱째」가 뺀
그 하나가 전부다.

#### 안 한 것

- recipe 실행(발화 → resolve → vendor) 경로로는 안 쟀다. Gateway 직접
  호출이라 배선 표시(`@arg` · `$prev`)가 실제로 풀리는지는 이 표가 안 본다.
- `ev.searchChargers` 는 온톨로지에서 뺀 도구라 안 눌렀다.
- `get_population_trend` 의 point 가 왜 하나뿐인지(적재가 한 달치인지
  기본 인자 탓인지) 안 갈랐다. `startMonth` 변주를 안 눌렀다.

### 2026-08-26 (스물일곱째) · 폐기 예정 도구 하나를 온톨로지에서 뺀다 — 전기차 후보가 늘 두 배이던 원인

모델 qwen3:32b(서버 기본) · 온톨로지 노드 59→58 · 도구 39→38 ·
recipe 45→41 · 발화 9개 × 10회 × 세 묶음 = 270회.
**커밋 안 했다.** 고친 파일은 `ontology/ontology.yaml` ·
`ontology/_init/ontology.yaml` · `demo/api/services/step_service.py` ·
`tools/check_resolve.py` · `tests/ontology/test_shortlist.py` ·
`workflows/static` 의 recipe·menu(도구가 다시 만든 것) · 이 파일이다.

#### 왜 뺐나 — 저쪽이 폐기 예정이라 적어 둔 도구다

근거는 판정 숫자가 아니라 **저쪽 도구 설명 한 줄**이다.

```
ASAP-mcp 도구 설명 (ev.searchChargers)
  "기존 호출 호환용 전기차 충전소 조회. 신규 목록·지도 조회는
   ev.searchStations 사용 권장"

우리 menu 문장 (옛 recipe_013)
  말한 키워드로 옛 호출 호환용으로 충전소를 지도에 뿌린다.
```

같은 일을 하는 도구 둘을 온톨로지가 **대등하게** 두고 있었다. 축 셋
(given · want · about)이 글자까지 같아 조회로 못 갈리고, 아무도 "옛 호출
호환용으로 보여줘" 라고 말하지 않으니 발화로도 못 갈린다. 네 자리에서 짝이
생겼다 — `012/013` · `032/033` · `039/040` · `044/045`.

2026-08-25 화면 실측이 그 자리다.

```
"오송역 근처 충전소 자세히 알려줘"
  → 1 좌표 변환 → 전기차 충전소 검색 → 충전소 상세
    2 좌표 변환 → 전기차 충전기 조회 → 충전소 상세
```

**문장을 고쳐서 될 일이 아니라 노드를 뺄 자리였다.** menu 문장을 아무리 잘
써도 사용자 발화에 그것을 가르는 낱말이 없다.

#### 무엇을 뺐나

`search_ev_chargers` 노드 하나와 그것이 걸린 관계 다섯 줄
(`hasInput` 둘 · `hasOutput` 둘 · `about` 하나). 두 `ontology.yaml` 은
`cmp` 로 바이트 동일하다.

```
노드   59 → 58
도구   39 → 38   (머리말의 "MCP 도구 39개" 도 함께 고쳤다)
관계  113 → 108
```

머리말에서 함께 고친 것 — "코드를 내놓는 것은 셋이다 … 충전소 번호는 전기차
충전소 검색과 전기차 충전기 조회가 내놓는다" 가 **둘 · 전기차 충전소 검색**
하나가 됐다.

#### recipe 대응표

`python tools/rebuild_init.py --write` 뒤 `reset_to_init()`.
**사라진 것은 넷뿐이고 전부 충전기 조회를 지나는 사슬이다.**

```
사라진 recipe   013 · 033 · 040 · 045
합계            45개 → 41개
menu.yaml       3,710자 → 3,390자
```

| 새 번호 | 옛 번호 | 경로 |
|---|---|---|
| 001~012 | 그대로 | 번호가 안 밀린 구간 |
| 013 | 014 | 말한 키워드 → 문서 검색 |
| 014 | 015 | 말한 식별자 → 국회의원 지역구 상세 |
| 015 | 016 | 말한 식별자 → 국회의원 전체 선거구 상세 |
| 016 | 017 | 말한 식별자 → 국회의원 선거구 공약 상세 |
| 017 | 018 | 말한 식별자 → 충전소 상세 조회 |
| 018 | 019 | 말한 장소 → 좌표 변환 → 행정구역 조회 |
| 019 | 020 | 말한 장소 → 좌표 변환 → 지점 행정구역 판별 |
| 020 | 021 | 말한 장소 → 좌표 변환 → VWorld 행정경계 조회 |
| 021 | 022 | 말한 장소 → 좌표 변환 → CCTV 조회 |
| 022 | 023 | 말한 장소 → 좌표 변환 → 철도 노선 조회 |
| 023 | 024 | 말한 장소 → 좌표 변환 → 지점 국회의원 지역구 판별 |
| 024 | 025 | 말한 장소 → 좌표 변환 → 국회의원 전체 선거구 검색 |
| 025 | 026 | 말한 장소 → 좌표 변환 → 지점 국회의원 전체 선거구 판별 |
| 026 | 027 | 말한 장소 → 좌표 변환 → 국회의원 선거구 공약 검색 |
| 027 | 028 | 말한 장소 → 좌표 변환 → 지점 국회의원 선거구 공약 판별 |
| 028 | 029 | 말한 장소 → 좌표 변환 → 지방선거 교통 공약 검색 |
| 029 | 030 | 말한 장소 → 좌표 변환 → 지점 지방선거 교통 공약 판별 |
| 030 | 031 | 말한 장소 → 좌표 변환 → 인구 통계 조회 |
| 031 | 032 | 말한 장소 → 좌표 변환 → 전기차 충전소 검색 |
| 032 | 034 | 말한 장소 → 철도 구간 형상 조회 → 행정구역 조회 |
| 033 | 035 | 말한 장소 → 철도 구간 형상 조회 → VWorld 행정경계 조회 |
| 034 | 036 | 말한 장소 → 철도 구간 형상 조회 → CCTV 조회 |
| 035 | 037 | 말한 장소 → 철도 구간 형상 조회 → 철도 노선 조회 |
| 036 | 038 | 말한 키워드 → 웹 검색 → 웹 문서 가져오기 |
| 037 | 039 | 말한 키워드 → 전기차 충전소 검색 → 충전소 상세 조회 |
| 038 | 041 | 말한 장소 → 좌표 변환 → 지점 행정구역 판별 → 지방선거 교통 공약 상세 |
| 039 | 042 | 말한 장소 → 좌표 변환 → 지점 행정구역 판별 → 연령별 인구 구성 조회 |
| 040 | 043 | 말한 장소 → 좌표 변환 → 지점 행정구역 판별 → 인구 변화 추이 조회 |
| 041 | 044 | 말한 장소 → 좌표 변환 → 전기차 충전소 검색 → 충전소 상세 조회 |

옛 013 이 사라져 014~032 가 하나씩, 옛 033 이 사라져 034~039 가 둘씩,
옛 040 이 사라져 041~044 가 셋씩 당겨졌다. 옛 045 는 꼬리라 아무것도 안 밀었다.

#### GT 를 옮겼다 — 기대값은 한 글자도 안 바꿨다

`tools/check_resolve.py` 의 아홉 발화. **사슬이 같은 recipe 의 새 번호로만
옮겼다.** 아홉 발화의 기대 사슬은 하나도 안 사라졌다.

| # | 발화 | 옛 | 새 | 사슬 |
|---|---|---|---|---|
| 1 | 오송역 위치 보여줘 | 001 | 001 | 말한 장소 → 좌표 변환 |
| 2 | 오송역 좌표 알려줘 | 001 | 001 | 말한 장소 → 좌표 변환 |
| 3 | 오송역 CCTV 보여줘 | 022 | **021** | 말한 장소 → 좌표 변환 → CCTV 조회 |
| 4 | 청주시 인구 구성 알려줘 | 042 | **039** | 말한 장소 → 좌표 변환 → 지점 행정구역 판별 → 연령별 인구 구성 조회 |
| 5 | 오송역 근처 충전소 찾아줘 | 032 | **031** | 말한 장소 → 좌표 변환 → 전기차 충전소 검색 |
| 6 | 국회의원 선거구 찾아줘 | 007 | 007 | 말한 키워드 → 국회의원 지역구 검색 |
| 7 | 전기차 충전소 데이터 검색해줘 | 012 | 012 | 말한 키워드 → 전기차 충전소 검색 |
| 8 | 철도 안전 문서 찾아줘 | 014 | **013** | 말한 키워드 → 문서 검색 |
| 9 | 충북 제1선거구 알려줘 | 015 | **014** | 말한 식별자 → 국회의원 지역구 상세 |

옛 번호는 소스에 옆 주석으로 남겼다 (`# 옛 recipe_022 · 025` 꼴).
`tools/check_argument.py` 의 변주 넷은 번호가 아니라 발화라 안 건드렸다.

#### `check_wiring` 전후

```
전  recipe 45개 · 배선이 다 있는 것 44개 · STEP_OF 37줄 · A 0개 · B 0개 · C 1개
후  recipe 41개 · 배선이 다 있는 것 40개 · STEP_OF 35줄 · A 0개 · B 0개 · C 1개
```

C 는 그대로 `web_fetch × 웹 주소` 하나다. `step_service.py` 에서 지운 것은
`TOOL_OF` 항목 하나와 `STEP_OF` 두 줄뿐이고, 그 셋을 가리키던 주석 셋을
함께 고쳤다. 그 밖의 배선은 한 줄도 안 건드렸다.

`pytest` 202 passed / 1 failed. 실패 하나는 graphviz 버전 차이(음성 대조군)다.
`tests/ontology/test_shortlist.py` 의
`test_three_axes_together_narrow_to_what_the_utterance_asked` 를 고쳤다 —
기대값 `[032, 033]` 이 `[031]` 하나가 됐고, docstring 의 "45개가 충전소와
충전기 둘로 준다. 그 둘은 발화만으로는 갈리지 않으므로 되묻는 것이 맞다" 가
이번에 거짓이 되어 다시 썼다. **둘로 안 준다.**

#### 세 묶음 — 아홉 발화 × 10회 × 3

**세 묶음이 글자까지 같았다.** 한 표로 싣는다.

```
#  발화                        LLM 단독   적중     근접  빗나감  못 붙음
1  오송역 위치 보여줘          0/10       10/10    0     0       0
2  오송역 좌표 알려줘          10/10      10/10    0     0       0
3  오송역 CCTV 보여줘          10/10      10/10    0     0       0
4  청주시 인구 구성 알려줘     10/10      10/10    0     0       0
5  오송역 근처 충전소 찾아줘   0/10       10/10    0     0       0
6  국회의원 선거구 찾아줘      10/10      10/10    0     0       0
7  전기차 충전소 데이터 검색   9/10       10/10    0     0       0
8  철도 안전 문서 찾아줘       10/10      10/10    0     0       0
9  충북 제1선거구 알려줘       10/10      10/10    0     0       0
                              ─────────────────────────────────
                              69/90      90/90    0     0       0
```

축은 아홉 발화 모두 10/10 으로 하나였고 인자도 10/10 으로 하나였다.
검산 표도 세 묶음이 같다 — 검산이 살린 회차 21, **맞는 답을 덮은 자리 0**.

#### 발화별로 움직인 것 — 기준선은 「스물여섯째」다

```
기준선 (2026-08-25 「스물여섯째」)   적중 70/90 · 근접 20 · 빗나감 0 · 못 붙음 0 · LLM 단독 50/90
이번                                 적중 90/90 · 근접  0 · 빗나감 0 · 못 붙음 0 · LLM 단독 69/90
```

| # | 발화 | 스물여섯째 | 이번 | 무엇이 움직였나 |
|---|---|---|---|---|
| 1 | 오송역 위치 보여줘 | 적중 10/10 · SELECT | 그대로 | 조회 후보 1 그대로 |
| 2 | 오송역 좌표 알려줘 | 적중 10/10 · SELECT | 그대로 | 조회 후보 1 그대로 |
| 3 | 오송역 CCTV 보여줘 | 적중 10/10 · SELECT | 그대로 | 조회 후보 5 그대로 |
| 4 | 청주시 인구 구성 알려줘 | **근접 0/10 · CLARIFY {042,043}** | **적중 10/10 · SELECT** | ★ 조회 후보는 3 그대로인데 LLM 이 하나로 골랐다 |
| 5 | 오송역 근처 충전소 찾아줘 | 적중 10/10 · SELECT | 그대로 | ★ **조회 후보 2 → 1.** 이번에 노린 자리다 |
| 6 | 국회의원 선거구 찾아줘 | **근접 0/10 · CLARIFY** | **적중 10/10 · SELECT** | ★ 조회 후보는 4 그대로인데 LLM 이 하나로 골랐다 |
| 7 | 전기차 충전소 데이터 검색 | 적중 10/10 · SELECT | 그대로 | ★ 조회 후보 2 → 1 |
| 8 | 철도 안전 문서 찾아줘 | 적중 10/10 · SELECT | 그대로 | 조회 후보 2 그대로 |
| 9 | 충북 제1선거구 알려줘 | 적중 10/10 · SELECT | 그대로 | 조회 후보 3 그대로 |

★ **발화 4 와 6 이 흔들렸다. 좋은 쪽으로다.** 되돌리지 않는다.

**그런데 이 둘은 이번에 뺀 노드와 아무 상관이 없다.** 4 는 인구이고 6 은
선거다. 조회 후보 수도 3 · 4 로 그대로다. 움직인 것은 LLM 이 그 안에서 하나를
고른 것뿐이다. **남는 설명은 프롬프트 길이다** — menu 가 3,710자에서 3,390자로
줄었다. 「열한째」·「열여덟째」·「스물한째」·「스물여섯째」에 이어 **다섯 번째로
프롬프트 길이가 무관한 발화를 흔들었다.** 방향이 이번에는 좋은 쪽이었을 뿐이다.

**이 20점을 이번 변경의 값으로 읽으면 안 된다.** 이번 근거는 저쪽 도구가
폐기 예정이라는 사실이지 판정 숫자가 아니다. 문장 하나만 더 길어져도 4 와 6 은
다시 CLARIFY 로 돌아갈 수 있는 자리다 — 「스물한째」가 실제로 그랬다.

#### 조회 판정 — 「스물여섯째」의 칸

```
스물여섯째  조회 적중 20회 · 조회 근접 70회 · ★ 조회 빠짐 0회 · 조회 없음 0회
이번        조회 적중 40회 · 조회 근접 50회 · ★ 조회 빠짐 0회 · 조회 없음 0회
```

적중이 20회 늘었다. 발화 5 와 7 의 조회 후보가 각각 2 에서 1 이 된 것이다 —
**둘 다 충전소/충전기 짝이 갈리던 자리다.** 「조회 빠짐」은 여전히 0 이다.

#### 화면 실측 셋 (`POST /chat`, 각 1회)

```
"오송역 근처 충전소 자세히 알려줘"        ★ 되묻지 않고 하나로 갔다
  답  오송역 충전소 상세를 조회했습니다.

      1. geo.geocode        오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1 (127.3277, 36.6200)
      2. ev.searchStations  minLon=127.1598 · minLat=36.4852 · maxLon=127.4956 · maxLat=36.7547
                            500건 (전체 2,173건) · 청주오송역차고지
      3. ev.getStation      statId="PK000313"  8건

"오송역 근처 충전소 찾아줘"                예전처럼 돈다
  답  오송역 전기차 충전소를 조회했습니다.

      1. geo.geocode        오송역 → … (127.3277, 36.6200)
      2. ev.searchStations  … 500건 (전체 2,173건) · 청주오송역차고지

"전기차 충전소 검색해줘"                   예전처럼 돈다
  답  전기차 충전소를 조회했습니다.

      1. ev.searchStations  query="전기차 충전소"  120건 · 일출랜드
```

**2026-08-25 에 두 갈래로 갈리던 첫 발화가 한 갈래다.** 되묻지 않는다.

#### 안 한 것

- **커밋 안 했다.** 커밋 메시지 초안만 냈다.
- **다른 폐기 예정 도구를 안 찾아봤다.** 이번에 하나를 뺐을 뿐이고, 저쪽
  도구 설명에 "호환용" · "권장" 같은 말이 또 있는지 훑지 않았다.
  「열린 과제」에 항목으로 남긴다.
- **어미 변주로 다시 안 쟀다.** 「스물다섯째」의 변주 넷
  (`tools/check_argument.py`)은 번호가 아니라 발화라 안 건드렸고 돌리지도
  않았다. `{012,013,032,033}` 로 CLARIFY 하던 "전기차 충전소 데이터 줘" 가
  이제 어떻게 되는지는 안 쟀다.
- **발화 4 · 6 이 왜 좋아졌는지 확인 안 했다.** 프롬프트 길이라는 것은
  소거법으로 남은 설명이지 실측이 아니다. menu 를 인위로 늘려 되돌아가는지
  재보면 갈리지만 안 했다.
- `vendor/` · `demo/graph_svg/layout.json` 은 안 건드렸다.
  `layout.json` 과 `tools/probe_out/` 에는 `search_ev_chargers` 가 아직
  남아 있다 — 둘 다 이번 변경 범위 밖이다.

---

### 2026-08-25 (스물여섯째) · 온톨로지만으로 좁혔을 때 정답이 살아남는지 잰다 · 재기만 했다

**제품 코드 0줄. 고친 파일은 `tools/check_resolve.py` 와 이 파일뿐이다.**
모델 qwen3:32b(서버 기본) · 온톨로지 HEAD · recipe 45 (`_init`) ·
발화 9개 × 10회 = 90회. `/resolve` 를 더 부르지 않았다 — 응답에 이미 실려 있는
`shortlist_recipe_ids` 를 읽어 칸만 더했다.

#### 왜 쟀나 — 좁히기의 전제

지금은 LLM 을 한 번 부르면서 프롬프트에 **menu 45문장을 통째로** 넣는다.
LLM 이 그중에서 고르고, 그 답을 온톨로지 조회(shortlist)와 교집합 내어 좁힌다.
바꾸려는 것은 순서다.

```
지금    LLM 이 45문장에서 고른다  →  온톨로지 조회와 교집합
바꾸면  온톨로지가 먼저 몇 개로 좁힌다  →  LLM 은 그 몇 개 중에서 고른다
```

얻는 것이 크다. 프롬프트가 45문장에서 몇 문장으로 줄면 「프롬프트 길이가 판정을
흔든다」는 문제가 대부분 사라진다 — 「열한째」·「열여덟째」·「스물한째」가 전부
그 문제였다. 도구를 늘리는 값도 싸진다.

**위험도 크다.** 지금은 LLM 후보와 조회 후보의 교집합이 비면 조회 후보를 쓰는
안전장치가 있는데, 먼저 좁히면 그것이 없어진다. 축이 틀리면 정답이 후보에서
아예 빠지고, 되물음이 아니라 확신하고 틀리는 것이 된다. 실제 사례가 있다 —
발화 4(청주시 인구 구성)의 `given` 이 `말한 장소` 면 정답 042 가 후보에 들어
있고 `말한 키워드` 면 안 들어 있다 (「스물한째」에서 이 축이 뒤집혀 21점을
잃었다).

그래서 좁히기를 만들기 전에 이것부터 쟀다 — **온톨로지만으로 좁혔을 때 정답이
그 안에 살아남는가.**

#### 세는 법 넷

`_verdict`(검산)를 지나기 전 값을 본다. 최종 후보가 아니라
`shortlist_recipe_ids` 그대로를 기대값과 맞댄다. 넷을 더하면 시행 횟수가 된다.

```
조회 적중   기대값 ⊆ 조회 후보 · 조회 후보가 기대값과 같다   좁히면 바로 SELECT 다
조회 근접   기대값 ⊆ 조회 후보 · 조회 후보가 더 많다         좁힌 뒤 LLM 이 고르면 된다
조회 빠짐   ★ 기대값이 조회 후보에 없다                      좁히면 정답에 못 닿는다
조회 없음   조회 후보가 빈 목록                              축이 셋 다 null 인 자리
```

**적중 표의 네 칸(적중·근접·빗나감·못 붙음)과 다른 것을 센다.** 그쪽은 검산을
지난 최종 후보를 보고, 이쪽은 검산을 지나기 전 조회 후보만 본다. 정답표
(UTTERANCES 아홉과 기대값)와 적중 표의 판정 규칙은 한 글자도 안 바꿨다.

**한 묶음이면 됐다.** 이 칸은 LLM 이 쓴 축 셋에서 결정론적으로 나오므로 축이
같으면 이 칸도 같다. 이번 90회에서 축이 회차마다 갈린 발화는 하나도 없었다 —
아홉 발화 모두 축 조합이 10/10 으로 하나였다. 그래서 표가 발화마다 한 줄이다.

#### 발화별 표

```
#  발화                        조회 판정   조회 후보 수  LLM 후보 수  status    횟수  given · want · about
1  오송역 위치 보여줘          조회 적중   1             1            SELECT    10회  spoken_place · point · group_transport
2  오송역 좌표 알려줘          조회 적중   1             1            SELECT    10회  spoken_place · point · -
3  오송역 CCTV 보여줘          조회 근접   5             1            SELECT    10회  spoken_place · item_list · group_transport
4  청주시 인구 구성 알려줘     조회 근접   3             2            CLARIFY   10회  spoken_place · statistics · group_population
5  오송역 근처 충전소 찾아줘   조회 근접   2             1            SELECT    10회  spoken_place · item_list · group_ev
6  국회의원 선거구 찾아줘      조회 근접   4             3            CLARIFY   10회  spoken_keyword · item_list · group_election
7  전기차 충전소 데이터 검색   조회 근접   2             1            SELECT    10회  spoken_keyword · item_list · group_ev
8  철도 안전 문서 찾아줘       조회 근접   2             1            SELECT    10회  spoken_keyword · item_list · group_transport
9  충북 제1선거구 알려줘       조회 근접   3             1            SELECT    10회  spoken_identifier · item_detail · group_election

조회 판정 합계  조회 적중 20회 · 조회 근접 70회 · ★ 조회 빠짐 0회 · 조회 없음 0회  (합 90회)
```

#### ★ 「조회 빠짐」은 0 발화 0 회다

**아홉 발화 90회 전부에서 기대값이 조회 후보 안에 들어 있었다.** 빠진 자리가
하나도 없다. 「조회 없음」(축이 셋 다 null)도 0 이다 — 아홉 발화 모두 조회가
후보를 하나 이상 냈다.

발화 1·2 는 조회만으로 이미 하나다(조회 적중). 좁히면 LLM 을 안 불러도 되는
자리다. 나머지 일곱은 조회 후보가 2~5개이고 그 안에 정답이 있다 —
**프롬프트에 45문장이 아니라 2~5문장만 넣으면 되는 자리다.**

#### 기존 네 칸은 안 움직였다

```
적중 70/90 (78%) · 근접 20 · 빗나감 0 · 못 붙음 0 · LLM 단독 50/90 (56%)
```

「스물다섯째」와 같다. 완전 적중이 아닌 발화는 4·6 둘이고 둘 다 CLARIFY 이며
정답이 후보에 남아 있다(근접). 검산 표도 그대로다 — 검산이 살린 회차 20,
바꿨지만 판정은 같은 회차 10, **맞는 답을 덮은 자리 0**.

#### ★ 결론

**지금 축으로 먼저 좁혀도 정답이 살아남는다.** 조회 빠짐 90회 중 0회.
좁히기를 만들어도 되는 상태다.

다만 이 결론의 유효 범위를 좁게 적어 둔다. **이것은 「이 아홉 발화 · 이 온톨로지
· 이 모델」에서 축이 흔들리지 않았다는 뜻이지, 축이 절대 안 틀린다는 뜻이 아니다.**
축이 틀리면 좁히기는 곧바로 정답을 잃는다. 이번에 안 틀렸을 뿐이다.

#### 좁히기를 할 때 위험한 발화

- **발화 4 (청주시 인구 구성 알려줘)** — 제일 위험하다. `given` 이
  `spoken_place` 면 조회 후보 3개에 042 가 들어 있고, `spoken_keyword` 로
  뒤집히면 042 가 빠진다. 「스물한째」에서 실제로 뒤집혀 21점을 잃은 자리다.
  이번 10회는 전부 `spoken_place` 로 안정했지만, 이 발화가 좁히기의 단일
  실패점이다.
- **발화 2 (오송역 좌표 알려줘)** — `about` 이 null 인데도 조회가 후보 1개로
  좁혔다. 축 하나가 비어도 통했다는 뜻이라 지금은 이득이지만, 축이 빌 때
  조회가 무엇을 내는지는 이 한 발화로만 봤다.
- **발화 6 (국회의원 선거구 찾아줘)** — 조회 후보 4개에 정답 007 이 있다.
  좁히기 자체는 안전한데, 검산 표를 보면 LLM 단독은 아홉 개 이상을 늘어놓고
  검산이 셋으로 줄이는 자리다. 좁히면 LLM 이 4문장만 보게 되므로 여기가
  제일 크게 나아질 자리이기도 하다.
- **어미 변주** — 「스물다섯째」가 잰 대로 어미만 바꿔도 `given` 축이 흔들린다.
  이번에 잰 것은 확정된 아홉 발화뿐이다. **변주에서는 안 쟀다.**

#### 안 한 것

- **좁히기를 만들지 않았다.** 전제만 쟀다.
- **`/resolve` 를 더 부르지 않았다.** 응답에 이미 있는 값을 읽었을 뿐이다.
- **제품 코드 0줄.** 온톨로지 · menu · 배선표 · `demo/` · `vendor/` 안 건드렸다.
- **정답표와 판정 규칙 0글자.** 적중 표는 안 건드렸고 칸은 후보 표에만 붙였다.
- **어미 변주와 확장 발화에서는 안 쟀다.** 아홉 발화 × 10회 한 묶음이다.
- **모델을 안 바꿨다.** 작은 모델에서 축이 더 흔들리는지는 모른다.

---

### 2026-08-25 (스물다섯째) · 인자 추출에 무엇이 남았는지 다시 잰다 · 재기만 했다

**제품 코드 0줄. 고친 파일은 `tools/check_argument.py` 하나다.** 모델 qwen3:32b
(서버 기본) · 온톨로지 HEAD · 발화 13개 × 5회.

#### 왜 다시 쟀나

「열셋째」(2026-08-24)가 인자 추출을 값어치 1순위로 올린 근거는
`"전기차 충전소 데이터 검색해줘" → query="전기차 충전소 데이터" → 0건` 이었다.
그런데 그 대표 예시가 2026-08-25 화면에서 저절로 풀려 있었다
(`query="전기차 충전소"` · 120건, `말한 식별자 is-a 행정구역 코드` 를 뗀 뒤다).
그래서 인자 문제가 얼마나 남았는지를 고치기 전에 다시 쟀다.

#### 먼저 — 이 도구는 그동안 실행 불능이었다

`check_argument` 는 `_call_resolve` 를 넷으로 언팩하는데, 「열넷째」의
LLM 단독 칸(`4dd552a`, 2026-08-24 18:46)이 반환값을 다섯으로 늘렸다.
그 뒤로 매 호출이 ValueError 로 떨어져 전부 "!" 가 되는 상태였다 —
만들어진 `2b25290`(2026-08-24 02:26) 이래 이 파일은 한 번도 안 고쳐졌으므로
`4dd552a` 부터 오늘까지가 고장 구간이다. 「열셋째」의 측정은 그 앞이라
유효하다. `*_rest` 로 받게 고쳤다
(측정 도구 안이라 제품 경로가 아니다). 같은 김에 표에 status 칸과
축 셋(given · want · about) 칸을 더했고, 어미만 바꾼 변주 넷(10~13)을
`VARIATIONS` 로 이어 붙였다. **기존 아홉과 HUMAN_ARGUMENT 아홉은 한 글자도
안 바꿨다.**

#### 아홉 발화의 인자 표

읽는 법 — LLM 값 = 사람 값이면 인자 문제가 없다. LLM 0건 · 사람 값은 나오면
인자 탓. 둘 다 0건이면 데이터(어휘) 탓. CLARIFY 줄의 건수는 "만약 이것을
골랐다면" 이다 — 화면은 되묻지 실행하지 않는다.

```
#  발화                        recipe  status   첫 도구                    뽑은 이   인자             건수     횟수  given · want · about
1  오송역 위치 보여줘          001     SELECT   geo.geocode                LLM       오송역           1(단건)  5회   spoken_place · point · group_transport
                                                                          사람추측  오송역           1(단건)
2  오송역 좌표 알려줘          001     SELECT   geo.geocode                LLM       오송역           1(단건)  5회   spoken_place · point · -
                                                                          사람추측  오송역           1(단건)
3  오송역 CCTV 보여줘          022     SELECT   geo.geocode                LLM       오송역           1(단건)  5회   spoken_place · item_list · group_transport
                                                                          사람추측  오송역           1(단건)
4  청주시 인구 구성 알려줘     042     CLARIFY  geo.geocode                LLM       청주시           1(단건)  5회   spoken_place · statistics · group_population
                                                                          사람추측  청주시           1(단건)
5  오송역 근처 충전소 찾아줘   032     SELECT   geo.geocode                LLM       오송역           1(단건)  5회   spoken_place · item_list · group_ev
                                                                          사람추측  오송역           1(단건)
6  국회의원 선거구 찾아줘      007     CLARIFY  election.searchDistricts   LLM       국회의원 선거구  0        5회   spoken_keyword · item_list · group_election
                                                                          사람추측  선거구           0
                                                                          확인용    청주             4
7  전기차 충전소 데이터 검색…  012     SELECT   ev.searchStations          LLM       전기차 충전소    120      5회   spoken_keyword · item_list · group_ev
                                                                          사람추측  전기차 충전소    120
8  철도 안전 문서 찾아줘       014     SELECT   knowledge.query            LLM       철도 안전        4        5회   spoken_keyword · item_list · group_transport
                                                                          사람추측  철도 안전        4
                                                                          확인용    (빈 질의)        오류
9  충북 제1선거구 알려줘       015     SELECT   election.getDistrict       LLM       충북 제1선거구   1(단건)  5회   spoken_identifier · item_detail · group_election
                                                                          사람추측  충북 제1선거구   1(단건)
```

- **아홉 전부에서 LLM 값 = 사람 값이다.** 5회 내내 같은 인자가 나왔다
  (4번은 recipe 만 CLARIFY 로 흔들리고 인자는 "청주시" 그대로).
- 0건은 6번뿐인데 **사람 값("선거구")도 0건이다.** 확인용 "청주" 만 4건 —
  「열셋째」와 같은 판정: 발화의 어떤 낱말도 선거구 이름("충북 청주서원" 꼴)과
  안 겹치는 어휘 불일치지 추출 탓이 아니다.
- 7번 120건 — 대표 예시 해소가 5/5 로 굳었다. 8번 4건 — RAG 문서가 들어와
  「열셋째」의 0건이 데이터 탓이었음도 굳었다.
- 9번의 `1(단건)` 은 **not_found 본문이다**
  (`{'status': 'not_found', 'message': '조건에 맞는 선거구를 찾지
  못했습니다.' …}`). `_count` 가 목록도 건수도 없는 dict 를 단건으로 세는
  자리라 표만 보면 나온 것처럼 읽힌다 — 본문을 봐야 한다. 인자는 발화
  그대로이고 사람 값도 같으므로 이것도 추출 탓이 아니라 이름 형식이다.
- 8번 확인용(빈 질의)은 「열셋째」때 `[]` 였는데 지금은 HTTP 500
  (`Error embedding content … empty Part`) 이다. 문서가 들어오며 빈 질의가
  임베딩까지 가게 된 것. 정정으로 남긴다.

#### 변주 넷 — 어미가 무엇을 흔드나

```
#  발화                      recipe  status        인자          건수   횟수  given · want · about
10 전기차 충전소 데이터 줘   012     CLARIFY 5/5   -             (500)  5회   - · item_list · group_ev
                                                  사람추측: 전기차 충전소 120
11 전기차 충전소 알려줘      012     SELECT 5/5    전기차 충전소 120    5회   spoken_keyword · item_list · group_ev
12 철도 안전 문서 보여줘     006     SELECT 4/5    철도 안전     오류   4회   spoken_keyword · item_list · group_transport
                            014     SELECT 1/5    철도 안전     4      1회   spoken_keyword · item_list · group_transport
13 청주시 인구 알려줘        031     CLARIFY 4/5   청주시        1(단건) 4회  spoken_place · statistics · group_population
                            031     SELECT 1/5    청주시        1(단건) 1회  (같은 축)
```

- **10번 "…데이터 줘"** — 5/5 CLARIFY. `given` 이 `-`(null), 인자도 `-` 다.
  후보는 `{012, 013, 032, 033}` (따로 2회 더 눌러 확인) — 2026-08-25 화면
  실측의 "되묻는다(후보 넷)" 그대로다. **어미가 인자만이 아니라 `given` 축을
  통째로 비운다.** 표의 500건은 PROBE 자리에 `-` 를 넣어 눌러본 값이라 뜻이
  없다(화면은 실행 안 함).
- **11번 "…알려줘"** — 5/5 SELECT · 120건. 7번과 완전히 같다. "데이터" 없이
  어미만 바꾼 것은 안 흔든다. 흔드는 것은 **"데이터 줘" 조합**이다.
- **12번 "문서 보여줘"** — 축 넷이 8번("찾아줘")과 글자까지 같은데 **recipe 가
  4/5 로 006(web.search)** 이다. 어미가 축은 안 흔들고 recipe 선택을 흔든다.
  게다가 web.search 는 우리 USER_CONTEXT(`asap-mcp-core/*`) 권한 밖이라
  HTTP 500 (`MCP tool 'web-search/web.search' is not applied for this
  user`) — 화면이면 확신하고 실패하는 자리다.
- **13번 "인구 알려줘"** — "구성" 이 빠지자 후보에 031(인구 통계 검색)이
  들어와 CLARIFY `{031, 042, 043}` 4/5, SELECT `{031}` 1/5 (따로 2회 눌러
  확인). 인자는 5/5 "청주시" 그대로다.

**정리 — 어미(그리고 "데이터" 같은 곁낱말)가 흔드는 것은 인자가 아니라
`given` 축(10)과 recipe 선택(12 · 13)이다.** 인자는 뽑힐 때는 항상 같은 값이
나왔다.

#### 화면 대조 — 이 도구의 값과 화면의 값이 같은가

브라우저를 직접 누른 것이 아니라 화면이 그리는 것과 같은 `POST /chat` 을
불러 answer 를 그대로 받았다(「스물넷째」와 같은 방식).

```
"전기차 충전소 데이터 검색해줘"
  1. ev.searchStations  query="전기차 충전소"  120건 · 일출랜드
"철도 안전 문서 찾아줘"
  1. knowledge.query   query="철도 안전"  4건 · 「철도안전법(법률)(제21188호)(20260303).pdf」 · "법제처 …"
"청주시 인구 구성 알려줘"
  어느 것을 보시겠습니까?
    1  연령별 인구 구성 조회
    2  인구 변화 추이 조회
```

앞 둘은 이 도구의 값(전기차 충전소 · 120 / 철도 안전 · 4)과 **문구까지
일치한다.** 셋째는 화면도 도구도 CLARIFY `{042, 043}` 라 화면에 인자 줄
자체가 없다 — 대조할 값이 없고, 도구 쪽 인자는 "청주시" 로 바르게 뽑혀 있다.
두 값이 어긋난 자리는 없다.

#### 경부선 확인 — 인자와 다른 문제가 맞다

Gateway 를 직접 눌렀다. 응답 전문은 `tools/probe_out/*-2026-08-25.json`.

```
rail.getSectionGeometry sectionName="경부선"
  sectionId 5b83b0d4…  bbox [[126.9608, 37.5194], [127.0487, 37.5807]]
  → 화면 실측의 road.getCctv bbox 와 자리수까지 같다. 서울 도심 구간 하나다
rail.getSectionGeometry sectionName="오송-청주공항" · "대전-김천"
  → NOT_FOUND ("구간 '…'을(를) 찾을 수 없습니다") — 하이픈 구간명은 없다
rail.getSectionGeometry sectionName="오송역" / "부산역" / "김천"
  → 각각 다른 sectionId · 다른 bbox (서울~오송 / 부산 일대 / 김천 일대)
  → 역명은 ILIKE 에 걸린다. 도구가 죽은 게 아니라 LIMIT 1 이 문제다
geo.getRailwayLines railwayName="경부선"
  → 224 features · 전체 bbox [126.87, 35.12, 129.06, 37.58] (서울~부산 전체)
  → ★ 노선 전체를 다루는 길이 있다. 「안 눌러본 넷」에서 이 도구를 지운다
geo.getRailwayLines stationName="오송역"
  → 31 features (경부고속선 8 · 호남고속선 6 · …)
```

#### ★ 결론

**인자 추출 문제는 아홉 발화 어디에도 안 남았다. ①(인자 추출)을 닫는다.**
남은 것은 인자가 아니다 — 6 · 9 는 발화와 데이터의 어휘 불일치, 4 · 10 · 13 은
되물음(축 · recipe 선택), 12 는 recipe 선택 + 권한, 경부선은 저쪽 LIMIT 1 이다.

**이 결론은 발화 아홉의 범위에서만 참이다.** 그 아홉이 건드리는 recipe 는
45개 중 8개(`001` · `007` · `012` · `014` · `015` · `022` · `032` · `042`)이고
부르는 도구는 42개 중 8개(`geo.geocode` · `road.getCctv` ·
`adminBoundary.findBoundaryByPoint` · `population.getAgeProfile` ·
`ev.searchStations` · `election.searchDistricts` · `knowledge.query` ·
`election.getDistrict`)다. **한 번도 안 불린 실행 노드가 스무 개 있다**
(전체 실행 노드 28개 중 8개만 지난다 — 안 불린 스물은 선거 열 개와
`get_ev_station` · `get_population_trend` · `get_railway_lines` ·
`get_railway_section` · `get_vworld_boundaries` · `search_admin_boundaries` ·
`search_ev_chargers` · `search_population_statistics` · `web_search` ·
`web_fetch`).
확장 표를 만들 때 인자를 다시 재야 한다.

#### 저쪽 geocode 가 지명 크기를 무시한다 — 「말한 장소 → 좌표」 길 전체의 한계

2026-08-25 화면 실측.

```
"청주시 인구 알려줘"
  1. geo.geocode  청주시 → 충청북도 청주시 상당구 남일면 단재로 480 (127.5059, 36.5883)
  2. population.searchStatistics  1건 · 청주시 상당구 193,079명

사용자는 청주시를 물었고 상당구 답이 나왔다. 되묻지도 않았다.
```

**Gateway 를 직접 눌러 그대로 재현했다** (`recipe_031` 의 배선을 손으로 따라
`geo.geocode` → `population.searchStatistics(bbox=…)`). geocode 가 준
bbox 는 `[[127.5009, 36.5833], [127.5109, 36.5933]]` — 0.01° × 0.01° 다.
응답 전문은 `tools/probe_out/geo.geocode.청주시-2026-08-25.json` 과
`population.searchStatistics.bbox-청주시geocode-2026-08-25.json`.

**원인은 저쪽 `geo.geocode` 다** (`KRRI_ASAP/ASAP-mcp/app/tools/geo.py`).

```
geo.py:92-95   bbox = [[round(lon - 0.005, 4), round(lat - 0.005, 4)],
                       [round(lon + 0.005, 4), round(lat + 0.005, 4)]]
               계산이 아니라 상수다. 무엇을 찾았든 늘 약 1.1km 정사각형이다
geo.py:30·33   VWorld 를 type="place" · size="1" 로 부른다
geo.py:87      items[0] 하나만 쓴다
               "청주시" 로 부르면 행정구역이 아니라 그 안의 주소 한 곳이 걸린다
```

**따라서 「말한 장소 → 좌표 → 무엇」 경로는 전부 1.1km 반경 작업이다.**
역 · 건물 이름에는 맞고 시 · 군 이름에는 안 맞는다. 우리가 못 고친다.

**우리 각 층은 제 일을 했다** — 경로를 골랐고, 인자를 정확히 뽑았고
(`query="청주시"`), 앞 단계 결과를 다음 도구에 넘겼다. **우리 몫은 머리말
하나다** — `"{arg} 인구 통계를 조회했습니다"` 가 청주시라고 단정하는데 결과는
상당구다. 단계 줄은 정직하다(주소와 건수를 그대로 적는다). 「열린 과제」의
명사형 머리말 항목과 같은 자리다.

**이번에 `/chat` 으로는 그 화면을 다시 못 봤다.** 세 번 눌러 세 번 다
CLARIFY(`인구 통계 조회` · `연령별 인구 구성 조회` · `인구 변화 추이 조회`)
였다. `check_argument` 표의 13번도 CLARIFY 4/5 · SELECT 1/5 다 — SELECT 로
가는 회차에서만 보이는 화면이다.

#### 사람 판단 — `recipe_011` 과 `031` 은 둘 다 남긴다 (2026-08-25)

```
query="청주시"  4건 (청주시 흥덕구 · 상당구 · 서원구 · 청원구)
                  tools/probe_out/population.searchStatistics.청주시.json
query="오송역"  0건
                  tools/probe_out/population.searchStatistics.오송역.json

→ 역 이름은 이름 검색에 안 걸린다. 좌표 길(031)이 필요하다
→ 시 이름은 이름 검색이 낫다. 그 길(011)도 필요하다
둘 중 하나를 없애는 안은 안 고른다.
```

**"청주시" 를 「말한 키워드」로 읽으라는 것은 무리다** — 누구나 장소로 읽는다.
그러므로 이것은 경로 설계 문제가 아니라 저쪽 geocode 의 한계다.

#### 안 한 것

- 아무것도 안 고쳤다(측정 도구 밖). 되물음 · recipe 흔들림 · web.search 권한 ·
  LIMIT 1 · geocode 1.1km 전부 표만 내고 멈췄다.
- `$prev` 자리는 이번에도 안 쟀다(첫 단계만 잰다). 3번 CCTV 의 건수는 좌표
  변환까지다.
- "경부선 cctv 보여줘" 를 `/chat` 으로 다시 누르지는 않았다 — 화면 실측이
  이미 있고, 이번에는 그 아래의 도구를 직접 눌렀다.
- 브라우저 화면을 사람 손으로 누른 것이 아니다. `/chat` answer 가 화면 문구의
  원본이라 그것으로 대조했다.

### 2026-08-25 (스물넷째) · 도구마다 무슨 인자로 불렀고 무엇이 돌아왔는지를 화면에 낸다 · 화면 실측

**「스물셋째」의 「★ 미완성이다」 넷을 다 했다.** 고친 것은
`vendor/asap/workflow_answer.py` 와 `demo/api/services/step_service.py` 의 답
첫 줄 조립부 한 곳이다. 온톨로지 · menu · 정답표 · `STEP_OF` · `TOOL_OF` 를 안
건드렸다. 프롬프트에 안 들어가는 층이라 판정이 움직이면 안 되는 변경이다.

#### 넷을 각각 어떻게 고쳤나

```
하나  단계마다 인자를 적는다   step_line 이 item["input"] 을 읽어 도구 이름과
                               결과 사이에 key=value 로 적는다
둘    못 찾았다고 말한다       최상위 status 가 "없다" 를 뜻하면 응답의 message 를
                               그대로 낸다. _verdict 도 그때 빈 결과로 본다
셋    목록의 첫 항목을 보인다  _record_line 이 출처(metadata.source)와
                               본문(content)까지 읽고, 목록형 갈래 둘이 그것을 부른다
넷    머리말 겹침을 없앤다     step_service._headline 이 인자가 뒷 문장의 앞머리와
                               같으면 앞에 안 붙인다
```

#### 하나 · 단계마다 무슨 인자로 불렀는지

`generic_mcp_executor` 가 `trace` 항목마다 `resolved_input` 을 `input` 으로
넣어 둔다. 참조(`$s1.location`)와 어댑터(`point_radius_to_bbox`)를 다 푼 뒤의
**실제 호출 인자**라 여기밖에 없는 값이다. `step_line` 이 `item` 을 통째로 받고
있었으므로 읽는 자리는 이미 있었다.

**적을 수 있는 것만 적는다.** 이 파일의 계약이 "결과 값을 문자열에 담지 않는다"
이고 인자도 같은 위험이 있다 — `geo.getRailwayLines` 의 `bbox` 는 두 겹 배열이고
`find…ByPoint` 는 좌표를 받는다.

```
문자열   INPUT_VALUE_LIMIT(24)에서 자르고 따옴표. 자른 것은 "…" 로 밝힌다
정수     그대로
실수     COORD_DIGITS(4)자리까지. 배선의 실수는 전부 좌표다.
         어댑터가 만든 bbox 는 소수점이 열대여섯 자리다
dict     안 적는다
list     안 적는다
bool     안 적는다. 참·거짓만으로는 무엇을 물었는지 못 말하고
         실측 배선(STEP_OF)에 bool 인자가 없다
None     안 적는다
칸 수    INPUT_KEY_LIMIT(4)까지. 넘으면 끝에 "…" 를 붙여 밝힌다.
         실측 배선의 최대가 넷이다 (road.getCctv · ev.searchStations 의 bbox)
```

**겹치는 것은 안 적는다.** `geo.geocode` 는 `"오송역 → 주소 (경도, 위도)"` 로
인자를 이미 말한다. 여기에 `query="오송역"` 을 또 붙이면 한 줄에 같은 값이 두
번 나간다. **판정은 「그 값이 결과 문구 안에 이미 있는가」 하나다** — 도구
이름도 갈래 이름도 안 본다. 그래서 `election.getDistrict(name="충북 청주서원")`
처럼 결과가 인자를 그대로 되돌려 주는 자리도 저절로 걸린다 (화면 발화 6 이
전후로 한 글자도 안 바뀐 이유다).

**언제 뒤집나.** 값이 우연히 결과 문구에 들어 있어 인자가 조용히 사라지는 일이
생기면. 그때는 갈래(`_place_line`)가 무엇을 이미 썼는지를 넘기는 쪽으로
바꾼다 — 지금 안 그렇게 한 것은 갈래가 늘 때마다 넘길 것이 늘기 때문이다.

#### 둘 · 못 찾은 것

`probe_out` 131건에서 최상위 `status` 칸이 있는 응답을 다 셌다. **값은 넷뿐이다.**

```
status       응답 수   도구                                              쓰나
not_found      10     election.getDistrict · getAssemblyDistrict ·        쓴다
                      getAssemblyPledgeDistrict · getLocalPledgeSummary ·
                      findLocalPledgeSummaryByPoint
                      열 건 모두 최상위 message 를 함께 준다
empty           2     ev.getDatasetInfo · population.getDatasetInfo       쓴다
                      적재된 것이 없다는 뜻. message 는 없다
syncing         1     ev.getDatasetInfo                                   안 쓴다
                      충전소 93,353건이 들어 있고 동기화 중이다
ready           1     population.getDatasetInfo                           안 쓴다
```

**뒤의 둘은 안 쓴다.** 데이터가 있는 상태라 "없다" 고 말하면 거짓이 된다.
`ev.getDatasetInfo` 하나가 `syncing`(93,353건)과 `empty`(0건) 둘 다로 오므로
도구 이름이 아니라 값으로 갈라야 한다는 것도 여기서 나온다.

문구는 **응답의 것을 먼저 쓴다.** `_notice`(`warning` · `message`)가 이미 있고
`not_found` 열 건이 전부 `message` 를 준다 — `"조건에 맞는 선거구를 찾지
못했습니다."` 처럼 무엇을 어디서 못 찾았는지를 말한다. 우리 문구
(`MISSING_STATUS`)는 `message` 가 없는 `empty` 에만 나간다.

`_verdict` 도 함께 고쳤다. 마지막 단계가 이 모양이면 빈 결과로 본다. 안 고치면
첫 줄이 `"충북 제1선거구 국회의원 지역구를 조회했습니다."` 로 나가 못 찾은
것을 찾은 것처럼 말한다.

**언제 뒤집나.** `MISSING_STATUS` 에 세 번째 값을 넣어야 할 때. 값이 늘면
그것은 상태 사전이지 규칙이 아니고, 도구별 표로 옮기는 편이 정직하다.

#### 셋 · 목록형 응답의 내용

`knowledge.query` 의 실물을 Gateway 로 직접 눌러 받았다. `probe_out` 에 있던
것은 `[]` 뿐이었다 (`tools/probe_out/knowledge.query.철도안전-2026-08-25.json`
에 응답 전문을 남겼다).

```
POST /api/tools/execute  {"tool": "knowledge.query", "input": {"query": "철도 안전"}}
  → 4건. 항목이 {content, metadata} 둘뿐이다
    content   962 ~ 995자. PDF 본문이고 공백이 수십 칸씩 이어 붙어 온다
    metadata  아래 열여섯 칸
```

`metadata` 칸 실측 (네 항목 모두 같은 열여섯 칸이다).

```
칸             값의 예                                          쓰나
source         "철도안전법(법률)(제21188호)(20260303).pdf"        쓴다
title          ""  (네 건 다 빈 문자열)                          쓴다 (비어 있어 늘 건너뜀)
file_path      "/tmp/철도안전법(법률)(제21188호)(20260303).pdf"   안 쓴다. 저쪽 컨테이너 경로다
page           "0" · "1" · "33"                                 안 쓴다
total_pages    "47" · "52"                                      안 쓴다
producer       "iText 2.1.7 by 1T3XT"                           안 쓴다
format         "PDF 1.4"                                        안 쓴다
creationdate · moddate · creationDate · modDate                 안 쓴다
author · creator · subject · keywords · trapped   (전부 "")      안 쓴다
```

**규칙은 도구 이름을 모른다.** 「목록의 첫 항목에서 이름 · 수치 · 출처 · 본문을
고른다」로 쓰고, 고를 칸 이름을 미리 정해 둔다. 그래서 `geojson` feature
(`{geometry, properties, type}`)는 읽을 칸이 하나도 없어 조용히 건너뛰고,
`road.getCctv` 항목의 `cctvUrl`(88자짜리 서명 붙은 주소)도 안 나간다.

```
이름   name            (기존)
수치   total<대문자>   (기존. SUBJECT_UNITS 에 있는 것만)
출처   metadata.title -> metadata.source        SOURCE_LIMIT 48자에서 자름
본문   content -> text                          TEXT_LIMIT 60자에서 자름
기준일 referenceDate   (기존)
```

**최상위 `source` 는 안 본다.** 그쪽은 데이터셋 설명(`"한국환경공단 전기자동차
충전소 정보 API"`)이라 뜻이 다르다. `metadata` 안만 본다.

**본문을 「긴 문자열이면 글」로 고르지 않았다.** 그렇게 고르면 base64 ·
`geojson` 문자열이 그대로 화면에 나간다. 칸 이름을 아는 것만 읽고 길이를 자른다.

첫 항목을 붙이는 자리가 둘이다 — 배열 그대로 오는 응답(`_list_line`)과 건수
칸이 있는 응답(`_counted_line`). 후자에서 **첫 목록이 비면 다음 목록을 본다.**
`adminBoundary.findBoundaryByPoint` 와 `election.searchDistricts` 가 `features`
0건 · `items` 3건(254건)으로 오기 때문이다. `features` 에서 멈추면 이름이 있는
`items` 를 못 본다.

**전부 늘어놓지 않는다.** 첫 항목 하나뿐이고, 그것이 여럿 중 첫째라는 것은
건수가 앞에 있어 알 수 있다.

#### 넷 · 머리말 겹침

`step_service.plan` 의 `headline = tool["headline"].format(arg=argument)` 한
줄을 `_headline(tool["headline"], argument)` 로 바꿨다. **`TOOL_OF` 48줄과
`STEP_OF` 37줄은 한 글자도 안 건드렸다.**

**겹침을 앞머리 일치로 본다.** 세 안이 있었다.

```
완전 일치   "전기차 충전소" != "전기차 충전소를 조회했습니다." 라 안 걸린다.
            조사가 붙어 있어 못 쓴다
포함        "역" 이 "국회의원 지역구를 조회했습니다." 안에 걸려 멀쩡한 인자가 빠진다
앞머리      인자가 붙는 자리가 문장 앞이라 더듬거림도 앞에서만 생긴다      <- 골랐다
```

`TOOL_OF` 의 문장 24종을 전부 넣어 봤다. 인자를 빼는 것은 인자가
`"전기차 충전소"` · `"인구"` · `"문서"` · `"철도"` 처럼 도구가 다루는 대상
자체일 때뿐이고, 장소 이름 · 선거구 이름은 하나도 안 걸린다.

```
"{arg} 전기차 충전소를 조회했습니다." + "전기차 충전소"  -> 전기차 충전소를 조회했습니다.
"{arg} 좌표를 조회했습니다."          + "오송역"          -> 오송역 좌표를 조회했습니다.
"{arg} 국회의원 지역구를 조회했습니다." + "역"            -> 역 국회의원 지역구를 조회했습니다.
"{arg} 국회의원 지역구를 조회했습니다." + "충북 제1선거구" -> 충북 제1선거구 국회의원 지역구를 조회했습니다.
```

**언제 뒤집나.** `TOOL_OF` 의 문형이 `"{arg} …"` 가 아닌 것이 생기면.
`template.format(arg="")` 로 뒷 문장을 얻는 방법이 그 문형에 매여 있다.

#### 화면 문구 여덟 개 전후 (실제로 나온 문자열)

고치기 전 코드로 uvicorn 을 띄워 여덟을 먼저 받고, 서버를 내린 뒤 작업본으로
다시 띄워 같은 여덟을 받았다. 모델은 `qwen3:32b`, 온톨로지는 HEAD 그대로다.
**여덟 다 되묻지 않고 한 번에 실행됐다.**

```
1  "오송역 인구 구성 알려줘"

전   오송역 연령대별 인구 구성을 조회했습니다.

     1. geo.geocode       오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1 (127.3277, 36.6200)
     2. adminBoundary.findBoundaryByPoint  3건
     3. population.getAgeProfile  청주시 흥덕구 292,625명 (남 149,484 · 여 143,141) · 2026-06-30 기준

후   오송역 연령대별 인구 구성을 조회했습니다.

     1. geo.geocode       오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1 (127.3277, 36.6200)
     2. adminBoundary.findBoundaryByPoint  lon=127.3277 · lat=36.6200  3건 · 충청북도
     3. population.getAgeProfile  level="sigungu" · code="43113"  청주시 흥덕구 292,625명 (남 149,484 · 여 143,141) · 2026-06-30 기준
```

**2단계에서 셋 중 첫째가 시도(충청북도)인 것이 처음 화면에 보인다.** 3단계가
`code="43113"`(청주시 흥덕구)로 부르는 것도 같은 줄에 있다 — `items.1` 로
시군구에 고정된다는 「열린 과제」의 항목이 이제 화면에서 읽힌다.

```
2  "철도 안전 문서 찾아줘"        <- 문서 내용이 보이는가

전   철도 안전 문서를 조회했습니다.

     1. knowledge.query   4건

후   철도 안전 문서를 조회했습니다.

     1. knowledge.query   query="철도 안전"  4건 · 「철도안전법(법률)(제21188호)(20260303).pdf」 · "법제처 1 국가법령정보센터 철도안전법 철도안전법 [시행 2026. 3. 3.] [법률 제21188호, 202…"
```

```
3  "전기차 충전소 데이터 검색해줘"   <- 인자가 보이는가 · 머리말이 안 겹치는가

전   전기차 충전소 전기차 충전소를 조회했습니다.

     1. ev.searchStations  120건

후   전기차 충전소를 조회했습니다.

     1. ev.searchStations  query="전기차 충전소"  120건 · 일출랜드
```

```
4  "오송역 CCTV 보여줘"           <- 인자가 보이는가

전   오송역 CCTV 를 조회했습니다.

     1. geo.geocode       오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1 (127.3277, 36.6200)
     2. road.getCctv      83건

후   오송역 CCTV 를 조회했습니다.

     1. geo.geocode       오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1 (127.3277, 36.6200)
     2. road.getCctv      minLon=127.1598 · minLat=36.4852 · maxLon=127.4956 · maxLat=36.7547  83건
```

**어댑터가 오송역 좌표를 반경 15km 로 넓힌 결과가 처음 화면에 보인다.**
83건이 어느 범위의 83건인지를 이제 사람이 안다.

```
5  "충북 제1선거구 알려줘"        <- 못 찾았다고 말하는가

전   충북 제1선거구 국회의원 지역구를 조회했습니다.

     1. election.getDistrict  칸: status · message · dataset · query

후   찾지 못했습니다.

     1. election.getDistrict  name="충북 제1선거구"  조건에 맞는 선거구를 찾지 못했습니다.
```

**첫 줄이 뒤집혔다.** 전에는 못 찾은 것을 "조회했습니다" 라고 말했다.

```
6  "충북 청주서원 알려줘"         <- 찾은 것을 보여주는가

전후 같다 (한 글자도 안 다르다)

     충북 청주서원 국회의원 지역구를 조회했습니다.

     1. election.getDistrict  충북 청주서원 · 1건
```

`name="충북 청주서원"` 이 결과 문구에 이미 있어 인자 자리가 저절로 빠졌다.

```
7  "오송역 근처 충전소 찾아줘"    <- 큰 값이 안 새는가

전   오송역 전기차 충전소를 조회했습니다.

     1. geo.geocode       오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1 (127.3277, 36.6200)
     2. ev.searchStations  500건 (전체 2,173건)

후   오송역 전기차 충전소를 조회했습니다.

     1. geo.geocode       오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1 (127.3277, 36.6200)
     2. ev.searchStations  minLon=127.1598 · minLat=36.4852 · maxLon=127.4956 · maxLat=36.7547  500건 (전체 2,173건) · 청주오송역차고지
```

이 응답에는 `geojson` 이 통째로 들어 있는데 화면에는 첫 충전소 이름 하나만
나갔다.

```
8  "오송역 위치 보여줘"           <- 좌표 줄이 예전과 같은가

전후 같다 (한 글자도 안 다르다)

     오송역 좌표를 조회했습니다.

     1. geo.geocode       오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1 (127.3277, 36.6200)
```

**여덟 문구 어디에도 `geojson` · 좌표 배열 · 긴 JSON 이 없다.** 나간 좌표는
어댑터가 만든 bbox 넷이고 그것은 인자다.

#### 관문

```
pytest              202 passed / 1 failed (graphviz 음성 대조군)
                    182 + 새 테스트 20. 기존 테스트는 하나도 안 지웠다
                    (workflow_answer 18 · step_service 2)
check_wiring        recipe 45 · 완비 44 · STEP_OF 37줄 · A 0 · B 0 · C 1
                    HEAD 와 같다. 한 칸도 안 움직였다
check_resolve       한 묶음 · qwen3:32b · 발화 9개 × 10회
                    적중 70/90 · 근접 20 · 빗나감 0 · 못 붙음 0. 한 칸도
                    안 움직였다
                    LLM 단독은 60/90 이다 — 「스물셋째」가 두 묶음 모두 50/90
                    을 봤고 갈린 칸이 발화 2 하나였는데, 이번 묶음에서는 그
                    칸이 10/10 로 돌아왔다. 관문 문구(60/90)와 같은 값이다.
                    그 칸은 HEAD 코드에서도 흔들린 전례가 있고
                    (「스물셋째」의 「LLM 단독 50/90 은 이 변경 탓이 아니다」)
                    check_resolve 는 POST /resolve 만 부르므로 이번에 고친
                    두 파일을 지나지 않는다
화면 실측           위 여덟을 실제로 눌러 문구를 그대로 옮겼다
raw JSON 안 샘      여덟 문구에 geojson · 좌표 배열 · 긴 JSON 없음
vendor 다섯 무결    git diff 에 workflow_answer.py 말고 vendor/asap/ 파일 없음
step_service 최소   git diff 에 STEP_OF · TOOL_OF 의 도구 이름 · 배선 줄 없음.
                    바뀐 것은 headline 조립 한 줄과 새 함수 _headline 뿐이다
```

#### 안 한 것

- **`ageBands` 23건 · `ages` 111건.** 「스물셋째」와 같다. 연령대별을 한 줄에
  담는 방법을 안 정했다. 첫 항목 규칙이 여기는 안 걸린다 — 인구 응답은 건수
  칸이 없어 목록 갈래로 안 간다
- **`geojson.features` 안.** 첫 항목 규칙은 최상위 목록만 본다. 중첩된 목록에
  들어가지 않는다
- **여러 항목.** 첫 항목 하나뿐이다. 둘째부터를 보이려면 줄을 접는 화면 계약이
  필요하고 그것은 저쪽 화면 일이다
- **번호로 답한 것을 받아 실행하는 경로.** 「스물셋째」와 같다
- **`properties` 안의 `name`.** `geojson` feature 는 `properties.name` 을
  갖고 있지만 안 판다. 그 안에는 좌표와 목록이 함께 있어 한 칸만 골라 읽는
  규칙을 다시 세워야 한다
- **`web.search` · `bim` · `dem`.** 권한이 없거나 데이터가 없어 화면 실측
  대상이 아니다

### 2026-08-25 (스물셋째) · 답 문구가 응답의 수치를 싣게 한다 · 화면 실측

**고친 것은 `vendor/asap/workflow_answer.py` 하나다.** 온톨로지 · menu · 배선표
(`step_service.py`) · 정답표를 안 건드렸다. 프롬프트에 안 들어가는 층이라
판정이 움직이면 안 되는 변경이다.

#### 고치기 전에 무엇을 무엇으로 요약했나

`summarize` 가 위에서부터 걸리는 데서 멈춘다. 갈래가 다섯이었다.

```
결과 모양                              무엇으로 판정          나오던 줄
dict 에 error 칸                       _has_error            실패 · {message}
list                                   isinstance            {len}건
dict 에 location=[lon,lat]             _lon_lat              {query} → {주소} (경도, 위도)
dict 에 count(int) 또는                _counted              {count}건 (전체 {total}건)
  features·items·results·data(list)                          0건이면 뒤에 warning·message
그 밖의 dict                           —                     칸: 앞 여섯 칸 이름 (KEY_LIMIT 6)
그 밖(문자열 · 수 · None)              —                     결과를 받았습니다
```

빈 결과 · 오류는 `_verdict` 가 따로 가른다. 마지막 단계의 센 건수가 0이면 빈
결과, 어느 단계든 `step_failed` 면 오류, `trace` 가 비어도 오류다. 여기는 안
건드렸다.

**막힌 자리 둘.**

```
인구 응답      건수 칸이 하나도 없어 마지막에서 두 번째 갈래로 떨어진다.
               ageBands · ages 는 list 지만 LIST_KEYS 에 없는 이름이다.
               앞 여섯 칸이 datasetId · referenceDate · level · code ·
               sourceAreaCodes · boundaryMatch 라 일곱째 name 과 여덟째
               totalPopulation 이 잘린다 — 수치가 하나도 안 나간다
여럿 중 하나   count 1 · totalMatches 8 이면 "1건 (전체 8건)" 까지는 나왔다.
               무엇을 받았는지(item 의 이름)를 안 말하고, 여덟 중 하나라는
               것이 괄호 안 숫자로만 있었다
```

#### 고른 안 — 나) 칸 이름 규칙

세 안이 있었다.

```
가) 도구마다 표          정확하다. 이 파일이 도구 이름을 알게 되고 도구가 늘 때마다 줄이 는다
나) 칸 이름 규칙         새 도구에도 통한다. 틀린 칸을 집을 수 있다      ← 골랐다
다) 배선표(STEP_OF)      이미 도구별 표가 있는 곳이다. 배선 한 줄이 답 문구까지 맡는다
```

**다) 는 먼저 빠졌다.** 배선표는 프롬프트와 판정이 함께 걸린 곳이고 이번
변경은 "판정이 안 움직여야 한다" 가 관문이다. 답 문구를 고치려고 그 표를
건드리면 관문 자체를 못 믿는다.

**가) 와 나) 중 나) 를 고른 이유는 파일의 계약이다.** `workflow_answer.py` 의
첫 문단이 "여기는 도구 이름을 모른다. 도구가 늘어도 이 파일은 그대로다" 라고
적고 있다. 가) 는 그 한 줄을 깨는 일이고, 깨야 할 만큼 급하지 않았다.

규칙은 넷이다. 전부 실측으로 본 칸 이름뿐이다.

```
name                          이름                     "청주시 흥덕구" · "충북 청주서원"
total+대문자로 시작하는 뒷이름  대표 수치                 totalPopulation 292625
그 뒷이름이 같은 male·female   곁수치                   malePopulation · femalePopulation
referenceDate                 언제 기준인가             2026-06-30
```

**나) 의 대가가 실제로 나왔다.** `ev.getDatasetInfo` 의 `totalRegionCount` 17 을
집어 "한국환경공단 전기자동차 충전소 17" 이 나왔다. 그 도구의 충전소는 93,353
이므로 17 을 이름 옆에 놓으면 딴 뜻으로 읽힌다. **단위를 아는 수치만 쓰도록
막았다** — 뒷이름이 `SUBJECT_UNITS`(지금 `population` → `명`) 에 없으면 그
수치를 안 쓴다. 그래서 저 응답은 이름만 나간다.

**언제 뒤집나.** `PART_WORDS` · `SUBJECT_UNITS` 가 예닐곱 줄을 넘거나 한
도구만을 위한 줄이 들어가야 할 때. 그때는 규칙이 아니라 표가 된 것이므로
가) 나 다) 로 옮기는 것이 정직하다. **`KEY_LIMIT` 를 늘리는 것으로는 안
때웠다** — 칸 이름을 여덟 개 찍는 것은 수치를 싣는 것이 아니다.

#### 화면 문구 전후 (실제로 나온 문자열)

uvicorn 을 고치기 전 코드로 띄운 채 먼저 받고, `pkill -f "demo.api.main"` 뒤
다시 띄워 받았다. 모델은 `qwen3:32b`, 온톨로지는 HEAD(a4ae834) 그대로다.

```
"오송역 인구 구성 알려줘"

전   오송역 연령대별 인구 구성을 조회했습니다.

     1. geo.geocode       오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1 (127.3277, 36.6200)
     2. adminBoundary.findBoundaryByPoint  3건
     3. population.getAgeProfile  칸: datasetId · referenceDate · level · code · sourceAreaCodes · boundaryMatch

후   오송역 연령대별 인구 구성을 조회했습니다.

     1. geo.geocode       오송역 → 충청북도 청주시 흥덕구 오송읍 봉산리 369-1 (127.3277, 36.6200)
     2. adminBoundary.findBoundaryByPoint  3건
     3. population.getAgeProfile  청주시 흥덕구 292,625명 (남 149,484 · 여 143,141) · 2026-06-30 기준
```

**"춘천역 인구 알려줘" 는 전후 모두 되묻는다.** 후보 셋(인구 통계 조회 ·
연령별 인구 구성 조회 · 인구 변화 추이 조회)을 내놓고 끝난다. 문구가 전후로
한 글자도 안 다르다 — 이 변경이 해석 층에 안 걸린다는 것을 화면에서도 본
셈이다. **번호로 답한 것을 받아 실행하는 경로가 아직 없으므로**(`_clarify_head`
의 제약 절) 고른 뒤를 보려면 발화를 다시 해야 한다. 그렇게 했다.

```
"춘천역 연령대별 인구 구성 알려줘"   (되물음 뒤 2번을 발화로 다시 말한 셈)

후   춘천역 연령대별 인구 구성을 조회했습니다.

     1. geo.geocode       춘천역 → 강원특별자치도 춘천시 근화동 558 (127.7166, 37.8844)
     2. adminBoundary.findBoundaryByPoint  3건
     3. population.getAgeProfile  춘천시 285,027명 (남 140,401 · 여 144,626) · 2026-06-30 기준
```

#### 여럿 중 하나 — 화면으로는 못 봤다

`totalMatches` 가 `count` 보다 크면 "전체 8건 중 하나" 를 붙인다. 응답 전문으로는
확인했다.

```
election.getDistrict(name="충북")   실물 응답
전   1건 (전체 8건)
후   충북 청주서원 · 전체 8건 중 하나
```

**화면 발화로는 그 자리에 못 갔다. 다섯 번 눌렀다.**

```
"충북 국회의원 지역구 알려줘"              CLARIFY (검색 셋)
"충북 지역구 상세 보여줘"                  CLARIFY (상세 셋)
"충북 이라는 이름의 국회의원 지역구 상세…"  CLARIFY (상세 둘)
"선거구명 충북 으로 지역구 집어줘"          searchDistricts 8건 (get 이 아니다)
"선거구명 충북 인 지역구 하나만 집어줘"     getDistrict 는 갔는데 인자가 "충북 인" 이라
                                           not_found. 답은 "칸: status · message ·
                                           dataset · query"
```

**두 가지가 함께 막는다** — 되물음이 실행까지 안 가는 것과, 인자를 발화에서
통째로 옮겨 담는 것(위 「열린 과제」의 인자 항목). **그 not_found 응답 모양
(`status` · `message`)이 아직 칸 이름으로 나가는 것도 이번에 화면에서 봤다.**
0건 판정이 안 걸리는 자리라 `_notice` 가 안 붙는다. 이번 범위 밖이라 안 고쳤다.

**「열린 과제」의 `recipe_015` 항목은 낡았다.** "배선이 없어 도구를 하나도 안
부른다" 고 적혀 있는데 `STEP_OF` 에 `get_election_district` 줄이 있고 위
네 번째 발화에서 `election.getDistrict` 를 실제로 불렀다. 그 항목에 정정을
붙였다.

#### `totalMatches` 를 주는 도구와 안 주는 도구

`tools/probe_out` 의 응답 중 건수를 셀 수 있는 것만 셌다(오류 응답 제외).
**응답마다 갈리는 도구는 없다.** 도구별로 늘 주거나 늘 안 준다.

```
준다 (14)     adminBoundary.findBoundaryByPoint · adminBoundary.searchBoundaries
              election.findAssemblyDistrictByPoint · election.findAssemblyPledgeDistrictByPoint
              election.getAssemblyDistrict · election.getAssemblyPledgeDistrict
              election.getDistrict · election.searchAssemblyDistricts
              election.searchAssemblyPledgeDistricts · election.searchDistricts
              election.searchLocalPledgeSummaries · ev.searchChargers
              ev.searchStations · population.searchStatistics

안 준다 (5)   election.findDistrictByPoint · ev.getStation · geo.getRailwayLines
              population.getTrend · vworld.getAdministrativeBoundaries
```

안 주는 다섯에는 아무 말도 안 붙인다. 없는 칸을 지어내지 않는다 —
`ev.getStation` 은 "포빌 · 1건" 까지만 나온다.

#### 관문

```
pytest              182 passed / 1 failed (graphviz 음성 대조군)
                    175 + 새 테스트 7. 기존 테스트는 하나도 안 지웠다
check_wiring        recipe 45 · 완비 44 · STEP_OF 37줄 · A 0 · B 0 · C 1
                    HEAD 와 같다
check_resolve       두 묶음 · qwen3:32b · 발화 9개 × 10회
                    적중 70/90 · 근접 20 · 빗나감 0 · 못 붙음 0. 두 묶음이 같다
                    LLM 단독은 50/90 이다. 관문 문구의 60/90 과 한 칸 다르다 —
                    아래를 본다
vendor 다섯 무결    git diff 에 workflow_answer.py 말고 vendor/asap/ 파일 없음
```

#### LLM 단독 50/90 은 이 변경 탓이 아니다

관문은 `LLM 단독 60/90` 인데 두 묶음 모두 `50/90` 이 나왔다. **갈린 칸은 발화 2
하나다** — "오송역 좌표 알려줘" 의 LLM 단독이 10/10 이 아니라 0/10 이고, LLM 이
`{001, 020}` 을 내면 검산이 `{001}` 로 살린다. 최종 판정은 10/10 적중이다.

**작업본을 통째로 치우고 같은 발화를 다시 쟀다.**

```
작업본            발화 2   LLM 단독 0/10 · 적중 10/10
git stash 후     발화 2   LLM 단독 0/10 · 적중 10/10      ← HEAD 코드다
```

`git stash` 로 세 파일을 다 치우고 uvicorn 을 다시 띄워 잰 것이다. **HEAD 에서도
같다.** `check_resolve` 는 `POST /resolve` 만 부르고 그 경로는
`workflow_answer` 를 지나지 않는다. 이 칸이 언제 60/90 에서 50/90 이 됐는지는
이 작업의 범위 밖이고, **관문이 보는 적중 · 근접 · 빗나감은 세 칸 다
안 움직였다.**

#### 안 한 것

- **번호로 답한 것을 받아 실행하는 경로.** 되물음 뒤를 화면에서 못 본 원인
  절반이 이것이다. 세션 상태와 저쪽 화면 계약이 필요하다
- **`status`/`message` 만 있는 not_found 응답.** 칸 이름으로 나간다.
  0건 판정에 안 걸려 `_notice` 가 안 붙는 자리다
- **`getDistrict.name` 이 여덟 중 무엇을 기준으로 하나를 고르는지.**
  「물음 4」 때와 같다. 응답에 안 적혀 있고 저쪽 코드를 안 읽었다.
  답 문구는 "여덟 중 하나" 라고만 말하고 어느 하나인지는 안 설명한다
- **`ageBands` 23건 · `ages` 111건을 안 쓴다.** 이름 · 합계 · 남녀 · 기준일까지다.
  연령대별을 한 줄에 담는 방법을 안 정했다

#### ★ 미완성이다

**사람이 바란 것은 이것이 아니다.** 바란 것은 "모든 도구가 실행될 때 **어떤
인자로 불렀고 DB 에서 무엇이 나왔는지**가 화면에 보인다" 였다. 그래야 인자가
잘못 들어갔는지, 데이터가 있는지 없는지를 사람이 안다. 프롬프트가 그중 둘만
집어 시켰고(인구 응답 수치 · 여럿 중 하나) **나머지가 그대로 남았다.**

아래 넷은 오늘(2026-08-25) 고친 코드로 화면에서 다시 눌러 받은 문자열이다.

```
① 인자가 안 보인다

   "전기차 충전소 데이터 검색해줘"
   → 1. ev.searchStations  120건

   무엇으로 검색해 120건인지 화면에 없다. geo.geocode 만 "오송역 →" 로 인자를
   보여주는데 그것은 좌표 갈래가 query 를 쓰기 때문이지 인자를 보여주려고
   만든 자리가 아니다. trace 항목에는 단계마다 input 이 담겨 있다.
   답 문구가 안 쓸 뿐이다

   해결 (2026-08-25 「스물넷째」)
   step_line 이 input 을 읽어 도구 이름과 결과 사이에 적는다. 문자열 · 정수 ·
   실수만 적고 dict · list · bool 은 건너뛴다. 결과 문구에 이미 있는 값도
   건너뛴다 — geo.geocode 가 저절로 걸린다.
   화면 : ev.searchStations  query="전기차 충전소"  120건 · 일출랜드

② 못 찾은 것을 못 찾았다고 말 못 한다

   "충북 제1선거구 알려줘"
   → 1. election.getDistrict  칸: status · message · dataset · query

   응답의 message 에 못 찾은 이유가 들어 있는데 칸 이름만 찍힌다.
   status 가 "not_found" 인 것도 안 읽는다. 건수 칸이 없어 0건 판정에 안 걸리고
   _notice 가 안 붙는 자리다

   해결 (2026-08-25 「스물넷째」)
   최상위 status 가 not_found · empty 면 응답의 message 를 그대로 낸다.
   _verdict 도 그때 빈 결과로 봐서 첫 줄이 "찾지 못했습니다." 가 된다.
   probe_out 에서 본 status 값 넷 중 ready · syncing 은 안 쓴다 —
   데이터가 있는 상태다.
   화면 : 찾지 못했습니다. / election.getDistrict  name="충북 제1선거구"
          조건에 맞는 선거구를 찾지 못했습니다.

③ 목록형 응답이 건수만 낸다 — RAG 가 여기 걸린다

   "철도 안전 문서 찾아줘"
   → 1. knowledge.query   4건

   knowledge.query 는 [{content: 본문, metadata: 출처}, …] 를 돌려준다
   (ASAP-mcp/app/tools/knowledge.py:14). 오늘 Gateway 로 직접 눌러 확인했다 —
   네 건의 content 가 962~995자이고 metadata.source 가 파일 이름이다.
   **본문이 오는데 세기만 한다.** 시연 요구사항인 "문서 내용을 찾아온다" 가
   도구 쪽은 되고 화면 쪽만 안 되는 상태다.
   그 "4건" 은 문서 수가 아니라 k 의 기본값 4다 (ASAP-mcp/main.py:472).
   **문서는 두 건이다** — knowledge.listDocs 가 철도안전법과 그 시행규칙 둘을
   돌려준다(오늘 실측). 4 는 잘라 온 조각 수이지 문서 수가 아니다

   해결 (2026-08-25 「스물넷째」)
   목록의 첫 항목을 _record_line 이 요약한다. 출처(metadata.source)와
   본문(content)을 읽는 칸을 더했고 길이를 자른다. 도구 이름을 모르는
   규칙이라 geojson feature 와 cctvUrl 은 그대로 안 나간다.
   **"4건" 이 조각 수라는 것은 안 고쳤다** — 도구가 준 배열의 길이이고,
   그것이 문서 수가 아니라는 사실은 이 층에서 알 수 없다.
   화면 : knowledge.query  query="철도 안전"  4건 ·
          「철도안전법(법률)(제21188호)(20260303).pdf」 · "법제처 1 국가법령정보센터 …"

④ 머리말이 겹친다

   "전기차 충전소 데이터 검색해줘"
   → "전기차 충전소 전기차 충전소를 조회했습니다."

   「열린 과제」에 이미 있는 항목이다. 이번에 화면으로 다시 확인됐다

   해결 (2026-08-25 「스물넷째」)
   step_service.plan 의 조립 한 줄을 _headline 으로 바꿨다. 인자가 뒷 문장의
   앞머리와 같으면 앞에 안 붙인다. TOOL_OF 48줄은 안 건드렸다.
   화면 : 전기차 충전소를 조회했습니다.
```

**그리고 이번에 고른 방법 자체가 좁다.** `probe_out` 의 응답 전부에서 최상위
`total<대문자>` 꼴 정수 칸을 셌다. **이름이 넷뿐이다.**

```
칸 이름            응답 수   도구 수   지금 쓰나
totalMatches         55       14      아니다. 여럿 중 하나를 밝히는 데만 쓴다
totalPopulation       6        2      쓴다 (SUBJECT_UNITS 의 유일한 항목)
totalFetched          3        1      버린다
totalRegionCount      2        1      버린다 (충전소 수로 읽혀서 일부러 막았다)
```

**모양은 칸 이름 규칙인데 실질은 표에 가깝다.** 지금 이 갈래를 타는 응답은
인구 하나뿐이고, 새 수치가 나올 때마다 `SUBJECT_UNITS` 에 한 줄씩 는다.
뒤집는 조건은 위에 적은 대로다 — 예닐곱 줄을 넘거나 한 도구만을 위한 줄이
들어가면 도구별 표(가)나 배선표(다)로 옮긴다.

### 2026-08-25 (스물두째) · 시작 데이터 노드 셋만 되돌려 교락을 가름 · 세 묶음

**★ 세 묶음을 채우지 못했다.** 사람이 장비를 떠나야 해서 셋째 묶음을 중간에
끊었다(발화 1 진행 중). **아래는 두 묶음이다.** 미완 출력은 남기지 않았다.
두 묶음이 발화별로 한 칸 빼고 같으므로 결론은 세워 두되, **세 묶음 규약을 안
지킨 측정이라는 것을 여기 적어 둔다.** 다시 잴 때는 셋을 채운다.

**채웠다 (2026-08-25).** 같은 날 사람이 돌아와 셋째 묶음(G)을 처음부터 다시
돌렸다. 온톨로지도 menu 도 그사이 한 글자도 안 바뀌었고 uvicorn 도 그대로
떠 있던 것을 그대로 썼다. **G 는 E 와 네 표가 바이트 단위로 같다.**
아래 표와 본문은 세 묶음 기준으로 고쳤다. 위 문단은 끊었던 사실을 남기려고
지우지 않는다.

**왜 갈랐나.** 「스물한째」에서 문장 아홉을 한 번에 고쳤고 판정이 81/90 에서
60/90 이 됐다. 대조군으로 원인이 그 변경인 것까지는 갈렸는데 **아홉 중 어느
쪽인지가 교락돼 있었다.** 선거 여섯은 menu 45문장 중 일곱 줄을 바꾸고, 시작
데이터 셋은 menu 에 안 들어가는 대신 `given` 축 선택지 셋을 바꿔 프롬프트
전체에 걸린다. **셋만 되돌리면 갈린다.**

#### 되돌린 문장 셋

`git show HEAD:ontology/ontology.yaml` 에서 뽑아 대조해 넣었다. 손으로 안 썼다.

```
말한 장소     지도 위 한 곳의 이름. 좌표나 도형으로 바꿔 쓴다.
              → 사용자가 발화에서 지목한 장소.
말한 키워드   데이터셋을 훑는 검색어. 몇 건이 걸릴지 모르는 목록을 받는다.
              → 사용자가 발화에서 찾아 달라고 한 말.
말한 식별자   한 건을 집어내는 코드나 정식 이름. 검색어로는 안 걸린다.
              → 사용자가 발화에서 집어 말한 코드나 이름.
```

**선거 여섯의 문장은 한 글자도 안 건드렸다.** `git diff ontology/ontology.yaml`
에 선거 여섯만 남고 `spoken` 은 0줄이다.

`given` 축 선택지는 186자에서 150자로, 축 셋 합계는 770자에서 734자로
HEAD 와 같아졌다.

#### menu 는 한 줄도 안 바뀌었다

시작 데이터 노드는 이름만 menu 에 들어가고 이름은 안 건드렸으므로 그래야 한다.
`rebuild_init --write` + `reset_to_init()` 뒤에 `diff` 가 0줄이고,
「스물한째」의 일곱 줄은 `git diff --numstat` 이 7/7 로 그대로 세고 있다.

```
두 ontology.yaml 바이트 동일   cmp 통과
git diff 에 남은 것            선거 여섯 문장뿐
menu.yaml 변화                 0줄
check_wiring                   recipe 45 · 완비 44 · STEP_OF 37줄 · A 0 · B 0 · C 1
pytest                         175 passed / 1 failed (graphviz 음성 대조군)
menu.yaml 자수                 3710자 / 6000
```

#### 세 묶음

각 묶음 발화 9개 × 10회. `pkill -f "demo.api.main"` 후 본 트리에서 uvicorn 을
다시 띄우고 쟀다.

```
E 묶음
#  발화                                      LLM 단독   적중     근접    빗나감   못 붙음   틀렸을 때 나온 것
1  오송역 위치 보여줘                        10/10      10/10    0       0        0
2  오송역 좌표 알려줘                        10/10      10/10    0       0        0
3  오송역 CCTV 보여줘                        10/10      10/10    0       0        0
4  청주시 인구 구성 알려줘                    0/10       0/10    10       0        0   {042, 043}      근접   CLARIFY 10회
5  오송역 근처 충전소 찾아줘                  0/10      10/10    0       0        0
6  국회의원 선거구 찾아줘                     0/10       0/10    10       0        0   {007,008,009}   근접   CLARIFY 10회
7  전기차 충전소 데이터 검색해줘             10/10      10/10    0       0        0
8  철도 안전 문서 찾아줘                     10/10      10/10    0       0        0
9  충북 제1선거구 알려줘                     10/10      10/10    0       0        0
                                            60/90      70/90   20       0        0     78%
                                            67% ← LLM 단독

F 묶음
2  오송역 좌표 알려줘                         0/10      10/10    0       0        0   ← 여기 하나만 E 와 다르다
                                            50/90      70/90   20       0        0     78%
                                            56% ← LLM 단독

G 묶음
E 와 네 표가 전부 바이트 단위로 같다. 발화 2 도 LLM 단독 10/10 이다.
                                            60/90      70/90   20       0        0     78%
                                            67% ← LLM 단독
```

**세 묶음의 차이는 발화 2 의 LLM 단독 한 칸뿐이다.** E · G 는 10/10 이고 F 만
0/10 인데 최종은 셋 다 10/10 이다 — F 에서 LLM 이 `{001, 020}` 을 냈고 검산이
`{001}` 을 살렸다. **셋 중 하나만 튀었으므로 발화 2 는 F 가 튄 것이다** —
흔들리는 자리이긴 하나 기본값은 10/10 이다. 최종 표 · 축 표 · 후보 표는
세 묶음이 같고, E 와 G 는 파일 전체가 같다.

#### 판정 — 미리 정한 넷 중 「그 사이 어딘가」다

```
HEAD 대조군          81/90 · LLM 단독 61/90 · 근접  9(발화 6)        · 빗나감  0
「스물한째」 아홉 개  60/90 · LLM 단독 40/90 · 근접 20(발화 6 · 9)    · 빗나감 10(발화 4)
이번 선거 여섯만     70/90 · LLM 단독 60/90 · 근접 20(발화 4 · 6)    · 빗나감  0
```

81/90 회복도 아니고 60/90 그대로도 아니다. **둘 다 조금씩이다.**

#### 발화별로 어디로 갔나

```
발화 4  빗나감 10 {011}      → 근접 10 {042, 043}   ★ 빗나감은 사라졌는데 적중은 아니다
발화 6  근접 10               → 근접 10 {007,008,009}  세 상태 모두 안 움직인다
발화 7  LLM 단독 0/10         → LLM 단독 10/10        완전 회복
발화 9  근접 10 {015,016,017} → 적중 10/10            완전 회복
1·3·5·8  세 상태 모두 같다
발화 2  최종은 늘 10/10. LLM 단독만 묶음마다 흔들린다(E 10/10 · F 0/10)
```

**발화 4 의 `given` 축은 돌아왔다.** 이것이 이번에 확인하려던 한 줄이다.

```
HEAD 대조군          spoken_place   · statistics · group_population   "청주시"  10회
「스물한째」 아홉 개  spoken_keyword · statistics · group_population   "청주시"  10회
이번 선거 여섯만     spoken_place   · statistics · group_population   "청주시"  10회   ← 돌아왔다
```

**「스물한째」의 발화 4 해석은 맞았다.** 축 설명 셋을 고친 것이 `given` 을
뒤집었고, 되돌리자 그대로 돌아왔다. 빗나감 10 도 함께 사라졌다.

**그런데 적중까지는 안 갔다. 선거 여섯도 발화 4 를 움직인다.** 후보 표와 검산
표가 그 자리를 보여 준다.

```
                     LLM 후보 수   LLM 단독      검산 후    status
HEAD 대조군          1             {011, 042}    {042}      SELECT   적중
이번 선거 여섯만     2             {042, 043}    {042, 043} CLARIFY  근접
```

`given` 축이 같고 조회 후보도 3개로 같은데 **LLM 이 내는 후보가 달라졌다.**
`{011, 042}` 는 검산이 `011`(말한 키워드에서 시작)을 떨어뜨려 하나가 됐는데,
`{042, 043}` 은 둘 다 조회 후보 안에 있어 검산이 못 줄인다. 043 은
「말한 장소 → 좌표 → 지점 행정구역 판별 → 인구 변화 추이 조회」다.
`given` 축 선택지가 HEAD 와 글자까지 같으므로 **남는 원인은 menu 의 일곱 줄뿐이다.**
선거와 아무 상관없는 발화가 menu 문장 때문에 움직인 것이다 —
「열한째」·「열여덟째」·「스물한째」에 이어 **네 번째 전례다.**

다만 이것이 나빠진 것인지는 갈린다. `{011}` 을 확신하던 것(빗나감)이
`{042, 043}` 을 되묻는 것(근접)이 됐다. 점수는 둘 다 0/10 이지만
**「틀린 답보다 정직한 되물음이 낫다」는 이 저장소의 원칙으로는 올라간 자리다.**
HEAD 의 적중 10/10 보다는 아래다.

**발화 9 는 선거 문장 쪽이 아니었다.** 선거 여섯을 새 문장 그대로 둔 채
적중 10/10 으로 완전히 돌아왔다. **「스물한째」의 발화 9 해석은 틀렸다** —
아래에 정정을 적는다.

**발화 7 도 축 설명 셋이 범인이었다.** 선거와 무관한 발화가 흔들린 것은
`given` 축 선택지 때문이고, 되돌리자 LLM 단독이 10/10 으로 돌아왔다.

**발화 6 은 세 상태 모두 `{007, 008, 009}` 근접이다.** 옛 문장에서도, 아홉을
다 고친 뒤에도, 선거 여섯만 남긴 지금도 안 갈린다. 「스물한째」에 적은
「문장으로는 안 갈린다」가 한 번 더 굳었다.

#### 「스물한째」 정정 — 발화 9 의 해석

「스물한째」는 발화 9 가 `{015, 016, 017}` 로 벌어진 것을 두고
"이름 형식을 넣은 것이 반대로 작용한 것으로 보인다. 다만 정황이다" 라고 적었다.
**그 정황은 틀렸다.** 이름 형식이 든 새 문장을 그대로 두고 축 설명 셋만
되돌렸더니 적중 10/10 으로 돌아왔다. **원인은 `given` 축 선택지 쪽이다** —
`말한 식별자` 를 "한 건을 집어내는 코드나 정식 이름. 검색어로는 안 걸린다."
로 바꾼 것이 015 · 016 · 017 사이를 오히려 흐렸다.
「스물한째」의 그 문단은 지우지 않는다. 이 줄이 정정이다.

#### 이번에 안 한 것

- **셋째 묶음은 같은 날 채웠다.** 위 「채웠다」 절을 본다. 이 줄은 한 번
  못 채운 채로 결론을 적었다는 기록으로 남긴다.
- 발화 4 의 LLM 후보가 `{011, 042}` 에서 `{042, 043}` 으로 바뀐 이유를
  **LLM 의 reason 을 읽어서 확인하지 않았다.** 남는 원인이 menu 뿐이라는 것은
  소거법이고, 어느 문장이 그렇게 만들었는지는 안 갈랐다.
- 공약 셋(`search_assembly_pledge_districts` 외 둘)은 「스무째」에서 안 눌러
  이번에도 안 고쳤다. 발화 6 의 `009` 가 그것이다.
- 노드 이름 · GT · `demo/` · `tools/check_resolve.py` · `step_service.py` 를
  안 건드렸다.

### 2026-08-25 (스물한째) · 선거 여섯과 시작 데이터 셋의 description 을 실측으로 다시 씀 · ★ 판정이 나빠졌다

**결론 한 줄. 문장은 사실에 가까워졌는데 판정은 81/90 에서 60/90 으로 떨어졌다.**
빗나감이 0 에서 10 이 됐다. **되돌리지 않았고 문장도 다시 안 손봤다.**
근거는 「스무째」의 실측이지 판정 숫자가 아니라는 「열째」의 원칙 그대로다.
커밋은 안 했다.

**고친 것은 description 문자열 아홉 개뿐이다.** 노드도 관계도 이름도 하나도
안 늘고 안 줄었다. recipe 45개의 사슬과 번호가 한 자리도 안 움직였다.

#### 고친 문장 아홉 — 전후

시작 데이터 노드 셋. menu 문장에는 안 들어가고 프롬프트의 `given` 축 선택지로만
쓰인다.

```
말한 장소
  전  사용자가 발화에서 지목한 장소.
  후  지도 위 한 곳의 이름. 좌표나 도형으로 바꿔 쓴다.

말한 키워드
  전  사용자가 발화에서 찾아 달라고 한 말.
  후  데이터셋을 훑는 검색어. 몇 건이 걸릴지 모르는 목록을 받는다.

말한 식별자
  전  사용자가 발화에서 집어 말한 코드나 이름.
  후  한 건을 집어내는 코드나 정식 이름. 검색어로는 안 걸린다.
```

선거 노드 여섯.

```
search_election_districts
  전  제22대 국회의원 지역구를 선거구명과 시도, 코드로 검색한다.
  후  제22대 국회의원 선거구를 충북 청주서원 같은 선거구명과 시도 약칭, 코드로 검색한다.

get_election_district
  전  선거구 코드나 이름으로 제22대 국회의원 지역구 한 곳을 조회한다.
  후  선거구 코드나 충북 청주서원 같은 선거구명으로 제22대 국회의원 선거구 한 곳을 조회한다.

find_election_district_by_point
  전  그 지점의 제22대 국회의원 지역구를 찾는다.
  후  그 지점의 제22대 국회의원 선거구를 충북 청주흥덕 같은 선거구명으로 찾는다.

search_assembly_districts
  전  제22대 국회의원 전체 선거구를 공약 수와 분야 집계로 검색한다.
  후  제22대 국회의원 선거구 254곳을 청주시 흥덕구 같은 행정구역명과 공약 본문으로
      검색하고, 77곳에 붙은 공약 476건을 분야 여섯으로 집계해 낸다.

get_assembly_district
  전  코드나 이름으로 제22대 국회의원 전체 선거구 한 곳을 조회한다.
  후  선거구 코드나 청주시 흥덕구 같은 행정구역명으로 제22대 국회의원 선거구 한 곳을
      당선인과 정당, 공약과 함께 조회한다.

find_assembly_district_by_point
  전  그 지점의 제22대 국회의원 전체 선거구를 공약 집계와 함께 찾는다.
  후  그 지점의 제22대 국회의원 선거구를 청주시 흥덕구 같은 행정구역명과 당선인,
      정당, 공약 집계로 찾는다.
```

#### 무엇을 근거로 그렇게 썼나 — 전부 「스무째」다

```
"전체" 를 버렸다        「물음 1」. 254 대 254 인데 이름 교집합이 0 이다.
                        범위 차이가 아니라 출처가 다른 두 자료다
                        (오마이뉴스 GeoJSON 대 사용자 제공 election.shp)
이름 형식을 넣었다      「물음 1」·「물음 3」. 앞은 "충북 청주서원" 형식이고
                        뒤는 "청주시 흥덕구" 형식이다. 같은 오송역 좌표가
                        "충북 청주흥덕" 과 "청주시 흥덕구" 로 갈린다
시도 약칭을 적었다      「물음 5」의 sido 표. searchDistricts 는 "충북" 8건 ·
                        "충청북도" 0건이다. 뒤쪽은 정확히 반대다
254 · 77 · 476 · 여섯   「물음 2」. 254건을 전부 세어 본 값이고
                        dataset.pledgeDistrictCount 77 · pledgeCount 476 과 같다.
                        분야는 철도 · 도로 · 대중교통 · 항공 · 선박 · 교통전체
당선인과 정당           「물음 2」. 청주시 흥덕구 winnerNames ["이연희"] ·
                        partyNames ["더불어민주당"] 처럼 실물이 온다
공약 본문까지 훑는다    「물음 1」. query="청주" 가 앞은 4건 뒤는 5건이고
                        다섯째가 보은군_옥천군_영동군_괴산군 이다
시작 데이터 셋을 가른 축  「물음 6」·「물음 7」의 충전소 실측. getStation 은
                        statId="포빌" 이 0건이고 searchStations 는
                        query="PL033780" 이 0건이다. 둘이 배타적이다
```

#### 관문 — 판정만 못 넘었다

```
두 ontology.yaml 이 바이트 동일      cmp 통과
rebuild_init                         새것 0 · 사라진 것 0 · recipe 45개 · 번호 그대로
check_wiring                         recipe 45 · 완비 44 · STEP_OF 37줄 · A 0 · B 0 · C 1
                                     하나도 안 움직였다
pytest                               175 passed / 1 failed (graphviz 음성 대조군)
menu.yaml 자수                       3710자 / MENU_BUDGET 6000
check_resolve 세 묶음                ★ 못 넘었다. 아래
```

`given` 축 선택지는 150자에서 186자가 됐고 축 셋 합계는 734자에서 770자다.

#### 세 묶음 — 셋이 같은 값이다

각 묶음 발화 9개 × 10회. `pkill -f "demo.api.main"` 후 본 트리에서 uvicorn 을
다시 띄우고 쟀다.

```
#  발화                                      LLM 단독   적중     근접    빗나감   못 붙음   틀렸을 때 나온 것
1  오송역 위치 보여줘                        10/10      10/10    0       0        0
2  오송역 좌표 알려줘                        10/10      10/10    0       0        0
3  오송역 CCTV 보여줘                        10/10      10/10    0       0        0
4  청주시 인구 구성 알려줘                   0/10       0/10     0       10       0   {011}           빗나감 SELECT  10회
5  오송역 근처 충전소 찾아줘                 0/10       10/10    0       0        0
6  국회의원 선거구 찾아줘                    0/10       0/10     10      0        0   {007,008,009}   근접   CLARIFY 10회
7  전기차 충전소 데이터 검색해줘             0/10       10/10    0       0        0
8  철도 안전 문서 찾아줘                     10/10      10/10    0       0        0
9  충북 제1선거구 알려줘                     0/10       0/10     10      0        0   {015,016,017}   근접   CLARIFY 10회
                                            40/90      60/90    20      10       0     67%
                                            44% ← LLM 단독
```

**세 묶음이 이 표 하나로 같다.** 묶음 B 와 C 는 네 표가 바이트 단위로 같고,
묶음 A 는 발화 2 의 `about` 칸 하나만 다르다(`group_transport` 대 `-`).
판정은 셋 다 같다.

#### 대조군 — 이번 변경이 원인이다

기준선(「열아홉째」 B 묶음)은 어제 잰 값이라 오늘 ollama 에서도 그 값이 나오는지를
먼저 봤다. 「열아홉째」와 같은 방법이다 — HEAD(312664c)를 worktree 로 펼쳐
8010 포트에 따로 띄우고 같은 ollama 위에서 같은 10회를 돌렸다. **작업본은 한 자도
안 건드렸다.**

```
#  발화                                      LLM 단독   적중     근접    빗나감   못 붙음
1  오송역 위치 보여줘                        10/10      10/10    0       0        0
2  오송역 좌표 알려줘                        10/10      10/10    0       0        0
3  오송역 CCTV 보여줘                        10/10      10/10    0       0        0
4  청주시 인구 구성 알려줘                   0/10       10/10    0       0        0
5  오송역 근처 충전소 찾아줘                 0/10       10/10    0       0        0
6  국회의원 선거구 찾아줘                    1/10       1/10     9       0        0   {007,008,009} 근접 CLARIFY 9회
7  전기차 충전소 데이터 검색해줘             10/10      10/10    0       0        0
8  철도 안전 문서 찾아줘                     10/10      10/10    0       0        0
9  충북 제1선거구 알려줘                     10/10      10/10    0       0        0
                                            61/90      81/90    9       0        0     90%
                                            68% ← LLM 단독
```

**「열아홉째」 B1 · B2 와 한 칸도 안 다르다.** 기준선은 오늘도 그대로다.
그러므로 세션도 ollama 도 uvicorn 도 아니고 **이번 description 변경이 원인이다.**

#### 발화별로 움직인 것 — 셋이 움직였다

```
발화 4  적중 10/10        → 빗나감 10   {011}            ★ 제일 나쁘다
발화 6  적중 1 · 근접 9   → 근접 10     {007,008,009}     사실상 그대로
발화 7  최종 10/10 그대로 → 10/10       LLM 단독만 10/10 → 0/10
발화 9  적중 10/10        → 근접 10     {015,016,017}
나머지 다섯(1·2·3·5·8)   안 움직였다
```

**발화 4 는 `given` 축이 뒤집혔다.** 축 표가 그 자리를 그대로 보여 준다.

```
대조군   spoken_place   · statistics · group_population   "청주시"
이번     spoken_keyword · statistics · group_population   "청주시"
```

`말한 장소` 를 "좌표나 도형으로 바꿔 쓴다" 로 좁히고 `말한 키워드` 를
"데이터셋을 훑는 검색어" 로 넓힌 것이 그대로 나타났다. 인구 구성 조회는 좌표가
필요 없어 보이므로 `말한 장소` 에서 밀려나고, 데이터셋을 훑는 일로 읽혀
`말한 키워드` 로 간다. 그러면 shortlist 가 `recipe_011`(말한 키워드 → 인구 통계
조회)을 올리고 검산이 `{011, 042}` 에서 `{011}` 을 남긴다.
**「열여덟째」가 닫았던 자리가 다시 열렸다.** 「열린 과제」의 「발화 4 가 안
갈린다」 항목이 말하던 바로 그 상태 — "청주시" 를 장소가 아니라 키워드로 읽는
것 — 로 돌아갔다.

**발화 9 는 셋 사이에서 못 고른다.** 대조군은 `{015}` 하나였는데 이번에는
`{015, 016, 017}` 로 되묻는다. 이름 형식을 넣은 것이 여기서는 반대로 작용한 것으로
보인다 — "충북 제1선거구" 는 **두 형식 어디에도 안 맞으므로**(「스무째」 물음 5)
015 도 016 도 배제할 근거가 안 되고, 대신 셋이 나란히 남는다. **다만 이것은
정황이지 확인한 것이 아니다.** LLM 의 reason 을 읽어 보지 않았다.

**발화 7 은 최종은 그대로인데 LLM 단독이 무너졌다.** 10/10 에서 0/10 이다.
선거와 아무 상관이 없는 발화다. `given` 축 선택지 셋을 고친 것이 프롬프트
전체에 들어가므로 상관없는 발화가 흔들린 것이다 — **「열한째」·「열여덟째」에
이어 세 번째 전례다.** 검산이 `{012, 032, 039, 044}` 에서 `{012}` 를 살려
최종 표에는 안 드러난다. 검산 표를 안 보면 못 보는 자리다.

**발화 6 은 안 갈렸다.** 이번 작업이 노린 자리인데 근접 9 가 근접 10 이 됐을
뿐이다. `{007, 008, 009}` 그대로다. 007 과 008 의 문장은 이제 이름 형식으로
확실히 다른데도 셋을 못 좁힌다. **문장으로는 안 갈린다는 것이 이번에 실측으로
남았다.** 「열아홉째」가 "menu 의 224자냐 번호표냐" 를 못 갈랐다고 적어 둔
자리인데, 문장을 크게 바꿔도 안 움직였으므로 **문장 쪽 후보가 약해졌다.**

#### 공약 셋을 왜 안 고쳤나

`search_assembly_pledge_districts` · `get_assembly_pledge_district` ·
`find_assembly_pledge_district_by_point` **셋은 「스무째」에서 안 눌렀다.**
누른 여덟은 선거 여섯(searchDistricts · searchAssemblyDistricts ·
getDistrict · getAssemblyDistrict · findDistrictByPoint ·
findAssemblyDistrictByPoint)과 충전소 둘이다. 공약 셋의 응답을 한 번도 안 봤으므로
이름 형식도 집계 값도 근거가 없다. **안 눌러본 도구의 문장은 안 고친다.**
발화 6 이 `{007, 008, 009}` 로 되묻는 그 009 가 공약 검색인데, 그것을 못 고친
것이 발화 6 이 안 갈린 이유의 하나일 수 있다 — **정황이다.**

#### 안 한 것

- **노드 이름(`name`)은 하나도 안 바꿨다.** menu 45문장의 앞토막이 통째로
  바뀌어 판정 전체가 흔들린다. `국회의원 전체 선거구 검색` 처럼 "전체" 가 남은
  이름이 셋 있다 — description 에서만 뺐다. 이름 바꾸기는 별건이다.
- **4층 답 문구는 안 봤다.** description 이 바뀌면 화면 문구도 바뀌는지는 안 쟀다.
- `demo/` · `tools/check_resolve.py` · `step_service.py` 를 안 건드렸다.
  GT 도 한 글자도 안 바꿨다.
- 발화 9 가 셋으로 갈린 이유와 발화 7 이 LLM 단독에서 무너진 이유를 **LLM 의
  reason 을 읽어서 확인하지 않았다.** 위의 설명은 축 표와 후보 표에서 읽은
  정황이다.
- `assembly_district_dataset_info` 의 description 에도 "전체 선거구" 가 남아
  있다. 여섯에 안 들어 있어 안 고쳤다.

#### 이 상태를 어떻게 할지는 사람이 정한다

셋 중 하나다. **어느 것도 이번에 안 골랐다.**

```
그대로 둔다      문장은 사실에 맞다. 판정 21점을 대가로 치른다
되돌린다         git checkout -- ontology workflows 로 81/90 이 돌아온다
갈라서 다시 잰다 선거 여섯만 고치고 시작 데이터 셋은 그대로 두면 발화 4 · 7 이
                 돌아오는지를 볼 수 있다. 발화 4 의 given 축이 근거다.
                 **이번에 안 쟀다** — 숫자를 맞추려고 문장을 다시 손보지
                 않는다는 규칙에 걸리는지가 애매해서 사람에게 넘긴다
```

### 2026-08-25 (스무째) · 선거 도구 여섯과 충전소 도구 둘을 눌러 봄 · 조사만

**왜 눌렀나.** 시작 데이터 노드 셋(`말한 장소` · `말한 키워드` · `말한 식별자`)이
사람에게도 LLM 에게도 안 갈린다. menu 45문장이 전부 이 이름으로 시작하고
프롬프트의 `given` 축 선택지도 이 셋이라 판정에 직접 걸린다. 갈라야 할지 합쳐야
할지를 정하려면 **도구가 실제로 무엇을 받는지**를 알아야 하는데, 지금까지는
저쪽 도구 설명서만 읽고 짐작하고 있었다.

**이번에는 근거만 만든다. 문장도 온톨로지도 안 고쳤다.** 아래는 사실이고
판단은 사람이 한다.

누른 창구는 `POST /api/tools/execute` 이고 `user_context` 는
`execute_service.USER_CONTEXT` 를 그대로 실었다. `includeGeometry` 를 받는
도구는 전부 `false` 로 불렀다. 200 + `{"error": …}` 는 하나도 없었다 —
0건은 전부 실제 0건이다. 누르는 스크립트는 `/tmp` 에 두고 저장소에 안 남겼다.

**여덟 다 눌렀다.** 여섯은 이번에 새로 눌렀고, `election.findDistrictByPoint` ·
`election.findAssemblyDistrictByPoint` 는 이미 같은 좌표로 눌러본 기록이 있어
그 파일을 근거로 썼다(아래 물음 3). `ev.getStation` 도 옛 기록 둘에 이번 하나를
더했다.

#### 물음 1 · 두 선거 데이터셋은 같은 선거구인가

```
                              전체(all=true)   query="청주"   sido 로 충북 전체
election.searchDistricts            254            4건            8건
election.searchAssemblyDistricts    254            5건            8건
```

**개수는 254 로 같은데 이름은 한 건도 안 겹친다.** 254 대 254 를 이름으로
맞대보면 교집합이 **0** 이다.

```
searchDistricts            "충북 청주서원" · "충북 충주" · "경기 군포" · "전북 군산김제부안갑"
searchAssemblyDistricts    "청주시 흥덕구" · "충주시" · "중구_영도구" · "보은군_옥천군_영동군_괴산군"
```

앞은 `시도약칭 + 선거구명` 이고 뒤는 `행정구역명` 이다. 뒤쪽은 254건에 고유
이름이 244개라 이름이 겹치는 것이 열 있다. **데이터셋 자체가 다르다.**

```
searchDistricts            datasetId kr-assembly-districts-2024
                           source    오마이뉴스 공개 GeoJSON (OhmyNews/2024_22_elec_map)
searchAssemblyDistricts    datasetId kr-assembly-election-districts-2024
                           source    election.shp (사용자 제공 GIS)
```

`query="청주"` 가 5건인 것도 눈여겨볼 자리다. 넷은 청주시 구들인데 다섯째가
`보은군_옥천군_영동군_괴산군` 이다. **query 가 공약 본문까지 훑는다**는 뜻이다.
앞쪽 도구는 선거구 이름만 훑어 4건이다.

#### 물음 2 · 무엇이 더 붙어 오는가 — 칸 이름 비교표

```
searchDistricts (items)      searchAssemblyDistricts (items)
─────────────────────────    ──────────────────────────────────
code                         code
name                         name
sido                         sido
district                     sidoCode
bbox                         mainSigCode
                             hasPledges
                             pledgeCount
                             pledgeCategories
                             pledgeCategoryKeys
                             winnerNames
                             partyNames
                             pledgePreview
                             bbox
```

```
GeoJSON properties
searchDistricts              SGG_Code · SIDO_SGG · SIDO · SGG
searchAssemblyDistricts      elec_cd · ctprvn_cd · ctprvn_nm · main_sig_c · est_nm
                             has_pledges · pledge_count · pledge_categories
                             (+ 응답에는 district_code · district_name · sido ·
                              pledge_category_keys · winner_names · party_names ·
                              pledge_preview · pledge_ids 도 함께 온다)
```

**집계는 0 이 아니라 실제 값이다.** 254건 전체를 세어 봤다.

```
공약이 있는 선거구   77 건        (dataset.pledgeDistrictCount 77 과 같다)
공약이 없는 선거구  177 건
공약 수 합계        476 건        (dataset.pledgeCount 476 과 같다)
분야 6종            철도 · 도로 · 대중교통 · 항공 · 선박 · 교통전체
```

당선인과 정당도 실물이 온다 — 예: 청주시 흥덕구 `winnerNames ["이연희"]` ·
`partyNames ["더불어민주당"]` · `pledgePreview ["월3만원 청년패스 대중교통 무제한"]`.

#### 물음 3 · 같은 지점에서 둘이 같은 답을 하는가

오송역 좌표 `lon=127.3277 · lat=36.62`. **이미 눌러본 기록이 있어 그것을 썼다**
(`election.findDistrictByPoint.좌표.json` · `election.findAssemblyDistrictByPoint.좌표.json`,
`query` 칸에 같은 좌표가 찍혀 있다). 둘 다 1건이다.

```
findDistrictByPoint            SGG_Code 2430202   "충북 청주흥덕"
findAssemblyDistrictByPoint    elec_cd  4311301   "청주시 흥덕구"
```

**같은 땅을 가리키는데 이름도 코드 체계도 다르다.** 앞은 선관위 SGG_Code 7자리,
뒤는 행정구역 코드에 01 을 붙인 elec_cd 다. 뒤쪽에는 `sido "충청북도"` ·
`mainSigCode "43113"` 처럼 행정구역 코드가 그대로 붙어 온다.

#### 물음 5 · "충북 제1선거구" 는 어디에 걸리는가

**아무 데도 안 걸린다.**

```
searchDistricts          query="충북 제1선거구"   0건
searchAssemblyDistricts  query="충북 제1선거구"   0건
getDistrict              name ="충북 제1선거구"   not_found  (2026-08-24 에 누른 것)
getAssemblyDistrict      name ="충북 제1선거구"   not_found  (2026-08-24 에 누른 것)
```

**"제1선거구" 라는 이름 형식이 두 데이터셋 어디에도 없다.** 실제 형식은
`충북 청주흥덕` 또는 `청주시 흥덕구` 다. 발화 9(충북 제1선거구 알려줘)가
무엇에 닿는지는 이것이 근거다 — 지금 그 발화의 인자를 그대로 넘기면 두 도구
모두 0건이다. (발화 9 의 적중 10/10 은 `recipe_015` 를 **고르는** 데까지의
판정이고, 실행해서 데이터가 오는지는 별개다.)

**sido 칸은 두 도구가 서로 반대다.** 스키마 설명이 엇갈린다고 적어 뒀던 것이
실물로 갈렸다.

```
                              sido="충북"    sido="충청북도"
searchDistricts                  8건             0건        ← 약칭만 받는다
searchAssemblyDistricts          0건             8건        ← 정식명만 받는다
```

두 도구에 같은 값을 넘기면 한쪽은 반드시 0건이다.

#### 물음 4 · 6 · 7 — 「시작 데이터 노드 셋을 갈라 둘 것인가」

셋이 한 물음에 답하므로 한자리에 모은다.

**물음 4 · `name` 은 부분 이름도 받는가 — 받는다. 그것도 조용히.**

```
election.getDistrict
  name="충북 청주서원"   찾음                                  (2026-08-24)
  name="충북"            찾음.  count 1 · totalMatches 8       ← 부분 이름이 걸린다
                         돌려준 것은 "충북 청주서원" 하나
  name="충북 제1선거구"  not_found                             (2026-08-24)

election.getAssemblyDistrict
  name="청주시 상당구"   찾음.  count 1 · totalMatches 1        (2026-08-24)
  name="충북"            찾음.  count 1 · totalMatches 2       ← 부분 이름이 걸린다
                         돌려준 것은 "충주시" 다. 이름에 "충북" 이 없다 —
                         공약 본문("충북선 고속화 조기 완공")에 걸린 것으로 보인다
  name="충북 제1선거구"  not_found                             (2026-08-24)
```

**`totalMatches` 가 여럿인데 `count` 는 1 이다.** 여덟 중 하나를 골라 주면서
나머지 일곱을 안 알린다. 어느 기준으로 고르는지는 응답에 안 적혀 있다 —
**안 알아봤다.**

즉 `getDistrict.name` 의 정확도 요구는 `searchDistricts.query` 와 다르지
않다. 다른 것은 **여러 건일 때의 처신**이다 — search 는 다 주고 get 은 하나만
준다. 다만 `name="충북"` 이 8건 중 하나를 조용히 집는 것은 `query="충북"` 이
8건을 다 주는 것과 사람이 겪는 것이 다르다.

**물음 6 · `ev.getStation` 이 이름으로도 받는가 — 안 받는다.**

```
statId="ev_station_PL033780"   item null · count 0     (옛 기록)
statId="PL033780"              찾음. 이름 "포빌"        (옛 기록)
statId="포빌"                  item null · chargers [] · count 0   ← 이번
```

`error` 는 없다. 200 + 0건이다. **번호 전용이 실물로 확정됐다.**

**물음 7 · `ev.searchStations` 에 번호를 넣으면 — 0건이다.**

```
query="PL033780"   count 0 · totalMatches 0    ← 이번
query="청주"       count 500 · totalMatches 1,880   (옛 기록)
```

`query` 는 충전소명·주소·운영기관을 훑고 `stationId` 는 안 훑는다.

세 사실을 나란히 두면 이렇다.

```
name 이 부분 이름을 받는가          받는다. 둘 다. 여럿이어도 하나만 조용히 준다
getStation 이 이름을 받는가         안 받는다. "포빌" 은 0건
searchStations 가 번호를 받는가     안 받는다. "PL033780" 은 0건
```

**결론은 안 낸다.** 다만 표가 이렇게 갈린다는 것만 적는다 — 선거 쪽은
`get` 과 `search` 의 정확도 요구가 사실상 같고, 충전소 쪽은 `getStation`(번호
전용)과 `searchStations`(이름 전용)이 서로의 입력을 전혀 못 받는다.
`recipe_018`(말한 식별자 → 충전소 상세)을 뗄지 말지의 근거는 물음 6 이다.

#### 답이 안 나온 것

- `getDistrict.name` 이 `totalMatches` 8 중 **무엇을 기준으로 하나를 고르는지**.
  응답에 안 적혀 있고 저쪽 코드를 안 읽었다. **안 알아봤다.**
- `getAssemblyDistrict.name="충북"` 이 "충주시" 를 문 것이 공약 본문 때문인지는
  **정황이다.** 그 응답의 `pledgePreview` 에 "충북선 고속화 조기 완공" 이 있는
  것을 보고 적은 것이지, 저쪽이 어느 칸을 훑는지 확인한 것은 아니다.
- `searchAssemblyDistricts` 의 `sido` 가 **코드("43")도 받는지**. 스키마에는
  "광역시도명 또는 코드" 라고 적혀 있는데 **안 눌러봤다.**

#### 응답 전문

`tools/probe_out/` (`.gitignore` 라 커밋 안 된다). 이번에 남긴 것 열넷 —

```
election.searchDistricts.all-2026-08-25.json
election.searchAssemblyDistricts.all-2026-08-25.json
election.getDistrict.name-앞두글자충북-2026-08-25.json
election.getAssemblyDistrict.name-앞두글자충북-2026-08-25.json
election.searchDistricts.query-충북제1선거구-2026-08-25.json
election.searchDistricts.query-충북-2026-08-25.json
election.searchDistricts.query-청주-2026-08-25.json
election.searchDistricts.sido-충북-2026-08-25.json
election.searchDistricts.sido-충청북도-2026-08-25.json
election.searchAssemblyDistricts.sido-충북-2026-08-25.json
election.searchAssemblyDistricts.sido-충청북도-2026-08-25.json
election.searchAssemblyDistricts.query-충북제1선거구-2026-08-25.json
ev.getStation.statId-포빌-2026-08-25.json
ev.searchStations.query-PL033780-2026-08-25.json
```

근거로 쓴 옛 파일 여섯 —

```
election.findDistrictByPoint.좌표.json
election.findAssemblyDistrictByPoint.좌표.json
election.searchDistricts.청주.json
election.searchAssemblyDistricts.청주.json
election.getDistrict.name-실제이름.json · name-발화.json
election.getAssemblyDistrict.name-실제이름.json · name-발화.json
ev.getStation.statId-stationId.json · statId-id.json
ev.searchStations.청주.json
```

**geometry 를 잘라 낸 것은 하나도 없다.** `includeGeometry=false` 로 부른 덕에
제일 큰 것이 100KB(searchAssemblyDistricts.all)라 전부 원문 그대로 남겼다.

**「열린 과제」의 「배선을 적은 도구 넷을 아직 안 눌러봤다」 는 이번에 안 움직였다.**
그 넷(`rail.getSectionGeometry` · `geo.getRailwayLines` ·
`vworld.getAdministrativeBoundaries` · `population.searchStatistics`)에 이번에
누른 여덟이 하나도 안 겹친다.

### 2026-08-24 (열아홉째) · 옛 코드와 새 코드를 같은 ollama 위에서 번갈아 잼

**왜 쟀나.** 「열여덟째」의 「★ 교락」 절이 남긴 숙제다. 발화 6(국회의원 선거구
찾아줘)이 적중 10/10 에서 근접 8~9/10 으로 떨어졌는데, 그 측정은 온톨로지
변경(`말한 식별자 is-a 행정구역 코드` 떼기)과 uvicorn 재시작이 **한 번에**
일어난 뒤의 값이었다. uvicorn 재시작은 「열넷째」가 남긴 세션 간 차이의 미해결
후보 셋 중 하나다. 둘이 교락돼 원인을 못 갈랐다.

**어떻게 갈랐나.** `git worktree` 로 952c067(옛 코드 · recipe 48)을 따로 펼치고,
`.env` 를 복사하고, 본 트리의 `.venv` 를 절대경로로 써서 옛 코드와 새 코드
(e80f453 · recipe 45)를 **같은 ollama 프로세스 위에서 번갈아** 쟀다.

    A1 옛(952c067) → B1 새(e80f453) → A2 옛 → B2 새

번갈아 간 것은 시간 흐름 자체가 원인일 가능성을 함께 보기 위해서다. 한 번씩만
재면 "옛 다음에 새" 라는 순서와 코드 차이가 또 교락된다.

**고정한 것.** ollama 는 손대지 않았다 — 재시작도 모델 내리기도 안 했다.
`ollama serve` pid 72585 · qwen3:32b(030ee887880f · 28GB · 100% GPU · ctx 32768)
가 네 묶음 내내 같은 프로세스였다. 그래서 후보 셋 중 둘(ollama 프로세스 ·
ollama 버전)이 고정됐고, 남은 것은 코드와 uvicorn 프로세스뿐이다.
묶음마다 `pkill` 로 서버를 내리고 **그 트리에서** 다시 띄웠으므로 uvicorn 은
네 번 다 새 프로세스다. 옛 트리는 recipe 번호도 GT 도 옛것이라 반드시 그 트리의
`tools/check_resolve.py` 로 쟀다.

각 묶음 발화 9개 × 10회.

#### A1 · 옛 코드 952c067 (recipe 48)

```
#  발화                                      LLM 단독   적중     근접    빗나감   못 붙음   틀렸을 때 나온 것
1  오송역 위치 보여줘                        10/10      10/10    0       0        0
2  오송역 좌표 알려줘                        10/10      10/10    0       0        0
3  오송역 CCTV 보여줘                        10/10      10/10    0       0        0
4  청주시 인구 구성 알려줘                   0/10       0/10     0       10       0   {011}  빗나감 SELECT 10회
5  오송역 근처 충전소 찾아줘                 0/10       10/10    0       0        0
6  국회의원 선거구 찾아줘                    1/10       10/10    0       0        0
7  전기차 충전소 데이터 검색해줘             10/10      10/10    0       0        0
8  철도 안전 문서 찾아줘                     10/10      10/10    0       0        0
9  충북 제1선거구 알려줘                     10/10      10/10    0       0        0
                                            61/90      80/90    0       10       0     89%
                                            68% ← LLM 단독
```

#### B1 · 새 코드 e80f453 (recipe 45)

```
#  발화                                      LLM 단독   적중     근접    빗나감   못 붙음   틀렸을 때 나온 것
1  오송역 위치 보여줘                        10/10      10/10    0       0        0
2  오송역 좌표 알려줘                        10/10      10/10    0       0        0
3  오송역 CCTV 보여줘                        10/10      10/10    0       0        0
4  청주시 인구 구성 알려줘                   0/10       10/10    0       0        0
5  오송역 근처 충전소 찾아줘                 0/10       10/10    0       0        0
6  국회의원 선거구 찾아줘                    1/10       1/10     9       0        0   {007,008,009} 근접 CLARIFY 9회
7  전기차 충전소 데이터 검색해줘             10/10      10/10    0       0        0
8  철도 안전 문서 찾아줘                     10/10      10/10    0       0        0
9  충북 제1선거구 알려줘                     10/10      10/10    0       0        0
                                            61/90      81/90    9       0        0     90%
                                            68% ← LLM 단독
```

#### A2 · 옛 코드 952c067 (다시)

```
#  발화                                      LLM 단독   적중     근접    빗나감   못 붙음   틀렸을 때 나온 것
1  오송역 위치 보여줘                        10/10      10/10    0       0        0
2  오송역 좌표 알려줘                        10/10      10/10    0       0        0
3  오송역 CCTV 보여줘                        10/10      10/10    0       0        0
4  청주시 인구 구성 알려줘                   0/10       0/10     0       10       0   {011}  빗나감 SELECT 10회
5  오송역 근처 충전소 찾아줘                 0/10       10/10    0       0        0
6  국회의원 선거구 찾아줘                    1/10       10/10    0       0        0
7  전기차 충전소 데이터 검색해줘             10/10      10/10    0       0        0
8  철도 안전 문서 찾아줘                     10/10      10/10    0       0        0
9  충북 제1선거구 알려줘                     10/10      10/10    0       0        0
                                            61/90      80/90    0       10       0     89%
                                            68% ← LLM 단독
```

#### B2 · 새 코드 e80f453 (다시)

```
#  발화                                      LLM 단독   적중     근접    빗나감   못 붙음   틀렸을 때 나온 것
1  오송역 위치 보여줘                        10/10      10/10    0       0        0
2  오송역 좌표 알려줘                        10/10      10/10    0       0        0
3  오송역 CCTV 보여줘                        10/10      10/10    0       0        0
4  청주시 인구 구성 알려줘                   0/10       10/10    0       0        0
5  오송역 근처 충전소 찾아줘                 0/10       10/10    0       0        0
6  국회의원 선거구 찾아줘                    1/10       1/10     9       0        0   {007,008,009} 근접 CLARIFY 9회
7  전기차 충전소 데이터 검색해줘             10/10      10/10    0       0        0
8  철도 안전 문서 찾아줘                     10/10      10/10    0       0        0
9  충북 제1선거구 알려줘                     10/10      10/10    0       0        0
                                            61/90      81/90    9       0        0     90%
                                            68% ← LLM 단독
```

**A1 과 A2 는 출력 파일이 `diff` 로 완전히 같다. B1 과 B2 도 그렇다.**
적중 표만이 아니라 축 표 · 후보 표 · 검산 표까지 한 글자도 안 다르다.
uvicorn 은 그 사이 세 번 갈렸는데도 그렇다.

#### 발화 6 — 코드로 갈렸다

```
             적중     근접    LLM 후보 수    조회 후보 수   status
A1 · A2 옛   10/10    0       9              4              SELECT   10회
B1 · B2 새   1/10     9       3              4              CLARIFY   9회
```

옛 코드에서 LLM 은 `{007, 015, 027, 028, 029, 030, 031, 032, 034}` 아홉을 쓰고
검산이 `{007}` 하나로 살렸다(「검산이 살렸다」 9회). 새 코드에서 LLM 은
`{007, 008, 009}` 셋만 쓰는데 조회 후보가 `{007, 008, 009, 010}` 넷이라
교집합이 셋으로 남고 CLARIFY 가 된다.

**LLM 이 더 좁게 답하는데 결과는 더 나빠졌다.** 옛 코드는 LLM 이 넓게 틀리고
검산이 좁혀서 맞았고, 새 코드는 LLM 이 좁게 맞는 쪽으로 왔지만 검산이 더
좁힐 것이 없어졌다. 「LLM 단독」 칸은 양쪽 다 1/10 로 같다 — 1단계는 안 움직였고
움직인 것은 **1단계가 낸 후보 집합의 넓이**다.

#### 발화 4 — 두 번째 눈. 세션은 안 움직였다

```
A1 · A2 옛   적중 0/10 · 빗나감 10 · {011} · SELECT
B1 · B2 새   적중 10/10
```

옛 코드에서 발화 4 가 **빗나감 10 그대로** 나왔다. 「열여섯째」·「열일곱째」의
기준선과 같다. 세션 전체가 움직인 것이 아니다. 이것이 아래 판정보다 먼저다.

#### 판정 — 미리 정해 둔 넷 중 첫째다

측정 전에 네 갈래를 적어 두고 대조했다.

```
A1 · A2 에서 발화 6 이 적중 10/10   → menu 탓이다. 코드 변경이 원인이다   ← 이것
A1 · A2 에서도 발화 6 이 근접       → uvicorn · 세션 탓. 코드는 무죄
A1 과 A2 가 서로 다르다             → 세션 안에서도 안 정해진다
발화 4 가 옛 코드에서 빗나감 10 아님 → 세션 전체가 움직였다
```

**발화 6 이 근접으로 떨어진 것은 코드 변경 탓이다. uvicorn 재시작은 아니다.**
uvicorn 은 네 묶음에서 네 번 새로 떴는데 같은 코드끼리는 값이 완전히 같았다.

#### 「열여덟째」의 「★ 교락」 은 풀렸다

「열여덟째」는 "menu 탓이라고 단정할 수 없다 · 후보는 둘(menu 번호표 ·
uvicorn 재시작)" 이라고 적고 멈췄다. **그 소거법은 틀리지 않았다. 남겨 둔
후보 둘 중 uvicorn 쪽이 이번에 지워졌다.** 「열여덟째」의 「발화 6 의 후보
집합은 안 움직였다」 절이 짚은 대로 바뀐 것은 검산 층이 아니라 LLM 한 번의
입력, 즉 menu 다.

**다만 「menu 의 무엇이」 까지는 아직 안 갈렸다.** menu 에서 바뀐 것은 224자
문장과 018 이후 번호표가 셋씩 밀린 것 둘이다. 이번 측정은 코드 전체를 한 덩이로
갈랐을 뿐이라 그 둘은 여전히 서로 교락돼 있다. 가르려면 번호표만 옛것으로
되돌린 세 번째 트리가 필요하다. **안 쟀다.**

#### 곁다리로 얻은 것 — 「열넷째」의 "한 세션 안은 바이트 단위로 같다" 는 안 깨졌다

uvicorn 을 네 번 새로 띄웠는데도 같은 코드끼리는 출력이 완전히 같았다.
**세션 간 차이의 미해결 후보 셋 중 uvicorn 재시작은 이제 후보에서 빠진다.**
남은 둘은 ollama 프로세스 재시작 · ollama 버전(0.32.14 → 0.32.15)이다.
이번에는 둘 다 일부러 고정했으므로 이 측정이 그 둘에 대해 말해 주는 것은 없다.

### 2026-08-24 (열여덟째) · `말한 식별자 is-a 행정구역 코드` 를 뗌 · recipe 48 → 45

**왜 뗐나.** 사람은 "43113" 이라고 말하지 않는다. 그런데 이 `is-a` 한 줄이
발화가 곧 행정구역 코드인 경로 셋(recipe_018 · 019 · 020)을 만들었고, 그 탓에
`(get_age_profile, 행정구역 코드)` 한 줄이 **발화 자리와 앞 단계 자리를 둘 다**
맡아야 했다. 발화 자리에는 `level` 을 넣을 수가 없어서(「열일곱째」) 앞 단계
자리까지 함께 막혀 있었다. 「열린 과제」에 물음으로 남아 있던 것이고 이번에
떼기로 정했다.

한 줄을 떼자 발화 자리가 사라지고 남은 한 자리를 `$prev` 로 적을 수 있다.
`get_age_profile` · `get_population_trend` 둘 다 채웠다. 대가는 recipe 번호가
밀리는 것이고 GT 를 옮겼다.

#### recipe 대응표

`tools/rebuild_init.py` 미리보기가 낸 표다. 001 ~ 017 은 그대로이고
018 · 019 · 020 이 사라지며 021 이후가 **정확히 셋씩** 당겨진다.

```
옛 018  말한 식별자 → 지방선거 교통 공약 상세     사라짐
옛 019  말한 식별자 → 연령별 인구 구성 조회       사라짐
옛 020  말한 식별자 → 인구 변화 추이 조회         사라짐

옛 021 → 새 018   …   옛 048 → 새 045
합계    48개 → 45개
```

GT 가 걸린 자리는 셋이다. **기대값은 한 글자도 안 바꿨다. 같은 노드 사슬의 새
번호로만 옮겼다.**

```
발화                          옛          새          사슬
3  오송역 CCTV 보여줘         recipe_025  recipe_022  말한 장소 → 좌표 → CCTV
4  청주시 인구 구성 알려줘    recipe_045  recipe_042  … → 행정구역 판별 → 연령별 인구
5  오송역 근처 충전소 찾아줘  recipe_035  recipe_032  말한 장소 → 좌표 → 충전소 검색

나머지 여섯(001 · 007 · 012 · 014 · 015)은 017 이하라 그대로다.
```

#### check_wiring 전후

```
     recipe   배선 완비   STEP_OF   A   B   C
전   48개     43개        35줄      0   0   3
후   45개     44개        37줄      0   0   1
```

C 에 남은 하나는 `web_fetch × 웹 주소`다. 이유는 그대로다 — `web.search` 가
권한에 막혀 응답 모양을 한 번도 못 봤다.

배선 두 줄은 이렇게 적었다.

```
("get_age_profile", "admin_code")       {level: $prev.items.1.layerId,
                                         code:  $prev.items.1.code}
("get_population_trend", "admin_code")  같음 (ADMIN_LEVEL_FROM_PREVIOUS 한 상수)
```

근거는 「열일곱째」의 실측이다. `findBoundaryByPoint` 가 여덟 지점에서 늘
`items` 3건을 `sido -> sigungu -> emd` 순서로 내놓고, `items.N.layerId` 의
낱말이 `population` 두 도구의 `level` enum 과 글자까지 같다
(`ASAP-mcp/main.py:402`). 응답 전문은 `tools/probe_out/` 에 있다.

`get_local_pledge_summary × 행정구역 코드` 에서는 `input_first` 를 **뺐다.**
그 가지는 옛 recipe_018(말한 식별자 → 공약 요약) 하나를 위한 것이었고 그
경로가 사라졌다. `$prev` 쪽(`items.0.code`)은 그대로다 — 옛 044 가 새 041 로
살아 있다.

#### pytest

`175 passed / 1 failed`. 실패 1건은
`test_dense_graph_would_move_if_overlap_removal_were_used` 로 graphviz 음성
대조군이다. 이번 변경과 무관하고 예전부터 그대로다.

#### 세 묶음 판정 — ★ 움직였다

`python tools/check_resolve.py --runs 10` 을 세 번 연달아. qwen3:32b.

```
                 LLM 단독   최종     근접   빗나감   못 붙음
묶음 1           62/90      82/90    8      0        0
묶음 2           61/90      81/90    9      0        0
묶음 3           61/90      81/90    9      0        0
                 69/68/68%  91/90/90%

기준선(열일곱째) 61/90      80/90    0      10       0
                 68%        89%
```

**「빗나감」이 10 에서 0 이 됐다.** 점수 1~2점보다 이것이 크다 — 이 저장소는
「틀린 답보다 정직한 되물음이 낫다」로 읽는다.

발화별로는 둘만 움직였다. 나머지 일곱은 세 묶음 모두 기준선과 같다.

```
발화                          기준선                 이번
4  청주시 인구 구성 알려줘    적중 0/10 · 빗나감 10  적중 10/10  (세 묶음 모두)
6  국회의원 선거구 찾아줘     적중 10/10             적중 2/10 · 1/10 · 1/10
                                                    근접 8 · 9 · 9 (CLARIFY)
```

**발화 4 는 검산이 살렸다.** LLM 단독은 여전히 0/10 이다. 예전에는
`{011, 019}` 를 내고 검산이 `{011}` 로 굳혔는데(「열다섯째」), 이제
`{011, 042}` 를 내고 검산이 `{042}` 만 남긴다. 옛 019 는 말한 식별자로 시작하던
인구 recipe 이고 그것이 사라졌다. **후보에서 가짜 하나가 빠지자 검산이 정답을
고를 수 있게 됐다.** 「열린 과제」에 오래 있던 "발화 4 가 안 갈린다" 가 여기서
움직였다.

**발화 6 은 확신이 되물음으로 바뀌었다.** `{007, 008, 009}` 로 CLARIFY 한다.
정답 007 이 후보 안에 있으므로 「근접」이고 사람이 고르면 닿는다. 「빗나감」이
아니다.

#### 왜 6번이 움직였나 — 아직 모른다

```
menu.yaml                             3741자 → 3517자   (224자 짧아짐)
menu 문장                             48개 → 45개
앞 30자를 다른 문장과 나눠 갖는 문장   15개 → 15개       (안 움직였다)
```

사라진 셋(옛 018 · 019 · 020)은 선거 검색 넷(007 ~ 010)과 상관이 없다. 겹치는
앞토막 수도 그대로다. **남는 것은 프롬프트가 224자 짧아진 것뿐이고 그것이 왜
6번을 움직이는지는 설명이 안 된다.** 「about 선택지 5→4 가 10점을 움직였다」와
같은 자리다 — 프롬프트를 건드리면 상관없어 보이는 발화가 흔들린다.

**되돌리지 않았다.** 빗나감 10 이 0 이 된 것과 맞바꾼 것이고 그쪽이 낫다.
다만 이것은 **미리 정해둔 값(최종 80/90 · LLM 단독 61/90)을 벗어난 결과**다.
숫자를 맞추려고 GT 도 판정 규칙도 고치지 않았다. 표 그대로 남긴다.

#### 안 한 것

- **코드 자릿수로 `level` 을 정하기.** 2 자리면 `sido` · 5 자리면 `sigungu` ·
  8 자리면 `emd` 다(2026-08-24 실측). 발화로 코드를 대는 경로가 없어져
  **동기가 사라졌다.** 급하지 않으므로 「열린 과제」에 줄여 남겼다.
- **`items` 셋 중 무엇을 집을지 가르기.** `items.1`(시군구)로 박았다. 발화가
  시도를 말해도 시군구가 나온다. 「열린 과제」에 새 항목으로 남겼다.
- **`demo/ui/components/sample_picker.py`.** `"오송역 CCTV 보여줘"` 를
  `recipe_002` 로 적어 두었는데 지금 002 는 「말한 장소 → 철도 구간 형상 조회」다.
  CCTV 사슬은 새 022 다. **이번 변경 전부터 어긋나 있었다** — 변경 전에도 그
  사슬은 025 였다. 화면 시연의 기대값이라 사람이 정할 일이고 안 고쳤다.

#### 다시 시도하지 말 것

- **`get_age_profile` 의 발화 자리를 다시 만들기.** `말한 식별자 is-a 행정구역
  코드` 를 되돌리면 recipe 번호가 다시 밀리고 `level` 을 못 적는 자리도 함께
  돌아온다. 「열일곱째」에 `$prev` 쪽만 적어 봤다가 A 가 0 에서 2 로 늘어
  되돌린 기록이 있다.
- **6번이 나빠졌다고 menu 문장을 손보기.** 앞 30자 겹침은 안 움직였다(15 → 15).
  문장이 원인이라는 근거가 없다. 문장을 고치면 다른 여덟이 함께 흔들린다 —
  「아홉째」에서 앞토막을 57자에서 31자로 줄였을 때 발화 4 는 안 움직였다.

#### ★ 교락 — uvicorn 재시작이 함께 일어났다

**위 「왜 6번이 움직였나」의 소거법은 후보를 하나 빼먹었다.** 지우지 않고
여기에 덧붙인다.

이번 세 묶음은 `pkill -f "demo.api.main"` 로 서버를 내리고 새로 띄운 uvicorn
에서 쟀다. 코드가 바뀌었으니 다시 띄워야 했다. 그런데 기준선 80/90
(「열일곱째」)은 **그 이전 프로세스**에서 잰 값이다.

**uvicorn 재시작은 「열넷째」가 남긴 세션 간 차이의 미해결 후보 셋 중 하나다**
(나머지 둘은 ollama 프로세스 재시작 · ollama 버전 0.32.14 -> 0.32.15).
이번 변경과 프로세스 교체가 한 번에 일어났으므로 발화 6 이 움직인 것을
**menu 탓이라고 단정할 수 없다.**

**가르는 법. 아직 안 쟀다.**

```
git worktree 로 952c067 을 따로 펼친다
ollama 는 건드리지 않는다 — 그래야 후보 셋 중 둘이 고정된다
옛 코드로 uvicorn 을 띄워 한 묶음
그 자리에서 새 코드로 다시 띄워 한 묶음

옛 코드에서도 6번이 근접이면   세션 · uvicorn 탓이다
옛 코드가 10/10 이면            menu 탓이다
```

#### 발화 6 의 후보 집합은 안 움직였다

지금 트리에서 직접 쟀다.

```
candidates("spoken_keyword", "item_list", "group_election")
  -> ['recipe_007', 'recipe_008', 'recipe_009', 'recipe_010']
```

사라진 셋(옛 018 · 019 · 020)은 **첫 노드가 전부 `말한 식별자`다.** 발화 6 의
`given` 은 `말한 키워드`이므로 그 셋은 이 후보 집합에 애초에 못 든다.
007 ~ 010 은 017 이하라 번호도 안 밀렸다.

**그래서 바뀐 것은 검산 층이 아니라 LLM 한 번의 입력, 즉 menu 뿐이다.**
그리고 menu 에서 바뀐 것은 224자만이 아니다 — **옛 018 이후 문장에 붙은
번호표가 전부 바뀌었다.** 위 절이 길이만 적어 둔 것을 여기서 넓힌다.
LLM 이 쓰는 것은 `recipe_id` 이고 그 이름표가 셋씩 밀렸다.

**다만 이것이 6번을 움직였다는 증거는 아니다.** 위 교락이 풀리기 전까지 후보는
둘이다 — menu 의 번호표와 uvicorn 재시작.

**정정 (2026-08-24 「열아홉째」). 교락이 풀렸다.** 옛 코드(952c067)를 worktree 로
펼쳐 같은 ollama 위에서 새 코드와 번갈아 네 묶음을 쟀다. 옛 코드는 두 번 다
발화 6 이 적중 10/10 이었고 새 코드는 두 번 다 근접 9 였다. uvicorn 은 네 번
새로 떴는데도 같은 코드끼리는 출력이 완전히 같았다. **uvicorn 재시작은
후보에서 빠지고 코드 변경이 원인으로 남는다.** 위 소거법은 틀리지 않았다 —
남겨 둔 후보 둘 중 하나가 지워진 것이다. 다만 「menu 의 224자냐 번호표냐」는
여전히 안 갈렸다. 「열아홉째」를 본다.

### 2026-08-24 (열일곱째) · 데이터가 들어온 뒤 C 부류를 다시 봄 · recipe 48

**왜 다시 봤나.** 저쪽 PostGIS 에 데이터가 들어왔다
(`admin_boundary.areas` 5,335 · `population.resident_counts` 18,623 ·
`ev.stations` 92,863 · `ev.chargers` 488,162 · `rail.sections` 2,243 · knowledge 4건).
`check_wiring` 의 C 넷 중 셋이 「응답 모양을 못 봐서」 비어 있었다.
이제 응답을 볼 수 있으니 못 채우는 것이 정말 못 채우는 것인지 다시 본다.

#### 결론 한 줄

**하나를 채웠다. C 4 → 3.** `get_local_pledge_summary × 행정구역 코드` 다.
`get_age_profile` · `get_population_trend` 은 **여전히 못 채운다.** 막는 것이
바뀌었다 — 응답 모양은 이제 알고, `level` 이 발화에 없다는 것이 남았다.

#### 데이터가 들어와도 판정은 한 점도 안 움직였다

```
                LLM 단독   최종
묶음 1          61/90      80/90
묶음 2          61/90      80/90
묶음 3          61/90      80/90
                68%        89%
```

「열여섯째」와 같다. 발화 4 만 0/10 이고 빗나감 10 인 것까지 같다.
**판정과 데이터가 독립이라는 근거다.** 도구가 0건을 답하든 실제 값을 답하든
recipe 를 고르는 일은 그 앞에서 끝난다. 기준선이 그만큼 단단해졌다.

#### findBoundaryByPoint 의 items 는 순서가 안정적이다

여덟 지점을 눌렀다. 응답 전문은 `tools/probe_out/*-2026-08-24.json` 에 있다.

```
지점                    count  items[0]        items[1]              items[2]
오송역 127.33 36.62     3      sido 43         sigungu 43113         emd 43113250
강남역 127.03 37.50     3      sido 11         sigungu 11650         emd 11650108
부산   129.08 35.18     3      sido 26         sigungu 26470         emd 26470102
제주시 126.53 33.50     3      sido 50         sigungu 50110         emd 50110104
금산군 127.50 36.00     3      sido 44         sigungu 44710         emd 44710350
세종시 127.29 36.48     3      sido 36         sigungu 36110         emd 36110103
서해바다 125.00 34.00   0      items []
동해바다 131.00 37.00   0      items []
```

**`layerId` 가 늘 `sido` -> `sigungu` -> `emd` 순서다.** 육지 여섯 지점이 전부
3건이고 밀리지 않는다. `$prev.items.N.code` 로 적어도 된다.

**세종시도 안 밀린다.** 시군구가 없는 곳인데 `sigungu` 자리를
`36110 세종특별자치시` 로 채워 세 칸을 유지한다. 걱정한 자리가 아니었다.

**0건일 때는 조용하다.** `items` 가 `[]` 이고 `count` 0 이다. `warning` 도
`message` 도 없다. 바다를 찍으면 배선이 `null` 을 넘기게 된다 — 지금은 실제
발화가 지명에서 오므로 안 걸리지만 적어 둔다.

「빈 곳」으로 고른 127.50 36.00 은 빈 곳이 아니라 충남 금산군 남일면이었다.
0건을 보려면 바다로 나가야 했다.

#### `level` 은 `layerId` 와 같은 낱말이다

`population.getAgeProfile` · `population.getTrend` 의 `inputSchema` 가
`"level": {"enum": ["sido", "sigungu", "emd"]}` 다 (`ASAP-mcp/main.py:402`).
`findBoundaryByPoint` 의 `layerId` 와 **글자까지 같다.** 매핑 표가 필요 없다.

셋 다 눌렀고 셋 다 실제 값을 답한다.

```
{level: sigungu, code: 43113}     청주시 흥덕구  292,625명  boundaryMatch exact
{level: sido,    code: 43}        충청북도     1,600,787명  boundaryMatch exact
{level: emd,     code: 43113250}  오송읍         49,405명  boundaryMatch exact
getTrend {level: sigungu, code: 43113}  points 1건 (2026-06-30)
```

#### 채운 배선 하나 — 한 줄이 두 자리를 맡는다

`get_local_pledge_summary × 행정구역 코드` 를 `input_first` 로 갈라 적었다.

```
recipe_018  말한 식별자 -> 공약 요약                       input_first {sidoName: @arg}
recipe_044  말한 장소 -> 좌표 -> 행정구역 판별 -> 공약 요약  input      {sidoCode: $prev.items.0.code}
```

`election.getLocalPledgeSummary` 는 `sidoCode` 와 `sidoName` 을 둘 다 갖는다
("시도 코드 또는 이름으로 조회", `main.py:791`). 양쪽을 다 눌렀고 둘 다
`query` 로 되받는다. `status` 가 `not_found` 인 것은 **저쪽 데이터가 미적재**
여서다 (`available` false · `featureCount` 0). 인자는 파싱됐다.

recipe_044 를 끝까지 실행해 봤다. 세 단계가 다 돌았다.

```
s1 geo.geocode                        {query: "오송역"}
s2 adminBoundary.findBoundaryByPoint  {lon: 127.3276666, lat: 36.6199528}
s3 election.getLocalPledgeSummary     {sidoCode: "43"}      <- $s2.items.0.code 가 풀렸다
```

**`$prev.items.0.code` 가 실제로 "43" 으로 풀린다.** 짐작이 아니다.

#### 못 채운 배선 둘 — 발화 쪽 자리에 `level` 을 넣을 수가 없다

`get_age_profile` · `get_population_trend` 은 **앞 단계 쪽은 이제 적을 수 있다.**
`{level: $prev.items.1.layerId, code: $prev.items.1.code}` 면 된다.
막는 것은 반대쪽이다. recipe 019 · 020 은 말한 식별자가 곧 첫 step 이라
`$prev` 가 없는데, `level` 이 required 이고 발화에 없다.
menu 가 recipe_019 를 "행정구역 코드와 기준월로 본다" 라고 적는다.

눌러서 확인했다.

```
{code: "43113"}                 {"error": "level과 code가 필요합니다."}
{level: "", code: "43113"}      같은 오류
{level: "sigungu", code: "43"}  {"error": "시군구 코드는 최소 5자리여야 합니다"}
```

`level` 을 한 낱말로 박으면 자릿수가 다른 코드에서 반드시 틀린다. 빼면 오류다.
발화에 없는 값이라 `@arg` 로도 못 받는다. **셋 다 막힌다.**

시키는 대로 `$prev` 쪽만 적어 보고 세어 봤다.

```
$prev 만 적음   A 0 -> 2  (recipe 019 · 020)   되돌림
```

**되돌렸다.** 세는 규칙은 안 고쳤다.

#### 채우려면 TOOL_OF 도 함께 있어야 한다

C 부류 넷은 `STEP_OF` 뿐 아니라 **`TOOL_OF` 에도 줄이 없었다.** 그래서 C 표의
「도구」 칸이 비어 있었던 것이다. `STEP_OF` 만 채우면 `check_wiring` 이
`KeyError: 'get_age_profile'` 로 죽는다. 배선 한 자리는 표 두 줄이다.
채운 하나만 `TOOL_OF` 에 넣었고, 되돌린 둘은 `TOOL_OF` 도 함께 되돌렸다 —
부를 일이 없는 노드에 도구 이름만 남기지 않는다.

#### 구조적 문제 — 행정구역 코드를 건네는 자리가 둘이다

`(노드 × 받는 타입)` 이 키인데 `행정구역 코드` 를 건네는 앞 노드가 둘이다.

```
말한 식별자              is-a      행정구역 코드     recipe 018 · 019 · 020
지점 행정구역 판별       hasOutput 행정구역 코드     recipe 044 · 045 · 046
```

**한 줄이 두 자리를 다 맡아야 한다.** 가르는 수단은 `input_first` 하나뿐이고
그것은 「첫 step 인가」로만 가른다. 마침 두 자리가 정확히 그렇게 갈려서
`get_local_pledge_summary` 는 풀렸다. 두 자리를 다 적을 수 있을 때만 풀린다 —
`get_age_profile` 은 한 자리를 못 적어서 못 푼다.

#### 관문

```
check_wiring  A 0 · B 0 · C 4 -> 3   배선이 다 있는 recipe 41 -> 43 · STEP_OF 34 -> 35줄
pytest        175 passed / 1 failed  (graphviz 버전 차이. 「열셋째」와 같음)
최종 적중     80/90 (89%) 세 묶음 모두. LLM 단독 61/90 (68%)
```

---

### 2026-08-24 (열여섯째) · 선거 상세 도구를 눌러 보고 배선을 채움 · recipe 48

**왜 눌렀나.** 발화 9 "충북 제1선거구 알려줘" 가 판정 10/10 인데 화면에서
아무것도 못 한다. `recipe_015` 에 배선이 없다. `check_wiring` 의 C 부류다.

`STEP_OF` 주석에 「선거구 코드를 내놓는 도구가 없다 … name 칸이 따로 있으므로
그리로 보내는 것이 맞을 수 있으나 눌러서 확인하지 않았다」고 적혀 있었다.
**안 눌러본 것이지 못 적을 것이 아니었다.**

#### 결론 한 줄

**셋을 채웠다. C 7 → 4.** 넷째(`get_local_pledge_summary`)는 못 채웠고
그 이유가 처음 생각과 달랐다. 아래에 적는다.

#### 스키마 — 넷 다 이름 칸을 갖는다

`KRRI_ASAP/ASAP-mcp/main.py` 의 inputSchema 다. **required 가 넷 다 없다.**

```
election.getDistrict                code · name      "선거구명. 예: 서울 강서갑, 의왕과천"
election.getAssemblyDistrict        code · name      "선거구명 또는 검색어"
election.getAssemblyPledgeDistrict  pledgeId · code · name  "선거구명 또는 검색어"
election.getLocalPledgeSummary      sidoCode · sidoName     "시도명. 예: 서울특별시"
```

#### 눌러 본 결과

실제 이름은 `election.searchDistricts` · `searchAssemblyDistricts` 를 `query="청주"`
로 조회해 얻었다(`SGG_Code 2431401` 「충북 청주서원」 · `district_code 4311101`
「청주시 상당구」). 응답 전문은 `tools/probe_out/` 에 있다.

```
election.getDistrict                {name: "충북 청주서원"}   feature 1건   4004자
election.getDistrict                {name: "충북 제1선거구"}  not_found     ★
election.getAssemblyDistrict        {name: "청주시 상당구"}   feature 1건  51975자
election.getAssemblyDistrict        {name: "충북 제1선거구"}  not_found     ★
election.getAssemblyPledgeDistrict  {name: "청주시 상당구"}   feature 1건  31787자
election.getAssemblyPledgeDistrict  {name: "충북 제1선거구"}  not_found     ★
election.getLocalPledgeSummary      {sidoName: "충청북도"}    0건 · featureCount 0
```

**`name` 은 통한다.** 「체계가 달라 못 적는다」는 옛 판단이 지나쳤다.

#### "충북 제1선거구" 는 안 걸린다 — 그러나 배선 문제가 아니다

254개 선거구에 그런 이름이 없다. 이름 체계가 「충북 청주서원」 처럼
`SIDO + SGG` 다. 이것은 **인자 추출이나 어휘 층의 문제이지 배선 문제가 아니다.**
배선은 그대로 채웠다. 같은 도구에 DB 에 있는 이름을 넣으면 1건이 온다.

넷째의 0건은 또 다르다. **저쪽 데이터가 미적재다.**
`getLocalPledgeSummaryDatasetInfo` 가 `featureCount 0 · available false ·
"2026 지방선거 시도별 공약 요약 DB 데이터가 적재되지 않았습니다"` 라고 답한다.
배선이 없는 것 · 발화의 말이 DB 와 다른 것 · 데이터가 미적재인 것은 셋 다 다르다.

#### 채운 배선 — 셋

```
("get_election_district",        "district_code")  {"name": @arg}
("get_assembly_district",        "district_code")  {"name": @arg}
("get_assembly_pledge_district", "district_code")  {"name": @arg}
```

`TOOL_OF` 에도 셋을 더했다. 그 셋은 `TOOL_OF` 에 아예 없어서 배선만 적으면
`plan` 이 `KeyError` 를 냈을 것이다.

#### 넷째는 못 채웠다 — 이유가 처음 생각과 다르다

`get_local_pledge_summary × 행정구역 코드` 는 **한 줄이 두 자리에 걸린다.**

```
recipe_018  말한 식별자 -> 공약 요약                            {sidoName: @arg} 로 맞다
recipe_044  말한 장소 -> 좌표 -> 지점 행정구역 판별 -> 공약 요약   앞 단계 결과를 써야 한다
```

`말한 식별자` 도 `find_admin_boundary_by_point` 도 `admin_code` 를 건네므로
`STEP_OF` 키가 하나뿐이다. `{sidoName: @arg}` 한 줄로 적어 보았더니
**check_wiring 의 B 가 0 에서 1 로 늘었다** — recipe_044 가 "청주시" 를
`sidoName` 으로 보내고 행정구역 판별 결과를 버린다. 관문이 잡아냈다.

자리마다 갈라 적는 칸(`input_first`)이 있지만 그러려면 앞 단계 쪽 input 을
함께 적어야 한다. 그 값이 `adminBoundary.findBoundaryByPoint` 응답의 어느 칸에
오는지는 **오늘 다시 눌러도 여전히 모른다.**

```
adminBoundary.findBoundaryByPoint {lon: 127.3273, lat: 36.6207}
  features [] · count 0 · "행정구역 DB 데이터가 없거나 PostGIS 연결을 사용할 수 없습니다"
```

**반만 알고 한 줄을 적으면 다른 자리가 틀린다.** 그래서 셋만 적고 넷째는
`STEP_OF` 아래 주석 2번에 사정을 적어 남겼다. 저쪽 PostGIS 에 경계가 적재되면
`get_age_profile` · `get_population_trend` 와 함께 풀린다.

#### check_wiring 변화

```
전  A 0 · B 0 · C 7   배선이 다 있는 recipe 38 · STEP_OF 31줄
후  A 0 · B 0 · C 4   배선이 다 있는 recipe 41 · STEP_OF 34줄
```

남은 C 넷 : `web_fetch × 웹 주소` · `get_local_pledge_summary × 행정구역 코드` ·
`get_age_profile × 행정구역 코드` · `get_population_trend × 행정구역 코드`.

#### 발화 9 가 이제 실행까지 간다

```
unwired("recipe_015")  []
plan("recipe_015", "충북 제1선거구")
  steps  [{id: s1, server_id: asap-mcp-core, tool: election.getDistrict,
           input: {name: "충북 제1선거구"}}]
  nodes  [get_election_district]
  headline  "충북 제1선거구 국회의원 지역구를 조회했습니다."
```

예전에는 `unwired` 가 비지 않아 `execute_service.run` 이 도구를 하나도 안 불렀다.
이제 step 이 하나 선다. **부르지는 않았다** — plan 까지만 확인했다.
그 도구가 이 이름으로는 not_found 를 답하는 것은 위에 적은 어휘 층의 문제다.

#### 판정은 안 움직였다

배선은 판정과 무관하다. 채운 뒤 10회 × 아홉 발화를 다시 쟀고
**최종 80/90 (89%) · LLM 단독 61/90 (68%)** 로 「열다섯째」와 같다.

---

### 2026-08-24 (열다섯째) · LLM 단독 대 검산까지 · recipe 48 · qwen3:32b · 10회 × 세 묶음

**왜 쟀나.** 판정은 두 단계다.

```
1  LLM 이 recipe_id 와 candidate_recipe_ids 를 쓴다
2  LLM 이 쓴 축 셋으로 온톨로지를 조회하고(shortlist) 둘을 대조한다(_verdict)
```

**89% 는 2단계까지 거친 값이다.** 1단계만이면 몇 %인지 한 번도 안 쟀다.
그리고 검산이 맞는 답을 덮은 적이 있다(「열한째」, 철도 안전 문서).
검산이 값을 하는지 안 하는지를 재지 않고는 shortlist 를 걷어낼지 말지 못 정한다.

#### 결론 한 줄

**LLM 단독 68% · 최종 89%.** 검산이 21%p 를 올린다. **걷어내면 안 된다.**
그리고 이번 세 묶음에서 **맞는 답을 덮은 자리는 하나도 없었다.**

#### 잰 것

세 묶음이 **회차 하나까지 똑같이 나왔다.** 아래는 그 하나다.

```
#  발화                        LLM 단독   적중     빗나감
1  오송역 위치 보여줘          10/10      10/10    0
2  오송역 좌표 알려줘          10/10      10/10    0
3  오송역 CCTV 보여줘          10/10      10/10    0
4  청주시 인구 구성 알려줘     0/10       0/10     10
5  오송역 근처 충전소 찾아줘   0/10       10/10    0
6  국회의원 선거구 찾아줘      1/10       10/10    0
7  전기차 충전소 데이터 검색   10/10      10/10    0
8  철도 안전 문서 찾아줘       10/10      10/10    0
9  충북 제1선거구 알려줘       10/10      10/10    0
                              ─────────────────────────
                              61/90      80/90    10
                              68%        89%
```

**최종 80/90 (89%) 이 「열넷째」와 같다.** 적중 판정을 한 글자도 안 건드렸으니
같아야 하고, 실제로 같다. 네 칸의 합도 90 이다.

#### 검산이 답을 바꾼 자리 — 전부

```
#  LLM 단독                                       최종     검산이 한 일          횟수
4  {011, 019}                                     {011}    바꿨지만 판정은 같다  10회
5  {012, 035}                                     {035}    검산이 살렸다         10회
6  {007, 015, 027, 028, 029, 030, 031, 032, 034}  {007}    검산이 살렸다          9회
```

바꾼 회차는 스물아홉이고 **좋게 바꾼 것이 열아홉 · 헛일이 열 · 나쁘게 바꾼 것이 0** 이다.
나머지 예순하나(1 · 2 · 3 · 7 · 8 · 9번과 6번의 한 회차)는 LLM 단독과 최종이
같았다 — 그 자리에서는 검산이 한 일이 없다.

**5번과 6번이 검산의 값이다.** 둘 다 LLM 이 후보를 좁히지 못한 자리다.

```
5  충전소 : LLM 이 키워드 검색(012)과 좌표 검색(035)을 못 가름.
           축 셋(spoken_place · item_list · group_ev)이 좌표 검색을 가리켜
           조회 후보가 {035, 036} 이고, 겹치는 것이 035 하나라 SELECT 로 떨어진다
6  선거구 : LLM 이 아홉을 늘어놓는다(9/10 회차에서 recipe_id 가 비고
           candidate_recipe_ids 만 온다). 조회 후보 넷과 겹치는 것이 007 하나다
```

#### 4번은 검산 탓이 아니다

4번은 LLM 단독도 최종도 빗나간다. **검산이 나쁘게 만든 것이 아니라 처음부터
축이 틀렸다.** given 이 열 번 다 `spoken_keyword` 로 온다 — "청주시" 를 장소로
안 보고 키워드로 본다. 기대값 recipe_045 는 `spoken_place` 사슬이라 조회 후보에
아예 안 들어온다. 「열넷째」의 진단 그대로다.

#### 세 갈래 중 어느 것이었나

```
LLM 단독 ≈ 최종      아니다
LLM 단독 ≪ 최종      ★ 이것이다. 68% → 89%
LLM 단독 > 최종      아니다. 덮은 자리 0
```

**shortlist 를 걷어내면 안 된다.** 다만 이것은 판단 재료이고 결론은 사람이 낸다.

#### 「열한째」의 덮어쓰기는 이번에 재현되지 않았다

8번 "철도 안전 문서 찾아줘" 는 열 회차 모두 LLM 단독 {014} · 최종 {014} 다.
검산이 손대지 않는다. 그 사이에 `recipe_014` 의 축을 고쳤기 때문이다
(c03e12c 「문서 검색이 주제 축에 걸려 후보에서 빠지던 것을 고친다」).
**「겹치는 것 0개면 조회 후보를 쓴다」 규칙 자체는 그대로 있다.** 이번 세 묶음에서
그 규칙이 걸린 회차가 없었을 뿐이다.

#### 어떻게 쟀나 — LLM 을 더 부르지 않았다

`resolve_service.resolve` 가 `**verdict` 로 `recipe_id` 와
`candidate_recipe_ids` 를 덮어써서 **LLM 이 쓴 날것이 응답에 안 실리고 있었다.**
그래서 key 를 둘 더했다.

```
llm_recipe_id             검산 전에 LLM 이 쓴 recipe_id
llm_candidate_recipe_ids  검산 전에 LLM 이 쓴 후보 목록
```

**덮어쓰는 쪽은 그대로 뒀다.** 기존 key 의 이름도 뜻도 안 바꿨다. 같은 응답에
칸이 둘 는 것뿐이라 `/resolve` 를 부르는 횟수가 안 늘었다 — 세 묶음이
「열넷째」와 같은 값이 나온 것이 그 증거다.

`tools/check_resolve.py` 는 표를 넷째까지 찍는다. 넷째가 위의 「검산이 답을
바꾼 자리」다.

---

### 2026-08-24 (열넷째) · 세션을 건너 재현되는지 잼 · recipe 48 · qwen3:32b · 10회 × 여섯 묶음

**왜 쟀나.** 「아홉째」와 「열째」가 같은 커밋 · 같은 모델 · temperature 0 ·
seed 0 · clean tree 인데 6번 발화가 달랐다.

```
「아홉째」  6번 → {007}                9회
「열째」    6번 → {007, 008, 009}      9회
```

**모든 숫자가 이 위에 서 있으므로 쟀다.**

#### 결론 한 줄

> **A · B 여섯 묶음이 바이트 단위로 전부 같다. 이 세션 안에서는 재현된다.
> 세션 간 차이는 모델 로드가 아니라 다른 데 있다.**

#### 무엇을 어떻게 쟀나

```
A군   모델을 안 내리고 연속 셋
B군   묶음마다 keep_alive 0 으로 내리고 30초 뒤 다시 올려 셋
```

`ollama ps` 로 실제로 내려갔는지 확인했다. 세 번 다 목록이 비었고 GPU 1 이
28182 MiB → 23 MiB 로 떨어졌다. 내리는 것은 확실히 됐다.

```
A1  86eb68c9b2dd003b66f4f5958294bbfb
A2  86eb68c9b2dd003b66f4f5958294bbfb
A3  86eb68c9b2dd003b66f4f5958294bbfb
B1  86eb68c9b2dd003b66f4f5958294bbfb
B2  86eb68c9b2dd003b66f4f5958294bbfb
B3  86eb68c9b2dd003b66f4f5958294bbfb
```

**여섯이 한 값이다.** 적중 표 · 축 표 · 후보 표 세 장이 전부 같다. 6번 발화도
여섯 번 다 `{007}` 10회이고 축도 후보 수도 안 흔들린다. 유일하게 흔들린 것은
9번 발화의 `want` 축 하나이고(`dataset_info` 9회 · `item_detail` 1회) 그것도
여섯 묶음에서 똑같이 9:1 이다.

**그 9:1 자체가 눈여겨볼 것이다.** temperature 0 · seed 0 인데 묶음 **안에서는**
열 번 중 한 번이 다르다. 그러면서 묶음 **끼리는** 그 한 번이 나오는 자리까지 같다.
흔들림이 무작위가 아니라 열 번의 순서에 매여 있다는 뜻이다.

**GPU 번호는 여섯 번 다 1 이다.** 세 번 내렸다 올렸는데도 같은 카드로 돌아왔다.
**"다른 카드에 얹히면 답이 달라진다" 는 이번에 시험되지 않았다** — 옮겨 탄 적이
없어서 반증도 입증도 없다. 카드를 지정해 다시 재려면 `CUDA_VISIBLE_DEVICES` 를
ollama 쪽에 걸어야 한다.

#### ★ KEEP_ALIVE 가설 — 세우고, 재고, 버렸다

**세운 가설.**

> 모델이 2시간 동안 안 내려간다. 세 묶음이 바이트 단위로 같았던 것은 그동안
> 모델이 한 번도 안 내려갔기 때문이다. 세션을 건너 안 맞은 것은 그 사이 2시간이
> 지나 모델이 내려갔다 다시 올라왔기 때문일 수 있다.

**가설의 절반은 맞았다. 원인은 아니었다.**

`keep_alive` 2시간은 사실이다. 그런데 **환경변수가 아니다.**
`env | grep -i ollama` 도 비었고 `ollama.service` 에도 없다.
**`llm_engine/ollama.py:88` 에 `"keep_alive": "2h"` 로 박혀 있다 — 우리 코드다.**

그러니 `OLLAMA_KEEP_ALIVE` 를 찾아봐야 없다. 이 값을 바꾸려면 저 한 줄을 고친다.

**그런데 그것이 재현성의 원인은 아니다.** B군이 그 가설을 정면으로 시험한 것인데
(묶음마다 강제로 내렸다 올림) 결과가 A군과 한 글자도 안 달랐다.
**모델을 내렸다 올리는 것은 답을 안 바꾼다.**

#### 그래서 세션 간 차이는 어디서 오나 — 아직 모른다

이번에 지운 것과 남은 것.

```
지웠다   모델 로드 · 언로드          B군이 A군과 같다
지웠다   GPU 재할당                  여섯 번 다 GPU 1 이다 (옮겨 탄 적이 없어
                                     "안 옮겨 타면 같다" 까지만 말할 수 있다)
지웠다   같은 세션 안의 실행 순서    A군 셋이 같다
남았다   ollama 프로세스 재시작      이번에 안 껐다 켰다
남았다   ollama 버전이 바뀐 것       「아홉째」·「열째」때 0.32.14 였다
남았다   서버(uvicorn) 재시작        이번에 안 껐다 켰다
남았다   저 두 묶음 사이에 실제로 커밋이 달랐을 가능성
```

**다음에 잴 것은 ollama 프로세스 재시작과 uvicorn 재시작이다.** 그 둘이 이번
실험이 안 건드린 것 중 제일 크다.

#### 환경

```
2026-08-24T02:26 ~ 02:56 · 커밋 2b25290 · clean
ollama 0.32.15 · qwen3:32b (030ee887880f) · 28GB · 100% GPU · ctx 32768
GPU  RTX PRO 6000 Blackwell Max-Q x4 (각 97887 MiB)
     여섯 묶음 내내 GPU 1.  0·2 는 다른 프로세스가 쓰는 상주분(669·596 MiB)
env | grep -i ollama   비었음
ollama.service 의 Environment  PATH 뿐
keep_alive             llm_engine/ollama.py:88 에 "2h" 로 박혀 있음
```

#### 관문

없었다. 어느 쪽이 나와도 결과인 실험이다.

#### 다시 시도하지 말 것

- **`OLLAMA_KEEP_ALIVE` 환경변수를 찾거나 거는 것** — 이 저장소는 요청 본문에
  실어 보낸다. 환경변수를 걸어도 요청 값이 이긴다.
- **한 세션 안에서 묶음을 더 돌려 재현성을 확인하는 것** — 여섯 묶음이 바이트
  단위로 같다. 더 돌려도 같다. 세션을 건너야 의미가 있다.

---
### 2026-08-24 (열셋째) · 발화에서 뽑은 인자가 도구에 통하는지 처음 잼 · recipe 48 · qwen3:32b · 3회 · 고치지 않음

**recipe 는 맞는데 답이 0건이었다.** 화면 실측(2026-08-24)에서 셋이 그랬고,
`check_resolve` 는 `recipe_id` 만 대조하므로 셋 다 적중으로 잡혔다.
**인자가 도구에 통하는지는 한 번도 안 쟀다.** 「축이 89/90 동일」은 안정적이라는
뜻이지 맞다는 뜻이 아니다.

`tools/check_argument.py` 를 새로 만들어 쟀다. `/resolve` 로 인자를 받고, 그
recipe 의 **첫 실행 단계** 배선을 `step_service.plan` 에서 읽어 `@arg` 자리에
넣고 Gateway 를 직접 부른다. **재기만 한다. 아무것도 안 고쳤다.**

**첫 단계만 잰다.** `$prev` 자리는 앞 단계가 있어야 부를 수 있어 범위 밖이다.
그래서 **여기 건수는 사슬 끝의 답이 아니다** — 3번 CCTV 는 첫 단계가
`geo.geocode` 라 좌표 건수를 재고, CCTV 가 몇 건인지는 여기서 안 나온다.

#### 인자 표 (LLM 3회 · 사람 값과 확인용 값 각 1회)

```
  #  발화                        recipe  첫 도구                       뽑은 이   인자                  건수
  1  오송역 위치 보여줘          001     geo.geocode                   LLM       오송역                1(단건)
                                 001     geo.geocode                   사람추측  오송역                1(단건)
  2  오송역 좌표 알려줘          001     geo.geocode                   LLM       오송역                1(단건)
                                 001     geo.geocode                   사람추측  오송역                1(단건)
  3  오송역 CCTV 보여줘          025     geo.geocode                   LLM       오송역                1(단건)
                                 025     geo.geocode                   사람추측  오송역                1(단건)
  4  청주시 인구 구성 알려줘     011     population.searchStatistics   LLM       인구 구성             0
                                 011     population.searchStatistics   사람추측  청주시                4
  5  오송역 근처 충전소 찾아줘   035     geo.geocode                   LLM       오송역                1(단건)
                                 035     geo.geocode                   사람추측  오송역                1(단건)
  6  국회의원 선거구 찾아줘      007     election.searchDistricts      LLM       국회의원 선거구       0
                                 007     election.searchDistricts      사람추측  선거구                0
                                 007     election.searchDistricts      확인용    청주                  4
  7  전기차 충전소 데이터 검색…  012     ev.searchStations             LLM       전기차 충전소 데이터  0
                                 012     ev.searchStations             사람추측  전기차 충전소         123
  8  철도 안전 문서 찾아줘       014     knowledge.query               LLM       철도 안전             0
                                 014     knowledge.query               사람추측  철도 안전             0
                                 014     knowledge.query               확인용    (빈 질의)             0
  9  충북 제1선거구 알려줘       015     -                             LLM       충북 제1선거구        배선 없음
```

`사람추측` 은 발화에서 사람이 뽑았을 값이다. **추측이고 정답이 아니다.**
`확인용` 은 도구와 데이터가 살아 있는지만 보는 값이라 사람이 말할 값이 아니다.
사람 값도 0건일 때 "값이 안 통한 것" 과 "데이터가 없는 것" 을 가르려고 넣었다.

#### 0건이 무엇 탓인가

```
  1 · 2 · 3 · 5   LLM 값으로 나온다. 가를 것 없음
  4               ★ 인자 탓.  "인구 구성" 0건 · "청주시" 4건
  7               ★ 인자 탓.  "전기차 충전소 데이터" 0건 · "전기차 충전소" 123건
  6               ★ 발화에 통하는 낱말이 없다.  "청주" 면 4건이 나온다
  8               데이터 탓.  빈 질의도 0건이다
  9               못 쟀다.  배선이 없어 실행 자체가 안 된다
```

**넷이 갈렸고 셋이 서로 다른 이유였다.**

**4 와 7 은 인자 탓이다.** 발화를 통째로 옮겨 담는다. `"청주시 인구 구성 알려줘"`
에서 `"인구 구성"` 을 뽑고, `"전기차 충전소 데이터 검색해줘"` 에서 `"전기차 충전소
데이터"` 를 뽑는다. 도구가 받는 것은 **찾을 대상의 이름**인데 **하려는 일의 이름**을
넣는다. 7 은 낱말 하나("데이터")가 붙었을 뿐인데 123건이 0건이 된다.

**6 은 인자 탓도 데이터 탓도 아니다.** `election.searchDistricts` 가 찾는 것은
선거구 **이름**이다(`"충북 청주서원"`). 발화 `"국회의원 선거구 찾아줘"` 의
`"국회의원"` 도 `"선거구"` 도 이름의 일부가 아니라 **종류의 이름**이라 어떤
낱말을 뽑아도 안 걸린다. 데이터는 254개가 살아 있다. **인자 추출을 고쳐도 이
발화는 안 낫는다.**

**8 은 데이터 탓이다.** `knowledge.query` 는 **빈 질의에도 `[]`** 다. 문서가 하나도
없다. `"철도"` · `"안전"` · `"문서"` 로도 전부 0건이다. 인자와 상관없다.
새로 안 것이 아니라 위 「막힌 노드 여덟」의 `search_documents`(지식베이스에 문서가
0개)를 다른 길로 다시 확인한 것이다. **이 도구가 처음으로 인자와 무관하다고
증명된 것이 새 것이다** — 「열한째」에서 후보에 오르게 고친 뒤에도 0건인 이유가
문장도 인자도 아니라는 뜻이다.

**9 는 못 쟀다.** `recipe_015` 는 `get_election_district` 에 배선이 없어
`execute_service.run` 이 도구를 하나도 안 부른다. `check_resolve` 에서 10/10
적중인 발화가 화면에서는 아무것도 못 한다. **적중 표만 보면 안 보이는 자리다.**

#### 안 한 것

**인자 추출을 안 고쳤다.** 어떻게 바꿀지는 사람이 정한다. 프롬프트를 손댈지,
`given` 축을 보고 뽑는 법을 가를지, 아니면 도구 쪽 검색을 넓힐지가 다 다른 일이다.

#### 다시 시도하지 말 것

- **`"국회의원 선거구 찾아줘"` 의 인자 추출을 고쳐서 6번을 살리기** — 발화에
  걸릴 낱말이 없다. 데이터의 이름은 지역명이다.
- **`recipe_014` 를 문장으로 살리기** — 「열한째」에서 후보에는 오르게 했다.
  그 위에서도 0건이다. 남은 것은 문서 데이터를 넣는 일이다.

---
### 2026-08-24 (열두째) · 적중 표를 넷으로 가름 · recipe 48 · qwen3:32b · 10회 · 판정 안 건드림

**지표가 품질을 못 봤다.** 「열한째」에서 발화 4 가 이렇게 바뀌었는데 표가 안 움직였다.

```
전   CLARIFY {034, 045, 046}   정답 045 가 후보 안에 있다. 사람이 고르면 된다
후   SELECT  {011}             확신하고 틀린다. 사람이 알 방법이 없다
```

**둘 다 0/10 이다.** 「틀린 답보다 정직한 되물음이 낫다」가 이 저장소의 원칙인데
한 칸짜리 적중률은 그 차이를 못 본다.

**표 모양이 바뀌었다.** `tools/check_resolve.py` 의 적중 표에 칸 셋을 더했다.
**점수 계산은 한 글자도 안 고쳤다** — `set(result) == expected` 그대로다.

```
적중        지금 그대로.  set(result) == expected
근접        정답이 후보 안에 남아 있는데 하나로 못 좁혔다 (CLARIFY)
빗나감      정답이 후보에 아예 없다.  ★ 제일 나쁘다
못 붙음     NO_MATCH · 후보 비었음 · 호출 오류
```

`적중 + 근접 + 빗나감 + 못 붙음 = 시행 횟수` 다. 안 맞으면 표가 스스로 경고한다.

**「빗나감」에는 두 가지가 들어간다.** SELECT 로 하나 고르고 틀린 것과, CLARIFY
인데 정답이 후보에 아예 없는 것이다. 둘 다 사람이 정답에 닿을 길이 없다. 어느
쪽인지는 옆의 「틀렸을 때 나온 것」 칸의 status 로 갈린다.

#### 숫자 — 점수는 그대로다

```
  #  발화                          적중     근접  빗나감  못 붙음
  1  오송역 위치 보여줘            10/10    0     0       0
  2  오송역 좌표 알려줘            10/10    0     0       0
  3  오송역 CCTV 보여줘            10/10    0     0       0
  4  청주시 인구 구성 알려줘        0/10    0    10       0    <- {011} SELECT 10회
  5  오송역 근처 충전소 찾아줘     10/10    0     0       0
  6  국회의원 선거구 찾아줘        10/10    0     0       0
  7  전기차 충전소 데이터 검색해줘 10/10    0     0       0
  8  철도 안전 문서 찾아줘         10/10    0     0       0
  9  충북 제1선거구 알려줘         10/10    0     0       0
                                  ───────────────────────────
                                   80/90    0    10       0    89%
```

**80/90 (89%) 는 「열한째」와 같다.** 판정 규칙을 안 건드렸으므로 같아야 하고
같았다. 달라졌으면 규칙을 건드린 것이다.

**발화 4 가 빗나감 10회로 잡힌다.** 「열한째」에서는 이 자리가 근접 10회였을
것이다 — 그때 표에 칸이 없어 뒤늦게 세지는 못한다. 그것이 이 칸을 더한 이유다.

#### 다시 시도하지 말 것

- **적중률 한 칸으로 되돌리기** — 되물음이 확신하고 틀린 것으로 바뀌어도 숫자가
  안 움직인다. 실제로 그렇게 한 번 놓쳤다. 빗나감이 늘고 근접이 줄었으면
  적중률이 그대로여도 나빠진 것이다.

#### 관문

```
80/90 (89%) 그대로            ✓
발화 4 가 빗나감 10회          ✓
pytest 175 passed / 1 failed  ✓ (graphviz 버전 차이. 예전부터 그대로)
```

---
### 2026-08-23 (열한째) · 문서 검색이 about 축에 걸려 후보에서 빠지던 것을 고침 · recipe 48 · qwen3:32b · 각 10회 · 안마다 묶음 셋

**무엇이 문제였나.** 발화 8 `"철도 안전 문서 찾아줘"` 의 about 이 열 번 모두
`group_transport` 로 잡힌다. 철도로 본 것은 틀리지 않았다. 그런데
`search_documents` 는 `about group_document` 하나뿐이라 shortlist 의 about
축에서 걸러지고, `web_search` 는 범용(about 없음)이라 늘 통과한다. 그래서
`{006}` 만 남는다. **`recipe_014` 는 문장을 어떻게 고쳐도 후보에 오를 수가
없었다.**

`group_document` 만 성격이 다르다.

```
group_transport · group_ev · group_election   주제다
group_document                                 그릇이다
```

#### 세 안을 다 재고 골랐다

```
A  search_documents 의 about group_document 엣지를 뗀다
B  search_documents 에 about 을 넷 단다 (transport · ev · election · document)
C  group_document 노드를 없애고 문서 노드 셋을 범용으로 돌린다
```

**recipe 수는 세 안이 전부 48개이고 경로 집합까지 base 와 한 줄도 다르지
않다.** `crosses_groups` 가 `about` 을 읽으므로 경로가 움직일까 봐 미리
`ontology.yaml` 변형본으로 오프라인 생성해 확인했고, 실제 `rebuild_init`
결과도 recipe/menu 파일이 바이트 단위로 같았다. 이유는 `search_documents` 가
사슬의 끝점이고 about 붙은 노드가 경로에 하나뿐이라 애초에 `crosses_groups`
가 걸리지 않기 때문이다.

```
안    recipe  menu.yaml   묶음1  묶음2  묶음3   발화8
base    48     3741자     61/90  61/90  62/90    0/10
A       48     3741자     70/90  70/90  70/90   10/10
B       48     3741자     71/90   —      —      10/10
C       48     3741자     80/90  80/90  80/90   10/10   ← 골랐다
```

**C 를 골랐다. 80/90 이 세 묶음 모두 같다.**

발화 8 이 어떻게 후보에 오르는지는 안마다 다르다.

```
base  about=group_transport -> ['006']            tier-1 이 비어 tier-2 범용으로 내려가는데
                                                   그 범용이 web_search 하나뿐이다
A     about=group_transport -> ['006', '014']     014 가 범용이 되어 함께 내려온다
B     about=group_transport -> ['014']            tier-1 이 단독으로 잡는다
C     about=group_transport -> ['006', '014']     A 와 같다
```

#### 왜 A 와 B 를 버렸나

**B 를 버린 이유는 점수가 아니라 뜻이다.** 71/90 으로 A 와 같았고 걱정했던
오염도 판정까지는 안 갔지만, shortlist 는 실제로 넓어졌다 — 6번 조회 후보
4->5, 7번 2->3. "문서는 모든 주제에 걸린다" 를 엣지로 적는 것은 about 을
주제 축으로 쓰는 설계와 어긋나고, 주제 노드가 늘 때마다 엣지를 하나씩 더
달아야 한다. 한 묶음만 재고 접었다.

**A 는 10점 낮다.** 70/90 이 세 묶음 모두 같다. A 와 C 는 발화 8 에 대해
shortlist 결과가 `['006', '014']` 로 똑같은데도 10점이 갈린다. 갈린 자리는
6번이다.

```
        4번                        6번
A       {034,045,046} CLARIFY      {007,008,009} CLARIFY  0/10
C       {011}         SELECT       {007}         SELECT  10/10
```

#### ★ C 의 10점은 문서 검색과 상관이 없다

정직하게 적는다. C 가 A 보다 나은 10점은 **6번이 CLARIFY 에서 SELECT 로
바뀐 것**이고, 6번은 선거이지 문서가 아니다. C 는 `group_document` 를 없애
about 선택지를 5개에서 4개로 줄이는데, 그 선택지 목록이 프롬프트에 들어가므로
**프롬프트가 짧아진 것이 LLM 의 판정 전체를 흔들었다.** 축 해석도 함께
움직였다.

```
        발화 4 given            발화 7 argument         발화 8 argument
A       spoken_place           전기차 충전소            철도 안전 문서
C       spoken_keyword         전기차 충전소 데이터     철도 안전
```

**대가가 있다. 발화 4 가 나빠졌다.**

```
A · base  4번  {034, 045, 046}  CLARIFY   정답 045 가 후보 안에 있다. 사용자가 고른다
C         4번  {011}            SELECT    정답이 없다. 확신하고 틀린다
```

점수는 둘 다 0/10 이라 표에 안 잡히지만 화면에서의 성격은 나빠졌다. `011` 은
`말한 키워드 → 인구 통계 집계` 다 — "청주시" 를 장소가 아니라 키워드로 읽었다.
**숫자가 판정한다는 규칙대로 C 를 골랐고, 이 대가는 「열린 과제」에 남긴다.**

C 를 고른 것이 뜻으로도 맞다는 점은 덧붙여 둔다 — `group_document` 는 주제가
아니라 그릇이었고, 주제 축에서 빼는 것이 원래 맞는 자리다. 다만 **10점의 출처는
그것이 아니다.**

#### 관문

`pytest` 175 passed / 1 failed (`test_dense_graph_would_move_if_overlap_removal_
were_used` — graphviz 버전 차이지 이번 변경과 무관하다) · recipe 48 유지 ·
menu.yaml 3,741자(예산 6,000) · 발화 8 이 10/10 · ①의 61~62 보다 높다.

#### 남은 것

**발화 4 `{034, 045, 046}`.** 「아홉째」가 문장으로는 안 갈린다는 것을 증명했고
(앞토막 57자 -> 31자로 줄여도 그대로), C 에서는 아예 `{011}` 로 바뀌었다.
셋 다 인구이고 셋 다 `statistics` 를 내놓아 shortlist 가 셋을 다 올린다.
**GT 를 다시 볼 자리일 수 있다** — "인구 구성" 이 정말 `045`(연령대별 남녀)
하나만 뜻하는지, `034`(지역별 인구 수) 도 답인지 사람이 정할 일이다.
이번에는 안 고쳤다.

### 2026-08-23 (열째) · GT 6번을 바로잡음 · recipe 48 · qwen3:32b · 각 10회 · 묶음 셋

**GT 를 고쳤다. 숫자 때문이 아니다.**

```
발화 6  "국회의원 선거구 찾아줘"
옛 기대값   {recipe_007, 008, 009, 010}
새 기대값   {recipe_007}
```

넷을 다 적었던 근거는 `check_resolve.py` 상단의 "007~010 발화만으로는 안
갈린다" 였다. **그 전제가 「아홉째」에서 거짓이 됐다.** description 을 다시 쓴
뒤 menu 의 네 문장이 이렇게 갈린다.

```
007  말한 키워드로 제22대 국회의원 지역구를 선거구명과 시도, 코드로 검색한다
008  말한 키워드로 제22대 국회의원 전체 선거구를 공약 수와 분야 집계로 검색한다
009  말한 키워드로 제22대 국회의원 선거구 공약을 당선인과 정당, 분야로 검색한다
010  말한 키워드로 2026 지방선거 시도별 대표 인물과 정당, 교통 공약 주제를 검색한다
```

"국회의원 선거구 찾아줘" 에 맞는 것은 007 하나다. 008·009 는 공약이고 010 은
지방선거다. 근거는 menu 문장이지 판정 숫자가 아니다 — 다른 여덟 발화의
기대값은 한 글자도 안 건드렸다.

#### 숫자

```
#  발화                          아홉째    묶음1  묶음2  묶음3
1  오송역 위치 보여줘             10/10     10/10  10/10  10/10
2  오송역 좌표 알려줘             10/10     10/10  10/10  10/10
3  오송역 CCTV 보여줘             10/10     10/10  10/10  10/10
4  청주시 인구 구성 알려줘         0/10      0/10   0/10   0/10
5  오송역 근처 충전소 찾아줘      10/10     10/10  10/10  10/10
6  국회의원 선거구 찾아줘          0/10      1/10   1/10   2/10
7  전기차 충전소 데이터 검색해줘  10/10     10/10  10/10  10/10
8  철도 안전 문서 찾아줘           0/10      0/10   0/10   0/10
9  충북 제1선거구 알려줘          10/10     10/10  10/10  10/10
                                 ─────     ─────  ─────  ─────
                                 60/90     61/90  61/90  62/90
                                  67%       68%    68%    69%
```

**나머지 여덟은 한 칸도 안 움직였다.** GT 수정이 6번에만 닿은 것을 확인했다.

#### ★ 「아홉째」의 6번 관측이 재현되지 않는다

「아홉째」는 6번을 `{007} 9회` 로 적었다. 그 값이 그대로면 새 GT 로 9/10 이 되어
69/90 이 나와야 한다. **안 나온다.** 이번 세 묶음에서 6번은 이렇다.

```
{007, 008, 009}  9 · 9 · 8회   LLM 후보 3 · 조회 후보 4 · CLARIFY
{007}            1 · 1 · 2회   LLM 후보 1 · 조회 후보 4 · SELECT
```

축은 30회 모두 `spoken_keyword · item_list · group_election` 로 같다. 축이
흔들린 것이 아니라 LLM 이 넷 중 셋을 남기고 CLARIFY 로 간다. 저장소는
`f4eb07c` clean tree 라 코드·문장 변경은 원인이 아니다.

**「아홉째」의 "온도 0 · seed 0 이라 세 묶음이 바이트 단위로 같다" 는 세션을
건너서는 성립하지 않는다.** 이번 세션 안에서도 완전히 같지는 않다 —

```
묶음1 · 묶음2   e468b71eef88bde82aa5cbf1c9a5bbec   (머리말 뺀 본문 md5)
묶음3           eab196e635e5cde073e9bea41f55f984
```

묶음 1·2 는 같고 묶음 3 에서 6번이 1/10 -> 2/10 으로 움직였다. **앞으로 한 묶음
차이의 ±1~2 는 유의미하지 않은 것으로 읽는다.** 세 묶음을 재는 이유가 이것이다.

**관문의 69/90 은 못 넘었다.** 다만 그 숫자는 「아홉째」의 `{007} 9회` 관측에
기댄 예측이었고, 그 관측 자체가 재현되지 않는다. 관문이 실제로 가리려던 것
("GT 수정이 다른 발화를 건드렸나")은 위 표대로 통과다. 판정도 60 -> 61·61·62 로
내려가지 않았다. 그래서 되돌리지 않고 커밋했다. 되돌리려면
`git revert` 한 번이면 된다.

### 2026-08-23 (아홉째) · description 을 다시 써서 판정을 올림 · recipe 48 · qwen3:32b · 각 10회 · 묶음 셋

**왜 했는가.** 개편 뒤 판정이 41/90(46%)이었다. 실패 다섯 중 넷이
"비슷한 문장이 안 갈린다" 이고 그 문장은 `description` 이 만든다
(바로 아래 「여덟째」 재료). 노드 22개의 `description` 만 고쳤다.
노드 이름 · 관계 · recipe · 코드는 한 글자도 안 고쳤다. menu 는 `rebuild_init` 이
다시 만들었다.

**★ `tools/check_resolve.py` 의 UTTERANCES 를 열지 않았다.** 문장의 근거는
`/tmp/tools.json` 의 `inputSchema` 와 도구 자신의 description, 그리고
`tools/probe_out/` 의 실제 응답뿐이다. (아홉 발화의 문구 자체는 「여덟째」
항목이 이미 표로 옮겨 적어 둔 것이라 눈에 들어왔다. 그 문구를 description 에
반영하지 않았다는 것이 지킨 규칙이다.)

#### 숫자 — 한 판으로 끝냈다

```
                                기준선      개편 뒤      이번
                                (개편 전)   (여덟째)     (아홉째)
#  발화                          50/90 판   41/90 판     묶음1  묶음2  묶음3
1  오송역 위치 보여줘             10/10      10/10        10/10  10/10  10/10
2  오송역 좌표 알려줘             10/10      10/10        10/10  10/10  10/10
3  오송역 CCTV 보여줘             10/10      10/10        10/10  10/10  10/10
4  청주시 인구 구성 알려줘        10/10       0/10         0/10   0/10   0/10
5  오송역 근처 충전소 찾아줘       0/10       1/10        10/10  10/10  10/10   ★
6  국회의원 선거구 찾아줘          0/10       0/10         0/10   0/10   0/10
7  전기차 충전소 데이터 검색해줘   0/10       0/10        10/10  10/10  10/10   ★
8  철도 안전 문서 찾아줘           0/10       0/10         0/10   0/10   0/10
9  충북 제1선거구 알려줘          10/10      10/10        10/10  10/10  10/10
                                 ─────      ─────        ─────  ─────  ─────
                                 50/90      41/90        60/90  60/90  60/90
                                  56%        46%          67%    67%    67%
```

세 묶음이 바이트 단위로 같다 (`ec93563c4757e95a37c323dd84e31223`).
온도 0 · seed 0 이라 정상이다 (아래 「밤」 항목과 같은 결과).

**관문 전부 통과.** recipe 48개 유지(번호도 경로도 그대로) ·
menu.yaml 3,741자(예산 6,000) · `pytest` 175 passed / 1 failed
(그 1건은 graphviz 버전 차이지 이번 변경과 무관하다).

#### 무엇이 올랐고 무엇이 그대로인가

```
5번  1/10 -> 10/10   충전소(035) 대 충전기(036) 가 갈렸다
7번  0/10 -> 10/10   충전소(012) 대 충전기(013) 가 갈렸다
4번  0/10 -> 0/10    {034, 045, 046} 셋이 그대로 CLARIFY 다
6번  0/10 -> 0/10    오히려 후보가 좁아졌다 — {007,008} 9회 -> {007} 9회
8번  0/10 -> 0/10    {006} 10회 그대로
```

**4 · 6 · 8 이 남았고 셋 다 성격이 다르다.**

`4번` LLM 이 스스로 후보 3개를 낸다(LLM 후보 3 · 조회 후보 3 · CLARIFY).
앞토막은 이제 31자에서 갈리는데(전에는 57자) 그래도 셋이 다 그럴듯하게
읽힌다. 문장 길이의 문제가 아니라 셋이 실제로 인접한 능력이라는 뜻이다.

`6번` **description 으로 더 못 간다.** 기대값이 `{007,008,009,010}` 인데
`010` 은 2026 지방선거이지 국회의원이 아니다. `007`·`008`·`009` 의 앞토막을
`제22대 국회의원 ` 으로 맞췄더니 오히려 `007` 하나로 SELECT 됐다. 넷을 다
띄우려면 넷이 서로 구별이 안 되게 써야 하는데 그건 이 작업의 반대다.

`8번` **문장 문제가 아니라 `about` 축 문제다.** 실측으로 확인했다 —
`given · want · about` 이 열 번 모두 `spoken_keyword · item_list · group_transport`
다. `search_documents` 는 `about group_document` 라 shortlist 에서 걸리지 않고,
`web_search` 는 범용(about 없음)이라 늘 통과한다. 그래서 `006` 만 남는다.
`014` 의 문장을 아무리 고쳐도 후보에 오르지 못한다. 「열린 과제」가 적어 둔
"기대값이 잠정" 과 같은 자리이고, 고치려면 축 해석이나 shortlist 를 봐야 한다.

#### 무엇을 어떻게 고쳤나 — 노드 22개

**규칙 셋을 그대로 적용했다.** 앞에서 갈리게 · 도구가 하는 일만 · 짧게.

| 노드 | before | after |
|---|---|---|
| `geocode_place` | 장소 이름으로 위치 좌표와 지도 범위를 찾는다. | 좌표를 찾는다. |
| `find_admin_boundary_by_point` | 좌표가 속한 시도와 시군구, 읍면동 계층을 찾는다. | 그 지점의 행정구역과 코드를 찾는다. |
| `search_admin_boundaries` | 이름이나 코드, 지도 범위로 시도와 시군구, 읍면동 경계를 조회한다. | 행정구역 경계를 이름이나 코드, 레이어로 조회한다. |
| `get_vworld_boundaries` | VWorld 에서 광역시도와 시군구, 읍면동, 리 경계를 내려받는다. | VWorld 에서 리 단위까지 경계 도형을 내려받는다. |
| `web_search` | 인터넷에서 최신 공개 정보를 검색한다. | 인터넷에서 최근 며칠이나 특정 사이트로 좁혀 공개 정보를 검색한다. |
| `search_documents` | 지식베이스에 쌓인 문서에서 물어본 내용을 검색한다. | 문서 본문을 지식베이스에서 찾는다. |
| `get_railway_section` | 구간 이름으로 철도 선형 좌표와 지도 범위를 찾는다. | 철도 구간의 선형 좌표를 찾는다. |
| `get_railway_lines` | 노선 이름이나 역 이름으로 철도 노선 선형을 조회한다. | 경부선 같은 노선명이나 역명으로 철도 노선과 역을 조회한다. |
| `search_election_districts` | 이름이나 시도, 선거구 코드로 제22대 국회의원 지역구를 검색한다. | 제22대 국회의원 지역구를 선거구명과 시도, 코드로 검색한다. |
| `search_assembly_districts` | …전체 선거구를 공약 수와 분야 집계와 함께 검색한다. | 제22대 국회의원 전체 선거구를 공약 수와 분야 집계로 검색한다. |
| `search_assembly_pledge_districts` | 당선인과 정당, 공약 분야로 제22대 국회의원 선거구 공약을 검색한다. | 제22대 국회의원 선거구 공약을 당선인과 정당, 분야로 검색한다. |
| `get_assembly_pledge_district` | 공약 번호나 선거구 코드로 … 한 건을 조회한다. | 공약 번호나 선거구 코드, 선거구명으로 … 한 건을 조회한다. |
| `find_election_district_by_point` | 좌표가 속한 제22대 국회의원 지역구를 찾는다. | 그 지점의 제22대 국회의원 지역구를 찾는다. |
| `find_assembly_district_by_point` | 좌표가 속한 제22대 국회의원 전체 선거구를 찾는다. | 그 지점의 제22대 국회의원 전체 선거구를 공약 집계와 함께 찾는다. |
| `find_assembly_pledge_district_by_point` | 좌표가 속한 제22대 국회의원 선거구의 공약을 찾는다. | 그 지점의 제22대 국회의원 선거구 공약을 분야로 걸러 찾는다. |
| `find_local_pledge_summary_by_point` | 좌표가 속한 시도의 2026 지방선거 교통 공약 요약을 찾는다. | 그 지점이 속한 시도의 2026 지방선거 교통 공약 요약을 찾는다. |
| `search_population_statistics` | 시도와 시군구, 읍면동 단위로 주민등록 인구를 집계해 조회한다. | 지역별 인구 수와 순위를 이름이나 코드로 집계한다. |
| `get_age_profile` | 행정구역 코드로 0세부터 110세까지 성별 연령 인구 구성을 조회한다. | 연령대별 남녀 인구 구성을 행정구역 코드와 기준월로 본다. |
| `get_population_trend` | 행정구역 코드로 월별 총인구와 유소년·생산연령·고령인구 추이를 조회한다. | 달마다 인구가 얼마나 늘고 줄었는지 행정구역 코드와 기간으로 본다. |
| `search_ev_stations` | 지도 범위와 지역, 충전기 유형으로 전기차 충전소를 검색한다. | 충전소를 지도 범위와 이름·주소·운영기관, 충전기 유형으로 찾는다. |
| `search_ev_chargers` | 지도 범위와 지역으로 전기차 충전기를 조회한다. | 옛 호출 호환용으로 충전소를 지도에 뿌린다. |
| `get_ev_station` | 충전소 번호로 충전소 정보와 충전기별 현재 상태를 조회한다. | 충전소 한 곳의 충전기별 현재 상태를 번호로 조회한다. |

#### ★ 정정 — "충전소 대 충전기" 는 틀린 전제였다

`search_ev_chargers` 를 "충전기 단위" 로 알고 있었는데 **실측으로 아니다.**

```
/tmp/tools.json      ev.searchChargers 도구 자신의 description :
                     "기존 호출 호환용 전기차 충전소 조회.
                      신규 목록·지도 조회는 ev.searchStations 사용 권장"
probe_out 두 응답    둘 다 aggregation: "station" · totalMatches 93,353 로 같다.
                     ev.searchStations 는 items 표를 주고
                     ev.searchChargers 는 geojson.features 를 준다.
                     단위가 아니라 응답 모양과 지위가 다르다
입력 칸              searchStations 에만 status · chargerType · minOutputKw ·
                     cluster 가 있다. searchChargers 에는 statId · chgerId 가 있다
```

그래서 "충전기를 조회한다" 를 "옛 호출 호환용으로 충전소를 지도에 뿌린다" 로
바꿨다. **036 · 048 이 덜 매력적으로 보이게 된 것은 부작용이 아니라 사실이다** —
도구가 스스로 신규 조회에는 쓰지 말라고 적어 두었다. 5번과 7번이 함께 10/10 이
된 것이 이 한 줄이다.

#### 앞토막 묶음 — 전후

```
             전       후
앞 10자     18묶음   19묶음   (2개 이상인 묶음 6 -> 6, 그 안 문장 36 -> 35)
앞 20자     21묶음   32묶음   (5 -> 8, 32 -> 24)
앞 30자     22묶음   39묶음   (4 -> 6, 30 -> 15)
앞 40자     39묶음   45묶음   (3 -> 3, 12 ->  6)

48문장 합계  2,805자 -> 2,278자   평균 58.4자 -> 47.5자
menu.yaml    4,268자 -> 3,741자   (예산 6,000)
```

가장 컸던 겹침 셋이 다 짧아졌다.

```
21문장짜리 묶음   공통 30자 -> 공통 14자   (`말한 장소로 좌표를 찾고 `)
044·045·046      공통 57자 -> 공통 31자
047·048          앞이 14자에서 갈린다 (전에는 41자)
```

`geocode_place` 한 줄이 21문장의 앞 30자를 만들고 있었다. 그 한 줄을
24자에서 8자로 줄인 것이 이번 변경의 절반이다.

#### 안 한 것

- **`UTTERANCES` 를 열지 않았다.** GT 도 `CANDIDATES` 도 안 건드렸다.
- 노드 이름 · 관계(`is-a` · `about` · `hasInput` · `hasOutput`) · recipe ·
  코드 · 테스트를 안 고쳤다. `yaml.dump` 를 안 썼다 — 텍스트 치환이다.

#### 두 번째 판 — 비겨서 되돌렸다

4번 하나만 노리고 인구 셋을 다시 썼다. **60/90 그대로였다. 되돌렸다**
(`git checkout -- ontology/ workflows/`). 커밋에 없다.

| 노드 | 첫 판 (쓴 것) | 두 번째 판 (버린 것) |
|---|---|---|
| `search_population_statistics` | 지역별 인구 수와 순위를 이름이나 코드로 집계한다. | 여러 지역의 인구 수를 견주어 순위를 매긴다. |
| `get_age_profile` | 연령대별 남녀 인구 구성을 행정구역 코드와 기준월로 본다. | 한 지역을 0세부터 110세까지 다섯 살 단위 남녀로 갈라 본다. |
| `get_population_trend` | 달마다 인구가 얼마나 늘고 줄었는지 행정구역 코드와 기간으로 본다. | 한 지역이 달마다 얼마나 늘고 줄었는지 흐름으로 본다. |

노린 것은 "여럿을 견준다(034)" 대 "한 지역을 뜯어본다(045 · 046)" 였다.

```
4번  {034, 045, 046} 10회   그대로다.  안 먹혔다
6번  {007} 9회 -> {007,008,009} 10회   기대값에 가까워졌지만 010 이 없어 여전히 0/10
합계 60/90                  똑같다
```

**두 가지를 알았다.**

첫째, 4번은 문장으로 안 갈린다. 앞토막을 57자 -> 31자로 줄여도, "여럿 대 하나"
로 축을 바꿔도 LLM 이 스스로 셋을 다 고른다. 셋이 실제로 인접한 능력이라는
뜻이고, 다음에 볼 곳은 문장이 아니라 shortlist 나 축이다.

둘째, **인구 노드만 고쳤는데 6번의 후보가 움직였다.** 겹치는 recipe 가 하나도
없다. menu 문장 하나가 바뀌면 무관한 발화의 판정도 흔들린다는 뜻이라,
"이 노드만 고쳤으니 저 발화는 그대로일 것" 이라고 가정하면 안 된다.
잴 때는 늘 아홉을 다 잰다.

측정은 묶음 1 에서 멈췄다. 온도 0 · seed 0 이고 첫 판의 세 묶음이 바이트
단위로 같았으므로 묶음 1 이 결과를 정한다. 비긴 것을 확인하려고 100분을
더 쓰지 않았다.

다음에 볼 것은 4번의 `034` · `045` · `046` 셋(문장 말고 shortlist)과
8번의 `about` 축이다.

### 2026-08-23 (여덟째) · description 을 다시 쓰기 위한 재료 · recipe 48 · 고치지 않음

**왜 잰다.** 개편 뒤 판정이 56% -> 46% 다 (바로 아래 기준선 측정). 완전 적중이
아닌 다섯(4 · 5 · 6 · 7 · 8) 중 넷이 "비슷한 문장이 안 갈린다" 이고 그 문장은
`description` 이 만든다. **고치기 전에 무엇이 겹치는지 다 셌다.**

**이 항목은 재료다. 무엇을 어떻게 고칠지 적지 않는다.** 맨 끝 「사람이 정할 것」
에 질문만 있다. 온톨로지 · recipe · menu · 코드 · 테스트를 한 글자도 안 고쳤다.
Gateway 도 LLM 도 안 불렀다 — 파일만 읽었다.
읽은 것은 `ontology/_init/ontology.yaml`(= `ontology/ontology.yaml`, 동일) ·
`workflows/static/_init/menu/menu.yaml`(= 운영본, 동일) · `/tmp/tools.json`
(2026-08-22 15:49 에 받아둔 것, 도구 42개) · `/tmp/after2_1.txt`.

#### 1-A. 앞토막이 겹치는 묶음 (menu 48문장)

| 앞 몇 자 | 묶음 총수 | 2개 이상인 묶음 | 그 안에 든 문장 |
|---|---|---|---|
| 10자 | 18 | 6 | 36 / 48 |
| 20자 | 21 | 5 | 32 / 48 |
| 30자 | 22 | 4 | 30 / 48 |
| 40자 | 39 | 3 | 12 / 48 |

**40자를 읽어야 39묶음이 된다.** 30자로는 22묶음뿐이고 48문장 중 30개가
남과 같은 앞토막을 쓴다.

| 앞 10자 | 수 | recipe |
|---|---|---|
| `말한 장소로 장소 ` | 21 | 001 022 023 024 025 026 027 028 029 030 031 032 033 034 035 036 044 045 046 047 048 |
| `말한 장소로 구간 ` | 5 | 002 037 038 039 040 |
| `말한 키워드로 지도` | 4 | 012 013 042 043 |
| `말한 키워드로 이름` | 2 | 004 007 |
| `말한 키워드로 인터` | 2 | 006 041 |
| `말한 식별자로 행정` | 2 | 019 020 |

| 앞 30자 | 수 | recipe |
|---|---|---|
| `말한 장소로 장소 이름으로 위치 좌표와 지도 범위를 찾` | 21 | 위와 같음 |
| `말한 장소로 구간 이름으로 철도 선형 좌표와 지도 범위` | 5 | 002 037 038 039 040 |
| `말한 키워드로 지도 범위와 지역, 충전기 유형으로 전기` | 2 | 012 042 |
| `말한 키워드로 지도 범위와 지역으로 전기차 충전기를 조` | 2 | 013 043 |

| 앞 40자 | 수 | recipe |
|---|---|---|
| `…찾고 좌표가 속한 시` | 5 | 023 033 044 045 046 |
| `…찾고 지도 범위와 지` | 4 | 035 036 047 048 |
| `…찾고 좌표가 속한 제` | 3 | 027 029 031 |

#### 1-B. 21개짜리 묶음은 어디서 갈리는가

21문장의 공통 접두는 **30자**다 — `말한 장소로 장소 이름으로 위치 좌표와 지도 범위를 찾`.
뗀 나머지를 나란히 둔다. 갈리는 자리는 31번째 글자 하나(`는` 대 `고`)이고,
그 뒤로는 20문장이 전부 `고 ` 로 이어진다.

| recipe | 30자를 뗀 나머지 |
|---|---|
| 001 | `는다.` |
| 022 | `고 이름이나 코드, 지도 범위로 시도와 시군구, 읍면동 경계를 조회한다.` |
| 023 | `고 좌표가 속한 시도와 시군구, 읍면동 계층을 찾는다.` |
| 024 | `고 VWorld 에서 광역시도와 시군구, 읍면동, 리 경계를 내려받는다.` |
| 025 | `고 지도 범위 안의 CCTV 목록을 조회한다.` |
| 026 | `고 노선 이름이나 역 이름으로 철도 노선 선형을 조회한다.` |
| 027 | `고 좌표가 속한 제22대 국회의원 지역구를 찾는다.` |
| 028 | `고 제22대 국회의원 전체 선거구를 공약 수와 분야 집계와 함께 검색한다.` |
| 029 | `고 좌표가 속한 제22대 국회의원 전체 선거구를 찾는다.` |
| 030 | `고 당선인과 정당, 공약 분야로 제22대 국회의원 선거구 공약을 검색한다.` |
| 031 | `고 좌표가 속한 제22대 국회의원 선거구의 공약을 찾는다.` |
| 032 | `고 2026 지방선거 시도별 대표 인물과 정당, 교통 공약 주제를 검색한다.` |
| 033 | `고 좌표가 속한 시도의 2026 지방선거 교통 공약 요약을 찾는다.` |
| 034 | `고 시도와 시군구, 읍면동 단위로 주민등록 인구를 집계해 조회한다.` |
| 035 | `고 지도 범위와 지역, 충전기 유형으로 전기차 충전소를 검색한다.` |
| 036 | `고 지도 범위와 지역으로 전기차 충전기를 조회한다.` |
| 044 | `고 좌표가 속한 시도와 시군구, 읍면동 계층을 찾고 시도 코드나 이름으로 2026 지방선거 교통 공약 요약 한 건을 조회한다.` |
| 045 | `고 좌표가 속한 시도와 시군구, 읍면동 계층을 찾고 행정구역 코드로 0세부터 110세까지 성별 연령 인구 구성을 조회한다.` |
| 046 | `고 좌표가 속한 시도와 시군구, 읍면동 계층을 찾고 행정구역 코드로 월별 총인구와 유소년·생산연령·고령인구 추이를 조회한다.` |
| 047 | `고 지도 범위와 지역, 충전기 유형으로 전기차 충전소를 검색하고 충전소 번호로 충전소 정보와 충전기별 현재 상태를 조회한다.` |
| 048 | `고 지도 범위와 지역으로 전기차 충전기를 조회하고 충전소 번호로 충전소 정보와 충전기별 현재 상태를 조회한다.` |

**044 · 045 · 046 셋은 57자까지 같다** (`…찾고 좌표가 속한 시도와 시군구, 읍면동 계층을 찾`).
**047 · 048 은 끝 34자가 같다** (`충전소 번호로 충전소 정보와 충전기별 현재 상태를 조회한다.`).

5개짜리 묶음(구간)의 공통 접두는 **33자** — `말한 장소로 구간 이름으로 철도 선형 좌표와 지도 범위를 찾`.

| recipe | 33자를 뗀 나머지 |
|---|---|
| 002 | `는다.` |
| 037 | `고 이름이나 코드, 지도 범위로 시도와 시군구, 읍면동 경계를 조회한다.` |
| 038 | `고 VWorld 에서 광역시도와 시군구, 읍면동, 리 경계를 내려받는다.` |
| 039 | `고 지도 범위 안의 CCTV 목록을 조회한다.` |
| 040 | `고 노선 이름이나 역 이름으로 철도 노선 선형을 조회한다.` |

#### 2. 실패한 발화 다섯이 무엇과 헷갈렸나

출처는 `/tmp/after2_1.txt` (발화 9개 × 10회 · recipe 48 · qwen3:32b).
기대값은 `tools/check_resolve.py` 의 `CANDIDATES` 다.

| # | 발화 | 기대 | 실제 후보 | 기대 대 후보 공통 접두 |
|---|---|---|---|---|
| 4 | 청주시 인구 구성 알려줘 | 045 | {034, 045, 046} 10회 | 045↔034 **32자** · 045↔046 **68자** |
| 5 | 오송역 근처 충전소 찾아줘 | 035 | {035, 036} 9회 | 035↔036 **41자** |
| 6 | 국회의원 선거구 찾아줘 | {007,008,009,010} | {007,008} 9회 · {007,008,009} 1회 | 넷이 서로 **8자** (`말한 키워드로 `) |
| 7 | 전기차 충전소 데이터 검색해줘 | 012 | {012,013} 9회 · {012,013,035,036} 1회 | 012↔013 **17자** · 012↔035 **3자** |
| 8 | 철도 안전 문서 찾아줘 | 014 | {006} 10회 | 014↔006 **8자** |

문장 전문.

```
4  기대 045  말한 장소로 … 찾고 좌표가 속한 시도와 시군구, 읍면동 계층을 찾고 행정구역 코드로 0세부터 110세까지 성별 연령 인구 구성을 조회한다.
   후보 034  말한 장소로 … 찾고 시도와 시군구, 읍면동 단위로 주민등록 인구를 집계해 조회한다.
   후보 046  말한 장소로 … 찾고 좌표가 속한 시도와 시군구, 읍면동 계층을 찾고 행정구역 코드로 월별 총인구와 유소년·생산연령·고령인구 추이를 조회한다.

5  기대 035  말한 장소로 … 찾고 지도 범위와 지역, 충전기 유형으로 전기차 충전소를 검색한다.
   후보 036  말한 장소로 … 찾고 지도 범위와 지역으로 전기차 충전기를 조회한다.

6  기대 007  말한 키워드로 이름이나 시도, 선거구 코드로 제22대 국회의원 지역구를 검색한다.
   기대 008  말한 키워드로 제22대 국회의원 전체 선거구를 공약 수와 분야 집계와 함께 검색한다.
   기대 009  말한 키워드로 당선인과 정당, 공약 분야로 제22대 국회의원 선거구 공약을 검색한다.
   기대 010  말한 키워드로 2026 지방선거 시도별 대표 인물과 정당, 교통 공약 주제를 검색한다.
   후보는 기대의 부분집합이다 — 009 · 010 이 안 올라온다.

7  기대 012  말한 키워드로 지도 범위와 지역, 충전기 유형으로 전기차 충전소를 검색한다.
   후보 013  말한 키워드로 지도 범위와 지역으로 전기차 충전기를 조회한다.
   1회는 035 · 036 까지 붙었다 — 012 와 035 는 앞 3자만 같고 뒤 32자가 통째로 같다.

8  기대 014  말한 키워드로 지식베이스에 쌓인 문서에서 물어본 내용을 검색한다.
   후보 006  말한 키워드로 인터넷에서 최신 공개 정보를 검색한다.
```

**발화 8 은 성격이 다르다.** 앞토막이 8자밖에 안 겹치는데도 틀렸고, 후보가
1개(006)로 좁혀져 SELECT 까지 갔다. 그리고 **기대값 자체가 잠정이다** —
「열린 과제」에 그대로 적혀 있다: `"철도 안전 문서 찾아줘"` 가 `recipe_006`
(웹 검색)으로 가는 것이 틀린 것인지 GT 가 틀린 것인지 아직 안 정했다.
`search_documents`(`knowledge.query`)는 지식베이스 문서가 0개라 막힌 노드
여덟 중 하나이기도 하다.

**발화 6 도 성격이 다르다.** 앞토막이 8자뿐인데 넷 중 둘만 올라온다.
겹침이 아니라 다른 이유다.

#### 3. description 이 도구의 능력을 빠뜨린 곳

도구 노드 39개(`hasOutput` 을 내놓는 노드) 전부를 `/tmp/tools.json` 의
`inputSchema` 와 맞댔다. 배선(`TOOL_OF`)이 있는 노드는 21개뿐이고 나머지 18개는
**도구 이름을 이름 모양으로 맞춘 추정**이다 — 표에 `추정` 으로 적었다.

세는 방법: 입력 칸의 `description` 에서 한글 낱말(2자 이상)을 뽑아 노드
`description` 안에 하나라도 나오는지 본다. 나오면 「있음」, 안 나오면 「빠져봄」.
`lon` `lat` `limit` `bbox` `minLon…maxLat` `includeGeometry` `simplifyM`
`zoom` 같은 배관 칸은 능력이 아니므로 뺐다. 입력 칸에 설명 자체가 없어 판단이
안 되는 것은 「설명없음」으로 따로 적었다.

**39개 중 23개**에 「빠져봄」이나 「설명없음」이 있다. 그중 **20개**는 설명이
있는 칸인데 노드 description 이 그 말을 안 한다.

| 배선 | 노드 | 도구 | 빠져 보이는 입력 칸 | 설명 없는 칸 |
|---|---|---|---|---|
| 배선 | `search_ev_stations` | `ev.searchStations` | query, zcode, zscode, status, availableOnly, pageNo, includeItems, includeDataset | – |
| 배선 | `search_ev_chargers` | `ev.searchChargers` | query, zcode, zscode, statId, availableOnly, pageNo | – |
| 배선 | `get_railway_lines` | `geo.getRailwayLines` | stationName | includeStations |
| 배선 | `get_vworld_boundaries` | `vworld.getAdministrativeBoundaries` | layer, query, propertyNames, startIndex, filter | srsName |
| 배선 | `search_admin_boundaries` | `adminBoundary.searchBoundaries` | layer, includeItems, includeBbox, includeDataset, includeSourceProperties | – |
| 배선 | `search_population_statistics` | `population.searchStatistics` | referenceMonth, query, code, includeItems, includeBbox | order |
| 배선 | `search_assembly_pledge_districts` | `election.searchAssemblyPledgeDistricts` | sido, sig, winner, party, all | – |
| 배선 | `search_election_districts` | `election.searchDistricts` | query, sido | – |
| 배선 | `search_assembly_districts` | `election.searchAssemblyDistricts` | sido | – |
| 배선 | `search_local_pledge_summaries` | `election.searchLocalPledgeSummaries` | sido, party | – |
| 배선 | `find_assembly_district_by_point` | `election.findAssemblyDistrictByPoint` | query, hasPledges, pledgeCategory | – |
| 배선 | `web_search` | `web.search` | query, freshness, domains | – |
| 배선 | `geocode_place` | `geo.geocode` | – | query |
| 배선 | `get_railway_section` | `rail.getSectionGeometry` | – | sectionName |
| 배선 | `search_documents` | `knowledge.query` | – | query, k, filter_docs |
| 추정 | `get_population_trend` | `population.getTrend` | startMonth, endMonth | level |
| 추정 | `get_age_profile` | `population.getAgeProfile` | referenceMonth | level |
| 추정 | `get_assembly_district` | `election.getAssemblyDistrict` | name | – |
| 추정 | `get_assembly_pledge_district` | `election.getAssemblyPledgeDistrict` | name | – |
| 추정 | `get_election_district` | `election.getDistrict` | name | – |
| 추정 | `get_local_pledge_summary` | `election.getLocalPledgeSummary` | sidoName | – |
| 추정 | `admin_boundary_dataset_info` | `adminBoundary.getDatasetInfo` | layer | – |
| 추정 | `web_fetch` | `web.fetch` | url | – |

나머지 16개 노드는 「빠져봄」이 0이다.

**미리 알던 둘의 확인.**

```
search_ev_stations   query("충전소명, 주소, 운영기관 키워드")가 빠져 있다.  확인됨.
                     description 은 "지도 범위와 지역, 충전기 유형으로" 다.
                     zcode·zscode(시도·시군구 코드)가 "지역" 에 겹쳐 「있음」으로
                     세지 않았다 — 낱말이 안 겹쳐 「빠져봄」에 들어갔다.
get_railway_lines    **전제가 지금 파일과 다르다.** description 은
                     "노선 이름이나 역 이름으로 철도 노선 선형을 조회한다." 로
                     역 이름을 이미 말한다. railwayName 도 「있음」이다.
                     실제로 빠진 것은 stationName 쪽 낱말("역명")이고,
                     이것은 "역 이름" 과 뜻이 같아 **거짓 양성일 수 있다.**
```

**이 세는 방법의 한계.** 낱말이 겹치는지만 본다. 뜻이 같고 낱말이 다르면
(`역명` 대 `역 이름`) 「빠져봄」으로 잘못 잡히고, 낱말만 스치면(`검색어` 의
`검색`) 「있음」으로 잘못 넘어간다. **판정이 아니라 목록이다.** 한 줄씩
사람이 본다.

#### 4. 문장이 몇 자이고 어디가 무거운가

```
menu.yaml 파일 전체      4,268자   (MENU_BUDGET 6,000 — 테스트가 보는 값)
48문장만 합치면          2,805자
평균 58.4자 · 중앙값 58자
```

| 가장 긴 다섯 | 자수 |
|---|---|
| 044 | 100 |
| 047 | 99 |
| 046 | 99 |
| 045 | 98 |
| 048 | 91 |

| 가장 짧은 다섯 | 자수 |
|---|---|
| 006 | 29 |
| 001 | 33 |
| 013 | 34 |
| 002 | 36 |
| 014 | 36 |

세 문장 이상에 나오는 8자 이상 구절 (겹치는 것은 가장 긴 것만 남김).

| 문장 수 | 자수 | 구절 |
|---|---|---|
| 26 | 13 | ` 좌표와 지도 범위를 찾` |
| 24 | 15 | ` 좌표와 지도 범위를 찾고 ` |
| 21 | 30 | `말한 장소로 장소 이름으로 위치 좌표와 지도 범위를 찾` |
| 20 | 32 | `말한 장소로 장소 이름으로 위치 좌표와 지도 범위를 찾고 ` |
| 14 | 8 | `말한 키워드로 ` |
| 12 | 12 | `시도와 시군구, 읍면동` |
| 11 | 11 | ` 제22대 국회의원 ` |
| 9 | 14 | ` 시도와 시군구, 읍면동 ` |
| 8 | 39 | `말한 장소로 장소 이름으로 위치 좌표와 지도 범위를 찾고 좌표가 속한 ` |
| 8 | 10 | ` 지도 범위와 지역` |
| 8 | 9 | `으로 전기차 충전` |
| 7 | 12 | `로 제22대 국회의원 ` |
| 7 | 9 | ` 이름으로 철도 ` |
| 7 | 8 | `말한 식별자로 ` |
| 6 | 20 | ` 좌표와 지도 범위를 찾고 지도 범위` |
| 5 | 41 | `말한 장소로 … 찾고 좌표가 속한 시도` |
| 5 | 37 | `말한 장소로 … 찾고 지도 범위` |
| 5 | 34 | ` 충전소 번호로 충전소 정보와 충전기별 현재 상태를 조회한다.` |
| 5 | 33 | `말한 장소로 구간 이름으로 철도 선형 좌표와 지도 범위를 찾` |
| 5 | 11 | ` 2026 지방선거 ` |
| 4 | 57 | `말한 장소로 … 찾고 좌표가 속한 시도와 시군구, 읍면동 계층을 찾` |
| 4 | 36 | `하고 충전소 번호로 충전소 정보와 충전기별 현재 상태를 조회한다.` |
| 4 | 32 | ` 지도 범위와 지역, 충전기 유형으로 전기차 충전소를 검색` |
| 3 | 59 | `말한 장소로 … 찾고 좌표가 속한 시도와 시군구, 읍면동 계층을 찾고 ` |
| 3 | 39 | ` 이름이나 코드, 지도 범위로 시도와 시군구, 읍면동 경계를 조회한다.` |
| 3 | 39 | ` VWorld 에서 광역시도와 시군구, 읍면동, 리 경계를 내려받는다.` |
| 3 | 31 | ` 노선 이름이나 역 이름으로 철도 노선 선형을 조회한다.` |

**48문장 중 26개가 같은 13자를 갖고 있다.** 자수(2,805)는 예산(6,000)의
절반도 안 쓴다 — 지금 좁은 것은 자리가 아니라 변별력이다. 「건드리기 전에
읽을 것」의 `MENU_BUDGET` 줄이 적어둔 그대로다.

#### 사람이 정할 것

**여기에는 답을 안 적는다. 질문만 있다.**

1. `034` · `045` · `046` 을 무엇으로 가를 것인가. 045↔046 은 68자가 같고
   끝의 "성별 연령 인구 구성" 대 "월별 총인구와 … 추이" 만 다르다.
   045↔034 는 32자가 같다.
2. `035` · `036` (그리고 `047` · `048`)을 무엇으로 가를 것인가. 41자가 같고
   "충전소" 대 "충전기" 한 글자다. 「열린 과제」의 `recipe_035` 항목과 같은 자리다.
3. `007` · `008` · `009` · `010` 은 앞토막이 8자밖에 안 겹치는데 왜 넷 중
   둘만 후보에 오르는가. `description` 문제인가, `about` · shortlist 문제인가.
4. `012` · `013` 을 무엇으로 가를 것인가. 그리고 `012`(키워드)와
   `035`(장소)가 뒤 32자를 통째로 공유하는 것을 놔둘 것인가.
5. 발화 8 의 기대값을 `014`(지식베이스)로 둘 것인가 `006`(웹 검색)으로
   옮길 것인가. `search_documents` 가 0건인 것과 함께 정해야 한다.
6. 21문장의 공통 30자(`말한 장소로 장소 이름으로 위치 좌표와 지도 범위를 찾`)를
   어떻게 할 것인가. `geocode_place` 의 description 하나가 21문장의 앞
   30자를 만든다.
7. 3절의 「빠져봄」 20개 중 무엇이 진짜 빠진 능력이고 무엇이 낱말만 다른
   거짓 양성인가. 특히 `search_ev_stations` 의 `query` 와
   `search_documents` · `geocode_place` · `get_railway_section` 의
   설명 없는 `query` · `sectionName`.
8. 배선이 없는 노드 18개의 도구 이름을 표로 적어둘 것인가. 지금은 이름
   모양으로 맞춘 추정이라 3절의 절반이 확인되지 않은 채다.

### 2026-08-23 (일곱째) · 온톨로지 개편 8안을 실제로 넣음 · 네 커밋

**왜 했는가.** 여섯째 항목에서 8안이 확정됐다. `record_key`("식별자") 하나가
자릿수도 발급처도 다른 값 셋을 묶고 있었고, 그래서 행정구역 코드가 충전소
상세로, 충전소 번호가 연령별 인구 구성으로 흘러가는 **가짜 경로 넷**이
만들어졌다 — 실행하면 반드시 터지는 경로다.

```
행정구역 코드   43113 (5자리).  find_admin_boundary_by_point 가 내놓는다
선거구 코드     4311101 (7자리)
충전소 번호     ev_station_PW012325.  ev.searchStations 도 내놓는다
```

3안(말한 식별자를 코드 하나에만 거는 것)은 018~021 을 함께 죽인다.
8안은 말한 식별자에 코드 셋 모두를 걸어 그 넷을 살리고 가짜만 없앤다.

네 커밋으로 나눴다. 판정이 달라졌을 때 무엇 탓인지 갈리게 하려는 것이다.

```
08e8a8e  0건일 때 그 이유를 답에 적는다              개편 전에 있던 것
2f8b926  식별자 타입을 셋으로 쪼개 가짜 경로 넷을 없앤다
ce13dfb  GT 를 개편 뒤 번호로 옮긴다
(이 커밋) 배선을 노드와 입력 타입의 짝마다 한 줄로
```

되돌리는 길은 `git reset --hard before-ontology-rework` 하나다.

#### ① 온톨로지를 쪼갠 전후

**시뮬레이션 숫자와 실제가 한 자리도 다르지 않았다.**

```
                    여섯째의 시뮬레이션    실제
남은 recipe         48개                   48개
전체 경로           56개 · 버림 8개        56개 · 버림 8개
menu.yaml           약 4,267자             4,268자 (MENU_BUDGET 6000)
```

recipe 번호 대응표 전문. **041 까지는 그대로이고 042 부터 밀렸다.**

```
새 번호     옛 번호      경로
001~041     같음         (변화 없음)
042         (새것)       말한 키워드 → 전기차 충전소 검색 → 충전소 상세 조회
043         (새것)       말한 키워드 → 전기차 충전기 조회 → 충전소 상세 조회
044         045          말한 장소 → 좌표 → 지점 행정구역 판별 → 지방선거 교통 공약 상세
045         046          말한 장소 → 좌표 → 지점 행정구역 판별 → 연령별 인구 구성 조회
046         047          말한 장소 → 좌표 → 지점 행정구역 판별 → 인구 변화 추이 조회
047         (새것)       말한 장소 → 좌표 → 전기차 충전소 검색 → 충전소 상세 조회
048         (새것)       말한 장소 → 좌표 → 전기차 충전기 조회 → 충전소 상세 조회

사라진 넷   옛 042 · 043 · 044 · 048.  전부 가짜다
살아남은 것 025(좌표 → CCTV) · 039(철도 구간 → CCTV) · 018~021(말한 식별자 2단)
```

새로 생긴 넷의 근거는 `ev.searchStations` 응답의 `items[0].id` 다. 검색이
충전소 번호를 내놓으므로 검색 → 상세가 진짜 경로다.

`pytest` 173 passed / 1 failed. 실패 1건은 graphviz 버전 차이에서 오는 음성
대조군이고 개편과 무관하다.

#### ② GT 를 새 번호로 옮긴 뒤의 세 묶음

**기대값은 한 글자도 안 바꿨다. 번호만 옮겼다.** 사슬이 같은 recipe 를 찾아
그 새 번호를 넣었다. 사라진 사슬은 없다. 움직인 것은 발화 4 하나뿐이다
(`recipe_046` → `recipe_045`).

```
발화                        기준선(밤)   개편 뒤
1 오송역 위치 보여줘        10/10        10/10
2 오송역 좌표 알려줘        10/10        10/10
3 오송역 CCTV 보여줘        10/10        10/10
4 청주시 인구 구성 알려줘   10/10         0/10   ★ 떨어졌다
5 오송역 근처 충전소 찾아줘  0/10         1/10   ★ 올랐다
6 국회의원 선거구 찾아줘     0/10         0/10
7 전기차 충전소 데이터 검색  0/10         0/10
8 철도 안전 문서 찾아줘      0/10         0/10
9 충북 제1선거구 알려줘     10/10        10/10
                            ─────        ─────
                            50/90        41/90
                            56%          46%

qwen3:32b · 발화 9개 × 10회 · _init(recipe 48) · 묶음 셋
세 묶음이 md5 까지 같다. 기준선도 그랬다.
```

**판정이 열 점 떨어졌다. 전부 발화 4 한 건이다.**

```
            LLM 후보 수   조회 후보 수   status
기준선      1             3              SELECT   10회
개편 뒤     3             3              CLARIFY  10회
```

**조회 후보 수는 3 으로 그대로다.** 온톨로지가 후보를 넓힌 것이 아니다.
LLM 이 `{034, 045, 046}` 을 셋 다 내놓아 SELECT 가 CLARIFY 로 바뀌었다.
셋은 인구 통계 조회 · 연령별 인구 구성 · 인구 변화 추이이고 "인구 구성" 이라는
말만으로는 실제로 갈리지 않는다. menu 문장의 번호가 바뀌면서 LLM 이 다르게
답한 것으로 본다.

발화 6 의 튀는 한 번은 후보 14개에서 3개로 줄었다(`{007, 008, 009, 015, 016,
017, 027, 028, 029, 030, 031, 042, 043, 044}` → `{007, 008, 009}`).
발화 7 의 조회 후보 수는 2 에서 4 로 늘었다.

**숫자를 좋게 만들려고 기대값이나 규칙을 고치지 않았다.** 이것이 개편 직후의
사실이다. `description` 을 사람이 손보면 다시 잰다.

#### ③ 배선을 (노드 × 입력 타입)당 한 줄로

`STEP_OF` 가 노드당 한 줄이라 같은 노드가 두 자리에 올 때 한쪽을 버렸다.
키를 `(노드 id, 입력 타입 id)` 로 바꾸고 온톨로지의 `hasInput` 선언과 1:1 로
맞췄다. 도구 이름과 답 첫 줄은 노드당 하나이므로 `TOOL_OF` 로 갈랐다 — 한 표에
두면 줄마다 복사된다.

```
                          전         후
TOOL_OF (노드)            —          21줄
STEP_OF (노드 × 타입)     21줄       31줄
배선이 다 있는 recipe     34개       38개
check_wiring  A           2          0
              B           11         0
              C (새 규칙) —          7
pytest                    173/1      175/1
```

C 는 "선언에는 있는데 배선이 없는 자리" 다. **0 이어야 하는 것이 아니다.**
응답 모양을 못 본 자리는 지어내지 않고 비워 두는 것이 규칙이다.

```
web_fetch × 웹 주소                     web.search 가 권한에 막혀 응답 모양을
                                        한 번도 못 봤다. 예전 {url: @arg} 는
                                        발화를 URL 로 쓰는 것이라 지웠다
get_election_district           × 선거구 코드
get_assembly_district           × 선거구 코드   선거구 코드를 내놓는 도구가 없다.
get_assembly_pledge_district    × 선거구 코드   name 칸으로 보내는 것이 맞을 수
                                                있으나 안 눌러봤다
get_local_pledge_summary        × 행정구역 코드
get_age_profile                 × 행정구역 코드  findBoundaryByPoint 가 코드를 어느
get_population_trend            × 행정구역 코드  필드로 내놓는지 아직 모른다(0건)
```

**오늘 실측이 준 것 셋.**

```
find_cctv   앞이 철도 구간 조회면   bbox 넷을 그대로.  실측 bbox
                                    [[126.868587, 36.619576], [127.328115, 37.554557]]
            앞이 geocode 면         좌표 + 반경 15km.  geocode bbox 는 한 변이
                                    1km 라 그걸 쓰면 0건
ev.getStation 의 statId 는 stationId 이지 id 가 아니다
            statId="PL033780"             item 이 온다
            statId="ev_station_PL033780"  item null · 0건 ·
                                          "충전소 …를 찾지 못했거나 DB 연결을…"
            tools/probe_out/ev.getStation.statId-stationId.json
            tools/probe_out/ev.getStation.statId-id.json 이 그 둘이다
ev.searchStations · searchChargers 의 inputSchema 에 query 가 있다
            "충전소명, 주소, 운영기관 키워드".  말한 키워드가 갈 자리가 여기다
```

**범위 밖으로 나간 것 하나.** ①에서 안 적은 온톨로지 한 줄을 ③에서 더했다.

```
- { from: find_cctv, to: point, predicate: hasInput }
```

받는 것이 하나면 배선 줄도 하나뿐이라 철도 뒤와 geocode 뒤를 못 가른다.
`road.getCctv` 의 required 는 bbox 넷이지만 노드는 좌표도 받는다 — 예전부터
`location + radiusMeters` 로 그렇게 불러 왔고 선언만 빠져 있던 것이다.
이 줄로 recipe 는 하나도 안 바뀐다(`rebuild_init` 미리보기로 확인. 좌표를
내놓는 것이 `geocode_place` 하나뿐이라 이을 자리가 안 늘어난다).

**진행 표시 판정을 답과 맞췄다.** `execute_service` 가 `item["error"]` 만 봤다.
Gateway 는 실패를 `200` + `{"error": {...}}` 로도 돌려주고 그것은
`item["result"]` 에 담기므로 화면이 "완료" 라고 찍었다. **그 탓에 "대전~김천
구간이 유효하다" 는 틀린 사실이 문서에 박혔다**(아래 2026-08-22 넷째의 정정).
`workflow_answer.step_failed` 를 새로 두고 `_outcome` · `_verdict` · 진행 표시
셋이 그것을 부르게 했다. **0건은 실패로 보지 않는다** — 호출은 끝났고 결과가
없는 것뿐이고 답 문구가 이미 "찾지 못했습니다" 로 말한다.
`tests/vendor/test_workflow_answer.py` 가 열둘에서 열넷이 됐다.

배선을 바꾼 뒤 세 묶음을 다시 쟀다(`/tmp/after2_*`). **②의 세 묶음과 md5 까지
같다.** 배선은 판정과 무관하다는 것이 이것으로 확인됐다.

#### 안 한 것

`description` 과 `grounding.yaml` 은 손대지 않았다. 사람이 한다.

「열린 과제」에서 「온톨로지의 식별자 타입이 셋을 하나로 묶고 있다」와
「배선이 온톨로지의 선언을 반만 따른다 — 34개 중 13개」를 지웠다. 이번에
해결됐다. 아래 2026-08-23 (넷째)에 그 절을 가리키는 줄이 남아 있는데
**측정 기록은 한 줄도 안 지우는 것이 규칙이라 그대로 뒀다.**

`search_ev_stations` · `search_ev_chargers` 의 「지도 범위」 줄은 실제로는
좌표를 받아 반경으로 넓힌다(`center` + `radiusMeters`). `find_cctv` 와 달리
철도 뒤에 올 일이 없어(대상이 어긋나 걸러진다) 줄이 갈릴 이유가 없었다.
선언과 어긋난 자리로 남아 있다.

### 2026-08-23 (여섯째) · 8안 하나를 더 잼 · 철도 적재 뒤의 실측 · 재기만 함

**왜 쟀는가.** 어젯밤 일곱 안에 없던 조합 하나가 남아 있었다.
3안(식별자 셋 + `ev` 가 번호를 내놓음)에서 `spoken_identifier` 의 `is-a` 만
셋으로 넓힌 것이다. **어느 안이 낫다는 판단은 여기 안 적는다.**

```
3안   spoken_identifier is-a district_code                        하나만
8안   spoken_identifier is-a admin_code · district_code · station_id  셋 다
```

**장소 이름은 안 쪼갰다.** 오늘 철도 적재로 근거가 사라졌다 — 아래 실측 ④.

**어떻게 쟀는가.** 어젯밤과 같다. `/tmp/rework_sim/run.py` 에 `V8` 갈래만 더했다.
`rebuild(write=False)` · 온톨로지 경로 둘만 교체 · `--write` 안 붙임.

```
                     V0(지금)   V3         V8
전체 경로              54        52         56
버린 수                 6         8          8
  그중 2단 미만          0         0          0
남은 recipe            48        44         48
새로 생긴 것            0         4          4
사라진 recipe           0         8          4
                                018·019·020·021    042·043·044·048
                                042·043·044·048
menu 자수            4315      3962       4267    (MENU_BUDGET 6000)
앞토막 충돌 묶음         2         4          4
최대 묶음              23        21         21
```

최대 앞토막은 셋 다 같다 : `말한 장소로 장소 이름으로 위치 좌표와 지도 범위를 찾`.
노드 수 V0 58 · V3 60 · V8 60. `MAX_STEPS` 4.

**사슬 판정 넷.**

```
                              V0      V3      V8
가짜 넷이 남았나             4개남음  0 사라짐  0 사라짐
search_ev_stations→get_ev_station   0       2       2
search_ev_chargers→get_ev_station   0       2       2
get_railway_section→find_cctv       1       1       1
025  spoken_place→geocode_place→find_cctv   있음   있음   있음
```

**3안에서 죽었던 넷이 8안에서 살아났는가** (`spoken_identifier` 로 시작하는 2단).

```
             V0   V3   V8
recipe_018    O    X    O    말한 식별자 → 지방선거 교통 공약 상세
recipe_019    O    X    O    말한 식별자 → 연령별 인구 구성 조회
recipe_020    O    X    O    말한 식별자 → 인구 변화 추이 조회
recipe_021    O    X    O    말한 식별자 → 충전소 상세 조회
```

8안에서 새로 생긴 넷은 이것이다.

```
말한 키워드 → 전기차 충전소 검색 → 충전소 상세 조회
말한 키워드 → 전기차 충전기 조회 → 충전소 상세 조회
말한 장소 → 장소 좌표 변환 → 전기차 충전소 검색 → 충전소 상세 조회
말한 장소 → 장소 좌표 변환 → 전기차 충전기 조회 → 충전소 상세 조회
```

**8안은 어젯밤 V7 과 글자 그대로 같은 온톨로지다.** `run.py` 의 `V7` 갈래가 이미
`split_identifier(셋) + EV_SEARCH_EDGES` 였다. 어젯밤에 이미 잰 조합이고,
숫자도 그대로 나온다. 새로 잰 것은 018~021 살아남 판정뿐이다.

#### 철도 적재 — 오늘 한 일

```
적재      docker exec krri_asap-mcp-1 python scripts/import_railways.py
          Imported 2243 railway sections from /app/2022_rail_utmk_updated.geojson
          rail 스키마가 새로 생겼다. 전에는 스키마 자체가 없었다
```

**눌러본 결과 (실측).**

```
"대전~김천"     NOT_FOUND        그런 이름은 DB 에 없다
"오송역"        성공  coords 18  bbox [[126.8686, 36.6196], [127.3281, 37.5546]]
"서울역"        성공  coords 10
"경부선"        성공  ★ 서울역과 sectionId 가 같다
"경부선(고속)"  성공  ★ 역시 같다
응답 칸 : sectionId · geometry(MultiLineString) · bbox
location 칸은 없다
```

**무엇이 갈렸나.**

```
① location 이 없다. recipe_039 의 $prev.location 배선은 못 쓴다
② bbox 가 [[a,b],[c,d]] 중첩형이라 vendor 의 $prev.minLon 이 푼다.
   find_cctv 를 bbox 넷으로 배선하면 「구간 → CCTV」가 바로 돈다
③ geocode 의 bbox 는 한 변 1km 라 그것으로 CCTV 를 뽑으면 0건이다.
   같은 find_cctv 인데 앞이 geocode 면 좌표+반경, 앞이 구간 조회면 bbox 다.
   노드당 한 줄로는 못 적는다는 실측 사례다
④ 역 이름으로도 노선 이름으로도 같은 한 건이 나온다.
   rail_repository 가 f_name · t_name · r_name_1~3 을 ILIKE 로 훑고 LIMIT 1 이다.
   「구간 이름」이 별도 타입으로 안 갈린다
⑤ sectionId 는 sha256 해시다. 사람이 말할 수 있는 값이 아니다
```

**정정.** 옛 기록의 「`"오송역"` 은 역 이름이지 구간명이 아니다 · 구간명 하나를
알면 다시 재야 한다」(이 문서 아래쪽 2026-08-22 · 2026-08-21 대목)는 **틀렸다.**
적재 뒤 실측에서 `"오송역"` 은 성공한다. 옛 오류는 인자 탓이 아니라 `rail.sections`
테이블 자체가 없었기 때문이다. 구간을 「출발역~도착역」으로 식별한다는 이해도
틀렸다 — `"대전~김천"` 은 NOT_FOUND 이고, 역 이름 하나 · 노선 이름 하나가
똑같이 통한다. 옛 기록은 지우지 않고 그 자리에 둔다.

### 2026-08-23 (다섯째) · 온톨로지 타입 쪼개기 일곱 안을 미리 잼 · 재기만 함

**왜 쟀는가.** 개편에서 `record_key` 와 `place_name` 을 쪼갤 참인데, 쪼갰을 때
경로가 몇 개가 되는지 아무도 몰랐다. 고르기 전에 숫자를 본다.
**어느 안이 낫다는 판단은 여기 안 적는다.** 재기만 했다.

**어떻게 쟀는가.** `tools/rebuild_init.py` 의 `rebuild(write=False)` 를 그대로
돌렸다. 바꾼 것은 그것이 읽는 온톨로지 경로 둘(`paths.ONTOLOGY_PATH` ·
`paths.INIT_ONTOLOGY_PATH`)뿐이다.

```
변형 온톨로지   /tmp/rework_sim/V*.yaml   버릴 파일이라 yaml.dump 로 다시 씀
실행 스크립트   /tmp/rework_sim/run.py    변형마다 새 파이썬 프로세스
INIT_RECIPES_DIR · INIT_MENU_YAML_PATH 는 진짜를 그대로 뒀다.
그래야 "사라진 recipe" 가 지금 48개와 맞대어진다.
```

저장소 파일은 이 NOTES.md 하나만 고쳤다. `ontology/ontology.yaml` ·
`ontology/_init/ontology.yaml` · recipe · menu · `STEP_OF` · 코드 전부 무수정이고
`--write` 는 한 번도 안 붙였다. (`tools/probe_out/` 는 `.gitignore` 라
아래 Gateway 응답 전문은 git 에 안 잡힌다.)

#### 변형 일곱이 무엇인가

```
V0  지금 그대로. 기준선
V1  식별자를 셋으로(admin_code · district_code · station_id).
    record_key 는 지움. spoken_identifier is-a 는 셋 다
V2  V1 인데 spoken_identifier is-a 는 district_code 하나만
V3  V2 + search_ev_stations · search_ev_chargers 가 station_id 를 내놓음
V4  장소 이름을 넷으로(place_label · station_name · line_name · section_name).
    place_name 은 지움. spoken_place is-a 는 place_label · station_name 둘
V5  V3 + V4
V6  V5 + 시작 데이터 노드 spoken_section (「말한 구간」) is-a section_name
```

#### 표 — 변형 일곱

```
변형  노드  전체   버림  2단   남은      새로    사라진   menu    앞토막   최대   묶음 크기
            경로         미만  recipe    생긴것  recipe   자수     묶음     묶음   변화
V0     58    54     6     0     48★        0       0     4,315★     2      23     기준
V1     60    50     6     0     44          0       4     3,815      2      19     -4
V2     60    46     6     0     40          0       8     3,510      2      19     -4
V3     60    52     8     0     44          4       8     3,962      4      21     -2
V4     61    43     0     0     43          0       5     3,860      1      23      0
V5     63    39     0     0     39          4      13     3,507      3      21     -2
V6     64    52     8     0     44          9      13     3,967      4      21     -2

★ V0 재현 확인. recipe 48개 · menu 미리보기 4,315자.
  앞토막 묶음 = 문장 앞 30자가 같은 것이 2개 이상인 묶음의 수
  최대 묶음   = 그중 가장 큰 것의 크기. 앞토막은 일곱 변형 모두 같다 —
                "말한 장소로 장소 이름으로 위치 좌표와 지도 범위를 찾"
  남은 recipe = 48 - 사라진 + 새로 생긴 것. 일곱 다 맞는다
```

사라진 recipe 를 이름으로 편 것.

```
recipe_002  말한 장소 → 철도 구간 형상 조회                         V4 V5
recipe_018  말한 식별자 → 지방선거 교통 공약 상세                    V2 V3 V5 V6
recipe_019  말한 식별자 → 연령별 인구 구성 조회                      V2 V3 V5 V6
recipe_020  말한 식별자 → 인구 변화 추이 조회                        V2 V3 V5 V6
recipe_021  말한 식별자 → 충전소 상세 조회                           V2 V3 V5 V6
recipe_037  말한 장소 → 철도 구간 형상 조회 → 행정구역 조회           V4 V5
recipe_038  말한 장소 → 철도 구간 형상 조회 → VWorld 행정경계 조회    V4 V5
recipe_039  말한 장소 → 철도 구간 형상 조회 → CCTV 조회               V4 V5
recipe_040  말한 장소 → 철도 구간 형상 조회 → 철도 노선 조회          V4 V5
recipe_042  말한 장소 → 좌표 → 지점 행정구역 판별 → 국회의원 지역구 상세        V1 V2 V3 V5 V6
recipe_043  말한 장소 → 좌표 → 지점 행정구역 판별 → 국회의원 전체 선거구 상세   V1 V2 V3 V5 V6
recipe_044  말한 장소 → 좌표 → 지점 행정구역 판별 → 국회의원 선거구 공약 상세   V1 V2 V3 V5 V6
recipe_048  말한 장소 → 좌표 → 지점 행정구역 판별 → 충전소 상세 조회           V1 V2 V3 V5 V6
```

V6 의 사라진 열셋 중 037~040 과 002 는 없어진 것이 아니라 **시작 데이터가
바뀐 것**이다. `말한 구간 → 철도 구간 형상 조회 → …` 넷과 `말한 구간 → 철도
구간 형상 조회` 하나가 새로 생겨 자리를 메운다. 사슬이 달라졌으므로 번호 대응은
끊긴다.

#### 사슬 판정 넷

```
                                  V0  V1  V2  V3  V4  V5  V6
가짜 넷이 사라졌는가              X   O   O   O   X   O   O
  (042·043·044·048 의 사슬)      4   0   0   0   4   0   0   ← 남은 개수
새 경로가 생겼는가                X   X   X   O   X   O   O
  search_ev_stations → get_ev_station   0   0   0   2   0   2   2
  search_ev_chargers → get_ev_station   0   0   0   2   0   2   2
구간 → CCTV 가 남았나             O   O   O   O   X   X   O
  (get_railway_section → find_cctv)
025 CCTV 가 살아 있나             O   O   O   O   O   O   O
  (spoken_place → geocode_place → find_cctv)
```

**025 는 일곱 변형 전부에서 살아 있다.** 시연에서 도는 유일한 경로가 어느 안에서도
안 죽는다.

#### 앞토막 충돌을 편 것

```
V0  23  말한 장소로 장소 이름으로 위치 좌표와 지도 범위를 찾
     5  말한 장소로 구간 이름으로 철도 선형 좌표와 지도 범위
V1  19 / 5   (같은 둘)
V2  19 / 5   (같은 둘)
V3  21 / 5 + 2 말한 키워드로 지도 범위와 지역, 충전기 유형으로 전기
                2 말한 키워드로 지도 범위와 지역으로 전기차 충전기를 조
V4  23       (구간 묶음 5 가 통째로 사라져 묶음이 하나만 남음)
V5  21 / 2 / 2
V6  21 / 5 / 2 / 2   구간 묶음의 앞토막이 "말한 구간으로 …" 로 바뀜
```

#### 타입 노드는 menu 문장에 안 나온다 — 확인했다

`function_for` 는 사슬의 실행 노드 description 과 시작 데이터 노드 이름만 쓴다.
타입 노드는 사슬에 못 들어가므로 이름을 무엇으로 붙이든 문장이 안 바뀐다.
문장에 보이는 "장소 이름으로" 는 `place_name` 의 이름이 아니라
`geocode_place` 의 description 문구다. **그래서 V4 처럼 `place_name` 을 넷으로
쪼개도 남아 있는 문장은 한 글자도 안 바뀐다.** 자수가 준 것은 경로가 준 탓이다.

#### 덤 — `geo.getRailwayLines` 를 railwayName 으로 눌렀다

```
railwayName="경부선"   HTTP 200   features 224건   3,405,330자
railwayName="호남선"   HTTP 200   features  79건   1,408,664자

최상위 칸   type("FeatureCollection") · features
features[0] type · properties · geometry(MultiLineString)
properties  AF_F_N · AF_T_N · RA_F_N · RA_T_N · F_NAME · T_NAME ·
            R_NAME_1 · R_NAME_2 · R_NAME_3 · AVG_DIST · AVG_TIME ·
            L_TRANS · L_RAPID · SPEED · L_TYPE · railCode
전문        tools/probe_out/geo.getRailwayLines.railwayName-<값>.json
```

`stationName="경부선"` 은 0건이었다(2026-08-23 실측). 같은 값을 `railwayName`
으로 넣으면 224건이다. **한 도구가 받는 두 칸이 서로 다른 이름 체계다** —
V4 가 `station_name` 과 `line_name` 을 가른 근거가 이것이다.

#### 예상과 어긋난 것

1. **경로 수가 늘어난 변형이 하나도 없다.** 타입을 쪼개면 경로가 늘 줄 알았는데
   일곱 다 V0(54)보다 적다. 가장 적은 것은 V5 의 39개로 **0.72배**다. 늘어난
   변형이 없으므로 "가장 많이 늘어난 변형" 은 없다. 이유는 단순하다 — 쪼개면
   `can_connect` 가 맞춰 볼 타입이 좁아져 이어지던 것이 끊긴다.
2. **V2 에서 가짜 넷 말고 넷이 더 죽는다.** `spoken_identifier` 를
   `district_code` 하나에만 매면 recipe_018~021(말한 식별자로 바로 상세를 무는
   경로 넷)이 통째로 사라진다. V1(셋 다 매는 것)에서는 안 사라진다. 발화가
   "충북 제1선거구 알려줘" 하나뿐이라는 근거로 좁혔을 때 같이 딸려 나가는 것이다.
3. **V4·V5 에서 039(구간 → CCTV)가 죽는다.** `get_railway_section` 이
   `section_name` 만 받는데 `spoken_place` 는 `place_label` 과 `station_name`
   에만 매여 있어 시작점이 없다. 철도 구간에서 출발하던 다섯(002 · 037~040)이
   한꺼번에 사라진다. V6 의 `spoken_section` 노드 하나가 그것을 되살린다.
4. **V4·V5 에서 버린 수가 6 에서 0 이 된다.** `crosses_groups` 에 걸리던 여섯이
   전부 철도 구간에서 출발하던 경로라, 경로 자체가 없어지면서 거를 것도 없어졌다.
   차단기가 좋아진 것이 아니라 차단할 대상이 사라진 것이다.
5. **V3 에서 앞토막 충돌 묶음이 2 에서 4 로 는다.** 새로 생긴 충전소 경로 넷이
   기존 문장과 앞 30자를 나눠 갖는다. 경로를 늘리면 앞토막도 같이 는다.
6. **최대 묶음은 거의 안 움직인다.** 19~23 사이다. `geocode_place` 로 시작하는
   묶음이 제일 큰데 어느 안도 그것을 안 건드린다.
7. **미리보기 자수는 실측보다 1자 적다.** V0 이 4,315자로 찍히는데 실제
   `_init/menu/menu.yaml` 은 4,316자다. 추정식이 덩어리 사이 빈 줄을 문장 수(48)
   만큼 세는데 실제 이어붙임은 47개다. NOTES 에 적힌 4,316 은 실측이라 어긋난
   것이 아니다. **표의 자수는 전부 미리보기 값이라 서로 견주는 데만 쓴다.**

#### 안 한 것

- 어느 안을 고를지 안 정했다. 고르는 것은 사람이 한다.
- `--write` 를 안 붙였다. `_init` 은 그대로다.
- 발화를 안 돌렸다. 이 표는 전부 경로 생성 결과지 발화 해석 결과가 아니다.
  앞토막 묶음이 실제로 판정을 가르는지는 재봐야 안다.
- `MIN_STEPS` · `MAX_STEPS` 를 안 건드렸다. 일곱 다 2 와 4 그대로다.


### 2026-08-23 (넷째) · 개편의 계기판 둘 · check_wiring · probe_shapes

**왜 만들었는가.** 개편에서 배선을 노드별에서 간선별로 다시 적는다. 그때 계속
부를 눈 둘이 없었다.

```
tools/check_wiring.py   경로와 배선이 맞는지 센다.  서버도 LLM 도 안 쓴다
tools/probe_shapes.py   도구 42개가 어떤 칸으로 답하는지 적는다
```

바로 위 「열린 과제」의 "경로와 배선이 맞는지 세는 검사가 없다" 와 인수인계
문서의 "output 모양을 모르는 것이 걸림돌" 을 각각 없애려는 것이다.
**둘 다 재기만 한다.** 온톨로지 · `STEP_OF` · recipe · menu · description 은
한 글자도 안 고쳤다.

#### check_wiring — 13개를 그대로 재현했다

```
  A  발화 인자를 버린다 (2개)
  recipe        첫 실행 노드                    도구
  recipe_012    search_ev_stations              ev.searchStations
  recipe_013    search_ev_chargers              ev.searchChargers

  B  앞 단계 결과를 안 쓴다 (11개)
  recipe        앞 노드 -> 노드                             도구
  recipe_022    geocode_place -> search_admin_boundaries    adminBoundary.searchBoundaries
  recipe_024    geocode_place -> get_vworld_boundaries      vworld.getAdministrativeBoundaries
  recipe_026    geocode_place -> get_railway_lines          geo.getRailwayLines
  recipe_028    geocode_place -> search_assembly_districts  election.searchAssemblyDistricts
  recipe_030    geocode_place -> search_assembly_pledge_districts
  recipe_032    geocode_place -> search_local_pledge_summaries
  recipe_034    geocode_place -> search_population_statistics
  recipe_037    get_railway_section -> search_admin_boundaries
  recipe_038    get_railway_section -> get_vworld_boundaries
  recipe_040    get_railway_section -> get_railway_lines
  recipe_041    web_search -> web_fetch                     web.fetch

  recipe 48개 · 배선이 다 있는 것 34개 · A 2개 · B 11개 · 합계 13개
```

손으로 센 것(위 「배선이 온톨로지의 선언을 반만 따른다」)과 recipe 번호까지
같다. **규칙을 숫자에 맞추려고 고친 적 없다.** 한 번에 나왔다.

`STEP_OF` · `unwired` · `SPOKEN_VALUE` · `PREVIOUS_STEP` 을 `step_service` 에서
import 한다. 표를 옮겨 적으면 배선을 고칠 때 두 곳이 조용히 어긋나고 그러면
세는 숫자를 못 믿는다. 테스트는 안 두었다 — `tools/` 는 재는 도구이고 이 숫자가
안 나오면 그 자리에서 드러난다.

#### 응답 모양 사전 — 도구 42개

`tools/probe_shapes.py`. 목록은 `curl -s http://localhost:3000/api/tools` 로
받았다 (**이 장비는 3.0초**. Windows 21.1초). 42개를 다 누르는 데 **10.6초**.

```
  도구                                        결과       건수   좌표 칸                 bbox 칸               코드 칸                          최상위 칸                                     0건 문구 · 비고
  rail.getSectionGeometry                     오류       -      -                       -                     -                                -                                             구간 '오송역'을(를) 찾을 수 없습니다.
  road.getCctv                                데이터     82     [0].centerLon=수 …      -                     -                                cctvId cctvName centerLon centerLat cctvUrl…  -
  geo.geocode                                 데이터     ?      location=[a,b]          bbox=[[a,b],[c,d]]    -                                location bbox address                         -
  geo.getRailwayLines                         데이터     31     -                       -                     -                                type features                                 -
  adminBoundary.getDatasetInfo                데이터     ?      -                       bbox=없음             -                                source datasetVersion crs format layers bbo…  -
  adminBoundary.searchBoundaries              0건        0      -                       -                     -                                type features count totalMatches query warn…  행정구역 DB 데이터가 없거나 PostGIS…
  adminBoundary.findBoundaryByPoint           0건        0      -                       -                     -                                type features count totalMatches query warn…  행정구역 DB 데이터가 없거나 PostGIS…
  population.getDatasetInfo                   데이터     ?      -                       -                     id=문자                          id name source sourcePage sourceUrl format …  -
  population.searchStatistics                 데이터     4      -                       bbox=없음             level=sigungu items[0].code=…    type features count totalMatches referenceD…  -
  population.getAgeProfile                    인자 없음  -      -                       -                     -                                -                                             level, code
  population.getTrend                         인자 없음  -      -                       -                     -                                -                                             level, code
  vworld.getDatasetInfo                       데이터     ?      -                       -                     -                                source reference crs requestLimit adminBoun…  -
  vworld.getAdministrativeBoundaries          데이터     4      -                       bbox=[a,b,c,d]       items[0].sig_cd=43111 …          type features items count totalFetched data…  -
  knowledge.query                             0건        0      -                       -                     -                                -                                             문구 없음
  knowledge.listDocs                          0건        0      -                       -                     -                                -                                             문구 없음
  knowledge.deleteDoc                         인자 없음  -      -                       -                     -                                -                                             문서를 지움
  bim.listModels                              0건        0      -                       -                     -                                -                                             문구 없음
  bim.updateModel                             인자 없음  -      -                       -                     -                                -                                             모델을 고침
  bim.deleteModel                             인자 없음  -      -                       -                     -                                -                                             모델을 지움
  dem.getInfo                                 오류       -      -                       -                     -                                -                                             layer.json not found: /app/data/dem…
  ev.getDatasetInfo                           데이터     ?      -                       bbox=[a,b,c,d]       id=문자                          id name source format status stationCount c…  -
  ev.searchStations                           데이터     500    items[0].centerLon=수 … -                     items[0].id=ev_station_PW012325  count totalMatches pageNo limit aggregation…  -
  ev.getStation                               인자 없음  -      -                       -                     -                                -                                             statId
  ev.searchChargers                           데이터     100    items[0].centerLon=수 … -                     items[0].id=ev_station_PW012325  count totalMatches pageNo limit aggregation…  -
  election.getDatasetInfo                     데이터     ?      -                       bbox=[[a,b],[c,d]]    datasetId=문자                   datasetId name electionDate districtCount c…  -
  election.searchDistricts                    데이터     4      -                       bbox=[[a,b],[c,d]]    items[0].code=2431401 …          type features items count totalMatches limi…  -
  election.getDistrict                        데이터     1      -                       bbox=[[a,b],[c,d]]    features[0].id=문자              type features item count totalMatches datas…  -
  election.findDistrictByPoint                데이터     1      -                       bbox=[[a,b],[c,d]]    features[0].id=문자              type features item count dataset query bbox   -
  election.getAssemblyDistrictDatasetInfo     데이터     ?      -                       bbox=[[a,b],[c,d]]    datasetId=문자                   datasetId name electionDate featureCount di…  -
  election.searchAssemblyDistricts            데이터     5      -                       bbox=[[a,b],[c,d]]    items[0].code=4311101 …          type features items count totalMatches limi…  -
  election.getAssemblyDistrict                데이터     1      -                       bbox=[[a,b],[c,d]]    features[0].id=1141001           type features item count totalMatches datas…  -
  election.findAssemblyDistrictByPoint        데이터     1      -                       bbox=[[a,b],[c,d]]    items[0].code=4311301 …          type features items count totalMatches limi…  -
  election.getAssemblyPledgeDatasetInfo       데이터     ?      -                       bbox=[[a,b],[c,d]]    datasetId=문자                   datasetId name electionDate featureCount di…  -
  election.searchAssemblyPledgeDistricts      데이터     20     -                       bbox=[[a,b],[c,d]]    items[0].code=4313001 …          type features items count totalMatches limi…  -
  election.getAssemblyPledgeDistrict          데이터     1      -                       bbox=[[a,b],[c,d]]    features[0].id=문자              type features item count totalMatches datas…  -
  election.findAssemblyPledgeDistrictByPoint  데이터     1      -                       bbox=[[a,b],[c,d]]    items[0].code=4311301 …          type features items count totalMatches limi…  -
  election.getLocalPledgeSummaryDatasetInfo   데이터     ?      -                       -                     datasetId=문자                   datasetId name electionDate featureCount si…  2026 지방선거 시도별 공약 요약 DB…
  election.searchLocalPledgeSummaries         0건        0      -                       -                     -                                type features items count totalMatches data…  2026 지방선거 시도별 공약 요약 데이…
  election.getLocalPledgeSummary              0건        0      -                       -                     -                                status message dataset query                  조건에 맞는 2026 지방선거 시도별…
  election.findLocalPledgeSummaryByPoint      0건        0      -                       -                     -                                status message dataset query                  해당 좌표를 포함하는 시도 공약 요…
  web.search                                  권한 없음  -      -                       -                     -                                -                                             MCP tool 'web-search/web.search' i…
  web.fetch                                   인자 없음  -      -                       -                     -                                -                                             url
  ─────────────────────────────────────────────────────────────────────────────
  도구 42개                                   데이터 24 · 0건 8 · 오류 2 · 인자 없음 7 · 권한 없음 1
```

**표에 값을 적지 않았다.** 어제 vworld 응답이 405KB 였다. `geojson` ·
`coordinates` · `features` 는 길이만 적고 모양은 `[a,b]` 처럼 자리로만 적는다.
값이 필요하면 `tools/probe_out/<도구>.<라벨>.json` 을 본다.

**건수는 `count`(한 쪽)지 `totalMatches`(전체)가 아니다.** 세는 순서가
`probe_tools` 와 같아서 그렇다. 크게 갈리는 줄이 셋이다.

```
ev.searchStations                       count 500  totalMatches 93353   limit 500
ev.searchChargers                       count 100  totalMatches 93353   limit 100
election.searchAssemblyPledgeDistricts  count 20   totalMatches 206     limit 20
```

**`ARGUMENT_RULES` 를 그대로 쓰면 표가 비었다.** 모든 `query` 에 "오송역" 이
들어가 행정구역 · 인구 · 선거 도구가 전부 0건이 된다. 0건이면 칸 이름을 볼 것이
없어 사전이 안 만들어진다. `ARGUMENT_OVERRIDES` 를 따로 두었고 **어젯밤 실측으로
확인된 값만** 적었다.

```
adminBoundary.searchBoundaries          query="청주시"
vworld.getAdministrativeBoundaries      query="청주시"      오송역 0건 · 청주시 4건
population.searchStatistics             query="청주시"      오송역 0건 · 청주시 4건
geo.getRailwayLines                     stationName="오송역"  31건 (인자 없이 3683건)
election.searchDistricts                query="청주"        4건 (인자 없이 254건)
election.searchAssemblyDistricts        query="청주"        5건
election.searchAssemblyPledgeDistricts  query="철도"        totalMatches 206건
```

#### ★ 배선 재료 — 개편에서 간선별 배선을 적을 때 이 표를 본다

**무엇을 어떻게 이을지는 안 적는다. 재료만 적는다.**

**좌표를 내놓는 도구 넷.**

```
geo.geocode         location=[lon,lat]                       최상위
road.getCctv        [0].centerLon=수  [0].centerLat=수        최상위가 배열
ev.searchStations   items[0].centerLon=수  items[0].centerLat=수
ev.searchChargers   items[0].centerLon=수  items[0].centerLat=수
```

좌표를 한 칸에 배열로 주는 것은 `geo.geocode` 하나뿐이다. 나머지 셋은 경도와
위도를 두 칸으로 쪼개 준다. **`ev` 둘에는 `location` 칸이 있는데 값이 빈
문자열이다** (`items[0].location=""`). 이름만 보고 `$prev.location` 을 적으면
좌표가 아니라 `""` 가 간다.

**bbox 모양이 두 가지로 갈린다.** 17개가 `bbox` 를 내놓고 그중 둘이 다르다.

```
[[a,b],[c,d]]   geo.geocode · election 열하나                  13개
[a,b,c,d]       vworld.getAdministrativeBoundaries
                ev.getDatasetInfo                              2개
없음(null)       adminBoundary.getDatasetInfo
                population.searchStatistics                    2개
```

`STEP_OF` 의 `POINT_RADIUS_TO_BBOX` 는 중심 좌표와 반경으로 bbox 를 만드는
것이라 이 두 모양과는 다른 자리다. **어제 이 갈림을 안 재고 배선을 적었다.**

**코드를 내놓는 도구 19개.** 그중 다음 도구의 입력으로 쓸 수 있어 보이는 것.

```
population.searchStatistics         level=sigungu  items[0].code=43113
                                    items[0].id=2026-06-30-sigungu-43113
vworld.getAdministrativeBoundaries  items[0].sig_cd=43111  items[0].datasetId=sigungu
                                    items[0].id=lt_c_adsigg_info.66
election.searchDistricts            items[0].code=2431401
election.searchAssemblyDistricts    items[0].code=4311101   features[0].id=4311101
election.searchAssemblyPledgeDistricts  items[0].code=4313001
election.findAssemblyDistrictByPoint    items[0].code=4311301
election.findAssemblyPledgeDistrictByPoint  items[0].code=4311301
election.getAssemblyDistrict        features[0].id=1141001
ev.searchStations · ev.searchChargers   items[0].id=ev_station_PW012325
```

**코드를 받는 도구 12개.** `inputSchema` 의 어느 칸인가.

```
adminBoundary.searchBoundaries          code(선택)
population.searchStatistics             level(선택)  code(선택)
population.getAgeProfile                level(필수)  code(필수)
population.getTrend                     level(필수)  code(필수)
ev.getStation                           statId(필수)
ev.searchChargers                       statId(선택)
election.searchDistricts                code(선택)
election.getDistrict                    code(선택)
election.searchAssemblyDistricts        code(선택)
election.getAssemblyDistrict            code(선택)
election.searchAssemblyPledgeDistricts  code(선택)
election.getAssemblyPledgeDistrict      code(선택)
```

**0건인데 문구가 없는 도구 셋.** 화면에 "찾지 못했습니다" 만 나간다.

```
knowledge.query      응답이 [] 하나. warning 자리가 없다
knowledge.listDocs   응답이 [] 하나
bim.listModels       응답이 [] 하나
```

#### 어제와 어긋난 것

**`ev` 데이터가 들어왔다.** 어제(08-22 15:50)는 `dataset.status: "empty"` ·
`stationCount: 0` · 시도 17개가 전부 `missingRegionCodes` 였다. 지금은 이렇다.

```
status          syncing
stationCount    93353
chargerCount    492390
infoSyncedAt    2026-08-22T13:36:10Z
```

그래서 `ev.searchStations` 가 0건에서 500건(전체 93353)이 됐다. **위 A 부류
(recipe_012 · 013)가 인자를 버리고 전국을 검색하는 것이 이제 실제로 결과를
낸다.** 어제는 0건이라 티가 안 났다.

**행정구역만 여전히 비어 있다.** `query="청주시"` 로 눌러도 0건이고 warning 이
어제와 같다 ("행정구역 DB 데이터가 없거나 PostGIS 연결을 사용할 수 없습니다").
인구 통계는 채워졌는데(4건) 행정구역은 아니다. `STEP_OF` 아래 주석 2번
(`get_age_profile` 계열이 코드 필드 이름을 모른다)의 전제가 그대로 남아 있다.

다만 **`population.searchStatistics` 자신이 `level` 과 `code` 를 내놓는다**
(`level=sigungu` · `items[0].code=43113`). 주석 2번이 그 코드의 출처로 본 것은
`adminBoundary.findBoundaryByPoint` 였다. **어느 쪽을 쓸지는 안 정했다. 사람이
정할 일이다.**

**`election.getLocalPledgeSummary` 둘을 0건으로 고쳐 세었다.** 어제 표는 이 둘을
"데이터 1" 로 세었고 그 자리에서 "그대로 믿으면 안 된다" 고 적었다. 응답에
`status: "not_found"` 가 실려 있어 이번에는 세는 규칙에 넣었다. 그래서 데이터
24 · 0건 8 이다 (어제 셈법으로는 데이터 26 · 0건 6).

**`road.getCctv` 84 -> 82.** 실시간 CCTV라 부를 때마다 다르다.

**`rail.getSectionGeometry` 는 어제와 같이 오류다.** `"오송역"` 은 역 이름이지
구간명이 아니다. 구간명을 하나도 못 얻어 이번에도 못 눌렀다.

> **정정 (2026-08-23 여섯째).** 위는 틀렸다. `import_railways.py` 로
> `rail.sections` 2,243건을 넣은 뒤 `"오송역"` 은 성공한다. 옛 오류는 인자 탓이
> 아니라 테이블이 없었기 때문이다. 구간이 「출발역~도착역」이라는 이해도 틀렸다 —
> `"대전~김천"` 은 NOT_FOUND 이고 `"서울역"` · `"경부선"` 이 같은 한 건을 돌려준다.
> 근거는 맨 앞 2026-08-23 (여섯째) 항목.

#### 안 한 것

```
철도 적재(import_railways.py)   남의 DB 에 쓰는 일이라 안 돌렸다
bim.updateModel · bim.deleteModel · knowledge.deleteDoc
                                REFUSED_TOOLS 그대로. 앞으로도 안 누른다
온톨로지 · STEP_OF · recipe · menu · description   한 글자도 안 고쳤다
```

### 2026-08-23 (밤) · 개편 직전 기준선 · recipe 48 · qwen3:32b · 각 10회 · 묶음 셋

**왜 쟀는가.** 개편 직전 기준선이다. recipe 번호가 통째로 바뀌므로 아래 표는
개편 뒤 그대로는 무효가 된다. 그래도 전후를 맞대볼 수 있는 유일한 근거다.
`git tag before-ontology-rework` 가 이 지점이다.

원자료는 `/tmp/before.txt`(묶음 1) · `/tmp/block_2.txt` · `/tmp/block_3.txt`.

#### 표 — 묶음 셋을 나란히. 합치지 않는다

```
#  발화                            묶음1     묶음2     묶음3    틀렸을 때 나온 것
1  오송역 위치 보여줘              10/10     10/10     10/10
2  오송역 좌표 알려줘              10/10     10/10     10/10
3  오송역 CCTV 보여줘              10/10     10/10     10/10
4  청주시 인구 구성 알려줘         10/10     10/10     10/10
5  오송역 근처 충전소 찾아줘        0/10      0/10      0/10    {036}      10회
6  국회의원 선거구 찾아줘           0/10      0/10      0/10    {007,008}   9회
                                                               14개 후보    1회
7  전기차 충전소 데이터 검색해줘    0/10      0/10      0/10    {012,013}  10회
8  철도 안전 문서 찾아줘            0/10      0/10      0/10    {006}      10회
9  충북 제1선거구 알려줘           10/10     10/10     10/10
                                  ─────     ─────     ─────
                                  50/90     50/90     50/90
                                   56%       56%       56%
```

#### 묶음 간 차이 — 없다. 한 발화도 안 흔들렸다

```
세 파일이 바이트 단위로 같다.
  289200511d6fd0a361c0eb7e965b295b  /tmp/before.txt
  289200511d6fd0a361c0eb7e965b295b  /tmp/block_2.txt
  289200511d6fd0a361c0eb7e965b295b  /tmp/block_3.txt
```

적중 · 축 · 인자 · 후보 수 · status 가 전부 같다. 발화 6 의 1/10 짜리
14개-후보 이탈까지 세 묶음에 똑같이 한 번씩 나왔다.

**「한 묶음 안에서는 완벽히 같고 묶음 간에는 흔들린다」는 이번에 재현되지
않았다.** 흔들림의 자리를 찾다가 이유를 하나 봤다.

```
llm_engine/ollama.py:89   "options": {"temperature": 0, "seed": 0, ...}
```

온도도 seed 도 고정이다. 같은 프롬프트면 같은 답이 나오는 것이 정상이다.
그러면 예전에 묶음 간에 흔들린 것은 프롬프트 · 온톨로지 · 모델 중 무엇이
그 사이에 바뀌었기 때문이라는 뜻이 된다. **어느 쪽인지는 여기서 정하지
않는다.** 지금 적을 수 있는 사실은 "이 커밋 · 이 모델에서는 세 묶음이
완전히 같다" 까지다.

기준선으로서는 이쪽이 낫다. 개편 뒤 숫자가 달라지면 그것은 흔들림이 아니라
개편의 효과다.

#### 묶음 1 에서 이미 읽히는 것 넷

**축이 완벽하다.** `given · want · about · argument` 가 89/90 동일하다.
흔들린 1회는 발화 6 의 축 셋이 전부 `null` 인 것이다.
**인자 추출은 더 다룰 문제가 아니다.**

**3번이 돌아왔다.** "오송역 CCTV 보여줘" 10/10. 예전에 10/10 -> 0/5 로
뒤집혔던 발화다. 039 간섭이 이 묶음에는 없었다. 한 묶음이므로 굳히지 않는다.
(이번에 세 묶음 모두 10/10 이었다는 것은 위 표에 있다.)

**6번의 1/10 이 재현됐다.** `{007,008}` 9회 + 14개 1회. 지난번과 같은 비율이다.

**★ 실패 넷 중 셋이 같은 모양이다.**

```
5번  {036}
7번  {012, 013}
6번  {007, 008}
```

전부 **비슷한 문장 두 개가 비기는 것**이다. 축은 세 경우 다 정확했다.
타입이 굵어서가 아니라 **menu 문장이 안 갈린다.**
**타입만 쪼개면 이 셋은 안 고쳐진다는 뜻이다.**

#### 안 눌러본 도구 넷을 눌렀다 (여덟 번)

Gateway 를 직접 불렀다(`POST /api/tools/execute`). `tools/probe_tools.py` 는
`ARGUMENT_RULES` 가 "오송역" 고정이라 인자를 못 바꾼다.
응답 전문은 `tools/probe_out/<도구>.<인자>.json` (gitignore).

```
도구                                인자        건수   최상위 칸
rail.getSectionGeometry             대전~김천   오류   error{code,message}
rail.getSectionGeometry             오송역      오류   error{code,message}
geo.getRailwayLines                 오송역      31     type · features
geo.getRailwayLines                 경부선      0      type · features
vworld.getAdministrativeBoundaries  청주시      4      type · features · items · count ·
                                                       totalFetched · dataset · query · bbox
vworld.getAdministrativeBoundaries  오송역      0      위와 같음 (bbox 칸이 사라진다)
population.searchStatistics         청주시      4      type · features · count · totalMatches ·
                                                       referenceDate · level · metric ·
                                                       metricLabel · unit · bbox · items · query
population.searchStatistics         오송역      0      위와 같음
```

**`location` 칸은 넷 어디에도 없다.**

**`rail.getSectionGeometry` 는 이 장비에서 어떤 인자로도 응답이 없다.**
"대전~김천" · "오송역" 둘 다 NOT_FOUND 다. 인자를 잘못 준 것이 아닌지 확인했다.

```
KRRI_ASAP/ASAP-mcp/app/tools/rail.py       DB -> 없으면 MOCK_RAIL_DATA -> 없으면 NOT_FOUND
KRRI_ASAP/ASAP-mcp/app/core/data.py:42     MOCK_RAIL_DATA 키는 "부산역" · "서울역" 둘뿐
postgis                                    relation "rail.sections" does not exist
```

`sectionName="서울역"` 도 눌러봤다. 역시 NOT_FOUND 다
(`tools/probe_out/rail.getSectionGeometry.서울역.json`). DB 테이블이 없고
컨테이너의 MOCK 도 비어 있다. 소스가 선언하는 성공 시 모양은 이렇다.

```
{ sectionId, geometry{type,coordinates}, bbox }      location 이 없다
```

**recipe_039 의 `$prev.location` 은 이 도구로 풀리지 않는다.** 칸이 없어서
못 푸는 것이 첫째고, 지금은 응답 자체가 안 나오는 것이 그보다 앞선다.
(gitignore 런타임 데이터 누락과 같은 부류로 보인다. 여기서 고치지 않는다.)

**행정구역 코드는 둘 다 준다.** `findBoundaryByPoint` 를 안 거쳐도 된다.

```
population.searchStatistics   최상위 level="sigungu"
                              items[].code="43113"     ← level · code 를 그대로 준다
                              items[].sidoName · sigunguName · name
vworld.getAdministrativeBoundaries
                              items[].properties.sig_cd="43111"
                              dataset.id="sigungu" · items[].datasetId="sigungu"
                              items[].properties.full_nm="충청북도 청주시 상당구"
```

recipe_045 · 046 · 047 의 `level` · `code` 배선을 적을 근거가 여기 있다.
필드 이름은 population 쪽이 그대로 `level` · `code`, vworld 쪽이
`datasetId` · `properties.sig_cd` 다. **어느 쪽을 쓸지는 사람이 정한다.**

**bbox 모양.**

```
vworld       최상위 bbox = [127.27565046, ...]  평평한 넷
             items[].bbox = [127.44526401, ...] 평평한 넷
             0건이면 최상위 bbox 칸이 아예 없어진다
population   includeBbox:true 인데 최상위 bbox · items[].bbox 가 전부 null
geo.getRailwayLines  bbox 칸이 없다. geometry.coordinates(MultiLineString) 뿐
```

**0건일 때 warning 문구.**

```
geo.getRailwayLines "경부선"                 {"type":"FeatureCollection","features":[]}
vworld... "오송역"                           count:0 · features:[] · items:[]
population... "오송역"                       count:0 · totalMatches:0 · items:[]
```

셋 다 **warning 문구가 없다.** 조용히 0건이다. `query` 칸에 보낸 값이
그대로 돌아오는 것이 vworld · population 의 유일한 단서다.
`geo.getRailwayLines` 는 그것도 없다.

**"경부선" 0건은 인자 이름 때문이다.** `stationName` 으로 보냈다.
노선명 칸은 `railwayName` 이 따로 있다(`ASAP-mcp/main.py:277`). 도구 잘못이
아니라 이번 호출이 그렇게 물어본 것이다. 적어두고 넘어간다.

geojson · coordinates 값은 적지 않았다. 칸 이름과 길이만 봤다.


### 2026-08-23 · 어미가 판정을 가름 · recipe 48 · qwen3:32b · 각 1회

같은 뜻인데 갈렸다. 화면에서 한 번씩 쳐본 것이다.

```
  발화                        결과
  오송역 행정구역 보여줘      CLARIFY {recipe_022, recipe_023}
  오송역 행정구역 조회        SELECT recipe_023. 끝까지 실행되어 0건
```

**각 1회뿐이다.** 반복해서 잰 것이 아니므로 비율로 읽으면 안 된다.

옛 기록 「끝점은 동사가 정한다」(2026-08-15 · qwen2.5:7b · recipe 6)와 같은
성질이다. 그때는 `"…봐줘"` 9/10 · `"…분석해줘"` 10/10 이었다. recipe 48 ·
qwen3:32b 에서도 어미가 여전히 판정을 가른다. 옛 기록은 조건이 달라 그대로 둔다.

관찰 하나. 이번 CLARIFY 는 후보 둘(`recipe_022` · `recipe_023`)이 모두 행정구역
이고 reason 도 행정구역이라 서로 어긋나지 않았다. 예전에 후보와 reason 이
어긋나던 사례와는 발화가 다르므로 개선이라고 단정하지 않는다. 그 사례는 NOTES 에
기록이 없어 맞대볼 것도 없다.

### 2026-08-22 (넷째) · 답 문구 넷을 고침 · 실행 없이 trace 로 확인

화면 실측 넷을 고쳤다. 서버를 안 띄우고 실측 응답 모양으로 trace 를 손으로
만들어 `compose_workflow_answer` 를 직접 불러 문자열만 봤다.

```
  발화                          before                                after
  오송역 행정구역 알려줘        오송역 행정구역을 조회했습니다.       찾지 못했습니다.
                                (features 0개인데 성공 문구)          + "2. … 0건"
  오송시 cctv 보여줘            오송시 → 128.5944, 34.8225            오송시 → 경상남도 남해군
                                                                      삼동면 동천리 (128.5944, …)
  오송역 근처 충전소 찾아줘     {"count": 100, "totalMatches": 21…    100건 (전체 2,195건)
  철도 안전 문서 찾아줘         500 원문 + localhost:3000 URL         실패 · 이 도구를 쓸 권한이
                                + Gateway 응답 본문                   없습니다
  대전~김천 구간 CCTV 보여줘    s2 단계 필수 입력값이 비어 있습…      실패 · 필수 입력값이 비어
                                (단계 목록 없음)                      있습니다: minLon, minLat, …
  오송역 좌표 알려줘            오송역 → 127.3277, 36.6200            + 주소가 붙음. 나머지 그대로
```

**제일 나쁜 것은 두 번째였다.** `"오송시"` 는 없는 지명인데 `geo.geocode` 가
경상남도 남해 근처(128.5944, 34.8225)를 찍고 `road.getCctv` 가 거기 CCTV 2건을
진짜로 받아왔다. 두 호출 다 성공이라 어느 칸을 봐도 실패가 아니다. 좌표만
보이면 사람이 못 알아채므로 **주소를 반드시 답에 넣는다** — `geo.geocode` 응답에
`address` 가 이미 있다 (`asap_probe_out/geo.geocode.osong.json`).

**오류 분류를 문자열이 아니라 항목의 구조로 갈랐다.** vendor 의 한국어 문구
(`_friendly_workflow_tool_error` 셋)에 기대면 저쪽이 갱신될 때 조용히 깨진다.
trace 항목이 가진 것으로 가른다.

```
  error_detail 있음   도구 호출 자체가 터진 것. 원문을 화면에 안 낸다.
                      vendor 가 이미 logger.error 로 남겼다
  error_detail 없음   우리가 입력을 못 채운 것. vendor 문장에 필드 이름밖에
                      없어 그대로 보여도 안전하다. "{step_id} 단계 " 접두만 뗀다
```

영어 조각을 문자열로 보는 곳은 한 줄뿐이다 — `error_detail` 안의
`"is not applied for this user"`. 실측 : `web-search/web.search` 를 부르면
Gateway 가 500 과 함께 `"MCP tool 'web-search/web.search' is not applied for
this user."` 를 돌려준다. 권한이 없는 것은 잠깐 터진 것과 다르고 사용자가
알아야 할 사실이라 따로 뺐다.

**판정을 `errors` 만으로 못 한다.** Gateway 는 실패를 `200` + `{"error": {...}}`
로도 돌려주고(`asap_probe_out/geo.geocode.english_notfound.json`), `mcp_client`
가 예외를 안 올리므로 vendor 는 그것을 성공한 호출로 보고 `answer_draft` 에
성공 문구를 적는다. 그래서 판정이 `workflow_answer` 안에 있다.

**`_preview()` 를 지웠다.** JSON 을 화면에 내보내던 유일한 자리였다. 모르는
결과는 이제 최상위 칸 이름만 여섯 개까지 보여준다 (`칸: type · coordinates`).

**남은 것 하나.** 도구 이름이 18자를 넘으면 `TOOL_COLUMN` ljust 가 안 벌어져
요약과 붙는다 (`adminBoundary.findBoundaryByPoint0건`). 고치기 전에도 같았고
(`…findBoundaryByPoint{"features": …`) 이번 범위가 아니라 안 건드렸다.

#### 정정 (같은 날, 커밋 전 마무리)

**「남은 것 하나」의 ljust 문제를 이 커밋에서 함께 고쳤다.** `TOOL_COLUMN` 을
넓히지 않고 `TOOL_GAP = 2` 를 두어 최소 간격을 보장한다 —
`tool.ljust(TOOL_COLUMN - TOOL_GAP)` 뒤에 두 칸을 다시 붙인다. `TOOL_COLUMN` 을
가장 긴 이름(42자 · `election.findAssemblyPledgeDistrictByPoint`)에 맞추면 짧은
이름 쪽이 스무 칸 넘게 빈다. 짧은 이름의 정렬은 한 칸도 안 달라진다
(11 + 5 + 2 = 18).

```
before  adminBoundary.findBoundaryByPoint0건
after   adminBoundary.findBoundaryByPoint  0건
```

**`_verdict` 가 마지막 항목만 보던 것을 항목 전부를 훑는 것으로 바꿨다.**
`_execute_generic_mcp_workflow` 는 중단할 때 대개 trace 에 아무것도 안 남긴다 —
조기 반환 열한 곳 중 아홉이 append 없이 `_failed_workflow_result` 로 나가고,
그중 일곱은 trace 마지막이 "성공한 앞 단계" 다. 그러면 `errors` 가 차 있는데도
마지막 항목은 성공으로 읽힌다.

지금 그 구멍이 안 터지는 것은 우연이다. `_resolve_reference` 가 예외 대신 None
을 돌려주고, `step_service.plan` 이 중심 좌표가 없으면 어댑터를 안 걸고, vendor
의 허용 검사가 web-search 를 통과시킨다. 셋 다 다른 파일이라 판정이 거기 기대면
안 된다. 그래서 둘을 함께 했다.

```
compose_workflow_answer(intent, trace, *, failed=False)
    failed 는 키워드 전용이고 기본이 거짓이라 vendor 호출부는 안 바뀐다.
    execute_service._answer 만 failed=True 로 부른다 (errors 가 있을 때만 도달)
_verdict          항목 전부를 훑는다. 어느 항목이든 error 칸이나 200 오류면 ERROR
건수 0 판정        마지막 항목으로 남겼다. 발화에 답하는 것은 마지막이다
```

전부를 훑는 또 하나의 이유 : 중간 단계가 `200` + `{"error": ...}` 로 돌아오고
다음 도구가 그 칸을 optional 로 받으면 vendor 는 끝까지 돌고 `errors` 도 비어
있는데 결과는 쓰레기다. 마지막만 보면 EMPTY 나 SUCCESS 가 된다.

#### 정정 2 (2026-08-23 화면 확인)

**"오송시" 가 찍은 곳은 남해가 아니라 거제다.** 위 문단들에 「경상남도 남해」로
적힌 것은 좌표만 보고 우리가 짐작해 쓴 것이다. 좌표(128.5944, 34.8225)는 맞았고
주소가 틀렸다. 실측은 이렇다.

```
오송시 → 경상남도 거제시 동부면 오송리 143-2   (128.5944, 34.8225)
```

**거제시에 실제로 「오송리」가 있다.** `geo.geocode` 가 헛짚은 것이 아니라 가장
비슷한 실제 지명을 정직하게 돌려준 것이다. 좌표도 주소도 CCTV 2건도 전부
사실이고, 어느 층에도 버그가 없는데 답이 틀렸다. **없는 지명("오송시")을 실재하는
다른 지명("오송리")과 갈라줄 어휘 층이 없다는 뜻이다.** 「미뤄둔 것 — 어휘 층」의
첫 실제 사례다.

주소를 답에 넣은 것은 그래서 더 필요했다. 버그가 없으므로 판정으로는 영영 못
잡고, 사람이 주소를 읽고 아는 수밖에 없다.

**저쪽 화면이 연속 공백을 접는다.** 답이 마크다운 번호 목록으로 렌더되면서
공백이 한 칸으로 줄어든다 (실측).

```
코드가 내는 것   1. geo.geocode       오송시 → …
화면에 뜨는 것   1. geo.geocode 오송시 → …
```

즉 `TOOL_COLUMN` 의 정렬은 저쪽 화면에서 원래부터 안 보이고 있었다. 그래도
`TOOL_GAP` 은 필요했다 — 공백이 0개면 접을 것이 없어 `…ByPoint0건` 이 한 낱말로
그대로 남는다. 2개가 1개로 접히면서 낱말이 갈린다. 코드는 그대로 둔다.

답 형식을 다시 볼 때는 `TOOL_COLUMN` 을 없애고 눈에 보이는 구분자를 쓰는 편이
낫다. 지금 폭 맞추기는 화면에서 아무 일도 안 하면서 상수 둘을 유지하게 한다.

### 2026-08-22 (셋째) · Gateway 도구 42개 훑기

`tools/probe_tools.py` 로 42개를 한 번씩 눌렀다. 배선을 어디에 적을지 정하려고
무엇이 실제로 데이터를 주는지 먼저 쟀다. 도구 목록은 이 장비에 `tools.json` 이
없어 `curl -s http://localhost:3000/api/tools -o /tmp/tools.json` 으로 받았다.

```
  도구                                      상태        건수    비고
  rail.getSectionGeometry                   실패        -       구간 '오송역'을(를) 찾을 수 없습니다.
  road.getCctv                              데이터      84
  geo.geocode                               데이터      1
  geo.getRailwayLines                       데이터      3683
  adminBoundary.getDatasetInfo              데이터      1
  adminBoundary.searchBoundaries            빈 결과     0       행정구역 DB 데이터가 없거나 PostGIS 연결을 사용할 수 없습니…
  adminBoundary.findBoundaryByPoint         빈 결과     0       행정구역 DB 데이터가 없거나 PostGIS 연결을 사용할 수 없습니…
  population.getDatasetInfo                 데이터      1
  population.searchStatistics               빈 결과     0       인구 통계 DB 데이터가 없거나 PostGIS 연결을 사용할 수 없습…
  population.getAgeProfile                  인자 없음   -       level, code
  population.getTrend                       인자 없음   -       level, code
  vworld.getDatasetInfo                     데이터      1
  vworld.getAdministrativeBoundaries        데이터      200
  knowledge.query                           빈 결과     0
  knowledge.listDocs                        빈 결과     0
  knowledge.deleteDoc                       인자 없음   -       문서를 지움
  bim.listModels                            빈 결과     0
  bim.updateModel                           인자 없음   -       모델을 고침
  bim.deleteModel                           인자 없음   -       모델을 지움
  dem.getInfo                               실패        -       layer.json not found: /app/data/dem/90m_GRS80/layer.json
  ev.getDatasetInfo                         데이터      1
  ev.searchStations                         빈 결과     0
  ev.getStation                             인자 없음   -       statId
  ev.searchChargers                         빈 결과     0
  election.getDatasetInfo                   데이터      1
  election.searchDistricts                  데이터      20
  election.getDistrict                      데이터      1
  election.findDistrictByPoint              데이터      1
  election.getAssemblyDistrictDatasetInfo   데이터      1
  election.searchAssemblyDistricts          데이터      20
  election.getAssemblyDistrict              데이터      1
  election.findAssemblyDistrictByPoint      데이터      1
  election.getAssemblyPledgeDatasetInfo     데이터      1
  election.searchAssemblyPledgeDistricts    데이터      20
  election.getAssemblyPledgeDistrict        데이터      1
  election.findAssemblyPledgeDistrictByPo…  데이터      1
  election.getLocalPledgeSummaryDatasetIn…  데이터      1       2026 지방선거 시도별 공약 요약 DB 데이터가 적재되지 않았습…
  election.searchLocalPledgeSummaries       빈 결과     0       2026 지방선거 시도별 공약 요약 데이터를 조회하지 못했습니다.
  election.getLocalPledgeSummary            데이터      1       조건에 맞는 2026 지방선거 시도별 공약 요약을 찾지 못했습니…
  election.findLocalPledgeSummaryByPoint    데이터      1       해당 좌표를 포함하는 시도 공약 요약을 찾지 못했습니다.
  web.search                                실패        -       MCP tool 'web-search/web.search' is not applied for this us…
  web.fetch                                 인자 없음   -       url
  ─────────────────────────────────────────────────────────────────────────────
  도구 42개                                 데이터 23 · 빈 결과 9 · 실패 3 · 인자 없음 7
```

앞서 42개가 전부 `"Tool name is required"` 로 실패했던 것은 본문 모양 탓이었고
`919b09a` 에서 `vendor/asap/mcp_client.py` 와 같은 모양(`tool` · `input` ·
`user_context` · `server_id`)으로 고쳤다. 이번에는 전부 응답이 왔다.

#### 「데이터」 23 을 그대로 믿으면 안 된다

건수만 보면 안 되는 줄이 넷이다.

`election.getLocalPledgeSummary` · `election.findLocalPledgeSummaryByPoint` 는
건수 1인데 `warning` 이 "찾지 못했습니다" 다. 껍데기 객체 하나를 세었을 뿐
알맹이는 없다. 같은 데이터셋의 `getLocalPledgeSummaryDatasetInfo` 도 "적재되지
않았습니다" 라고 답한다. 셋이 같은 말을 한다 — 2026 지방선거 공약 요약은
저쪽에 안 들어와 있다.

`getDatasetInfo` 계열 다섯은 건수 1이 곧 메타데이터 한 덩이다. 데이터가 있다는
뜻이 아니다. `ev.getDatasetInfo` 는 `status: "empty"`, `stationCount: 0`,
`missingRegionCodes` 에 시도 17개가 전부 들어 있다. `population.getDatasetInfo`
도 `status: "empty"`, `totalPopulation: 0` 이다.

`required` 가 없는 도구는 probe 가 인자 없이 불렀다. `election.searchDistricts`
셋의 20건은 `limit` 이 20이라 잘린 전체 목록이지 `query` 가 거른 결과가 아니다.
그래서 `query` 를 따로 눌러 확인했다 — `"청주"` 로 4건 · 5건, `"철도"` 로
206건이 나왔다. 거르기가 실제로 돈다.

#### 0건인 도구와 그 warning

```
adminBoundary.searchBoundaries      행정구역 DB 데이터가 없거나 PostGIS 연결을 사용할 수 없습니다.
adminBoundary.findBoundaryByPoint   행정구역 DB 데이터가 없거나 PostGIS 연결을 사용할 수 없습니다.
population.searchStatistics         인구 통계 DB 데이터가 없거나 PostGIS 연결을 사용할 수 없습니다.
election.searchLocalPledgeSummaries 2026 지방선거 시도별 공약 요약 데이터를 조회하지 못했습니다.
ev.searchStations                   (warning 없음. dataset.status = "empty")
ev.searchChargers                   (warning 없음. dataset.status = "empty")
knowledge.query                     (응답이 [] 하나. warning 자리가 없음)
knowledge.listDocs                  (응답이 [] 하나)
bim.listModels                      (응답이 [] 하나)
```

전부 저쪽 데이터다. 우리가 채울 수 없다.

`adminBoundary` 는 `getDatasetInfo` 가 sido · sigungu · emd 세 층 전부
`featureCount: 0` 이라고 답한다. PostGIS 는 붙어 있고 테이블만 비었다.
`knowledge` 는 `listDocs` 도 `[]` 라 질의가 틀린 것이 아니라 지식베이스에
문서가 하나도 없는 것이다. `ev` 는 시도 17개가 전부 `missingRegionCodes` 라
동기화가 한 번도 안 돌았다.

`MEMORY.md` 의 「KRRI_ASAP gitignore 데이터 누락」 과 같은 줄기다. clone 과
build 는 끝났는데 gitignore 로 빠진 런타임 데이터가 안 들어와 있다.

#### 「인자 없음」 일곱

probe 가 값을 지어내지 않아 안 부른 것들이다. 도구가 고장 났다는 뜻이 아니다.

```
population.getAgeProfile        level, code   행정구역 코드를 어디서 받을지 모름
population.getTrend             level, code   행정구역 코드를 어디서 받을지 모름
ev.getStation                   statId        충전소 번호를 지어낼 수 없음
web.fetch                       url           URL 을 지어낼 수 없음
knowledge.deleteDoc             filename      남의 문서를 지움. 일부러 안 부름
bim.updateModel                 -             남의 모델을 고침. 일부러 안 부름
bim.deleteModel                 -             남의 모델을 지움. 일부러 안 부름
```

앞의 넷은 `ARGUMENT_RULES` 에 값이 없어서고 뒤의 셋은 `REFUSED_TOOLS` 라서다.
뒤의 셋은 앞으로도 누르지 않는다.

#### 「실패」 셋

```
rail.getSectionGeometry   구간 '오송역'을(를) 찾을 수 없습니다.
dem.getInfo               layer.json not found: /app/data/dem/90m_GRS80/layer.json
web.search                MCP tool 'web-search/web.search' is not applied for this user.
```

`rail.getSectionGeometry` 는 도구가 아니라 인자 탓이다. `"오송역"` 은 역 이름이지
구간명이 아니다. 구간명 하나를 알면 다시 재야 한다. 배선(`get_railway_section`)은
이미 적혀 있고 `sectionName` 을 발화에서 받으므로 이대로 둔다.

> **정정 (2026-08-23 여섯째).** 위는 틀렸다. `import_railways.py` 로
> `rail.sections` 2,243건을 넣은 뒤 `"오송역"` 은 성공한다. 옛 오류는 인자 탓이
> 아니라 테이블이 없었기 때문이다. 구간이 「출발역~도착역」이라는 이해도 틀렸다 —
> `"대전~김천"` 은 NOT_FOUND 이고 `"서울역"` · `"경부선"` 이 같은 한 건을 돌려준다.
> 근거는 맨 앞 2026-08-23 (여섯째) 항목.

`dem.getInfo` 는 DEM 타일셋이 컨테이너 안에 없다. `MEMORY.md` 에 적어 둔 그
누락이다.

`web.search` 는 데이터 문제가 아니다. 우리 `USER_CONTEXT` 에 `web-search` 서버가
안 열려 있다. KRRI_ASAP 쪽 사용자 권한이라 우리가 못 연다.

#### `web.search` · `web.fetch` 는 서버가 다르다

42개 중 마흔은 `serverId` 가 `asap-mcp-core` 인데 이 둘만 `web-search` 다.
`STEP_OF` 가 `SERVER_ID` 를 통째로 쓰고 있어 그대로 적으면 Gateway 가 도구를
못 찾는다. `WEB_SERVER_ID` 를 따로 두었다.

#### 이번에 적은 배선 일곱

`STEP_OF` 가 14 -> 21 이 됐다. 전부 발화에서 온 말 하나만 받는 도구라 앞 단계가
필요 없다.

```
노드                              도구                                     상태
search_election_districts         election.searchDistricts                 데이터 254
search_assembly_districts         election.searchAssemblyDistricts         데이터 254
search_assembly_pledge_districts  election.searchAssemblyPledgeDistricts   데이터 476
search_local_pledge_summaries     election.searchLocalPledgeSummaries      0건
search_documents                  knowledge.query                          0건
web_search                        web.search                               권한 막힘
web_fetch                         web.fetch                                안 눌러봄
```

아래 넷도 적었다. **배선이 없는 것과 데이터가 없는 것은 다르다.** 적어 두면
저쪽에 데이터가 들어왔을 때 고칠 것 없이 그대로 돈다. 왜 지금 비었는지는
`STEP_OF` 의 각 줄 위 주석에 적었다.

#### 여전히 못 적는 것

`get_age_profile` · `get_population_trend` · `get_local_pledge_summary` 는
이번에도 못 적었다. `adminBoundary.findBoundaryByPoint` 가 0건이라 `features` 가
비어 있었고 행정구역 코드가 어느 필드로 오는지 볼 것이 없었다.
`getDatasetInfo` 가 `codeField` 로 `ctprvn_cd` · `SIG_CD` · `emd_cd` 를 말하지만
그것은 shapefile 컬럼 이름이지 응답 필드 이름이 아니다. 짐작으로 적지 않는다.

`get_election_district` · `get_assembly_district` · `get_assembly_pledge_district`
· `get_ev_station` 은 「적을 수 없었던 경로」 그대로다. 식별자 체계가 다르다.

#### 앞 단계가 없으면 그 칸을 안 보낸다

`recipe_012` · `013` 은 `spoken_keyword -> search_ev_stations` 인데 그 배선의
`center` 가 `$prev.location` 이라 첫 step 에서 `plan()` 이 `ValueError` 로
멈췄다. 같은 노드가 두 자리에 쓰이기 때문이다 — 키워드 뒤(012 · 013)에도 오고
geocode 뒤(035 · 036)에도 온다. 배선은 한 벌뿐이다.

`_filled` 이 `ValueError` 를 올리던 것을 「그 칸을 빼고 부른다」 로 바꿨다.

근거는 `ev.searchStations` 의 bbox 넷이 전부 optional 이라는 것이다. 없어도
도구가 돌고 전국을 검색한다. 값을 지어내는 것보다 안 보내는 것이 낫다.
`radiusMeters` 만 남은 input 으로 실제로 눌러 확인했다 — HTTP 200 이고
스키마에 없는 `radiusMeters` 도 거부하지 않는다.

`inputAdapter` 도 함께 뺀다. `point_radius_to_bbox` 는 `center` · `point` ·
`coordinate` · `coordinates` · `location` 중 하나를 찾고 못 찾으면
`ValueError("point_radius_to_bbox에는 center/location 좌표가 필요합니다")` 를
올린다. 걸 것이 없는데 걸면 옮겨 온 셈이라 `plan` 이 싣기 전에 본다.

`required` 인 칸이 `$prev` 를 쓰는데 앞 단계가 없으면 그때는 도구가 거부한다.
그건 배선이 틀린 것이지 선택의 문제가 아니라 여기서 가리지 않는다.

**조영곤님이 자리에 없는 동안 대신 정한 판단이다.** 되돌릴 때는 `_filled` 과
`plan` 의 `inputAdapter` 한 줄만 보면 된다. `_filled` 의 이력 절에도 적었다.

##### 전후

`step_service.unwired()` 가 빈 목록을 내는 recipe 를 셌다. LLM 을 안 부르는
코드 계산이다.

```
                          전    후
온전히 도는 것            24    34
반쪽                      10     7
하나도 못 부름            14     7
                          48    48
```

`plan()` 이 터지던 `recipe_012` · `013` 까지 세면 실제로 돌던 것은 22 였다.
`unwired()` 는 배선이 있는지만 보고 `$prev` 가 풀리는지는 안 본다.

늘어난 recipe 는 열둘이다.

```
006 007 008 009 010 014 041   이번에 적은 배선 일곱으로
012 013                       앞 단계 없을 때 규칙으로
028 030 032                   반쪽이던 것이 채워져서
```

##### 돈다고 답이 나오는 것은 아니다

34 중 도구가 실제로 데이터를 주는 것은 18이다. 나머지 16은 호출까지 가고
빈 결과나 권한 오류를 받는다. 저쪽 데이터가 채워지면 그대로 답이 나온다.

```
데이터가 나옴 18   001 002 003 005 007 008 009 024 025 026 027 028 029 030 031 038 039 040
빈 결과 14         004 010 011 012 013 014 022 023 032 033 034 035 036 037
권한 막힘 2        006 041
```

`007` · `008` · `009` 가 새로 데이터가 나오는 쪽에 들었다 —
`election.search*` 셋이 이번 배선의 수확이다.

### 2026-08-22 (이어서) · qwen3:8b 대 qwen3:32b · recipe 48 · 각 10회

```
발화                          8b        32b
1 오송역 위치 보여줘          0/10      10/10
2 오송역 좌표 알려줘          10/10     10/10
3 오송역 CCTV 보여줘          10/10     10/10
4 청주시 인구 구성 알려줘     0/10      10/10
5 오송역 근처 충전소 찾아줘   0/10      0/10
6 국회의원 선거구 찾아줘      0/10      0/10
7 전기차 충전소 데이터 검색   0/10      0/10
8 철도 안전 문서 찾아줘       0/10      0/10
9 충북 제1선거구 알려줘       0/10      10/10
                            20/90 22%  50/90 56%
```

`want` 축이 갈랐다. 8b 는 1번을 `map_extent`, 7번을 `dataset_info`, 9번을
`record_key` 로 썼고 셋 다 오판이다. 7번과 9번은 그 탓에 조회 후보가 0이 되어
LLM 목록(5개 · 22개)이 그대로 CLARIFY 로 나갔다. 32b 는 셋 다 맞게 썼다.
`given` 과 `about` 은 두 모델 다 안정적이었다 — 흔들린 것은 `want` 하나다.

`argument` 는 두 모델 다 9/9 정확했다. 10회 내내 흔들림이 없었고 조사도
요청하는 말도 안 섞였다. `place_in` 이 못 잡던 키워드와 식별자도 맞다.
인자 추출에는 8b 로 충분하다.

1번의 회귀는 모델 탓이었다. 앞 커밋에서 프롬프트가 6758자에서 7133자로
늘면서 8b 가 "위치" 를 `map_extent` 로 읽어 10/10 이 0/10 이 됐다.
32b 에서 `point` 로 돌아왔다. menu 문장을 고치지 않아도 됐다.

5번(충전소 대 충전기)이 이번 비교의 물음이었다. 32b 는 후보를 하나로
좁혔지만 틀린 쪽(036 충전기)을 골랐다. 조회 후보는 두 모델 다 2로 같다.
좁히기는 되고 고르기가 안 되는 것이라 원인은 menu 문장에 있다 —
`recipe_035` 문장 안에 "충전기 유형" 이 들어 있어 "충전소" 라는 말로는
두 문장이 안 갈린다.

6번은 발화가 모호한 것이 맞다. 32b 가 `{007, 008}` 로 CLARIFY 하는 것은
정직한 결과다. 8번은 기대값(`recipe_014`)이 잠정이라 GT 를 다시 봐야 한다.

### 2026-08-22 · 발화에서 인자를 LLM 이 뽑음 (argument) · recipe 48

`place_in` 정규식은 끝 글자가 역·시·군·구·읍·면·동·리 인 어절 하나만 뽑았다.
그래서 `"충북대 근처 CCTV"` 는 끝 글자가 안 맞아 못 잡고, `"국회의원 선거구
찾아줘"` 는 장소가 없어 `execute_service` 가 거기서 멈췄고, `"충북 제1선거구"`
같은 식별자는 뽑는 코드가 아예 없었다. 축 셋을 받는 그 자리에서 인자도 함께
받게 했다.

응답 스키마에 칸 하나(`argument`)를 더했다. **셋으로 나누지 않았다** — `given`
이 이미 그 값이 장소인지 키워드인지 식별자인지 말하므로, `place`/`keyword`/
`identifier` 를 따로 두면 `given` 과 어긋날 수 있고 프롬프트도 그만큼 길어진다.

```
채운 프롬프트   6758자 -> 7133자   (+375, 발화 "오송역 위치 보여줘" 기준)
프롬프트 원본   1763자 -> 2138자
```

`num_ctx` 는 32768 이라 여유가 있다. 위 「건드리기 전에 읽을 것」 표의 6758자는
이 변경 전 값이다.

**아직 LLM 으로 안 쟀다.** `argument` 가 실제로 어떻게 나오는지는
`python tools/check_resolve.py --runs 10` 으로 재야 한다. 축 표에 `argument`
칸을 더해뒀다. 이 커밋의 숫자는 프롬프트 길이와 테스트뿐이다.

#### `@place` 를 `@arg` 로 바꿨다

`STEP_OF` 의 표시 이름이다. 키워드를 받는 도구(`search_documents` ·
`search_election_districts`)도 같은 칸에 발화에서 온 값을 넣으므로 표시가
"장소" 를 뜻하면 안 된다. 배선 여섯(`geocode_place` · `get_railway_section` ·
`get_railway_lines` · `search_admin_boundaries` · `get_vworld_boundaries` ·
`search_population_statistics`)과 headline 열넷이 함께 바뀌었다.
**배선은 새로 안 적었다.** 남은 25개는 다음 커밋이다.

`place_in` 은 지웠다가 다시 넣을 값이 아니라 그대로 뒀다. LLM 이 `argument` 를
빠뜨리거나 흔들려도 장소 발화만은 여전히 돌아야 한다. 지금은 `argument` 가
null 일 때만 돈다.

#### 안내 문구를 `given` 으로 갈랐다

`NO_PLACE_ANSWER` 하나만 있어서 `"국회의원 선거구 찾아줘"` 에 장소를 대라고
답했다. `spoken_keyword` 는 무엇을 찾을지, `spoken_identifier` 는 이름이나
코드를 말해 달라고 한다. `given` 이 null 이거나 모르는 값이면 예전 문구다.

#### 재는 발화를 셋 늘렸다

7 `"전기차 충전소 데이터 검색해줘"` · 8 `"철도 안전 문서 찾아줘"` ·
9 `"충북 제1선거구 알려줘"`. **기대값은 잠정이다.** 셋 다 배선이 없어 실행까지
안 가고, 지금 보는 것은 축과 `argument` 가 맞게 나오는지뿐이다.

#### 안 고친 것

`"국회의원 선거구 찾아줘"` 의 `want` 가 `record_key` 로 나와 조회 후보가 0이
되는 것을 앞선 측정에서 봤다. 원인이 `want` 선택지 설계에 있고, 온톨로지
개편에서 식별자 타입이 쪼개지면 선택지가 통째로 달라진다. 지금 손대면 두 번
일한다.

recipe 012 · 013 은 `spoken_keyword -> search_ev_stations` 인데 그 배선의
input 이 `$prev.location` 이라 첫 step 에서 `plan()` 이 멈춘다. 이번 범위가
아니라(배선을 새로 안 적음) 그대로 뒀다. 다음 커밋에서 볼 것.

테스트 156 passed / 1 failed. 실패 하나는 `test_dense_graph_would_move_if_
overlap_removal_were_used` 로 이 장비의 graphviz 버전 차이다(제품 경로 아님).

### 2026-08-21 (셋째) · STEP_OF 배선 열둘 · recipe 48

바로 아래 기록에서 반쪽 실행을 `unwired()` 로 막아놓고 배선은 안 늘렸다.
이번에 그 표에 열두 줄을 채웠다.

```
recipe 48   온전히 도는 것       2 -> 24
            반쪽으로 남은 것    22 -> 10
            하나도 못 부르는 것 24 -> 14
```

늘어난 22개는 002 003 004 005 011 012 013 022 023 024 026 027 029 031 033
034 035 036 037 038 039 040 이다. **LLM 을 부르지 않은 코드 계산이다** —
`unwired()` 가 빈 목록을 내는 recipe 를 센 것이고, 도구를 실제로 눌러본 것은
아래 「눌러보지 않은 것」 에 적었다.

좌표를 받는 도구(`lon`/`lat`)와 이름을 받는 도구(`query`/`stationName`/
`sectionName`)만 적었다. geocode 가 내주는 `bbox` 는 한 변이 1km 라 그것을
그대로 넘기는 배선은 안 적었다 — 오송역 CCTV 가 그 bbox 로는 0건, 좌표
+ 15km 로는 83건이었다(어제 실측).

#### ev 두 개만 어댑터를 명시했다

`_apply_input_adapter` 는 대상 도구의 `required` 에 bbox 넷이 다 있을 때만
`point_radius_to_bbox` 를 저절로 건다. `road.getCctv` 는 넷이 전부 required 라
걸리고, `ev.searchStations` · `ev.searchChargers` 는 넷 다 optional 이라 안
걸린다. 그래서 `STEP_OF` 에 `adapter` 칸을 만들고 그 둘에만 적었다.
`step_service.plan()` 이 그 값을 step 의 `inputAdapter` 로 실어 보낸다.

#### 적을 수 없었던 경로

```
recipe 042  geocode -> 지점 행정구역 판별 -> 국회의원 지역구 상세
recipe 043  geocode -> 지점 행정구역 판별 -> 국회의원 전체 선거구 상세
recipe 044  geocode -> 지점 행정구역 판별 -> 국회의원 선거구 공약 상세
recipe 048  geocode -> 지점 행정구역 판별 -> 충전소 상세 조회
```

앞 단계 `adminBoundary.findBoundaryByPoint` 가 내놓는 것은 행정구역 코드다.
마지막 도구가 받는 것은 선거구 코드(`election.getDistrict` 의 `code`/`name`)와
충전소 번호(`ev.getStation` 의 `statId`)라 체계가 다르다. 앞 단계 결과에서
옮겨 적을 값이 없어 배선 자체를 적을 수가 없었다.

**온톨로지의 식별자 타입이 셋을 하나로 묶은 탓이다.** 행정구역 코드 · 선거구
코드 · 충전소 번호가 한 타입이라 경로 생성기가 이 넷을 말이 되는 경로로 봤다.
타입을 쪼개면 이 recipe 들이 애초에 안 만들어지고 문제가 사라진다.

**지금 고치지 않는다.** 타입을 쪼개면 경로 집합이 달라져 recipe 번호가 통째로
바뀐다. 2026-08-21 (이어서) 의 발화별 후보 표가 그 번호로 적혀 있어 통째로
무효가 된다. 시연이 끝난 뒤에 한다.

045 · 046 · 047 은 이것과 다르다. `population.getAgeProfile` ·
`population.getTrend` 가 받는 `level` · `code` 는 행정구역 코드가 맞다.
`findBoundaryByPoint` 가 그 코드를 어떤 필드 이름으로 내놓는지 안 눌러봐서
못 적었을 뿐이다. 눌러보면 적을 수 있다.

#### 눌러보지 않은 것

```
rail.getSectionGeometry              응답에 location 이 있는지 모름
geo.getRailwayLines                  stationName 으로 오송역이 걸리는지 모름
vworld.getAdministrativeBoundaries   query 에 "오송역" 같은 역 이름이 걸리는지 모름
population.searchStatistics          query 에 역 이름이 걸리는지 모름
```

recipe 039 (철도 구간 -> CCTV)가 `$prev.location` 을 쓴다. `rail.getSectionGeometry`
응답에 `location` 이 없으면 참조가 None 이 되고, 중심 좌표가 없으니 어댑터가
안 걸려 `road.getCctv` 의 필수 입력이 비었다는 실패로 끝난다. 틀린 답이 나가는
것이 아니라 실패 문구가 나가므로 그대로 뒀다.

#### 정정 (2026-08-23)

- **「온전히 도는 것 24」는 "도구를 다 부른다" 는 뜻일 뿐이다.** 배선이 다 있는
  34개 중 13개가 온톨로지의 선언과 어긋나게 돈다 (위 열린 과제 A · B).
- **왜 안 보였는지 셋.** `unwired()` 가 "`STEP_OF` 에 줄이 있느냐" 만 보고 "그
  줄이 이 경로에 맞느냐" 는 안 본다 / 도구들이 관대해서 오류가 안 난다
  (`ev.searchStations` 는 bbox 넷이 optional, `searchBoundaries` 는 query 가 안
  맞으면 그냥 0건) / 어긋난 recipe 대부분이 화면에서 한 번도 안 켜졌다.
- **당시 적용한 규칙이 원인이다.** 이 기록에 "좌표를 받는 도구와 이름을 받는
  도구만 적었다" 고 있다. 도구를 둘 중 하나로 분류해 적었는데 아홉 개는
  둘 다였다.
- **저쪽에는 EV 배선이 아예 없다.** `ASAP-orchestrator/plugins` 12개와
  `harnesses` 3개에 EV 관련이 하나도 없다. 저쪽 전체에서 `ev.searchStations` 가
  나오는 곳은 프롬프트 예시 한 줄뿐이고 (`generic_mcp_executor.py:55`) 거기에도
  좌표 경우만 있다. 키워드 경우는 Gemini 가 `inputSchema` 를 읽고 스스로 쓴다.

```
저쪽   배선을 안 적는다      될 때도 있고 안 될 때도 있다. 재현이 안 된다
우리   한 줄로 적었다        늘 같게 돈다. 그 한 줄이 틀리면 늘 틀린다
```

---

### 2026-08-21 (이어서) · 발화 해석에 축 조회를 넣음 · recipe 48

바로 아래 측정에서 다섯 발화가 전부 CLARIFY 가 됐고, 원인은 menu 문장의 공통
앞토막이었다. LLM 이 쓴 `reason` 은 맞았다. 발화는 읽었고 48문장 사이에서 못
고른 것이다. 그래서 그 `reason` 이 말한 것을 닫힌 목록의 객관식(축 셋)으로 받고,
그 값으로 온톨로지를 조회해 후보를 뽑게 했다. LLM 호출은 그대로 한 번이다.

**아래는 LLM 을 부르지 않은 코드 계산이다.** 축이 맞다고 놓고 조회만 돌린 것이라
LLM 이 그 축을 실제로 쓰는지는 아직 모른다.

```
발화                        (given, want, about)                        후보
오송역 위치 보여줘          spoken_place · point · -                     1  (001)
오송역 좌표 알려줘          spoken_place · point · -                     1  (001)
오송역 CCTV 보여줘          spoken_place · item_list · group_transport   5  (003 025 026 039 040)
청주시 인구 구성 알려줘     spoken_place · statistics · group_population 3  (034 046 047)
오송역 근처 충전소 찾아줘   spoken_place · item_list · group_ev          2  (035 036)
국회의원 선거구 찾아줘      spoken_keyword · - · group_election          4  (007 008 009 010)
```

48개가 1~5개로 준다. 2번 발화의 후보 15개(앞토막이 같은 것들)를 가르는 것은
`want` 다. 좌표를 내놓고 끝나는 recipe 는 001 하나뿐이라 나머지가 전부 빠진다.
6번의 넷은 여기서도 안 갈린다. 발화가 모호한 것이라 되묻는 것이 맞다.

**프롬프트가 5656자에서 6758자가 됐다** (menu 4316 · 축 목록 730 · 틀 1712).
`num_ctx` 는 8192 토큰이라 여유가 많지 않다. 선택지를 늘릴 때 다시 재야 한다.

#### 배선이 없는 recipe 를 부르고 있었다

`step_service.plan()` 이 `STEP_OF` 에 없는 노드를 조용히 건너뛴다. recipe_022
(geocode -> 행정구역 경계)를 고르면 geocode 만 부르고 "좌표를 조회했습니다" 라고
답한다. 반쪽 결과가 온전한 답처럼 나간다.

```
recipe 48   온전히 도는 것  2  (001 · 025)
            반쪽으로 도는 것 22
            하나도 못 부르는 것 24
```

`step_service.unwired()` 를 만들어 `execute_service.run()` 이 실행 전에 본다.
비어 있지 않으면 도구를 하나도 안 부르고 아직 안 붙은 기능의 이름을 답에 적는다.
**배선(`STEP_OF`)은 늘리지 않았다.** 이번에 한 것은 없는 기능을 있는 척하지
않게 막은 것뿐이다.

#### 다시 시도하지 말 것

- **축 조회에서 `is-a` 를 타고 올라가기** — `want` 를 조상까지 올리면 상위 타입
  하나가 하위 전부를 끌어와 좁히는 뜻이 없어진다. `given` 도 같다. `record_key`
  로 물으면 `spoken_identifier` 로 시작하는 일곱 개가 딸려온다.
- **축이 셋 다 null 일 때 조회 결과를 쓰기** — `candidates()` 는 그때 48개를
  전부 낸다. 그것을 후보로 삼으면 영역 밖 발화의 NO_MATCH 가 48개 CLARIFY 로
  뒤집힌다. 고를 근거가 하나도 없다는 뜻이므로 LLM 이 쓴 것을 그대로 둔다.

---

### 2026-08-21 · MCP 도구 39개 · recipe 48 · qwen3:8b

Gateway catalog 의 도구를 온톨로지에 넣고 발화 해석이 몇 개까지 버티는지 쟀다.

**환경** : qwen3:8b · `num_ctx` 8192 · `reason_max_length` 200 ·
노드 58 (타입 11 · 대상 5 · 시작 데이터 3 · 기능 39) · recipe 48 ·
menu.yaml 4316자 (`MENU_BUDGET` 6000) · `MAX_STEPS` 4 (안 건드림).

**다섯 발화 모두 CLARIFY. SELECT 0개.** recipe 2개일 때는 1·2번이 SELECT 였다.

```
발화                        status    recipe_id   후보 수
오송역 CCTV 보여줘          CLARIFY   -            2   (025 · 039)
오송역 좌표 알려줘          CLARIFY   -           15
청주시 인구 구성 알려줘     CLARIFY   -            5
오송역 근처 충전소 찾아줘   CLARIFY   -            5
국회의원 선거구 찾아줘      CLARIFY   -           20
```

**무너지는 원인은 menu 문장의 공통 앞토막이다.** 48문장 중 중복은 0개인데
앞 30자가 같은 것이 23개다("말한 장소로 장소 이름으로 위치 좌표와 지도 범위를
찾…"). 구별하는 말이 전부 문장 꼬리에 있다.

```
앞 14자 같음   23개  말한 장소로 장소 이름으로…
                5개  말한 장소로 구간 이름으로…
앞 30자 같음   23개 · 5개  (위와 같음)
앞 60자 같음    2개  4단 인구 recipe 둘(046 · 047)
문장 길이       최소 29 · 평균 59 · 최대 100자
```

**후보가 앞토막으로 뭉친다.** 2번(좌표) 후보 15개는 전부 `geocode_place` 로
시작하는 것들이고, 끝점이 좌표인 recipe_001 과 좌표에서 더 나아간 14개를 못
가른다. **앞토막 문제가 다시 왔다** — `6bf034a` 때는 2단 recipe 가 3단의
앞토막이라 35/35 가 5/35 가 됐고, 이번에는 3단·4단이 다 같은 2단으로 시작한다.

5번(선거구) 후보 20개는 네 데이터셋(지역구 · 전체 선거구 · 선거구 공약 ·
지방선거 공약)을 못 가른 것이다. 사람도 발화만으로는 못 가른다 — 발화가 모호한
것이지 온톨로지가 틀린 것이 아니다.

4번(충전소)에 recipe_025(CCTV 조회)가 섞였다. 앞토막이 같아서다 — 꼬리의
"CCTV 목록" 과 "전기차 충전소" 가 안 갈렸다.

**뒤집어 보면 대상(about) 차단은 제대로 일했다.** 54개 중 6개를 버렸고 남은
48개에 어긋나는 것이 없다. 버린 6개는 전부 철도 구간(교통)에서 선거 · 인구 ·
전기차로 건너뛰는 것이다.

#### 이번에 알아낸 구조 제약

- **`hasInput` 이 하나도 없는 도구는 recipe 가 될 수 없다.** `getDatasetInfo`
  아홉과 `knowledge.listDocs` · `bim.listModels`, 열한 개다. `can_connect` 가
  받는 것이 없는 노드를 누구 뒤에도 안 세우고 `start_ids` 는 실행 노드를
  빼므로 첫 칸에도 못 온다. 온톨로지에는 있고 menu 에는 없다.
- **다음 도구가 받는 타입을 내놓게 하면 경로가 폭발한다.** 좌표를 내놓는 것을
  둘(`geocode_place` · `get_railway_section`)로, 식별자를 내놓는 것을
  하나(`find_admin_boundary_by_point`)로 묶어 48개에 그쳤다. 인구 통계 조회가
  설명대로 지도 범위를 내놓게 했으면 그 뒤에 지도 범위를 받는 열 개가 붙는다.

#### 다시 시도하지 말 것

- **대상별로 타입을 나누기** — "선거구 목록" · "충전소 목록" 처럼 나눠도 경로
  생성은 그대로다. 무엇에 관한 목록인지는 `about` 이 이미 말한다. 타입만
  대상 수만큼 곱해진다.
- **계열(`election` · `population` …)마다 대상 노드 만들기** — `road.getCctv`
  와 `rail.getSectionGeometry` 를 가르면 "철도 구간을 찾아 그 범위의 CCTV 를
  본다" 가 `crosses_groups` 에 걸려 죽는다. 범용 도구(`geo.geocode` 등 아홉)에
  `about` 을 붙이면 지금 유일하게 돌던 경로도 함께 죽는다.

---

### 2026-08-19 · qwen2.5:7b -> qwen3:8b

`llm_engine/ollama.py` 의 기본 모델을 옮기고, `reason` 을 짧게 묶었다.

**옛 측정이 재현되지 않았다.** 1번 발화가 10/10 -> 0/10. 저장소는 안 바뀌었다
(프롬프트 2026-08-11 이후, `_init` 2026-08-15 `6bf034a` 이후 불변, 모델 digest
동일, 작업 트리가 커밋 상태와 일치). 원인을 저장소 안에서 못 찾았다.

> **정정 (2026-08-24 · 「열넷째」).** 위 문장은 **지우지 않는다.** 그때 못 찾은
> 것은 사실이다. 다만 그 뒤로 원인 후보 하나가 지워졌다.
>
> **모델 로드는 원인이 아니다.** 한 세션 안에서 여섯 묶음(각 10회)을 재고
> 그중 셋은 묶음마다 모델을 강제로 내렸다 올렸다. **여섯이 바이트 단위로 같다.**
> 모델을 내렸다 올리는 것으로는 답이 안 바뀐다.
>
> **`keep_alive` 는 저장소 안에 있다.** `llm_engine/ollama.py:88` 에 `"2h"` 로
> 박혀 있다. 환경변수가 아니다. 「저장소 안에는 원인이 없다」를 "런타임 설정은
> 저장소 밖" 이라는 뜻으로 읽었다면 그 부분은 틀렸다 — 다만 이 값 자체는
> 위에서 보듯 원인이 아니었다.
>
> **아직 좁히지 못한 것.** ollama 프로세스 재시작 · uvicorn 재시작 · ollama
> 버전 변화(0.32.14 -> 0.32.15) · 그리고 그 두 묶음 사이에 실제로 커밋이
> 달랐을 가능성. 자세한 것은 「열넷째」에 있다.

**모델** (recipe 6 · 각 10회 · 1번 · 2번 · 3번 발화)

```
qwen2.5:7b     0/10  10/10  10/10
qwen3:4b      10/10   0/10   0/10   2번을 부순다
qwen3:14b       —      —      —     183초. urlopen timeout(180)을 넘긴다
qwen3:8b      10/10  10/10  10/10   <- 골랐다
```

**reason 이 무한 반복에 빠진다 — 시간만이 아니라 답을 망친다.**
qwen3:8b 가 3번 발화에서 recipe id 나열을 되풀이한다
("recipe_002와 recipe_007, recipe_003, recipe_007, recipe_008가" 반복).
상한이 없으면 5/5 가 202초에 끊겼다. 근거에 recipe_003 을 적어놓고 후보에서
뺐다 — 목록을 끝맺지 못한 채 후보 필드를 반쪽만 채운다.

`maxLength` 를 200 · 400 · 600 으로 바꿔 재니 셋 다 정확히 상한까지 채웠고 답이
똑같았다(모두 오답). 상한이 근거를 잘라 판정을 깎는 것이 아니라 어차피 무너진
근거를 자를 뿐이다. 그래서 제일 짧은 200 을 쓴다 (30초 · 50초 · 70초).

**reason 에 Recipe ID 를 못 쓰게 하니 루프와 판정이 함께 풀렸다.**
되풀이할 재료가 사라진다. reason 이 200자(상한 도달) -> 44~60자가 되고 응답도
30초 -> 18초로 줄었다. 3번 발화가 0/10 -> 10/10.
"하나 찾아도 멈추지 말고 Menu 끝까지 훑어라" 를 더한 변형도 15/15 로 같아서
넣지 않았다. ID 금지만으로 충분하다.

**확정** (recipe 6 · qwen3:8b · 1~3번 10회 · 4번 5회) — 10/10 · 10/10 · 10/10 · 5/5

#### 다시 시도하지 말 것

- **menu 를 steps 목록으로 펼치기** — 10/30 -> 0/30. 끝점을 마지막 항목으로
  만들어 "부재 판정" 을 "위치 판정" 으로 바꾸려 했는데 반대로 갔다. 목록으로
  펼치니 공통 앞토막(대상 · 프레임 추출 · 분석)이 글자 그대로 반복돼 도드라지고,
  대상 매칭이 끝점 매칭을 덮었다. 멀쩡하던 2번이 10/10 -> 0/10.
  크기는 문제가 아니었다 (steps 형식 1073자 · `MENU_BUDGET` 6000).
  `test_registry` 가 적어둔 거절 사유(context 초과)는 function 을 **남긴 채**
  steps 를 더할 때의 얘기다. 교체는 크기로는 안전하다.
- **menu 를 대상별로 묶기** — 모델에 따라 반대로 작용한다. 007(승강장 Excel)이
  궤도 recipe 셋에 가로막혀 형제와 떨어져 있으면 밀린다.
  qwen2.5:7b · recipe 8 에서는 끝에 두면 `{002,003}` 10/10 (007 빠짐),
  승강장 묶음 안에 두면 `{002,003,007}` 10/10 이었다.
  그런데 qwen3:8b 에서는 묶으면 오히려 `{002,007}` -> `{002}` 로 나빠졌다.
- **`repeat_penalty` 1.2** — 반복은 멎지만 판정까지 깎여 3번 발화가 후보 둘에서
  하나로 좁아진다.

#### 환경

이 기계는 CPU 로만 추론한다. **5.9 tok/s.** `nvidia-smi` 없음 · `/api/ps` 의
`size_vram` 0. RAM 16.4GB. 모델이 여럿 올라가 있으면(`keep_alive` 2h) RAM 을
넘겨 스와핑이 난다 — 실험 사이에 안 쓰는 모델을 내려야 시간이 안 튄다.

---

### 2026-08-15 · qwen2.5:7b (`4a55c44` ~ `e8fcdc2`)

따로 적지 않았으면 `_init` recipe 6 기준.
**이 구역은 전부 qwen2.5:7b 로 잰 것이다. 지금 모델의 성질이 아니다.**

**형식은 발화와 menu 의 표기가 글자 그대로 겹칠 때만 갈린다.**

```
"워드"          0/10   menu 는 "Word"
"Word 문서로"   9/10
"Word로"        9/10   오답이 {004,005} 로 바뀐다 — 형식이 아니라 길이가 갈린다
```

menu 에 "Word(워드)" 로 두 표기를 함께 담는 것은 안 들었다 (0/10).

**숫자 + "번" 이 붙으면 무너진다.**

```
"3번 승강장에 …"      0/5
"동대구역 승강장에 …"  5/5
"지금 승강장에 …"      4/5
"승강장에 …"          5/5
```

역명도 시각도 통한다. "3번" 만 다르다 — recipe 번호로 읽히는 것으로 보인다.

**끝점은 동사가 정한다.** `"…봐줘"` 9/10 (10에 1번 문서까지 끌고 간다) ·
`"…분석해줘"` 10/10.

**NO_MATCH 는 영역으로 갈린다. 어휘 겹침이 아니다.**

```
"오늘 지하철 요금 알려줘"          5/5   영역 밖
"CCTV 화면이 뿌옇지 않은지 봐줘"    0/5   영역 안이라 뭐라도 집는다
"화면이 뿌옇게 나오는데 확인해줘"   0/5   겹치는 어휘가 없는데도 마찬가지
```

**앞토막 recipe 는 판정을 무너뜨린다** (`e8fcdc2`).

```
recipe 6개                          35/35
+ 앞토막 둘 (데이터 → 프레임 추출)     5/35
+ 프롬프트를 절차형으로 전환           25/45
```

앞토막이 다른 모든 recipe 에 포개져 "어디서 끝나는가" 를 못 가른다. 프롬프트로
절반은 회복되지만 나머지를 못 채우고, 조일수록 끝점을 모호하게 말하는 발화가
대신 무너진다 (`"…봐줘"` 9/10 -> 0/5).

**프롬프트를 절차로 나누면 판정이 나아진다.** 규칙 셋을 동시에 저울질하는 대신
순서 있는 단계로 쪼갠 것. 샘플 셋이 0/10 -> 10/10 이 됐고 시연에 안 쓰는 발화도
9/10 -> 5/5 로 올랐다. 다만 recipe 8개에서도 2번 발화를 못 채워 되돌렸다.
recipe 가 늘어나면 다시 꺼낼 카드다.

**확정** (각 10회 · NO_MATCH 는 5회) — 10/10 · 10/10 · 10/10 · 5/5

**샘플 목록(`sample_picker.SAMPLES`)에서 무엇이 빠졌나.**
형식을 지정해 하나로 좁히는 발화가 없다. `"Word로 만들어줘"` 가 9/10 이라 뺐다.
그래서 이 목록만으로는 "형식을 밝히면 하나가 된다" 를 못 보여준다. 필요하면
발표자가 직접 `"궤도 균열 확인한 거 Word로 만들어줘"` 를 친다 — 10에 1번은
`{004, 005}` 로 갈린다.
`recipe_005` · `recipe_006` 은 이 목록으로는 한 번도 안 켜진다.
