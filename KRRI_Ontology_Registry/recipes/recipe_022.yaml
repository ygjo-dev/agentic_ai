steps:

  - node: point

  - node: find_assembly_pledge_district_by_point

example: 이 위치 선거구가 내건 공약 쭉 보여줘

execution:

  spoken_needed: false

  context_needs:
    point: {from: context.selectedLocation, fields: {lon: lon, lat: lat}}

  workflow:

    - id: s1
      node: find_assembly_pledge_district_by_point
      server_id: asap-mcp-core
      tool: election.findAssemblyPledgeDistrictByPoint
      input:
        lon: {from: context.point.lon}
        lat: {from: context.point.lat}
