steps:

  - node: place_name

  - node: geocode_place

example: 천안역 위치 위경도로 짚어줘

execution:

  spoken_needed: true

  context_needs: {}

  workflow:

    - id: s1
      node: geocode_place
      server_id: asap-mcp-core
      tool: geo.geocode
      input:
        query: {from: spoken.argument}
