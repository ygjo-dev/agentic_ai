steps:

  - node: keyword

  - node: search_election_districts

example: 대전 지역구 목록 뽑아줘

execution:

  spoken_needed: true

  context_needs: {}

  workflow:

    - id: s1
      node: search_election_districts
      server_id: asap-mcp-core
      tool: election.searchDistricts
      input:
        query: {from: spoken.argument}
