# Menu

Static Workflow 가 실행할 수 있는 Recipe 목록이다.
사용자 요청은 여기 적힌 Recipe 중 하나로만 해석한다.

이 문서는 사람이 읽으라고 둔 사본이다.
LLM 에 Context 로 전달되는 것은 [menu.yaml](menu.yaml) 뿐이므로, 한쪽을 고치면 다른 쪽도 같이 고친다.

실행 순서(steps)는 menu 에 없다. 각 Recipe 의 실행 정의는 [../recipes/](../recipes/)`<recipe_id>.yaml` 에 있다.

## 목차

| Recipe ID | 기능 |
| --- | --- |
| recipe_001 | 승강장에 설치된 CCTV 영상을 불러와 영상에서 분석용 이미지 프레임을 추출하고 이미지에서 승강장 혼잡도를 분석한다. |
| recipe_002 | 승강장에 설치된 CCTV 영상을 불러와 영상에서 분석용 이미지 프레임을 추출하고 이미지에서 궤도 균열을 검출한다. |
| recipe_003 | 검측차가 촬영한 궤도 이미지를 불러와 이미지에서 궤도 균열을 검출한다. |
| recipe_004 | 관측 장비에서 기상 관측값을 불러와 기상 관측값에서 결빙 위험도를 분석한다. |
| recipe_005 | 관측 장비에서 기상 관측값을 불러와 기상 관측값에서 강풍 위험도를 분석한다. |
| recipe_006 | 승강장에 설치된 CCTV 영상을 불러와 영상에서 분석용 이미지 프레임을 추출하고 이미지에서 승강장 혼잡도를 분석하고 분석 결과를 Word 보고서로 생성한다. |
| recipe_007 | 승강장에 설치된 CCTV 영상을 불러와 영상에서 분석용 이미지 프레임을 추출하고 이미지에서 승강장 혼잡도를 분석하고 분석 결과를 PPT 보고서로 생성한다. |
| recipe_008 | 승강장에 설치된 CCTV 영상을 불러와 영상에서 분석용 이미지 프레임을 추출하고 이미지에서 궤도 균열을 검출하고 분석 결과를 Word 보고서로 생성한다. |
| recipe_009 | 승강장에 설치된 CCTV 영상을 불러와 영상에서 분석용 이미지 프레임을 추출하고 이미지에서 궤도 균열을 검출하고 분석 결과를 PPT 보고서로 생성한다. |
| recipe_010 | 검측차가 촬영한 궤도 이미지를 불러와 이미지에서 궤도 균열을 검출하고 분석 결과를 Word 보고서로 생성한다. |
| recipe_011 | 검측차가 촬영한 궤도 이미지를 불러와 이미지에서 궤도 균열을 검출하고 분석 결과를 PPT 보고서로 생성한다. |
| recipe_012 | 관측 장비에서 기상 관측값을 불러와 기상 관측값에서 결빙 위험도를 분석하고 분석 결과를 Word 보고서로 생성한다. |
| recipe_013 | 관측 장비에서 기상 관측값을 불러와 기상 관측값에서 결빙 위험도를 분석하고 분석 결과를 PPT 보고서로 생성한다. |
| recipe_014 | 관측 장비에서 기상 관측값을 불러와 기상 관측값에서 강풍 위험도를 분석하고 분석 결과를 Word 보고서로 생성한다. |
| recipe_015 | 관측 장비에서 기상 관측값을 불러와 기상 관측값에서 강풍 위험도를 분석하고 분석 결과를 PPT 보고서로 생성한다. |

---

# Recipe 001

## 기능

승강장에 설치된 CCTV 영상을 불러와 영상에서 분석용 이미지 프레임을 추출하고 이미지에서 승강장 혼잡도를 분석한다.

---

# Recipe 002

## 기능

승강장에 설치된 CCTV 영상을 불러와 영상에서 분석용 이미지 프레임을 추출하고 이미지에서 궤도 균열을 검출한다.

---

# Recipe 003

## 기능

검측차가 촬영한 궤도 이미지를 불러와 이미지에서 궤도 균열을 검출한다.

---

# Recipe 004

## 기능

관측 장비에서 기상 관측값을 불러와 기상 관측값에서 결빙 위험도를 분석한다.

---

# Recipe 005

## 기능

관측 장비에서 기상 관측값을 불러와 기상 관측값에서 강풍 위험도를 분석한다.

---

# Recipe 006

## 기능

승강장에 설치된 CCTV 영상을 불러와 영상에서 분석용 이미지 프레임을 추출하고 이미지에서 승강장 혼잡도를 분석하고 분석 결과를 Word 보고서로 생성한다.

---

# Recipe 007

## 기능

승강장에 설치된 CCTV 영상을 불러와 영상에서 분석용 이미지 프레임을 추출하고 이미지에서 승강장 혼잡도를 분석하고 분석 결과를 PPT 보고서로 생성한다.

---

# Recipe 008

## 기능

승강장에 설치된 CCTV 영상을 불러와 영상에서 분석용 이미지 프레임을 추출하고 이미지에서 궤도 균열을 검출하고 분석 결과를 Word 보고서로 생성한다.

---

# Recipe 009

## 기능

승강장에 설치된 CCTV 영상을 불러와 영상에서 분석용 이미지 프레임을 추출하고 이미지에서 궤도 균열을 검출하고 분석 결과를 PPT 보고서로 생성한다.

---

# Recipe 010

## 기능

검측차가 촬영한 궤도 이미지를 불러와 이미지에서 궤도 균열을 검출하고 분석 결과를 Word 보고서로 생성한다.

---

# Recipe 011

## 기능

검측차가 촬영한 궤도 이미지를 불러와 이미지에서 궤도 균열을 검출하고 분석 결과를 PPT 보고서로 생성한다.

---

# Recipe 012

## 기능

관측 장비에서 기상 관측값을 불러와 기상 관측값에서 결빙 위험도를 분석하고 분석 결과를 Word 보고서로 생성한다.

---

# Recipe 013

## 기능

관측 장비에서 기상 관측값을 불러와 기상 관측값에서 결빙 위험도를 분석하고 분석 결과를 PPT 보고서로 생성한다.

---

# Recipe 014

## 기능

관측 장비에서 기상 관측값을 불러와 기상 관측값에서 강풍 위험도를 분석하고 분석 결과를 Word 보고서로 생성한다.

---

# Recipe 015

## 기능

관측 장비에서 기상 관측값을 불러와 기상 관측값에서 강풍 위험도를 분석하고 분석 결과를 PPT 보고서로 생성한다.
