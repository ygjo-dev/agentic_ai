# Python 3.12 기준
#
# LLM 호출(llm_engine/ollama.py)
#   ollama serve
#   ollama pull qwen3:14b
#
# 설치
#   pip install -r requirements.txt

# --- 런타임 ---
PyYAML>=6.0.3          # 온톨로지 / Recipe / Policy YAML 파싱
fastapi>=0.100.0       # Backend API (backend/main.py)
uvicorn>=0.24.0        # FastAPI 서버 실행
streamlit>=1.60.0      # 데모 UI (frontend/main.py)
requests>=2.31.0       # Frontend에서 Backend 호출
shapely>=2.0.0         # 동 경계 ∩ 도달 범위 교집합 넓이 (execution/district_overlap)
python-dotenv>=1.0.0   # .env 로드. 진입점에서만 부른다

# --- 개발 / 테스트 ---
pytest>=9.1.1          # tests/ · 실행은 python -m pytest (저장소 루트가 sys.path 에 필요)
httpx>=0.27.0          # fastapi TestClient (tests/api_contract)
