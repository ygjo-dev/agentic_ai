# Python 3.12 기준
#
# LLM 호출(llm_engine/ollama.py)은 표준 라이브러리 urllib 만 사용하므로
# 별도의 HTTP 클라이언트 의존성이 없다.
# 대신 Ollama 데몬이 실행 중이어야 한다.
#   ollama serve
#   ollama pull qwen3:14b
#
# 설치
#   pip install -r requirements.txt

# --- 런타임 ---
PyYAML>=6.0.3          # 온톨로지 / Recipe / Policy YAML 파싱
fastapi>=0.100.0       # Backend API (backend/main.py)
uvicorn>=0.24.0        # FastAPI 서버 실행
streamlit>=1.60.0      # 데모 UI (frontend/main.py)
requests>=2.31.0       # Frontend에서 Backend 호출

# --- 개발 / 테스트 ---
pytest>=9.1.1          # tests/test_static_workflow.py
