# Python 3.12 기준
#
# LLM 호출(common/llm/ollama.py)은 표준 라이브러리 urllib 만 사용하므로
# 별도의 HTTP 클라이언트 의존성이 없다.
# 대신 Ollama 데몬이 실행 중이어야 한다.
#   ollama serve
#   ollama pull qwen3:14b
#
# 설치
#   pip install -r requirements.txt

# --- 런타임 ---
PyYAML>=6.0.3          # 온톨로지 / Recipe / Policy YAML 파싱
streamlit>=1.60.0      # 데모 UI (streamlit/main.py)

# --- 개발 / 테스트 ---
pytest>=9.1.1          # tests/test_static_workflow.py
